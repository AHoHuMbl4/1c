# Plans.md — пилот harness: кластер Э3 (выбор сущности и величины)

Purpose: закрыть главную открытую проблему продукта — недетерминированный и подстроенный
под базу выбор сущности (пп. 3, 10, 12 TARGET.md). Источник задач — `work/doc-audit/DEV_PLAN.md §Э3`;
этот файл — рабочий реестр цикла plan → work → review, правды о контракте здесь нет
(контракт — `TARGET.md`, статус — `docs/TARGET_STATUS.md`).

Сквозное правило DoD: замер «до» снимается ПЕРЕД первой правкой, приёмка 58 вопросов —
пачками и дважды, `REGRESSION_BASE1` — после ПОСЛЕДНЕЙ правки захода, не после какой-нибудь.

| Task | Содержание | DoD | Depends | Status |
|------|-----------|-----|---------|--------|
| 1 | `[lane:gate]` `[tdd:skip:sql-measured-by-acceptance]` `F243`: в двух запросах выбора сущности `ORDER BY` без разделителя равенства — добавить детерминированный разделитель | Число ничьих в первой восьмёрке до/после — прямым запросом (методика аудита); после правки недетерминированных ничьих 0; приёмка 58×2 не хуже базовой | - | cc:完了 [ffe97c6] (частично: код и оба замера ничьих сняты — до 3–5 ничьих и срез на ничье у 6 из 7 вопросов, после 0 совпавших ключей; приёмка 58×2 НЕ снята — ждёт пункта 2 списка владельца; правка не выкачена) |
| 2 | `[lane:gate]` `[tdd:skip:needs-serenedb-docs-first]` `F246`: понятия вопроса режутся константой «6 групп × 6 синонимов» — отсечку в пути правильности заменить отбором в базе (сначала `search_docs`, при сомнении агент serenedb-native) | Константы 6×6 в пути правильности нет; отбор ведёт индекс/запрос; приёмка 58×2 не хуже; в диффе нет новой подобранной константы (гейт check-diff молчит) | 1, 8-12 | cc:TODO (переоценить после Ф5: на поверхности сущностей отсечка означает другое) |
| 3 | `[lane:gate]` `[tdd:skip:index-side-work]` `№17`: до модели доходит 6–13 % перечня сущностей — отбор кандидатов обязан вести индекс, а не бюджет промта | На четырёх замерных базах аудита (1575/1617/1645/591 сущностей) доля релевантных кандидатов в выборке ≥ прежней при контексте, НЕ растущем с размером базы (замер числом); приёмка 58×2 не хуже | 1, 8-12 | cc:TODO (закрывается задачами 8-12 по существу — переоценить после Ф5) |
| 8 | `[lane:gate]` `[tdd:skip:sql-measured-by-bench]` **Ф1** [`PLAN_ENTITY_SURFACE.md`](work/entity-choice/PLAN_ENTITY_SURFACE.md): `search_entity_card` — одна строка на сущность (метка, синонимы, величины, отличительные термы, родитель, текст карточки), одним SQL внутри движка, база `ut_test` | 1 502 строки; покрытие каждого поля названо числом (ожидаемо `aliases` 697); ноль строк наружу; существующие таблицы не тронуты (откат — один `DROP`) | 1 | cc:完了 (`entity_card_build.sql`; 1502 строки: метка 1502, синонимы 697, описание 697, величины 539, табличных частей 272. Поправка: `sig_terms` в карточку не вошли — движок запрещает `ts_dict_*` с `GROUP BY`, а по одной это 1502 запроса против п. 20) |
| 9 | `[lane:gate]` **Ф2**: вектор карточки штатным `embed_missing.sh` + 🔴 сравнение с вектором МЕТКИ тем же прибором (метка проверена и отвергнута: у «продажи» ближайшая — «Склады») | Без вектора 0; top-1/3/5 карточки против метки — числом. Карточный не лучше → векторная ветка в слиянии выключается, и это записано числом, а не мнением | 8 | cc:完了 (1502 карточки за 45 с, без вектора 0. Карточка ЛУЧШЕ метки: до реранкера 31/44 (70 %) против 23/44 (52 %), в пятёрке 27 против 22; верх списка +1 — там решает реранкер. Попутно починен сам прибор: бил в корень эмбеддера, HTTP 404) |
| 10 | `[lane:gate]` **Ф3**: инвертированный индекс по карточке (лексика + ngram-поле). 🔴 `ivf` не используется нигде | `duckdb_indexes()` видит индекс; ветки лексики и `ts_ngram` дают непустое на эталонном вопросе; DDL сделан между тактами, не во время | 8 | cc:TODO |
| 11 | `[lane:gate]` **Ф4**: режим RRF в `entity_choice_bench.py` (три ветки, `k=60`, окно 60, разделитель равенства) | top-1/top-3/top-5 на всех парах набора против нынешнего ранжирования, отдельно на покрытых и непокрытых синонимами; окно ветки проверено замером, а не назначено | 9,10 | cc:TODO |
| 13 | `[lane:fast]` Скорер боевой базы: `AUDIT_CODE_1 §2.7` — из семи работающих проверяли три, бусты и RRF не проверяли вовсе; `F008` — живой скорер (`tfidf`) разошёлся с записанным выбором (`lm_dirichlet`); `F243` — у `tfidf` 3–5 ничьих на вопрос против 0–2 у остальных. Проверить прибором, не подобрать | top-1/3/5 по всем семи скорерам на парах набора, одним прогоном прибора; вывод числом. Смена скорера — отдельное решение после числа, вместе с выкаткой | - | cc:完了 (шесть скореров на 44 вопросах: сумма поверхностей 82–84 %, разброс ОДИН вопрос; боевой `tfidf` не хуже прочих. Менять нечего. `F008` остаётся дефектом учёта, но ценой ответа не является) |
| 12 | `[lane:gate]` **Ф5**: решение по числу. Выигрыш по top-3 → правка шага A в `serene_ask.py`; выигрыша нет → отрицательный результат числом, продукт не трогаем | Решение опирается на число прибора (он детерминирован, разброса нет); правка НЕ выкачена без слова владельца; такт (`build.sh`) правится только после этого | 11 | cc:完了 (выигрыш подтверждён на живых функциях отбора: смысл 52 %→66 %, сумма поверхностей 75 %→82 %. `near_tables` переведена на карточку с честным откатом по `emb_ready`; карточка внесена в такт шагом 7-бис, публикуется `MERGE`-ем — повторный прогон не пересчитал ни одного вектора. Выкачено по слову владельца, живая проба `focus` → 272) |
| 4 | `[lane:fast]` `[tdd:required]` `F245`: гарда резолвера зашита на `[0-9a-zа-яё]` — на CJK/греческой/арабской резолвер молча выключается; определять письменность универсально | Тест: строка на CJK и греческой проходит гарду, резолвер зовётся; ни одного перечня букв конкретных алфавитов в коде гарды | - | cc:TODO |
| 5 | `[lane:fast]` `[tdd:skip:prompt-text-only]` `F249`: описание инструмента `report_1c` содержит пример из нашей базы («топ-5 городов по числу банков») — заменить на универсальное без имён чьей-либо базы | В описании нет имён сущностей какой-либо базы (проверка гейтом кириллицы в коде); приёмка 58×2 не хуже | - | cc:TODO |
| 6 | `[lane:release]` Финальный замер захода: приёмка 58×2 + `REGRESSION_BASE1` после последней правки; итоги — в `TARGET_STATUS` (пп. 3, 10, 12), `CHANGELOG`, `activeContext` | Оба замера сняты ПОСЛЕ последней правки; числа в `TARGET_STATUS` совпадают с файлами прогонов; документы обновлены тем же заходом | 1,2,3,4,5 | cc:TODO |
| 14 | `[lane:gate]` **Мерило владельца 03.08**: считать ОШИБКИ, а не «правильность» — переспрашивать разрешено | Прогонщик печатает сводку «ошибок / верных / не утверждал»; проба подменой зелёная | - | cc:完了 (`run_acceptance.harm`, `test_acceptance.py` прошёл) |
| 15 | `[lane:gate]` **Право на ответ (а): согласие поверхностей.** Отвечаем, только если независимые поверхности сходятся на одной сущности; разошлись — вопрос кнопками из данных | Число ошибок (новое мерило) не выросло, а доля «не утверждал» объяснима; замер детерминированным прибором, без модели | 12 | cc:TODO |
| 16 | `[lane:gate]` **Право на ответ (б): гейт величины.** Подходит больше одной величины — спрашиваем. Живой случай: вопрос 6 отдал 94 784 при эталоне 92 683, величину выбрали молча | Ни одного ответа, где величина выбрана при нескольких подходящих; ошибок не прибавилось | 12 | cc:TODO |
| 17 | `[lane:gate]` **Право на ответ (в): арбитр — детектор, а не выбирающий.** Посчитанные ответы существенно разошлись → это доказанная неоднозначность, спрашиваем | Расхождение ответов арбитра ведёт к вопросу, а не к выбору; замер на наборе | 12 | cc:TODO |
| 18 | `[lane:gate]` **Право на ответ (г): пересчёт вторым путём** (шапка против табличной части) — бьёт в семейство «строка ≠ объект», где лежат эталоны вопросов 2, 4, 6, 7 | Не сошлось — вопрос, а не число; на вопросах 2/4/6/7 ошибок нет | 12 | cc:TODO |
| 19 | `[lane:fast]` Свой поход плагина за данными — починить две причины и включить обратно: спрашивает сервис сырым вопросом (минуя вики) и гасит `requireDataTool` | Вопрос идёт тем же путём, что у бота; появление своего эталона НЕ снимает требование переписать; ошибок не прибавилось | 15 | cc:TODO (код есть, `askUrl` очищен) |
| 20 | 🔴 Ждёт владельца: доступ к Windows (чтение SSH-ключа остановлено рубежом runtime). Цель — проверить выгрузку конфигурации в XML: человеческие имена объектов и состав стандартных отчётов | Ответ: выгружается или нет. Если да — заводится задача на использование этих данных вместо догадок модели | - | blocked (ждёт владельца) |
| 7 | `№20`: правило владельца «несколько разных объектов → переспрашивать» — реализация по содержанию (кнопки из данных), НЕ через `ASK_ALIAS_VETO` (он рубит верные ответы, `HOW_NOT_TO §1.25`) | Форму подтверждает владелец (вопрос №8 списка); до ответа — не начинать | - | blocked (ждёт ответа владельца на вопрос №8) |

## Пометки цикла

- Spec skip reason: продукт-контракт этого проекта — `TARGET.md`; заход Э3 контракт не
  меняет (и не вправе — правка TARGET.md только словом владельца). Отдельный `spec.md`
  не заводится, чтобы не плодить второе место правды.
- team_validation_mode: manual-pass — перспективы пройдены одним проходом с явной
  разбивкой: Продукт (пп. 3/10/12 — прямое попадание в контракт), Архитектура (отбор
  индексом — девиз «считает база»), Безопасность (secret-read нет, состав секретов не
  задет), QA (у каждой задачи числовой DoD и общий финальный замер), Скептик (главный
  риск — «не хуже» по приёмке при разбросе прибора ±1: потому пачки и дважды).
- Память проверена: `HOW_NOT_TO §1.25` (вето по алиасам рубит верное — потому задача 7
  blocked), «уже проверено» в `PLAN_VECTOR_CHECKS` не задевается, граф зависимостей
  подскажет задетое при правке (хук check-deps).
