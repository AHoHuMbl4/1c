# Карта проекта: где что лежит и что делать дальше

Заведено 2026-07-28 по указанию владельца: открыв новую сессию в любой момент, надо
сразу понимать устройство и следующий шаг.

**Читать в этом порядке:** этот файл → [`docs/TARGET_STATUS.md`](docs/TARGET_STATUS.md)
(что на какой стадии) → `memory_bank/activeContext.md` (что делаем сейчас) →
`TARGET.md` (чем продукт обязан быть) → `docs/HOW_NOT_TO.md` (на чём уже спотыкались).

---

## 1. Что делает продукт

Бот отвечает на вопросы по данным 1С. Цепочка:

```
1С (Windows, IIS, OData только на чтение)
  └─ шлюз :6011 — режет всё, кроме GET
      └─ витрина в SereneDB — таблицы 1С как есть
          └─ ПОДГОТОВКА + СБОРКА (внутри движка, SQL): корпус, величины, ссылки, векторы
              └─ индекс `search_idx` (инвертированный, BM25)
                  └─ сервис ответов — ищет индексом, считает базой, формулирует моделью
                      └─ мост MCP `ask_1c` (:6016) → бот OpenClaw в Telegram
```

**Баз 1С бывает несколько, и у каждой свой контур целиком:** свой шлюз OData, своя база
внутри движка, свой сервис ответов. На стенде их две: первая (Бухгалтерия 3.0, база движка
`postgres`, сервис :8091) и вторая (УТ 11.4.9, база `ut_test`, сервис :8099). **Боевые
ответы идут по второй** — это задано `ASK_URL` в юните `1c-mcp-ask`.

**Главный принцип** (п. 20 `TARGET.md`): данные лежат в SereneDB — работа с ними делается
**внутри** SereneDB. Своим кодом остаётся только то, чего у движка нет.

---

## 2. Файлы, которые работают

### Сборка данных — `ubuntu/serenedb/`, всё на SQL, питона нет

Такт **не ночной**: система обязана отражать изменения в 1С не позднее 20 минут
(п. 17 `TARGET.md`, подтверждено владельцем 28.07). Отсюда замок от наложения запусков,
докатка прерванного прогона и цена такта по числу изменений, а не по размеру базы.

| Файл | Что делает |
|---|---|
| `pipeline.sh` | **точка входа конвейера**: синк-дельта → сборка. Один атомарный такт |
| `serene_sync.py` | синк витрины из 1С: **дельта по `DataVersion`**, перепись схемы раз в час |
| `poc_load_entity.py` | загрузка сущности: полная и **`load_entity_delta`** (только изменения) |
| `build.sh` | сборка поискового слоя. Связывает шаги, держит замок, создаёт/удаляет секреты |
| `corpus_init.sql` | развёртывание с нуля: таблицы, словарь, индекс, **права**. Идемпотентно |
| `corpus_precheck.sql` | проверки ДО такта: индекс, права, источники, живость эмбеддера. Отпечаток «как было» |
| `corpus_build.sql` | сборка корпуса: `$metadata`, классификация колонок, имена, ссылки, даты, величины (`nums`), булевы реквизиты (`flags`), метки сущностей (`search_tables`) |
| `corpus_merge.sql` | перенос в боевой корпус по отпечатку + публикация индекса |
| `corpus_postcheck.sql` | проверки ПОСЛЕ такта: сверка с отпечатком. Код возврата — код сборки |
| `coverage_build.sql` | **перепись полноты** (п. 13): 1С → витрина → корпус → индекс → вектор + причина |
| `resolver_build.sql` | значения измерений для резолвера, инкрементально, одним `MERGE` |
| `embed_missing.sh` | досчёт векторов `ai_embed`: пачки по строкам И символам, докатка, перенос по ключу, `ROWS_WHERE` для очерёдности |
| `classify_entities.py` | разметка сущностей моделью: о чём спрашивают / служебное. Кэш в `search_entity_class`, задаёт **очерёдность** векторизации, ничего не выбрасывает |
| `deploy.sh` | раскладка кода в рабочий каталог, атомарной подменой. Убирает ручной шаг «скопировать в `/opt`». 🔴 **Ни одного `systemctl` в нём нет** — см. врезку ниже |
| `serenedb_backup.sh` | оффлайн-снапшот каталога данных движка: `stop → tar.gz → start`, ротация `KEEP=7`, процедура `RESTORE` в комментарии. Расписания у него нет, зовётся руками |
| `embed_all.sh` | **только досчёт векторов**, без пересборки корпуса. Для продолжения после обрыва и при первой заливке крупной базы |
| `wiki_build.sql` | страницы вики собираются **в базе**: чем сущность отличается от соседей. Ни сумм, ни счётов, ни дат — они бы устаревали и стали готовым неверным ответом |
| `wiki_publish.sh` | пишет страницы в хранилище `memory-wiki` и зовёт `openclaw wiki compile` |
| `wiki_alias.sh` | человеческие слова к сущности — **штатным агентом OpenClaw**, пачками по близости смысла; второй проход разводит слова-столкновения. Сколько сущностей за такт — ручка `WIKI_ALIAS_PER_TACT` (умолчание 100, `build.sh`) |

🔴 **Раскладка кода никого не перезапускает — момент выката боевых ответов — это ребут.**
`deploy.sh` кладёт в `/opt/1c-mcp-reports` в том числе `serene_ask.py` (код сервиса ответов)
и `mcp_reports.py`, но ни `deploy.sh`, ни `pipeline.sh` не делают ни одного
`systemctl restart`. Долгоживущий сервис продолжает исполнять версию, загруженную при
старте, — то есть разложенный код впервые вступает в силу при перезагрузке и может к тому
моменту копиться днями, ни разу не проверенный замером. Это же — обратная сторона правила
«сверить, что исполняется то, что правится».

⚠ И `/opt/1c-mcp-reports` **не зеркало репозитория**: `deploy.sh` копирует только
`*.sh|*.sql|*.py|*.tsv`, которые отличаются, и **ничего не удаляет**. Поэтому в рабочем
каталоге живут неверсионированные `bench_build.py`, `bench_run.py`, `serene-entities.txt.review`
и каталог `release-42/` с 15 журналами опытов от 26.07. Восстановить каталог из репозитория
«как есть» нельзя, и при разборе аварии непонятно, что из лежащего исполняется.

### Слой бота — `ubuntu/openclaw/`

| Файл | Что делает |
|---|---|
| `instance/AGENTS.md` | персона: тон, порядок работы с вопросом (сперва вики, потом данные), что видит человек |
| `mcp_ask.py` | мост MCP `ask_1c` → сервис ответов. Служебные маркеры `[NO DATA]`, `[CLARIFICATION NEEDED]` — **протокол с плагином-гейтом**, менять только вместе с ним |
| `verify-plugin/` | гейт исходящего типизированными хуками OpenClaw: ход без обращения к данным, число без опоры, уточнение от сервиса |
| 🔴 `deploy_instance.sh` | **выкатка всего перечисленного.** Пока её не было, бот двое суток работал по копии от 30.07 и правки промта не влияли ни на один ответ (`HOW_NOT_TO §1.27`). Заодно нейтрализует стартовые заготовки движка — 6 124 символа в промте каждый ход |

### Приёмка — `work/acceptance/`

| Файл | Что делает |
|---|---|
| `run_acceptance.py` | гоняет вопросы из `docs/ACCEPTANCE_UT.md` **через бота**, сверяет число и **ожидаемое поведение** (`no_data` / `clarify`). Форма вызова: `RUN_TAG=v<N> python3 work/acceptance/run_acceptance.py <от> <до>` — **границы диапазона обязательны** (без них `IndexError`). `RUN_TAG` технически необязателен (без него подставляется метка времени), но **он обязан быть новым на каждый прогон**: при повторе ключа бот помнит прошлый ответ |

Контракт `/ask` для фронта — [`docs/API_ASK.md`](docs/API_ASK.md): адреса, `kind`,
уточнение кнопками, и два ограничения, которые надо знать до начала (loopback без CORS,
токен нельзя класть в браузер).

### 🔴 Чем что запускать — берите САМЫЙ УЗКИЙ инструмент

Дважды за 29.07 я брал инструмент, который делает всё, когда нужно было одно, и терял
часы (`HOW_NOT_TO §3.22`). Таблица, чтобы не соображать заново:

| Задача | Запускать | НЕ запускать |
|---|---|---|
| правка персоны, моста, плагина | `ubuntu/openclaw/deploy_instance.sh` | ничего: без выкатки правка не действует |
| замер качества ответов | `RUN_TAG=v<N> python3 work/acceptance/run_acceptance.py <от> <до>` — через бота | прямой вызов `/ask` — он минует вики и даёт другой ответ. 🔴 Именно ему учит §0 `docs/ACCEPTANCE_UT.md`: тот раздел для замера качества не годится |
| загрузить/обновить **одну сущность** | `poc_load_entity.py <Имя>` — [замер] 4,6 с | `serene_sync.py` (все 1 588, ~40 мин) |
| подтянуть **изменения** из 1С | `serene_sync.py` | `pipeline.sh` |
| **продолжить досчёт векторов** | `embed_all.sh [потоков]` | `build.sh` (пересоберёт корпус) |
| пересобрать корпус после правки `corpus_build.sql` | `build.sh` | — |
| **полный такт** (данные изменились) | `pipeline.sh` или таймер | — |
| посмотреть полноту | `SELECT * FROM search_quality WHERE k LIKE 'cov_%'` | пересборка |

🔴 **У всех перечисленных инструментов умолчание — `dbname=postgres`, то есть ПЕРВАЯ база.**
Бот отвечает по второй (`ut_test`). Задавайте `SERENEDB_DSN` с явным `dbname` в каждой
команде: иначе «посмотреть полноту» молча покажет перепись чужой базы, а `build.sh` /
`embed_all.sh` потратят такт и деньги эмбеддера не на ту базу. Ошибки не будет ни на одном
шаге — числа просто окажутся из другой базы.

Крупный инструмент берётся, когда нужен **весь** его результат, а не когда он «заодно
сделает и это».

### Документы, заведённые 29.07

| Файл | О чём |
|---|---|
| [`docs/PLAN_ROW_IS_OBJECT.md`](docs/PLAN_ROW_IS_OBJECT.md) | 🔴 **строка корпуса ≠ объект 1С**: бот отвечал «216 партнёров» вместо 164. План из четырёх шагов, прошёл три независимых разбора. Сделаны шаги 1 и 2 (проверены на двух базах), шаг 3 закрылся шагом 2, **шаг 4 тоже сделан**: `coverage_build.sql` сравнивает корпус с числом объектов витрины и называет раздутие причиной («витрина развёрнута по табличной части» — 85 сущностей, «регистр: `$count` считает регистраторы» — 80). *Прежняя запись «шаг 1 сделан» отставала и от кода, и от собственного плана.* |
| [`docs/BOXED_BLOCKERS.md`](docs/BOXED_BLOCKERS.md) | что мешает работать на чужой базе: резолвер мёртв на не-кириллице, словарь индекса с русской локалью, остатки рукописного списка сущностей |
| [`docs/ACCEPTANCE_UT.md`](docs/ACCEPTANCE_UT.md) | приёмочный набор: 58 вопросов под 12 классов дефектов. Эталонный SQL — у **49** из них; остальные девять проверяют не число, а **вид ответа** (`kind`: уточнение или отказ) — §5 «Уточнение вместо догадки» и §12 «Хрупкость формулировки» сверяются между собой, а у №27 («На какую сумму?») эталона нет вовсе, это ловушка на `clarify`. *Было ошибочно «с эталонным SQL к каждому»: пересчёт по самому набору даёт 58 вопросов и 49 маркеров «SQL:».* 🔴 §0 того же файла («Как пользоваться») учит прямому `curl` на `/ask` — для замера качества этот путь запрещён, см. таблицу выше |
| [`docs/API_ASK.md`](docs/API_ASK.md) | контракт `/ask` для фронта |
| [`docs/REGRESSION_BASE1.md`](docs/REGRESSION_BASE1.md) | чем доказано, что первая база цела после каждой правки |

Проверка «не сломали ли первую базу» — [`docs/REGRESSION_BASE1.md`](docs/REGRESSION_BASE1.md):
там сведено, какой правкой и каким замером это доказано.

### Юниты systemd — их двадцать и они в ВОСЬМИ каталогах

Все двадцать под git, восемнадцать побайтно совпадают с `/etc/systemd/system`.

| Каталог | Что там |
|---|---|
| `ubuntu/systemd/` | `1c-serene-pipeline.service/.timer` (боевой такт), `1c-serene-index.service` (**боевая** копия, на `build.sh`), `1c-odata-gateway@.service` (шаблон на каждую базу) |
| `ubuntu/serenedb/systemd/` | `1c-serene-ask.service`, `1c-mcp-reports.service`, `1c-serene-sync.*`, ⚠ **двойник** `1c-serene-index.service/.timer` |
| `ubuntu/openclaw/systemd/` | `1c-mcp-ask.service` (мост бота), `1c-mcp-braine.service` + drop-in `override.conf` |
| `ubuntu/1c-gateway/systemd/` | `1c-odata-gateway.service` (первая база) |
| `ubuntu/1c-etl/systemd/`, `ubuntu/1c-config-ui/systemd/`, `ubuntu/monitoring/`, `ubuntu/serenedb/` | ETL, config-ui, монитор бота, юнит движка |

🔴 **Двойник `1c-serene-index` опасен.** В `ubuntu/systemd/` лежит боевая версия
(`ExecStart=/opt/1c-mcp-reports/build.sh`), в `ubuntu/serenedb/systemd/` — снятая
(`ExecStart=… serene_search_build.py`), то есть **тот самый питоновский сборщик, который
снёс корпус и индекс 28.07**. Разворачивать надо только копию из `ubuntu/systemd/`.
Отдельная дыра: боевого таймера `1c-serene-index.timer` (`OnUnitActiveSec=20min`) в
репозитории нет вовсе — единственная его версия в git — снятое ночное расписание 03:55.

*Было ошибочно: «Юниты systemd — в `ubuntu/systemd/`» — по этому адресу нет ни одного юнита
работающих сейчас служб (мост бота, сервис ответов, шлюз первой базы, монитор, движок лежат
в других каталогах). **Чем опровергнуто:** `find ubuntu -name '*.service' -o -name '*.timer'`
→ 20 файлов в восьми каталогах, все `git ls-files` TRACKED.*

### Ответы — `ubuntu/serenedb/serene_ask.py`

Единственный большой файл своего кода в контуре ответа. Что важно: поиск штатными
функциями (`ts_*`, `bm25`, `<=>`), выбор сущности **реранкером**, величину — **по вопросу**
(базовая предпочтительнее уточнённой), **калькулятор** (числа ставит код, не модель),
**резолвер** (слово → значение через отдельную роль), связка **уточнение → выбор `focus`
→ ответ**, честный **503 при сбоях**, видимое старение данных.

### Проверка

| Файл | Что делает |
|---|---|
| `golden.sh` + `golden-questions.txt` | приёмочный набор, 8 вопросов, гоняется после КАЖДОЙ правки |
| `ab_scorer.py` | второй набор: 8 вопросов, истина считается независимым SQL по источнику |

### Выведено из контура, не трогать

`serene_search_build.py` — прежняя питоновская сборка. 🔴 **Запускать нельзя**: при
нынешнем составе корпуса она сносит таблицу и индекс (авария 28.07).
`serene_report.py`, `mcp_reports.py` — инструмент `report_1c`, решено вывести. ⚠ Решение не
исполнено: юнит `1c-mcp-reports` жив и подключён к боту записью `second-brain-reports`.
`build_resolver_index.py` — прежняя сборка резолвера, заменена на `resolver_build.sql`.

🔴 **Выведенное по репозиторию — не то же, что выведенное на стенде.** В `/opt/serenedb-tools`
лежит `report_query.py` (5 818 байт, от 23.07) — самостоятельная программа «NL→запрос по
витрине»: интроспектирует схему, отдаёт её в DeepSeek, получает `SELECT`, проверяет
регуляркой и исполняет. Это ранняя версия ровно того пути, от которого владелец отказался
решением «схема в модель не уходит никогда». Программа никем не запускается (ноль упоминаний
в репозитории), но полностью работоспособна и стартует одной командой. Одноимённый файл в
`ubuntu/serenedb/` — безобидная обёртка на 585 байт. Уборка каталога — решение владельца.

---

## 3. Документы: какой о чём

### Закон и правила

| Файл | О чём |
|---|---|
| `TARGET.md` | **контракт результата, 21 пункт.** Менять только по слову владельца. При расхождении прав он |
| `CLAUDE.md` | правила работы: что читать, куда писать, когда коммитить. Читается автоматически каждую сессию |
| `memory_bank/owner-memory/` | решения владельца, которые нельзя пересматривать |

### Состояние и история

| Файл | О чём |
|---|---|
| `memory_bank/activeContext.md` | **что делаем сейчас и с чего начать.** Первое, что читать после этой карты |
| `CHANGELOG.md` | что менялось, почему и **чем доказано**. Новое сверху, метки `[замер]`/`[код]`/`[решение]` |
| `docs/SERVER_HANDOFF.md` | состояние сервера: службы, таймеры, что не доделано |

### Опыт и ловушки

| Файл | О чём |
|---|---|
| `docs/HOW_NOT_TO.md` | **на чём уже спотыкались** (45+ разборов). Формат «что сделал не так → чем кончилось → как надо». Начать с §0 |
| `memory_bank/techContext.md` | **ловушки движка** (таблица «что важно знать» + **21 нумерованный разбор, номера 15–35**), всё проверено замером: что он умеет, что роняет, что молчит. *Было ошибочно «разборы 15–22» — за границей описания оставались тринадцать, среди них Ловушка 25 (`duckdb_tables` отдаёт таблицы всех присоединённых баз), 26 (секреты движка общие на инстанс), 28 (`ai_embed` занимает исполнителя), 33 (сравнение колонки из LEFT JOIN в фильтре молча не применяется), 35 (булев реквизит)* |

### Знания о SereneDB (движок)

| Файл | О чём |
|---|---|
| `docs/SERENEDB.md` | что за движок, его возможности — главный файл про SereneDB |
| `docs/SERENE_CAPS_1..3.md` | аудит возможностей: какие функции есть в нашей сборке 26.07.3 |
| `docs/VECTOR_DECISION.md`, `docs/SERENEDB_IVF_ISSUE_RU.md` | почему векторный индекс `ivf` заблокирован |
| `docs/AUDIT_SERENE_SUMMARY.md`, `docs/BENCH_BRAINE_VS_SERENE.md` | аудит и замеры движка |

🔴 **Перед тем как писать свой SQL/код поверх базы — сходить в официальные доки движка через
MCP `serenedb-docs` (`search_docs` → `read_section`) и при сомнении спросить агента
`serenedb-native`**, а не гадать. Правило п. 20 `TARGET.md`. Найденное в доках обязательно
проверяется на живом инстансе: доки описывают текущую версию, у нас сборка 26.07.3.

*Было ошибочно: «спросить агента `serenedb-native` (у него полный клон исходников движка)».
**Чем опровергнуто:** агент переведён на официальные доки —
`.claude/agents/serenedb-native.md` объявляет источниками `mcp__serenedb-docs__*` и живой
движок и прямо говорит: «Прежний источник — локальный клон исходников `serenedb-src` —
больше не используется»; то же в `CLAUDE.md`. Указание владельца 01.08: «для доков SereneDB
используй mcp сервер, который уже настроен, не нужно изучать доки сырые».*

### Планы и разборы

| Файл | О чём |
|---|---|
| `docs/PRODUCTION_PLAN.md` | все работы до продакшена, по вреду для клиента |
| `docs/PREPARE_BASE.md` | подготовка базы как отдельный шаг — направление, заданное владельцем 28.07 |
| `docs/SCALE_BLOCKERS.md` | что растёт с размером базы |
| `docs/CHECKLIST_SEARCH_FIX.md` | правки поиска, что сделано и что отклонено |
| `docs/VECTOR_DECISION.md`, `docs/SERENEDB_IVF_ISSUE_RU.md` | вектор: почему `ivf` заблокирован |
| `docs/RUNBOOK_DEPLOY.md`, `docs/ARCHITECTURE.md` | развёртывание и полная картина слоёв |

### Исследования (читать при необходимости, не подряд)

🔴 **Это ИСТОРИЧЕСКИЕ отчёты, а не описание сегодняшнего состояния.** Написаны в настоящем
времени о стенде, которого больше нет: семейство `CHECK1_*`/`CHECK2_*` целиком снято на
корпусе 97 965 строк с `emb FLOAT[1536]`, тогда как сегодня корпус первой базы 103 808 строк
и `FLOAT[1024]`, а `serene_search_build.py`, о котором они говорят как о боевом коде, из
такта выведен. Правила, выведенные в них, местами действуют до сих пор — но отличить
действующее от отменённого по самому файлу нельзя. Сверяться с ними как с состоянием нельзя.

- `docs/CHECK1_*`, `docs/CHECK2_*` — проверки тремя независимыми агентами со сводками.
  ⚠ `docs/CHECK2_INCR_SUMMARY.md` (593 строки) **признан тремя ревизорами «к исполнению не
  годным» и заменён** на `docs/CHECK2_INCR_SUMMARY_V2.md` (193 строки) — разбор в §0 версии 2.
  В самом файле V1 об этом ни слова, а он объёмнее и выглядит солиднее: открывшему его
  первым достанутся выводы, признанные негодными.
- `docs/AUDIT_CODE_*`, `docs/SERENE_CAPS_*` — аудиты кода и возможностей движка.
  ⚠ Приём `MERGE … RETURNING merge_action`, который они рекомендуют, **не сработает на
  целевой архитектуре** (`RETURNING` не реализован для attach-нутых Postgres-таблиц,
  `ARCHITECTURE.md`).
- `docs/HANDOFF_CHECK_*` — проверки передаточных документов.

---

## 4. Где что живёт на сервере

| Что | Где |
|---|---|
| репозиторий | `/srv/1c` — работаем здесь |
| боевой код сборки и ответов | `/opt/1c-mcp-reports/` — раскладывает `deploy.sh` (руками не копировать) |
| 🔴 рабочая папка бота | `/home/undebot/.openclaw/workspace` — сюда `deploy_instance.sh` кладёт персону, отсюда её читает бот. **Именно её отсутствие в карте стоило двух суток:** правки промта не влияли ни на один ответ, потому что бот читал другую копию |
| хранилище вики | `/home/undebot/.openclaw/wiki/main` — 698 страниц сущностей (плагин `memory-wiki`, `vaultMode: isolated`); по ней бот перефразирует вопрос ДО обращения к данным |
| остальные рабочие каталоги | `/opt/1c-bot-monitor`, `/opt/1c-config-ui`, `/opt/1c-etl`, `/opt/1c-gateway`, `/opt/1c-odata-gateway`, `/opt/openclaw`, `/opt/openclaw-engine`, `/opt/openclaw-mcp` (venv питона всех юнитов) |
| секреты | `/etc/*.env`, права 600, в git не входят |
| данные движка | `/var/lib/serenedb` — 12 ГБ: `engine_duckdb` 11 ГБ, `engine_search` 159 МБ, подкаталог второй базы `ut` 786 МБ |
| выгрузка 1С | там же, `/var/lib/serenedb/*.csv` — загрузчик пишет CSV и **после `read_csv` не удаляет**: 1767 файлов на 826 МБ, старейшие от 24.07. Растёт с размером базы; при планировании места это первое, что надо учесть |
| ~~исходники движка~~ | ~~`/srv/serenedb-src`~~ — **больше не источник**: агент `serenedb-native` переведён на MCP `serenedb-docs`. Каталог на диске ещё лежит и путает |

**Опись копий витрины** (её раньше не было вовсе, и «резерв и возврат» держался на памяти сессии):

| Где | Что | Дата |
|---|---|---|
| `/root/backups/serenedb/serenedb-20260729-044937.tar.gz` | единственный штатный снапшот, 2,7 ГБ. 🔴 Снят **до** `DROP DATABASE ut_test` — восстановление по нему откатит боевую вторую базу | 29.07 |
| `/var/lib/serenedb-rescue-20260727` | 2,0 ГБ, `store.db` + два WAL — след аварии 27.07, не значился ни в одной описи | 27.07 |

**Подключение к базе:** `psql 'host=127.0.0.1 port=7890 user=postgres dbname=<база>'` на
запись, роль `serene_ro` — на чтение. 🔴 **`dbname` указывать явно всегда:** первая база —
`postgres`, боевая вторая — `ut_test`; без `dbname` libpq берёт имя базы равным имени роли и
молча уводит вас в первую. Перепутать роль = получить `permission denied` и решить, что
нашёл дефект.

⚠ **В обеих базах рядом с боевыми таблицами лежат остатки опытов**, которых нет ни в одном
документе: `base1_aff`, `base1_before`, `probe_dim`/`probe_map`/`probe_s`, `rb_first`,
`step2_first`, `res_chunk`, `res_dim`, `res_emb_0…res_emb_11` (12 шардов, `emb FLOAT[1024]`;
ни одна строка репозитория их не создаёт и не читает), `tmp_prod_corpus`, `tmp_refmap`,
`tbl_chunk`, `search_*_bak`. Рабочие, но тоже неописанные: `cov_mart`, `cov_mart_obj`
(перепись полноты), `base_profile_meta`, представления `wiki_pages`, `wiki_entity_facts`.
Отбор источников идёт по `$metadata`, поэтому в корпус остатки не лезут — но человек,
открывший базу, боевое от мусора не отличит. Уборка — решение владельца.
*Попутно: `docs/HOW_NOT_TO §1.х` про аварию 28.07 ссылается на страховочный набор
`emb_done`/`emb_part_*` — его в базе уже нет, уцелели только шарды `res_emb_*`.*

---

## 5. Состояние [замер 01.08]

🔴 **Стадии по каждому пункту контракта живут в [`docs/TARGET_STATUS.md`](docs/TARGET_STATUS.md)
— здесь только устройство и числа.** Раздел раньше назывался «Состояние на вечер 28.07» и
не менялся с 28.07, хотя сам файл правился 29, 30 и 31.07: читающий по порядку получал из
MAP «оба набора 8/8, крупного без внешнего не осталось», а из `TARGET_STATUS` следующим
файлом — «на 58 вопросах верных меньше половины» и четыре 🔴.

**Числа [замер 01.08]:**

| | первая база (`postgres`) | боевая вторая (`ut_test`) |
|---|---|---|
| корпус | 103 808 строк / 231 сущность | 623 565 строк / 1502 сущности |
| резолвер `resolver_index` | 115 151 значение | 188 068 |
| строк без вектора | 5 091 — все служебные, по решению; очередь пуста | 340 464 (55 %), из них **92 080 деловых, то есть в очереди** |

*Было ошибочно: «вектор 1024 у всех» — не у всех: служебные сущности не векторизуются по
решению 29.07 (`cov_noemb_service`), и это видно штатно в `search_quality`. И «оба набора
8/8» — 8/8 давал лёгкий golden-набор регрессии первой базы, а не приёмка из 58 вопросов.*

**Свежесть по готовности:** конвейер `1c-serene-pipeline` (синк-дельта → сборка → повтор
через `OnUnitInactiveSec`). Синк тянет из 1С **только изменения** по `DataVersion`.
«Ночного» такта нет. Старые таймеры `sync`/`index` отключены; боевой юнит `index` — на
`build.sh` (не на снёсший корпус `serene_search_build.py`), но в репозитории рядом лежит
его двойник со старым `ExecStart` — см. §2.

**Что дальше:** вторая конфигурация 1С (УТ 11.4.9) **пройдена и стала боевой** — по ней
идут все ответы бота, без единой правки кода; п. 9 закрыт до остатка «другой язык».
Открытым по этой части остаётся п. 15 (приёмка из базы клиента — владелец: делаем в конце).
Главная незакрытая проблема — **выбор сущности**: все неверные ответы одного рода, счёт при
этом верен всегда. Дальше — решение владельца о периметре (п. 16) и инсталлятор (п. 14).

*Было ошибочно: «Упирается во вторую конфигурацию 1С (п. 9, 15)» и «крупного без внешнего
не осталось». **Чем опровергнуто:** боевой путь :6016 → :8099 → `dbname=ut_test`;
`TARGET_STATUS` п. 9 — «проверено на чужой конфигурации, всё без правок кода» с 28.07.
Сессия, открывшая MAP первым файлом, могла начать «подключать вторую базу», которая уже
работает.*

**Пайплайн синка данных (порядок слоёв):**

```
1С (OData) ──синк-дельта (serene_sync + poc_load_entity)──► витрина (таблицы 1С)
                                                               │
                                              build.sh ────────┘
                                                 corpus_init → precheck → corpus_build →
                                                 corpus_merge → embed → resolver_build →
                                                 coverage_build → postcheck ──► корпус+индекс+резолвер
```
