# Карта проекта: где что лежит и что делать дальше

Заведено 2026-07-28 по указанию владельца: открыв новую сессию в любой момент, надо
сразу понимать устройство и следующий шаг.

**Читать в этом порядке:** этот файл → [`docs/TARGET_STATUS.md`](docs/TARGET_STATUS.md)
(что на какой стадии) → `memory_bank/activeContext.md` (что делаем сейчас) →
`TARGET.md` (чем продукт обязан быть) → `docs/HOW_NOT_TO.md` (на чём уже спотыкались).

---

## 1. Что делает продукт

Бот отвечает на вопросы по данным 1С. Цепочка:

```
1С (Windows, IIS, OData только на чтение)
  └─ шлюз :6011 — режет всё, кроме GET
      └─ витрина в SereneDB — таблицы 1С как есть
          └─ ПОДГОТОВКА + СБОРКА (внутри движка, SQL): корпус, величины, ссылки, векторы
              └─ индекс `search_idx` (инвертированный, BM25)
                  └─ сервис ответов — ищет индексом, считает базой, формулирует моделью
                      └─ мост MCP `ask_1c` (:6016) → бот OpenClaw в Telegram
```

**Баз 1С бывает несколько, и у каждой свой контур целиком:** свой шлюз OData, своя база
внутри движка, свой сервис ответов. На стенде их две: первая (Бухгалтерия 3.0, база движка
`postgres`, `:8091`) и вторая (УТ 11.4.9, база `ut_test`, `:8099`). **Боевые ответы идут по
второй** — это задано `ASK_URL` в юните `1c-mcp-ask@ut_test`.

🔴 **02.08 оба сервиса ответов стали шаблонными юнитами** `1c-serene-ask@<база>` с
побазовым `/etc/1c-serene-ask-<база>.env`. До этого боевая конфигурация второй базы жила
ТОЛЬКО в окружении процесса, поднятого руками: `SERENEDB_DSN_RO` не был записан ни в один
файл, журнал уходил в файл под `/tmp` от завершённой сессии, и ребут оставил бы бота без
ответов. Тем же заходом побазовым стал `ASK_SCORER` — и `ab_scorer.py`, который его
переписывает, физически перестал доставать до боевой базы.

**Главный принцип** (п. 20 `TARGET.md`): данные лежат в SereneDB — работа с ними делается
**внутри** SereneDB. Своим кодом остаётся только то, чего у движка нет.

🔴 **PostgreSQL в продукте нет** (решение 22.08, Ф3 `docs/PLAN_UPGRADE_NATIVE.md`):
SereneDB — самостоятельный сервер (`store.db`); PG-протокол — только для клиентов
(`psql`, Grafana). Отдельные установки PostgreSQL (dev `:5432`, Windows) — наследие /
**[действие владельца, в работе]** вывод, не слой ответа. Витрина пишется нативно
(`serene_sync.py` → `:7890`), `ATTACH (TYPE postgres)` в пайплайне не используется.

---

## 2. Файлы, которые работают

### Сборка данных — `ubuntu/serenedb/`, всё на SQL, питона нет

Такт **не ночной**: система обязана отражать изменения в 1С не позднее 20 минут
(п. 17 `TARGET.md`, подтверждено владельцем 28.07). Отсюда замок от наложения запусков,
докатка прерванного прогона и цена такта по числу изменений, а не по размеру базы.

| Файл | Что делает |
|---|---|
| `pipeline.sh` | **точка входа конвейера**: форма `EMBED_HOST` → синк-дельта → сборка. Один атомарный такт |
| `box_tune.sh` | **железо → conf** (E4): по RAM/vCPU пишет `cpu_threads`/`io_threads`/`memory_limit`/swap и `BUILD_THREAD_MIN`; после первой сборки возвращает лимит и swap (PLAN_KEY_DEDUP_RECOVERY §0). Железо читается прослойкой, не в формуле |
| `serene_sync.py` | синк витрины из 1С: **дельта по `DataVersion`**, перепись схемы раз в час |
| `poc_load_entity.py` | загрузка сущности: полная и **`load_entity_delta`** (только изменения; с 05.08 дельта работает и у документов с табличной частью — развёрнутый набор ей больше не помеха, оффлайн-проба `test_delta.py`). 🔴 Размер страницы OData (`ETL_PAGE`, умолчание 10 000) решает больше, чем объём данных: `[замер 05.08]` 165 строк/с при `$top=1000` против 1 316 при `$top=20000` — время уходит на **число обращений**, а не на строки. Страница самонастраивается: при отказе 1С уменьшается вчетверо до `ETL_PAGE_MIN` и сущность читается сначала (иначе смешались бы упорядоченные и неупорядоченные страницы). Прибор — `work/acceptance/odata_page_bench.py` |
| `build.sh` | сборка поискового слоя. Связывает шаги, держит замок, создаёт/удаляет секреты |
| `corpus_init.sql` | развёртывание с нуля: таблицы, словарь, индекс, **права**. Идемпотентно |
| `corpus_precheck.sql` | проверки ДО такта: индекс, права, источники, живость эмбеддера. Отпечаток «как было» |
| `corpus_build.sql` | сборка корпуса: `$metadata`, классификация колонок, имена, ссылки, даты, величины (`nums`), булевы реквизиты (`flags`), метки сущностей (`search_tables`), **связь «кто пишет этот источник»** (`written_by`, раздел 2-тер — регистр ← его регистратор) |
| `corpus_merge.sql` | перенос в боевой корпус по отпечатку + публикация индекса |
| `corpus_postcheck.sql` | проверки ПОСЛЕ такта: сверка с отпечатком. Код возврата — код сборки |
| `coverage_build.sql` | **перепись полноты** (п. 13): 1С → витрина → корпус → индекс → вектор + причина |
| `resolver_build.sql` | значения измерений для резолвера, инкрементально, одним `MERGE` |
| `embed_missing.sh` | досчёт векторов `ai_embed`: **одна** сессия psql на цель (Э1), пачки по строкам И символам, докатка, перенос по ключу, `ROWS_WHERE` для очерёдности; параллель — `SET threads`, не веер клиентов |
| `classify_entities.py` | разметка сущностей моделью: о чём спрашивают / служебное. Кэш в `search_entity_class`, задаёт **очерёдность** векторизации, ничего не выбрасывает. Замер границы и цены слоёв — [`docs/DATA_SCOPE_2026-08-21.md`](docs/DATA_SCOPE_2026-08-21.md) |
| `deploy.sh` | раскладка кода в рабочий каталог, атомарной подменой. Убирает ручной шаг «скопировать в `/opt`». 🔴 **Ни одного `systemctl` в нём нет** — см. врезку ниже |
| `serenedb_backup.sh` | оффлайн-снапшот каталога данных движка: `stop → tar.gz → start`, ротация `KEEP=7`, процедура `RESTORE` в комментарии. Расписания у него нет, зовётся руками |
| `embed_all.sh` | **только досчёт векторов**, без пересборки корпуса. Для продолжения после обрыва и при первой заливке крупной базы |
| `wiki_build.sql` | страницы вики собираются **в базе**: чем сущность отличается от соседей. Ни сумм, ни счётов, ни дат — они бы устаревали и стали готовым неверным ответом |
| `wiki_publish.sh` | пишет страницы в хранилище `memory-wiki` и зовёт `openclaw wiki compile` |
| `wiki_alias.sh` | человеческие слова к сущности **и к её величинам** — штатным агентом OpenClaw, пачками по близости смысла; добор величин для уже описанных сущностей (алиасы записи не перезаписываются); второй проход разводит слова-столкновения. Сколько сущностей за такт — ручка `WIKI_ALIAS_PER_TACT` (умолчание 100, `build.sh`). 🔴 Проходы ограничены **временем** (`WIKI_ALIAS_MAX_SEC`, умолчание 120 с). Пустая запись — попытка, не ответ (`WIKI_ALIAS_RETRY_H`). Словарь величин — `search_measure_alias` (поле → слова). Столкновения сущностей помнит `search_alias_probe`. Разбор ответа модели — `wiki_alias_parse.py`. На юните без `/srv/1c` полный прогон — шаблон `1c-wiki-alias@<база>` (`ubuntu/serenedb/systemd/1c-wiki-alias@.service`), код из `/opt/1c-mcp-reports`; добор только величин — `WIKI_ALIAS_COLLISIONS=0` в `/etc/1c-wiki-alias-%i.env` |

🔴 **Раскладка кода никого не перезапускает.** `deploy.sh` кладёт в `/opt/1c-mcp-reports` в
том числе `serene_ask.py` (код сервиса ответов), но ни он, ни `pipeline.sh` не делают ни
одного `systemctl restart`: сервис продолжает исполнять версию, загруженную при старте.
Разложенный код вступает в силу только при перезапуске и может копиться днями, ни разу не
проверенный замером — обратная сторона правила «сверить, что исполняется то, что правится».

🔴 **Но ребут сегодня — не выкатка, а остановка бота.** Отвечает процесс вне systemd
(`:8099`, база `ut_test`); после перезагрузки он умрёт, `systemd` поднимет `1c-serene-ask` на
`:8091` по **первой** базе, а мост `ask_1c` продолжит стучаться в `:8099`, где никого нет.
Прежде чем перезагружать — см. `ARCHITECTURE §6`.

⚠ И `/opt/1c-mcp-reports` **не зеркало репозитория**: `deploy.sh` копирует только
`*.sh|*.sql|*.py|*.tsv`, которые отличаются, и **ничего не удаляет**. Поэтому в рабочем
каталоге живут неверсионированные `bench_build.py`, `bench_run.py`, `serene-entities.txt.review`
и каталог `release-42/` с 15 журналами опытов от 26.07. Восстановить каталог из репозитория
«как есть» нельзя, и при разборе аварии непонятно, что из лежащего исполняется.

### Пакетный транспорт — `ubuntu/packet/` (новый контур, 06.08)

Windows шлёт шифрованные пакеты, Ubuntu принимает и кладёт в витрину — замена прямому
доступу Ubuntu к OData (план `docs/PLAN_MVP_PACKET_TRANSPORT.md`, контракт
`docs/PACKET_CONTRACT.md`, endpoint `https://1c-gate.timpul.ru`).

| Файл | Что делает |
|---|---|
| `packet_crypto.py` | обёртка над штатным age CLI: keygen/encrypt/decrypt/sha256. Своя криптография не пишется |
| `packet_server.py` | **приёмник пакетов**: манифест + чанки → verify (age+sha256+zstd) до состояния `verified`; перевод в `applied` — apply-компонент (следующий шаг). Лимиты карантина, идемпотентность, `/health` |
| `packet_apply.py` | **apply**: `verified → applied` — merge в витрину семантикой `poc_load_entity` (full/delta/gone) + контрактные таблицы последней DML-транзакцией. 🔴 DDL в движке не транзакционен: атомарность = идемпотентные операции + маркер в конце, обрыв → пакет остаётся `verified` и переприменяется |
| `packet_kit.py` | **генератор комплекта базы**: identity age + токен + начальный конфиг + `packet-setup.json` для установщика. Вывод — `work/packet/kit/` (в git не входит, секреты), раскладка в `/etc` — за владельцем |
| `packet_config.py` | **config-builder** (Б2): итоговый контур сущностей для агента (`search_entity_force` > тень `_RecordType`/`_RowType` > `search_entity_class` > `only_binary`), `config_version` растёт только при изменении, `search_entity_skipped` с причинами. Пересчёт — онбординг **и** шаг после classify (`PACKET_BASE_ID` в `build.sh`). Приёмник перечитывает конфиг по mtime без перезапуска |
| `deploy_packet.sh` | раскладка контура в `/opt/1c-packet` атомарной подменой; **никого не перезапускает** (как `deploy.sh`) |
| `systemd/` | юниты контура (подготовлены, не развёрнуты): `1c-packet-server.service` (приёмник), `1c-packet-apply.service` + `.timer` (apply каждые 2 мин) |
| `test_packet_crypto.py` | оффлайн-проба крипты, 14 случаев |
| `test_packet_server.py` | оффлайн-проба приёмника, 30 случаев: полный цикл, идемпотентность, карантин при подмене, лимиты 413/429, config-канал |
| `test_packet_apply.py` | проба apply на живом движке, 24 проверки: merge/delta/gone, контрактные таблицы, идемпотентность, карантин `mix_versions`; гигиена `pkt_probe_*` с самоочисткой |

### Пакетный транспорт — Windows-сторона (агент) и проба формата (06.08)

| Файл | Что делает |
|---|---|
| `windows/packet-agent/src/PacketAgent.cs` | **агент пакетов** (C# 5, csc .NET 4, без NuGet — как setup): CLI по `AGENT_TZ §7` (--version/--smoke/демон/single-instance через mutex), такт: config → проба версий/контент-отпечаток → дельта → чанки → zstd+age (внешние exe) → отправка с догрузкой missing → индекс версий ПОСЛЕ verified (К2). Индекс — файлы `<entity>.idx` в data_dir |
| `work/packet/golden/` | **golden-проба формата (К5)**: живые фикстуры OData стенда + `make_reference.py` (эталон через настоящий `poc_load_entity`) + `probe.cmd` (`fc /b` на Windows). Снимок $metadata — `metadata.xml.zst` (20 МБ → 0,5). Зелёная проба — условие выката агента |

### Почтовый приёмник — `ubuntu/mail/` (08.08, подготовлен)

Компания пересылает выбранные письма на внутренний ящик; дальше их читает
OpenClaw (чтение — отдельный трек, здесь его нет). Postfix (приём :25,
`ai@<MAIL_DOMAIN>` + catch-all через `luser_relay`) → Maildir пользователя
`aimail`; Dovecot отдаёт ящик по IMAP только на `127.0.0.1:143`. Установка —
`install-mail.sh` (root, идемпотентно, со встроенным smoke-письмом), секрет —
`/etc/1c-mail.env` (640 root:1c-secrets). Устройство и приёмка —
`ubuntu/mail/README.md`. Место компонента в прод-контуре — открытый вопрос
`docs/PLAN_PROD_LXC.md §7`.

### Модели — `/etc/1c-embed.env`, одно место на весь продукт

🔴 **02.08 эмбеддер и реранкер переехали в контур владельца** (`Qwen3-Embedding-8B` +
`Qwen3-Reranker-8B`, dim 1024). Причина: [замер 02.08] облачный аккаунт DashScope ответил
`Arrearage` на все 11 ключей, и бот отдавал 503 **на каждый вопрос** — эмбеддинг вопроса
обязателен и отката не имеет. Наружу теперь уходит только модель ответов (DeepSeek).

| Что | Где | Зачем знать |
|---|---|---|
| адрес, ключ, модель, размерность, предел длины, размер пачки | `/etc/1c-embed.env` (600, не в git) | одно место; юниты `1c-serene-ask@` и сборка читают его |
| **две двери** | движок → `/v1/embeddings` (документы), сервис ответов → `/embed` с `is_query=true` (вопрос) | Qwen3 считает вопрос и документ по-разному; звать одинаково — молча потерять качество |
| отметка модели | `emb_model_<таблица>` в `search_quality` | смысловой путь включается только при совпадении с моделью вопроса — иначе несравнимые векторы дают мусор без ошибки |
| проверка модели | `embed_check.sh` (сборка) и `serene_ask.embed_model_live()` (ответ) | сначала форма `EMBED_HOST` (схема+хост+порт — иначе curl 000), затем `/health` и живой вектор. С 18.08 зовётся и из onboard/firstbuild до такта |
| прибор выбора сущности | `work/acceptance/entity_choice_bench.py` | сравнивает МОДЕЛИ между собой: детерминирован, без вызовов модели ответов, ранжирование делает движок |

### Слой бота — `ubuntu/openclaw/`

| Файл | Что делает |
|---|---|
| `instance/AGENTS.md` | персона: тон, порядок работы с вопросом (сперва вики, потом данные), что видит человек |
| `mcp_ask.py` | мост MCP `ask_1c` → сервис ответов. Служебные маркеры `[NO DATA]`, `[CLARIFICATION NEEDED]` — **протокол с плагином-гейтом**, менять только вместе с ним |
| `verify-plugin/` | гейт исходящего типизированными хуками OpenClaw: ход без обращения к данным, число без опоры, уточнение от сервиса |
| 🔴 `deploy_instance.sh` | **выкатка персоны, моста и плагина — В ЛЮБОЙ экземпляр бота.** `OPENCLAW_PROFILE_ID=<профиль>` задаёт каталог состояния, `OPENCLAW_ASK_UNIT` — какой мост перезапустить, `DRY_RUN=1` — сухой прогон. Резервирует персону перед заменой; `DEPLOY_PERSONA=0` не трогает её вовсе. Пока выкатки не было, бот двое суток работал по копии от 30.07 (`HOW_NOT_TO §1.27`), а второй бот 02.08 сутки был бы с чужой персоной и целыми заготовками движка |


### Приёмка — `work/acceptance/`

| Файл | Что делает |
|---|---|
| `run_acceptance.py` | гоняет вопросы из `docs/ACCEPTANCE_UT.md` **через бота**, сверяет величины эталона и **ожидаемое поведение**. Форма вызова: `RUN_TAG=v<N> python3 work/acceptance/run_acceptance.py <от> <до>` — **границы диапазона обязательны** (без них `IndexError`). `RUN_TAG` технически необязателен (без него подставляется метка времени), но **он обязан быть новым на каждый прогон**: при повторе ключа бот помнит прошлый ответ |
| `test_acceptance.py` | 🔴 **проба самого прибора подменой** — без базы, сети и модели. Прибору подсовывают заведомо верный ответ, заведомо неверный, отказ и уточнение, и он обязан назвать все четыре. Гоняется после каждой правки прогонщика |

🔴 **`kind` прибор берёт из стенограммы сессии бота, а не из русских слов ответа** (правка
02.08, кластер Э1). Результат вызова `ask_1c` лежит в `details.structuredContent.result`
файла сессии, а мост ставит туда машинные маркеры `[NO DATA]` / `[CLARIFICATION NEEDED]` /
`[SERVICE ERROR]` — это `kind` сервиса как есть. Отсюда два следствия: прибор не привязан к
языку ответа, и он видит класс, невидимый прежде, — **сервис сказал «нет данных», а модель
назвала число**. Путь к стенограмме спрашивается у движка (`openclaw sessions --json`),
имя файла не собирается догадкой.

[замер 02.08] Что дала правка на настоящем наборе: читается **58 вопросов вместо 54**,
сверяется величиной **47 вместо 26**, поведением 9, и **2** (№20, №53) прибор честно
называет непроверяемыми вместо того, чтобы засчитать любой ответ. 🔴 Прежние отметки
приёмки сняты сломанной линейкой и базой сравнения быть не могут.

Контракт `/ask` для фронта — [`docs/API_ASK.md`](docs/API_ASK.md): адреса, `kind`,
уточнение кнопками, и два ограничения, которые надо знать до начала (loopback без CORS,
токен нельзя класть в браузер).

### 🔴 Чем что запускать — берите САМЫЙ УЗКИЙ инструмент

Дважды за 29.07 я брал инструмент, который делает всё, когда нужно было одно, и терял
часы (`HOW_NOT_TO §3.22`). Таблица, чтобы не соображать заново:

| Задача | Запускать | НЕ запускать |
|---|---|---|
| правка персоны, моста, плагина | `ubuntu/openclaw/deploy_instance.sh` | ничего: без выкатки правка не действует |
| замер качества ответов | `RUN_TAG=v<N> python3 work/acceptance/run_acceptance.py <от> <до>` — через бота | прямой вызов `/ask` — он минует вики и даёт другой ответ. 🔴 Именно ему учит §0 `docs/ACCEPTANCE_UT.md`: тот раздел для замера качества не годится |
| загрузить/обновить **одну сущность** | `poc_load_entity.py <Имя>` — [замер] 4,6 с | `serene_sync.py` (все 1 588, ~40 мин) |
| подтянуть **изменения** из 1С | `serene_sync.py` | `pipeline.sh` |
| **продолжить досчёт векторов** | `embed_all.sh [потоков]` | `build.sh` (пересоберёт корпус) |
| пересобрать корпус после правки `corpus_build.sql` | `build.sh` | — |
| **полный такт** (данные изменились) | `pipeline.sh` или таймер | — |
| посмотреть полноту | `SELECT * FROM search_quality WHERE k LIKE 'cov_%'` | пересборка |

🔴 **У всех перечисленных инструментов умолчание — `dbname=postgres`, то есть ПЕРВАЯ база.**
Бот отвечает по второй (`ut_test`). Задавайте `SERENEDB_DSN` с явным `dbname` в каждой
команде: иначе «посмотреть полноту» молча покажет перепись чужой базы, а `build.sh` /
`embed_all.sh` потратят такт и деньги эмбеддера не на ту базу. Ошибки не будет ни на одном
шаге — числа просто окажутся из другой базы.

Крупный инструмент берётся, когда нужен **весь** его результат, а не когда он «заодно
сделает и это».

### Документы, заведённые 29.07

| Файл | О чём |
|---|---|
| [`docs/PLAN_ROW_IS_OBJECT.md`](docs/PLAN_ROW_IS_OBJECT.md) | 🔴 **строка корпуса ≠ объект 1С**: бот отвечал «216 партнёров» вместо 164. План из четырёх шагов, прошёл три независимых разбора. Сделаны шаги 1 и 2 (проверены на двух базах), шаг 3 закрылся шагом 2, **шаг 4 тоже сделан**: `coverage_build.sql` сравнивает корпус с числом объектов витрины и называет раздутие причиной («витрина развёрнута по табличной части» — 85 сущностей, «регистр: `$count` считает регистраторы» — 80). *Прежняя запись «шаг 1 сделан» отставала и от кода, и от собственного плана.* |
| [`docs/BOXED_BLOCKERS.md`](docs/BOXED_BLOCKERS.md) | что мешает работать на чужой базе: резолвер мёртв на не-кириллице, словарь индекса с русской локалью, остатки рукописного списка сущностей |
| [`docs/ACCEPTANCE_UT.md`](docs/ACCEPTANCE_UT.md) | приёмочный набор: 58 вопросов под 12 классов дефектов. **Эталонный SQL есть не у всех** (маркеров «SQL:» — 49, ещё у нескольких вопросов запрос дан без маркера): §5 «Уточнение вместо догадки» проверяет не число, а **вид ответа** (`kind`), а у №27 («На какую сумму?») нет ни SQL, ни эталона — ловушка на `clarify`. *Было ошибочно «с эталонным SQL к каждому».* 🔴 §0 того же файла («Как пользоваться») учит прямому `curl` на `/ask` — для замера качества этот путь запрещён, см. таблицу выше |
| [`docs/API_ASK.md`](docs/API_ASK.md) | контракт `/ask` для фронта |
| [`docs/REGRESSION_BASE1.md`](docs/REGRESSION_BASE1.md) | чем доказано, что первая база цела после каждой правки |

Проверка «не сломали ли первую базу» — [`docs/REGRESSION_BASE1.md`](docs/REGRESSION_BASE1.md):
там сведено, какой правкой и каким замером это доказано.

### Юниты systemd — их двадцать и они в ВОСЬМИ каталогах

Все двадцать под git, восемнадцать побайтно совпадают с `/etc/systemd/system`.

| Каталог | Что там |
|---|---|
| `ubuntu/systemd/` | `1c-serene-pipeline.service/.timer` (боевой такт), `1c-serene-index.service` (**единственная** копия, на `build.sh`), `1c-odata-gateway@.service` (шаблон на каждую базу) |
| `ubuntu/serenedb/systemd/` | `1c-serene-ask.service`, `1c-mcp-reports.service`, `1c-serene-sync.*` (двойник index удалён, Э5) |
| `ubuntu/openclaw/systemd/` | `1c-mcp-ask@.service` — **шаблон моста на базу; боевые — его инстансы** (`@ut_test` :6016, `@postgres` :6017). ⚠ `1c-mcp-ask.service` (одиночный) — СНЯТ 07.08: у него нет `MCP_PORT`, умолчание :6016 занято боевым инстансом, и рестарт по старому имени ставил его в петлю падений (36 980 рестартов за два дня). На стенде остановлен и disabled; удаление файла из `/etc/systemd/system` — root-шаг владельца. Также `openclaw-gateway-buh.service.d/env.conf` (drop-in второго шлюза), `1c-mcp-braine.service` + `override.conf` |
| `ubuntu/1c-gateway/systemd/` | `1c-odata-gateway.service` (первая база) |
| `ubuntu/1c-etl/systemd/`, `ubuntu/1c-config-ui/systemd/`, `ubuntu/monitoring/`, `ubuntu/serenedb/` | ETL, config-ui, монитор бота, юнит движка |

🔴 **`1c-serene-index.service` — одна копия.** Канон раскатки —
`ubuntu/systemd/1c-serene-index.service` (`ExecStart=/opt/1c-mcp-reports/build.sh`).
Мёртвый двойник в `ubuntu/serenedb/systemd/` удалён (Э5, 27.08): он звал
выведенный `serene_search_build.py` (снёс корпус 28.07). Контроль —
`test_systemd_execstart_lock.py`. Снятый таймер index (ночное `OnCalendar 03:55`)
жил только у двойника и удалён вместе с ним; такт — у `1c-serene-pipeline`.

*Было ошибочно: «Юниты systemd — в `ubuntu/systemd/`» — по этому адресу нет ни одного юнита
работающих сейчас служб (мост бота, сервис ответов, шлюз первой базы, монитор, движок лежат
в других каталогах). **Чем опровергнуто:** `find ubuntu -name '*.service' -o -name '*.timer'`
→ 20 файлов в восьми каталогах, все `git ls-files` TRACKED.*

### Ответы — `ubuntu/serenedb/serene_ask.py`

Единственный большой файл своего кода в контуре ответа. Что важно: поиск штатными
функциями (`ts_*`, `bm25`, `<=>`), выбор сущности **реранкером**, величину — **по вопросу**
(базовая предпочтительнее уточнённой), **калькулятор** (числа ставит код, не модель),
**резолвер** (слово → значение через отдельную роль), связка **уточнение → выбор `focus`
→ ответ**, честный **503 при сбоях**, видимое старение данных.

🔴 **Точка входа сервиса — `answer_checked`, а не `answer`** (05.08). Вокруг ответа стоит
шаг «достаточен ли вопрос для ответа» ([`serene_enough.py`](ubuntu/serenedb/serene_enough.py)):
дешёвая половина ДО поиска, решающая — ПОСЛЕ счёта. Шаг стоит **снаружи** `answer()`
намеренно: круг арбитра собирает ответы кандидатов тем же `answer(..., no_arbiter=True)`,
и проверка внутри превратила бы ответ кандидата в уточнение — сравнивать стало бы нечего,
и арбитр-детектор замолчал бы.

| Файл | Что делает |
|---|---|
| `code_map.py` + `docs/audit/zones.json` | **машинная карта** `serene_ask.py` по 20 зонам аудита (без нарезки модуля): зоны по якорям `start`[/`end`] (имена функций), строки — из AST; функции, граф вызовов, env, psql/ds_chat/embed_one/rerank/urlopen → `docs/audit/code-map.json` + `docs/CODE_MAP_ASK.md`. Замок `test_code_map.py` |
| `serene_enough.py` | **шаг «достаточен ли вопрос»**: вопрос указывает на ОДИН неназванный предмет из класса («сколько штук **товара** мы продали») — спрашиваем, что за товар и за какой период, словами человека. Вердикт собирает КОД: языковой факт от модели (`FACTS_SYS` только ОПИСЫВАЕТ вопрос) плюс число из базы «итог сложен больше чем из одной записи». Признак «поле разбора пусто» негоден сам по себе: «сколько у нас контрагентов» тоже без предмета и периода, и это полный вопрос. Выключатель `ASK_ENOUGH=0` |
| `test_enough.py` | оффлайн-проба шага: **75 случаев** без базы, сети и вызовов модели, включая связку с сервисом (молчит при `focus`/`measure`, не трогает отказ и чужое уточнение, не роняет сервис без файла) |
| `ask_journal.sql` + `ask_journal_apply.sh` | **журнал исходов** (шаг 5): таблица `ask_journal`, GRANT INSERT/DELETE/`SELECT(id)` для `serene_ro`; `corpus_init.sql` не трогает |
| `test_ask_journal.py` / `ask_journal_label.py` | замки журнала (17) и разметка selftest-потока прямой SQL-сводкой |
| `ask_choice_memory.sql` + `ask_choice_memory_apply.sh` | **память явного выбора** (шаг 6, shadow): таблица `ask_choice_memory`, GRANT INSERT/SELECT/UPDATE для `serene_ro`; клик не пишет |
| `ask_choice_memory_collisions.sql` | SQL-отчёт коллизий классов памяти |
| `ask_choice_mem.py` / `test_ask_choice_memory.py` | ключ класса, remember/forget (только поле memory), shadow в diag; замки 48 |
| `partial_visible.py` / `test_partial_flag_propagation.py` | **п. 13 / Э4:** `ensure_partial_visible` — LOSS/budget/uncounted из атома и diag → `partial`+хвост `text`. Проводка в `serene_ask.answer_checked` — патч оркестратора (`COMPLETENESS_P13.md` §11). Замок 27.08: **19/1** (S6 undated-гейт) |
| `work/acceptance/selftest_*.py` | самопроверка из данных (ярус 1 — поверхности без модели; ярус 2 — живой `/ask` + сверка атома SQL). База замка 17 — `docs/SELFTEST_BASELINE.md`; регресс промахов — `selftest-misses-*-tier2.jsonl` |

### Проверка

| Файл | Что делает |
|---|---|
| `golden.sh` + `golden-questions.txt` | приёмочный набор, 8 вопросов, гоняется после КАЖДОЙ правки |
| `ab_scorer.py` | второй набор: 8 вопросов, истина считается независимым SQL по источнику. 🔴 **С 02.08 привязан к именованной базе**: `AB_BASE` (умолчание `postgres`) задаёт и env `/etc/1c-serene-ask-<база>.env`, и юнит `1c-serene-ask@<база>`, и порт. Прежняя редакция писала `ASK_SCORER` в общий env, где его перекрывает побазовый, и рестартовала нешаблонный юнит — то есть либо падала, либо мерила неизменённый скорер |
| 🔴 `test_gate.py` | **оффлайн-прогон гейта: без базы, без сети, без вызовов модели** — 40 случаев (`gate`, `check_claims`, `asked_figure_missing`, `prompt_leak`, `stale_note`, токенайзер). Единственный прибор, которым правки гейта доказываются бесплатно и воспроизводимо: приёмка через модель стоит денег и даёт разброс ±1 верный ответ из десяти. Парный на стороне бота — `ubuntu/openclaw/verify-plugin/test-verify.mjs`, 118 случаев |
| 🔴 `test_intent.py` | **оффлайн-прогон шага 1 «разбор вопроса»: без базы, без сети, без вызовов модели** — 135 случаев. Модель подменяется заготовленными ответами, и проверяется, что ЛЮБОЙ из них даёт либо структуру заданных типов с перечнем потерь, либо честный отказ. На коде до 04.08 те же пробы дают шесть падений |
| `work/acceptance/intent_parse_bench.py` | живой прибор шага 1 по 58 вопросам приёмки: зовёт рабочую `parse_intent`, меряет устойчивость разбора, потери условий, домыслы и сверяет разбор с разметкой `intent_cases.tsv`. Окружение сервиса читает сам (`ASK_ENV_FILES`, разбор как у systemd) |
| 🔴 `test_step4_guards.py` | **оффлайн-проба защит шага 4: без базы, без сети, без вызовов модели** — 51 случай. Формы правил вынуты отдельными функциями (`alias_supported`, `figures_numbers`, `same_number`, `not_for_excludes`, `pair_unanswered`) именно затем, чтобы их таблица истинности читалась и проверялась бесплатно, а выбор между формами делался замером, а не спором |
| 🔴 `work/entity-choice/alias_rank_bench.py` | прибор **словаря синонимов**: на каком месте эталонная сущность в его ранжировании по каждому из 44 вопросов. Без модели и без денег — секунды, поэтому редакции словаря сравниваются сколько угодно раз, а платная приёмка снимается один раз в конце. `ALIAS_TABLE`/`ALIAS_INDEX` выбирают редакцию, `SCORERS=tfidf,bm25` — линейку |
| 🔴 `work/entity-choice/not_for_probe.py` | прибор правила **«не отвечает»** (`not_enough_for`): какая форма отсева кандидатов ловит подмены соседей и не трогает эталоны. Восемь форм, без модели и денег; пары подмен читает из `SPEC_RETAIL_VS_SALES.md`, основы считает движок. По нему выбрана боевая форма отсева в `serene_ask.py` |
| `work/entity-choice/answer_rate_probe.py` | прибор **доли ответов** (п. 21): 44 вопроса через живой `/ask`, полный ответ построчно в JSONL. Вердикт берёт у `step4_bench` — линейка одна на оба прибора |
| `work/entity-choice/answer_rate_what_if.py` | по тому же следу считает **поимённо**, какая проверка съела какой ответ и чем кончилась бы ДРУГАЯ форма правила. Без единого вызова модели: платный прогон снимается один раз, формы сравниваются задним числом |
| `work/acceptance/intent_cases.tsv` | **разметка 58 вопросов** — что шаг 1 обязан вынуть из текста: `want`, значения для `terms`, период, порог, величина, основы `kind`. Это разметка ВОПРОСА, а не базы, поэтому годится на любой базе |
| `.claude/hooks/test-hooks.sh` | проба самих хуков: опознаватели, git-гейт, Claude, Kimi и Cursor (адаптер + сторож) — на временном репозитории в `/tmp`. Без сервера и базы |

🔴 **Последний рубеж — `.githooks/pre-commit`** (поставлен владельцем 02.08): git зовёт его
**всегда**, кто бы ни коммитил — сессия любого типа, человек, Cursor. Он прогоняет три
детерминированные проверки (документы, граф, размер `activeContext`) и **блокирует** коммит,
а не спрашивает. Включение на новом клоне — `git config core.hooksPath .githooks`; осознанный
обход — `git commit --no-verify`; коммит целиком из `windows/odata-setup` пропускается (трек
владельца). Проверяется настоящими коммитами в `test-hooks.sh` — `--dry-run` хуки не зовёт.

🔴 **Хук может быть исправен и при этом не вызываться** (`[замер 02.08]`, `HOW_NOT_TO §1.42`).
В дочерней сессии нет `CLAUDE_PROJECT_DIR`, а регистрация вида
`"$CLAUDE_PROJECT_DIR/.claude/hooks/…"` схлопывается в `/.claude/hooks/…` — файла нет,
движок молчит, коммиты идут без единой проверки. Регистрация теперь через
`${CLAUDE_PROJECT_DIR:-$(git rev-parse --show-toplevel || pwd)}`, а корень репозитория хук
ищет в том числе **от своего собственного места**. Проверка, что хук зовут, — метка в
первой его строке; журнал `.claude/hooks.log` показывает, чьи коммиты через него прошли.

🔴 **Cursor — своя проводка, не settings.json** (`[замер 13.08]`, `docs/PLAN_CURSOR_GATES.md`).
Matcher `Bash` из Claude-конфига на команды `Shell` не сработал. Канон:
`.cursor/hooks.json` (гейты через `.claude/hooks/cursor-wrap.sh`) и `.cursor/mcp.json`
(`serenedb-docs`, `memory`). Файлы — root:root, сторож сверяет содержимое.
Claude и Kimi этот слой не читают. Снайпер `kimi -p` в Cursor **не** подключается
(`[замер]` 403 квоты Kimi). `check-golden`: scp/rsync в `/tmp` не выкат; мёртвый
эмбеддер — строка **с начала** HEAD `Золотой: невозможен …` (середина сообщения не закрывает). Последний рубеж git — общий на три движка.


### Выведено из контура, не трогать

`serene_search_build.py` — прежняя питоновская сборка. 🔴 **Запускать нельзя**: при
нынешнем составе корпуса она сносит таблицу и индекс (авария 28.07).
`serene_report.py`, `mcp_reports.py` — инструмент `report_1c`, решено вывести. ⚠ Решение не
исполнено: юнит `1c-mcp-reports` жив и подключён к боту записью `second-brain-reports`.
`build_resolver_index.py` — прежняя сборка резолвера, заменена на `resolver_build.sql`.
`ubuntu/1c-etl/` и `ubuntu/1c-config-ui/` — контур ETL→KB кормил braine; таймер `1c-etl`
`disabled`. ⚠ Сервис `1c-config-ui` при этом **жив и enabled**, но его выход
`/etc/1c-etl-selected.txt` не читает никто: боевая сборка берёт состав из переписи
`$metadata`. Гасить или переводить на боевой отбор — решение владельца (вопрос №47).

🔴 **Выведенное по репозиторию ≠ выведенное на стенде.** В `/opt/serenedb-tools` лежит
`report_query.py` (5 818 байт, 23.07) — работоспособная программа «NL→запрос по витрине»,
отдающая схему в DeepSeek: ранняя версия пути, от которого владелец отказался. Никем не
запускается, но стартует одной командой. Уборка — решение владельца (вопрос №44).

---

## 3. Документы: какой о чём

### Закон и правила

| Файл | О чём |
|---|---|
| `TARGET.md` | **контракт результата, 21 пункт.** Менять только по слову владельца. При расхождении прав он |
| `CLAUDE.md` | правила работы: что читать, куда писать, когда коммитить. Читается автоматически каждую сессию |
| `memory_bank/owner-memory/` | решения владельца, которые нельзя пересматривать |

### Состояние и история

| Файл | О чём |
|---|---|
| `memory_bank/activeContext.md` | **что делаем сейчас и с чего начать.** Первое, что читать после этой карты |
| `CHANGELOG.md` | что менялось, почему и **чем доказано**. Новое сверху, метки `[замер]`/`[код]`/`[решение]` |
| `docs/SERVER_HANDOFF.md` | состояние сервера: службы, таймеры, что не доделано |
| `docs/HOW_IT_WORKS.md` | **как это работает и почему именно так**: путь вопроса, гейт, вики, что уходит наружу. Обязателен при любой правке поведения бота (`CLAUDE.md`) |

### Опыт и ловушки

| Файл | О чём |
|---|---|
| `docs/HOW_NOT_TO.md` | **на чём уже спотыкались** (94 разбора). Формат «что сделал не так → чем кончилось → как надо». Начать с §0 |
| `memory_bank/techContext.md` | **ловушки движка** (таблица «что важно знать» + **22 нумерованных разбора, номера 15–35** (под номером 35 их два — «Ловушка» и «Возможность»)), всё проверено замером: что он умеет, что роняет, что молчит. *Было ошибочно «разборы 15–22» — за границей описания оставались четырнадцать, среди них Ловушка 25 (`duckdb_tables` отдаёт таблицы всех присоединённых баз), 26 (секреты движка общие на инстанс), 28 (`ai_embed` занимает исполнителя), 33 (сравнение колонки из LEFT JOIN в фильтре молча не применяется), 35 (булев реквизит)* |

### Знания о SereneDB (движок)

| Файл | О чём |
|---|---|
| `docs/SERENEDB.md` | что за движок, его возможности — главный файл про SereneDB |
| `docs/SERENE_CAPS_1..3.md` | аудит возможностей: какие функции есть в нашей сборке 26.07.3 |
| `docs/VECTOR_DECISION.md`, `docs/SERENEDB_IVF_ISSUE_RU.md` | почему векторный индекс `ivf` заблокирован |
| `docs/AUDIT_SERENE_SUMMARY.md`, `docs/BENCH_BRAINE_VS_SERENE.md` | аудит и замеры движка |

🔴 **Перед тем как писать свой SQL/код поверх базы — сходить в официальные доки движка через
MCP `serenedb-docs` (`search_docs` → `read_section`) и при сомнении спросить агента
`serenedb-native`**, а не гадать. Правило п. 20 `TARGET.md`. Найденное в доках обязательно
проверяется на живом инстансе: доки описывают текущую версию, у нас сборка 26.07.3.

*Было ошибочно: «спросить агента `serenedb-native` (у него полный клон исходников движка)».
**Чем опровергнуто:** агент переведён на официальные доки —
`.claude/agents/serenedb-native.md` объявляет источниками `mcp__serenedb-docs__*` и живой
движок и прямо говорит: «Прежний источник — локальный клон исходников `serenedb-src` —
больше не используется»; то же в `CLAUDE.md`. Указание владельца 01.08: «для доков SereneDB
используй mcp сервер, который уже настроен, не нужно изучать доки сырые».*

### Планы и разборы

| Файл | О чём |
|---|---|
| 🔴 `work/doc-audit/DEV_PLAN.md` | **ведущий документ этапа после сдачи**: очерёдность работ по остатку аудита, десять кластеров Э0-Э9, у каждого — чем проверяется. Здесь же список того, что ждёт владельца |
| `work/doc-audit/DISCREPANCIES.md` | сам аудит доков (01.08): 285 расхождений — 53 дефекта кода и стенда (б), 41 вопрос (в), 191 находка по документам (а) |
| `work/doc-audit/QUESTIONS_TO_OWNER.md` | 47 развилок с решением разработки под каждой («РЕШЕНИЕ (разработка): … Основание: …») и местом для ответа владельца |
| `docs/PLAN_MVP_PACKET_TRANSPORT.md` | **MVP без VPN**: Windows шлёт zstd+AEAD-пакеты (full, потом дельта по `DataVersion`); Ubuntu — витрина и сборка. План 06.08, кода ещё нет |
| `docs/PLAN_FIRST_DUMP_SAFE.md` | 🔴 **первая полная выгрузка не раздувает диск 1С** (13.08, klient-1). Проверка по коду: очередь агента — нет; «GET → `.ldf` 100 ГБ» — гипотеза. Порции отправки + меньше GET; бутстреп-отсев служебного из снимка **невозможен**. Кода ещё нет |
| `docs/PLAN_PROD_LXC.md` | 🔴 **прод-контур: свой чистый LXC на компанию** (решение 08.08). Этот сервер = dev/стенд/стейдж. Заведение компании — одна команда `new-company.sh` + передать комплект. Этап 1 — per-base DSN в apply (иначе две базы сольют таблицы в одной СУБД) |
| `docs/PRODUCTION_PLAN.md` | все работы до продакшена, по вреду для клиента |
| `docs/PREPARE_BASE.md` | подготовка базы как отдельный шаг — направление, заданное владельцем 28.07 |
| `docs/SCALE_BLOCKERS.md` | что растёт с размером базы |
| `docs/CHECKLIST_SEARCH_FIX.md` | правки поиска, что сделано и что отклонено |
| `docs/VECTOR_DECISION.md`, `docs/SERENEDB_IVF_ISSUE_RU.md` | вектор: почему `ivf` заблокирован |
| `docs/RUNBOOK_DEPLOY.md`, `docs/ARCHITECTURE.md` | развёртывание и полная картина слоёв |

### Исследования (читать при необходимости, не подряд)

🔴 **Это ИСТОРИЧЕСКИЕ отчёты, сверяться с ними как с состоянием нельзя.** Семейство
`CHECK1_*`/`CHECK2_*` снято на корпусе 97 965 строк с `emb FLOAT[1536]` — сегодня 103 808 и
`FLOAT[1024]`; написаны они в настоящем времени, и отличить действующее от отменённого по
самому файлу нельзя.

- `docs/CHECK1_*`, `docs/CHECK2_*` — проверки тремя агентами со сводками. ⚠ Из двух сводок
  `CHECK2_INCR_SUMMARY.md` **забракована ревизорами**, действующая — `..._V2.md` (разбор в её §0).
- `docs/AUDIT_CODE_*`, `docs/SERENE_CAPS_*` — аудиты кода и возможностей движка. ⚠ Приём
  `MERGE … RETURNING merge_action` из них **не сработает на целевой архитектуре**
  (`ARCHITECTURE.md`, врезка).
- `docs/HANDOFF_CHECK_*` — проверки передаточных документов.

---

## 4. Где что живёт на сервере

| Что | Где |
|---|---|
| репозиторий | `/srv/1c` — работаем здесь |
| боевой код сборки и ответов | `/opt/1c-mcp-reports/` — раскладывает `deploy.sh` (руками не копировать) |
| 🔴 рабочая папка бота | `/home/undebot/.openclaw/workspace` — сюда `deploy_instance.sh` кладёт персону, отсюда её читает бот. **Именно её отсутствие в карте стоило двух суток:** правки промта не влияли ни на один ответ, потому что бот читал другую копию |
| хранилище вики | `/home/undebot/.openclaw/wiki/main` — 698 страниц сущностей (плагин `memory-wiki`, `vaultMode: isolated`); по ней бот перефразирует вопрос ДО обращения к данным |
| остальные рабочие каталоги | `/opt/1c-bot-monitor`, `/opt/1c-config-ui`, `/opt/1c-etl`, `/opt/1c-gateway`, `/opt/1c-odata-gateway`, `/opt/openclaw`, `/opt/openclaw-engine`, `/opt/openclaw-mcp` (venv питона всех юнитов). В `/opt` есть и остальное — дистрибутив движка, остатки braine, `serenedb-tools`; полный список смотреть `ls /opt`, здесь только наши |
| секреты | `/etc/*.env`, права 600, в git не входят |
| данные движка | `/var/lib/serenedb` — 12 ГБ: `engine_duckdb` 11 ГБ, `engine_search` 159 МБ, подкаталог второй базы `ut` 786 МБ |
| выгрузка 1С | там же, `/var/lib/serenedb/*.csv` — загрузчик пишет CSV и **после `read_csv` не удаляет**: 1767 файлов на 826 МБ, старейшие от 24.07. Растёт с размером базы; при планировании места это первое, что надо учесть |
| ~~исходники движка~~ | ~~`/srv/serenedb-src`~~ — **больше не источник**: агент `serenedb-native` переведён на MCP `serenedb-docs`. Каталог на диске ещё лежит и путает |

**Опись копий витрины** (её раньше не было вовсе, и «резерв и возврат» держался на памяти сессии):

| Где | Что | Дата |
|---|---|---|
| `/root/backups/serenedb/serenedb-20260729-044937.tar.gz` | единственный штатный снапшот, 2,7 ГБ. 🔴 Снят **до** `DROP DATABASE ut_test` — восстановление по нему откатит боевую вторую базу | 29.07 |
| `/var/lib/serenedb-rescue-20260727` | 2,0 ГБ, `store.db` + два WAL — след аварии 27.07, не значился ни в одной описи | 27.07 |

**Подключение к базе:** `psql 'host=127.0.0.1 port=7890 user=postgres dbname=<база>'` на
запись, роль `serene_ro` — на чтение. 🔴 **`dbname` указывать явно всегда:** первая база —
`postgres`, боевая вторая — `ut_test`; без `dbname` libpq берёт имя базы равным имени роли и
молча уводит вас в первую. Перепутать роль = получить `permission denied` и решить, что
нашёл дефект.

⚠ **Рядом с боевыми таблицами лежат остатки опытов — в базе `postgres` их 56 сверх
предметных.** Часть описана (`HANDOFF_CHECK_1/2`, `HOW_NOT_TO`), часть не упомянута нигде:
`base1_aff`, `base1_before`, `probe_*`, `rb_first`, `step2_first`, `res_chunk`, `res_dim`,
`res_emb_0…11`, а из рабочих — `cov_mart`, `cov_mart_obj`, `base_profile_meta`, представления
`wiki_pages`, `wiki_entity_facts`. Списки по базам разные. Отбор источников идёт по
`$metadata`, поэтому в корпус остатки не лезут, но боевое от мусора по виду не отличить.
Опись и уборка — решение владельца (вопрос №44).
*Попутно: `HOW_NOT_TO §2.9` про аварию 28.07 ссылается на страховочный набор, от которого
пропали `emb_done`/`emb_part_*`; `tmp3_corpus`, `res_val` и `res_emb_*` живы.*

---

## 5. Состояние [замер 01.08]

🔴 **Стадии по каждому пункту контракта живут в [`docs/TARGET_STATUS.md`](docs/TARGET_STATUS.md)
— здесь только устройство и числа.** Раздел раньше назывался «Состояние на вечер 28.07» и
не менялся с 28.07, хотя сам файл правился 29, 30 и 31.07: читающий по порядку получал из
MAP «оба набора 8/8, крупного без внешнего не осталось», а из `TARGET_STATUS` следующим
файлом — «на 58 вопросах верных меньше половины» и четыре 🔴.

**Числа [замер 01.08]:**

| | первая база (`postgres`) | боевая вторая (`ut_test`) |
|---|---|---|
| корпус | 103 808 строк / 231 сущность | 623 565 строк / 1502 сущности |
| резолвер `resolver_index` | 115 151 значение | 188 068 |
| строк без вектора | 5 091 — все служебные, по решению; очередь пуста | ~~340 464 (55 %), из них 92 080 деловых в очереди~~ → `[замер 03.08]` **очередь пуста** (`cov_noemb_pending`=0), без вектора 257 991 — все служебные, по решению |

*Было ошибочно: «вектор 1024 у всех» — не у всех: служебные сущности не векторизуются по
решению 29.07 (`cov_noemb_service`), и это видно штатно в `search_quality`. И «оба набора
8/8» — 8/8 давал лёгкий golden-набор регрессии первой базы, а не приёмка из 58 вопросов.*

**Свежесть по готовности — так устроено, но 🔴 сейчас НЕ РАБОТАЕТ.** [замер 02.08]
`1c-serene-pipeline.service` и `1c-serene-sync.service` — оба в `failed`, последний такт
29.07 06:41, таймер `enabled`, но `inactive`. Пока конвейер не поднят, данные стареют и
п. 17 контракта не выполняется. Устройство: синк-дельта → сборка → повтор через
`OnUnitInactiveSec`; синк тянет из 1С **только изменения** по `DataVersion`, «ночного» такта
нет. Старые таймеры `sync`/`index` отключены; юнит `index` — одна копия в
`ubuntu/systemd/`, на `build.sh` (не на снёсший корпус `serene_search_build.py`).

**Что дальше:** вторая конфигурация 1С (УТ 11.4.9) **пройдена и стала боевой** — по ней
идут все ответы бота, без единой правки кода; п. 9 закрыт до остатка «другой язык».
Открытым по этой части остаётся п. 15 (приёмка из базы клиента — владелец: делаем в конце).
Главная незакрытая проблема — **выбор сущности**: все неверные ответы одного рода, счёт при
этом верен всегда. Дальше — решение владельца о периметре (п. 16) и инсталлятор (п. 14).

*Было ошибочно: «Упирается во вторую конфигурацию 1С (п. 9, 15)» и «крупного без внешнего
не осталось». **Чем опровергнуто:** боевой путь :6016 → :8099 → `dbname=ut_test`;
`TARGET_STATUS` п. 9 — «проверено на чужой конфигурации, всё без правок кода» с 28.07.
Сессия, открывшая MAP первым файлом, могла начать «подключать вторую базу», которая уже
работает.*

**Пайплайн синка данных (порядок слоёв):**

```
1С (OData) ──синк-дельта (serene_sync + poc_load_entity)──► витрина (таблицы 1С)
                                                               │
                                              build.sh ────────┘
                                                 corpus_init → precheck → corpus_build →
                                                 corpus_merge → embed → resolver_build →
                                                 coverage_build → postcheck ──► корпус+индекс+резолвер
```
