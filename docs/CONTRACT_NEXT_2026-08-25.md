# Следующий ход контракта ответа — 25.08.2026

**Задача:** сверка [`PLAN_ANSWER_CONTRACT.md`](PLAN_ANSWER_CONTRACT.md) с фактом
на дереве. Код и сам план **не** менялись этим документом.
**Мерило:** TARGET пп. 3 / 9 / 10 / 12 / 13 / 21; рядом —
[`PLAN_UPGRADE_NATIVE.md`](PLAN_UPGRADE_NATIVE.md) Ф6 / ворота 6,
[`SCORER_CLASSES_2026-08-25.md`](SCORER_CLASSES_2026-08-25.md),
проекты осей и формы.

Снято с git (`git log --since=2026-08-16` по `serene_ask.py` /
`PLAN_ANSWER_CONTRACT.md`) и с уже записанных замеров в docs/work —
**без** похода на `:8091`/`:8092` и без живого SQL в этой сессии.

---

## 1. Таблица шагов §8

Все восемь пунктов в плане помечены ✅ (15–17.08). Ниже — что из этого
держится **числом после 23.08**, что только кодом/старым замком, что
**притворяется** закрытым.

| Шаг | План | Статус 25.08 | Доказательство |
|---|---|---|---|
| **0** Неполнота okna | ✅ 15–16.08 | **закрыт числом** | `d84564a` + корпус; после 23.08: `/health` **200**, gap **0/0**, бой md5 **11d8f158** ([`REGRESSION_BASE1`](REGRESSION_BASE1.md) 24.08). AB_PROBE 8/8 — **не** доказательство п. 13 |
| **1** Детектор | ✅ 15.08 | **закрыт только кодом** (+замер 15.08 ut_test) | Коммиты `0d50ca1`/`b1e3554`/`d84564a`. После 23.08 **нет** живого замка «обе ветки без сомнения» на полном gold: scorer бой/стейдж **15/24**, класс K4/K6 = пространство R без нужных форм ([`SCORER_CLASSES`](SCORER_CLASSES_2026-08-25.md)) |
| **1.5** Самопроверка | ✅ 17.08 | **закрыт числом (17.08), не перемерён** | `bbced7a`, [`SELFTEST_BASELINE`](SELFTEST_BASELINE.md) ярус 1 **0/178, 0/697, 0/254**. После 23.08 перегона яруса 1 в git **нет** |
| **2** AnswerAtom + renderer | ✅ 16.08 | **закрыт только кодом; дыра выдачи чинилась отдельно** | `build_answer_atom`/`render_atom_pair`. [замер 25.08] K5: atom **307** / compare **1 755 883** считались, наружу clarify/refuse — до `ASK_ATOM_TERMINAL` (`9d6d788`); бой флаг **не** включён |
| **3** `decision_id` | ✅ 16.08 | **закрыт числом (косвенно)** | Код билетов 16.08; [замер 25.08] в `ask_journal` **ticket_used=104** при **1445** строк ([`MEMORY_COLLISIONS`](MEMORY_COLLISIONS_2026-08-25.md)) — клики живы |
| **4** A/B/C + словарь §7 | ✅ 16.08 | **дыра / притворяется закрытым** | Код `ASK_FORK_OUTCOMES` + таблицы `search_fork_class`/`search_fork_label` (`corpus_init.sql`, `branch_alias.sh`). [замер 25.08] `search_fork_label` okna **10 475** строк — таблица **не** пуста; day-basis-ключи подписей пусты (отдельный контур). Исходы B/C в журнале **142**, но эталоны K4/K6 требуют форм, которых в R₀ нет → «шаг 4 закрыт» ≠ «п. 12/21 закрыты» |
| **5** Shadow / `ask_journal` | ✅ 17.08 | **закрыт числом** | `589beb0`; [замер 25.08] журнал **1445** строк, интервал 18–24.08 (`MEMORY_COLLISIONS`) |
| **6** Память выбора | ✅ 17.08 SHADOW | **закрыт только кодом (SHADOW); бой — дыра** | `e525b7f` / `9f86370`. [замер 25.08] `ask_choice_memory` **0** строк при journal **1445**, B+C **142**, ticket_used **104** → замер коллизий **непоказателен**, `ASK_MEMORY_APPLY` в бой **нельзя** |

**Сводка счёта:** закрыто числом (с живым следом ≥23.08 или не опровергнуто) — **3**
(шаги **0**, **3**, **5**); закрыто только кодом / устаревшим замком — **3**
(шаги **1**, **1.5**, **2**); дыры / притворство — **2** (шаги **4**, **6** бой).
Шаг **1.5** в «только код» не кладём: число 17.08 есть, но после 23.08 не
подтверждено — в таблице выше отдельной строкой.

Дополнительно к §8 (не шаги плана, но рядом):

| Тема | Статус | Доказательство |
|---|---|---|
| §7bis календарь | код шагов 1–3 в дереве; **live no-op** | `8a43d2f`/`7c8564c`; `ASK_CALENDAR_AXIS`; замки 30/0 + 36/0; `search_meta.calendar_*` на okna **пусты** (пакетный контур / корпус в `/opt` без карты) — записано в TARGET_STATUS п. 12 и REGRESSION 24.08 |
| §7bis валюта | **только проект** | `4855500`, [`work/currency-axis-design.md`](../work/currency-axis-design.md); код ask **нет**; §7 исход **B** (Δ **375 437** на 40 FX-шапках) |
| K6+K4 форма | **только проект** | [`work/entity-compare-form-design.md`](../work/entity-compare-form-design.md); `ASK_ENTITY_FORM` умолч. 0; промт `.claude/state/prompt-k6k4-impl.md` |
| K1+K2+K3 / K5 | код+замер на стейдже/probe | `7ba1946` / `9d6d788`; бой `:8091` флаги **выкл.** |
| Ф6 / ворота 6 | **не закрыты** | Ф6.1 в бою; 6.2–6.4 на `:8092`; ворота 6: гибрид на **обеих** базах + рост смысловых + снятие python-RRF — **нет**. Scorer 25.08: бой=стейдж **15/24**, VERDICT_DIFFS **0** |

---

## 2. Три следующих хода (порядок планов)

Без починки отдельных вопросов gold. После свободного `serene_ask.py`
(сериализация выкатов — оркестратор).

1. **Форма ответа K6+K4** — [`work/entity-compare-form-design.md`](../work/entity-compare-form-design.md),
   промт `.claude/state/prompt-k6k4-impl.md`. Ввести в R формы
   `distinct_axis` / `complement` / `compare` за флагом; исходы на полном
   пуле форм. Закрывает класс скорера, не три строки TSV. Блокировка:
   **наша** (очередь на файл ask), не внешняя.

2. **Валютная ось C1** — [`work/currency-axis-design.md`](../work/currency-axis-design.md)
   §7 исход **B**, промт `.claude/state/prompt-currency-impl.md`.
   Порядок дизайна: `search_meta` currency_* → `ASK_CURRENCY_AXIS` →
   замки+словарь+скорир. До оси на окне с FX нельзя молча смешивать
   валюты в одном atom (п. 13). Блокировка старта кода: **наша**
   (после K6+K4 / свободного ask); карта meta — тот же контур корпуса,
   что и календарь.

3. **Календарь и словарь развилок**
   - **Календарь:** код в дереве, ось **внешне** no-op, пока
     `calendar_*` пусты — пакетный контур не отдаёт `$metadata` /
     карта не в выкаченном `corpus_build` на okna. Включать
     `ASK_CALENDAR_AXIS` на бою до карты **бессмысленно**. Починка
     контура meta / выкат корпуса — **внешний/выкатной** блок относительно
     ask-кода.
   - **Словарь §7:** таблицы и код чтения/очереди **есть**; наполнение
     `search_fork_label` на okna **непустое** (10 475). Дыра — не
     «таблицы нет», а (а) подписи новых классов осей (day-basis /
     amount-basis) не генерируются в такт; (б) шаг 4 плана считается
     закрытым при неполном R. Наполнение агентом — **наш** контур
     (`branch_alias.sh`); пустые day-basis — **наша** очередь после
     живой оси.

---

## 3. Что нельзя делать

| Запрет | Почему |
|---|---|
| Чинить «топ-3 за вчера» / любой одиночный FAIL gold отдельно | Класс лечится формой/осью (K1–K6, C1), не строкой TSV; иначе HOW_NOT_TO «класс-ветка» |
| Включать бой `:8091` (новые флаги Ф6 / calendar / atom / rank / memory) | Слово владельца / чеклист Ф6.5; стейджинг и probe — да, бой — нет |
| Принимать AB_PROBE **8/8** как закрытие п. 13 (или пп. 3/10/21) | Лёгкий набор 8 вопросов; полный scorer **15/24** при том же контуре; п. 13 = молчаливая потеря / неполнота до клиента, не «проба зелёная» |
| Включать `ASK_MEMORY_APPLY` по замеру коллизий 25.08 | `memory_total=0` — непоказательно |
| Переписывать `PLAN_ANSWER_CONTRACT.md` под текущий статус | Статус живёт здесь; план — рамка |

---

## 4. TARGET (срез) и что притворяется закрытым

| Пункт | TARGET_STATUS | Связь с планом |
|---|---|---|
| **3**, **10** | 🔴 | План помечает шаги 0–6 ✅; scorer и K4/K6 показывают: детерминизм/ошибки сущностей **не** закрыты |
| **9** | 🟡 | Оси за meta — ок для коробки; packet-okna без `$metadata` ломает «из коробки» на боевой базе |
| **12** | 🟡 | A/B/C в коде есть; «не та сущность молча» на K6.2 = догадка |
| **13** | 🟡 | Health/gap зелёный ≠ полнота ответа клиенту (K5 до флага; смешение валют без unit) |
| **21** | 🟡 | Отказ/clarify при данных (K5/K6); AB_PROBE не мерило |

**Притворства (явный список):**

1. «§8 шаги 0–6 закрыты» → закрыты как **механизм волны 15–17.08**, не как
   контракт TARGET 3/10/12/13/21 на okna 25.08.
2. «Шаг 4 + словарь §7 закрыт» → таблицы и рендер есть; **полнота пространства
   прочтений** и подписи осей — нет.
3. «Шаг 6 закрыт» → только SHADOW; бой заблокирован пустой памятью.
4. «Календарная ось готова» (код+замки) → на okna **no-op** без `calendar_*`.
5. «AB_PROBE 8/8 / Ф6 на стейдже» → паритет с боем, **0** сдвигов вердикта
   полного набора; качество FAIL не чинит.

---

## 5. Источники (git / docs, без сервера)

- `git log --since=2026-08-16 -- serene_ask.py PLAN_ANSWER_CONTRACT.md`
  (в т.ч. `8a43d2f`, `4855500`, `7ba1946`, `9d6d788`, `9c0e7d8`, `7c8564c`).
- [`PLAN_ANSWER_CONTRACT.md`](PLAN_ANSWER_CONTRACT.md) §2, §7, §7bis, §8.
- [`PLAN_UPGRADE_NATIVE.md`](PLAN_UPGRADE_NATIVE.md) Ф6, ворота 6.
- [`TARGET_STATUS.md`](TARGET_STATUS.md) пп. 3/9/10/12/13/21.
- [`SCORER_CLASSES_2026-08-25.md`](SCORER_CLASSES_2026-08-25.md),
  [`MEMORY_COLLISIONS_2026-08-25.md`](MEMORY_COLLISIONS_2026-08-25.md),
  [`REGRESSION_BASE1.md`](REGRESSION_BASE1.md) (calendar_* пусто; fork_label 10475).
- `work/calendar-axis-design.md` (статус: код в дереве, бой не включён),
  `work/currency-axis-design.md` (шапка + §7 исход B),
  `work/entity-compare-form-design.md` (шапка).
- Grep: `search_fork_*`, `fork_label`, `ask_choice_memory` в
  `serene_ask.py` / `corpus_init.sql` / `ask_choice_memory.sql` /
  `branch_alias.sh`.
