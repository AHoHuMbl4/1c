# Продуктовый аудит (взгляд снаружи) — 25.08.2026

**Роль:** сторонний инженер; мерило — [`TARGET.md`](../TARGET.md).  
**Угол:** обещание и клиент, не устройство кода. Внутренние аудиты
[`COMPLEXITY_AUDIT_2026-08-25.md`](COMPLEXITY_AUDIT_2026-08-25.md) и
(если появится) корректности — не повторяю; при совпадении — одна строка.  
**Режим:** только чтение кода/доков/замеров; серверы не трогал;
`ubuntu/serenedb/serene_ask.py` только читали чужие документы (файл правит
другой агент).  
**Дата среза:** 25.08.2026 по документам в дереве.

---

## 1. Расхождение слов и дела

Сверка: формулировка пункта в `TARGET.md` → стадия в
[`TARGET_STATUS.md`](TARGET_STATUS.md) (обновлён 25.08) → факт из замера/кода
в git. Правило статуса: «🟢 только по замеру» — принято.

| # | Заявлено (`TARGET_STATUS`) | Факт | Доказательство | Вердикт |
|---|---|---|---|---|
| 1 | 🟡 продакшен = сумма остального | Согласуется: эксплуатация под нагрузкой не замерена | `TARGET_STATUS` п.1 | расхождений нет |
| 2 | 🟡 «отвечает», но не «верно»; в тексте ещё **8/25** (23.08) | На бою okna полный набор **15/24**; со стейджа rank-канон **19/24** | `SCORER_CLASSES_2026-08-25.md` §2 / §0; `REGRESSION_BASE1.md` 25.08 | **стадия верна**; **обоснование устарело** (8/25 → 15–19/24) |
| 3 | 🔴 нет детерминизма / много ошибок | Подтверждаю: бой **15/24**, стейджинг без rank = тот же; с `ASK_SALES_RANK_CANON` **19/24** ≠ «без ошибок» | `SCORER_CLASSES` §2–§3; `TARGET_STATUS` правка 25.08 «стадии не повышали» | стадия верна |
| 4 | 🟢 «1С + PostgreSQL на Windows», путь `C:\1c\bases\buh_test` | Контракт (`TARGET` п.4) ещё требует PG на Windows. Решение владельца **22.08** слой PG **отменило**; живая схема — OData → SereneDB | `TARGET.md` п.4; `owner-memory/project-architecture-pg-windows-attach.md`; `PLAN_UPGRADE_NATIVE.md` §0; `MAP.md` §1 | **🟢 завышен относительно живой архитектуры**; текст контракта vs решение владельца — вопрос к владельцу (TARGET менять нельзя без слова) |
| 5 | 🟡 эмбеддер в контуре; 4B на okna; klient-1 не закрыт | Согласуется | `TARGET_STATUS` п.5; `activeContext` / `PLAN_UPGRADE_NATIVE` Ф4 | расхождений нет |
| 6 | 🟡 SereneDB + IVF на okna; Ф6.2–6.4 не в бою | Согласуется: бой только `ASK_SQL_RRF`; IVF/solr/freshness выкл. | `TARGET_STATUS` п.6; `F6_ROLLOUT_CHECKLIST.md` §0; `activeContext` | расхождений нет |
| 7 | 🟢 поиск «только нативными» + исключение HTTP-эмбеда | Механизмы в дереве есть; **в бою** синонимы/IVF-резолвер/native freshness **выкл.**; словарь/солр — за флагом. Зелёное «готово» читается как «весь путь нативен в проде» | `TARGET_STATUS` п.7; `F6_ROLLOUT_CHECKLIST` §0 (solr/IVF/freshness — очередь); исключение `ai_embed` в той же строке статуса | **стадия оптимистичнее факта боя** |
| 8 | 🟢 всё вокруг SereneDB | Согласуется с критерием контракта (ответ агента, не число) | `TARGET.md` таблица приёмки «7, 8»; `TARGET_STATUS` п.8 | расхождений нет |
| 9 | 🟡 чужая конфигурация (УТ) без правок кода; остался «другой язык» | УТ-проба была (28.07). На пакетном okna карта календаря **пуста** (нет `$metadata`). Вход в sales-канон — лексика «продали»/«купил» (RU) | `TEST_SECOND_BASE.md`; `F6_ROLLOUT_CHECKLIST` §0; `COMPLEXITY_AUDIT` H5 (строки `serene_ask` 4590–4618); `F6_HARDCODE_AUDIT` #10 (локаль ошибки) | **🟡 верна, но текст «работает из коробки» шире факта** (язык + meta-контур) |
| 10 | 🔴 ошибки запрещены — не закрыт | Подтверждаю: **5–9** FAIL на 24 | `SCORER_CLASSES` §4; `TARGET_STATUS` | стадия верна |
| 11 | 🔴 финальная приёмка последней | Согласуется с контрактом | `TARGET` п.11; статус | расхождений нет |
| 12 | 🟡 уточнение есть; догадка (не та сущность) ещё есть | K6 = **2** FAIL «не та сущность»; K5 = отказ при готовом атоме | `SCORER_CLASSES` §4 K5/K6 | стадия верна |
| 13 | 🟢 полнота: gap **0/0**, перепись, сверка таблиц | Покрытие «1С→корпус→индекс» зелёное. **Путь ответа** молча теряет уже посчитанное: K5 — atom `count=307` / `compare` computed → клиенту clarify/figures без числа (п.13 TARGET: молчаливая потеря = дефект) | `TARGET_STATUS` п.13 (`/health` gap 0/0); `SCORER_CLASSES` §4 K5 (2 вопроса); `TARGET.md` п.13 | **крупнейший разрыв: 🟢 при живом K5** |
| 14 | 🟡 инсталлятор частичный; Windows/секреты сзади | Трек владельца в Cursor; «в конце» скорректировано владельцем | `TARGET_STATUS` п.14; `owner-memory/project-installer-is-owner-track-via-cursor.md` | стадия верна; упирается в решение владельца |
| 15 | 🔴 приёмка из базы клиента — в конце | Согласуется; набор `ab-gold-okna.tsv` — под наши данные | `TARGET_STATUS` п.15; `SCORER_CLASSES` §1 | расхождений нет |
| 16 | 🟡 модели ответов локально; веб-DeepSeek снаружи | Согласуется | `TARGET_STATUS` п.16 | расхождений нет |
| 17 | 🟡 инкремент при нуле изменений есть; задержка «1С→ответ» не замерена | Согласуется; условие владельца 06.08 про «не трогать неизменённое» записано | `TARGET_STATUS` п.17 | расхождений нет |
| 18 | 🟡 механизм 503/старения есть; общая с п.12 приёмка не снята | Согласуется | `TARGET_STATUS` п.18 | расхождений нет |
| 19 | 🟢 модель не считает; бюджеты 8k/24k | Согласуется с записанным замером символов | `TARGET_STATUS` п.19; `TARGET.md` приёмка п.19 | расхождений нет |
| 20 | 🟡 данных внутри движка много; `psql` в цикле в `embed_missing` / `build.sh` остаётся | Согласуется; требуемого замера «за такт» нет | `TARGET_STATUS` п.20 | расхождений нет |
| 21 | 🟡 ответ→уточнение→отказ; «правильный» не закрыт | **15/24** бой / **19/24** стейдж с rank ≠ критерий | `SCORER_CLASSES`; `TARGET_STATUS` (стадии не повышали) | стадия верна; числа в старых абзацах (8/25) отстают |

### Сводка §1

| | Число |
|---|---:|
| Пунктов с **завышенной стадией или смыслом «готово»** | **3** (п.13, п.7, п.4) |
| Пунктов, где **стадия верна, а числа в обосновании устарели** | **4** (п.2, 3, 10, 21 — ещё «8/25» при 15–19/24) |
| Пунктов, где текст 🟡 **шире** факта | **1** (п.9) |
| Где сделано **больше**, чем отражает стадия (намеренно) | Rank-канон **19/24** и код Ф6.1–6.4 — стадии 2/3/10/21/6 **не** поднимали (`TARGET_STATUS` шапка 25.08) — это честность, не недозапись |

**Самый крупный разрыв:** п.13 🟢 (gap 0/0) при K5 — посчитанный ответ не доходит до клиента.

---

## 2. Что первым сломается у клиента

Новая база: другая конфигурация 1С, другой язык, другой размер. Опора — девиз
автономности и пп. 9, 14, 15 `TARGET.md`. Без правки кода и без ручного разбора.

| Ранг | Что сломается | Вероятность | Цена | Доказательство |
|---:|---|---|---|---|
| 1 | **Путь «продали / топ / клиент» на не-RU формулировках** — sales-канон завязан на подстроки «продали»/«купил»/… | Высокая на любом не-русском (и на синонимах) | Неверный лидер / clarify вместо ответа (класс K1–K3 до фикса; морфология остаётся в COMPLEXITY H5) | `COMPLEXITY_AUDIT` H5; `SCORER_CLASSES` §0 (канон чинит RU-набор, не язык) |
| 2 | **Пакетный контур без `$metadata`** → пустые `calendar_*` и любые оси/карты из meta | Уже факт на боевой okna | Фичи §7bis **no-op**; клиент с packet-only не получит календарную ось «из коробки» | `F6_ROLLOUT_CHECKLIST` §0; `activeContext` «пакетный контур не отдаёт `$metadata`» |
| 3 | **Инсталлятор / два ручных шага + остальное** — Windows-сторона, роли, секреты, выкат юнитов | Высокая на чистой машине | Продукт не «ставится сам» (п.14) | `TARGET_STATUS` п.14 🟡; трек владельца `project-installer-is-owner-track-via-cursor.md` |
| 4 | **Приёмочный набор не из базы клиента** (п.15) | 100 % до конца трека | «Работает у нас на gold» ≠ «работает у клиента»; независимые агенты (п.15) нечем кормить | `TARGET_STATUS` п.15 🔴; набор `ab-gold-okna.tsv` в `SCORER_CLASSES` §1 |
| 5 | **Выбор сущности / формула «не продаётся» / «реально покупают»** (K6) | Средняя–высокая на чужой схеме имён | Уверенный clarify/чужой регистр при наличии данных (п.21 дефект) | `SCORER_CLASSES` §4 K6 (**2**/9 FAIL) |
| 6 | **Масштаб klient-1 / очередь эмбеда / латентность** | Высокая на «другом размере» | Минуты на вопрос при занятом GPU; смысловой путь отстаёт | `TARGET_STATUS` п.5–6; `SCORER_CLASSES` §5 (латентность 60–76 с при GPU 100 %) |

Подтверждаю находку сложности H5 (лексический sales-гейт) — с продуктовой стороны это п.9, не «красивый код».

Упирается в решение владельца: п.14 (делает сам), п.15 (в конце), отмена PG (п.4 текста TARGET).

---

## 3. Правильные ли приоритеты

Сейчас: классы провалов okna + Ф6 нативизация. По всему контракту:

| Приоритет | Что | Почему числом / пунктом |
|---:|---|---|
| **1** | **Закрыть K5 (молчаливый отказ при `proof_status=computed`)** и следом K4/K6 | Прямо пп. **13 + 21 + 10**: 2 FAIL K5 уже опровергают 🟢 п.13; плюс «отказ при данных» = дефект п.21. Rank (K1–K3) на стейдже уже **4/4** (`SCORER_CLASSES` §0) — следующий кластер по цене для клиента именно «число было — человек не увидел» |
| **2** | **Довести контур meta/`$metadata` на packet-okna** (иначе §7bis и любая meta-ось мертвы) | Без этого п.9 «из коробки» и план `PLAN_ANSWER_CONTRACT` §7bis на **боевой** базе no-op (`calendar_*` пусто). Ф6 флаги на стейдже дали **0** сдвигов вердикта полного набора (`SCORER_CLASSES` §3) — нативизация **не чинит** текущие FAIL качества |
| **3** | **Заменить гейт выката «AB_PROBE 8/8» как единственное доказательство «база цела»** на полный scorer (или явный двухуровневый порог) | П.3/10/21 и урок `HOW_NOT_TO` / `TARGET_STATUS` 31.07: восемь лёгких ≠ «без ошибок». Сейчас коммит/`check-golden` кормятся `0err/8` при полном **15/24** (`REGRESSION_BASE1`, `SCORER_CLASSES`) |

Ф6 дожимать в бой по чеклисту — разумно **после** или **параллельно узко** (freshness только `/health`), но не вместо п.1 таблицы: стейджинг со всеми Ф6 = **те же 15/24**, diffs **0**.

Чем **не** стоило бы занимать ближайшее окно ценой качества: валютная ось (только проект, `activeContext`); память выбора в бой при `memory_total=0` (`MEMORY_COLLISIONS_2026-08-25.md`).

---

## 4. Самообман

**Сильный пример: AB_PROBE 8/8 как зелёный свет продукта.**

| Что меряем | Число | Что это доказывает | Чего не доказывает |
|---|---:|---|---|
| AB_PROBE / маркер `okna probe live 0err/8` | **8/8** почти на каждом выкате 24–25.08 | Транспорт `/ask`, часть лёгких сумм, «первая база не упала» | Верность на полном контракте |
| Полный scorer okna | бой **15/24**, стейдж+rank **19/24** | Реальное качество ответа | — |
| Оффлайн-замки | **2150 / 0** падений, 70 файлов | Регрессия юнит-логики | Поведение на живых данных (9 live не зачтены; `LOCKS_SWEEP_2026-08-25.md`) |
| `/health` gap | **0/0** | Покрытие корпуса | Доставку числа клиенту (K5) |
| Коллизии памяти | **0** классов | Пустая таблица | Готовность `ASK_MEMORY_APPLY` (`MEMORY_COLLISIONS`: `ask_choice_memory` **0** строк при журнале **1445**) |
| Флаги Ф6 на стейдже | AB **8/8**, scorer diffs **0** | Не ломают лёгкий/полный набор | Не улучшают полный набор |

Разбор: гейт выката и `REGRESSION_BASE1` снова и снова пишут «первая база цела» по **8** вопросам. Тот же файл статуса 31.07 уже понижал пп. 2/3/10 за то, что 🟢 держали на «восьми лёгких». Метрика **растёт и остаётся зелёной**, пока полный scorer стоит **15/24**. Это не «плохой прибор» — это прибор не того слоя, которому приписывают смысл пп. 3 и 10.

Второй (короче): **🟢 п.13 по gap 0/0** при K5 — замер полноты **загрузки**, а контракт п.13 включает молчаливую потерю на пути к ответу.

Подтверждаю COMPLEXITY H3/H4 (unique→clarify при computed) — продуктово это тот же K5.

---

## 5. Чего не хватает как продукту

Только то, что уже требует `TARGET.md` (без новых хотелок).

| Пробел | Пункт TARGET | Факт «не заведено / не закрыто» |
|---|---|---|
| Ответ без ошибок / без догадки | 3, 10, 12 | **15/24** бой; K6 догадка сущности; K5 отказ при данных |
| Видимость неполноты **в ответе**, не только в `/health` | 13 | K5; статус 🟢 по coverage |
| Установка «сам» (≤2 ручных шага) | 14 | 🟡; Windows/секреты; трек владельца |
| Приёмка из **базы клиента**, агенты без контекста | 15 | 🔴; gold под okna |
| Задержка изменения 1С → ответ ≤20 мин **замером** | 17 | 🟡; замер задержки не снят (статус честен) |
| Честный отказ при сбоях — приёмка общими с п.12 вопросами | 18 | 🟡; общей приёмки нет |
| Нет своих циклов `psql` / замер за такт | 20 | 🟡; замера за такт нет |
| Независимая приёмка «пункт закрыт» | 11, 15 | Финал отложен |
| Обновление/апгрейд движка как продуктовая процедура | 1, 14 (коробка) | Ф2 плана есть; клиентского «обновился сам» нет |
| Восстановление после сбоя движка | 1, 18 | `serenedb_backup.sh` без расписания (`MAP.md`); dev `:7890` залип — рестарт за владельцем (`DEV_ENGINE_HANG_2026-08-25.md`) |
| Объяснимость «откуда число» клиенту | 12, 21 (уточнение vs ответ) | Паспорт/атомы в плане A/B/C; наружу при K5 не доходят |

Сценарии, которых контракт ждёт, а продукт на okna ещё не держит устойчиво: сравнение периодов (K4), «не продаётся» / «реально покупают» (K6), выдача уже посчитанного (K5).

---

## Три вывода (по убыванию)

1. **П.13 нельзя считать закрытым, пока жив K5** — gap 0/0 и 🟢 статуса расходятся с контрактом («молчаливая потеря») и с замером **2/24**.
2. **Ф6 на стейдже не двигает полный scorer (0 diffs при 15/24)** — ближайший выигрыш клиента в классах ответа (K5→K4/K6), не в очередном native-флаге.
3. **AB_PROBE 8/8 как единственный «зелёный» выката воспроизводит ошибку «восьми лёгких»** (уже записана в `TARGET_STATUS` 31.07) — при полном **15/24**.

## Вопрос владельцу

**П.4 `TARGET.md` всё ещё требует PostgreSQL на Windows, а решение 22.08 слой PG отменило и статус по-прежнему 🟢 по старому пути. Правим текст контракта под живую схему (SereneDB-only), или 🟢 п.4 снимаем до явной новой формулировки?**

(П.14/15 «в конце» и трек установщика не пересматриваю — это уже ваши решения.)
