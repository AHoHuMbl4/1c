# Архитектура «второго мозга» на данных 1С

Полное устройство системы: компоненты, потоки данных, безопасность, эксплуатация.
Как раскатать с нуля — [`RUNBOOK_DEPLOY.md`](RUNBOOK_DEPLOY.md).

Проверено end-to-end 2026-07-22..23 на стенде: Windows 11 + платформа 1С 8.3.27.1786 (x86) +
Бухгалтерия 3.0 → OData/IIS → Ubuntu LXC → бот в Telegram отвечает по данным 1С: **факты** (braine
RAG, с цитатами) и **аналитику/отчёты** (SereneDB, таблица + график), живым тоном (OpenClaw).

Слои документированы отдельно: бот-слой — [`OPENCLAW_BOT.md`](OPENCLAW_BOT.md); аналитика/витрина —
[`SERENEDB.md`](SERENEDB.md). Этот файл — вся картина и то, как слои связаны.

> **Актуальное состояние 2026-08-02.** braine выведен из контура 26.07 (замеры —
> `BENCH_BRAINE_VS_SERENE.md`); на вопросы отвечает `serene_ask` поверх SereneDB. Мост
> `1c-mcp-braine` выведен 30.07, бот ходит через `1c-mcp-ask`. Стенд работает на **двух**
> базах 1С, боевые ответы идут по второй (УТ 11.4.9). Стадии по контракту —
> [`TARGET_STATUS.md`](TARGET_STATUS.md).
>
> Целевое размещение данных — PostgreSQL на Windows + `postgres_attach`
> (решение владельца 26.07): текущий стек и целевая схема — `TARGET_ARCHITECTURE.md` §0.
> 🔴 **Ограничение целевой схемы, которое надо учесть при её реализации:** у движка
> `RETURNING` **не реализован для attach-нутых Postgres-таблиц** ([замер] `strings
> /usr/local/bin/serened` → `RETURNING is not implemented for Postgres yet`). Значит приём
> «`MERGE … RETURNING merge_action`, чтобы получить список изменившихся ключей», который
> рекомендуют `AUDIT_CODE_2/3` и `SERENE_CAPS_*`, на целевой архитектуре не сработает —
> дельту придётся получать иначе. На сегодняшнем нативном корпусе ограничение не мешает.
>
> *Дата врезки была «2026-07-26» при том, что ниже в этом же файле стоят «выведен 30.07» и
> «погашен 29.07»; «актуальное состояние» отставало от собственного текста на четыре дня.*

---

## 1. Цель и инвариант

Дать AI-боту доступ к данным компании из 1С, чтобы он отвечал на вопросы бизнеса.

🔴 **Инвариант: 1С только читаем.** Ни один запрос не должен ничего изменить в 1С. Гарантия — слоями (§4), не на честном слове.

Замысел: данные 1С регулярно выгружаются во внешние хранилища; бот отвечает по ним, а не дёргает
1С на каждый вопрос (и не под риском записи). Замышлялось два «мозга» по типу вопроса,
**остался один**:
- ~~**braine (RAG)**~~ — факты и «найди по смыслу», ответ с цитатами. 🔴 **Выведен из
  контура 26.07** (замер — `BENCH_BRAINE_VS_SERENE.md`): тот же поиск по смыслу делает
  индекс SereneDB, отдельный слой не окупался.
- **SereneDB (витрина)** — и точные агрегации по ВСЕМ строкам, и поиск: инвертированный
  индекс несёт словесный путь, вектор — смысловой. Под read-only ролью.

Сверху — **OpenClaw** (диалоговая оболочка тона, Telegram-бот): переформулирует факты мозгов живо,
сам данные не трогает. 🔴 **2-й инвариант (правило владельца): правила/ограничения — КОДОМ, не
промтом.** Анти-галлюцинации (сверка чисел), анти-слив внутреннего, read-only — детерминированным
кодом/гейтом/ролью; промт (персона) держит только тон. Эмбеддинги — только Qwen `text-embedding-v4`.

---

## 2. Топология (машины и сеть)

| Узел | Адрес | Роль |
|---|---|---|
| Windows-стенд | доступен **только через роутер**: SSH `192.168.56.1:2222`, OData `192.168.56.1:6003` → IIS:80 | 1С + IIS (OData). Только штатное ПО 1С, ничего кастомного. |
| Роутер сети LXC | `192.168.56.1` | Проброс портов → Windows (SSH :2222, IIS :6003). Прямого маршрута к Windows с LXC нет. |
| Ubuntu LXC | `192.168.56.42` (внешний — исходящий NAT `167.235.37.94`, вход только через пробросы) | Весь наш код: шлюз, ETL, config-ui, витрина, бот-слой. |
| FreeBSD `msk-1-vm-uqv5` | белый IP `201.34.130.46` прямо на `vtnet0`, файрволов нет | Опорный узел с белым IP: WireGuard `wg0` = `10.77.0.1`, слушает `:51820/udp`; будущая точка встречи для Windows за NAT (пакетный транспорт). Поднят 06.08, автозапуск в `rc.conf`. |
| WireGuard-мост | `10.77.0.0/24` | FreeBSD слушает, Ubuntu — инициатор (`PersistentKeepalive=25`): входящий UDP на LXC снаружи не транслируется ([замер 06.08]). Устройство и замеры — `ubuntu/wireguard/README.md`. |

*Было ошибочно: «Windows-стенд `10.8.0.58` (SSH), `192.168.122.141` (IIS)». **Чем
опровергнуто:** `ping` обоих адресов с сервера → UNREACHABLE, а `ssh -p 2222
unde@192.168.56.1` работает (проверено 28.07, `RUNBOOK §2`). Заодно `192.168.56.1:6003`
теперь ведёт на IIS/OData, а не на выведенный MCP-тулкит: `curl -I` → 200,
`Server: Microsoft-IIS/10.0`.*

Сеть: LXC и Windows в разных подсетях; связь — через проброс порта на роутере `.1` (`192.168.56.1:<порт> → Windows-IIS:80`). Соединение по IP (без SSH-туннеля — встроенный сервер тулкита на туннеле зависал, а OData/IIS по IP работает штатно).

---

## 3. Компоненты и потоки данных

### Windows (штатная 1С, ничего кастомного)
- **1С:база** — файловая (стенд) или клиент-серверная (прод).
- **IIS** (служба Windows) публикует базу; включён штатный **OData** (`/1c/odata/standard.odata/`). Состав OData (какие объекты отдавать) задаётся один раз при публикации перебором всех справочников/документов.
- **Пользователь `ai_reader`** — роль «Только просмотр» (read-only). OData-запросы идут под ним (Basic auth).

### Ubuntu LXC (наш контроль)
**Наполнение из 1С:**
| Сервис | Порт | Роль |
|---|---|---|
| `1c-odata-gateway` | 6011 | Read-only шлюз ПЕРВОЙ базы: только GET к OData; POST/PATCH/PUT/DELETE → 405. Хранит креды 1С. |
| `1c-odata-gateway@<имя>` | свой на базу | **Шаблон для каждой следующей базы 1С** (`@ut` → 6021). Читает `/etc/1c-odata-gateway-<имя>.env` (600). Баз у клиента бывает несколько — у каждой свой шлюз, своя база движка, свой отвечающий сервис. |
| `1c-config-ui` | 6012 | Веб-галочки «что тянуть» → `/etc/1c-etl-selected.txt`. |
| ~~`1c-etl.timer/.service`~~ | — | 🔴 **Выведен вместе с braine.** Выгружал в 03:00 сущности → md-таблицы → KB-репо (GitLab); потребитель (RAG braine) из контура убран 26.07, таймер `disabled`/`inactive`. Разворачивать не надо. |

**braine (RAG-мозг — факты/текст) — 🔴 СЛОЙ ВЫВЕДЕН ИЗ КОНТУРА 26.07, таблица оставлена как
история установки.** Все перечисленные ниже юниты `disabled` и `inactive`; при развёртывании
с нуля их не ставить. Исключение — `postgresql`, см. §6.
| Сервис | Порт | Роль |
|---|---|---|
| `open-webui` | 3000 | Индекс/retrieval. Эмбеддинги **Qwen `text-embedding-v4` @ 1536** (DashScope), pgvector 1536. |
| `oikb` | 8081 | Синк KB-репо (GitLab) → Open WebUI. |
| `kb-poll` | — | Новый коммит в KB-репо → триггер синка oikb. |
| `rerank-shim` | 8082 | Реранкер qwen3-rerank (DashScope). |
| `api` | 8090 | HTTP API `POST /ask` (retrieval → гейты точности → DeepSeek → ответ с цитатами). |
| `postgresql` | 5432 | pgvector (вектора OWUI) + служебные схемы braine. |
| ~~`tg-bridge`~~ | — | **Выведен из строя (disabled):** Telegram забрал OpenClaw. braine теперь фронт не держит, только `/ask`. |

**SereneDB (аналитика/отчёты):**
| Сервис | Порт | Роль |
|---|---|---|
| `serenedb` | 7890 | Витрина: колоночный OLAP + поиск (IResearch: BM25/инвертированный индекс). Postgres-протокол, loopback, системный юзер **`serenedb`** (`serened` — имя бинаря). Бот — под read-only ролью `serene_ro`. |

🔴 **Векторного индекса у нас нет.** Возможность `ivf` в движке формально есть, но на нашей
сборке 26.07.3 непригодна и **заблокирована решением владельца** (`docs/VECTOR_DECISION.md`,
`memory_bank/owner-memory/project-serenedb-ivf-blocked.md`). Смысловой отбор идёт полным
перебором по колонке `emb`; индексов в базах ровно два — `search_idx` (инвертированный) и
`alias_idx`.

*Было ошибочно: «вектор через `ivf`» стоял в перечне рабочих свойств витрины без единой
оговорки, а системным пользователем назывался `serened`. **Чем опровергнуто:**
`id serened` → нет такого пользователя, `systemctl show serenedb -p User` → `User=serenedb`
(uid 999); по вектору — `VECTOR_DECISION.md` и решение владельца от 27.07, снимок этого
файла был написан 26.07, то есть ДО замера.*

**Бот-слой (OpenClaw, диалог):**
| Сервис | Порт | Роль |
|---|---|---|
| `1c-mcp-ask` | **6016** | MCP-инструмент `ask_1c` над `serene_ask` (:8099, база `ut_test`). Пробрасывает `focus`/`measure`/`context` — поэтому уточнение двустороннее. Адрес задан `Environment=ASK_URL` в теле юнита — это единственный рычаг выбора отвечающего сервиса (`RUNBOOK §10.7`). |
| ~~`1c-mcp-braine`~~ | ~~6014~~ | 🔴 **выведен 30.07.** Смотрел на braine `:8090`, а тот выведен из продукта и не слушает: [замер] бот на любой вопрос отвечал «сервер 1С не отвечает». Запись `second-brain` из конфигурации бота убрана. |
| `1c-mcp-reports` | 6015 | ⚠ MCP-инструмент `report_1c`: NL→SQL по SereneDB (под ro-ролью) + резолвер Qwen + график PNG. **Решением владельца 26.07 выведен из контура, но фактически ещё подключён к боту** — юнит `active`, запись `second-brain-reports` в конфигурации бота на месте. Расхождение стенда с решением, не свойство архитектуры. |
| `openclaw-gateway` (user) | 18800 | Движок OpenClaw (юзер `undebot`): Telegram `@test1c_mcp_bot`, DeepSeek-тон, verify-плагин (гейт кодом). |
| `1c-bot-monitor.timer` | — | Каждые 3 мин: проверка живости. ⚠ Сторожит **выведенный** слой: список `SYS_SERVICES` — `serenedb 1c-mcp-braine 1c-mcp-reports api`, ни одного из двух сервисов ответов там нет, а `api` — из braine. Состояние монитора: `down: api` |

⚠ **Замер по `1c-mcp-reports` [01.08]:** прежний инстанс за ~26,5 ч работы сжёг
**5 ч 34 мин процессорного времени** (пик памяти 149,4 МБ) и оставил в журнале ~300 записей
`starlette.requests.ClientDisconnect` — это штатное логирование обрыва клиента библиотекой
MCP, наш код в стеке не участвует. Текущий инстанс (с 31.07 06:03) простаивает: ~2 мин
CPU, ошибок нет. Для «выведенного из контура» компонента такой расход — признак того, что он
всё ещё в работе.

### Холодный контур (наполнение, по готовности — не ночью)
```
1С OData ──GET── шлюз :6011 ── ETL: выгрузка выбранных сущностей → md-таблицы
   (резолв ссылок guid→наименование, чистка полей) →
   └─► SereneDB-витрина: такт `1c-serene-pipeline.timer` ПО ГОТОВНОСТИ (не по часам):
         раскладка кода → синк-дельта из 1С → сборка корпуса, величин, резолвера, индекса
```

🔴 **Так устроено, но сейчас не работает.** [замер 02.08] `1c-serene-pipeline.service` и
`1c-serene-sync.service` — оба в `failed` (`Result=signal`), последний такт 29.07 06:41,
таймер `enabled`, но `inactive`, следующего запуска нет. Пока конвейер не поднят, свежесть
по п. 17 контракта **не обеспечивается** — ни этой схемой, ни какой-либо другой.
*Было ошибочно: ветка «git push в KB-репо → kb-poll → oikb → OpenWebUI → pgvector» подавалась
как действующая (слой выведен 26.07), а витрина — как ночной `1c-serene-sync.timer` в 03:40.
**Чем опровергнуто:** `systemctl is-enabled 1c-serene-sync.timer` → `disabled`, `is-active` →
`inactive`; свежесть держит `1c-serene-pipeline.timer` (`OnUnitInactiveSec=1min`), а
`1c-serene-index.timer` ещё 28.07 переведён с 03:55 на `OnUnitActiveSec=20min` и тоже
выключен. Понятия «ночной такт» в продукте нет — этого требует п. 17 контракта.*

### Горячий контур (диалог)
```
человек → Telegram @test1c_mcp_bot → OpenClaw (тон, DeepSeek) — сам выбирает инструмент:
   ├ report_1c → SereneDB: NL→запрос (LLM понимает намерение) → read-only SELECT (валидатор+ro-роль)
   │             + резолвер (термин→точное значение, Qwen-вектор) → таблица + график PNG
   └ ask_1c    → serene_ask: индекс SereneDB → счёт в базе → гейт чисел → ответ
                 ↑ перед вызовом бот перефразирует вопрос по вики (`memory-wiki`):
                   слово человека → имя сущности В ЭТОЙ базе. Поиск по вики с 02.08
                   СМЫСЛОВОЙ, на нашем эмбеддере (`agents.defaults.memorySearch`,
                   `extraPaths` на хранилище, `corpus: all`); до этого он был
                   буквальным и на мешок слов возвращал пусто — шаг не работал
                 ↓ при двух прочтениях — уточнение человеку, ответ возвращается полем focus/measure
   → verify-плагин (ГЕЙТ, КОДОМ): числа ответа сверяет с данными инструмента (несверенное режет/
       заменяет); режет внутреннее (SQL/пути) на исходящем и в подписи к фото
   → живой ответ клиенту (таблица/график); «нет данных» — честно, не выдумывает
```
Точность и безопасность здесь дают **код**, не промт: braine-гейты + `verify-плагин` +
read-only роль SereneDB. Персона (тон, маршрутизация инструментов) — промт, но НЕ слой гарантий.
Детали бот-слоя — [`OPENCLAW_BOT.md`](OPENCLAW_BOT.md); аналитики — [`SERENEDB.md`](SERENEDB.md);
стек braine — [`UBUNTU_SETUP.md`](UBUNTU_SETUP.md).

---

## 4. Безопасность (read-only + анти-галлюцинации кодом)

**1С — только чтение, два независимых слоя** (для OData):
1. **Пользователь `ai_reader`** — прав записи нет: любая попытка записи через OData → отказ (проверено: чтение 200, запись 500).
2. **OData-шлюз** — deny-by-default по методу: только GET проходит; POST/PATCH/PUT/DELETE → 405, до 1С не доходят (проверено с LXC).

Плюс: **язык OData на чтение** (GET) не меняет данные по построению; состав OData ограничен опубликованными объектами; креды 1С — только в шлюзе (`/etc/1c-odata-gateway.env`, chmod 600), мозг ходит через шлюз.

**Витрина SereneDB — read-only для бота:** отчёты идут под ролью `serene_ro` (GRANT SELECT), запись
БД физически запрещена (проверено: INSERT/UPDATE/DROP → permission denied). Сверх этого — валидатор
`serene_report`: LLM генерит только SELECT/WITH, любое изменяющее ключевое слово режется до выполнения.
Загрузчик витрины — отдельно, под rw.

🔴 **Анти-галлюцинации и анти-слив — КОДОМ (не промтом), на бот-слое:** `verify-плагин` OpenClaw
детерминированно (а) сверяет числа исходящего ответа с данными инструмента за этот ход — несверенное
режет/заменяет; (б) режет внутреннее (SQL, серверные пути, служебные маркеры) из текста И подписи
к фото. Промт (персона) держит только тон. Правило владельца: правила, которые должны ДЕРЖАТЬ, —
кодом. Детали — [`OPENCLAW_BOT.md`](OPENCLAW_BOT.md).

Сеть: OData не торчит в интернет — доступен только внутри доверенной сети LXC через роутер `.1`. Прод-усиление: HTTPS на IIS + firewall (только LXC) — см. §7.

Почему НЕ встроенный MCP-тулкит: он обслуживает HTTP через клиентский idle-обработчик 1С (~1 req/s, полностью встаёт при любом модальном окне сессии), и его UI-тумблеры не блокируют вызов инструментов. Детально из кода — [`TOOLKIT_TRANSPORT_ROOTCAUSE.md`](TOOLKIT_TRANSPORT_ROOTCAUSE.md). Артефакты MCP-пути (whitelist-прокси, форк) оставлены в репо как dev-опция.

---

## 5. Настройка под бизнес (разовая) vs автомат

Раскатка на новый бизнес = **разовая настройка**, дальше — само.

**Разовое (минуты):**
- Доступы + публикация OData на их 1С, пользователь-читатель, шлюз, ключи моделей и токен бота в `/etc/*.env` — по [`RUNBOOK_DEPLOY.md`](RUNBOOK_DEPLOY.md). *Прежняя редакция звала настраивать «`.env` braine (их ключи/бот/KB-репо)» — слой выведен, KB-репо продукту не нужен.*
- **Галочки «что тянуть»** (config-ui): отметить бизнес-разделы (продажи, склад, деньги, контрагенты…) и сохранить. Типовая 1С содержит сотни служебных классификаторов — их не отмечаем.

  ⚠ **Процедура «открыл `http://<lxc>:6012` в браузере» не работает.** Сервис слушает
  `127.0.0.1:6012` (адрес задаётся `UI_HOST`, умолчание в коде — тоже loopback) и требует
  `Authorization: Bearer $UI_TOKEN` и на GET, и на POST; без `UI_TOKEN` он вообще не
  стартует. Обычный браузер такого заголовка не шлёт — по прежней инструкции человек получал
  только 401, и локально, и по сети. Ходить надо с токеном
  (`curl -H "Authorization: Bearer $UI_TOKEN"`). *Защита сама по себе сделана правильно —
  неверна была документированная процедура. Но что именно она сегодня защищает, см. врезку
  ниже: её выход не читает никто.*
- (Опц.) golden-вопросы бизнеса для ночного регресса.

**Дальше автоматически:** такт `1c-serene-pipeline` по готовности → сборка корпуса и
индекса → бот отвечает по свежим данным. *Прежняя редакция называла здесь «ночной ETL» —
он выведен вместе с braine (§3).*

🔴 **И «галочки» сегодня ни на что не влияют.** Выход `1c-config-ui` —
`/etc/1c-etl-selected.txt` (последняя правка 26.07), а его единственный читатель `oc_etl.py`
запускается таймером `1c-etl`, который `disabled`. Боевая сборка состав сущностей берёт из
переписи `$metadata`, а не из этого файла. То есть единственный веб-экран продукта работает,
слушает порт и закрыт токеном — но включён в контур, которого больше нет. Гасить его или
переводить на боевой отбор — решение владельца (вопрос №47).

**Что универсально (код, копипаст на любой бизнес):** весь механизм — шлюз, ETL, config-ui, резолв ссылок guid→наименование, чистка полей. **Что конфиг (под бизнес):** список сущностей (галочки) + доступы/ключи в `.env`. Список — это конфигурация, а не хардкод в коде.

---

## 6. Zero-touch: что переживает ребут
- **Windows:** IIS (W3SVC) = Automatic; публикация/состав OData/пользователи персистентны.
- **LXC (system-сервисы `enabled`), [замер 01.08]:** `postgresql`, **`serenedb`**,
  `1c-odata-gateway`, **`1c-odata-gateway@ut`** (шлюз второй базы: стартует после ребута, но
  с пустым `ODG_PASS` ответит 401 на всё), `1c-config-ui`, **`1c-mcp-ask`**,
  `1c-mcp-braine`, `1c-mcp-reports`, **`1c-serene-ask`**
  + таймеры **`1c-serene-pipeline`** и **`1c-bot-monitor`**.
  `tg-bridge` — **disabled** (Telegram у OpenClaw), `nightly-eval` — **погашен 29.07**
  (евал выведенного слоя braine).

  *Было ошибочно: в списке стояли `open-webui`, `oikb`, `rerank-shim`, `api`, `kb-poll` и
  таймер `1c-etl`, а живых `1c-mcp-ask` и `1c-serene-ask` не было вовсе. **Чем опровергнуто:**
  `systemctl list-unit-files --state=enabled` — все пять braine-сервисов `disabled` и
  `inactive`, `1c-etl.timer` `disabled`. То есть §6 разворачивал бы мёртвый слой и умалчивал
  о живом.*

🔴 **Перезагрузка сегодня НЕ собирает контур обратно, и это надо знать до ребута.** Юнита у
второй базы нет (записанный ручной шаг, Фаза 2 `PLAN_AUTONOMY`): сервис ответов на :8099 —
процесс вне systemd. После ребута `systemd` поднимет `1c-serene-ask`, тот займёт :8091 и
будет отвечать по **первой** базе (умолчания кода: `ASK_LISTEN_PORT=8091`,
`dbname=postgres`; ни одна из этих переменных не лежит в подключённых env-файлах), а мост
`ask_1c` уйдёт в :8099, где никого нет — человек получит «данные недоступны» на каждый
вопрос. Оба юнита при этом стартуют успешно, поэтому отказ выглядит как сбой сети.
`1c-bot-monitor` этого не заметит: в его списке `SYS_SERVICES` — `serenedb`,
`1c-mcp-braine`, `1c-mcp-reports`, `api`, то есть **ни одного из двух сервисов ответов**, зато
есть `api` из выведенного braine; монитор с 26.07 сидит в состоянии `down: api`.
А проверка `/health` шлюза бота **пропускается всегда**: `GATEWAY_HEALTH_URL` в
`/etc/1c-bot-monitor.env` задан, но юнит этот файл `EnvironmentFile`-ом не подключает и
скрипт его не читает — переменная до кода не доходит.

- **PostgreSQL 16 (:5432) — остаток выведенного слоя braine.** `enabled`, поднимается после
  ребута вместе с базами `openwebui` и `mem0`, хотя ни один живой сервис к нему не ходит
  (все боевые DSN — на `127.0.0.1:7890`). Сносить или оставить — решение владельца.
- **OpenClaw gateway** — systemd **user**-юнит юзера `undebot` с **linger=on** → стартует на буте
  без логина (проверено). Telegram — long polling, токен в `tokenFile`.
- 🔴 **Итог на сегодня — обратный тому, что было записано здесь раньше:** перезагрузка
  подняла бы стек, но **не собрала бы контур** (сервис ответов и шлюз второй базы живут вне
  systemd), а мониторинг об этом не сообщит — он сторожит выведенный слой. Прежняя строка
  «перезагрузка любой машины → всё поднимается само; мониторинг алертит, если что-то не
  встало» описывает замысел, а не стенд; исполнится она, когда закроется Фаза 2
  `PLAN_AUTONOMY` (поэкземплярные юниты) и монитор переведут на живые сервисы.

---

## 7. Тюнинг прода (не хвосты — работает; усилить на реальной ERP)
1. **Боевая ERP вместо стенда** — публикация OData на клиентской 1С:ERP, состав под их разделы, `ai_reader` по их правилам.
2. **Приватность LLM** — сейчас DeepSeek (ответы/NL→SQL) + Qwen `text-embedding-v4` (эмбеддинги, DashScope) облачные. Для реальных данных решить: компромисс (метаданные/агрегаты в облако) или self-host. У braine есть NER-анонимизация.
3. **Регистры** (остатки/обороты — «сколько на складе», «продажи за период») — в состав OData + `TOP_PREFIXES` ETL; в витрину SereneDB как факт-таблицы.
4. **Дельта-синк витрины.** Такт `1c-serene-pipeline` (по готовности) тянет из 1С дельту по изменениям — механизм сделан, но на стенде такт стоит с 29.07 (см. §3); для больших баз остаётся инкремент документов по дате вместо полного ре-пула. *Прежняя редакция называла штатным ночной `1c-serene-sync.timer` (03:40) — он `disabled` с 28.07.*
5. **HTTPS на IIS + firewall** (только LXC к OData); **ротация bot-токена** `@test1c_mcp_bot` перед реальным продом.
6. **Backup/restore SereneDB — штатные средства ЕСТЬ.** [замер 02.08 на нашей сборке 26.07.3]
   движок знает `EXPORT DATABASE '<кат>'`, `IMPORT DATABASE '<кат>'` и
   `COPY FROM DATABASE <src> TO <dst>` (по докам движка последнее копирует одну прикреплённую
   базу в другую). Контрольные опыты: `EXPORTX`, `IMPORTX`, `BACKUP`, `RESTORE DATABASE`,
   `COPY FROM DATABASEX` → **все** дают `syntax error`, то есть неизвестный оператор выглядит
   иначе. Наш оффлайн-снапшот каталога (`serenedb_backup.sh`) при этом никуда не девается: он
   на **весь инстанс**, то есть на все базы разом (`RUNBOOK §10.9`). Отдельно: бэкап pg OWUI;
   golden set под домен.

   🔴 **Граница доказанного:** замером подтверждено только то, что операторы **существуют и
   доходят до исполнения**. Ни один экспорт не доведён до конца, восстановление обратно не
   проверялось ни разу, и покрывает ли `EXPORT DATABASE` файлы инвертированного индекса — не
   установлено ни замером, ни доками. *В первой редакции этого пункта (02.08) стояло
   «которого `EXPORT DATABASE` не выгружает» и «не останавливая движок» — оба утверждения
   без доказательства, в абзаце, который сам требует проверять отрицательные выводы.*

   *Было ошибочно: «Backup/restore SereneDB (штатного нет — тест + вопрос фаундерам)». **Чем
   опровергнуто:** три оператора выше распознаются живым движком. Это ровно тот отрицательный
   вывод «штатного средства нет», который по `CLAUDE.md` требует такой же проверки, как
   положительный, — и она не была сделана. Заодно: поправлен здесь только этот документ,
   `docs/SERENEDB.md`, `ubuntu/serenedb/README.md` и `memory_bank/progress.md` то же
   утверждение пока держат.*
7. Этап 4 — миграция RAG braine на SereneDB. Предметно: движок умеет это нативно —
   инвертированный индекс на IResearch (BM25, фразы, префикс/фаззи) + `ai_embed()` прямо в SQL.
   Плюс `postgres_attach` (искать по живой Postgres без копии) и Zero-ETL по файлам.
   ⚠ Векторный индекс `ivf` в этот перечень **не входит**: на нашей сборке он непригоден и
   заблокирован решением владельца (`docs/VECTOR_DECISION.md`) — смысловой отбор идёт полным
   перебором. Детали и что мерить — `docs/SERENEDB.md` §«Возможности движка».

---

## 8. Ключевые доступы и настройки (значения — вне git)

**Настройки, которых не было в руководстве по развёртыванию** (найдены аудитом 01.08; имена
перечислены, значения — в `/etc/*.env`). Из них в `*.md` не встречались вовсе только
`GATEWAY_HEALTH_URL` и `WIKI_ALIAS_PER_TACT`; `UI_TOKEN` описан в `RUNBOOK §0`,
`ALIBABA_API_KEYS` — в `CHANGELOG` и `progress`. *Прежняя редакция этой врезки (02.08)
говорила «не было ни в одном документе» — это было сильнее правды.*

| Переменная | Где | Что делает |
|---|---|---|
| `UI_TOKEN` | `/etc/1c-config-ui.env` | доступ к веб-настройке :6012; без неё сервис не стартует |
| `GATEWAY_HEALTH_URL` | `/etc/1c-bot-monitor.env` (права **644**, а не 600) | адрес проверки живости бота. 🔴 **До кода не доходит вовсе:** значение в файле задано, но юнит `1c-bot-monitor` не подключает этот `EnvironmentFile`, и скрипт его не читает — при пустом значении `bot_health_check.sh` проверку молча пропускает, то есть она пропускается **всегда**. Так и не заметили трёх суток простоя |
| `WIKI_ALIAS_PER_TACT` | ручка кода, умолчание 100 | сколько сущностей размечать словами за такт (`build.sh`) — определяет, за сколько тактов база обрастёт человеческими словами |
| `ETL_PAGE` / `ETL_PAGE_MIN` | ручки кода, умолчания 10 000 / 250 | сколько строк за один заход тянуть из 1С по OData. 🔴 Главная ручка скорости синка: `[замер 05.08]` 165 строк/с при 1 000 против 1 316 при 20 000 — цена в **числе обращений**, а не в объёме. Крупная страница снижает нагрузку на 1С (второе предложение п. 17), а не повышает. Под базу не подбирается: при отказе 1С страница уменьшается вчетверо до `ETL_PAGE_MIN` |
| `WIKI_ALIAS_MAX_SEC` | ручка кода, умолчание 120 | 🔴 сколько **времени** за такт отдаётся словам сущностей — обоим проходам `wiki_alias.sh` вместе. Ограничение временем, а не числом вызовов модели: шаг идёт внутри такта свежести (п. 17), а цена вызова зависит от модели. [замер 05.08] прежний предел «40 кругов» означал час из двух с четвертью. `0` снимает бюджет (остаётся `WIKI_ALIAS_COLLISION_ROUNDS`) |
| `ALIBABA_API_KEYS` | `/etc/1c-mcp-reports.env` | несколько ключей эмбеддера с чередованием; в `RUNBOOK` фигурировал только `ALIBABA_API_KEY` в единственном числе |
| `UI_HOST` | `/etc/1c-config-ui.env` | адрес, на котором слушает веб-настройка (умолчание в коде — `127.0.0.1`). Именно настройка, а не свойство: `0.0.0.0` открыл бы экран в сеть |


KB-репо `money/1c-test` (GitLab id 95); бот `@test1c_mcp_bot` (Telegram у OpenClaw, юзер `undebot`);
пользователи 1С `admin`/`ai_reader`; роль SereneDB `serene_ro` (read-only, бот); ключи DeepSeek
(LLM) и Alibaba (Qwen-эмбеддинги/реранк) + GitLab. Всё в `credentials/` (в `.gitignore`) и в
`/etc/*.env` на LXC (chmod 600). Пароли в git не коммитятся.
