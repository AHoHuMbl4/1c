# Сетевая схема продукта

Сетевое устройство системы на одной схеме: публичные точки, Hetzner Cloud network, vSwitch, LXD-контейнеры, RU↔EU туннель и потоки данных между компонентами.

🔴 **Источник фактов — замеры 22.08–23.08.** IP, порты и маршруты не догаданы, а сняты на живых хостах. Схема действительна до следующего переключения инфраструктуры; процедуры подключения нового сервера приведены, чтобы повторить её без поиска.

---

## 1. Карта сети

```
RU (клиент)                         EU (Hetzner)
┌─────────────────────┐             ┌─────────────────────────────────────────────┐
│ LAN klient-1        │             │ Hetzner Cloud network-1  10.3.0.0/16        │
│ 10.1.1.7 (1С)       │             │  • subnet 10.3.0.0/24                       │
│       │             │             │    10.3.0.4  okna (старая)                  │
│       ▼             │             │    10.3.0.3  pro-router   2.28.54.129       │
│  RU-роутер/точка    │  WireGuard  │    10.3.0.2  openclaw-okna 2.28.49.158      │
│  89.23.101.22       │◄───────────►│  • subnet 10.3.1.0/24                       │
│  WG UDP 51820       │  ~44 мс     │    gateway 10.3.1.1 (VLAN 4003, vSwitch)    │
└─────────────────────┘             │         │                                   │
                                    │         ▼                                   │
                                    │  ┌─────────────────────────────┐            │
                                    │  │  GPU-хост gpu-erw.timpul.pro│            │
                                    │  │  178.63.211.188  (FSN1-DC20)│            │
                                    │  │  eno1.4003 = 10.3.1.11/24   │            │
                                    │  │  LXD lxdbr0 = 10.10.10.1/24 │            │
                                    │  │  форварды: 2201→klient1,    │            │
                                    │  │            2202→okna (SSH)  │            │
                                    │  └─────────────┬───────────────┘            │
                                    │                │                           │
                                    │     ┌──────────┴──────────┐                │
                                    │     ▼                   ▼                │
                                    │  LXC klient1        LXC okna               │
                                    │  10.10.10.11        10.10.10.12            │
                                    │  125 ГБ / 40 vCPU   125 ГБ / 40 vCPU       │
                                    │  400 ГБ             400 ГБ                 │
                                    │  Ubuntu 26.04       Ubuntu 26.04           │
                                    └─────────────────────────────────────────────┘
                                                  │
                              ┌───────────────────┘
                              ▼
                  GPU-хост gpu-qwen27b
                  49.13.97.101  (NBG1-DC6)
                  eno1.4003 = 10.3.1.12/24
                  эмбеддер :8002

Канон GPU внутри vSwitch (см. §2.1):
  10.3.1.11 (= gpu-1c / 178.63.211.188): embed :8000, rerank :8005 (whisper :8006 ОСТАНОВЛЕН 23.08)
  10.3.1.12 (= gpu-qwen27b / 49.13.97.101): 27B :8000 (контекст 64k с 23.08), embed :8002 (diarize/pyannote :8001 ОСТАНОВЛЕН 23.08)
  Подсеть 10.3.1.0/24, шлюз 10.3.1.1, VLAN 4003; внутри сети без фильтра — режется только внешний периметр.
```

*Публичные форварды gpu-1c разведены по портам: **2201 → klient1**, **2202 → okna** (SSH). Файрвол хостинга пропускает 201.34.142.160 (dev), 89.23.101.22 (точка klient-1 RU).*

---

## 2. Таблица адресов

| Хост | Публичный IP | Внутренние IP | Роль | Порты / примечания |
|---|---|---|---|---|
| **gpu-erw.timpul.pro** / **gpu-1c** | 178.63.211.188 | 10.3.1.11/24 (`eno1.4003`), 10.10.10.1 (`lxdbr0`) | GPU-хост + LXD-гипервизор (FSN1-DC20, RTX 6000 Ada) | SSH 22, форварды 2201→klient1, 2202→okna; **живой** embed :8000, rerank :8005; whisper :8006 **остановлен** 23.08 |
| **LXC klient1** | — (NAT хоста) | 10.10.10.11/24 | Контур klient-1 (125 ГБ RAM / 40 vCPU / 400 ГБ, Ubuntu 26.04) | SSH 22 внутри LXD; доступ извне — только через 178.63.211.188:2201 |
| **LXC okna** | — (NAT хоста) | 10.10.10.12/24 | Новый контур okna (125 ГБ RAM / 40 vCPU / 400 ГБ, Ubuntu 26.04) | SSH 22 внутри LXD; доступ извне — только через 178.63.211.188:2202; **пакетный приёмник** `:6090` (`1c-packet-server`) — с EU cloud network через LXD `proxy6090` на `10.3.1.11:6090` хоста |
| **gpu-qwen27b** | 49.13.97.101 | 10.3.1.12/24 (`eno1.4003`) | Второй GPU-хост (NBG1-DC6, RTX 6000 Ada) | SSH 22 (файрвол для 201.34.142.160); **живой** LLM 27B :8000, embed :8002; diarize/pyannote :8001 **остановлен** 23.08 |
| ~~okna (старая)~~ | ~~167.233.249.110~~ | — | **УДАЛЁН владельцем 23.08** — переезд закрыт | — |
| **pro-router** | 2.28.54.129 | 10.3.0.3/24 | EU-конец RU↔EU туннеля | WireGuard UDP 51820; HAProxy 80/443 (mTLS-релей пакетов) |
| **openclaw-okna** | 2.28.49.158 | 10.3.0.2/24 | Open WebUI + Caddy (`okna.timpul.pro`) | Веб 443/80 |
| **RU-роутер klient-1** | 89.23.101.22 | — | RU-конец WG; за ним LAN клиента | WireGuard UDP 51820; за ним 1С-сервер klient-1 = 10.1.1.7 |

*Контейнеры на gpu-1c выходят наружу через NAT хоста; в облачную сеть 10.3.0.0/16 — через vSwitch-ногу хоста без дополнительного NAT [замер 22.08]: okna-контейнер → 10.3.0.4 = 3,3 мс.*

### 2.1 Канон GPU-эндпоинтов (внутренние адреса первичны)

🔴 **Канон [решение владельца 23.08]:** везде использовать **внутренние** адреса vSwitch 4003.
Внешние IP — только запасной путь (файрвол/отладка снаружи). API-ключ один на оба хоста
(`/etc/1c-embed.env` на коробках — не дублировать в git).

| Сервис | Канон (vSwitch) | Запасной (публичный) | Хост |
|---|---|---|---|
| **Embeddings** | `http://10.3.1.11:8000/v1/embeddings` | `http://178.63.211.188:8000/v1/embeddings` | gpu-1c |
| **Rerank** | `http://10.3.1.11:8005/rerank` | `http://178.63.211.188:8005/rerank` | gpu-1c |
| **Whisper** ⏸ остановлен 23.08 (GPU под пересчёт) | `http://10.3.1.11:8006/v1/audio/transcriptions` | `http://178.63.211.188:8006/…` | gpu-1c |
| **LLM (Qwen3.8-27B FP8, контекст 64k с 23.08)** | `http://10.3.1.12:8000/v1/chat/completions` | `http://49.13.97.101:8000/…` | gpu-qwen27b |
| **Diarize (pyannote)** ⏸ остановлен 23.08 (GPU под пересчёт) | `http://10.3.1.12:8001/` | `http://49.13.97.101:8001/` | gpu-qwen27b |
| **Embeddings (2-й)** (падал по памяти, починен 23.08) | `http://10.3.1.12:8002/v1/embeddings` | `http://49.13.97.101:8002/…` | gpu-qwen27b |

Env прод-коробок: `EMBED_HOSTS` = оба embed через `|ключ`; `EMBED_BASE_URL`/`EMBED_HOST` →
`10.3.1.11:8000`; `RERANK_URL` → `10.3.1.11:8005/rerank`; `DEEPSEEK_BASE` → `10.3.1.12:8000`
(имя переменной историческое — модель Qwen3.8-27B).

**[замер 23.08]** Новая okna (`10.10.10.12`) переведена на внутренние адреса: curl пяти
эндпоинтов с коробки 200 (whisper GET `/v1/audio/transcriptions` → 405 Method Not Allowed —
живой ответ); RTT `10.3.1.11` avg **0,073 мс**, `10.3.1.12` avg **3,1 мс**; ask «воскресенье»
→ `kind=answer`, sum `0.00`, `code_md5=d0945244`.

---

## 3. vSwitch 4003 — подключение нового выделенного сервера

Используется vSwitch **#84994**, VLAN **4003**, subnet **10.3.1.0/24**. Шлюз — 10.3.1.1 (на ICMP не отвечает, это норма).

### Пошагово

1. В **Hetzner Robot** → vSwitch #84994 → кнопка **«Add servers»** добавить сервер.
   🔴 Без членства в vSwitch кадры уходят в никуда: ARP без ответа, RX=0 [замер 22.08].
2. На сервере создать VLAN-интерфейс и persist через netplan:
   ```yaml
   # /etc/netplan/02-vswitch.yaml
   network:
     version: 2
     ethernets:
       eno1: {}
     vlans:
       eno1.4003:
         id: 4003
         link: eno1
         mtu: 1400
         addresses:
           - 10.3.1.XX/24
         routes:
           - to: 10.3.0.0/16
             via: 10.3.1.1
         nameservers:
           addresses: [213.133.98.98, 213.133.99.99]
   ```
3. `netplan apply`; проверить: `ip addr show eno1.4003`, `ip route | grep 10.3.0.0/16`, `ping 10.3.0.4`.
4. MTU 1400 — обязательно: туннель/инкапсуляция Hetzner Cloud network режет большие фреймы.

### Текущие адреса vSwitch

| Сервер | Интерфейс | IP |
|---|---|---|
| gpu-erw.timpul.pro | eno1.4003 | 10.3.1.11/24 |
| gpu-qwen27b | eno1.4003 | 10.3.1.12/24 |

---

## 4. Туннель RU↔EU

### Замеры (89.23.101.22 ↔ 2.28.54.129) [замер 22.08]

| Вариант | Задержка | Скорость RU→EU | Скорость EU→RU | Примечание |
|---|---|---|---|---|
| **WireGuard UDP 51820** | ~44 мс | ~192 Mbps | ~240 Mbps | **Выбран основным** |
| ssh -w (tun поверх TCP 22) | — | ~120 Mbps | ~70 Mbps | Запасной; требует `PermitTunnel` |
| GRE/IPIP | — | проходят | проходят | Запасные на случай блокировки UDP |

### Открытые направления

* TCP 22/80/443/1194/51820/46000 и UDP 500/1194/4500/51820/46000 открыты в обе стороны [замер 22.08].*

### Топология klient-1

```
Windows 1С (klient-1) → ru-router 89.23.101.22 → WG → pro-router 2.28.54.129 / 10.3.0.3
                        → Hetzner Cloud network → vSwitch 4003 → gpu-1c → LXC klient1
```

---

## 5. Потоки данных

| Поток | Путь | Примечание |
|---|---|---|
| **Telegram-бот (ask)** | `openclaw-gateway` (undebot, 127.0.0.1:18800; веб 0.0.0.0:18801) → MCP `serene-ask` 127.0.0.1:6016 (`1c-mcp-ask@postgres`, `MCP_PORT=6016` в `/etc/1c-mcp-ask-postgres.env`) → `ask` 127.0.0.1:8091 (`1c-serene-ask@postgres`) → SereneDB 127.0.0.1:7890 | Polling, egress-only; inbound не нужен. [замер 22.08] живой конфиг undebot: единственный MCP-сервер — `serene-ask` на 6016; 6014/6015 встречаются только в старых сессиях/бэкапах, не в `mcp.servers` |
| **Канал данных 1С (пакетный)** | Windows-клиент → mTLS `1c-gate*.timpul.pro` / `1c-gate.timpul.ru` → **HAProxy pro-router** (`2.28.54.129`, `:443`, CN `okna-1` → backend `packet_unit_okna_1`) → `10.3.1.11:6090` на gpu-1c (**LXD `proxy6090`**: `listen=tcp:10.3.1.11:6090` → `connect=tcp:10.10.10.12:6090` в LXC okna) → `1c-packet-server` | :6090 — приёмник пакетов, НЕ OData-шлюз. Reverse SSH на новой okna **не найден** [замер 23.08] — путь только через HAProxy. 🔴 **[замер 23.08]** HAProxy уже на `10.3.1.11:6090`, LXD proxy жив (curl с gpu-1c → `/health` 200), но backend **DOWN**: TCP `:6090` с pro-router (`10.3.0.3`) timeout при живом `:8000`; tcpdump на `eno1.4003` — 0 SYN → блокировка **вне хоста** (Hetzner firewall whitelist); открытие `:6090` from `10.3.0.0/16` — владелец |
| **Миграции** | rsync old→new напрямую: 110,8 МБ/с внутри Hetzner | S3 (`s3.twcstorage.ru`) — только мелочь; квота ~1 ГБ, на 95 ГБ — 403 QuotaExceeded |
| **Эмбеддинги / rerank / LLM / whisper / diarize** | Контейнеры → канон §2.1 (`10.3.1.11` / `10.3.1.12`) | Внутренние адреса первичны [замер 23.08 новая okna]. Запасной путь — публичные IP тех же хостов |

---

## 6. Правила

- **1С — только читаем.** Доступ к OData только GET; запись физически запрещена двумя слоями (пользователь `ai_reader` + OData-шлюз).
- **Секреты только в `/etc/*.env`.** Никаких ключей, паролей и токенов в git, документах и аргументах командной строки.
- **Inbound только через форварды владельца.** Публичный SSH на gpu-1c разведён по портам 2201/2202; прямого доступа к LXC из интернета нет.
- **GPU внутри vSwitch.** Прод-трафик embed/rerank/LLM — канон §2.1 на `10.3.1.11` / `10.3.1.12`, не через публичные адреса. Whisper и diarize/pyannote **остановлены** (23.08).
- **Внутри vSwitch / cloud network ограничений нет.** Фильтруются только **внешние** адреса (Hetzner Robot firewall / whitelist на публичных IP). Между `10.3.1.11` ↔ `10.3.1.12` и контейнерами на `10.10.10.0/24` — полный доступ по внутренним адресам.
- **RU↔EU — WireGuard UDP 51820.** Запасные варианты (ssh -w, GRE/IPIP) держатся на случай изменения политики провайдера.
- **Hetzner Cloud network 10.3.0.0/16** — магистраль EU; vSwitch 4003 соединяет с ней выделенные серверы Robot.

---

## 7. Старая схема (история до конца приёмки переезда)

Старая okna (`167.233.249.110`) держала весь контур сама: SereneDB 26.07.3 `:7890`, `ask` `:8091`, `packet-server` `:6090`, `openclaw gateway` `18800/18801`, `lan-relay` `10.3.0.4:7890` (`enp7s0` — cloud network). klient-1 — аналогично на `10.1.1.7` в LAN клиента. Схема оставлена как историческая; боевой трафик переключается на новые контейнеры gpu-1c.
