# Тест на второй базе 1С: Управление торговлей 11.4.9 (п. 9 контракта)

**Начат 2026-07-28, вечер.** Живой протокол: что делали, что нашли, чем доказано.
Ведётся по ходу теста — не задним числом.

**Дополнено 2026-08-26 (Э2 / `PLAN_TO_TARGET`):** офлайн-охота привязок к языку и базе —
§7–§10; сценарий англ. базы — §8. Код не менялся.

**Зачем.** П. 9 `TARGET.md`: «Без хардкода и подстройки под конкретную базу. Работает из
коробки на базе с любыми данными и любой размерностью». Критерий приёмки: «на чужой
базе — другой язык, другая конфигурация, другой масштаб — работает **без правки кода**».

До этого теста всё делалось на одной базе — Бухгалтерия 3.0.190.11. Пункт 9 стоял 🟡
именно поэтому: известные хардкоды вынесены, но на чужой конфигурации не проверялось.

---

## 0. Что взяли и почему

| | Рабочая база | Тестовая база |
|---|---|---|
| конфигурация | Бухгалтерия предприятия **3.0.190.11** | **УправлениеТорговлей 11.4.9.73** |
| путь | `C:\1c\bases\buh_test` | `C:\1c\bases\ut_demo` |
| публикация | `/1c` | `/ut` |
| сущностей в `$metadata` | 4 585 | **2 795** |
| регистры бухгалтерии | есть (главная книга) | **0 — их в УТ нет вовсе** |
| витрина SereneDB | `dbname=postgres` | `dbname=ut_test` (изолировано) |

Владелец сначала скачал «чистую» Бухгалтерию — я отсоветовал: та же конфигурация
проверила бы только другие ДАННЫЕ. Взяли УТ демо — **другая конфигурация с данными**,
это и есть предмет п. 9. Остаётся незакрытым только «другой язык».

**Что в УТ есть, чего наш код не видел ни разу:** бизнес-процессы (6), задачи (1),
журналы документов (11), константы (665). И наоборот — нет ни одного регистра
бухгалтерии, на которых мы отлаживали связь «обёртка → движения».

---

## 1. Изоляция: рабочий стенд не должен пострадать

- Витрина УТ — **в отдельной базе движка** `ut_test` (`CREATE DATABASE` → `ATTACH`).
  [замер] из `ut_test` рабочая витрина не видна: `duckdb_tables()` даёт 0 своих таблиц.
- Отдельный шлюз OData на `:6021` → публикация `/ut`. Рабочий `:6011` не тронут
  (проверено: отвечает 200 во время всего теста).
- Отдельный каталог CSV `/var/lib/serenedb/ut`.
- Рабочий конвейер `1c-serene-pipeline.timer` **остановлен на время теста** — иначе два
  такта столкнулись бы на квоте эмбеддера. Вернуть после.

---

## 2. Находки — по мере теста

### 🔴 Н-1. Имя базы SereneDB было зашито как `'postgres'`
**Найдено до загрузки данных**, при подготовке изоляции.
`corpus_build.sql` (×2) и `coverage_build.sql` фильтровали источники по
`database_name = 'postgres'` — имени нашей базы на ЭТОМ стенде. На установке, где база
названа иначе, отбор дал бы **ноль источников**, и корпус собрался бы пустым **молча**.
Починка: `current_database()`. [замер] в `ut_test` возвращает `ut_test`, в рабочей —
`postgres`.

### 🔴 Н-2. Замок сборки не зависел от базы
`build.sh` брал `flock` по фиксированному пути `/var/lock/1c-serene-build.lock`. Два
конвейера над **разными базами 1С** (у клиента их бывает несколько) блокировали бы друг
друга: второй молча уходил бы, и его данные не обновлялись **никогда**. Починка: имя
замка из `dbname` в DSN — из того же источника, что и сама база, без отдельной настройки.

### Н-3. Публикация OData создаётся с выключенным OData
`webinst -publish -iis` отработал с кодом 0, но записал `standardOdata enable="false"` —
эндпоинт отдавал **404**. Это не наш дефект, а поведение утилиты платформы, но для
инсталлятора (п. 14) существенно: **после публикации надо проверять `enable="true"`**,
иначе установка выглядит успешной, а данных нет. Записано для п. 14.

### 🔴 Н-4. `duckdb_tables()` видит ВСЕ базы движка — утечка между базами
[замер] из `ut_test` он показал 1 605 своих таблиц **и 302 чужих**. Четыре наших запроса
не фильтровали по базе, поэтому источник считался существующим, потому что таблица с
таким именем есть в ДРУГОЙ базе — и `query_table()` падал: «Table
catalog_внешниекомпоненты does not exist». Починено фильтром `database_name =
current_database()` в `corpus_build` (×2), `corpus_precheck`, `corpus_init`.
**На одной базе эффекта нет** — там всё и так в текущей базе, поэтому дефект был скрыт.

### 🔴 Н-5. Наша обрезка значения создавала БИТУЮ КОДИРОВКУ и роняла всю сущность
Самая важная находка теста.

[замер] `catalog_правилаинтеграциис1сдокументооборотом`: исходное значение соединяется с
картой ссылок нормально, а **после нашей же `substr(…,1,20000)`** соединение падает —
`Invalid unicode (byte sequence mismatch) detected in value construction`. То есть дефект
**наш**, а не данных: на первой базе он просто не стрелял, потому что там не оказалось
таких значений.

Хуже последствие: с `ON_ERROR_STOP on` вместе с одной сущностью падал **весь такт** —
1 500 здоровых сущностей не доезжали из-за одной.

**Две починки, обе универсальные:**
1. **Соединение с картой ссылок — только по GUID-образным значениям.** Ссылкой может быть
   ТОЛЬКО GUID, значит сужение не теряет ни одной связи; GUID по определению ASCII, и
   битые байты до хеша не доходят. Побочно снимается лишняя работа: раньше движок хешировал
   ВСЕ ячейки (миллионы) против 39 тыс. записей карты.
2. **Перебор вариантов вместо падения** (указание владельца): не собралось полной сборкой —
   пробуем `p_doc_plain`, намеренно простой вариант (текст из пар «колонка: значение», без
   ссылок, величин и дат). Качество ниже, но сущность остаётся в поиске. Что не собралось
   и им — уходит числом в `search_quality.build_failed`, то есть **видно**, а не проглочено.

---

### 🔴 Н-6. Замок, рабочие таблицы и СЕКРЕТЫ назывались одинаково во всех базах (29.07)

Три дефекта одного класса — «общее имя на все базы». Ни один не виден, если гонять базы
по очереди; все три вылезли, когда две базы пошли рядом.

| Что | Чем кончилось | Замер |
|---|---|---|
| замок брал имя базы **разбором строки DSN** | в окружении конвейера `dbname=` не задан — libpq подставляет базу по умолчанию, разбор дал `default` против `postgres`. Два такта по ОДНОЙ базе, общие `tmp3_*`, **66 дублей ключа** | корпус цел: остановила защита `corpus_merge` |
| рабочие таблицы досчёта векторов | перенос мог взять таблицу чужой базы (чужой вектор на нашей строке), уборка — снести чужую оплаченную работу | из `postgres` было видно **5** рабочих таблиц `ut_test`; после починки 0 |
| **секреты движка общие на инстанс** | упавшая сборка второй базы трапом снесла `odg`/`qwen`, и такт ПЕРВОЙ базы получил `ai_embed: secret 'qwen' not found` | было 16 строк без вектора → осталось 16; после починки 17 → **0** |

Починка по указанию владельца — **отдельные имена, а не фильтр**: `emb_<база>_<таблица>`,
`odg_<база>`, `qwen_<база>`, имя базы спрашиваем у движка (`current_database()`).
Разбор — `HOW_NOT_TO §3.14`, `§3.16`, `§3.17`.

### 🔴 Н-7. `Edm.Stream`/`Edm.Binary` валят ВЕСЬ ответ OData, а не одно поле (29.07)

При загрузке с нуля одна сущность из 1 588 ответила `HTTP 500` устойчиво — при `$top`
1000, 100, 10 и даже 1, — тогда как `$count` честно отдавал **37**. Причина: поля
хранилища значения (`ИсточникДанных` `Edm.Stream`, `ИсточникДанных_Base64Data`
`Edm.Binary`). Исключил их через `$select` — пришли все 37 строк, код 200.

**Но наличие такого поля отказа НЕ гарантирует:** [замер] `Catalog_ШаблоныПоясненийДляФНС`
имеет ровно те же два типа, 35 строк, отдаётся 200. Типов с такими полями **470 из 2 795**
на УТ и **961 из 4 585** на первой базе — это размер подверженности, а не число поломок.

Отсюда починка — **перебор, а не сужение всем подряд**: как обычно → повтор → без
неотдаваемых полей → честный отказ с числом в переписи. Сузить все 961 значило бы
выбросить колонки там, где всё работает, сменить схему таблиц витрины и переписать ключи
строк корпуса (`ловушка 22`). Подробности — `techContext` ловушка 27.

---

## 2-бис. Пересборка второй базы С НУЛЯ (29.07)

По указанию владельца: «делаешь бэкап, куда-то складываешь файл, сносишь старую и начисто
делаешь новую». Это и есть приёмка автономности, а не обвязка вокруг неё.

1. **бэкап** — `serenedb_backup.sh` → `/root/backups/serenedb/serenedb-20260729-044937.tar.gz`,
   **2,6 ГБ**; проверено, что внутри `engine_duckdb/store.db`, где лежат все базы;
2. **снос** — `DROP DATABASE ut_test`;
3. **чистая** — `CREATE DATABASE ut_test`, 0 таблиц;
4. **конвейер без единой ручной правки по ходу.**

| Этап | Замер |
|---|---|
| синк с нуля | **1 588 сущностей, 769 842 строки, 80 минут**, ошибок 1 (та самая Н-7) |
| медиана на сущность | 0,13 с; **71% времени — восемь сущностей** |
| самая дорогая | `Document_ПланПродаж`: 169 строк за 156 с — **одна страница, один запрос** |
| разброс скорости | от 1 до 902 строк/с при одинаковом размере страницы |

**Вывод про скорость:** одинаковая страница, одна сеть, один наш код, разброс в 150 раз —
время уходит **внутри 1С на отрисовку OData**, не у нас. Верхняя оценка нашей доли: на
быстрых сущностях мы даём 900 строк/с вместе с записью CSV и слиянием, значит наши
накладные — доли секунды на сущность. И это **разовая цена подключения базы**: дальше
работает дельта по `DataVersion`, обычный такт первой базы — 150-180 с.

Что можно ускорить (не сделано, записано): `$select` только нужных колонок; страничность
по ключу вместо `$skip` (сейчас доходит до `$skip=97000` — платформа обязана пройти 97
тысяч записей, чтобы начать отдавать, и стоимость всей выгрузки растёт квадратично);
загрузка нескольких сущностей параллельно.

---

## 3. Ход теста — ИТОГ

| Шаг | Итог | Замер |
|---|---|---|
| распаковка базы | ✅ | `1Cv8.1CD` 1 725,9 МБ |
| вход через COM | ✅ | `УправлениеТорговлей 11.4.9.73` |
| состав OData | ✅ | **1 771 объект** (346 справочников, 187 документов, 457 регистров сведений, 88 накопления, 665 констант, 11 журналов, 6 бизнес-процессов, 1 задача) |
| публикация в IIS | ✅ | после починки `enable="true"` |
| `$metadata` | ✅ | 10,8 МБ, **2 795 сущностей** |
| **синк витрины** | ✅ | **1 588 сущностей, 1 ошибка** (сторона 1С: `$count`=37, данные — 500) |
| **сборка корпуса** | ✅ | **1 501 из 1 501, не собралось 0**; **632 683 строки** |
| карта сущностей | ✅ | 1 501 метка, **без метки 0**, табличных частей 272 |
| индекс | ✅ | 632 683 строки |
| векторы | ⏹ намеренно остановлены | 632 тыс. эмбеддингов — часы и деньги, для п. 9 не нужны |
| **ответы бота** | ✅ | см. ниже |

### Ответы на чужой конфигурации — числа сверены с SQL

| Вопрос | Ответ бота | SQL | |
|---|---|---|---|
| сколько документов реализации | **272** | 272 | ✅ |
| сколько номенклатуры | **311** | 311 | ✅ |
| сколько контрагентов | **180** | 180 | ✅ |
| какие партнёры есть | ответил по **контрагентам** (180) | партнёров 216 | ⚠ выбор сущности |

Три числа из четырёх — **точное совпадение**. Четвёртый промах не дефект кода: в УТ
`Партнёры` и `Контрагенты` — разные справочники (деловой субъект против юрлица), и модель
выбрала второй. Это случай для среднего рубежа (уточнение), а не для правки кода.

---

## 4. Что подтвердилось про универсальность

Всё ниже — **без единой правки кода под УТ**:

- **разворот вложенных регистров** отлаживался на регистрах бухгалтерии, которых в УТ нет
  вовсе, — и сработал на накопительных регистрах УТ: [замер] обёртка развёрнута в
  **2 280 строк** при `$count=637`;
- **дельта/полная загрузка** выбирается сама по наличию уникального `Ref_Key`;
- **классификация колонок и карта сущностей** прошли на незнакомых типах: 1 501 метка,
  **без метки ноль**, связь «шапка ↔ табличная часть» найдена у 272;
- **разрешение ссылок** работает: в тексте видно `Ref: ТД00-000001 / Пробит`;
- **1 588 сущностей загружено, 1 501 собрана, ноль отказов**.

---

## 5. 🔴 Проверка, что не сломали ПЕРВУЮ базу

Требование владельца: правки обязаны быть универсальными, а не под конкретную базу.
После всех изменений прогнана рабочая база:

| | Замер |
|---|---|
| сборка | **231 из 231 сущностей, не собралось 0** |
| корпус | 103 808 → 103 808 (+0), 231 сущность, без вектора 0 |
| такт | 171 с |
| приёмка | **`ab_scorer` 8/8, `golden` 8/8** |

Ни одна правка не потребовала различать базы: `current_database()`, фильтр по базе в
`duckdb_tables()`, замок по `dbname`, GUID-образный ключ соединения — на одной базе
поведение прежнее, на двух корректное.

---

## 6. Что осталось (снимок после УТ, до охоты Э2)

- **Другой язык** — англоязычную конфигурацию 1С достать сложно; п. 9 закрывается по
  «другой конфигурации» и «другому масштабу», по языку остаётся оговорка.
- **Вектора на УТ** не считались намеренно (632 тыс. эмбеддингов). Семантический путь и
  резолвер на второй базе не проверялись — они проверены на первой.
- Одна сущность УТ недоступна со стороны 1С (`$count`=37, данные 500) — не наш дефект,
  видна в переписи.

---

## 7. Охота Э2 (26.08.2026) — привязки к базе и к русскому языку

**Режим:** офлайн. Сеть / ssh / psql / сервисы / живой прогон — не использовались.
Код не правился. `ubuntu/serenedb/serene_ask.py` — **только чтение** (правки — другому
агенту). Область: `ubuntu/**`, `windows/**`. Комментарии и кириллица в сообщениях
журнала/оператору учтены отдельно (куча **в**), если они не влияют на отбор данных.

**П. 9 `TARGET.md` дословно:** «Без хардкода и подстройки под конкретную базу. Работает
из коробки на базе с любыми данными и любой размерностью». Критерий приёмки: «на чужой
базе — другой язык, другая конфигурация, другой масштаб — работает **без правки кода**».

**Различие двух «языков» (важно для кучи а):**

| Слой | Что на англ. базе | Опора в коде |
|---|---|---|
| платформенные поля OData | `Ref_Key`, `Description`, `IsFolder`, `Date`, `Posted` — **английские всегда** (`INSTALL_LOG`) | законно |
| префиксы типов OData | `Catalog_`, `Document_`, … | законно |
| имена объектов/реквизитов конфигурации | `Products` вместо `Номенклатура`, `Quantity` вместо `Количество` | опасная зона |
| COM-имена платформы | на EN-локали — `Metadata`/`Name`/`Catalogs`; на RU — `Метаданные`/`Имя`/`Справочники` | опасная зона установщика |

Уже закрыто ранее (не повторять как открытое): `current_database()` вместо
`'postgres'`; замок/`emb_*`/`odg_*` по имени базы от движка; warehouse по `Edm.*` без
`IN ('quantity','количество')` (`F6_HARDCODE_AUDIT` #11); термы `measure_resolver` из argv.

### 7а. Блокирует запуск / установку / путь данных на чужой (в т.ч. англ.) базе

| # | путь:строка | чем ломается | чем заменить |
|---|---|---|---|
| A1 | `windows/odata-setup/src/ai-ext.ps1:109-133` — секции COM только RU (`Справочники`…), `Метаданные`, `Имя` без EN-fallback | На англ. локали платформы `InvokeMember` по RU-имени даёт пустой список → `FATAL: метаданные пусты` → роль-читатель не ставится → OData 401 → витрина пустая | Как в `manifest-gen.ps1` (`PE`/`ME`): RU→EN (`Catalogs`/`Metadata`/`Name`). Либо один раз спросить у COM оба имени |
| A2 | `windows/odata-setup/src/ai-ext.ps1:204` — `$mainLang = "Русский"` до разбора `DefaultLanguage` | Если разбор XML не нашёл язык, расширение ссылается на `Language.Русский`, которого в EN-конфиге нет → `/LoadConfigFromFiles` / `/UpdateDBCfg` падает | Язык **только** из дампа `Configuration.xml` (`DefaultLanguage`); без находки — стоп с явной ошибкой, не умолчание |
| A3 | `windows/odata-setup/src/manifest-gen.ps1:62` — `$md = P $ib "Метаданные"` (без `PE`); далее много `P … "Имя"` (напр. `:257`, `:278`, `:301`) при том что `PE`/`ME` уже есть | Синтетический `$metadata` на EN-платформе не строится → packet-контур без манифеста не стартует | Везде `PE $o "Метаданные" "Metadata"`, `PE $o "Имя" "Name"` (образец уже в том же файле `:59`, `:67-68`) |
| A4 | `ubuntu/serenedb/ask_choice_memory_apply.sh:11-14`, `ask_journal_apply.sh:12-15` — `sed` по `dbname=` в DSN | Класс Н-6: нет `dbname=` в строке → правка имени молчит / бьёт не ту базу; журнал/память чужой базы | Имя базы — `current_database()` у движка или явный argv/`DB=`, не разбор DSN (`HOW_NOT_TO §3.14`) |
| A5 | `ubuntu/serenedb/serene_ask.py:1864-1866`, `:6037-6039` — `"не существует"` рядом с en в тексте ошибки движка | На EN-локали движка ветка «таблицы нет» может не сработать → ложный сбой контура вместо честного `[]` | Только стабильные en-маркеры (`does not exist` / `not found` / `Catalog`), как в `solr_synonyms_build.py` (`F6_HARDCODE_AUDIT` #10) |

**Итого куча а: 5 находок** (A1–A5). Без починки A1/A3 установка «из коробки» на EN-платформе
ломается ещё до вопросов боту.

### 7б. Ухудшает качество, но контур поднимается

| # | путь:строка | чем ломается на англ. метаданных | чем заменить |
|---|---|---|---|
| B1 | `serene_ask.py:4818-4829` — маркеры рейтинга (`больше всего`, `номенклатур`, `лидер`…) | EN-вопрос «top products» частично покрыт (`top`/`most`), RU-формы без пары → want/rank не включается | Фразы вопроса — моделью в `intent`; списки слов не носить. Либо словарь NL→intent из данных/алиасов |
| B2 | `serene_ask.py:5240-5268` `sales_sum_intent` — `продали`/`выручк`/`оборот` + en | Канон продаж на EN-конфиге с другими глаголами/kind слабее; compare-окно на `недел`/`месяц` | Intent/measure из модели + канон по структуре (`written_by`, меры из `$metadata`), не по стемам `реализац` |
| B3 | `serene_ask.py:5271-5286` `_sales_register_score` — `количество`/`сумма`/`всего`/`реализац`/`книга`/`ндс` | Меры `Qty`/`LineTotal` без этих подстрок → неверный регистр в вилке | Скор по типам Edm + роли мер из карты/`search_measure_alias`, имена src — из `written_by`/`parent`, не стемы |
| B4 | `serene_ask.py:5495-5499`, `:6246-6250` — стемы `реализац`/`продаж`/`книгапродаж`/`актсверки` в имени src | EN-имена `SalesOfGoods`/`VATSalesBook` не матчятся теми же стемами (часть en есть: `sale`/`reconciliation`) | Классификация источника — моделью по `label`/синонимам сущности или структурным признакам регистра |
| B5 | `serene_ask.py:5877-5920` `_is_price_list_noise` / `_is_product_catalog` / `prefer_entity_for_catalog_count` — `номенклатур`/`товар`/`установкацен`/`ценыноменклатур` + whitelist `catalog_номенклатура` | На `catalog_products` whitelist частично есть; `PricesOfItems` без `ценыноменклатур` — шум прайса не отсекается | Признак «товарный справочник» / «регистр цен» — из `$metadata` + разметки такта (`search_entity_class` / wiki-alias), не LIKE по стемам |
| B6 | `serene_ask.py:5961-5984` — подсказки меры `количество` по словам `товар`/`сколько` | На EN-мере `Quantity` hint `количество` не совпадёт с именем колонки | Hint = каноническое имя меры из `search_measure_alias` / списка величин сущности |
| B7 | `serene_ask.py:11303-11318` `_period_day_label` — всегда `DD.MM.YYYY` | Для EN-пользователя/локали ожидаем `YYYY-MM-DD` или `MM/DD/YYYY` — косметика ответа + риск путаницы дня/месяца | Формат из локали вопроса/настройки контура; ISO внутри всегда |
| B8 | `ubuntu/serenedb/period_relative_forms.json` — по сути только `prev_week`, RU+2 en | Другие относительные периоды на EN не покрыты словарём шага 1-period | Расширить словарь **данными** (не код) или строить формы моделью; пустой/неполный словарь уже виден в `/health` |
| B9 | `ubuntu/1c-config-ui/oc_discover.py:39-45` `SYSTEM_HINTS` — RU-стемы (`Служебн`, `Удалить`, `Настройк`…) | На EN-именах (`Settings`, `Delete`…) предотметка «служебное» в UI врёт | Подсказка из класса сущности такта / эвристик по префиксам платформы, либо двуязычный список **как данные**, не фильтр |
| B10 | `ubuntu/serenedb/serene_sync.py:48` — `"присоединенныефайлы"` / `startswith("удалить")` | EN: `AttachedFiles` / `Delete…` — служебное не отфильтруется в подсказках преполёта | Те же отсечки по navigation/`$metadata` или двуязычные маркеры платформы из доков 1С |
| B11 | `ubuntu/1c-etl/oc_etl.py:30-31` (док-пример `Catalog_Номенклатура…`), `:191` — заголовок MD всегда «Справочник»/«Документ» | Legacy-ETL KB: на EN-базе текст KB на русском ярлыке типа | Ярлык типа из префикса OData (`Catalog`→язык UI) или без перевода |
| B12 | `ubuntu/open-webui/configure-branding.py:46-48` — примеры «Сколько контрагентов?» / «Номенклатура» | Подсказки UI чужой базы про чужие сущности | Примеры из `label` топ-сущностей этой базы (п. 15) или нейтральные |
| B13 | `ubuntu/serenedb/ab-gold.tsv` и `ab-*-okna.tsv` — SQL/имена `document_реализация…`, `СуммаДокумента`, `Город` | Это **файл данных** (шапка файла это пишет), не код — но без нового набора «приёмка на EN» невозможна | Новый `AB_GOLD_FILE` из `$metadata`+живых чисел этой базы (п. 15); код `ab_scorer` путь уже параметризует |
| B14 | `serene_ask.py:11456-11476` — шаблоны `period_empty` на русском | EN-вопрос → RU-отказ «За … записей нет» | Язык ответа = язык вопроса (уже есть ветка для balance clarify `:6227-6233`) |

**Итого куча б: 14 находок** (B1–B14). Из них B1–B6 / B14 сосредоточены в `serene_ask.py`
(не править здесь — оркестратору). B13 — не дефект кода, но блокер **приёмки** п. 9 по языку.

### 7в. Косметика (логи, докстринги, операторские сообщения)

| # | путь:строка (образцы) | заметка |
|---|---|---|
| C1 | `ubuntu/serenedb/build.sh`, `embed_*.sh`, `corpus_*.sql` RAISE/echo на русском | Такт на любой базе; язык сообщений оператору |
| C2 | `ubuntu/serenedb/code_map.py` — ошибки якорей на русском | Инструмент разработки, не путь клиента |
| C3 | `ubuntu/packet/packet_*.py`, `ubuntu/openclaw/deploy_instance.sh` — FATAL/логи RU | Не влияют на отбор сущностей |
| C4 | `ubuntu/serenedb/coverage_build.sql:92-104` — причины `'закрыто правами в 1С'` и др.; `corpus_build.sql:867` фильтр по ним | Внутренний словарь **наших** причин (писатель и читатель — мы); к языку метаданных 1С не привязан |
| C5 | Кириллица в `test_*.py` / фикстурах | Замки; на прод-путь не влияют |
| C6 | `windows/odata-setup/src/ai-ext.ps1` / `manifest-gen.ps1` — текст FATAL на русском | После A1/A3 достаточно; язык сообщений установщика |

**Итого куча в: 6 групп** (не считая сотни отдельных литералов в логах — это один класс).

### 7г. Что сознательно не считалось дефектом п. 9

- Префиксы OData и поля платформы (`Ref_Key`, `Description`, `Edm.*`) — контракт 1С.
- Имена **наших** таблиц (`search_corpus`, `search_entity_alias`, …) и ролей.
- Дефолт DSN `dbname=postgres` в скриптах — стендовый fallback; боевой контур обязан
  задавать DSN явно (`RUNBOOK` §10).
- `ab-gold.tsv` как файл данных конкретной базы — законная форма; дефект только если
  код **требует** именно этот файл без `AB_GOLD_FILE`.

---

## 8. Сценарий проверки «вторая база на другом языке»

Офлайн-заготовка. Команды — из того, что есть в репозитории / `RUNBOOK_DEPLOY.md` §10 и
истории этого файла. Где артефакта нет — **нужно от владельца**.

### 8.0. Что должно быть у владельца до старта

| # | Нужно | Есть в репо? |
|---|---|---|
| 1 | Англоязычная конфигурация 1С **с данными** (не «та же УТ с переводом UI», а metadata names на EN — напр. англ. демо Trade Management / аналог) | **нужно от владельца** — в дереве файла базы нет |
| 2 | Платформа 1С на Windows + право публиковать IIS / OData | **нужно от владельца** (контур) |
| 3 | Решение: COM-локаль платформы RU или EN (влияет на A1/A3) | **нужно от владельца** (замер `$ib` / `InvokeMember`) |
| 4 | Имя базы движка, порт шлюза, порт ask (пример ниже — условный) | выбирается при прогоне |
| 5 | Ключи эмбеддера / модели ответа, GPU если нужен живой `/ask` | **нужно от владельца** (GPU в норме с 26.08: 27B + embed + reranker живы — слово владельца) |
| 6 | Явный ок на `CREATE DATABASE`, публикацию, остановку чужих таймеров на время синка | **нужно от владельца** |

### 8.1. Изоляция (не трогать postgres / okna)

Подставить свои `DB`, `GW`, `ASK_PORT` (пример: `en_test`, `6031`, `8097`):

```bash
export DB=en_test
export GW=6031
export ASK_PORT=8097
# на управляющей машине с доступом к SereneDB — только после слова владельца:
psql "host=127.0.0.1 port=7890 user=postgres dbname=postgres" \
  -c "CREATE DATABASE $DB"
```

- Отдельный шлюз: шаблон `1c-odata-gateway@…` / порт `$GW` → публикация IIS этой базы
  (не `:6011` и не `:6021` УТ).
- Каталог CSV / packet-meta — свой (как `/var/lib/serenedb/ut` для УТ).
- На время полной загрузки — не гонять параллельно другой тяжёлый такт на той же
  квоте эмбеддера (урок §1 этого файла).

### 8.2. Windows: публикация и права

1. Опубликовать базу в IIS (`webinst`), **проверить** `standardOdata enable="true"` (Н-3).
2. Читатель + расширение: `windows/odata-setup/src/ai-ext.ps1` через установщик/env
   (`OC1C_EXT_*`).  
   ⚠ Если COM-локаль EN — ожидать A1; фиксировать `OBJECTS:` / `FATAL` в логе как
   блокер п. 9, не «чинить руками список объектов».
3. Проба: `GET /$metadata` и `$count` по 2–3 сущностям под читателем → 200.
4. При необходимости манифеста: `manifest-gen.ps1` (A3 на EN-платформе).

### 8.3. Ubuntu: контур базы движка

По `RUNBOOK_DEPLOY.md` §10 и шаблонам:

| Что | Где в репо / на сервере |
|---|---|
| env такта | `/etc/1c-serene-pipeline-$DB.env` — `SERENEDB_DSN` **с** `dbname=$DB`, `ETL_ODATA_BASE` → шлюз `$GW` |
| юнит такта | `ubuntu/systemd/1c-serene-pipeline@.service` → `systemctl start 1c-serene-pipeline@$DB` |
| env ask | `/etc/1c-serene-ask-$DB.env` — `SERENEDB_DSN_RO`, `PORT=$ASK_PORT`, … |
| юнит ask | `ubuntu/serenedb/systemd/1c-serene-ask@.service` → `1c-serene-ask@$DB` |
| код | выкат в `/opt/1c-mcp-reports` штатным `deploy` контура — **без правок под эту базу** |

Критерий «без правки кода»: ни одного diff под имена `Products`/`Quantity`/… .

### 8.4. Синк и сборка

```bash
# после слова владельца, на контуре этой базы:
systemctl start "1c-serene-pipeline@$DB"
# ход: journalctl -u "1c-serene-pipeline@$DB" -f
```

Снять числа (как в §3): сущностей в `$metadata`, загружено, собрано в корпус,
`build_failed`, без метки. Векторы — по бюджету; для п. 9 языка достаточно BM25-пути
+ 3–5 вопросов со сверкой SQL (как §3), если GPU ограничен.

### 8.5. Приёмочный набор из этой базы (п. 15 + п. 9)

1. **нельзя** гонять `ab-gold.tsv` / okna-наборы — они про русские имена УТ/okna (B13).
2. Собрать новый TSV: вопрос TAB SQL-эталон по **английским** именам витрины
   (`AB_GOLD_FILE=/path/to/ab-gold-en_test.tsv`).  
   Источник правды имён — `$metadata` / `duckdb_tables` этой базы, не догадка.
3. Прогон: `python3 ubuntu/serenedb/ab_scorer.py` с `AB_GOLD_FILE` и DSN этой базы
   (см. шапку `ab_scorer.py` / env `AB_DSN`).
4. Параллельно — 3–5 ручных вопросов боту (`/ask` или MCP) со сверкой SQL.

Минимум для языка (предложение; точный список — из данных базы):

| Класс | Зачем |
|---|---|
| count по главному каталогу товаров | B5/B6 |
| sum продаж / revenue за период | B2/B3 |
| top-N items by qty | B1/B6 |
| вопрос без однозначного источника | п. 12/18 |
| EN-вопрос на относительный период («last week») | B8 |

### 8.6. Регрессия первой базы

После любых правок по находкам A/B — снова замер первой базы
(`docs/REGRESSION_BASE1.md` / golden 8/8), как §5. Без этого п. 9 не закрывать.

### 8.7. Готовность сценария к прогону

| | |
|---|---|
| Текст шагов и команды | ✅ в этом файле |
| Англ. база 1С с данными | ❌ **нужно от владельца** |
| Доступ Windows/IIS + ок на CREATE/pipeline | ❌ **нужно от владельца** |
| Новый AB_GOLD под EN-имена | ❌ строится на месте из базы (шаблон — `ab-gold.tsv`) |
| Починка A1–A3 до прогона | рекомендуется: иначе установщик на EN-COM не пройдёт «без ручного разбора» |
| Живой `/ask` при выключенной 27B | ❌ **нужно от владельца** (включить модель или принять BM25-only) |

**Вердикт:** сценарий **не готов к прогону** до появления англоязычной базы и слова
владельца на контур. Документ готов как чеклист охоты + шагов.

---

## 9. Критерий «пункт 9 закрыт» (язык)

П. 9 по **конфигурации и масштабу** уже имел замер (УТ, §3–§5). По **языку** пункт
закрывается только когда одновременно:

1. Поднята база с **английскими** именами объектов/реквизитов в `$metadata` (не только
   EN UI поверх русских имён).
2. Установка читателя + синк + сборка корпуса + ответы — **без diff кода** и без ручного
   списка сущностей/мер под эту базу.
3. Приёмка: новый `AB_GOLD_FILE` (или эквивалент) — доля верных не ниже зафиксированного
   порога стенда (ориентир истории: лёгкий набор 8/8 на знакомой базе); числа сверены SQL.
4. Регрессия первой базы после любых сопутствующих правок — зелёная.
5. Находки кучи **а** либо закрыты кодом до прогона, либо опровергнуты замером на той же
   EN-платформе («A1 не стреляет, потому что COM отвечает на RU-имена» — с логом).

Пока нет п.1 и прогона — в `TARGET_STATUS` остаётся оговорка «остался другой язык».

---

## 10. Сводка охоты Э2 (26.08)

| Куча | Число |
|---|---|
| **а** блокирует | **5** |
| **б** качество | **14** |
| **в** косметика (групп) | **6** |

**Три самые тяжёлые:**

1. `windows/odata-setup/src/ai-ext.ps1:109-133` — COM только по-русски → пустые метаданные
   на EN-платформе (блокер установки).
2. `ubuntu/serenedb/serene_ask.py:5240-5286` (`sales_sum_intent` + `_sales_register_score`) —
   канон продаж/мер на стемах RU(+частично EN); на англ. именах мер/сущностей — неверный
   или слабый канон.
3. `ubuntu/serenedb/serene_ask.py:5877-5920` — отбор «товар vs прайс» LIKE/`номенклатур` +
   whitelist русских имён каталога.

`serene_ask.py` в этом заходе **не правился** — строки выше для оркестратора.

---

## 11. Аудит привязок 27.08 (Э2 / п. 9)

**Режим:** офлайн (без сети, без живых баз — контур занимает оркестратор). Код
**не правился.** Область добора: `ubuntu/serenedb/*.{py,sh,sql}` + сверка якорей
§7 (в т.ч. `windows/odata-setup`, UI/ETL из охоты 26.08). Образец таблицы —
`docs/F6_HARDCODE_AUDIT.md` (сам файл не менялся).

**Критерий (п. 9 + девиз):** на чужой базе — другой язык / конфигурация / масштаб —
работает **без правки кода и без ручного разбора**. Нужное знает `$metadata` /
движок / модель / словарь в данных (`PLAN_AUTONOMY`).

**Метод:** не новая охота с нуля, а **вердикт по каждой находке §7–§10** + добор
мест, которые §7 не нумеровала, но ломают EN-конфиг по коду.

### 11.0. Сводка чисел

| Категория | Число | Состав |
|---|---|---|
| **Настоящие (а)** | **24** | A1–A5; B1–B12; B14; D1–D6 |
| **Ложные (б)** | **9** | L1–L9 (уже снято / платформа / модель / env) |
| **Осознанные остатки (в)** | **7** | C1–C6; B13 (файл данных) |
| **Всего строк аудита** | **40** | 24+9+7 |

Топ-5 настоящих (тяжесть для «из коробки» на EN):

1. **A1** `ai-ext.ps1:129-133` — COM `Метаданные`/`Имя`/`$s[0]` RU; EN-имена в
   массиве есть (`$s[2]=Catalogs`), но **не вызываются**.
2. **A3** `manifest-gen.ps1:62` — `P … "Метаданные"` без `PE` (при том что
   `Количество`/`Count` уже через `ME`).
3. **B2+B3+D3** `serene_ask.py:5245-5280`, `:5548-5580` — канон продаж/мер по
   стемам RU(+частично EN).
4. **B5+D1** `serene_ask.py:5879-5922`, `:6081` — товар/прайс LIKE + whitelist
   RU-имён; ось остатков зашита как `'ТМЦ'`.
5. **D2** `serene_ask.py:4013`, `:4597` — `endswith("документа")` для шапочной
   суммы (на EN будет `…OfDocument` / `DocumentTotal`, не `…Документа`).

### 11.1. Таблица вердиктов

| # | место (файл:строка) | находка | вердикт | что делать |
|---|---|---|---|---|
| A1 | `windows/odata-setup/src/ai-ext.ps1:129-133` | `$md = P … "Метаданные"`; цикл `(P $md $s[0])` + `(P $o "Имя")` — только RU, хотя `$s[2]` уже EN | **(а) настоящая** | `PE`/`ME` как в `manifest-gen` (`Метаданные`/`Metadata`, `Имя`/`Name`); для секций — `$s[0]` затем `$s[2]` |
| A2 | `ai-ext.ps1:204` | `$mainLang = "Русский"` до разбора `DefaultLanguage` | **(а)** | язык **только** из `Configuration.xml`; без находки — FATAL, не умолчание |
| A3 | `manifest-gen.ps1:62` (+ места с голым `"Имя"`) | `$md = P $ib "Метаданные"` без EN-fallback | **(а)** | везде `PE … "Метаданные" "Metadata"` |
| A4 | `ask_choice_memory_apply.sh:11-14`, `ask_journal_apply.sh:12-15` | `sed` по `dbname=` в DSN | **(а)** | argv/`DB=` или `SELECT current_database()` у rw-сессии; не разбор строки (`HOW_NOT_TO §3.14`) |
| A5 | `serene_ask.py:1864-1866`, `:6037-6041` | `"не существует"` рядом с en в тексте ошибки движка | **(а)** | достаточно стабильных en-маркеров (`does not exist` / `not found` / `Catalog`), как `solr_synonyms_build` |
| B1 | `serene_ask.py:4812-4824` `rank_question_text` | маркеры `больше всего`/`номенклатур`/`лидер` (+ частичный EN) | **(а)** | want/rank из `intent` модели; списки NL не носить |
| B2 | `serene_ask.py:5245-5257` `sales_sum_intent` | стемы `продали`/`выручк`/`недел`… | **(а)** | intent/measure моделью; окна — из period-форм данных |
| B3 | `serene_ask.py:5270-5279` `_sales_register_score` | меры/src: `количество`/`всего`/`реализац`/`книга` | **(а)** | скор по Edm + `search_measure_alias` / `written_by`, не стемы имён |
| B4 | `serene_ask.py:5498`, `:6248-6253` (+ `sales_noncanon_focus:5840`) | `реализац`/`книгапродаж`/`актсверки`/`книга`/`ндс` в имени src | **(а)** | класс источника — модель/wiki-alias/label, не LIKE по стемам |
| B5 | `serene_ask.py:5879-5922` | `_is_price_list_noise` / whitelist `catalog_номенклатура`… | **(а)** | класс «товарный catalog» / «регистр цен» из `$metadata` + `search_entity_class` |
| B6 | `serene_ask.py:5980-5986` `rank_measure_hint` | hint всегда строка `"количество"` | **(а)** | канон меры из `search_measure_alias` / списка величин сущности |
| B7 | `serene_ask.py:11334-11341` `_period_day_label` | всегда `DD.MM.YYYY` | **(а)** | формат из локали вопроса; ISO внутри |
| B8 | `period_relative_forms.json` | по сути только `prev_week` (RU+2 en) | **(а)** | расширить **данными** или моделью; пустота уже в `/health` |
| B9 | `ubuntu/1c-config-ui/oc_discover.py:39-45` | `SYSTEM_HINTS` RU-стемы | **(а)** | класс сущности такта / двуязычные маркеры **как данные** |
| B10 | `serene_sync.py:48` | `"присоединенныефайлы"` / `startswith("удалить")` | **(а)** | отсев по navigation/`$metadata` или двуязычные маркеры платформы |
| B11 | `ubuntu/1c-etl/oc_etl.py:191` | ярлык типа всегда «Справочник»/«Документ» | **(а)** | ярлык из префикса OData → язык UI, либо без перевода |
| B12 | `open-webui/configure-branding.py:46-48` | примеры «контрагентов»/«Номенклатура» | **(а)** | примеры из `label` топ-сущностей базы |
| B14 | `serene_ask.py:11483-11521` `format_period_empty_text` | RU-шаблоны при кириллице в вопросе | **(а)** частично | EN-ветка **уже есть** (нет кириллицы → EN); добить язык ответа = язык вопроса устойчивее, чем эвристика скрипта |
| D1 | `serene_ask.py:6081` `balance_registers_with_goods` | `map_extract_value(refs_map, 'ТМЦ')` — имя оси окna/УТ | **(а) новая** | ось товара — из карты/метаданных регистра, не литерал `ТМЦ` |
| D2 | `serene_ask.py:4013`, `:4597/:4627` | `endswith("документа")` / `"всего"` для головной меры | **(а) новая** | структурный признак шапочной суммы из `$metadata`/ролей мер, не суффикс RU |
| D3 | `serene_ask.py:5548-5580` `sales_money_measure` / `sales_qty_measure` | стемы `всего`/`сумм`/`колич`/`шт`/`единиц` | **(а) новая** (усиление B3) | выбор меры через alias/роль, не стем имени колонки |
| D4 | `serene_ask.py:6001-6004` `_STOCK_MARKERS` | NL-список RU+EN+RO для «остаток» | **(а) новая** | маркеры остатка — intent/about моделью или словарь в данных |
| D5 | `ab_scorer.py:391-396` | эталон-ветка на `accumulationregister_реализациятмц` | **(а) новая** | классификация zero→figures по структуре SQL (`doc_date`/op), не имени okna |
| D6 | `serene_ask.py:5380-5382` `_sales_rank_top_n` | «лучших»+«три/трое/трёх» → K=3 | **(а) новая** | K только из `intent.amount` / `топ N` / plan |
| B13 | `ab-gold.tsv`, `ab-*-okna.tsv` | русские имена в **файле данных** | **(в)** | не дефект кода (`AB_GOLD_FILE` уже параметризуется); для EN — новый набор из `$metadata` (§8.5) |
| C1–C6 | build/logs/тесты/RAISE | RU в сообщениях оператору | **(в)** | косметика; на отбор сущностей не влияет (§7в) |
| L1 | `build.sh:73` + corpus `current_database()` | бывш. `'postgres'` / разбор DSN для замка | **(б)** | уже снято: имя базы у движка (Н-1/Н-6) |
| L2 | `corpus_build.sql` warehouse | бывш. `IN ('quantity','количество')` | **(б)** | уже Edm-numeric (`F6_HARDCODE_AUDIT` #11) |
| L3 | `measure_resolver.py` | бывш. список городов | **(б)** | термы только из argv (`F6` #1) |
| L4 | поля `Ref_Key` / `IsFolder` / префиксы `Catalog_` | «английские имена» в коде | **(б)** | контракт платформенного OData, не конфигурации |
| L5 | `DSN=… dbname=postgres` в скриптах | дефолт стенда | **(б)** | боевой контур задаёт DSN явно (`RUNBOOK` §10); не имя сущностей 1С |
| L6 | «B14 всегда RU-ответ» | казалось полным блокером EN | **(б)** уточнение | EN-текст при отсутствии кириллицы в вопросе уже есть; остаётся B7 (формат даты) и устойчивость языка |
| L7 | `classify_entities.py` | отбор служебных | **(б)** | решает **модель**, список слов в коде запрещён явно |
| L8 | `wiki_alias.sh` промт | «SAME language as its title» | **(б)** | правильный путь: язык из данных сущности, не словарь в коде |
| L9 | calendar / solr_synonyms / `search_meta` | имена наших таблиц / dict из env | **(б)** | законно (`F6` #3–#6, #9): не сущности клиента |

### 11.2. Сценарий англ. базы (§8) — что ломается по коду

Пока нет англ. `$metadata` у владельца, это **офлайн-прогноз** (не живой прогон).

| Этап §8 | Что стреляет | Следствие |
|---|---|---|
| 8.2 установка читателя (EN-COM) | **A1, A3** (A2 если XML без языка) | `OBJECTS: 0` / FATAL → OData без прав → витрина пустая. «Без ручного разбора» **не выполняется** |
| 8.3–8.4 синк/корпус | L1/L2/L4 — ок; **B10** — служебные EN-имена могут попасть в подсказки преполёта | корпус в целом встанет (платформенные поля EN); качество подсказок хуже |
| 8.4 словарь/алиасы | L7/L8 — ок (модель + язык title) | алиасы на EN-title возможны **если** прогон словаря живой |
| 8.5 `/ask` count товаров | **B5, B6, D1** | `catalog_products` частично в whitelist; `Prices…` без `ценыноменклатур`; остатки без оси `ТМЦ` → пустой goods-filter |
| 8.5 sum/revenue / top-N | **B1–B4, D2, D3, D6** | слабый/неверный sales-канон; шапочная сумма не находится по `*Документа`; hint `"количество"` ≠ `Quantity` |
| 8.5 relative period | **B8** | «last week» есть в json; другие относительные EN — дыра |
| 8.5 stock balance | **D4** | часть EN-фраз покрыта (`in stock`…); голый `stock`/`balance` без маркера — путь остатков может не включиться |
| 8.5 period_empty текст | B14/L6 | при EN-вопросе без кириллицы — EN-текст; даты всё равно **B7** `DD.MM.YYYY` |
| 8.5 приёмка AB | **B13, D5** | `ab-gold.tsv` / ветка `реализациятмц` в скорере — не прогонять; нужен свой `AB_GOLD_FILE` |

**Вердикт по п. 9 (язык):** конфигурация+масштаб на УТ уже замерены (§3–§5). По языку пункт
**не закрыт**: куча **а** установки (A1–A3) и канон ask (B/D) — настоящие привязки;
сценарий §8 **не готов к прогону** без англ. базы владельца. После правок — регрессия
первой базы (§8.6).

### 11.3. Что оркестратору (код не здесь)

Приоритет снятия: **A1→A3** (установщик) → **D1+D2+B5** (структура, не стемы) →
**B2/B3/D3** (sales-канон) → остальное B/D. `serene_ask.py` / `windows/odata-setup` в
этом заходе **не трогались**.
