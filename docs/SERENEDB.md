# SereneDB — движок отчётов/аналитики над 1С (решение владельца 2026-07-23)

> Владелец знает фаундеров лично, доверяет команде → строим витрину/аналитику на **SereneDB**.
> Установка/раскатка — `ubuntu/serenedb/README.md`. Бот-слой — `docs/OPENCLAW_BOT.md`.

## Что это и зачем
SereneDB (Apache-2.0 single-node, Berlin 2025) — одна БД, объединяющая то, что мы иначе
стыковали бы руками: **полнотекст + вектор (HNSW/BM25) + колоночный OLAP** (Velox; под капотом
DuckDB) с real-time обновлениями, всё через **PostgreSQL-протокол**. Проверено локально: коннект
`psql`, представляется как `PostgreSQL 18.3 (SereneDB 26.07.3)`, агрегации GROUP BY считают верно.

Закрывает наши три задачи разом:
1. **Витрина-аналитика** (колоночный OLAP) → скорость на объёме, точные суммы/обороты по ВСЕМ строкам.
2. **Семантический резолвер** (vector+fuzzy) → 2-этап «найти нужную сущность по смыслу → точно
   посчитать», можно одним гибридным запросом (см. `OPENCLAW_BOT.md` про NL→запрос).
3. **Свежесть** — real-time ingestion.
Postgres-фронт → низкий порог входа (у нас уже Postgres/pgvector; переезд почти drop-in).

## Почему риск молодой БД у нас ОГРАНИЧЕН
SereneDB держит **производную витрину, пересобираемую из 1С в любой момент**. Источник истины —
1С (read-only, не трогаем). Даже полная потеря витрины = пересинхрон из 1С, данные не теряются.
Поэтому ставить на неё аналитику/поиск — низкий риск (это НЕ система-оф-рекорд).

## Как развёрнуто (.42)
Сервис `serenedb` (systemd, **enabled = reboot-safe**), бинарь `/usr/local/bin/serened`, конфиг
`/etc/serenedb/serened.conf`, данные `/var/lib/serenedb`, под юзером `serened` (не root).
**Только loopback `127.0.0.1:7890`** — наружу не выставлено. Версия 26.07.3.

## Безопасность
- Слушает только loopback; доступ локальный (как Postgres:5432 на этом же боксе).
- Есть `--hba_config` (pg_hba-стиль), `--auth_api_key/bearer`, TLS — включим при выносе за хост.
- Инвариант 1С read-only не затронут: SereneDB читает выгрузку, к 1С не ходит.

## Статус (2026-07-23)
- [x] Установлена, systemd/loopback/enabled, коннект по PG-протоколу, агрегация верна, данные
      переживают рестарт сервиса. ✅ Фундамент готов.
- [x] **Этап 1 (POC на реальных данных 1С):** `Catalog_КлассификаторБанков` (2779 строк) через
      OData → CSV → SereneDB (`read_csv`) → агрегация «банков по городам». **Замеры:** пул из
      OData 1.12с; загрузка 235 мс; count/distinct 1.4 мс; GROUP BY city 3.2 мс. Результат верный
      (Москва 630, СПб 172, Ростов 154…). Конвейер 1С→OData→SereneDB→отчёт работает. Скрипт-проба
      `ubuntu/serenedb/poc_load_entity.py` (конфиг-нейтрально, авто-колонки, любая сущность аргументом).
      ⚠️ В тестовой базе НЕТ бизнес-транзакций (только классификаторы) — «продажи по месяцам»
      покажем на реальной ERP; 2.8к строк движок не грузит (миллионы = его профиль, замерим позже).
- [~] **Этап 2 (ядро готово + доказано на реальных данных):** `ubuntu/serenedb/report_query.py` —
      интроспекция схемы (+ **авто-примеры значений**, без хардкода) → LLM превращает вопрос в
      read-only SELECT → **read-only валидатор** (только SELECT/WITH, режет DELETE/DROP/…) →
      выполнение → показываем результат И **сам SQL** (прозрачность трактовки). Проверено на банках:
      • «топ-5 городов по числу банков» → верно (Москва 630…);
      • «банков в Москве?» — сперва LLM сделал точное сравнение → **0 (неверно)**, прозрачность это
        показала; после добавления примеров значений (`Г. МОСКВА`) → `LIKE '%г. москва%'` → 636. ✅
      • «удали все банки» → LLM выдал `DELETE`, **валидатор отклонил**. ✅ (инвариант read-only держится)
      Урок: числа реальны из SQL, но риск — «понял не так»; лечим прозрачностью + примерами значений +
      (Этап 3) семантическим резолвером + сверкой.
- [x] **Инструмент `report_1c` в боте — ЖИВОЙ в Telegram.** Отдельный MCP-сервер `1c-mcp-reports`
      (:6015, systemd, enabled) поверх `serene_report.py`; подключён `openclaw mcp add
      second-brain-reports … --include report_1c`. Бот сам маршрутизирует: аналитика→`report_1c`,
      факт/текст→`ask_1c` (персона `AGENTS.md`). Гейт заземляет числа обоих (`toolNames:
      [ask_1c, report_1c]`). Проверено в Telegram: «топ-5 городов по числу банков» → бот выбрал
      `report_1c`, отдал верную таблицу (Москва 630…), гейт allow (`hasRef=true`). ✅
      Тюнинг: счётчики отчётов бывают 3-значными (< `minDigits`=4) → сейчас не проходят числовую
      сверку (но берутся из таблицы инструмента = верны). При желании — `minDigits`=3 для отчётов.
- [x] **График картинкой в Telegram — ЖИВОЙ.** `render_chart` (matplotlib Agg, DejaVu/кириллица) →
      PNG; `report_1c` пишет файл и просит бота отправить его `message`'ом (штатный паттерн `diffs`).
      Проверено: `sendPhoto deliveryKind=photo` — bar-chart «топ-5 городов» ушёл в Telegram. ✅
      🔑 Находка: файл-медиа движок отдаёт клиенту ТОЛЬКО из **разрешённой директории** (workspace
      бота / media-store) — иначе `OutboundDeliveryError: not under an allowed directory`. Поэтому
      `CHART_DIR = <workspace>/charts` (env). Inline-картинка из MCP-результата НЕ проходит (та же
      песочница) — нужен файл под workspace + отправка агентом. Персона `AGENTS.md` это делает.
- [x] **Read-only РОЛЬ в SereneDB (защита в глубину, кодом БД).** Бот-отчёты ходят под ролью
      `serene_ro` (LOGIN + GRANT SELECT на public + default privileges на будущие таблицы);
      загрузчик витрины — под rw (postgres). Проверено: под ro SELECT работает, CREATE/INSERT/
      UPDATE/DROP → `permission denied`. Сверх валидатора «только SELECT» — теперь и БД физически
      не даёт писать. Setup: `ubuntu/serenedb/setup_ro_role.sql`; пароль в env (не git).
      ⚠ Схему бот читает через `duckdb_columns()` (под ro `information_schema.columns` пуста).
- [ ] Мелочь Этапа 2: бот иногда шлёт лишние текстовые чанки рядом с графиком.
- [x] **Этап 3 v1 — резолвер (лексический fuzzy).** `resolve_hints` (serene_report): термины вопроса
      → ТОЧНЫЕ значения колонок-измерений (низкая кардинальность; сущности-имена отсекаются), падежи
      через стем + `jaro_winkler` + LIKE; подсказки в промт `gen_sql`. Проверено: «ростове» →
      `WHERE city IN ('Г. РОСТОВ-НА-ДОНУ','Г. РОСТОВ-НА ДОНУ')` (подсказал вариант без дефиса). Под ro-ролью.
- [x] **Этап 3 v2 — семантический (векторный) слой на Qwen.** Эмбеддер — РОВНО как braine:
      **Qwen `text-embedding-v4` @ 1536** (DashScope; ollama НЕ используем). `build_resolver_index.py`
      эмбеддит distinct значения колонок-измерений → таблица `resolver_index(…, emb FLOAT[1536])`;
      резолвер ищет ближайшее к термину (косинус, `array_cosine_similarity`, под ro). Проверено:
      «спб»/«питере» → `Г. САНКТ-ПЕТЕРБУРГ` → `WHERE city='Г. САНКТ-ПЕТЕРБУРГ'` (лексика бы не смогла).
      Живой `report_1c` уже с семантикой. ⚠ HNSW-индекс в SereneDB 26.07.3 недоступен (`access method
      hnsw does not exist`) — сейчас перебор косинуса (240 значений = мс; для больших справочников
      добавить индекс/новую версию). Границы: многословные разговорные («северная столица») пофразно
      не резолвятся. Индекс пересобирать при обновлении витрины (`build_resolver_index.py`, под rw).
- [x] **Штатный синк ETL→SereneDB.** `serene_sync.py` (под rw): цикл по выбранным сущностям из
      `/etc/1c-etl-selected.txt` (те же галочки, что braine) → `load_entity()` (OData → CSV → таблица
      SereneDB, полная идемпотентная перезагрузка + GRANT SELECT ro-роли) → пересборка `resolver_index`.
      Systemd `1c-serene-sync.timer` **ночью 03:40** (после ETL), enabled/reboot-safe. Смоук: загружено
      10 бизнес-таблиц (контрагенты/организации/склады/статьи ДДС…), пустые/`Not Found` пропускаются,
      резолвер пересобран (472 значения). Инкремент по дате — оптимизация позже (для больших документов).
- [x] **Grain-инвариант витрины — «одна строка на объект 1С» (общий фикс целостности, кодом). ПРИМЕНЕНО.**
      Диагностика по «топ-5 банков в Казани»: бот вернул 5 строк = 2 банка, хотя в Казани 37 банков.
      Причина оказалась НЕ в запросе и НЕ в гейте (цифры совпали с выборкой) — **битая витрина**:
      `banks` 2779 строк при лишь 2058 уникальных `ref_key`. Стабильная перепроверка (`$orderby=Ref_Key`)
      показала: источник даёт **2779 РАЗНЫХ** банков → кривая пагинация не просто задвоила 721 строку,
      а **721 банк ПОТЕРЯЛА** (их места заняли дубли). Значит дедуп на месте не годился — нужен перезалив.
      Корень — пагинация OData `$skip/$top` **без сортировки**: 1С не гарантирует порядок между страницами
      → страницы перекрываются (одни строки дважды, другие пропущены). Лечение — общее, в загрузчике
      (`poc_load_entity.py`), без единого правила «под вопрос»: (1) тянем с `$orderby=Ref_Key`
      (универсальный ключ объекта 1С, определяем по данным) → страницы не перекрываются; (2) дедуп-сеть
      на укладке — `QUALIFY row_number() OVER (PARTITION BY ref_key …)=1` (без ref_key — `SELECT DISTINCT *`);
      (3) стат загрузки: `rows` (grain) + `rows_raw` (отдача OData — видно перекрытие).
      `banks` — витрина-проекция (англ. схема Город→city…), пересобрана скриптом `rebuild_banks.py`
      (переиспользует общий стабильный `fetch_all`). **Проверено live:** `banks` → 2779/2779, Казань → 37
      разных банков (было фейковые «2»), «5 банков» → 5 РАЗНЫХ; `resolver_index` пересобран (626 значений,
      `banks.city` 394 города). Деплой на .42 + пересборка выполнены (владелец одобрил).
      **Урок:** «понял вопрос не так» — не единственный класс риска аналитики; есть ещё «данные грязные».
      Оба лечатся ОБЩИМ кодом (инвариант загрузки / прозрачность трактовки), не патчами под формулировку.
      `banks` (БИК-классификатор) обновляется штатным ночным синком отдельным шагом (`serene_sync` →
      `rebuild_banks` → резолвер), т.к. это витрина-ПРОЕКЦИЯ, а не сырьё из списка сущностей
      (`Catalog_Банки` в списке = банк компании, 1 строка, другой объект). Расхождения с 1С не копится.
- [ ] **Этап 4:** миграция RAG braine на SereneDB (полнотекст+вектор в одном), вывод старого
      стека (OWUI/pgvector/oikb) — только после проверки на реальных данных, бота не ломаем на ходу.
- [ ] Пред-агрегация/партиции — **только если замеры покажут, что тормозит** (не заранее).

## Реальность данных 1С-базы (замер OData 2026-07-23) 🔴 ВАЖНО
Проба служебного документа OData вскрыла главное про источник:
- Подключённая база — **полноценная конфигурация** (ERP-класса): **2968 сущностей** (1419 справочников,
  1549 документов). Но **бизнес-данных в ней почти нет**: `Catalog_Номенклатура`=0, `Контрагенты`=2,
  `Организации`=1; **все документы = 0** (проверено 60 верхнеуровневых: продажи/поступления/платежи/касса
  — пусто). Единственная населённая таблица — встроенный `Catalog_КлассификаторБанков` (2779).
  → Поэтому «банки в Казани» работают, а «продажи по месяцам» пока невозможны: **данных нет**, не код виноват.
- **Регистры (Accumulation/Accounting/Information/Calculation) в OData НЕ опубликованы — 0 по префиксу.**
  А обороты/остатки/суммы в 1С живут именно в регистрах → **готовых агрегатов через OData нет**.
- Список синка `1c-etl-selected.txt` содержит имена не из этой конфигурации (отсюда 404 в синке):
  `Catalog_Договоры` → реально `Catalog_ДоговорыКонтрагентов`; плоских `ЕдиницыИзмерения`/`Кассы` нет
  (есть `Catalog_КлассификаторЕдиницИзмерения`; каталога касс нет). Файл, возможно, общий с braine ETL —
  **не трогал**; чинить имена у источника выбора.

**Вывод:** аналитический стек (SereneDB + NL→SQL + резолвер + гейт + графики) готов и корректен, но
упирается в ДАННЫЕ, а не в код. Для реальной аналитики нужны две вещи на стороне 1С (решение владельца):
(1) база с реальными данными (эта — пустая); (2) публикация нужных `AccumulationRegister` (продажи,
выручка, ДС…) в стандартный интерфейс OData 1С — тогда SereneDB считает обороты по периодам. Документы
даже заполненные дают строки, а не готовые проводки/обороты.

## Защита от рассинхрона выбора сущностей с OData (без хардкода)
**Урок:** в списке синка `1c-etl-selected.txt` 3 из 23 имён были написаны «как обычно в 1С» и не
сверены с этой конфигурацией → молчаливый 404, висели незамеченными. Наш косяк — не сами имена, а то,
что пайплайн их **тихо глотал**. Принцип-лекарство: **правда об именах берётся из живого OData, а не из
предположений; ручной выбор валидируется против источника и подсказывается семантикой.** Никаких
имён-констант.
- [x] **Слой 1 — преполёт-сверка (`serene_sync._preflight`).** Перед загрузкой тянем служебный документ
      OData (`poc_load_entity.published_entity_sets`) и сверяем весь выбор. Несуществующее имя →
      **громко** + прогон помечается failed (`sys.exit(3)` → systemd покажет unit failed, не упустить).
      Валидные сущности при этом грузятся; невалидные пропускаются (не долбим заведомый 404).
- [x] **Слой 2 — подсказка ближайших.** Для мёртвого имени показываем ближайшие реальные (сравнение
      по ядру без префикса + длина наибольшего общего фрагмента, в пределах типа): `Договоры →
      ДоговорыКонтрагентов`. **Не подменяем молча** — только показываем (иначе новый «понял не так»).
      При **неоднозначности** (много равно-близких, напр. десятки «Договор…») показываем **несколько**
      (до 3 + «…»), а не один наугад — выбор за человеком. Слабое совпадение → «похожего нет».
- [x] **Слой 3 — свой список у serene, из реальности, в git.** Ушли от рукописной копии braine:
      serene владеет **собственным** файлом `ubuntu/serenedb/serene-entities.txt` (версионируется,
      деплоится рядом с кодом, `serene_sync` читает его по умолчанию — не `/etc/1c-etl-selected.txt`).
      Файл **сгенерирован и провалидирован против живого OData** (21 имя, все реально опубликованы;
      `Договоры`→`ДоговорыКонтрагентов`, несуществующие `Кассы`/`ЕдиницыИзмерения` убраны). Копий,
      которые разъезжаются, больше нет; правки — через git + преполёт. Braine держит свой список
      (разные системы: RAG vs аналитика — совпадать не обязаны).
      Тот же принцип шире списка: пайплайн выводит структуру из источника (интроспекция), а не из
      предположений; где нужен ручной ввод — валидировать против источника.
      ↳ Опционально (не блокер): интерактивный picker «галочки прямо по живому OData» — UX-удобство
      поверх уже надёжной основы; когда/если понадобится массовый ре-выбор сущностей.

## Тесты (ловят класс «правдоподобно, но неверно»)
Урок банковского бага: один вопрос вскрыл большую дыру, потому что **не было теста на этот класс**.
Теперь — детерминированные наборы (read-only / чистые функции), годятся как гейт перед деплоем/после синка:
- **`test_integrity.py`** — целостность витрины: T1 grain (`count(*)==count(distinct ref_key)`),
  T2 витрина==живой OData (нет потерь/дублей), T3 resolver sanity, T4 нет «фантомных» строк.
  **Поймал бы банковский баг сам** (grain FAIL). Live: 12 таблиц — ВСЁ PASS.
- **`test_validate.py`** — валидатор read-only SQL, изощрённо: MUST_PASS (не ломать аналитику) +
  MUST_FAIL (запись/инъекция/**чтение файлов**). **Вскрыл 7 багов:** `replace()` и ключевое-слово-
  в-строке ложно резались; и **5 ДЫР** — `read_csv`/`read_text`/`glob`/`read_blob`/`read_parquet`
  просачивались → **утечка файлов сервера** (`/etc/passwd`, ключи). Исправлено: strip строковых
  литералов перед проверкой, `replace` убран из запретов, `FS_ACCESS` режет файловые табличные функции.
  Прогон после правки — ВСЁ PASS (25 кейсов).
- **`test_ro_role.py`** — защита в глубину: под ролью `serene_ro` любая запись (INSERT/UPDATE/DELETE/
  DROP/CREATE/TRUNCATE) отвергается БД, GRANT-эскалация = no-op, SELECT работает. Даже в обход
  валидатора писать нельзя. PASS.
- **`test_e2e.py`** — интеграционный гейт над реальным `run_report` (NL→SQL→validate→exec под ro):
  ground-truth якоря (факт 37/2779 обязан быть в ответе) + danger-wiring (валидатор вшит в путь).
  Устойчив к вариативности LLM. No-data/фейк-метрика — на независимых тест-агентах (нужна оценка смысла).
- **`verify-plugin/test-verify.mjs`** (гейт бота) — +7 adversarial: вскрыл **ложное substring-заземление**
  (короткое выдуманное число проходило как подстрока обоснованного/склейки) → фикс (точный токен), 36 PASS.
- Харнесс `probe.sh`/`sql.sh` — прогон вопросов и ground-truth под `serene_ro` (для e2e и тест-агентов).
  ⏳ Глубже (belt-and-suspenders, не блокер): движковый `SET enable_external_access=false` на ro-пути
  отчётов снял бы зависимость от денай-листа функций (денай-лист может отстать от новых функций DuckDB).

## Backup/restore (проверено 2026-07-23)
- **Стратегия:** первичное восстановление витрины — **ре-синк из 1С** (`serene_sync`), т.к. витрина
  ПРОИЗВОДНАЯ (не система-оф-рекорд). Быстрый restore без пула OData/ре-эмбеддинга — **оффлайн-снапшот**
  `/var/lib/serenedb` (`ubuntu/serenedb/serenedb_backup.sh`, ротация KEEP).
- **Проверено round-trip:** снапшот → дроп таблицы → restore из снапшота → данные вернулись (маркер
  777), витрина+резолвер целы. Онлайн-пути: `EXPORT DATABASE` только в writable-подпапку (sandbox
  ProtectSystem); `pg_dump` не годится (version mismatch — SereneDB не полноценный PG).

## Открытые вопросы фаундерам (контакт владельца)
- Штатный online-backup/PITR single-node (кроме снапшота/EXPORT) — есть ли?
- Граница open-source / commercial (HA, scale-out, HNSW-индекс — в 26.07.3 `CREATE INDEX USING HNSW` нет).
- Прод-готовность single-node, durability под нагрузкой; реальная скорость на нашем объёме (замер, не маркетинг).

## Инвариант
Отчёт/график достоверен **по построению**: числа считает наш SQL из реальных данных, не LLM.
LLM только (а) понимает вопрос и (б) оживляет текст — а его гейт (`verify-plugin`) сверяет.
Новый класс риска здесь — не «выдумал число», а «понял вопрос не так» → страховка = прозрачность
трактовки (метрика/разрез/период/фильтры рядом с ответом) + сверка итогов с родным отчётом 1С.
