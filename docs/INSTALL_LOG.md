## 2026-08-29 — okna (gpu-erw.timpul.pro): env и словарь (инцидент-починка)

- `/etc/1c-serene-ask-postgres.env`: добавлено `DEEPSEEK_MODEL=Qwen3.8-27B`
  (ночной рестарт vLLM сменил имя модели; ask звал deepseek-v4-pro → 404 на всех
  LLM-звеньях; бэкап `.bak-model-20260829` рядом).
- `/etc/1c-serene-ask-staging.env` — оверрайд-файл стейджинга (порт 8092):
  systemd `EnvironmentFile` перекрывает `Environment=`, порт задаётся только
  отдельным файлом ПОСЛЕДНИМ в цепочке.
- `search_entity_alias` строка catalog_контрагенты = «Контрагенты, клиенты,
  поставщики, список партнеров, Покупатели» (восстановление после затирания,
  бэкап /tmp/kontr-alias-backup-20260828.txt на окне); `VACUUM (REFRESH_TABLE)`;
  `search_dict_syn` пересобран из боевой (17 правил).
- Выкат ask-кода: скрипт `work/deploy-ask.sh` (md5-дифф → scp → atomic mv;
  сверять md5 z-файлов после выката).

# Журнал установок и проблем

Что ставили, что сломалось, как чинили. Ведётся по ходу работ — чтобы на новом сервере не наступать
на те же грабли и чтобы понимать, из чего складывается развёртывание коробочного продукта.

Формат записи: **что делали → что пошло не так → как решили**. Нерешённое помечено ⛔.

---

## 2026-07-25

### Windows: `setup-1c-odata.exe` — автонастройка 1С/IIS/OData
Собран прямо на машине компилятором из состава .NET Framework, зависимостей нет.
Прогнан полный цикл: снос слоя IIS → установка с нуля → восстановление канала.

**Проблемы и решения:**
- **`git pull` молча падал.** Готовый exe был закоммичен туда же, куда `build.cmd` кладёт локальную
  сборку → git отказывался перезаписать неотслеживаемый файл, HEAD застревал, собирался старый код.
  → Сборка переведена в `build/` (в `.gitignore`), готовый бинарь остался в `dist/`.
- **Права выдавались заново каждый запуск** (`icacls /T` по всей базе) → добавлена проверка по
  идентификатору пула перед выдачей.
- **Ротация бэкапов снесла чужую копию** — имена совпадали с теми, что делает `backup-1c.ps1`.
  → Свои копии помечаются `_setup-odata_`, ротация ищет строго по метке.

### Windows: состав OData расширен до регистров
`--scope analytics`: состав 1128 → 3645 объектов, сущностей 2968 → 4585.
- **Каветат:** права 1С действуют **поверх** состава — часть подсистемных регистров под read-only
  профилем отдаёт «Доступ запрещен» (код 20). Это защита, а не сбой.

### Windows: VC++ x64 runtime — установлен, оставлен
Понадобился для 64-битных программ (1С ставила только x86). 24 МБ, безвреден.

### ✅ Windows: PostgreSQL 17.6 — УСТАНОВЛЕН (после долгой диагностики)
Цель была померить, сколько ресурсов съест СУБД рядом с 1С.

| Шаг | Итог |
|---|---|
| Скачивание бинарников (315 МБ) | ✅ 7 с, скорость ~5 МБ/с |
| Распаковка (848 МБ) | ✅ 109 с |
| `psql.exe --version` | ✅ работает |
| `postgres.exe` / `initdb.exe` | ❌ `0xC0000142` |
| То же под служебным аккаунтом `pgsvc` | ❌ та же ошибка |
| Штатный установщик EDB (348 МБ) в тихом режиме | ❌ exit=1 за 16 с, **лога не создал** |

**Причина:** серверные бинари PostgreSQL отказываются работать под администратором и пересоздают
себя с ограниченным токеном; в неинтерактивной SSH-сессии это не проходит. Установщик, судя по всему,
тоже требует интерактивного сеанса.

**Ещё две попытки обойти без RDP (обе неудачные):**
- **Установщик через планировщик задач от SYSTEM** — впервые *запустился* и создал лог (значит дело
  было в оконной станции), но упал на `The database cluster initialisation failed`. В
  `installation_summary.log` видно причину выбора аккаунта: запуск от SYSTEM заставил установщик взять
  `NT AUTHORITY\NetworkService`.
- **То же с явным `--serviceaccount postgres --servicepassword ...`** — тот же отказ на инициализации.

**Итоговая причина:** `initdb.exe` и `postgres.exe` не стартуют **в сессии 0** в принципе (та же
`0xC0000142`), тогда как `psql.exe` там работает. Это те бинари, что пересоздают себя с ограниченным
токеном. **Это не приговор PostgreSQL на Windows** — с консоли он ставится штатно. Ограничение именно
headless-подхода: нужен интерактивный сеанс.

### 🎯 НАСТОЯЩАЯ ПРИЧИНА (найдена не сразу — все версии выше были ошибочными)

Дело оказалось **не в PostgreSQL, не в сеансе и не в правах**. Диагностика по шагам:

1. Загрузил каждую DLL из `bin` по отдельности → нашёл виновника:
   `icuuc67.dll` — ошибка **193 (ERROR_BAD_EXE_FORMAT)**, остальные 15 падали каскадом
   с ошибкой 126 («модуль не найден»), потому что зависят от неё.
2. Разобрал PE-заголовок: файл целый, `MZ`/`PE` на месте, machine = x64. Не порча и не битность.
3. Загрузил с флагом `DONT_RESOLVE_DLL_REFERENCES` → **загрузилась** ⇒ проблема в зависимостях.
4. Проверил зависимости ICU — библиотеки MSVC:
   `msvcp140.dll`, `vcruntime140_1.dll`, `msvcp140_1/2.dll`, `concrt140.dll` → **ошибка 193**,
   хотя физически лежат в `System32` с правильной версией 14.44.
5. Проверил по полному пути — та же ошибка ⇒ **системный VC++ runtime был нерабочим**.

**Причина:** мой ранний `vc_redist.x64.exe /install /quiet /norestart` отчитался `exit=0`,
но оставил runtime в нерабочем состоянии (файлы разложены, но не активированы — нужен был перезапуск).

**Решение — одна команда:**
```
vc_redist.x64.exe /repair /quiet /norestart
```
После неё: `msvcp140.dll` грузится, `initdb --version` → `initdb (PostgreSQL) 17.6`.

**Урок:** `/norestart` у VC++ redist может оставить систему в состоянии «файлы есть, версия правильная,
но ничего не грузится». Признак — ошибка **193 на заведомо валидной x64-библиотеке**.
После установки runtime обязательно проверять загрузку, а не доверять `exit=0`.

### Результат установки
```
initdb        exit=0 за 7 с, кластер 39 МБ
служба        postgresql17 = Running, автозапуск
порт          5432 слушает
версия        PostgreSQL 17.6 on x86_64-windows, msvc-19.44
```

**Сколько ест в покое (замер):**

| Показатель | Значение |
|---|---|
| процессов | 6 |
| RAM (WorkingSet суммарно) | **73 МБ** |
| RAM (уникальной, Private) | **17 МБ** |
| `shared_buffers` по умолчанию | 128 МБ (16384 × 8 КБ) |
| CPU в покое | 3.5% (вместе со всей машиной) |
| место на диске | кластер 39 МБ + установка ~800 МБ |

Свободной памяти на машине осталось 6 из 8 ГБ. IIS и 1С работают.

**Ложный след, стоивший времени:** проверка `cmd /c "... & echo EXIT=%ERRORLEVEL%"` показала `EXIT=0`
и создала иллюзию, что всё работает. `%ERRORLEVEL%` в однострочнике `cmd /c` подставляется **до**
запуска команды. Нужна отложенная подстановка либо отдельный запуск.

**Убрано после неудачи:** `C:\pgsql`, `C:\pgdata`, `C:\PostgreSQL`, дистрибутивы (663 МБ), аккаунт
`pgsvc`. IIS и канал 1С проверены — работают.

### ⛔ SereneDB на Windows — невозможно нативно
Проверено по репозиторию, не по догадкам: CI собирает и публикует только `linux-amd64`,
`linux-arm64` и Docker-образы. `windows-latest` в workflow относится к `serene-ui` (клиенту), не к
серверу. CONTRIBUTING: единственный тулчейн — clang-21 + Ninja.

Варианты запуска на Windows: WSL2, Docker Desktop (сам поверх WSL2), Hyper-V.
**WSL на стенде не установлен** — требуется `wsl --install` и перезагрузка.

### Ubuntu: баг шлюза на кириллице — исправлен
Кириллица в строке запроса (`$select=СуммаДокумента`) роняла прокси:
`'ascii' codec can't encode characters`. `self.path` у `BaseHTTPRequestHandler` декодирован как
latin-1, а `http.client` требует ASCII.
→ Добавлена функция, кодирующая **только не-ASCII**; уже закодированное не портится.
Проверено: было 400 → стало 200; запись по-прежнему режется 405.

### Ubuntu: авария DeepSeek — исправлена
**24.07 в 15:59 UTC DeepSeek снял `deepseek-chat`** → весь LLM-путь отвечал HTTP 400.
→ Переведено на `deepseek-v4-flash`; режим размышлений выключается параметром
`"thinking":{"type":"disabled"}` (найден экспериментом: 3 токена против 28–75;
`enable_thinking` и `chat_template_kwargs` молча игнорируются).
→ **Имя модели вынесено в конфиг** (`DEEPSEEK_MODEL`/`DEEPSEEK_THINKING`), чтобы следующее
переименование не роняло прод. Конфиг бота OpenClaw обновлён тоже.

### 1С: тестовые данные заведены
104 проведённых документа за 6 месяцев (280 проводок, 44 записи в регистрах накопления),
13 контрагентов, 14 номенклатуры. Перед этим — бэкап базы.
- **Регистров накопления заполнилось 2 из ~200** — корректно: у организации УСН «Доходы», и
  БП 3.0 держит товарные остатки в регистре бухгалтерии, а не в отдельных РН.

### 📊 ЗАМЕР: PostgreSQL на Windows рядом с 1С (эксперимент завершён)

**Топология:** изначально LXC и Windows связаны только пробросом порта 80 (для OData) — остальные
порты закрыты, поэтому первый заход пришлось делать SSH-туннелем через рабочую станцию по VPN.
**Владелец пробросил 5432**, и замеры переделаны по настоящей сети. Разница показательна:

| Запрос (200 тыс. строк) | Через VPN-туннель | **По сети** | Разница |
|---|---|---|---|
| `count(*)` | 5395 мс | **268 мс** | ×20 |
| `GROUP BY` город + `SUM` | 4462 мс | **238 мс** | ×19 |
| Числовой фильтр | 2443 мс | **107 мс** | ×23 |
| Постройка индекса | 14 965 мс | **2922 мс** | ×5 |
| Поиск по городу через индекс | 154 мс | **5 мс** | ×30 |

Вывод на будущее: **никогда не мерить производительность через обходные туннели** — искажение
до 30 раз.

#### Ресурсы на Windows (замер локальный, без сети — потому достоверный)

| Состояние | postgres RAM | CPU | RAM свободно |
|---|---|---|---|
| В покое | **73 МБ** (6 процессов) | 3.5% | 6 из 8 ГБ |
| После заливки 200 тыс. строк | 119 МБ | 2% | 6 ГБ |
| Под нагрузкой (2 потока аналитики) | **128 МБ** | **81.5%** | 5.7 ГБ |

**Влияние на 1С — главный вопрос:**

| | Задержка OData |
|---|---|
| 1С при простаивающем Postgres | **77 мс** (74/64/91/78) |
| 1С при Postgres, грузящем CPU на 81% | **83 мс** (89/75/99/68) |

**+8% к задержке 1С при загрузке процессора на 81%.** То есть на 2-ядерной машине с 8 ГБ
PostgreSQL с 200 тыс. строк соседствует с 1С практически незаметно.
30 аналитических запросов по 200 тыс. строк локально — **3.1 с**.

#### Два режима работы SereneDB с данными на Windows

| Режим | Что происходит с данными | Замер ПО СЕТИ (200 тыс. строк) |
|---|---|---|
| **Живое чтение** (`ATTACH`, без индекса) | **остаются на Windows** | `count(*)` 268 мс; группировка+сумма 238 мс; числовой фильтр 107 мс; период+топ 125 мс |
| **Индекс над VIEW** | втягиваются в SereneDB | постройка 2.9 с; город 5 мс; число 3.8 мс; текст+число 4.7 мс; агрегация 7.9 мс |

**Сверка корректности — цифра в цифру с прямым SQL по источнику:**

| Метрика | Через индекс | Эталон |
|---|---|---|
| свыше 500 тыс | 91 076 | 91 076 |
| Казань | 40 000 | 40 000 |
| Казань + свыше 500 тыс | 17 951 | 17 951 |

**Главный размен:** живое чтение — сотни миллисекунд, данные не покидают Windows.
Индекс — единицы миллисекунд (**в 30–50 раз быстрее**), но проиндексированное копируется к нам.

#### ⚠ Найдено: NUMERIC нельзя индексировать
```
ERROR: Column 'amount' has unsupported type DECIMAL(14,2) and can not be indexed
```
Денежные колонки в PostgreSQL обычно `numeric` — инвертированный индекс их не берёт.
Лечится приведением **в самом VIEW**: `SELECT ..., amount::DOUBLE AS amount FROM ...`.
Это надо закладывать в генератор представлений.

#### Вывод
Опасение «СУБД рядом с 1С задушит машину» **не подтвердилось**: +8% к задержке 1С при пиковой
загрузке CPU, 128 МБ памяти. Но напомню про главное ограничение — **SereneDB под Windows не собирается**,
поэтому схема «всё у клиента» всё равно требует Linux-машины; PostgreSQL на Windows решает только
задачу «данные остаются у клиента», и то частично (индекс копирует к себе то, что индексирует).


---

## Грабли окружения (повторяются, держать под рукой)

### Kimi Code
- **`su - claudedev -c '... kimi'` давал старый бинарь** (обрыв 22.08, 40111 «no credential
  configured»). Экспорт `~/.kimi-code/bin` в PATH жил в `.bashrc`, который для неинтерактивного
  шелла отсекается по `case $- in *i*` → резолвился `/usr/local/bin/kimi` 0.33.0, а он не читает
  креды формата 0.38.0. → Дубль PATH в `~claudedev/.profile` (читается логин-шеллом всегда);
  `/usr/local/bin/kimi` обновлён до 0.38.0. Правило: версию проверять `su - claudedev -c 'which kimi;
  kimi --version'`, а не из интерактивного шелла.
- **Канон `work/hooks/kimi-config.toml` должен совпадать с эпохой бинаря**: 0.38.0 требует
  `base_url = https://api.kimi.ai/coding/v1` и `oauth_host = "https://auth.kimi.ai"` в секциях
  oauth; конфиг от 0.33.0 (`api.kimi.com`, без oauth_host) даёт тот же 40111 даже с живым токеном.

### PowerShell
- **`$args` — автоматическая переменная.** Назвал так параметр функции → всё сломалось.
  Аналогично `R` — это алиас `Invoke-History`, функцию с таким именем не вызвать.
- **Кириллические члены COM не резолвятся напрямую:** `$obj.Наименование` возвращает пусто,
  `$ib.Справочники` — NULL. Работают английские псевдонимы (`$obj.Description`) и `InvokeMember`
  для обоих языков. Динамические члены менеджеров (`Справочники.X`) не резолвятся вообще —
  обход через запрос + `ПолучитьОбъект()`.
- **Длинные скрипты — только `-EncodedCommand`** (base64 от UTF-16LE), иначе ломается кириллица.
  Совсем длинные — файлом через `scp`, обязательно **UTF-8 с BOM**.
- **Заголовок `Range` в `Invoke-WebRequest`** задать нельзя — использовать `curl.exe` (есть в Windows 10+).

### 1С через COM
- **COM-коннектор 32-битный** (платформа x86) → запускать
  `C:\Windows\SysWOW64\WindowsPowerShell\v1.0\powershell.exe`, иначе `Class not registered`.
- **Платёжные документы молча не делают проводок**, если в строке `РасшифровкаПлатежа` не заполнить
  `КурсВзаиморасчетов`, `КратностьВзаиморасчетов`, `СуммаВзаиморасчетов`. Документ при этом
  записывается и помечается проведённым, ошибок нет, а движений ноль.
- **У банковских документов два разных «счёта»:** `СчетОрганизации` — ссылка на справочник
  банковских счетов, `СчетБанк` — счёт учёта из плана счетов.
- **Безопасный режим** (`ОбщегоНазначения.ВыполнитьВБезопасномРежиме`) удобен для экспериментов —
  всё откатывается; но **сохранить через него ничего нельзя** (БСП оборачивает в транзакцию и
  блокирует фиксацию). Запись — только прямыми COM-вызовами.

### OData 1С
- **Стандартные поля именуются по-английски** даже в русской конфигурации: `Ref_Key`, `DataVersion`,
  `Posted`, `Date`, `Description`, `IsFolder`. Пользовательские реквизиты — на языке конфигурации.
- **Служебный документ отдаётся в XML**, не в JSON (нужен `$format=json` или парсить XML).
- **Ключ у каждого типа объявлен в `$metadata`** и он разный: `Ref_Key`, `Ref_Key + LineNumber`,
  `Recorder + Recorder_Type`, встречается и кириллический (`Организация_Key`).

---

---

## 22.08 — переезд okna (167.233.249.110 → gpu-erw LXD) [замер]

**Что делали:** перенос боевого контура okna на новую коробку (LXD `10.10.10.12` на
`gpu-erw.timpul.pro`, хост **gpu-1c** `178.63.211.188`, RTX 6000 Ada, **125 ГБ RAM / 40 vCPU / 400 ГБ**,
Ubuntu **26.04**).

### Доступы

| Роль | Адрес / порт |
|---|---|
| Старая okna (источник) | `167.233.249.110`, SSH root + deploy-ключ с dev |
| Новая okna (цель) | `10.10.10.12`, снаружи `ssh -p 2202 root@gpu-erw.timpul.pro` |
| Сосед klient-1 (позже) | `10.10.10.11`, SSH `:2201` |
| Ключ миграции old→new | `/root/.ssh/id_ed25519_migrate` → `authorized_keys` цели; **удалить после всех миграций** |
| S3 мелочь | `s3.twcstorage.ru`, bucket `klient1-migration` (~365 МБ); store.db **не влезает** (квота ~1 ГБ, 403) |

### rsync и стена формата

- rsync `store.db` **27 206 889 472** байт за **3:54** (**110,8 МБ/с**); tar конфигов/юнитов/`/opt`/`/home/undebot` без venv.
- **26.08.1 НЕ открыл** rsync-копию: `FATAL: serialized data has 5 element(s), expected 4`.
- **26.07.3** открывает за **5 с** (`search_corpus` **1 230 156**). Dev-копия (Ф1) — 26.08.1 открыл.

### EXPORT/IMPORT (канонический путь)

Доки: `sql/statements/export_and_import_database`. Процедура и ловушки — `docs/RUNBOOK_MIGRATE_OKNA.md` §3.4.

| Ловушка | Суть | Лечение |
|---|---|---|
| 1 | IMPORT `syntax error at or near ")"` | В `schema.sql` — `USING inverted ()` (пустые скобки из `duckdb_indexes().sql`, `serene_search_build.py:55`). Удалить 3 строки; индексы — `corpus_init.sql`, `entity_card_build.sql` |
| 2 | `schema public already exists` | `DROP SCHEMA public CASCADE` перед IMPORT |
| 3 | IMPORT не переносит роли, словари, views, backing индексов | Роли из `/etc/1c-*.env`; `corpus_init.sql -v dict_locale=ru_RU.utf8`; views `wiki_entity_facts` → `wiki_pages`; DROP `search_idx`/`alias_idx`/`entity_card_idx` перед CREATE INDEX |

**Итог:** **418** таблиц count **1-в-1**; `search_idx` **1 230 156**; alias/entity_card **254**; wiki **254/351**; **26.08.1**, `:7890` отвечает.

### vSwitch 4003

Подсеть **10.3.1.0/24**, Robot → vSwitch → Add servers (иначе RX=0). На хостах: VLAN `eno1.4003`, mtu **1400**, IP **10.3.1.11** / **10.3.1.12**, маршрут `10.3.0.0/16 via 10.3.1.1`; netplan `/etc/netplan/02-vswitch.yaml`. Эмбеддеры: **10.3.1.11:8000**, **10.3.1.12:8002** — `EMBED_HOSTS` на okna переключён. Контейнер okna → `10.3.0.4` за **3,3 мс**.

### Что осталось

| # | Шаг | Статус |
|---|---|---|
| 1 | Пересчёт эмбеддингов 4B (`EMBED_RESET=1`, labels **254** ~1 с — идёт) | ⏳ |
| 2 | Приёмка §7 (`docs/RUNBOOK_MIGRATE_OKNA.md`): probe 8, scorer ≥14/25 | ⏳ |
| 3 | Переключение prod §8: бот, Telegram, OData :6090, DNS | ⏳ |
| 4 | Контур бота undebot + openclaw gateway на цели | ⏳ |

---

## 22.08 — Ф3: инвентарь PostgreSQL (dev) и репо-слой [замер]

**Решение 22.08:** PostgreSQL как компонент продукта выводится (`docs/PLAN_UPGRADE_NATIVE.md`
Ф3). Ниже — снимок dev без смены состояния серверов.

### Dev: `postgresql@16-main` на `127.0.0.1:5432`

| Факт | Доказательство |
|---|---|
| Кластер online | `pg_lsclusters` → `16 main 5432 online postgres /var/lib/postgresql/16/main` |
| Пакеты | `postgresql-16`, `postgresql-16-pgvector`, `postgresql-client-16` (client **оставляем** — нужен `psql` к SereneDB) |
| Слушатель | `ss -tln` → `127.0.0.1:5432` |
| Юнит | `postgresql@16-main.service` active/enabled; meta `postgresql.service` exited |
| Соединений нет | `ss -tnp \| grep :5432` → пусто (замер 22.08) |
| БД при установке braine | `openwebui`, `mem0` — `docs/UBUNTU_SETUP.md` (размеры/`pg_stat` — у оркестратора: `sudo -u postgres psql`, у `claudedev` peer/password auth не проходит) |

**Потребители `:5432` на dev — на момент замера нет.** Единственная systemd-зависимость —
`open-webui.service` (`Wants=postgresql.service`), но сам `open-webui.service`:
`disabled`/`inactive` (braine выведен 26.07). `1c-etl.service`/`timer` — `disabled`/`inactive`;
`oc_etl.py` пишет в GitLab KB, не в PG.

Все живые DSN продукта — `host=127.0.0.1 port=7890` (`build.sh`, env-файлы `1c-serene-*`).
В репозитории **нет** `port=5432` / `postgresql://` в исполняемом коде (`grep ubuntu/` — пусто;
упоминания `:5432` — только комментарии-ловушки в `onboard_unit.sh`, приборах).

**[действие владельца, в работе]:** `apt purge postgresql-16 postgresql-16-pgvector`,
`systemctl disable postgresql@16-main`, удаление `/etc/1c-pg-windows.env`.

### Windows PostgreSQL

Установлен headless 25–26.07 (ниже в этом файле). **[действие владельца, в работе]:**
остановка/удаление службы PG на Windows — оркестратор/владелец (Ф3.2 плана).

### Репо-инвентарь слоя PG / attach

| Область | Где | Статус |
|---|---|---|
| `/etc/1c-pg-windows.env` | PGW_HOST/PORT/USER/DB | **сирота:** `grep PGW_` по `ubuntu/`, `work/acceptance/`, `/opt/1c-mcp-reports`, `/opt/1c-etl` → пусто; ни один `1c-*.service` не подключает файл |
| `ATTACH (TYPE postgres)` в пайплайне | `serene_sync.py`, `build.sh`, `pipeline.sh` | **не используется** — витрина нативная |
| Запрет слова attach в SQL бота | `serene_report.py:76` | regex FORBIDDEN (не postgres_attach) |
| Целевая схема PG+attach | `docs/TARGET_ARCHITECTURE.md`, `docs/BOXED_BLOCKERS.md`, `memory_bank/owner-memory/project-architecture-pg-windows-attach.md` | **отменена 22.08** — черновик правки owner-memory: `docs/drafts/project-architecture-pg-windows-attach.md` |
| История braine+pgvector | `docs/UBUNTU_SETUP.md`, RUNBOOK §7–8 | выведено 26.07, развёртывание с нуля пропускать |
| `1c-etl` | `ubuntu/1c-etl/oc_etl.py` | KB→GitLab, не PG; timer disabled |
| «Поиск поверх Postgres» | — | **в репо нет** такой формулировки (единственное — задача Ф3 в `PLAN_UPGRADE_NATIVE.md`) |

---

## Что осталось нерешённым

| # | Проблема | Что нужно |
|---|---|---|
| 1 | PostgreSQL на Windows в контуре | **[действие владельца, в работе]** остановка/удаление службы (Ф3.2); установка headless была 25–26.07 — история выше |
| 2 | PostgreSQL 16 на dev (:5432) | **[действие владельца, в работе]** снос пакетов после подтверждения, что braine/OWUI не вернутся (Ф3.3) |
| 3 | `/etc/1c-pg-windows.env` | **[действие владельца, в работе]** удаление вместе с выводом Windows-PG |
| 4 | SereneDB на Windows | только через WSL2/Docker — требуется `wsl --install` + перезагрузка |

## 03.08 — плагин claude-code-harness 5.6.0 (пилот)

- Поставлен по слову владельца: `claude plugin marketplace add Chachamaru127/claude-code-harness`
  → `claude plugin install claude-code-harness@claude-code-harness-marketplace`
  (scope: user, `~/.claude/plugins/…` — в новых сессиях подхватывается сам).
- Что привозит: 21 навык (`/harness-plan|work|review|release|setup`, `cursor-*`,
  `failure-codifier`, `memory`…), 4 агента, 27 хуков на Go-движке (`bin/harness`,
  линукс-бинарь исполним). Всегда в контексте ~2,9k токенов.
- Проверено вживую: `rm -rf /` через его `hook pre-tool` → «ask» с причиной; безобидные
  команды — молча. Наша обвязка не задета: test-hooks 6/6, git-гейт независим.
- Особо ценное под наши болячки: file-lease на Write|Edit (двум сессиям не даст
  толкаться в одном файле — HOW_NOT_TO §3.34), PostCompact-вброс WIP после сжатия
  контекста, session-summary в журнал на Stop.
- Осторожность: при ненайденном корне его хуки fail-open («hook skipped» в stderr);
  наш git-гейт остаётся последним рубежом. Снять пилот: `claude plugin disable
  claude-code-harness`.

## 03.08 — harness: конфиг проекта, границы пилота

- `harness doctor` на проекте: хотел standalone-раскладку (`hooks/hooks.json`,
  `.claude-plugin/`). НЕ заводим сознательно: при живом плагине это двойное
  срабатывание хуков; его генератор CLAUDE.md/settings.json к нашим файлам не
  подпускаем. Заведён только минимальный `harness.toml` (i18n=en — русского у них
  нет, язык владельца закреплён в CLAUDE.md; protectedBranchPush=allow — наше
  правило «push каждый шаг»).
- Подтверждено в перезапущенной сессии: их PreToolUse живёт (предупреждения о
  защищаемых путях на CLAUDE.md и harness.toml), SessionStart-монитор, авто-оповещение
  других сессий о правке CLAUDE.md. Сводки движка — ja/en.

---

## 2026-08-03

### Выгрузка конфигурации 1С в XML (`DumpConfigToFiles`) — получилась, даёт человеческие имена

Зачем: `$metadata` OData отдаёт только **технические** имена (`СуммаРегл`,
`ПриобретениеТоваровУслуг`) и ноль аннотаций — проверено. Человеческие названия объектов и
реквизитов живут в самой конфигурации, и достать их можно только конфигуратором.

**Что делали → что пошло не так → как решили:**

- Первый запуск без пользователя ИБ → `Пользователь ИБ не идентифицирован`, 0 файлов.
  → Пользователь тот же, которым ходит шлюз OData: `ODG_USER` из
  `/etc/1c-odata-gateway-ut.env` = `Администратор (ФедоровБМ)`, `ODG_PASS` **пуст**.
  Инвариант «секреты не в командной строке» не задет: передавать нечего.
- Кириллица в аргументах через `ssh` + `cmd` калечится кодовой страницей (`chcp 65001` в той
  же строке не помогает — cmd разбирает строку раньше). → Всё, что содержит кириллицу,
  гнать через `powershell -NoProfile -EncodedCommand <base64 UTF-16LE>`; листинги каталогов
  с кириллическими именами читать так же, а не `dir`.
- `powershell` подмешивает в вывод `#< CLIXML` и записи прогресса. → `$ProgressPreference =
  "SilentlyContinue"`; файлы забирать копией на ASCII-имя и потом `cmd /c type`.

**Рабочая команда** (папки `C:\1c\dump\ut_demo` и `C:\1c\logs` создать заранее):

```
"C:\Program Files (x86)\1cv8\8.3.27.1786\bin\1cv8.exe" DESIGNER /F"C:\1c\bases\ut_demo" ^
  /N"Администратор (ФедоровБМ)" /P"" /DisableStartupDialogs /DisableStartupMessages ^
  /DumpConfigToFiles "C:\1c\dump\ut_demo" /Out "C:\1c\logs\dump_ut_demo.log"
```

Идёт минуты, кончается кодом 0. `[замер 03.08]` **60 388 файлов**; объектов верхнего уровня:
`Reports` 230, `InformationRegisters` 457, `Catalogs` 346, `DataProcessors` 239,
`Documents` 187, `AccumulationRegisters` 88. В логе остаётся предупреждение самой 1С про
имена длиннее 80 символов — на выгрузку не влияет.

Стенд: SSH на `192.168.56.1:2222`, пользователь `unde`, ключ `~/.ssh/id_ed25519_1c`. Чтение
ключа останавливает рубеж runtime, поэтому владелец поднимает `ssh-agent -a <сокет>` и
делает `ssh-add`; дальше сессия ходит с `SSH_AUTH_SOCK=<сокет>`, не читая ключ сама.

**Что выгрузка даёт** (образцы из `AccumulationRegisters\Закупки.xml` и
`Documents\ПриобретениеТоваровУслуг.xml`): у каждого объекта и каждого реквизита есть
`<Synonym>` с языковой меткой `<v8:lang>`, то есть механизм многоязычный, а не русский:

| техническое имя | `Synonym` |
|---|---|
| `ПриобретениеТоваровУслуг` | Приобретение товаров и услуг |
| `СуммаДокумента` | Сумма документа |
| `СуммаСНДС` | Сумма с НДС |
| `СуммаРегл` | Сумма регл. |
| `СуммаВВалютеДокумента` | Сумма в валюте документа |
| `Количество` (строки Товары) | Количество (в единицах хранения) |

Последняя строка показывает, ради чего всё: синоним несёт **уточнение, которого в
техническом имени нет вовсе**.

## 29.08 — веб-фронт «Server Connection Error»: чинил хоп фронт → okna [замер]

Симптом: `https://baulogistic.timpul.pro` отвечает (200, `/health` ok), но чат
пишет «Open WebUI: Server Connection Error» — вопрос до бота не доходит.

Причина (три слоя, все от переезда 22.08):

1. морда фронта (env `/home/webui/.open-webui.env`) тыкала в
   `http://10.3.0.4:18801/v1` — адрес старого бэкенда, которого больше нет;
2. web-шлюз `openclaw-gateway-web` в контейнере okna слушал только loopback
   (`gateway.bind: loopback` в конфиге; юнит не имел `--bind`);
3. наружу из контейнера никто не публиковал :18801 (контейнер за LXD-NAT
   10.10.10.0/24, у фронта в vSwitch маршрута в неё нет).

Починка (та же схема, что 6090/18090 — LXD proxy на vSwitch-адресе хоста):

- хост gpu-1c: `lxc config device add okna gw-web-18801 proxy
  listen=tcp:10.3.1.11:18801 connect=tcp:10.10.10.12:18801 nat=true`;
- okna: юниту web-шлюза добавлен `--bind lan`
  (бэкап `openclaw-gateway-web.service.bak-bind-20260829`), рестарт;
- фронт: env `OPENAI_API_BASE_URL=http://10.3.1.11:18801/v1`
  (бэкап `.open-webui.env.bak-backend-20260829`), рестарт open-webui.

Проверено: curl с фронта `http://10.3.1.11:18801/` → **200**; телеграм-шлюз
:18800 (loopback) и ask :8091 не рестартовались.

Диагностика, которая вела (чтобы не повторять обходом): контейнер окно — это
veth в lxdbr0 (10.10.10.12, DHCP), НЕ vSwitch-адрес; vSwitch-нога только у хоста
(eno1.4003 = 10.3.1.11/24, mtu 1400); «пинг в одну сторону» (окно → 10.3.0.2
проходит) — маскарад хоста, обратный коннект он не заменяет.

Хвост: Grafana-дашборды → SereneDB через `10.3.0.4:7890` — мертвы тем же
корнем, не чинено (ждёт слова владельца; способ — тот же proxy + datasource).
Скрипты `setup-okna-*-web.sh` всё ещё печатают старый `BACKEND_IP=10.3.0.4` —
поправить агентом при следующем заходе в каталог.

## 29.08 (продолжение) — веб-чат: четыре слоя починки + решения владельца [замер]

Симптом предыдущего раздела оказался только первым слоем. Полная цепочка
(каждый шаг подтверждён журналом/схемой, разведка — курсор-агент по докам
сборки 2026.7.1-2, отчёт `.claude/state/recon-openai-compat.md`):

1. **Совместимый endpoint выключен.** Переезд 22.08 перенёс `~/.openclaw`, но
   НЕ `~/.openclaw-web` (RUNBOOK_MIGRATE_OKNA) — конфиг web-профиля
   пересобрался без `gateway.http.endpoints.chatCompletions.enabled` →
   `POST /v1/chat/completions` = 404, любой неизвестный путь = HTML
   контроль-панели (ловушка: GET `/` и «200» ≠ рабочий API). Включено
   каноническим путём; ❗ `gateway.http.enabled` — невалидный путь (схема
   `$strict`, только `endpoints`/`securityHeaders`), с ним шлюз падает 78/CONFIG.
2. **Токен.** `.gateway-token` web-профиля не пережил переезд; 401. Выровнен
   `gateway.auth.token` = ключ морды (правка в одном месте вместо двух на фронте).
3. **Дефолтный агент = dict.** `agents.list` без флага `default` → первым стал
   сервисный `dict` (добавлен 27.08 для генератора синонимов) → чат принимал
   генератор словаря с strict-моделью vllm и `compat.supportsTools:false` →
   «No callable tools remain». Починено явным `{"id":"main","default":true}`
   (main наследует `agents.defaults.model.primary` = deepseek).
4. **Auth main-агента.** Ключ deepseek живёт в per-agent sqlite
   (`agents/main/agent/openclaw-agent.sqlite`), каталог не перенёсся →
   «No API key found for provider deepseek». Скопирован portable-способом из
   основного профиля (как предписывает само сообщение об ошибке).
5. **Эмбеддинг памяти.** memorySearch обоих профилей смотрел в мёртвый туннель
   thundercompute (ретраи в журнале; на ответы не влиял — «привет» отвечал 3 с).
   Решение владельца: наш эмбеддер, по домену →
   `http://gpu-erw.timpul.pro:8000/v1`, модель **Qwen3-Embedding-4B**
   (на сервисе 4B, в старом конфиге было 8B), ключ из `/etc/1c-embed.env`.
   После правки — ретраев в журнале нет.
6. **verify-гейт.** `braine-verify` отсутствовал у ОБОИХ ботов с 23.08 (stale
   entry, файлов на коробке не было вообще) — числа с 23.08 уходили без сверки
   кодом. Переустановлен штатно из репо (`npm pack` → `openclaw plugins install
   npm-pack:… --force`) в оба профиля; оба шлюза: «3 plugins: braine-verify,
   memory-core, memory-wiki».

Проверки сквозные: «скажи ок» → 3 с; «сколько покупателей было за этот
месяц» → ответ с данными за **2 мин 48 с** (тяжёлый класс, см.
`docs/drafts/s4-latency-2026-08-29.md`); `/v1/models` → настоящий список
(openclaw/default, openclaw/dict).

**Решение владельца 29.08: телеграм-бот выводится из контура.**
`openclaw-gateway` (:18800) остановлен и `disable` — ничего не удалено,
возврат одним `systemctl --user enable --now openclaw-gateway`. Рабочий
фронт — веб (baulogistic). Генератор синонимов (агент dict) живёт на
web-шлюзе и решения не затрагивает.

Хвосты (не срочно): stale-запись `plugins.entries.deepseek` (плагин не
ставился, провайдер работает нативно — убрать запись при следующем заходе);
Grafana-релей 10.3.0.4:7890 — мёртв тем же корнем, не чинен.
