# Рабочий план треков — 22.08.2026

Снимок на 22.08 ~07:10 UTC. Правило дня: факт = rid + code_md5 + порт + текст вопроса
(HOW_NOT_TO §3.94). Выкат только из HEAD; scp в /opt|/etc = гейт выката (пробы — в свежий
каталог /tmp/имени-захода, не в /opt и не в общий /tmp).

## Контекст

Баг «остатки на складах» → диагноз дня: класс-ветки в пути ответа + знание о базе в коде.
Решение: один универсальный путь (большое окно), приборы/факт-протокол (окно приборов),
гейты невозврата (окно гейтов, не запущено). Два раунда независимого аудита — приняты,
ошибки обеих сторон закрыты журналом (§3.94).

## Трек 1. Универсальный путь (большое окно) — ЖДЁТ ВОЗВРАТ-13

- Промт v7 запущен; кандидат staged, md5 ask 1ab7d928, замки 24+60+56 зелёные.
- Проба оркестратора на :8092 (22.08): **КРАСНАЯ, 0err, 5/8**:
  - воскресенье → no_data «Проверенный ответ дать невозможно» (канон верный, финал отказал;
    регрессия против возврата-12, где 0.00);
  - петли → clarify «коэфф.?», выбрана catalog_единицыизмерениятмц (старая болезнь,
    не bridge-clarify);
  - «лучше всего продавалось» → figures, гейт зарезал имя (вчера отдавал).
  - зелёные: «сколько товара на складе?» clarify ok; прошлый месяц 2 767 450.98; прайс 2384.
- Флаг аудитора: «именованность» списком токенов RU+RO + len>=3 — тот же класс хардкода;
  брать из разбора модели (terms intent).
- Дальше: окно итерирует → коммит пат-спеком → МОЯ проба :8092 → при зелёной выкат на
  :8091 (юнит **1c-serene-ask@postgres**, не @okna!) → scorer 25 с построчным diff →
  сигнал владельцу.
- Файлы кандидата на okna: /tmp/probe-v13/ (prob-инстанс снят, порт 8092 свободен).

## Трек 2. Приборы (окно приборов) — ЖДЁТ ВОЗВРАТ

- Коммит 0adff25 на origin/main. Замки 8/8, главный дефект (обрезка вопроса) закрыт.
- Мой живой прогон (rid probe-20260822T064643Z-914775, :8091) подтвердил 3 дефекта:
  1. PROBE-строка штампует code_md5 из локального дерева (1ab7d928), отвечал бой c560aebe;
     источник — только ask_journal.code_md5 или md5 на целевом хосте;
  2. самопроверка журнала на удалённой стороне сломана (probe_protocol не копируется,
     кавычки — вопрос уезжает в шелл);
  3. «PROBE INVALID», но exit 0 — невалидный замер обязан падать с exit != 0.
- После их коммита — мои живые прогоны: okna-ask-dump (вопрос целиком + journal verify),
  verify_gold_no_data --host okna и klient-1, env-map.sh → снимок в RUNBOOK_DEPLOY.

## Трек 3. Гейты невозврата — НЕ ЗАПУЩЕН (новое окно)

Промт готов (в переписке 22.08): ① снайпер знаний на путь ответа (детерминированный,
новые человеческие слова в решающем коде = стоп-коммит), ② эталон no_data несёт
доказательство отсутствия, ③ precheck такта (гранты/версия init на каждой базе).
Разработка в work/hooks/, установка владельцем: bash work/hooks/install-gates.sh.
Приёмка оркестратором — только пробоями + test-hooks.sh зелёный.

## Трек 4. klient-1 — ЖДЁТ РЕШЕНИЕ ВЛАДЕЛЬЦА («рестартуй» или нет)

- Сделано: corpus_init.sql + corpus_build.sql из HEAD в /opt (md5 3a9a6ea6 / e0d61ec9).
- Стоп: SereneDB в клине — юнит active, «ready» в журнале 06:57 местного, порт 7890 не
  принимает соединения (2 пробы connect timeout; хост пуст, load 0.09). init с грантами
  не выполнен (умер по таймауту на соединении).
- Пайплайн: таймер dead, такт убит SIGKILL 06:42 местного — это окно владельца
  (ускорение эмбеддинга, embed_pool.sh с 06:31 — тоже заблокирован клином).
- Дальше по слову владельца: systemctl restart serenedb → повтор init (минута) →
  проверка гранта → **такт только после завершения окна владельца** → balance_registers
  наполнится (corpus_build 1-бис) → перезамер «остатки на складах» :8091 klient-1
  (базлайн 22.08: no_data при живом accumulationregister_товарынаскладах).
- Базлайн-факт: search_meta.balance_registers на klient-1 пуст; гранта serene_ro не было.

## Решения, ждущие владельца

1. «рестартуй» — рестарт serenedb на klient-1 (или это делает его окно).
2. Вставить возврат-13 в окно универсального пути.
3. Вставить возврат в окно приборов.
4. Когда будет готов — запустить окно гейтов (промт в переписке), в конце install-gates.sh.

## Отложено осознанно (не терять)

- Субконто счетов в контур (одобрено): сначала большое окно проверяет $metadata
  (ExtDimension*), потом отдельный промт окну пакета. Запуск после коммита большого окна.
- Scorer 25 на okna после выката возврата-13 (было 14/25 на возврате-12).
- Хвосты: 1c-serene-sync failed на деве; service-витрина (владелец одобрил удаление при
  доказанности «100% не нужно» — отчёт окна не приходил); приёмка канала (манифест такта
  без теней/service); /tmp на okna почистить (старые ask-*.py); ротация DeepSeek-ключа;
  ставка/ETA досчёта klient-1 после окна владельца.

## Хозяйство дня

- work/hooks/{check-golden,lib-hooks,test-hooks}.sh синхронизированы с развёрнутыми
  (probe-выкат из 874555d жил незакоммиченным); канон == deployed == .new по cmp.

## Ловушки дня (не наступать снова)

- pkill -f с именем файла в той же команде убивает собственный шелл — pkill отдельным
  вызовом с [s]-трюком.
- /tmp на okna содержит старые ask-*.py — пробу запускать из свежего каталога.
- Юнит боя на okna — 1c-serene-ask@postgres (имя = dbname), @okna не существует.
- TEMPORARY SECRET переживает смену env (§3.93) — ai_embed ходит по секрету, не по env.
- ssh … bash -s "$Q" режет вопрос по словам (§3.94) — только heredoc/json.
