# И2 — приёмка client-gold okna (28.08.2026)

**Задача:** К4 — разбор классов провалов независимого прогона И2.  
**Код не менялся** (параллельная сессия правит `serene_ask.py`).  
**Контур:** бой `:8091`, полный набор флагов Ф6 + K6b, код `24557f1`.  
**Набор:** 48 вопросов из `ubuntu/serenedb/client-gold-okna.tsv` (12 эталонов 1С из И0).  
**Артефакты:** `work/acceptance/runs/i2-validate/run2|run3|run4/` (summary.tsv + answers.jsonl).

**Корпус заморожен** 24.08 05:40 (`search_quality.build_ts` ≈ 94,3 ч на момент прогона;
`mart_changed_ts` ≈ 19,9 ч — витрина новее, такт Д5 не бежит).

---

## 1. Сводка прогонов

| Метрика | run2 / run3 / run4 |
|---|---|
| Вопросов | 48 |
| `kind=answer` | 10 |
| `kind=figures` | 25 |
| `kind=no_data` | 8 |
| `kind=clarify` | 4 |
| `kind=error` | 1 |
| status `match` (12 эталонов) | 4 |
| status `mismatch` | 8 |
| status `pending` (без эталона / ask_journal / search_tables) | 35 |

Средняя лatency run2: **37,4 с** (хвост — error 189 с на «Варианты Расчета Налогов»).

---

## 2. И3 — детерминированность (факт)

Три прогона run2–run4 на одном контуре:

| Проверка | Результат |
|---|---|
| `kind` по 48 вопросам | **48/48 стабильны**, расхождений **0** |
| Числа ответа (`nums` без минут свежести) | идентичны во всех трёх прогонах |
| Текст | отличается только минутами в приписке «⚠ Данные могли устареть… **N** мин назад» (N растёт между прогонами: 5656 → 5685 → 5702) |
| `summary.tsv` status/kind/etalon/got | идентичны; в колонке `got` плавает только минута свежести |

**Вывод И3:** поведение контура **детерминировано** по `kind` и содержательным числам;
единственный «разброс» — монотонный счётчик `data_age_sec` в приписке свежести.

---

## 3. Таблица классов провалов

Ориентир: каждый **не-answer** и каждый **mismatch** из run2.  
Класс — первичный (некоторые вопросы затрагивают два слоя, см. примечания).

| Класс | N | Вопросы | Причина | Чем закрывается | Приоритет |
|---|---:|---|---|---|---|
| **Д5 — лаг корпуса / свежесть** | **4** | сколько сделали за эту неделю; сегодня сколько уже наотгружали; позавчера сколько было продаж; вчера сколько продали | Корпус обрывается на снимке до 25.08; витрина (эталон И0) уже содержит 26–28.08. Бот честно видит «пусто» или устаревшее «сегодня» (52 ≠ 0). Приписка ~5656 мин = `build_ts`, не баг класса | **Такт корпуса** (Д5 Windows `--smoke` → пересборка → `build_ts`) + для «сегодня» проверить путь счёта (I0: корпусное today=0, бот отдал 52 — не только лаг) | P0 |
| **§7.10 — пустое относительное окно** | **2** | позавчера сколько было продаж; вчера сколько продали | При пустом дне модель иногда не ставит `period`+`assumed` → проба `period_window_empty` не срабатывает → fallback `figures`/нулевой `period_empty` при `date_max=25.08`. Трасса `:8096` для «позавчера» показывает **рабочий** `period_empty` — класс нестабилен между формулировками | **Код:** резолв `yesterday`/`day_before` из `period_relative_forms` (Д4 в meta); gate fallback → `period_empty`, не паспорт (`SMALL_CLASSES_DIAG §2`) | P0 |
| **prev_week — «данных нет» при живой неделе** | **1** | а на прошлой неделе сколько наторговали | `kind=answer`, текст: «данных нет», окно **17–21.08**; в корпусе неделя **17–23.08** = **1 262 950,40** (И0 match витрина↔корпус). Разрыв «бот не видит данные, которые есть» — неверное/урезанное окно `prev_week` или `period_empty` на частичном интервале; на `:8096` оркестратор воспроизвёл тот же дефект | **Код:** канон `prev_week` 17–23 (`period_relative_forms`, `apply_period_leader`); не смешивать с wtd | P0 |
| **«всего без сущности» → clarify** | **1** | сколько было в прошлом месяце всего | `clarify` «Какие именно записи…» — **не регрессия**: И0 мерил только SQL витрина↔корпус (2767450,98 = match); ответ бота на этом наборе раньше **не** мерялся. По контракту §2 исход **C/clarify** допустим без подписанной сущности | **Код** (если нужен answer): дефолт «продажи/итого» без угадывания; иначе принять clarify как целевой исход | P2 |
| **K6a — гигант / неверная сущность** | **4** | Сколько валют?; Сколько организаций?; сколько за этот месяц уже вышло; Сколько движений в регистре «Стоимостной Базис ОС…»? | `figures` с **План Счетов Основной2014** (189043 / 3624 / 77979) или **контрагенты 353** вместо «организаций» / «валют» / регистра. K6 v2 не удержал лидера | **Код** K6/rank + entity_form (класс K6a) | P0 |
| **K6 — clarify на мерах вместо count** | **1** | сколько у нас вообще клиентов сейчас | `clarify` с вариантами «дней отсрочки / кредитный лимит / **353**» вместо count=362. Число 353 = **корпусное** прочтение (И0 Δ9); эталон 362 = витрина | **Такт** (353→362) + **код** K6-двойка (выбор count vs реквизиты) | P0 |
| **K6 — неверная мера (figures)** | **1** | сколько из клиентов реально покупают | `figures`: 353 «уникальных поставщиков и покупателей» — не «кто покупал», а перепись контрагентов | **Код** K6 + sales/rank intent | P1 |
| **Склад / номенклатура — no_data** | **4** | сколько крепежа…; профиль…; ручки…; петли… | Честный `no_data` «проверенный ответ невозможен» — в корпусе нет слоя остатков по обиходным словам или сущность не достигнута. Не freshness | **Код:** поверхность номенклатуры/остатков + словарь; иначе оставить честный `no_data` если данных нет в 1С | P1 |
| **К8 — сравнительные / без базы** | **3** | мы просели или выросли; сегодня лучше вчeraшнего или нет; сравни с прошлым разом | `no_data` или `clarify` (слоты итого/кол-во) — составной вопрос без декомпозиции. «В этом месяце больше…» уже **answer** (compare в коде) — вне класса | **К8** OpenClaw-скилл над ask (`PLAN_TO_TARGET` §К8) | P1 |
| **rank / measure — clarify** | **1** | какая фурнитура сейчас лучше всего идёт | `clarify` итого vs кол-во — развилка B/C, не провал счёта | **Код** rank + подписи веток; частично К8 | P2 |
| **no_data — прочее (journal)** | **1** | покажи по механизмам | `no_data` — нет оси/сущности «механизмы» | К8 или новый класс разрезов | P2 |
| **error — таймаут контура** | **1** | Сколько позиций в справочнике «Варианты Расчета Налогов»? | `error`: `HTTP Error 503: Service Unavailable`, latency **189 с**, пустой text/nums | **Инфра/perf** (нагрузка 48× подряд); повтор отдельно | P2 |
| **Каталог — лаг без этalon-mismatch в summary** | **2** | сколько позиций у нас в прайсе всего; Сколько движений в регистре реализации ТМЦ? | `figures` 2384 vs 2389, 77901 vs 78176 — summary **match** (допуск), И0 **mismatch**/Δ — лаг такта | **Такт корпуса** | P1 |

**Не класс провала (pending, эталона нет):** 13 `figures` по search_tables (регистры/справочники с корректной подписью и числом), 7 `answer` journal (воскресенье, compare месяца, топ-3, …) — ожидают эталонов И0/журнала, не входят в очередь K4.

---

## 4. Разбор «прошлая неделя» (разрыв bot ↔ корпус)

| Слой | Факт |
|---|---|
| И0 SQL | корпус **17–23.08** = **1 262 950,40** = витрина |
| И2 run2 | `kind=answer`, «данных по продажам за прошлую неделю **нет**», окно **17–21.08**, `outside_period=77173` |
| `:8096` (оркестратор 28.08) | тот же паттерн — не артефакт run2 |
| «прошлый месяц всего» на `:8096` | `clarify` — класс «всего без сущности», **отдельный** от prev_week |

**Диагноз:** не freshness (сумма в корпусе есть), а **неверное окно prev_week** или ложный `period_empty` на урезанном интервале. CHANGELOG 28.08 фиксировал похожий ложный регресс при отсутствии словаря (`period_leader=wtd`); в бою словарь в meta частичный (1 форма), поведение может расходиться с эталонным SQL.

---

## 5. Двадцать пять `figures` — контракт или дефект?

По [`PLAN_ANSWER_CONTRACT.md`](PLAN_ANSWER_CONTRACT.md) §2 и [`PIPELINE.md`](PIPELINE.md):  
`kind=figures` — **штатный исход**, когда гейт отверг прозу, но **числа из базы верны** (те же атомы, исход A или B без текста).

| Подкласс | N | Оценка |
|---|---:|---|
| Регистры/справочники search_tables — верная подпись + count | **13** | **По контракту** (pending эталона) |
| Справочник банков 293, рег. реализации 77901 (Δ275) | **2** | **По контракту**; Δ — лаг корпуса, summary match |
| Номенклатура прайс 2384 (Δ5) | **1** | **По контракту** по форме; Δ — лаг |
| K6a — План счетов / не та сущность | **4** | **Дефект** (не A/C) |
| «Клиенты покупают» — не та мера | **1** | **Дефект** |
| «Сегодня наотгрузили» 52 при etalon 0 | **1** | **Дефект** пути/окна (I0: корпус today=0; бот не совпал с прямым SQL) |
| Прочие pending без эталона | **3** | figures как форма OK |

**Итого:** из 25 `figures` **~16 по контракту**, **~6 дефект** (K6a + мера + today), **~3** pending без эталона.

---

## 6. Сколько провалов уйдёт

Оценка по **8 mismatch** эталона + **29** содержательных не-answer (без 13 pending figures-only search_tables):

| Закрытие | Вопросов (оценка) | Комментарий |
|---|---:|---|
| **Такт корпуса (Д5)** | **~4–6** | эта неделя; частично клиенты/прайс/рег.строки; вчера/позавчera **только если** в корпус доедут дни **и** сработает §7.10 |
| **Код (классы K)** | **~12–14** | §7.10 (2), prev_week (1), K6a (4), K6 clarify/count (2), склад (4), «месяц вышло» (1), today path (1) |
| **К8 (скилл)** | **~3–4** | сравнительные + «сравни с прошлым» + возможно «механизмы» |
| **Не цель / infra** | **~2** | «прошлый месяц» clarify (by design); error 503 (повтор) |

После **такта + кода P0** реалистично снять **6–8 из 8** etalon-mismatch; «прошлый месяц» может остаться mismatch эталона при целевом clarify.

---

## 7. Вывод

1. **И2 пройден как измерение:** контур стабилен (И3 ✅), картина провалов **классовая**, не шум.
2. **Главные очереди K4:** (a) prev_week + §7.10 — период; (b) K6a/K6-двойка — сущность; (c) Д5 — такт; (d) К8 — сравнения.
3. **25 figures — не монолит:** большинство search_tables **контрактны**; дефектны ~6 с неверной сущностью/мерой.
4. **«прошлый месяц всего»** — уточнение класса, не регрессия относительно И0 SQL.
5. Следующий шаг по [`PLAN_TO_TARGET.md`](PLAN_TO_TARGET.md): закрытие классов P0 одним агентом на `serene_ask.py` / `entity_rank_v2.py`, затем повтор И2→И3.

**Связанные документы:** [`I0_ETALON_TOOL.md`](I0_ETALON_TOOL.md), [`SMALL_CLASSES_DIAG_2026-08-28.md`](SMALL_CLASSES_DIAG_2026-08-28.md), [`PLAN_ANSWER_CONTRACT.md`](PLAN_ANSWER_CONTRACT.md).
