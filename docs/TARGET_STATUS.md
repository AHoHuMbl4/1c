# Статус по контракту: что на какой стадии

Спутник [`TARGET.md`](../TARGET.md). Тот говорит, **чем продукт обязан быть**; этот —
**где мы по каждому пункту сейчас**. Менять `TARGET.md` нельзя; этот файл живой.

**Обновлено: 2026-08-27.** Правило стадий — как в п. 11 контракта: «готово»
ставится только по замеру на живых данных, не по рассуждению. 🔴 **Замера нет — 🟢 не
бывает**, даже если механизм на вид работает: обоснование обязано содержать число,
воспроизводимую команду или предписанную контрактом проверку. 🔴 **Сделано ≠ доказано:**
проект патча, разбор и зелёный замок на моках — не закрытие пункта.

*Срез 28.08 день (К9-ф1, И2/И3/К4/И4):* **И2+И3 закрыты** на независимом
client-gold 48: три прогона `:8091` — **48/48 kind стабильны, 0 расхождений**
(детерминированность); сводка answer 10 / figures 25 / no_data 8 / clarify 4 /
error 1; эталоны (12): match 4 / mismatch 8; классы провалов —
`docs/I2_ACCEPTANCE_CLIENT_GOLD_2026-08-28.md` (~16/25 figures по контракту,
~6 дефект; такт закроет 4–6, код 12–14, К8 3–4). **Ambiguous-26 на бою
7/18** (было 5/18; провалы: no_data при живых именах, угадывание меры,
«2022» answer). Лаг корпуса живьём: build_ts **94,3 ч** / mart_changed_ts
**19,9 ч** (Д5). **К9 фаза 1 в git `2e42e61`**: action_class/action_axis из
разбора (event/object/none — модель, без списков слов), event-ярус первый
ключ ранга, форма DISTINCT, event_axis_lock; замки 13/0+51/0+30/0+92+47,
bench v2_lead **19/23** регресс 0; пробы: «реально покупают»→event,
«всего клиентов»→object; сквозной исход на событийных формулировках ещё
НЕ достигнут (фаза 2 — PLAN_TO_TARGET К9). Подгонка первого захода K6-двойки
откачена владельцем (HOW_NOT_TO §3.96). И4 реестр наборов закрыт
(`bb41bef`), К8-декомпозатор и К10-нарезка модулей в плане.

*Срез 28.08 ночь (В2 закрыт):* флаги Ф6+ранговая тройка **в бою `:8091`** —
каждый шаг (HEALTH_NATIVE_FRESHNESS, RESOLVER_IVF, SOLR_SYNONYMS+DICT,
ATOM_TERMINAL, ENTITY_FORM, SALES_RANK_CANON) с AB_PROBE **8/8 0err**
(срезы `work/acceptance/runs/f6-rollout/…`). Контур-24 (today-gold,
24 строки) лестницей: RRF-only **15/24** → +F/I/S **16/24** → полный
набор **20/24**, средняя **24,58 с** (код `24557f1` = K6b: ранг-лидер
после исхода B; md5 ask `e57873e1`). Кандидат :8096 тот же код —
**19/24**. Регрессия рангов патча 2 изолирована и починена
(`docs/K6_ENTITY_RANK.md` §7.9–7.11: дневной замер §7.6 снят на
докоммитной версии). Остаток FAIL-4 — записанные классы: «молодец»-сленг
(ранг без глагола покупки), 2×K6-обиходных (в базлайне тоже FAIL),
«как у нас дела» (§10). Построчных регрессий против базлайна 23/25 нет.
С1 закрыт замером: боковая `alias_okna_c1` **258/257**, новых **0**,
расхождений **0**; `ts_lexize('search_dict_syn','клиент')` =
**{клиент,контрагент,поставщик}** — K6-двойка НЕ в словарном слое.
С4 закрыт: `period_relative_forms` в `search_meta` живьём (1 форма);
Д4: файлы шага 1-period доставлены (dc72fd8).

*Срез 27.08 (только обоснования с числом; стадии без повышения без оснований):*
Словарь/индекс на okna: С2 `3350b12` — `search_entity_alias` **254→257**, avg aliases
**55→84** (боковая `alias_okna_c1` **258**); С3 `343d856` — Snowball `ru_RU`, формы
«клиент*» → один класс, alias_rank gold-23 лидер **0→3**; С5 `fbbc84c` — stem в
пайплайне, `alias_idx` **257**, alias_rank **3/0/20**, замок `test_morph_dict_pipeline`
**19/0**; Д3 `5fb8bf3` — `alias_idx` был **пуст** при наполненной таблице, после
`VACUUM (REFRESH_TABLE)` **257/257**, замок `test_inverted_index_guard` **13/0**.
K6 живьём **не** сошлись: С2/С4 `b80d935` — оба контрольных (эталоны SQL **141** /
**1891**) обрыв на выборе сущности, не на предикате; K6 live **0/2**. Мера
`entity_answer_fit` (`bb6779f`): gold-23 лидер **2→1**, «покупают» place **5→1**; в
`serene_ask` **не** вшита. Гипотеза `ASK_ENTITY_FORM=1` на `:8092` **не подтверждена**
(Q1 тот же мусорный clarify; Q2 → догадка, п. 12) — `docs/K6_ENTITY_RANK.md`.
Сравнение периодов: К2 `4ac3c38` — живой `:8091` **0/8**; золото SQL
**1 049 991,33** живо. Э4 `bf8ac41` — флаг частичности в ответе; замок
`test_partial_flag_propagation` **13/5→20/0**; AB_PROBE **8/8**. К5 в дереве
(замки guess **13/0**, meta **12/0**, nodata **11/0**, axis **11/0**, sales_rank
**51/0**; стейджинг AB_PROBE **8/8**, **20,70 с**, md5 ask `4ec52861…`); ambiguous
**5/18** после К5 **не** перемерено (`docs/K5_APPLIED.md`). Д4 `247c2f3`/`0e050b3` —
такт падал **3083×** на `:"solr_syn_dict"` с **24.08 19:36**; после выката init OK
(`solr_syn_dict_ok=1`), дальше блокер: **нет `$metadata`** в packet-meta
(сущностей **0**); простой свежести ~**3** суток. Полный scorer **23/25**
(`c706625`, 26.08) **после** работ 27.08 **не** переснимался. База приёмки —
**только okna** (ut_test выведен).

*Срез 25–26.08 (исторический; 23/25 — последний полный scorer до словаря 27.08):
полный скорер okna `:8092` **23/25**, сбоев **0**, средняя **29,09 с**, `code_md5`
**177208d9** (коммит `c706625`, `/tmp/scorer-k3.log`); `check_deploy_gate.py` →
PASS против базлайна 21/25. AB_PROBE **8/8**, **28,78 с**. Тогда: K4/K5a OK;
rank top-N OK; K5b/К2 на том срезе OK — **чем опровергнуто [замер 27.08
`4ac3c38`]:** compare-набор **0/8** на боевом `:8091`. Остаток K6 открыт и после
С2/С3. П. 4/16: PostgreSQL выведен, на Windows базы данных нет.*

Стадии: 🟢 **готово** (проверено замером) · 🟡 **частично** (работает, но не по полному
критерию) · 🔴 **не начато** · ⚪ **по контракту — последним**.

---

*Правка 25.08 (K5 atom-terminal): `ASK_ATOM_TERMINAL=1` на локальном probe —
K5a **OK** (307); K5b atom наружу, refuse снят; выкат `:8092` ждёт оркестратора.
**Устарело как хвост K5b:** на 23/25 K5b закрыт (`c706625`). Стадии не повышали.*

*Правка 25.08 (rank-путь): на стейджинге okna `:8092` с `ASK_SALES_RANK_CANON=1`
скорер **19/24** (было **15/24**); класс K1+K2+K3 **4/4**; AB_PROBE **8/8**.
Стадии пп. 2/3/10/21 **не** повышали — 19/24 ≠ критерий «отвечает верно».
**Устарело как текущий срез:** полный набор **25** → **21/25** (25.08) →
**23/25** (26.08, `c706625`).*

*Правка 25–26.08 (полный скорер 21/25): обоснования пп. 2, 3, 4, 5, 6, 7, 10, 12,
13, 14, 16, 21 по **21/25** и `DEPLOY_GATE`; **стадии не сдвигались**.*

*Правка 26.08 вечер (И1, `PLAN_TO_TARGET`): текущий срез **23/25** (`c706625`);
K5b/К2 закрыт; остаток **2×K6** до С2; И5 — скрипт+проба 7/7 в git (`0cd2e52`),
установка `install-gates.sh` — внешний шаг владельца; И0 — `etalon_1c.py` /
`test_etalon_1c.py` есть в дереве, в git не внесены (`??`). **Стадии не
сдвигались** — 23/25 ≠ критерий «отвечает верно» / «без ошибок» / «правильный».*

## Одной таблицей

| # | Пункт | Стадия | Чем закрыт / что осталось |
|---|---|---|---|
| 1 | Полноценный продакшен | 🟡 | сумма остального; эксплуатация (журналы, поведение под нагрузкой) не замерена |
| 2 | Бот отвечает по данным 1С | 🟡 | отвечает через бота, числа считает база, гейт держит. **[замер 28.08 ночь, В2]** полный набор флагов в бою `:8091`, контур-24 (today-gold) **20/24**, средняя **24,58 с**, код `24557f1` (= HEAD); лестница выката 15→16→20 в шапке-срезе. **[замер 26.08]** полный scorer `:8092` **23/25**, сбоев **0**, средняя **29,09 с**, `code_md5` **177208d9** (`c706625`) — после словаря/морфологии 27.08 **полный набор не переснимался**. **[замер 27.08]** K6 live **0/2** (С2 `3350b12`, С4 `b80d935`; эталоны SQL **141**/**1891**); compare-набор `:8091` **0/8** (`4ac3c38`); ambiguous **5/18** (`96b51ab`) после К5 **не** перемерено; AB_PROBE стейджинг после К5 **8/8** (`docs/K5_APPLIED.md`). Приёмка — **только okna**. Стадию не повышаем: ни 23/25, ни 8/8 лёгкого probe ≠ критерий «отвечает верно по данным» |
| 3 | Ответы без ошибок, детерминированно | 🔴 | **[замер 27.08 И3]** детерминированность переснята: **три прогона живого набора подряд — 0 расхождений** на всех 30 вопросах (`docs/I2_ACCEPTANCE_OKNA_2026-08-27.md`); прежний «±1 из десяти» (31.07) не воспроизводится. Ошибки остались, но стабильны. Главная причина ошибок — **выбор сущности** на rank/leader (5 из 24 провалов И2); мера v2 К6 даёт лидера **18/23** без регрессий (`43d2482` §7.5, патч не применён — файл занят К2). Счёт при верной сущности верен. Полный срез И2: **6/30** + 2 broken (TypeError NoneType после гейта, детерминированные). Стадию не повышаем: ошибки есть |
| 4 | 1С на Windows, база данных — в SereneDB | 🟡 | Контракт обновлён владельцем 25.08 (`TARGET.md` п. 4, п. 16; решение 22.08 подтверждено): **на Windows базы данных нет** — только 1С и агент выгрузки (пакеты на packet-server); **витрина и индекс — в SereneDB на Ubuntu**. PostgreSQL выведен из контура полностью. Сам Windows-хост — трек владельца (`windows/odata-setup`), с этой стороны не проверяем |
| 5 | Эмбеддинг Qwen | 🟡 | 🔴 **02.08 сменился поставщик, и это меняет весь пункт.** Прежний (`text-embedding-v4` в облаке Alibaba) [замер 02.08] отвечал `Arrearage` на **все 11 ключей** — бот отдавал 503 на каждый вопрос. По решению владельца эмбеддер переехал в наш контур. Модель по-прежнему Qwen, то есть буква пункта соблюдена. Итоговый выбор сделан **замером**: `Qwen3-Embedding-8B` BF16 dim 1024 (GGUF Q8_0 неотличим по качеству — 44 из 44 тот же выбор, — но втрое медленнее). Векторы старой модели несравнимы с новой, поэтому 02.08 шла перевекторизация боевой базы. ✅ **Она завершилась** `[замер 03.08]`: `cov_noemb_pending` = 0 (было 92 080 деловых строк в очереди), отметка `Qwen3-Embedding-8B` стояла у всех трёх — `search_tables`, `search_corpus`, `resolver_index`. **[замер 22–23.08]** на боевой okna (SereneDB 26.08.1) пересчёт на **`Qwen3-Embedding-4B`** завершён: не-service корпус **1 206 662**, резолвер **128 674**, карточки **254** (service без вектора по политике); scorer-паритет после переезда **8/25=8/25** — устарело как срез качества. **[замер 25.08]** полный scorer **21/25** — промежуточный. **[замер 26.08]** полный scorer **23/25** (`c706625`); GPU в норме (слово владельца 26.08: embed+27B+reranker живы). **[замер 24.08]** klient-1: пересчёт 4B — `resolver_index` **0** без вектора; `search_corpus` в работе. 🟡 оставлено: польза смыслового пути для верных ответов своим числом выше паритета не доказана; klient-1 на 4B ещё не закрыт воротами Ф4 (`PLAN_UPGRADE_NATIVE`) |
| 6 | SereneDB, поиск через неё | 🟡 | словесный отбор — `search_idx`; IVF на okna стоят (**[замер 23.08]** recall@10 ≈**1,0**). **[замер 27.08, Д3 `5fb8bf3`]** дыра: `alias_idx` был **пуст** при 254/257 строках таблицы (ранг по алиасам молча не работал) → `VACUUM (REFRESH_TABLE)` + замок **13/0**; сейчас `search_entity_alias`/`alias_idx` **257/257**. **[С2 `3350b12`]** словарь **254→257**, avg **55→84**. **[С3 `343d856` / С5 `fbbc84c`]** stem pipeline: alias_rank лидер **0→3** (из 23). Ф6.1 в бою; Ф6.2–6.4 бой выкл. K6 live после словаря всё ещё **0/2** — лексика/морфология **не** закрыли выбор сущности. 🟡: ворота Ф6 не закрыты; K6 открыт; латентность Ф6.1 не перемерена |
| 7 | Поиск только нативными средствами | 🟡 | штатные `ts_*`/`bm25`/`<=>`/`solr_synonyms`/`ai_embed` — как прежде. **[замер 27.08]** Snowball `ru_RU` через `text`/`pipeline` жив на 26.08.1 (`343d856`): `ts_lexize` клиент/клиента/клиентов → `{клиент,контрагент,поставщик}`; С5 зашил stem в `corpus_init` + `solr_synonyms_build` (`fbbc84c`, замок **19/0`). **[С2]** после подмены `ts_lexize('клиент')` = `{клиент}` без связи «клиент→каталог» на singular — plural «клиенты» даёт каталог (`docs/C2_ALIAS_SWAP.md`). `ASK_EMBED_NATIVE` умолч. 0; `ai_embed` жив в **26.08.1**. 🟡: HTTP по умолчанию на бое; K6 не закрыт нативным словарём |
| 8 | Всё вокруг SereneDB | 🟢 | корпус, индекс, резолвер, величины, ссылки — внутри движка. Тем же разбором `serenedb-native`, что и п. 7: контракт для строки «7, 8» требует именно ответа агента, а не числа (`TARGET.md`) |
| 9 | Без хардкода, на любой базе | 🟡 | **проверено на чужой конфигурации** — УТ 11.4.9 (2 795 сущностей): синк 1 588, сборка 1 501/1 501, ответы сверены с SQL — всё **без правок кода**. Первая база после правок цела (8/8 лёгкого golden-набора). ⚠ Повторить этот замер сегодня можно, но не тем способом, каким он записан: штатный юнит `1c-serene-ask` (он настроен как раз на **первую** базу — `dbname=postgres` в `/etc/1c-mcp-reports.env`) сейчас `inactive`, а по `ut_test` отвечает процесс вне systemd на :8099. То есть нужен `systemctl start 1c-serene-ask`, а не другой DSN — но тогда на :8099 не останется никого, и бот замолчит. Остался только **другой язык** — см. [`TEST_SECOND_BASE.md`](TEST_SECOND_BASE.md) |
| 10 | Ошибки в ответах запрещены | 🔴 | как п. 3. **[замер 26.08 `c706625`]** на полном scorer было **2 FAIL** (оба K6); K4/K5a/rank top-N OK. **[замер 27.08]** дополнительно: compare **0/8** (`4ac3c38`) — прежнее «K5b/К2 закрыт на 23/25» **не** переносится на боевой compare-набор; K6 **0/2**; ambiguous после выката К5+К7 — **8/18** (`§9 ACCEPTANCE_AMBIGUOUS.md`, было 5/18). **[замер 27.08 И2]** независимый живой набор: **6/30**, 2 broken (TypeError сущность=None → 503, детерминированные), разбор классов — `docs/I2_ACCEPTANCE_OKNA_2026-08-27.md`. Арифметика и гейт чисел — без известных пропусков. Критерий «любые ошибки запрещены» не закрыт; стадию не повышаем |
| 11 | Ни одного нерешённого пункта | 🔴 | финальная приёмка, когда закрыто остальное |
| 12 | Отказ/уточнение — не ошибка, догадка — ошибка | 🟡 | средний рубеж есть; 🟢 нельзя: на неоднозначных система часто **берёт не ту сущность** (п. 12 догадка). **[замер 27.08 Э3 `96b51ab`]** ambiguous okna **5/18** верных. **[К5]** четыре класса починки в дереве + замки (guess **13/0** … sales_rank **51/0**); стейджинг AB_PROBE **8/8** — ambiguous **после К5 не перемерено** (`docs/K5_APPLIED.md`). **[K6]** гипотеза `ASK_ENTITY_FORM=1` на `:8092` дала догадку на Q2 — **нарушение п. 12**, гипотеза закрыта как ложная (`docs/K6_ENTITY_RANK.md`). K6 live **0/2** после С2/С3. Стадию не повышаем |
| 13 | Полнота данных | 🟡 | перепись / `/health` gap **0/0** (замеры 23–24.08). **[замер+код 27.08 Э4 `bf8ac41`]** флаг частичности проведён в ответ (`ensure_partial_visible`); замок `test_partial_flag_propagation` **13/5→20/0**; живая AB_PROBE **8/8**, **18,52 с**. Это закрытие **проводок** флага, не критерия «всё из 1С доступно». **[Д3]** пустой `alias_idx` был молчаливой потерей поверхности поиска — починен (`5fb8bf3`, **257/257**). **[Д4]** корпус не обновляется ~**3** суток (такт не доходит до конца — нет `$metadata` в пакетах) — свежесть/полнота относительно 1С **не доказаны** сейчас. Стадию не повышаем |
| 14 | Инсталлятор | 🟡 | **[контракт изменён 26.08, синхронизация `a9eb0bc`]:** ручное действие одно — логин/пароль администратора базы 1С; читателя в 1С и пользователя Windows создаёт сам инсталлятор. Проверка: «установка на чистую машину — одно ручное действие». Ранее — снято ручных шагов: раскладка кода (`deploy.sh`), шлюз OData шаблонным юнитом, **выкатка бота** (`deploy_instance.sh`). Осталось: сторона Windows (агент выгрузки; базы данных там нет — п. 4/16) и роли/секреты на Ubuntu |
| 15 | Приёмка из базы клиента | 🔴 | **владелец: приёмка в конце; инфраструктура — сейчас (26.08).** Задача **И0** (`PLAN_TO_TARGET`): восстановить `etalon_1c.py` (сверка эталонов с живой 1С по OData), генератор из `$metadata` + `ask_journal`, двойной эталон. **Статус И0 [26.08]:** инструмент есть в дереве (`ubuntu/serenedb/etalon_1c.py`, `test_etalon_1c.py`) — **в git не внесён** (`git status` → `??`); генератор/прогон на живой базе — не закрыты. Проверка генератора в плане — на ut_test/klient-1; **приёмка продукта с 26.08 — только okna** (ut_test выведен, `52d1933`). Набор пока под наши данные. Сдвиг 31.07: эталоны набора **сверены с живой 1С** по OData (та сверка поймала 1 неверный эталон из 17); скрипт той сверки утерян — восстановление и есть И0 |
| 16 | Периметр (модели внутрь) | 🟡 | **[контракт изменён 26.08, синхронизация `a9eb0bc`]:** наружу уходит **только вызов DeepSeek для ответов OpenClaw** (временно, для тестов); Alibaba из контракта убран — документы с упоминанием действующего egress на Alibaba (`RUNBOOK_DEPLOY.md`, `ARCHITECTURE.md`, `UBUNTU_SETUP.md`, `PLAN_PRIVACY.md`, `PLAN_THREE_STEPS.md`) требуют синхронизации при ближайшей правке. **размещение данных (25.08, `TARGET.md` п. 16):** витрина и индекс — в SereneDB на Ubuntu; на Windows базы данных нет, только 1С и агент выгрузки; PostgreSQL выведен из контура (`CHANGELOG` 26.08 «PostgreSQL выведен», `work/root-tasks/02-purge-pg.sh`). **Сдвиг 02.08: эмбеддер И реранкер переехали в наш контур.** **[решение+замер 17.08]** ask okna на vLLM **`Qwen3.8-27B`**. **[замер 23.08]** канон GPU на vSwitch: `DEEPSEEK_BASE` → `10.3.1.12:8000`. **[поправка владельца 23.08]** DeepSeek облако — **только веб-фронт**; **ask всегда на локальной**. **[26.08]** GPU в норме (embed+27B+reranker). 🟡: за периметр ещё уходит веб-канал DeepSeek (`deepseek-v4-flash` в web-профиле) |
| 17 | Свежесть — как можно быстрее | 🟡 | контракт 26.08: фиксированного срока нет; эмбеддинг — `ai_embed`. **[замер 27.08 Д4 `247c2f3`]** такт okna failed с **24.08 19:36** — **3083×** `:"solr_syn_dict"` (init новый, build.sh старый); последний OK **24.08 05:40:32 UTC**; простой ~**3** суток. Починка выкатана: `solr_syn_dict_ok=1`, замок `test_psql_init_vars` **9/0**. **[замер `0e050b3`]** после починки такт доходит до «сборка корпуса» и встаёт: сущностей **0** в packet-meta (**74** delta, `metadata.included=false`) — **не доказано**, что свежесть восстанавливается. Замер задержки «1С → ответ бота» по-прежнему **не снят**. Стадию не повышаем |
| 18 | Честный отказ при сбоях | 🟡 | механизм есть: модель/база down → 503 с понятным сообщением; 1С down → корпус консистентен; старение видно припиской. Но контракт даёт пп. **12 и 18 одну строку приёмки** («проверяется вопросами, у которых нет одного верного прочтения»), и этой проверки не было — той же непроведённой проверкой понижен п. 12, значит и здесь 🟢 стоять не может |
| 19 | Модель не считает и не ищет | 🟢 | считает база, отбирает индекс. Объём в модель **замерен**: `CHANGELOG` — **6–20 тыс. символов на вопрос**, и это не растёт с базой. Границы держит код: `ASK_PICK_BUDGET_CHARS`=8000, `ASK_ROWS_BUDGET_CHARS`=24000. Числа в текст подставляет код, не модель |
| 20 | Нет своих скриптов над базой | 🟡 | **[замер 27.08 Э1]** last-OK такт okna **24.08 05:37:55–05:40:32**: реконструкция **≈49** `psql`, wiki наружу **254** стр / **≈314 КБ**; journal PID ≥**19**. Решение по заходам — `docs/TICK_PSQL_AUDIT.md`. **[код Э1]** веер `embed_missing` снят (одна сессия; замок **11/0**); [замер] 26.08.1 ai_embed×**256** без SetSize. **[код Э6/Э7]** `$metadata` через `read_text` (`e1056c7`); wiki dump снят (`5633514`). Остаётся: classify/wiki_alias (**б** LLM); solr compile ещё Python; единицы `psql` на SQL-шаги. Стадию не повышаем до нормы «единицы за такт» на живом полном прогоне с `count_psql_per_tick.sh` |
| 21 | Отдать ответ, правильный | 🟡 | порядок ответ → уточнение → отказ держится механизмом. **[замер 26.08]** последний полный scorer **23/25** (`c706625`). **[замер 27.08]** поверх этого: K6 **0/2**, compare **0/8**, ambiguous **5/18** (после К5 не перемерено); мера `answer_fit` улучшает ранг «покупают» place **5→1**, но в ответный путь **не** вшита (`bb6779f`) — **не доказано** закрытие K6. Стадию не повышаем. Набор, на котором чинили, независимой оценкой не является (`PLAN_TO_TARGET` И2) |

**Итог (по таблице выше): 🟢 2 · 🟡 15 · 🔴 4 = 21.**
(🟢 8,19 · 🟡 1,2,4,5,6,7,9,12,13,14,16,17,18,20,21 · 🔴 3,10,11,15)

*Правка 26.08 (арифметика итога): было «🟢 5 · 🟡 12 · 🔴 4» и список
(🟢 4,7,8,13,19 …) — с `ffdfbbc` пп. 4/7/13 уже 🟡, а строка «Итог» не
пересчитана (`docs/audit/target-status-verify.md`). **Чем опровергнуто:**
пересчёт по таблице → 🟢 **2** (8,19) · 🟡 **15** · 🔴 **4**. Держит замок
`ubuntu/serenedb/test_target_status_totals.py`.*

*Правка 24.08 обновила обоснования пп. 5, 6, 7, 12, 13, 17 по Ф6 и §7bis; **стадии не сдвигались**.*

*Правка 25–26.08 (задача И1 ранняя, `PLAN_TO_TARGET`): обоснования по полному
scorer **21/25** и `DEPLOY_GATE`; **стадии не сдвигались**.*

*Правка 27.08 (задача Т1): срез поднят на факты дня (С2–С5, Д3–Д4, Э4, К2,
K5, K6); стадии **не** сдвигались — ни один критерий «отвечает верно» /
«без ошибок» / «правильный» новым полным scorer не закрыт. Счётчики
итога без изменения (см. строку Итог выше).*

*Правка 26.08 вечер (И1 повтор): срез поднят до **23/25** (`c706625`); классы
K4/K5a OK; rank top-N на 23/25 OK; K5b/К2 на том срезе OK; **2×K6** тогда
«до С2» — **устарело как формулировка**: С2/С3 сделаны, K6 live всё ещё
0/2. **Стадии не сдвигались**.*

*Правка 23.08 обновила обоснования пп. 2, 3, 5, 6, 10, 12, 13, 16, 21 по замерам
переезда/4B/IVF/scorer; **стадии не сдвигались** (повышать без числа, закрывающего
критерий пункта, файл запрещает).*

*Правка 02.08 сперва записала сюда «🟢 7 · 🟡 10», держа п. 17 в списке зелёных, хотя в
таблице понизила его до 🟡. Ошибка в арифметике честности — в файле, который правился ради
честности; поймана независимой перепроверкой в тот же день. Тогда же понижен п. 18: у него
с п. 12 общая строка приёмки в контракте, и понижать одну её половину, оставляя другую
зелёной, — тот же двойной стандарт.*

🔴 **Отметки понижены 02.08 по аудиту документации** (`work/doc-audit/DISCREPANCIES.md`).
Это снова не ухудшение системы, а исправление завышенной оценки: правило файла и п. 11
контракта требуют ставить 🟢 **только по замеру**, а пять зелёных стояли по формулировке
либо по замеру, которого нет. Прежние обоснования не стёрты — они разобраны здесь:

| # | Было | Чем опровергнуто |
|---|---|---|
| 5 | 🟢 «корпус и резолвер без вектора 0» | [замер 02.08] в боевой `ut_test` без вектора 340 464 строки из 623 565, из них **92 080 деловых в очереди** (`cov_noemb_pending`). «0» верно только для резолвера и только про очередь первой базы |
| 6 | 🟢 «весь отбор — инвертированным индексом» | [замер 02.08] `duckdb_indexes()` в обеих базах → только `search_idx` и `alias_idx`; индекса по `emb` нет, `EXPLAIN` kNN даёт `SEQ_SCAN`. Формулировка описывала лишь словесный путь и скрывала единственное место, где стоимость вопроса растёт с базой |
| 12 | 🟢 «средний рубеж; отказ при данных — дефект» | ни числа, ни прогона неоднозначных вопросов, которого требует контракт; и прямое противоречие с 🔴 пп. 3/10 в этой же таблице — «взята не та сущность» и есть запрещённая п. 12 догадка |
| 19 | 🟢 «объём замерен: до 160 КБ на вопрос» | числа «160 КБ» не было больше нигде в проектных документах — оно появилось только в этой строке; единственный записанный замер того же пункта даёт **6–20 тыс. символов**. `ARBITER_MAX` в списке «границ объёма» — вообще не объём, а число кандидатов для арбитра (=3). Стадия оставлена 🟢: замер есть, неверна была ссылка на него |
| 20 | 🟢 «сборка — один `psql`» | **[замер 27.08]** last-OK ≈**49** `psql` + wiki **254** наружу; веер embed снят кодом Э1; Э6/Э7 закрыты. Не 🟢: solr ещё Python; полный такт с `count_psql_per_tick` после выката не снят |

⚠ **Отдельно: числа, которыми обоснованы пп. 5 и 9, сняты с состояния, которого на
стенде уже нет.** Служебная запись последнего успешного такта в `ut_test`
(`build_rows`=632 720, `build_noemb`=205 479, `build_ts`=29.07 17:37) расходится с живыми
таблицами (623 565 / 340 464), а перепись `cov_*` пересчитана позже и с ними согласована.
Значит корпус менялся после последнего полного такта, и **слой, по которому бот отвечает
сегодня, ни разу не принят собственными проверками сборки**. Почему менялся — известно и
без домысла: 30.07 корпус пересобирали руками под переделку «строка = объект»
(`CHANGELOG` 30.07, две пересборки подряд дали побитово одно и то же). А переснять
постпроверку нельзя, пока стоит конвейер.

🔴 **Отметки понижены 31.07, и это не ухудшение системы, а исправление завышенной
оценки.** Пункты 2, 3, 10 стояли зелёными и жёлтыми по набору из **восьми лёгких**
вопросов. Полная приёмка из 58 показала другое, а замер разброса — что и прежние
сравнения «стало лучше на один ответ» ничего не значили. Разбор — `HOW_NOT_TO §1.34`,
и это ровно тот урок, что уже записан в §1.17-§1.18: «ноль неверных, устойчиво» нельзя
объявлять по лёгкому набору.

Изменение против 29.07: п. 2 понижен с 🟢 до 🟡, п. 3 и 10 — с 🟡 до 🔴 по полной
приёмке; п. 21 понижен с 🟢 до 🟡 (ответ отдаётся, но у половины вопросов это уточнение
или неверная сущность). П. 14 продвинулся: выкатка бота стала кодом.

---

## Что мешает закрыть жёлтые и красные

🔴 **Что мешает приёмке прямо сейчас (срез 27.08, только замеры):**

1. **Такт свежести не доходит до конца.** Д4: после починки `solr_syn_dict`
   (`247c2f3`, `solr_syn_dict_ok=1`) такт встаёт на пустом `$metadata` в
   packet-meta (сущностей **0**, **74** delta без `metadata.included` —
   `0e050b3`). Данные относительно 1С старше ~**3** суток (последний OK такт
   **24.08 05:40:32 UTC**). Отчёты: `docs/D4_PIPELINE_BROKEN.md`.
2. **Контрольные вопросы K6 не сходятся.** Live **0/2** после С2/С3/С5
   (`3350b12`, `b80d935`); эталоны SQL **141** / **1891** живы; обрыв на
   сущности/форме F. Мера `answer_fit` (`bb6779f`) — прибор place **5→1**, в
   ask **не** вшита. `ASK_ENTITY_FORM=1` **не** чинит (Q2 → догадка).
3. **Сравнение периодов 0/8** на боевом `:8091` (`4ac3c38`); золото
   **1 049 991,33** SQL подтверждено; diff считается и стирается rank при
   `ASK_ENTITY_FORM=0`. Прежнее «K5b/К2 закрыт на 23/25» к этому набору
   **не** относится.
4. **Ambiguous 5/18** (`96b51ab`) — после К5 (стейджинг AB_PROBE **8/8**)
   **не** перемерено; закрытие п. 12 на неоднозначных **не доказано**.

Полный scorer **23/25** (`c706625`, 26.08) после словаря 27.08 **не**
переснимался — текущий «сколько верных из 25» **не доказан**.

Статусы классов (факт / не план):
- **K4** compare «эта неделя…» — OK на scorer 21/25+ (`DEPLOY_GATE`);
- **K5a** — OK (307) с 21/25;
- **rank top-N** — OK на 23/25 (`scorer-k3.log`);
- **K5b/К2 на полном scorer 23/25** — был OK (`c706625`); **compare-набор
  8q на бое 27.08 — 0/8** (`4ac3c38`) — открыт;
- **K6×2** — открыты после С2/С3 (словарь/морфология недостаточны);
- **К5 (ambiguous)** — код в дереве + AB_PROBE **8/8**; **5/18** не
  перемерено.

**Инфраструктура измерения (не стадии контракта):**
- **И4** — реестр [`GOLD_SETS.md`](GOLD_SETS.md) закрыт **[28.08]`**: все tsv в
  `ubuntu/serenedb/` с ролями; контур-24 (`/tmp/.../ab-gold-24.tsv`) описан как
  контроль окна, не приёмка; замок `test_gold_sets_split` **30/0**; зеркало UT
  `test_acceptance_ut_tsv` **15/0**.
- **И5** — гейт `check-gold-split.sh` + проба **7/7** (`0cd2e52`); установка
  владельцем через `install-gates.sh` — **внешний шаг**.
- **И0** — `etalon_1c.py` + `test_etalon_1c.py` в дереве; статус внесения в
  git на 27.08 **не перепроверялся здесь** — см. п. 15.

Внешнее, чего мы сделать не можем:

- **п. 9** — вторая конфигурация ПРОЙДЕНА (УТ 11.4.9, 28.07); остался
  **другой язык**.
- **п. 15** — приёмка из базы клиента в конце; инфраструктура И0 — сейчас
  (слово владельца 26.08).
- **п. 16** — модели внутрь — **решение владельца**.
- **п. 3/10** упираются в **п. 15** для доказательства «без ошибок» на
  большом наборе из самой базы.
- **п. 14, 1, 11** — инсталлятор и финальная приёмка — последними.

*Было ошибочно: «из того, что двигается без внешнего, крупного не осталось».
**Чем опровергнуто:** полная приёмка 30-31.07 — верных меньше половины; и
срез 27.08 выше — четыре живых блокера приёмки без внешнего.*

---

## Что сделано 29.07

**Ведущий документ дня — [`PLAN_AUTONOMY.md`](PLAN_AUTONOMY.md)** (цель: «поставил и
работает», ни одной ручной правки). Закрыта Фаза 3, начата Фаза 2.

- **разметка сущностей моделью** — что бизнес, что служебное; [замер] УТ 1 501/1 501,
  ноль неразобранных. Задаёт **очерёдность** векторизации, ничего не выбрасывает (п. 13);
- **порядок векторизации**: метки → резолвер → корпус, и внутри корпуса не-служебное
  раньше служебного, а служебному вектор **не считается вовсе** (решение владельца 29.07).
  [замер] это 41% корпуса; отсутствие вектора названо в переписи причиной;
- **три дефекта одного класса — «общее имя на все базы»**, каждый ломал чужую базу молча:
  замок брал имя разбором DSN; рабочие таблицы досчёта назывались одинаково; **секреты
  движка общие на инстанс**. Все три вскрылись только при работе двух баз рядом;
- **ручных шагов минус два**: раскладка кода (`deploy.sh`), шлюз OData шаблонным юнитом;
- **`nightly-eval` погашен** — чужой евал слал владельцу алерт с боевого токена;
- **регрессия первой базы** сведена в [`REGRESSION_BASE1.md`](REGRESSION_BASE1.md):
  [замер] такт 163 с, 17 строк без вектора → 0, приёмка 8/8.

## Что закрыто именно в сессию 28.07 (чтобы не потерять контекст)

- **п. 13** — перепись полноты; догрузка 6 сущностей из 9 (главная книга 280 проводок,
  справочник 5 881); ложная тревога переписи о тенях регистров убрана;
- **п. 17** — «ночное» упразднено; дельта-синк из 1С по `DataVersion` (добавление/
  удаление/перемещение); перепись схемы отделена от синка (такт 10 мин → ~6 мин);
  конвейер по готовности;
- **п. 21** — средний рубеж уточнения; кнопочная развилка (бэкенд готов, UX в боте —
  последним); «обороты по счёту 62» через выбор регистра;
- **п. 18** — честный отказ при трёх сбоях, видимое старение данных;
- **п. 19** — резолвер (слово → значение), калькулятор (числа ставит код, не модель).
  С 14.08 калькулятор на зерне group сворачивает `GROUP BY` оси ссылки в базе
  (не 40 сырых строк и не NL→SQL).

Подробности с замерами — [`CHANGELOG.md`](../CHANGELOG.md), уроки — [`docs/HOW_NOT_TO.md`](HOW_NOT_TO.md).
