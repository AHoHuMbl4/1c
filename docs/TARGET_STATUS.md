# Статус по контракту: что на какой стадии

Спутник [`TARGET.md`](../TARGET.md). Тот говорит, **чем продукт обязан быть**; этот —
**где мы по каждому пункту сейчас**. Менять `TARGET.md` нельзя; этот файл живой.

**Обновлено: 2026-08-26.** Правило стадий — как в п. 11 контракта: «готово»
ставится только по замеру на живых данных, не по рассуждению. 🔴 **Замера нет — 🟢 не
бывает**, даже если механизм на вид работает: обоснование обязано содержать число,
воспроизводимую команду или предписанную контрактом проверку.

*Срез 24.08 (только обоснования с числом; стадии без повышения без оснований):
Ф6.1 `ASK_SQL_RRF=1` в бою okna `:8091`; Ф6.2–Ф6.4 и §7bis шаги 1–2 в git, на стейджинге
`:8092` AB_PROBE **8/8** (44,19 / 69,02 / 48,62 с — разброс от очереди эмбеддера klient-1);
бой md5 **11d8f158** без флагов Ф6.2/6.3/6.4/§7bis; klient-1: `resolver_index` без
пробелов, `search_corpus` в пересчёте. Источники — `CHANGELOG` 24.08,
`PLAN_UPGRADE_NATIVE` Ф6, `REGRESSION_BASE1.md`.*

*Срез 25–26.08 (только обоснования с числом; стадии без повышения без оснований):
полный скорер okna `:8092` **21/25**, сбоев **0**, средняя **29,26 с**, `code_md5`
**733f693f**; `check_deploy_gate.py` → PASS (`CHANGELOG` «полный скорер 21/25»,
`docs/DEPLOY_GATE.md`). Закрыты классы K4 и K5a; остаток **4 FAIL** (rank top-N,
K5b, два K6). Генератор алиасов исправлен в git, живой пересбор — нет.
П. 4/16 контракта: PostgreSQL выведен, на Windows базы данных нет.*

Стадии: 🟢 **готово** (проверено замером) · 🟡 **частично** (работает, но не по полному
критерию) · 🔴 **не начато** · ⚪ **по контракту — последним**.

---

*Правка 25.08 (K5 atom-terminal): `ASK_ATOM_TERMINAL=1` на локальном probe —
K5a **OK** (307); K5b atom наружу, refuse снят; выкат `:8092` ждёт оркестратора.
Стадии не повышали — K5b scorer vs gold и полная приёмка на `:8092` не сняты.*

*Правка 25.08 (rank-путь): на стейджинге okna `:8092` с `ASK_SALES_RANK_CANON=1`
скорер **19/24** (было **15/24**); класс K1+K2+K3 **4/4**; AB_PROBE **8/8**.
Стадии пп. 2/3/10/21 **не** повышали — 19/24 ≠ критерий «отвечает верно».
**Устарело как текущий срез:** полный набор **25** вопросов даёт **21/25**
(`CHANGELOG` «полный скорер 21/25»).*

*Правка 25–26.08 (полный скорер): обоснования пп. 2, 3, 4, 5, 6, 7, 10, 12, 13,
14, 16, 21 по **21/25** и `DEPLOY_GATE`; **стадии не сдвигались** — 21/25 ≠
критерий «отвечает верно» / «без ошибок» / «правильный».*

## Одной таблицей

| # | Пункт | Стадия | Чем закрыт / что осталось |
|---|---|---|---|
| 1 | Полноценный продакшен | 🟡 | сумма остального; эксплуатация (журналы, поведение под нагрузкой) не замерена |
| 2 | Бот отвечает по данным 1С | 🟡 | отвечает через бота, числа считает база, гейт держит. Но на приёмочном наборе из **58 вопросов** верных меньше половины: пачка 1-10 даёт 2-3 верных (три прогона), пачка 11-20 — **4 верных, один прогон, разброс не замерен**. Прежняя отметка 🟢 стояла по набору из **восьми лёгких** вопросов. **[замер 23.08]** scorer-25 после переезда okna: старая=новая=**8/25** (паритет, регрессии переезда нет; провалы — дрейф дат gold против снапшота 22.08 + известные просадки; `CHANGELOG` «Приёмка переезда») — **устарело как текущий срез**. **[замер 25.08]** полный scorer `:8092` **21/25**, сбоев **0**, средняя **29,26 с**, `code_md5` **733f693f**; `check_deploy_gate.py` → PASS (`CHANGELOG` «полный скорер 21/25», `DEPLOY_GATE`). Остаток **4 FAIL** — см. `DEPLOY_GATE`. Стадию не повышаем: 21/25 ≠ критерий «отвечает верно по данным» |
| 3 | Ответы без ошибок, детерминированно | 🔴 | **детерминированности нет, и это замерено**: прогоны БЕЗ единой правки между ними расходятся на ±1 верный ответ из десяти — один вопрос даёт то уточнение, то неверный ответ. Главная причина — **выбор сущности** отдан модели (п. 19 это разрешает) и неустойчив; но она не единственная: первоисточник замера (`ACCEPTANCE_UT`) говорит «выбор сущности **и величины**», 10 из 11, и отдельно №10 — там сущность была выбрана верно, а посчитаны группы справочника вместо позиций; **этот дефект закрыт 30.07** («папка справочника не позиция», 252 − 25 групп = 227 = эталон), но он показывает, что причина не сводится к одной. Счёт при этом верен всегда. **[замер 23.08]** scorer **8/25** на обеих коробках — **устарело как текущий срез**. **[замер 25.08]** полный scorer **21/25**, сбоев **0** (`CHANGELOG` «полный скорер 21/25»); **4 FAIL** по gold (`DEPLOY_GATE`); детерминированность (±1 из десяти, 31.07) с тех пор не переснимали (`PLAN_TO_TARGET`). Стадию не повышаем |
| 4 | 1С на Windows, база данных — в SereneDB | 🟡 | Контракт обновлён владельцем 25.08 (`TARGET.md` п. 4, п. 16; решение 22.08 подтверждено): **на Windows базы данных нет** — только 1С и агент выгрузки (пакеты на packet-server); **витрина и индекс — в SereneDB на Ubuntu**. PostgreSQL выведен из контура полностью. Сам Windows-хост — трек владельца (`windows/odata-setup`), с этой стороны не проверяем |
| 5 | Эмбеддинг Qwen | 🟡 | 🔴 **02.08 сменился поставщик, и это меняет весь пункт.** Прежний (`text-embedding-v4` в облаке Alibaba) [замер 02.08] отвечал `Arrearage` на **все 11 ключей** — бот отдавал 503 на каждый вопрос. По решению владельца эмбеддер переехал в наш контур. Модель по-прежнему Qwen, то есть буква пункта соблюдена. Итоговый выбор сделан **замером**: `Qwen3-Embedding-8B` BF16 dim 1024 (GGUF Q8_0 неотличим по качеству — 44 из 44 тот же выбор, — но втрое медленнее). Векторы старой модели несравнимы с новой, поэтому 02.08 шла перевекторизация боевой базы. ✅ **Она завершилась** `[замер 03.08]`: `cov_noemb_pending` = 0 (было 92 080 деловых строк в очереди), отметка `Qwen3-Embedding-8B` стояла у всех трёх — `search_tables`, `search_corpus`, `resolver_index`. **[замер 22–23.08]** на боевой okna (SereneDB 26.08.1) пересчёт на **`Qwen3-Embedding-4B`** завершён: не-service корпус **1 206 662**, резолвер **128 674**, карточки **254** (service без вектора по политике); scorer-паритет после переезда **8/25=8/25** (`CHANGELOG` «Приёмка переезда») — устарело как срез качества. **[замер 25.08]** полный scorer на стейджинге **21/25** (`CHANGELOG` «полный скорер 21/25»). **[замер 24.08]** klient-1: пересчёт 4B идёт — `resolver_index` **0** строк без вектора; `search_corpus` в работе; GPU под пересчётом — очередь эмбеддера (`POST /v1/embeddings` таймаут 25 с при `/health` 0,003 с), разброс латентности AB_PROBE на `:8092` (44–69 с), не флаги Ф6. 🟡 оставлено: польза смыслового пути для верных ответов своим числом выше паритета не доказана; klient-1 на 4B ещё не закрыт воротами Ф4 (`PLAN_UPGRADE_NATIVE`) |
| 6 | SereneDB, поиск через неё | 🟡 | словесный отбор — инвертированным индексом `search_idx`, это верно. *Было [замер 02.08]:* векторного индекса не было ни в одной базе (`duckdb_indexes()` → только `search_idx`/`alias_idx`; `ivf` заблокирован — `VECTOR_DECISION.md`), kNN шёл `SEQ_SCAN`. **[замер 23.08]** на боевой okna 26.08.1 индексы **стоят**: `resolver_ivf_idx` (~69 621, ~312 МБ) и `corpus_ivf_idx` (1,23M docs / emb 1 206 662, ~5,7 ГБ), `metric='ip'` (нормы≈1); recall@10 IVF vs exact = **1,0** (197q) / перепроверка **0,9995**/200q (nprobe=8). Честно: на этом объёме IVF **не быстрее** exact (медиана exact **510** мс, IVF ~**530** мс; `WHERE emb IS NOT NULL` на IVF — ловушка ~1,4 с). `search_tables` (254) — IVF пропущен. **[замер 24.08, Ф6]** Ф6.1 `ASK_SQL_RRF=1` в **бою** okna `:8091` (md5 **11d8f158**), AB_PROBE **8/8**; перемер латентности отложен до конца пересчёта klient-1. Ф6.2 `ASK_RESOLVER_IVF` (умолч. 0): замок `test_resolver_ivf.py` **18/0**; на `:8092` `_resolver_ivf_ready()`=True за 0,10 с, IVF `<#>` отвечает — **бой выкл.** Ф6.3 `ASK_SOLR_SYNONYMS` (умолч. 0): словарь `search_dict_syn` на okna **221** правило / 23 305 байт, `ts_lexize` отвечает; замки `test_solr_synonyms_build.py` **28/0**, `test_solr_synonyms_apply.py` **14/0**; флаг на `:8092`, бой выкл. **[замер 25.08]** K6-провалы: `ts_lexize('search_dict_syn','клиент')` без связи с каталогом контрагентов (`CHANGELOG` «словарь синонимов», `DEPLOY_GATE`); генератор алиасов исправлен в git, живой пересбор — нет. 🟡: ворота Ф6 не закрыты (Ф6.2–6.4 и §7bis не в бою; klient-1 без IVF/гибрида; латентность Ф6.1 не перемерена на свободном GPU) |
| 7 | Поиск только нативными средствами | 🟡 | `ts_phrase`, `ts_levenshtein`, `ts_like`, `ts_any`, `ts_compound`, `bm25`, `ts_highlight`, `ts_dict_agg`, `<=>` — у каждого демка или тест движка + проверка на живой сборке. Разбор агентом `serenedb-native`, `CHANGELOG` 28.07 «Штатность поиска подтверждена по каждому механизму». **[замер 24.08, Ф6.3]** штатный `solr_synonyms` + `ts_lexize` на okna: словарь `search_dict_syn` из 254 алиасов → **221** правило; query-side за `ASK_SOLR_SYNONYMS`, замки **28/0** + **14/0** — бой выкл. **[код+замер 25.08, словарь]** генератор алиасов: обиходные слова + title, не морфология metadata; на живом словаре `ts_lexize('search_dict_syn','клиент')` по-прежнему без связи с каталогом (`CHANGELOG` «словарь синонимов»); живой пересбор aliases — не прогонялся. **[замер+код 25.08, проход 2]** вопрос-эмбеддинг через штатный `ai_embed` (доки `sql/functions/ai#ai_embed`): флаг **`ASK_EMBED_NATIVE`** (умолч. **0** = HTTP; **1** = `to_json(ai_embed(…)::FLOAT[dim])`); секрет `TYPE openai` → vLLM `10.3.1.11:8000`. Патч применён в `serene_ask.py`; замок `test_ai_embed_question.py` **14/0**. Живой okna (N=6): медиана **66,5 мс** (`ai_embed`) vs **31,1 мс** (HTTP `texts`); dim **1024**. Стейджинг `:8092`: baseline AB_PROBE **8/8** (средняя **8,09 с**, HTTP-путь, отметка `okna probe live 0err/8`); scp `gpu-erw:2202` / выкат с `ASK_EMBED_NATIVE=1` — **RUNTIME_FLOOR:egress**, ждёт оркестратора. Бой `:8091` флаг **выкл.**; в бой — с воротами Ф6. 🟡: HTTP по умолчанию на бое; нативный путь в коде, на стейджинге после выката |
| 8 | Всё вокруг SereneDB | 🟢 | корпус, индекс, резолвер, величины, ссылки — внутри движка. Тем же разбором `serenedb-native`, что и п. 7: контракт для строки «7, 8» требует именно ответа агента, а не числа (`TARGET.md`) |
| 9 | Без хардкода, на любой базе | 🟡 | **проверено на чужой конфигурации** — УТ 11.4.9 (2 795 сущностей): синк 1 588, сборка 1 501/1 501, ответы сверены с SQL — всё **без правок кода**. Первая база после правок цела (8/8 лёгкого golden-набора). ⚠ Повторить этот замер сегодня можно, но не тем способом, каким он записан: штатный юнит `1c-serene-ask` (он настроен как раз на **первую** базу — `dbname=postgres` в `/etc/1c-mcp-reports.env`) сейчас `inactive`, а по `ut_test` отвечает процесс вне systemd на :8099. То есть нужен `systemctl start 1c-serene-ask`, а не другой DSN — но тогда на :8099 не останется никого, и бот замолчит. Остался только **другой язык** — см. [`TEST_SECOND_BASE.md`](TEST_SECOND_BASE.md) |
| 10 | Ошибки в ответах запрещены | 🔴 | как п. 3. *Было как полный срез:* «все неверные одного рода — не та сущность» — **чем опровергнуто [замер 25.08]:** из **4 FAIL** один rank top-N (мера), один K5b (compare-хвост), два K6 (сущность); `DEPLOY_GATE`. Арифметика не ошибается нигде, гейт ни разу не пропустил число, которого нет в данных. **[замер 23.08]** scorer **8/25** — устарело как срез. **[замер 25.08]** **21/25**, **4 FAIL** (`CHANGELOG` «полный скорер 21/25», `DEPLOY_GATE`); K4 и K5a закрыты на базлайне. Критерий «любые ошибки запрещены» не закрыт; стадию не повышаем |
| 11 | Ни одного нерешённого пункта | 🔴 | финальная приёмка, когда закрыто остальное |
| 12 | Отказ/уточнение — не ошибка, догадка — ошибка | 🟡 | средний рубеж держится, отказ при данных чинится второй попыткой. Но 🟢 стоять не может: контракт проверяет п. 12 «вопросами, у которых нет одного верного прочтения», а по пп. 3 и 10 в этой же таблице система на таких вопросах **не уточняет, а берёт не ту сущность** — это и есть запрещённый п. 12 выбор наугад. Источник фактов один и тот же (приёмка из 58), значит и стадии обязаны сойтись. **Сдвиг механизма 15–17.08:** план A/B/C шаги 0–6 закрыты (`PLAN_ANSWER_CONTRACT` — детектор/атомы/исходы A·B·C). **[замер 23.08]** scorer всё ещё **8/25** — устарело как срез. **[замер 25.08]** полный scorer **21/25**; K4 и K5a **OK** на базлайне (`DEPLOY_GATE`); остаток **4 FAIL**, из них два K6 — корень в словаре синонимов (`ts_lexize('search_dict_syn','клиент')` без связи с каталогом; `CHANGELOG` «словарь синонимов»). Разметка clarify okna: **339** clarify (`work/clarify-journal-okna.md`). **[код+замер 24.08, §7bis]** шаг 1: карта календаря в `corpus_build.sql`, замок `test_calendar_meta_build.py` **30/0**; шаг 2: ось за `ASK_CALENDAR_AXIS` (умолч. 0), замок `test_calendar_axis.py` **36/0**. На okna `search_meta calendar_*` **пусто** (`corpus_build.sql` в `/opt` от 18.08) — ось no-op; `:8092` с флагом=1: AB_PROBE **8/8**, 48,62 с. Стадию не повышаем |
| 13 | Полнота данных | 🟡 | перепись `search_coverage`; до поиска доходит всё, кроме закрытого правами. **[замер 23.08]** okna `/health`: ложный systemic **503** (423k missing = строки витрины vs объекты корпуса) снят — object-level gap + `tmp3_key`; после выката **760c3923**: **200**, gap **0/0**, AB_PROBE **8/8**. **[замер 24.08]** бой md5 **11d8f158**: `/health` **200** за **~0,05 с**, gap **0/0**, AB_PROBE **8/8**, 0 сбоев. ⚠ 18.08 `/health` по итогам скрывал разворот по дням (okna 17.08 корпус 331 vs 1С 176) — сверка дней в git. ⚠ вечер 18.08: packet-синк съедал отметки apply до rebuild («пересобирали 0 из 351») — замок `changed_sources_sql`/`tmp3_lag` в git, догон на okna. **[замер 22.08]** переезд okna (EXPORT/IMPORT → 26.08.1): сверка **418** таблиц **1-в-1**, `search_idx` **1 230 156**, alias/entity_card **254** (`CHANGELOG` «Переезд okna»). 🟡 25.08, аудит корректности: полнота до клиента не держится — **47/67** единственных ответов ушли в уточнение, **180** ответов отдали figures при уже посчитанном атоме (класс K5; K5a на полном scorer **OK** — `DEPLOY_GATE`) |
| 14 | Инсталлятор | 🟡 | **[контракт изменён 26.08]:** ручное действие одно — логин/пароль администратора базы 1С; читателя в 1С и пользователя Windows создаёт сам инсталлятор (было: «ручных действий два»). Проверка: «установка на чистую машину — одно ручное действие». Ранее — снято ручных шагов: раскладка кода (`deploy.sh`), шлюз OData шаблонным юнитом, **выкатка бота** (`deploy_instance.sh` — персона, MCP-мост, плагин-гейт, чистка рабочей папки). Последнее оказалось не удобством: пока копирование стояло ручным шагом в руководстве, бот двое суток работал по старой копии, и правки промта не влияли ни на один ответ. Осталось: сторона Windows (агент выгрузки; базы данных там нет — п. 4/16) и роли/секреты на Ubuntu |
| 15 | Приёмка из базы клиента | 🔴 | **владелец: приёмка в конце; инфраструктура — сейчас (26.08).** Задача **И0** в `PLAN_TO_TARGET.md`: восстановить утерянный `etalon_1c.py` (сверка эталонов с живой 1С по OData), генератор вопросов из `$metadata` + `ask_journal`, двойной эталон с независимым прочтением; проверка на ut_test/klient-1. Набор пока под наши данные. Сдвиг 31.07: эталоны набора **сверены с живой 1С** по OData, а не с нашей витриной — витрина не может проверять сама себя. Один эталон при этом оказался неверным (считал группы справочника складами) |
| 16 | Периметр (модели внутрь) | 🟡 | **[контракт изменён 26.08]:** наружу уходит **только вызов DeepSeek для ответов OpenClaw** (временно, для тестов); Alibaba из контракта убран — документы с упоминанием действующего egress на Alibaba (`RUNBOOK_DEPLOY.md`, `ARCHITECTURE.md`, `UBUNTU_SETUP.md`, `PLAN_PRIVACY.md`, `PLAN_THREE_STEPS.md`) требуют синхронизации при ближайшей правке. **размещение данных (25.08, `TARGET.md` п. 16):** витрина и индекс — в SereneDB на Ubuntu; на Windows базы данных нет, только 1С и агент выгрузки; PostgreSQL выведен из контура. **Сдвиг 02.08: эмбеддер И реранкер переехали в наш контур** (`Qwen3-Embedding-8B` BF16 dim 1024 и `Qwen3-Reranker-8B` BF16, решение владельца) — в чужое облако они больше не ходят. *Было далее:* снаружи оставалась модель ответов `deepseek-v4-pro`. **[решение+замер 17.08]** ask okna переведён на свою vLLM **`Qwen3.8-27B`** (FP8); оплата API ответов = 0 (`CHANGELOG` «контур ответов okna»). **[замер 23.08]** канон GPU на vSwitch: `DEEPSEEK_BASE` → `10.3.1.12:8000` (имя переменной историческое — модель Qwen3.8-27B), пять эндпоинтов с коробки живы (`NETWORK` §2.1; `CHANGELOG` «Приёмка переезда»). **[поправка владельца 23.08]** DeepSeek облако — **только веб-фронт**; **ask всегда на локальной**. 🟡: за периметр ещё уходит веб-канал DeepSeek (`deepseek-v4-flash` в web-профиле, откат с Qwen 17.08) |
| 17 | Свежесть — как можно быстрее | 🟡 | 🔴 **[контракт изменён 26.08]: фиксированного срока нет** — на огромной базе эмбеддинг физически не успевает посчитать векторы в наперёд заданное окно; «как можно быстрее», без заметной нагрузки. Проверка: задержка от изменения в 1С до ответа бота **измерена на живой базе и показана владельцу**, порог не задаётся. Правило п. 20 дополнено 26.08: эмбеддинг — штатным вызовом движка `ai_embed` (замер 26.08: жив в сборке 26.08.1). Прежняя норма: ~~≤ 20 мин~~. **Условие владельца 06.08: «20 минут не обязательно; обязательно не делать проходы по данным, которые не изменялись».** `[замер 06.08, такт 06:18:45 → 06:31:17]` сборка запущена принудительно при нуле изменений: **«пересобирали 0 из 1502», проходов по источникам ноль**, шаг корпуса 30 с против 802 с, вся сборка 343 с против 1123 с, корпус цел (623 565 строк, 1502 сущности). Карта ссылок стала постоянной, текст собирается по изменившимся, переименования ловятся сравнением имён до и после (проверено: одно имя из `refs` встречается в 22 источниках). Такт за сутки: **8301 → 748 с**. ⏳ **Что ещё читается при нуле изменений:** 305 источников, 189 с — те, для которых 1С признака изменения не даёт вовсе; узнать, менялись ли они, можно только прочитав. **[замер 24.08, Ф6.4]** `ASK_HEALTH_NATIVE_FRESHNESS` (умолч. 0): на `:8092` `/health` **200** за **~0,1 с**, блок `freshness` из `sdb_metrics` (buffered/failed_commits/refresh_pending/refresh_active); замок `test_health_native_freshness.py` в git — **бой выкл.** 🟡 стоит потому, что контракт требует замера самой ЗАДЕРЖКИ «изменение в 1С → ответ бота», а снять его нельзя: 1С мы только читаем |
| 18 | Честный отказ при сбоях | 🟡 | механизм есть: модель/база down → 503 с понятным сообщением; 1С down → корпус консистентен; старение видно припиской. Но контракт даёт пп. **12 и 18 одну строку приёмки** («проверяется вопросами, у которых нет одного верного прочтения»), и этой проверки не было — той же непроведённой проверкой понижен п. 12, значит и здесь 🟢 стоять не может |
| 19 | Модель не считает и не ищет | 🟢 | считает база, отбирает индекс. Объём в модель **замерен**: `CHANGELOG` — **6–20 тыс. символов на вопрос**, и это не растёт с базой. Границы держит код: `ASK_PICK_BUDGET_CHARS`=8000, `ASK_ROWS_BUDGET_CHARS`=24000. Числа в текст подставляет код, не модель |
| 20 | Нет своих скриптов над базой | 🟡 | вся работа с данными идёт внутри движка — это верно; но «своего кода в сборке нет» сказать нельзя: `build.sh` зовёт `classify_entities.py`, а `embed_missing.sh` крутит цикл раундов с параллельными `psql` — а «`psql` в цикле» контракт запрещает поимённо. Но контракт требует по п. 20 **замера за такт** (число запусков `psql` и объём наружу), а его нет: единственный записанный замер («один запуск `psql`, ноль строк наружу») снят с фазы `corpus_build.sql` до появления досчёта векторов. Статически за такт: до 12 запусков `psql` в `build.sh` (часть под условиями) плюс **три** захода `embed_missing.sh` — каждый с циклом раундов и веером из 8 параллельных `psql`; четвёртый заход стоит за флагом `EMBED_SERVICE`, который не задан. Старый питон-сборщик снят с таймера |
| 21 | Отдать ответ, правильный | 🟡 | порядок соблюдён: ответ → уточнение → отказ, и уточнение работает связкой `focus`. *Было (приёмка 58, 31.07):* «половина пачки уходит в уточнение или в неверную сущность» — **устарело как текущий срез [замер 25.08]:** полный scorer **21/25**, неверных **4/25**, не половина (`CHANGELOG` «полный скорер 21/25», `DEPLOY_GATE`). Отдельно закрыто: **отказ при отсутствии данных проверяется прогонщиком** («Ромашки» нет вовсе, велосипедов ноль, данные кончаются 2019-11-18). **[замер 23.08]** scorer **8/25** на обеих коробках после переезда — устарело как срез. **[замер 25.08]** полный scorer **21/25**, сбоев **0**; **4 FAIL**; «правильный» не закрыт; стадию не повышаем. Промежуточная цель оркестратора ≥14/25 достигнута, критерий контракта — нет |

**Итог (по таблице выше): 🟢 2 · 🟡 15 · 🔴 4 = 21.**
(🟢 8,19 · 🟡 1,2,4,5,6,7,9,12,13,14,16,17,18,20,21 · 🔴 3,10,11,15)

*Правка 26.08 (арифметика итога): было «🟢 5 · 🟡 12 · 🔴 4» и список
(🟢 4,7,8,13,19 …) — с `ffdfbbc` пп. 4/7/13 уже 🟡, а строка «Итог» не
пересчитана (`docs/audit/target-status-verify.md`). **Чем опровергнуто:**
пересчёт по таблице → 🟢 **2** (8,19) · 🟡 **15** · 🔴 **4**. Держит замок
`ubuntu/serenedb/test_target_status_totals.py`.*

*Правка 24.08 обновила обоснования пп. 5, 6, 7, 12, 13, 17 по Ф6 и §7bis; **стадии не сдвигались**.*

*Правка 25–26.08 (задача И1, `PLAN_TO_TARGET`): обоснования пп. 2, 3, 4, 5, 6, 7,
10, 12, 13, 14, 16, 21 по полному scorer **21/25** и `DEPLOY_GATE`; **стадии не
сдвигались**.*

*Правка 23.08 обновила обоснования пп. 2, 3, 5, 6, 10, 12, 13, 16, 21 по замерам
переезда/4B/IVF/scorer; **стадии не сдвигались** (повышать без числа, закрывающего
критерий пункта, файл запрещает).*

*Правка 02.08 сперва записала сюда «🟢 7 · 🟡 10», держа п. 17 в списке зелёных, хотя в
таблице понизила его до 🟡. Ошибка в арифметике честности — в файле, который правился ради
честности; поймана независимой перепроверкой в тот же день. Тогда же понижен п. 18: у него
с п. 12 общая строка приёмки в контракте, и понижать одну её половину, оставляя другую
зелёной, — тот же двойной стандарт.*

🔴 **Отметки понижены 02.08 по аудиту документации** (`work/doc-audit/DISCREPANCIES.md`).
Это снова не ухудшение системы, а исправление завышенной оценки: правило файла и п. 11
контракта требуют ставить 🟢 **только по замеру**, а пять зелёных стояли по формулировке
либо по замеру, которого нет. Прежние обоснования не стёрты — они разобраны здесь:

| # | Было | Чем опровергнуто |
|---|---|---|
| 5 | 🟢 «корпус и резолвер без вектора 0» | [замер 02.08] в боевой `ut_test` без вектора 340 464 строки из 623 565, из них **92 080 деловых в очереди** (`cov_noemb_pending`). «0» верно только для резолвера и только про очередь первой базы |
| 6 | 🟢 «весь отбор — инвертированным индексом» | [замер 02.08] `duckdb_indexes()` в обеих базах → только `search_idx` и `alias_idx`; индекса по `emb` нет, `EXPLAIN` kNN даёт `SEQ_SCAN`. Формулировка описывала лишь словесный путь и скрывала единственное место, где стоимость вопроса растёт с базой |
| 12 | 🟢 «средний рубеж; отказ при данных — дефект» | ни числа, ни прогона неоднозначных вопросов, которого требует контракт; и прямое противоречие с 🔴 пп. 3/10 в этой же таблице — «взята не та сущность» и есть запрещённая п. 12 догадка |
| 19 | 🟢 «объём замерен: до 160 КБ на вопрос» | числа «160 КБ» не было больше нигде в проектных документах — оно появилось только в этой строке; единственный записанный замер того же пункта даёт **6–20 тыс. символов**. `ARBITER_MAX` в списке «границ объёма» — вообще не объём, а число кандидатов для арбитра (=3). Стадия оставлена 🟢: замер есть, неверна была ссылка на него |
| 20 | 🟢 «сборка — один `psql`» | `grep` по `build.sh` → 12 запусков `psql` плюс четыре захода `embed_missing.sh` (цикл раундов, 8 параллельных `psql`). Замер относился к одной фазе, а не к такту; требуемого контрактом подсчёта за такт нет |

⚠ **Отдельно: числа, которыми обоснованы пп. 5 и 9, сняты с состояния, которого на
стенде уже нет.** Служебная запись последнего успешного такта в `ut_test`
(`build_rows`=632 720, `build_noemb`=205 479, `build_ts`=29.07 17:37) расходится с живыми
таблицами (623 565 / 340 464), а перепись `cov_*` пересчитана позже и с ними согласована.
Значит корпус менялся после последнего полного такта, и **слой, по которому бот отвечает
сегодня, ни разу не принят собственными проверками сборки**. Почему менялся — известно и
без домысла: 30.07 корпус пересобирали руками под переделку «строка = объект»
(`CHANGELOG` 30.07, две пересборки подряд дали побитово одно и то же). А переснять
постпроверку нельзя, пока стоит конвейер.

🔴 **Отметки понижены 31.07, и это не ухудшение системы, а исправление завышенной
оценки.** Пункты 2, 3, 10 стояли зелёными и жёлтыми по набору из **восьми лёгких**
вопросов. Полная приёмка из 58 показала другое, а замер разброса — что и прежние
сравнения «стало лучше на один ответ» ничего не значили. Разбор — `HOW_NOT_TO §1.34`,
и это ровно тот урок, что уже записан в §1.17-§1.18: «ноль неверных, устойчиво» нельзя
объявлять по лёгкому набору.

Изменение против 29.07: п. 2 понижен с 🟢 до 🟡, п. 3 и 10 — с 🟡 до 🔴 по полной
приёмке; п. 21 понижен с 🟢 до 🟡 (ответ отдаётся, но у половины вопросов это уточнение
или неверная сущность). П. 14 продвинулся: выкатка бота стала кодом.

---

## Что мешает закрыть жёлтые и красные

🔴 **Было ошибочно (до 02.08): «почти всё оставшееся упирается во внешнее, а не в нашу
работу».** Чем опровергнуто: аудит доков 01.08 нашёл **53 дефекта нашего кода и стенда**, и
они разложены по десяти кластерам в [`work/doc-audit/DEV_PLAN.md`](../work/doc-audit/DEV_PLAN.md).
Внешнего там — восемь пунктов из сорока семи развилок. Ниже перечислено именно внешнее,
но оно не «почти всё».

Порядок работ и то, чем каждый кластер проверяется, — в `DEV_PLAN.md`; сюда стадии
переносятся по мере закрытия, замером.

Внешнее, чего мы сделать не можем:

- **п. 9** — вторая конфигурация ПРОЙДЕНА (УТ 11.4.9, 28.07): работает без правок кода,
  найдено и починено 5 дефектов, невидимых на одной базе. Остался **другой язык** —
  англоязычную конфигурацию 1С достать сложно.
- **п. 15** (приёмка из базы клиента) — набор вопросов должен строиться из самой базы;
  по слову владельца делаем в конце.
- **п. 16** (модели внутрь контура) — **решение владельца** о периметре, не техника.
- **п. 3/10** (любые ошибки запрещены) упираются в **п. 15**: чтобы доказать «без
  ошибок», нужен большой набор из самой базы и прогон независимыми агентами.
- **п. 14, 1, 11** — инсталлятор и финальная приёмка, по контракту делаются последними.

🔴 **Крупное без внешнего осталось по полному scorer 25.08 (`DEPLOY_GATE`):** **4 FAIL**
из **21/25** — rank top-N (мера `Количество` vs деньги; правило в git, живьём не
сошлось — `CHANGELOG` «rank×sales»), K5b compare-хвост, два K6 (не та сущность;
корень — словарь синонимов без обиходных слов, генератор исправлен, живой пересбор
— нет). *Было как полный срез:* «выбор нужного объекта — причина **всех** неверных
ответов» — **чем опровергнуто [замер 25.08]:** K4 и K5a закрыты; остаются разные
классы провала. Плюс два дела на стенде, без которых обоснования не пересдать:
поднять конвейер свежести (п. 17) и завести сервису ответов второй базы штатный юнит.

*Было ошибочно: «из того, что двигается без внешнего, крупного не осталось». **Чем
опровергнуто:** полная приёмка 30-31.07 — верных меньше половины, и ни один из
перечисленных внешних блокеров к этому отношения не имеет.*

---

## Что сделано 29.07

**Ведущий документ дня — [`PLAN_AUTONOMY.md`](PLAN_AUTONOMY.md)** (цель: «поставил и
работает», ни одной ручной правки). Закрыта Фаза 3, начата Фаза 2.

- **разметка сущностей моделью** — что бизнес, что служебное; [замер] УТ 1 501/1 501,
  ноль неразобранных. Задаёт **очерёдность** векторизации, ничего не выбрасывает (п. 13);
- **порядок векторизации**: метки → резолвер → корпус, и внутри корпуса не-служебное
  раньше служебного, а служебному вектор **не считается вовсе** (решение владельца 29.07).
  [замер] это 41% корпуса; отсутствие вектора названо в переписи причиной;
- **три дефекта одного класса — «общее имя на все базы»**, каждый ломал чужую базу молча:
  замок брал имя разбором DSN; рабочие таблицы досчёта назывались одинаково; **секреты
  движка общие на инстанс**. Все три вскрылись только при работе двух баз рядом;
- **ручных шагов минус два**: раскладка кода (`deploy.sh`), шлюз OData шаблонным юнитом;
- **`nightly-eval` погашен** — чужой евал слал владельцу алерт с боевого токена;
- **регрессия первой базы** сведена в [`REGRESSION_BASE1.md`](REGRESSION_BASE1.md):
  [замер] такт 163 с, 17 строк без вектора → 0, приёмка 8/8.

## Что закрыто именно в сессию 28.07 (чтобы не потерять контекст)

- **п. 13** — перепись полноты; догрузка 6 сущностей из 9 (главная книга 280 проводок,
  справочник 5 881); ложная тревога переписи о тенях регистров убрана;
- **п. 17** — «ночное» упразднено; дельта-синк из 1С по `DataVersion` (добавление/
  удаление/перемещение); перепись схемы отделена от синка (такт 10 мин → ~6 мин);
  конвейер по готовности;
- **п. 21** — средний рубеж уточнения; кнопочная развилка (бэкенд готов, UX в боте —
  последним); «обороты по счёту 62» через выбор регистра;
- **п. 18** — честный отказ при трёх сбоях, видимое старение данных;
- **п. 19** — резолвер (слово → значение), калькулятор (числа ставит код, не модель).
  С 14.08 калькулятор на зерне group сворачивает `GROUP BY` оси ссылки в базе
  (не 40 сырых строк и не NL→SQL).

Подробности с замерами — [`CHANGELOG.md`](../CHANGELOG.md), уроки — [`docs/HOW_NOT_TO.md`](HOW_NOT_TO.md).
