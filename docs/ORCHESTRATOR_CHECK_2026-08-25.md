# Перепроверка оркестратора — 25.08.2026 (~11:30–12:05 UTC)

**Режим:** только чтение / живые `/ask` / журнал; процессы не
перезапускались, бой `:8091` не трогали, `ubuntu/serenedb/serene_ask.py`
не правили. Туннели: `127.0.0.1:18091` → бой `:8091`,
`127.0.0.1:18092` → стейджинг `:8092`, `17890` → okna SereneDB
(`ss -tln` — слушаются).

Срез: Qwen3.8-27B доступен (ответы 7–30 с, не пустые 0,3 с).

---

## Таблица по пяти пунктам

| # | Утверждение оркестратора | Вердикт | Доказательство |
|---|---|---|---|
| 1 | На `:8092` выложен код = `9d6d788` (`md5` **6ec29dc2…**) | **ПОДТВЕРЖДЕНО** | `git show 9d6d788:ubuntu/serenedb/serene_ask.py \| md5sum` → `6ec29dc2930d077d5f32b8844d5d4770`. В `ask_journal` с 11:39 у запросов через `:18092` тот же `code_md5` (в т.ч. `chkK5a001`, `chkrank01`). Wire оркестратора: `scp` → `/opt/1c-mcp-reports/serene_ask_probe.py` с тем же md5 на обеих сторонах. |
| 2 | Рестарт `:8092` с 4× `source` env + флаги (calendar/freshness/IVF/rank/atom/solr/RRF) | **ПОДТВЕРЖДЕНО** | Финальный запуск `bash-gtdu6wa0` (11:42): `source` `/etc/1c-mcp-reports.env`, `1c-embed.env`, `1c-serene-ask.env`, `1c-serene-ask-postgres.env` + перечисленные `ASK_*`. `/health` `:18092` отдаёт блок `freshness` (на бое его нет). Живой `/ask`: «какой клиент больше всех купил в этом месяце?» → `kind=answer`, атом `rank`, текст «Реализация ТМЦ (регистр накопления)», `diag.rank_deterministic=true`, `sql_rrf=true`. K5a «сколько документов реализации за декабрь 2025?» → `answer` **307**, `fork_outcome=unique`, не clarify. |
| 3 | AB_PROBE **8/8** за **23,55** с, отметка обновлена | **ПОДТВЕРЖДЕНО** | Вывод скорера в wire (11:46): `верных 8/8, средняя 23.55 с` + `отметка: okna probe live 0err/8`. Файл `.claude/.probe-okna-last-run`: mtime **11:46:57**, содержимое `okna probe live 0err/8`. |
| 4 | Бой `:8091` не трогали | **ПОДТВЕРЖДЕНО** | `/health` `:18091` — `serene-ask-ok` **без** `freshness`. Контрольный `/ask` «сколько клиентов в базе?» → `answer` (353). `ask_journal` для `chkboy001`: `code_md5=11d8f158…` (старый бой), не `6ec29dc2`. |
| 5 | Полный скорер ~24 вопроса на `:8092` | **РАСХОЖДЕНИЕ** (запуск да, итог ≠ ожиданию) | Процесс `AB_CONTOUR=okna ASK_URL=…18092… ab_scorer.py` шёл с 11:47, завершён. Итог в `bash-q6o95dzy/output.log`: **`live верных 20/25`, средняя 21,61 с** (5 FAIL). Ожидание оркестратора **21/24** не сбылось: в `ab-gold-okna.tsv` **25** вопросов; K5a OK, K5b («в этом месяце продали больше…») — FAIL (`figures`, атом 1755883.45 ≠ gold). Отметки `.golden-okna-last-run` нет (hits &lt; n — ожидаемо). |

---

## Замеченные риски

1. **Первый рестарт `:8092` без DSN/пароля** (`bash-w87712cv`): `/health` **503**,
   в логе `SERENEDB_DSN_RO is required` / `ASK_TOKEN не задан`. Починено
   повторными рестартами; к AB_PROBE процесс уже был с полным env. Риск
   повтора при «голом» `env ASK_…=1` без `source` четырёх файлов.
2. **Строка `PROBE … code_md5=` в `ab_scorer`** считает md5 **локального**
   `/opt/1c-mcp-reports/serene_ask.py` на машине прогона (здесь
   `760c3923…`), а не файл на okna. Версия стейджинга — только
   `ask_journal.code_md5` (или ssh `md5sum` на сервере).
3. **K5b не закрывает золото:** атом compare наружу есть, но скорер по
   digits всё равно FAIL — «+2 к 19» не получается; база после K5 =
   **20/25**, не 21/24.
4. Параллельный полный скорер и чужие `/ask` на одном `:8092` дают шум в
   журнале; на итог `tail -40` скорера это не повлияло.

---

## Вывод

Выкат `9d6d788` (`6ec29dc2`) на стейджинг `:8092` с нужными флагами и
AB_PROBE 8/8@23,55с подтверждён, бой не задет; полный скорер дал **20/25**,
а не обещанные 21/24.
