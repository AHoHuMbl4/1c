# ПЛАН «Молекулы» v2 (финал) — фиксы долгих ответов

Дата: 11.09. Статус: волны M×4 + N×4 внесены дословно; контрольный круг — до
стоп-точки владельца. Источники: mol-k6/par/psql/red (M), mol-plan-n1..n4 (N).
База: полигон :8092 (код B+A), прод не тронут. Цели W4: меню ≤20с; полный
отчёт ≤40с; простой ≤15с.

## 0. Бюджет (анти-двойной счёт: A2 и K6 живут на РАЗНЫХ классах вопросов)

Класс «меню» (составной): A2 перехватывает ДО K6 → K6-выигрыш НЕ считается.
Класс «полный путь» (однозначный/выбранная ветка): A2 молчит → работает K6.

| Пункт | Класс | −сек (ожидание) | Остаток тяжёлого |
|---|---|---|---|
| факт | — | — | 88+ с (74 к отсеву + ~14) |
| 1. A2 | меню | −60…70 (K6+post не платим) | меню ≤20с |
| 3. K6-индекс/витрина | полный | −35…45 (52→≤10) | ~40с |
| 4c stock#2 | полный | −10…15 | — |
| 4a/4d fork | полный | −6…12 | — |
| 4b measures-кэш | полный | −1…2 | — |
| 4e psycopg | оба | −2…4 (после SQL — опция) | — |

## 1. A2 — лёгкое меню ДО K6 (первый; критерий N1, дословно)
- **Пул** = пересечение (а) `cands` после буквально+смысл+prefer_* (z20:2222-2239)
  и (б) источников с непустым label (search_tables / fork_labels_covering НА
  ЭТОМ наборе). НЕ расширение fork_key-siblings; kind-only alias-FTS как
  единственный источник — ЗАПРЕЩЁН. `search_fork_label` не имеет kind и не
  даёт «все продажи» (N1-опровержение) — из него только подписи.
- **Точка**: непосредственно перед `K6R.apply_to_candidates` (z20:2254).
  Intent — да; fork-детектор — нет; wiki — см. §1-wiki.
- **§1-wiki (стоп-точка, решение владельца)**: wiki-выбор сегодня идёт после
  K6 → A2 без префлайта перехватывает вопросы, где wiki ответила бы без меню.
  Варианты: (W1) wiki-префлайт до A2 (быстрый зонд, те же wiki-запреты);
  (W2) A2 только на лексике составного (узкий allow) — wiki-перехват
  маловероятен, риск принят; (W3) не внедрять A2 до решения.
- **Allow**: только составной/отчётный (`want=list` / «полный отчет» / «и
  чего» / rank-ось) — как `_ec_composite` у A; `_ec_src_ambig` НЕ копировать.
- **Запреты**: полный список A (mol-plan-n1 таблица 1-14) + A2-1…A2-7
  (детали в mol-plan-n1.md §запреты — канон для исполнителя); любое сомнение,
  неполное покрытие labels, ≤1 opts → полный путь.
- **Приёмка**: (а) негатив — A2 молчит, где полный путь (без дедлайна) даёт
  answer/figures/wiki-lock/canon; (б) на пересекающихся Q сверка МНОЖЕСТВ src
  (+label/hint, тот же mk_opts/preds); (в) тайминг ≤20с, diag early_a2_path=1
  (отдельно от early_clarify_path); (г) замки + **отдельный L67-A2** (группа
  A2+B+A; L67 — ослабление, свой прогон).
- п.0: холодная база без labels → A2 no-op (полный путь).

## 2. EXPLAIN+TRACE вход (оркестратор, до правок SQL; лист N2 дословно)
E1-E7 mol-k6-m1 + E1i (live через search_idx @@), E3b (corp count+FILTER
doc_date из INCLUDE), E3c (JOIN idx→corpus / расширенный INCLUDE), E3d
(OR×N vs IN на малой базе и okna), E4i (unnest: nums в индексе? зонд
search_measure_alias / F1∪M1 одним SQL), E6 (stock EXISTS vs n_with_nums).
Отдельно **E8-TRACE-субмаркеры** (stock#1, expand_holders, expand_stem_and_live,
features_table, stock#2) — разрезать 52с/21.5с. Стоп п.3 без wall E1/E2/E4 и
рабочего index/mat-пути с эквивалентом FILTER F1.

## 3. K6: индекс и/или витрина (после §2; L67) — три молекулы, три решения
(1) corp F1: search_idx для n_rows±n_dated (если E3b выигрывает);
    n_cards/n_with_nums — JOIN E3c / расширение INCLUDE (flags,nums) /
    materialized `search_src_stats(src_table,n_rows,n_dated,n_cards,
    n_with_nums)` — DDL в corpus_init, refresh на такте, без ручной набивки;
    дрейф: diag src_stats_ts(+lag) — «приемлемо до такта» — явное решение
    владельца на стоп-точке.
(2) live L1: префикс через индекс (E1i) + nums/refcols без полного скана;
    либо предвычисленный пул live-accum на такте.
(3) unnest M1: слить с F1 в один SQL / ключи из search_measure_alias /
    колонка витрины — не второй table-scan IN(77).
Запреты: питон-пулы поверх базы; калибровка OR×77 только под okna без E3d.
Приёмка: 3 класса полигона ГДЕ A2 НЕ ПЕРЕХВАТИЛ: wall «K6 v2» (z20:2269)
52→≤10с; L67-ранг; замки; малая база — E3d не регресс (или fallback table
scan при малом |corpus|).

## 4. Хвосты (порядок N3): 4b → 4a → 4d → 4c (после §3) → стоп → 4e
- 4b: кэш measures_of, ключ (rid, src) — сейчас 2 SQL без кэша.
- 4a: reuse fork_detector_scan при совместимом пуле (два вызова: z20:2702
  _fork_early пул cands[:16…], z20:3508 arb_pool — не «на A/B», а reuse
  при совместимости; риск B8-01 — в замок).
- 4d: fork readings — multi-window SQL вместо цикла fork_scan_readings
  (риск порядка cells/meta — замок).
- 4c: stock-фильтр — потребитель витрины п.3 (n_with_nums / src_stats),
  не клон индекса; E6-атрибуция 21.5с ДО решения (порядок M4 «до K6» снят
  N3/N4: после §3, при E6-доказательстве).
- 4e (последний, опционально): psycopg3 thread-local; после SQL-выигрышей
  может не понадобиться (−2…4с); если да: замок CSV↔fetchall, thread-safety.

## 5. Не делаем
ThreadPool поверх тяжёлого SQL; SAMPLES=1; дедлайн ≥90с; A2 в прод без
негативной приёмки §1(а) и без решения §1-wiki; FORK_DETECT=0 как обход;
«лечить K6 psycopg'ом»; калибровка форм под okna без E3d; такт/B/flip в этом
пакете; выкат «всё одним окном» — только пошаговый бой (§6).

## 6. Бой (пошагово, прод только после стоп-точки владельца)
Шаг 1: A2 (полигон: замки → негатив+множества → тайминг → **L67-A2**) →
стоп-точка-мини (владелец смотрит меню вживую) → выкат A2+B+A на прод.
Шаг 2: §2-замеры (оркестратор) → §3 K6 (полигон: замки → тайминг → L67-ранг
+ полный L67) → стоп-точка → выкат.
Шаг 3: 4b → 4a → 4d → 4c (каждый: замок + тайминг; L67 после 4c) → стоп →
выкат. Шаг 4: 4e — отдельное решение.

## 7. Откат
Каждый пункт — атомарный коммит; revert + рестарт ask; тайминги после
каждого отката; L67 после отката не требуется.

## 8. Стоп-точка владельцу (формат N4)
Свод −сек по классам (§0); порядок с зависимостями; риски+контрмеры (wiki,
дрейф витрины, малые базы); откат; решение §1-wiki (W1/W2/W3); скоуп недели:
Шаг 1 (A2) — да/нет; §3 — после замеров.
