# СНОС-15: ИСПОЛНИТЕЛЬНЫЙ ПЛАН v2 (2026-09-11, после красной волны K)

Формула №15 (владелец, 11.09): вопрос → интерпретация из вики → запрос в базу →
варианты с описаниями из вики (если прочтений >1) → выбор человека → ответ.
Ничего больше. Снос СПЛОШНОЙ. Владелец: «не точечно, а весь путь».
Входы: snos15-MERGE.md, M1-M4, P1-waves, P2-cuts, P3-accept, K1-K4 (красная).
**Нумерация волн этого файла — единственный канон** (W* в P1/P3 — устаревшие
ярлыки). До стоп-точки владельца (§7) код не трогаем.

## 0. Правила исполнения (все волны)

1 волна = 1 коммит (пат-спек, код+_bootstrap+замки ОДНИМ коммитом, «Числа:»/
«Доки:», граф тем же коммитом) = 1 выкат (deploy-ask.sh + рестарт) = замер.
Цепочка: правка → офлайн-замки (не pytest) → load_all-замок (импорт без
NameError, diff патча ⊆ снесённое) → полигон :8092 → прод.
Откат: revert волны N валиден ТОЛЬКО до волны N+1 (K3 §6): код+bootstrap+замки
одним пат-спеком, выкат симметричен; частичный возврат z11 без call-sites
запрещён.
СТОП-критерий: Δconfident_wrong > 0 — откат; ИЛИ diag-стоп: kind=answer ∧
(прочтений >1: pool>1 по карточкам / мер >1 / окон >1) ∧ меню не показано —
откат, даже если число совпало с эталоном (скрытый wrong, K3 §3).
match→clarify = НОРМ. no_data при живых данных = дефект п.21.
Резать по МАРКЕРАМ (имена функций/символов), не по номерам строк M-карт —
строки плавают (K2 §2).

## 1. Приёмка (по K3)

- Срезы групп после КАЖДОЙ волны (jsonl kind+verdict, не md-колонка):
  В1: stock A(18)+B(3 списки складов=«3»)+«петли»; В2: sales 8-10 + catalog
  (прайс/банки/валюты/орг) + stock A + книгапродаж+реализациятмц; В3: sales 8-10
  + вопросы без явной меры; В4: register-count ВСЕ 31 + sales 8-10; В5: period 9
  ∩ sales + sales multi-src + catalog; В6: register «за 30 дней» + 1 event без
  окна; В7: полный L67.
- Полный L67: до пакета (якорь), после В2, В4, В7 (+ после любой, где на срезе
  wrong или no_data на бывшем match).
- Diag-разбор (src_table/measure_id/period) на ВСЕХ бывших match среза после
  В2, В3, В4, В5, В6 (K3 §3.2). Замок: answer при measures_of>1 без choice.
- Реестр 3 wrong эпохи (прайс-ось 2402, кассовыеордера-лаг, позавчера):
  wrong→match НЕ засчитывать как победу волны без разбора корня; допуск:
  clarify / тот же wrong / тот же класс корня.
- Замер скорости В2: ОДИН зафиксированный rid класса «полный путь» + маркер
  времени до входа wiki_cascade, полигон; Δ среднего L67 как доказательство
  −52с НЕ использовать (K3 §5). Обещание «−52с по пакету» снято: K6 просто
  уходит из тракта.

## 2. Волны (7)

### В1 «обёртки» — цели 10, 12, 13
Снести: stock_bypass_empty_by (обе ветки → no_data); _try_memory_apply +
ACM.probe_memory_apply вход; enough-слой (_answer_checked_core ветвление
ENOUGH_ON + helpers question_facts, ЕСЛИ только для enough; импорт
serene_enough из _imports убрать).
Остаются: билеты z14 целиком; shadow-память diag-only.
Замки: test_final_stock_route_filters_absent +bypass-негатив;
test_ask_choice_memory apply→негатив; test_enough → негатив «слоя нет».

### В2 «до-вики порядок» — цели 1, 2, 4, 14a, parent-before-child
Снести (НАРЕЗКОЙ, не файлом — K2 §1.1):
- из z11: prefer_entity_for_sales, sales_canon_src, sales_canon_force_pool,
  sales_canon_engaged-выборную часть, sales_money_measure,
  sales_force_money_measure, prefer_entity_for_catalog_count (+hardcode имён),
  period_zero_why_question, sales_rank_* выборные; КЛАССИФИКАТОРЫ
  (sales_sum_intent, sales_rank_engaged, sales_noncanon_focus и всё, что зовут
  z20/z05/z16 напрямую) — ОСТАВИТЬ до В5 либо перенести в z02 в этой же волне;
  _ZONE_FILES чистить только если файл уходит целиком (не уходит);
- call-sites z20: 2228-2230 (prefer×3), 2660-2665, 3270-3278, 3378-3380;
  lock-запись 4077-4086; helpers sales_period_empty/fork-empty 1206-1285;
  YoY-clarify 1859-1871 переписать без канона; _ec_locks опору на lock убрать;
- z21:671-673 bypass; prefer_entity_for_rank (z10:428-491);
- K6: блок 2232-2261 + K6R из _imports (entity_rank_v2.py остаётся файлом,
  вне тракта) + event_filter_pool→K6R (z05:1005-1008) + z16:392-393;
- сито emb+rerank КАК ФИНАЛ порядка сущности (2394-2543) — из пути сущности;
  rerank как порядок входов LLM/verify можно оставить, если не даёт [0]-ответа;
- parent-before-child (2544-2577) — здесь же (15∥14);
- _bootstrap: ветки sales_canon_post (:198-219) + fork_b (:221-237) — тем же
  коммитом.
Замки: НОВЫЙ test_no_pre_wiki_reorders.py (0 call-sites prefer_*/K6R.apply/
order_by=rerank-как-сущность/parent-reorder); test_sales_* → негатив;
test_no_domain_wordlists до/после; wiki stubs обновить; load_all-замок.
Контроль В2: полный L67 + diag (см. §1).

### В3 «мера и ось» — цели 3, 14b (+хвосты K1 §1.2)
Снести: канон-ветки меры z20 4144-4170, 4230-4265, 4326-4332, 4360-4379;
pick_measure how=rerank/base-winner (z16:589-634 — при >1 fits → ask/alts);
unresolved_quantity names[0] (z16:415-435); rank_axis ordered[0] + авто
(z10:233-295; z20:4467-4490 включая decide_grain/_kh); rank_fold тело
(z20:4541-4609); rank_deterministic_answer (z10:338+; z20:4994);
rank_gate_fallback_answer (z10:419+; z20:5180); stock_breakdown_leader_fallback
(зовы z20:4362, 4676, 4701, 4722, 4741, 4781 — ×6, НЕ orphan);
_bootstrap: meas/skip/rank_fold (:145-196).
При >1 живой мере: ТОЛЬКО меню (measure_alts 4386-4443 + z18 подписи);
«totals_of всем именам → compose» при >1 мере — ЗАПРЕЩЕНО (K4 §3.1: это обход
меню; безальтернативная мера — totals_of допустим).
Замки: test_k4_guess_vs_clarify, measure_*, rank_* под меню; НОВЫЙ негатив
«answer при measures_of>1 без measure_choice»; замки меры инвертировать В ЭТОЙ
же волне (K1 §5.8 — не оставлять ложно-зелёных между В2/В3: либо В2+В3 одним
коммитом, если полигон покажет ложно-зелёные).

### В4 «вторые судьи + fork + подписи» — цели 5, 6, 7 + построитель
Снести: арбитр-цикл 3688-3918 (recursion no_arbiter, cand_src[0]:3861,
arbitrate, mute_measure_blocks:3885); signals_disagree 3149-3169;
writer_pair/doubt/arb_pool «судья» 3221-3333 (добавление соперника — только
если сразу clarify); stop2+_alias_verdict 3340-3376 + 2923-3076;
_checked alias 3407-3433, 3992-4009; wiki_arbiter_locked (запись :3247, гейты
:3458, :3626); fork-авто A/unique/B-лидер (z20 3451-3593 част., z13:286+,
fork_leader_class:79-134); prefer_mute_computed_over_clarify (z13:446-474);
collapse-equal (z21:820-900 + зов 980-983; до слова владельца — дефолт снос).
РАСШИРИТЬ ранний clarify A (K2 §2): вход при len(picked)>1 ИЛИ
len(arb_pool)>1; снять баны, опирающиеся на снесённые locks; diverge-меню
перенести на простой путь (mk_opts по picked ∪ rivals) — без потери класса
writer/stop2 соперников (K2: mk_opts смотрит picked — добавить туда rivals из
живого детектора, не из снесённых сигналов).
Сохранить: deadline_hit :3465 перед fork_detector_scan (K2 §5); детектор z09
+ fork_labels + fork_outcome_c (z13:728-767); wiki-tie (z21:978).
ПОСТРОИТЕЛЬ ПОДПИСЕЙ (единственная новая функция, контракт K4 §2.1):
форматтер 0 выбора: N вариантов на входе → N на выходе, порядок сохранён,
текст только из wiki-passport/card, БЕЗ LLM, без фильтрации/сортировки/подстановки
победителя. Замок N→N.
Замки: test_step4_guards/leader_hatch обновить; НОВЫЕ негативы: cand_src[0]
→answer нет; wiki_arbiter_locked нет; test_fork_outcomes A/mute→запрет, C
зелёный; замок подписей N→N + «options несут вики-описание при паспорте».
Контроль: два src с разными числами → меню; равные числа → меню (до слова
владельца); «организации» → число; ПОЛНЫЙ L67 после В4.

### В5 «период + intent» — цели 8, 9 (+apply_prior_period)
Снести: prefer_window_leader (z03:377-393), apply_period_leader (z03:581-611;
z20:1873-1903; связка ASK_SALES_RANK_CANON z03:595-598); day/currency prefer
(z04:186+, z04b:158+; зовы z20:1875-1885 — резать ЗОВЫ и defs);
assumed-exempt продаж (z20:1715-1743); period_form first-hit (z03:427-437);
apply_prior_period (z20:1777-1805, зов 1857-1858 — молчаливое наследование
окна; K4 R13); majority/_merge_intents (z01:889-890; z02:588-623, 669-673);
_intent_text first-of-list (z02:39-50, 430-434); canon_claims (z16:359-375);
period_is_canon_guess (z20:1688+) — как догадку, датчиком clarify не оставлять.
Поведение при нестабильном intent (вместо majority — K4 R12): расхождение
сэмплов по смыслу ≠ молчаливый победитель; конкретика — см. §7 вопрос 5
(до слова владельца: один проход, расхождение в parse.unstable без ветвления
kind/меры).
Остаются: period_preds/readings/render_window_label; assumed-clarify
(1972-1999); z02-слоты как техника (до §7 вопрос 3).
Замки: period_*/calendar/currency/fork_window — silent leader ожидания убрать;
test_intent критерий согласия переписать; НОВЫЙ негатив prior-period.
Контроль: полный L67 (после В5 — если В4 прошёл чисто, допустим срез period
9 + sales + L67×2 при подозрении флуктуации).

### В6 «z05 авто-формы» — цель 11
Снести: try_entity_form_answer (z20:3400-3405 ТОЧНО, ecp1 3394-3399 НЕ ТРОГАТЬ
— K2 §2); авто rolling_year (z05:521-531); entity_form_pick авто (z05:1135);
флаг ASK_ENTITY_FORM + boot ef_gate (:239-245) и ecp-reorder (:247-298 —
станет no-op, удалить); сцепки с z11-остатками (z05:879+).
Остаются: try_event_count_period_clarify (ecp1 3394-3399, ecp2 4355-4358);
event_kind_catalog_expand_pool (техника — под замером §7 вопрос 6).
Замки: test_entity_form авто-answer кейсы → удалить, period-clarify зелёный.

### В7 «хвост + зачистка» — цели 15, 16
Снести (по маркерам, ядро 4611-5362 НЕ резать по номеру — сохранить
aggregate/rows_of/compose/gate/build_answer_passport/ensure_answer_passport):
by_period_fill (2181-2184); by_vector fallback (2159-2180); not_for/
without_value/money-фильтры (2263-2637 из маршрута; замер clarify-rate);
ask_back (5253-5281); merge_compare_term_groups (2001-2012);
warehouse_clarify (1746-1760); терминалы канонов в хвосте; инертные функции
(M2 список); orphan z12 (prefer_entity_for_stock, filter_stock_*,
balance_bridge_clarify + их тесты-сироты); мёртвые OR
catalog/stock/register_count_locked; _bootstrap: _net_anchor/_net2 (:101-143)
— ДО любого рефактора no_axis_member (K2 §1), затем урезка всей
_patch_z20_wiki_primary до пустой/wiki-only; coverage (1931-1941) — по слову
владельца (§7 вопрос 2), до ответа не трогать.
Замки: zone_names; test_gate/compose/passport; load_all-замок; НОВЫЕ
негативы by_period_fill/by_vector/warehouse_clarify. Полный L67 — главный
замер пакета.

## 3. Порядок и параллельность
Строго: В1→В2→В3→В4→В5→В6→В7. В2 без В3 рядом — только с инверсией
measure-замков или одним коммитом (K1 §5.8). Параллельно — только замки
следующей волны и черновик построителя (без подключения).

## 4. Остатки R1-R20 (K4 §1.6) — что НЕ закрыто этим пакетом
z02-слоты смысла (R1) — суррогат ступени 2, живёт до отдельного пакета
вики-интерпретации (§7 вопрос 3); один verify-yes (R2, §7 в.4);
assumed-clarify (R4, ок с меню); expand_pool (R8, §7 в.6 — замер или снос);
порядок probe (R9); equal-tie (R11, §7 в.1); fork classes==1 (R10, ок как
«одно прочтение»); дедлайн (R19, периметр). Критерий «формула достигнута» =
чеклист K4 §5.5: путь ответа не содержит шагов вне 1-6, R-остатки = 0 или
помечены владельцем как периметр.

## 5. Ожидаемый итог
Тракт = 6 ступеней; z20 с 6225 → ~3.5-4k строк; судья один (вики-паспорт);
меню с вики-подписями при любом >1; wrong не растёт (Δwrong>0 = откат);
clarify растёт — норм; K6/stock-путь времени не тратят; п.0: хардкод z11/z10
имён уходит.

## 6. Вариант «тонкого тракта» (K4 §6) — отклонён оркестратором
K4 предложил выбор: A (снос волнами, этот план) vs B (выращивание нового
тонкого тракта z21+z17+z14+z18). Владелец ранее указал: «нельзя выдумывать
новые системы... ничего нового, только оптимизация» — выбран A (снос
существующего). B не строим; вопрос закрыт, упомянут для истории.

## 7. Открытые вопросы владельцу (СТОП-ТОЧКА — без них волны 2+ не стартуют)
1. Equal-tie (два прочтения, число одинаковое): строгое меню (дефолт В4)
   или «одно число, люк не открывать» (исход A старого плана)?
2. Coverage-вопросы («сколько таблиц в базе»): оставить как отдельный путь,
   вынести из тракта или снести?
3. z02-слоты (kind/want/период из LLM без вики): временный суррогат ступени 2
   — оставить до отдельного пакета «вики-интерпретация» или сносить сейчас
   (большой переворот preds)?
4. Один wiki-verify yes при pool>1 карточек: лидер без меню (дефолт, «одно
   прочтение по verify») или всегда меню при pool>1? K3: без ответа после В2
   действует diag-стоп «answer ∧ pool>1 ∧ без меню = откат».
5. Нестабильный intent (сэмплы разошлись): один проход + unstable без
   ветвления (дефолт В5) или меню разбора?
6. expand_pool (дописывает классы сущностей в пул) и rerank-порядок входов
   LLM: замерить влияние (не меняют ли leader/меню) и снести, если влияет —
   или снести сразу?
7. Подтверждение порядка волн В1-В7 и запуск В1 (обёртки) — прямо сейчас,
   без ожидания ответов на 1-6 (В1 от них не зависит)?
