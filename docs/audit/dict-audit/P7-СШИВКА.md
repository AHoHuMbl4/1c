# P7 — СШИВКА: финальный пакет промтов v2 генератора словаря (на утверждение владельцу)

Дата: 13.09.2026. Основание: армия P1–P6 (6 cursor-агентов) поверх аудита A/B (12
агентов). Пакет = 2 промта + фильтр + формат + 5 кодовых проводок. Ничего не исполнено —
проект. п.0 TARGET: канон промтов — английский каркас, язык ответа = язык title,
никаких слов okna/русских фикс-словарей в продукте (решение красной P6).

## 1. Промт инициализации (канон: P1 дословно + ужатие P5)

Текст: `P1-промт-init.md` §1 (дословно, одна строка в printf wiki_alias.sh:182/240/397).
Правки сшивки по P5/P6:
- каждый запрет сформулирован коротким императивом (P5 §0: 27B исполняет буквальное);
- добавлен антипример языка: «never copy the English example strings when the title
  language is different» (P6 §1.1 — единственный EN-скелет без антипримера — известный
  провал);
- тематические темы «не я» — только «topics plausible for THIS title» (не чеклист;
  P6: закрытый рус-ERP профиль в промте — запрещён);
- лимиты P4: aliases 3–8 / фразы 1–3 слова; BUF 2–4; NEF 3–8; без запятых/скобок.

## 2. Промт коллизионного круга (канон: P2 дословно)

Текст: `P2-промт-collision.md` §3 (в коде — wiki_alias.sh:312). Обязательные проводки
кода (иначе промт слеп):
- `<SHARED_WORD>` подставляется из раунда (сейчас слово в Input не попадает);
- в Input добавить текущие aliases сущностей (иначе MERGE-перезапись сносит роли
  первого прохода — B1);
- различители: 1–2 на сущность; общий слово не выкидывать (замер 25.08).

## 3. Стоп-фильтр мета-слов (канон: P3)

22 леммы ru+en (`P3-метафильтр.md` §2), критерий — класс объекта платформы 1С
(OData-виды), не предмет; «тмц/склад/номенклатура» НЕ фильтруются (предметные).
Точки: расширение filter_entity_aliases (wiki_alias_parse.py:87–109) + закрыть обход
в wiki_alias.sh:321–354 (inline-разбор круга мимо фильтра). Логировать отброшенное.
Расширение локалей (uk и др.) — при появлении базы (P6), не заранее.

## 4. Формат-контракт (канон: P4)

- Схема ответа: strict JSON Schema, minItems/maxItems по лимитам; добиваться
  response_format/guided у vLLM 27B (поддержка доказана 13.09); пока gateway не
  пробрасывает — схема в промте + пост-валидатор (фолбэк).
- Хранение: CSV-разделитель `,` → ` | ` (30 битых nef + 13 best чинит; `|` в элементах
  запрещён схемой); миграция живых 258 строк — snap + replace, эвристика запрещена (P6).
- Обрезки: parse _join 900 → 1600; паспорт 1500 не трогать; batch=20 оставить.
- Вызов: temperature=0 и seed (если примет) — провести через alias_infer_gateway
  (сейчас не передаются — A5); ретраи: 1 повтор пачки → salvage целых + mark_skip
  только битых.

## 5. Кодовые проводки (сопровождение промтов, не исполнено)

1. WORD + aliases в collision Input (SQL wiki_alias_collision_round).
2. Стоп-фильтр в двух точках (parse + inline круга) с логом.
3. ` | ` разделитель: _join/разбор + потребители (wiki_build str_split, hybrid,
   passport, collision ILIKE — список у P4) + миграция snap.
4. temperature=0/seed/response_format через alias_infer_gateway.
5. Force-перегенерация живых строк (WIKI_ALIAS_FORCE=1 юнитом) — иначе новый промт
   не обновит заполненное (MERGE пишет только в пустые — B1).

## 6. Песочница и приёмка (план, на слове владельца)

1. Провайдер vllm в OpenClaw окна → http://178.63.211.188:8000/v1 (локальный 27B
   владельца; OpenRouter не тратится).
2. Песочница: копия словаря (alias_okna_c5-механика), 1 пачка из 5 сущностей по
   критериям P5 §2 (самые коллизионные / самые пустые / обычная) → ручная сверка
   критериев промта.
3. Полный прогон песочницы force-перегенерацией; приёмка — метрикой A3/B2 на живом
   журнале вопросов: покрытие непокрытых слов ↑, каша (≥3 сущностей) ↓, мета-слова
   в aliases = 0. НЕ L67.
4. Стоп-точка владельца → перегенерация боевого словаря (прод не трогаем до слова).
