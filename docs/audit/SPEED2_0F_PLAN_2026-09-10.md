# ПЛАН «Speed-II-2 + 0f + сторож A» v1 (волна S×4 + аудит плана T×4; разруливания внесены)

Обновления v1 (по T1/T2/T3/T4): cols_sig + edm (из tmp3_cls0, не duckdb_columns);
пустой/неполный кэш ≡ полный пересчёт + STOP при all-hit∧nonempty-changed;
namecol-STOP только при text-кандидатах; Q1-гард duckdb_tables ВВОДИМ (канон
«таблицы нет → такт не падает»); Q2 force_full расширяем на tmp3_changed-без-rows
(форма T3); rebuild_mode — per-src (канон §0:53-56; v=0/1, note=mode|reason;
агрегат отклонён); перенос блока :936-972+:980-981 ЦЕЛИКОМ; wipe = WHERE NOT on_;
замки content_hash/emb-NULL вне tmp3_build в приёмке; writer_failed⊆scope;
ON_ERROR_STOP-политика (off у numhint/writer остаётся, p_stats/p_ref — on, STOP
после gexec); dual \set: regex + явное потребление + список env; маски
'build_phase:%'/'rebuild_mode:%'; приёмка НЕ атрибутирует кэш/вектора сторожу A;
фазовые метки по фактическому порядку после переноса (t0→numhint→p_writer→
p_stats→p_ref→done); секундное зерно — оговорка; md5 ПАРЫ build+merge — замок
выката. Черновики SQL — s2-plan-audit-t3.md (а)-(ж), с поправкой sig на edm.

Источники: s2-audit-s1/s2/s3/s4.md. Каноны: TAKT_SPEED2_PLAN §2, FULLB_PLAN
0f/§1.7, FULLB_1PAR3 пакет B/R6. Базлайн замков: measures 31/0, skip_canon
29/0, pdoc_tail 70/70. Зоны Speed-II-2 и 0f не пересекаются по строкам (S3).
Один выкат канона подтверждён (S3 §6); откат = пара build+merge целиком.

## ФАЙЛЫ: corpus_build.sql, corpus_merge.sql, corpus_init.sql (DDL кэша), 2-3 замка. Оси календаря/валют НЕ ТРОГАТЬ. DELETE/vec-budget merge НЕ ТРОГАТЬ (пакет B).

### P1. Рубильник (build)
- corpus_build.sql шапка: `\set partial_rebuild 0` — строка ИДЕНТИЧНА merge :6
  (один источник при двух psql-сессиях; env/quality-каналы запрещены).
- Замок равенства: оба файла содержат ровно одну `\set partial_rebuild N`,
  значения совпадают (парсинг), в unit EnvironmentFile нет PARTIAL_*/MERGE_*.
- `\if` в этом пакете НЕ вводим (S2: CASE достаточен; \if — пакету B/1a).

### P2. Speed-II-2: скоупы инфрафаз (corpus_build.sql)
1. **tmp3_infra_scope** (single-source, S4-Р3): CTAS сразу после tmp3_changed
   (:1025): `tbl` = предикат зеркалом p_ref :1272-1273: `NOT (SELECT on_ FROM
   tmp3_inc) OR tbl IN (SELECT tbl FROM tmp3_changed)`. p_stats EXECUTE и p_ref
   EXECUTE читают ТОЛЬКО её (заменить предикаты обоих на JOIN scope). Замок:
   множества равны (статически + живой прогон: число EXECUTE p_stats =
   EXECUTE p_ref = |scope ∩ Ref_Key|).
2. **p_writer** (:1114-1170): EXECUTE — JOIN tmp3_infra_scope; **NULL-wipe
   :1168-1170 → выполнять ТОЛЬКО при NOT (SELECT on_ FROM tmp3_inc)** (полный
   проход — как сейчас; на инкременте строки вне link НЕ ТРОГАЕМ; ушедший
   регистратор в дельте — MERGE выставит NULL на перечитанном :1147-1150).
   Замок: на такте on_=1 с непустым changed — written_by/written_by_share/
   written_by_all по регистрам ∉ scope бит-в-бит стабильны; фикстура «голый
   UPDATE … NOT EXISTS(tmp3_link) без on_-гарда → FAIL».
3. **p_stats** (:1220-1223): селектор EXECUTE — JOIN tmp3_infra_scope (PREPARE
   не менять). 🔴 ON_ERROR_STOP-политика: gexec p_stats/p_ref остаётся под on
   (как сейчас); numhint/writer — off как в файле. После gexec — namecol-STOP
   ТОЛЬКО ПРИ TEXT-КАНДИДАТАХ (T4-A8b): `EXISTS (scope∩Ref_Key∩(кандидат namecol
   в tmp3_cls по форме PREPARE p_stats) ∧ NOT EXISTS namecol)` → STOP; Ref_Key
   без text-кандидатов — легитимно пусто, не STOP (п.9/0).
4. **numhint → кэш** (S1 §4 + S4-Р1/Р2):
   - **Перенос блока numhint+финала tmp3_cls после :1025** (слот: после SELECT
     «по-изменившемуся», до MERGE search_tables :1042) — иначе скоуп невыразим.
   - DDL в corpus_init.sql: `CREATE TABLE IF NOT EXISTS search_cls_numhint
     (tbl VARCHAR, col VARCHAR, num_ratio DOUBLE, cols_sig VARCHAR, PRIMARY KEY
     (tbl,col))` + GRANT SELECT serene_ro; fail-closed: нет таблицы → полный
     пересчёт (идемпотентный CREATE IF NOT EXISTS в build дублирует).
   - **cols_sig = string_agg(col||':'||coalesce(data_type,'')||':'||coalesce(edm,''),
     ',' ORDER BY col) FROM tmp3_cls0 GROUP BY tbl** — edm ОБЯЗАТЕЛЕН (T1/T2:
     классификатор смотрит edm прежде типа; Decimal→String при том же
     data_type обязан инвалилировать). cls0 (:823-880) жив до DROP в слоте.
   - 🔴 ПУСТОЙ/НЕПОЛНЫЙ КЭШ ≡ ПОЛНЫЙ ПЕРЕСЧЁТ (T4-A1): todo включает tbl, если
     в кэше НЕТ строки с текущей sig (miss ≠ hit с ratio=0 — запрещено);
     первый такт после DDL: numhint_recomputed=|tbl|, numhint_cache_hit=0.
   - 🔴 Гейт: nonempty tmp3_changed ∧ numhint_recomputed=0 (все hit) →
     STOP error('numhint: changed непуст, кэш не пересчитан') — ловит
     «предикат не заметил changed» (T2).
   - Инвалидация/пересчёт (SQL такта): для tbl, где NOT on_ ∨ tbl∈changed ∨
     cols_sig IS DISTINCT FROM текущей сигнатуры → ПОЛНЫЙ пересчёт колонок tbl
     (fail-closed, без «оставить старое»); DELETE устаревших (tbl,col) при
     смене sig/уходе колонки; MERGE свежих ratio.
   - Потребитель: tmp3_cls с LEFT JOIN search_cls_numhint (вместо сессионной).
   - Quality: `numhint_recomputed` / `numhint_cache_hit` счётчики; 🔴 100% hit
     при непустом changed — ГЕЙТ STOP (см. выше), не «видимый дефект».
   - Замки: на неизменённых (¬changed ∧ sig совпал ∧ on_=1) kind бит-в-бит с
     эталоном полного прохода; смена data_type при тех же именах → инвалидация;
     grep: EXECUTE p_cls_numhint стоит ПОСЛЕ CREATE tmp3_changed.
   - RUNBOOK §10: откат пакета → `DROP TABLE IF EXISTS search_cls_numhint;`
     + один полный такт numhint (NOT on_).

### P2.4-доп: перенос блока (T3-а/T4-A2)
- Двигать ЦЕЛИКОМ :936-972 + :980-981 (numhint + финал tmp3_cls + DROP cls0 +
  SELECT «классификация»; комментарий 932-935 с блоком). НЕ двигать: tmp3_cls0
  (:823-880), tmp3_src, :974-978 (sources-missing). Слот: после SELECT
  «по-изменившемуся» :1025, до tmp3_names :1033. Порядок в слоте: build_phase:t0
  → tmp3_infra_scope → numhint(кэш)+cls+DROP → build_phase:numhint → SELECT
  «классификация». Замки: EXECUTE p_cls_numhint после CREATE tmp3_changed; нет
  второго CREATE tmp3_cls; DROP cls0 в слоте.
- MERGE search_tables (:1042) НЕ читает tmp3_cls — подтверждено (T3/T4).

### P2.2-доп: wipe = WHERE-гард (T3-в)
UPDATE search_tables SET … WHERE (… AND NOT EXISTS(tmp3_link)) AND
NOT (SELECT on_ FROM tmp3_inc) — без \if, без psql-веток. ON_ERROR_STOP off
вокруг gexec writer остаётся как в файле; writer_failed quality — по
tmp3_regsrc∩scope (замок failed⊆scope).
### P3. 0f: колонка mode (corpus_build.sql, тот же файл)
- CTAS tmp3_build (:1343-1354): `SELECT s.tbl, CASE WHEN :partial_rebuild = 0
  THEN 'full' WHEN <bridge_keys_ok> AND NOT <force_full> THEN 'partial' ELSE
  'full' END AS mode FROM … WHERE <членство БЕЗ ИЗМЕНЕНИЙ>` — ОДИН statement
  (P10), членство не трогаем, построчный мост не зовём (S2 §2.1); полный
  черновик WHERE — s2-plan-audit-t3.md (г), байт-в-байт по дизъюнктам, без
  LATERAL.
  - bridge_keys_ok = EXISTS(tmp3_key k WHERE k.entity=lower(s.tbl) AND
    len(k.key_cols)>0)
  - force_full = tbl∈tmp3_lag ∨ renamed>200 ∨ child-of-lag ∨ table_full
    (rows: key_text='*' AND op='full') ∨ **changed-без-rows** (tbl∈tmp3_changed
    AND NOT EXISTS rows — Q2-решение: шире HTTP-без-rows, форма T3; новый
    источник без маркеров не получит partial) — все предикаты lower()-согласованы
    (S2-Q3). 🔴 Q1-гард: ВСЕ обращения к search_changed_rows внутри CTAS —
    под EXISTS(duckdb_tables() WHERE table_name='search_changed_rows' AND
    database_name=current_database()); при отсутствии таблицы эти ветки ложны
    (мост/ключи могут дать partial при flip=1 — поэтому ДОПОЛНИТЕЛЬНО ветка
    `WHEN NOT EXISTS(duckdb_tables…rows) THEN 'full'` при flip≠0; канон §0:57-58
    «таблицы нет → все full, такт не падает»).
- L4(б) сразу после CTAS (форма T3-г).
- 🔴 Оговорка T4-A3: quarantine/membership packet-контура (витрина изменена,
  sources/rows не записаны из-за contract_tx_failed) — ВНЕ СКОПА mode/0f;
  force_full НЕ расширять «под quarantine»; «все mode=full» ≠ «витрина=корпус
  после quarantine» — дыра полноты членства живёт в packet-треке отдельно.
- **rebuild_mode per-src** (канон §0:53-56; агрегат ОТКЛОНЁН разруливанием):
  DELETE k LIKE 'rebuild_mode:%'; INSERT INTO search_quality
  k='rebuild_mode:'||tbl, v=CASE mode WHEN 'partial' THEN 1 ELSE 0 END,
  note=mode||'|'||reason (reason∈{row-keys, table_full, lag, entity-scope,
  entity-scope-interim при flip=0}) — черновик T3-ж; ~100-350 строк, роста нет
  (DELETE каждый такт); вставка сразу после L4(б).

### P4. Сторож A (corpus_merge.sql)
- После empty-entity :121, ДО дублей/unmatched/vec-budget (S2 §3.2):
  `SELECT CASE WHEN :partial_rebuild = 0 AND EXISTS (SELECT 1 FROM tmp3_build
  WHERE mode IS DISTINCT FROM 'full') THEN error('corpus_merge:
  partial_rebuild=0, но mode≠full (R6: interim обязан быть full)') END;`
- При flip=0+R6 — no-op; ловит рассинхрон выката build≠merge и регрессию CTAS.
- НЕ считать его защитой от introspection/кэша (S4-Р6) — в комментарии так и
  написать.

### P5. Фазовые метки (S3 §3 + T4-A8a)
- 6 вставок по ФАКТИЧЕСКОМУ порядку после переноса: t0 (после :1025, с DELETE
  LIKE 'build_phase:%' — один раз) → numhint → p_writer → p_stats → p_ref →
  done (конец файла). Форма T3-е (epoch(now())::BIGINT; v BIGINT — ок).
- Оговорка зерна: секунды; Δ=0 у коротких фаз ≠ регрессия; выводы Speed-II —
  по длинным фазам (numhint/p_stats/p_ref) и wall такта.
- Идемпотентность: DELETE только на t0.

### P6-доп (по T1/T2, внести в замки)
- hint-множество: при on_=1 tbl с пересчётом ⊇ tmp3_changed (вкл.
  new-empty-corpus); при on_=0 — все text-кандидаты cls0.
- 🔴 Фикстура edm-only (T2): смена ТОЛЬКО edm при тех же именах+data_type →
  сигнатура меняется → инвалидация → пересчёт tbl (замок рядом с
  data_type-фикстурой).
- grep-замок: между PREPARE p_stats и namecol-STOP нет голого
  `\set ON_ERROR_STOP off` без парного пост-чека (T2-3).
- writer_failed ⊆ scope (quality-счётчики по tmp3_regsrc∩scope).
- grep-запрет голого `NOT EXISTS(tmp3_link)`-wipe без `AND NOT (SELECT on_…`.
- Приёмка-замеры (T2): content_hash и emb-NULL вне tmp3_build бит-стабильны
  до/после такта; §3.113: любой сдвиг kind/hash на неизменённых = стоп выката,
  сверка с бэкапом (НЕ «досчитать GPU»).
- Приёмка НЕ атрибутирует кэш/вектор-защиту сторожу A (T2-4).

### P7-доп (T2-5/T4-6/T3)
- Замок dual \set: regex `^\\set partial_rebuild [01]\s*$` ровно один в каждом
  файле, значения равны; явное потребление `:partial_rebuild` ≥1 в build (CASE)
  и merge (сторож A); список env-файлов: /etc/1c-mcp-reports.env, 1c-embed.env,
  1c-serene-pipeline-*.env, 1c-serene-sync.env + git-шаблоны ubuntu/systemd/ —
  нет PARTIAL_*/MERGE_*.
- Выкат: md5 ПАРЫ (тройки с init) — полуоткат merge = тихая потеря A (T4-A6);
  замок выката обязан сверять оба файла.
- Откат: пара build+merge + DROP search_cls_numhint + один полный такт numhint.

### P6. Замки (итог)
- test_changed_rows_lock.py: (а) dual \set равенство build↔merge; (б) build
  CTAS содержит `AS mode` и ветку `'full'` при `:partial_rebuild = 0` (R6);
  (в) merge: сторож A (литералы `mode IS DISTINCT FROM 'full'`, `error`) стоит
  ДО `tmp3_merge_vec_budget`; (г) L4(б) после CTAS; (д) СТАРОЕ «\if
  :partial_rebuild нет» — снять (заменить формулировкой S2 §5.1).
- test_corpus_build_measures.py: обновить на кэш search_cls_numhint +
  cols_sig-типы + порядок numhint>changed + scope p_stats/p_ref равные.
- Новое (или расширение существующих): written_by∉scope стабилен; kind
  бит-в-бит на неизменённых; namecol-STOP; numhint hit/recompute.
- Прогон всех: measures, skip_canon, pdoc_tail, rows_lock, hash_kill, bridge,
  sources, key_form, vector_budget — до и после.

### P7. Приёмка/выкат
- Бэкап emb parquet ПЕРВЫМ (свято).
- Выкат АТОМАРНО: corpus_build.sql + corpus_merge.sql + corpus_init.sql (если
  DDL) → /opt/1c-mcp-reports, md5 бит-в-бит; стоп pipeline-таймера (packet не
  трогаем — SQL не пишет rows) → idle → scp → md5 → ручной такт → start.
- Замеры: (1) build_phase_sec до/после (M1/M2: SKIP ≤5 мин — метрики §0
  Скорости); (2) вектора count/NULL; (3) rehash_gate=0; (4) written_by∉scope
  стабилен; (5) kind стабилен вне changed; (6) mode: SELECT mode,count(*)
  GROUP BY 1 → только 'full'; (7) numhint_cache_hit/recomputed >0;
  (8) rebuild_mode-отчёт заполнен.
- Откат: пара build+merge целиком + DROP search_cls_numhint + полный такт.

### P8. Документы тем же коммитом
FULLB_PLAN 0f-текст: interim «до этапа 2» → «до flip=1 (R6)» (S2 чеклист);
CHANGELOG (числа до/после фаз); activeContext; граф MCP (S3 §4: search_cls_numhint
сущность+связи, build.sh→corpus_build.sql, observations Speed-II-2/0f/A);
techContext (ловушки: узкий writer, кэш-сигнатуры, dual \set); RUNBOOK §10
(откат с DROP кэша).

## Чего НЕ делаем
Оси в скоуп · персистентный namecol · голый NULL-wipe при узком link · \if до
1a · flip/`\set 1` · row-фильтр p_doc · ветвление DELETE/vec-budget (пакет B) ·
.cols_sig без типов · «оставить старое» при miss кэша · третий канал рубильника
· scp одного файла из пары · DROP кэша «забыть» при откате · env PARTIAL_*.
