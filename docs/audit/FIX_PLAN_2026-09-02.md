# План фиксов по аудиту TARGET — v2 (02.09, после армии аудита плана p1-p5)

Основание: docs/audit/TARGET_AUDIT_2026-09-02.md (К1-К10) + TD1-TD5 (5/5)
+ аудит плана p1-p5 (.claude/state/result-tp-p*.md). Изменения v2 против v1
помечены [v2]. План идёт армию на повторный аудит до сходимости, затем
исполнение, затем армия на дифф.

## Контракт плана (не нарушить)
- TARGET п.0/9/12/19/20/21 — без новых подстроек под базу/вопросы.
- Решение №10: простота; удаление вместо заплатки; один механизм.
- Решение №11: текущая схема (wiki-каскад, судья один) — лучшая; закрываем
  обходы и дыры коробки, схему не ослабляем.
- Решение №2: чиним НАЧАЛО цепочки (понимание/пул/выбор), не сторожей хвоста.
- п.10: каждое изменение поведения — полным прогоном 67 (L-прогон), не пробой.
- Вектора: правки corpus_merge (этап 6) — только после двух зелёных тактов;
  до того боевые такты живут с известным окном DELETE→xfer — interim-защита
  [v2, p4]: перед каждым ручным merge убедиться, что tmp3_merge_emb_xfer
  применена (count целей NULL = 0), при обрыве — применять живую карту руками
  из существующей таблицы, НЕ перезапуская merge.
- Классы 6/6b/7/8/9c — живые победы; их регресс = провал этапа. [v2, p2]
  Якоря сверты фиксируются на этапе 0 списком (см. 0.3).
- К8 — последний из ЭТАПОВ 1-6; этапы 7/8 — по слову владельца [v2, p5].

## Этап 0 — базовая линия (без правок кода)
0.1. Приёмка такта №1 по baseline → такт №2 → таймеры (идёт).
0.2. L20 текущим прибором (67×2, workers 16) — база «до».
0.3. [v2, p2] Якоревой чеклист сверки — явный список: ID/текст кейсов классов
     6/6b (wiki-clarify collapse), 7 (verify), 8 (post-verify меры), 9c (2402
     axis_not_carried), sales-q2 (эталон Всего/Period), «записей в книгапродаж»,
     «движений в регистре». Для sales-вопросов — срез diag: wiki_arbiter_locked,
     wiki_verify, prefer/cold-lift следы. Чеклист кладётся в
     .claude/state/l-anchor-checklist.md и живёт до конца этапов.
Критерий: числа «до» + чеклист сняты; без них этапы 1/3 не принимаются.

## Этап 1 — TD2: прибор перестаёт маскировать п.21
Файлы: work/gold/i2_runner.py, work/gold/reshoot-rules-okna.json,
work/gold/test_client_gold.py.
1.1. [v2, p3: один слой] Единая ветка в classify_verdict: kind∈honest при
     ЧИСЛОВОМ эталоне → вердикт refusal_with_data; при эталоне no_data —
     honest_no как раньше (служебные числа не сравниваются — причина раннего
     возврата сохранена). Один замок на правило. ВТОРОГО отчётного слоя не
     строим: refusal_with_data — обычный вердикт в существующей сводке.
1.2. no_stock: etalon_origin=declared (не live) у константного no_data; SQL
     остатков в правило НЕ вписываем (подгонка под okna) — бэклог п.15.
     Отдельный счётчик не заводим — фильтр по origin в сводке. [v2, p3]
1.3. Замки: clarify+число → refusal_with_data; no_data-kind+no_data-эталон →
     honest_no; declared → не live.
Критерий: замки зелёные; пересчёт answers.jsonl L20 старым/новым прибором:
     match/wrong НЕ меняются, разница только honest_no→refusal_with_data;
     отчёт владельцу «сколько honest_no — замаскированные отказы при данных».
     [v2, p2] Этап 3 не начинается, пока L-база не пересчитана этим прибором.
Риски: нет (прибор).

## Этап 2 — К2: коробка перестаёт быть глухой
Файлы: wiki_publish.sh, build.sh, wiki_card_build.sql, period_relative_forms.
2.1. wiki_publish.sh после wiki_build.sql выполняет wiki_card_build.sql
     fail-closed; [v2, p4] синхронно ужесточить проводку в build.sh:573 —
     ошибка вики-шага роняет шаг (не `|| echo`), после card_build —
     VACUUM (REFRESH_INDEX) wiki_entity_card_emb_idx.
2.2. [v2, p3+p4] Досчёт emb карточек — ОТДЕЛЬНЫЙ вызов embed_missing по
     образцу entity_card (build.sh:601-602), не «тот же вызов с корпусом».
     Сначала замер: если после 2.1 emb IS NULL = 0 на окне (карточки уже с
     векторами) — код не добавляем, только проводка вызова. tick-guard не
     опора (порог 100k ≫ 1.5k): контроль — «emb IS NULL = 0» в приёмке.
2.3. [v2, p3] Load УЖЕ есть в build.sh (~351-359). Правка = замена файла
     данных ubuntu/serenedb/period_relative_forms.json на полный словарь из
     work/acceptance; [v2, p1] из словаря ВЫКИНУТЬ доменный сленг
     («наотгружали» и подобное) — только нейтральные календарные формы;
     сленг живёт в alias базы, не в данных установки.
2.4. Замки: test_wiki_card_build; grep-замок «build.sh → wiki_publish →
     wiki_card_build» по образцу test_wiki_dump_engine.
Критерий: один такт на окне строит карточки; count(search_wiki_entity_card)>0
     и emb IS NULL → 0; [v2, p2] до/после — сверка пула/лидеров на якорях
     чеклиста 0.3 (не только count); «прошлый месяц» решается словарём
     (probe: origin≠llm).
Риски: карточки ~1.5k — дельта копеечная; merge карточек хранит emb (замок
     есть). [v2, p2] Этапы 2 и 3 НЕ выкатываются в один L-прогон.

## Этап 3 — TD1+К4: «один путь» держится кодом
Файлы: z20, z21, замки; приёмка по чеклисту 0.3.
3.1. [v2, p3: удаление, не гейт] ВЫРЕЗАТЬ post-lock вызовы: prefer_entity_
     for_sales/catalog_count/stock и sales_canon_force_pool ПОСЛЕ установки
     wiki_arbiter_locked — не `if locked`, а удаление строк (до-wiki prefer
     на cands ~z20:2227 остаётся входом пула). Мёртвые OR-замки
     (*_locked) в force_pool удалить вместе.
3.2. При замке: arb_pool СХОППНУТЬ до [locked] [v2, p2], круг арбитра не
     зовётся; code_ambiguous не дописывает держателей после замка.
3.3. Удалить sales_canon_locked-подстановку в z21:671-673 (обход каскада).
3.4. verify degraded: не молчаливый пик — clarify по пулу, иначе no_data
     с причиной degraded. [v2, p1] degraded-хвост = точка входа для 3.6.
3.5. [v2, p3] clarify<2 opts → no_data с причиной (БЕЗ одноопционного
     вопроса — меньше ветвлений, п.12 соблюдён).
3.6. [v2, p1: разделить] (а) warehouse_clarify — реально мёртв (0 вызовов):
     удалить сразу. (б) SIGNAL_DISAGREE-дописывание, writer_pair-добавление,
     _alias_clarify/REQUIRE_SUPPORT — ЖИВОЙ до-вики хвост (срабатывает при
     сбое пары wiki-признаков), НЕ мёртвый: удалять только потому, что 3.4
     делает их недостижимыми при wiki-fail → clarify/no_data; удаление
     доказывается полным L-прогоном с кейсами БЕЗ wiki_arbiter_locked [v2, p1].
Критерий: замки зелёные; [v2, p2] до выката — sales-срез чеклиста: доля
     вопросов, где wiki_verify ≠ src ответа и prefer его менял (если такая
     доля существенна — ЭСКАЛАЦИЯ ВЛАДЕЛЬЦУ до выката: universality vs цифры
     okna — решение владельца); L20 после этапа 2, пересчитанный прибором
     этапа 1: wrong не растёт, match не падает, якоря 6/6b/7/8/9c — ответы
     те же. При падении match — откат, доклад; чинить wiki/карточки (начало
     цепочки), НЕ возвратом prefer.
Риски: prefer иногда был единственным путём к регистру-эталону — см.
     эскалацию выше.

## Этап 4 — К1: коробка сама включает боевой путь
4.1. [v2, p1: критерий переформулирован] Дефолты → 1 ТОЛЬКО для флагов, у
     которых (а) в живом env окна =1 по замеру аудита К1 И (б) комбо входило
     в L19: ASK_ENTITY_FORM, ASK_SQL_RRF, ASK_RESOLVER_IVF,
     ASK_SOLR_SYNONYMS(+DICT), ASK_MEMORY_APPLY, ASK_SALES_RANK_CANON,
     ASK_ATOM_TERMINAL, ASK_HEALTH_NATIVE_FRESHNESS. На окне no-op (env=1
     поверх). [v2, p1] ASK_SLOT_COVER — дефолт НЕ трогаем (код сам: «не
     включать без замера доли ответов»; в env окна есть — остаётся env-фактом
     площадки). ASK_CALENDAR/CURRENCY_AXIS — не трогаем (оси не в бою).
     ASK_MONEY_UNIT/ASK_SCORER — никогда в дефолтах кода, только установка
     площадки. [v2, p1]
4.2. [v2, p4: отдельный мини-выкат ПОСЛЕ этапа 3] ASK_EMBED_NATIVE:
     (1) AB-доказательство: один текст двумя каналами — dim+cosine; сверка
     EMBED_MODEL/HOST/PATH с emb_model_* корпуса; сверка лидеров hybrid-пула
     на якорях чеклиста; решение по потере is_query (замер качества, не smoke);
     (2) стейджинг :8092 живой прогон; (3) отдельный L-прогон; (4) только
     после этого default=1 + [v2, p3] УДАЛЕНИЕ HTTP-ветки вопроса (не
     «вторичный канал»: два канала = два пространства, №10).
     Внимание [v2, p4]: в прод-env флага НЕТ — сегодня вопрос идёт HTTP;
     смена канала меняет прод, шаг не смешивать с 4.1.
4.3. firstbuild_unit.sh создаёт env-файл из шаблона при отсутствии (только
     установка: DSN/порт/токен; без поведенческих флагов).
4.4. Чистка мёртвого ASK_WIKI_CHOICE=1 из /etc (прод+стейджинг) — ops-строка
     вне критической цепи [v2, p3].
4.5. Замок «дефолты боевых флагов = 1» (grep по определения).
Критерий: замки; чистый стенд без env стартует с боевым путём (health+вопрос);
     L20 не меняется.
Риски: 4.2 — см. AB-доказательство; откат = env на окне до разбора.

## Этап 5 — TD4: live-fallback честен
5.1. aggregate_live_column: потерян ЛЮБОЙ входной предикат (doc_date без
     date-колонки; ts_/@@) → return None (отказ/уточнение), не широкая сумма.
5.2. [v2, p3: решено сейчас] match на live-fallback: НЕмовязка — при
     непустом match live не зовётся/возвращает None. Проброс match в live-SQL
     НЕ делаем (новый механизм против №10). Одно правило с 5.1: «любой
     потерянный предикат → не число».
Критерий: замок aggregate_live_flags + кейс «период без date-колонки →
     отказ»; [v2, p2] ДО фикса — замер: есть ли в answers.jsonl L20 кейсы
     via=live_column + wiki_clarify_collapsed=equal (если есть — это не
     «честный отказ вместо match», а поломка победы 6: чинить витрину/
     date-колонку, не только fallback; находка в доклад владельцу). L20 не
     хуже базы (с учётом ожидаемого роста refusal_with_data).
Риски: рост честных отказов (п.10 > п.21-молчание) — ожидаем и виден.

## Этап 6 — К8: вектор-окно (после 2 зелёных тактов)
6.1. [v2, p4: уточнено] Построение карты УЖЕ до DELETE (corpus_merge:511-561);
     дыра — ПРИМЕНЕНИЕ после. Правка: порядок MERGE → APPLY xfer-UPDATE →
     DELETE unmatched (образец — wiki_card_build.sql:90-103). Плюс страховка
     от повторного merge: НЕ пересоздавать (CREATE OR REPLACE) карту, пока
     есть неприменённые цели (pending-условие в шапке скрипта).
     Полная атомарность chunked-MERGE на 26.07.3 недоступна — безопасность =
     порядок + идемпотентный догон [v2, p4], не транзакция.
6.2. Замки: mid-abort тест «kill после DELETE → повтор восстанавливает emb из
     живой карты»; порядок gate→xfer→DELETE зафиксирован тестом (сейчас тесты
     порядок xfer↔DELETE НЕ фиксируют — p4).
6.3. EMBED_TRANSFER_STRICT=1 default в embed_bulk.sh; обход — существующий
     ручной ритуал, не штатная фича коробки.
     [v2, p5: К8-хвосты] Постчек такта печатает exact count новых emb-NULL
     (не только долю против 0.5%); MERGE_VECTOR_LOSS_BYPASS остаётся ручным
     люком — факт использования уже пишется в search_quality, добавить в
     постчек-отчёт строку «bypass использован: да/нет».
Критерий: замки зелёные; приёмка такта после выката — по md5/count emb
     baseline (не L20 первым сигналом) [v2, p2]; бэкапы векторов до выката.
Риски: крит-зона; отдельный коммит; contingency п. выше (interim-защита).

## Этап 7 — К3/К5: универсальность (по слову владельца)
Выжигание подстрок «реализац/номенклатур/книгапродаж/сторн...» (z11/z12/z13/
z10/z02/z16/z09/z04b) → wiki/alias/носители из базы. К5-якоря: MTD/WTD
молчаливый лидер (z03:373-393), sliding→календарная перепись (z03:297),
assumed не снимается (z02:557), sales-освобождение от assumed-clarify
(z20:1722), мера names[0]/реранк (z16:431/617), «продали=деньги» (z11:386),
«колич» (z12:671), stock_canon_src (z12:1065), readings[0] (z04:262),
дефолт-лидеры осей (z04:207/z04b:183). [v2, p5: якоря вписаны явно]
[v2, p3] TD5 микро-фикс переносится СЮДА (или в пакет этапа 3, если красная
команда сочтёт уместным): FX пишется только в amount_col-меру — 3-5 строк,
путь выключен, ловушка закрыта до будущего включения оси.

## Этап 8 — К7/К10: периметр и юниты (по слову владельца; перечень [v2, p5])
serene_report (схема→модель: вывод из контура или переписывание);
classify_entities (колонки→модель); psql-на-каждый-SQL в ask (z01:271-305);
z05:1226 diff в питоне; poc_load_entity diff в памяти; sync-ошибки не роняют
такт (pipeline.sh:58); plain-fallback затирает nums без метки (corpus_build:
2154+); solr census потерянных алиасов; coverage LIMIT 20; backup restore
(гарантированный start при сбое tar); firstbuild проверяет итог такта;
drop-in’ы `-/|| true` (embed-secrets, wal); packet_kit дефолт URL
1c-gate.timpul.ru → обязательный параметр; чистка старых юнитов;
OnFailure-алерты; автопрогонщик приёмки в установке (п.15) + эталоны не из
search_corpus (самосверка).

## Порядок и стоп
1 ∥ 2 (параллельно, прибор≠продукт [v2, p3]) → 3 → 4 (4.2 после 3) → 5 → 6 →
7/8 по слову владельца. Каждый этап: исполнитель → красная команда на дифф →
выкат → проба → L-прогон (меняет поведение) → коммит+док. Этапы 2 и 3 — в
разные L-прогоны [v2, p2].
СТОП: рост wrong / падение match → откат + доклад владельцу. Этап 6: стоп по
md5/emb-baseline; бэкапы до выката обязательны.

## Разметка отвергнутых находок [v2, p5]
- К4 post-verify (z21:1067-1096 «роняет верифицированного лидера») —
  ОТВЕРГНУТО осознанно: это рабочий судья классов 8/9c (мера/ось обязаны
  нестись лидером); аудитор видел отказ, контракт видит паспортную полноту.
- К9 SQL остатков в reshoot — отвергнуто (подгонка под okna), бэклог п.15.

## Что НЕ делаем (не предлагать снова)
- Менять TARGET.md; ослаблять пункты; новые сторожа-судьи; новый «выбиратель»;
  меню сумм; возврат prefer-канонов как починку регресса; HTTP-дубль эмбеда
  вопроса после 4.2; ASK_SLOT_COVER в дефолтах без замера; включение
  валютной оси до TD5-фикса.
