# План фиксов по аудиту TARGET — v3 (02.09, после p1-p5 + контрольного круга c1-c3)

Основание: TARGET_AUDIT_2026-09-02 + TD1-TD5 + аудит плана p1-p5 + контроль
v2 (c1: армия закрыта ДА; c2: 6 противоречий; c3: 7 правок готовности).
Правки v3 помечены [v3]. Статус: контрольный круг v3 — финальный перед
исполнением.

## Контракт плана
- TARGET п.0/9/12/19/20/21; решения №2/№10/№11; п.10 — полный L-прогон.
- Классы 6/6b/7/8/9c — живые победы; их регресс = провал этапа.
- Вектора: правки corpus_merge (этап 6) — после двух зелёных тактов.
  [v3, c2] Interim-защита, честный scope: дыра DELETE→xfer на АВТО-тактах
  остаётся до этапа 6 (принятый долг, виден здесь); для РУЧНОГО merge —
  правило: применить живую карту, merge заново не перезапускать. Предикат
  проверки перед ручным merge: `SELECT count(*) FROM search_corpus c JOIN
  tmp3_merge_emb_xfer x USING (src_table, row_key) WHERE c.emb IS NULL` = 0;
  ручное применение: `UPDATE search_corpus SET emb = x.emb FROM
  tmp3_merge_emb_xfer x WHERE search_corpus.src_table = x.src_table AND
  search_corpus.row_key = x.row_key AND search_corpus.emb IS NULL` (шаблон из
  текущего \gexec-блока corpus_merge:900-915).
- К8 — последний из этапов 1-6; 7/8 — по слову владельца.

## Этап 0 — базовая линия
0.1. Приёмка такта №1 → такт №2 → таймеры (идёт).
0.2. L20 текущим прибором: зафиксировать M0/W0/H0/R0 (match/wrong/honest_no/
     unresolved) + путь answers.jsonl. Канон команды — activeContext §МЕХАНИКА
     (i2_runner run, workers 16; TSV = ubuntu/serenedb/client-gold-okna.tsv,
     на выкате /opt/1c-mcp-reports/work/gold/; выхлоп /tmp/i2-l20).
0.3. [v3, c3] Якорный чеклист .claude/state/l-anchor-checklist.md — условие
     старта этапов 2 и 3. Поля по строке: id | text | class | expected_kind |
     expected_src | expected_answer (из baseline answers.jsonl) | diag-поля
     (wiki_arbiter_locked, wiki_verify, wiki_clarify_collapsed, prefer/cold-
     traces, axis_not_carried, via). Состав: кейсы классов 6/6b, 7, 8, 9c
     (2402), sales-q2, «записей в книгапродаж», «движений в регистре»,
     period_empty («продаж ноль»), salvage→leader (класс 7 без degraded),
     [v3, c1] 1-2 period-кейса («прошлый месяц»). Заполненный файл —
      предусловие этапов 2 и 3.
Критерий: M0/W0/H0/R0 + заполненный чеклист.

## Этап 1 — TD2: прибор (одним прогоном; L = пересчёт jsonl)
Файлы [v3, f2]: work/gold/i2_runner.py, work/gold/reshoot-rules-okna.json,
work/gold/test_client_gold.py. Слой refusal_with_data: константа VERDICT_*
+ включение в _ALL_VERDICTS + ветка classify_verdict + строка сводки
summarize/format_report (один слой, без второго отчётного языка).
1.1. classify_verdict (i2_runner.py, ветка ~234-239): kind∈{clarify,no_data,
     unavailable} при ЧИСЛОВОМ эталоне → refusal_with_data; при эталоне
     no_data — honest_no. Один слой, без второго отчётного языка.
1.2. no_stock (правило в reshoot-rules-okna.json): etalon_origin=declared;
     SQL остатков остаётся в бэклоге п.15 (подгонка под okna).
1.3. Замки test_client_gold: clarify+число→refusal_with_data; no_data-kind+
     no_data-эталон→honest_no; declared≠live.
Критерий: замки; тот же answers.jsonl двумя приборами: Δmatch=0, Δwrong=0,
     разница только honest_no→refusal_with_data; отчёт владельцу числом.
Риски: нет.

## Этап 2 — К2: коробка не глухая. [v3, c3: 2A/2B раздельные L]
2A (дифф+L): wiki_card в такт.
  2A.1. wiki_publish.sh: после wiki_build.sql → wiki_card_build.sql
       fail-closed; build.sh:573 — вики-шаг роняет такт (вместо `|| echo`).
  2A.2. Порядок [v3, c2; v3-финал f2: место правки]: SQL wiki_card_build —
       внутри wiki_publish.sh (2A.1); embed_missing и VACUUM — в build.sh
       СРАЗУ ПОСЛЕ `./wiki_publish.sh`, по образцу entity_card (build.sh:
       601-604). embed_missing — при emb IS NULL > 0 (первый шаг — замер:
       один count SQL, результат+timestamp в отчёт этапа) → после embed —
       VACUUM (REFRESH_INDEX) wiki_entity_card_emb_idx (имя индекса сверить
       grep'ом по wiki_card_build.sql). tick-guard не опора (порог 100k);
       контроль — «emb IS NULL = 0».
  Критерий 2A: count>0, NULL=0; сверка лидеров/пула на ВСЁМ чеклисте 0.3
       (не только 6/9c) [v3, c3]; L20: match≥M0, wrong≤W0.
2B (дифф+L): период-словарь.
  2B.1. Замена файла ubuntu/serenedb/period_relative_forms.json полным из
       work/acceptance; [v3, c3] фильтр: только нейтральные календарные
       ключи (prev_*/this_*/last_*; week/month/day/year/квартал/сегодня/
       вчера/позавчера); сленг («наотгружали» и доменные слова) — выкинуть,
       его место в alias базы. Load уже в build.sh:351-359 — нового шага нет.
  Критерий 2B: probe «прошлый месяц» origin≠llm; L20: match≥M0, wrong≤W0.
2A и 2B — разные L-прогоны.

## Этап 3 — TD1+К4: один путь. [v3, c3/c2: 3A/3B/3C]
Файлы [v3, f1]: ubuntu/serenedb/ask/z20_ask_main_http.py,
ubuntu/serenedb/ask/z21_wiki_choice.py (строки ниже — в этих файлах).
3A (дифф+L): вырезание обходов.
  3A.1. Вырезать post-lock вызовы (удаление строк, ветка if — не вариант):
       prefer_entity_for_sales/catalog_count/stock + sales_canon_force_pool
       после установки замка — call-sites: z20:3293-3300 и повторный
       force_pool z20:3400-3403 (сверить при чтении) [v3, c2]; до-wiki
       prefer z20:2227 остаётся. Ориентир [v3, c2-ловушка]: z20:3406-3428 —
       ASK_ENTITY_FORM-ветка, не prefer — вслепую не трогать, см. 3A.4.
  3A.2. При замке: arb_pool=[locked], doubt=False, арбитр не зовётся;
       code_ambiguous не дописывает после замка.
  3A.3. Удалить sales_canon_locked-подстановку z21:671-673.
  3A.4. [v3, c2] z20:3406-3428 (entity_form early-return после зоны замка):
       если чтение подтверждает подмену src при wiki_arbiter_locked — гейт
       замком в той же правке «один путь»; если меняет только форму/меру
       внутри лидера — не трогать, отметить в отчёте.
  3A.5. Удалить warehouse_clarify (0 вызовов, мёртв).
  Критерий 3A [v3, c3: без «доли существенна»]: до выката — sales-срез
       чеклиста: каждый кейс, где wiki_verify ≠ src ответа и prefer/cold менял
       голову пула → СТОП-эскалация владельцу до выката (universality vs
       цифры — решение владельца); ноль таких — выкат. L20 (после 2, прибор
       этапа 1): match≥M0', wrong≤W0'; якоря чеклиста — bit-identical
       (kind/src/answer); при падении — откат + чинить wiki/карточки; путь
       возврата prefer закрыт (см. «Что НЕ делаем»).
3B (дифф+L): честность degraded/clarify.
  3B.1. z21 outcome==degraded (~561/568/621/946-962 — сверить при чтении):
       вместо молчаливого пика — clarify при len(pool)≥2, иначе no_data
       reason=verify_degraded.
  3B.2. clarify<2 opts → no_data с причиной (одноопционный вопрос не строим).
  Критерий 3B: замки; якоря; допустим рост clarify/refusal_with_data;
       wrong ≤ W0'.
3C (после 3B, отдельный L [v3, c2/c3]): удаление живого до-вики хвоста —
       SIGNAL_DISAGREE-дописывание (z20:3170+), writer_pair-добавление,
       _alias_clarify/REQUIRE_SUPPORT. Условие: L-прогон 3B с кейсами без
       wiki_arbiter_locked, доказавший уход в clarify/no_data; grep
       отсутствия call-site; дифф отделён от 3A. Если кейсов без lock в
       чеклисте нет — 3C ждёт их появления (удаление «на веру» закрыто).

## Этап 4 — К1: коробка включает боевой путь. [v3, c3: 4A/4B/4C]
4A (дифф, на окне bit-identical): дефолты.
  4A.1. Таблица [v3, c3]: флаг → текущий default → целевой: ASK_ENTITY_FORM
       0→1, ASK_SQL_RRF 0→1, ASK_RESOLVER_IVF 0→1, ASK_SOLR_SYNONYMS 0→1
       (+DICT), ASK_MEMORY_APPLY 0→1, ASK_SALES_RANK_CANON 0→1,
       ASK_ATOM_TERMINAL 0→1, ASK_HEALTH_NATIVE_FRESHNESS 0→1. Условие
       включения: env окна =1 по замеру К1 и комбо в L19. На окне — no-op.
       Вне дефолтов: ASK_SLOT_COVER (нет замера доли ответов), оси
       CALENDAR/CURRENCY (не в бою), ASK_MONEY_UNIT/ASK_SCORER (площадка).
       [v3, c1/c2] Оговорка: ENTITY_FORM/SALES_RANK_CANON — мера/форма
       внутри wiki-лидера; если L этапа 3A показывает подмену src при lock
       (в т.ч. через z20:3406-3428) — флаг уходит из дефолтов в бэклог
       этапа 7.
  4A.2. firstbuild_unit.sh: env из шаблона при отсутствии (DSN/порт/токен;
       без поведенческих флагов).
  4A.3. Замок [v3, c2]: allow-list ровно по списку 4A.1; ASK_EMBED_NATIVE —
       отдельная строка замка, активируется вместе с закрытием 4B; флаги
       вне дефолтов (SLOT_COVER, оси, MONEY_UNIT, SCORER) — grep-замок
       «default остаётся "0"».
  Критерий 4A: замки; на окне — якоря чеклиста bit-identical к базе 3;
       чистый стенд без env — health + 1 вопрос боевым путём. [v3, c2:
       формулировка «L20 не меняется» на весь этап 4 снята — у 4B свой L]
4B (полностью отдельный мини-выкат после 3): ASK_EMBED_NATIVE.
  AB до выката: dim равен; cosine одного текста двумя каналами ≥ порога
  (число вписать после замера, иначе выката нет); EMBED_MODEL/HOST/PATH =
  emb_model_* корпуса; лидеры hybrid-пула на всём чеклисте не меняются;
  is_query — замер качества, не smoke. Стейджинг :8092 живой прогон; свой
  L-прогон; затем default=1 + удаление HTTP-ветки вопроса. В прод-env флага
  нет — сегодня вопрос идёт HTTP, смена канала меняет прод.
4C (ops, вне критической цепи): убрать мёртвый ASK_WIKI_CHOICE=1 из /etc
  (прод+стейджинг), с замером «ответы не изменились».

## Этап 5 — TD4: live-fallback честен
5.1. z17 aggregate_live_column: потеря любого входного предиката (doc_date
     без date-колонки; ts_/@@) или непустого match при fallback → return
     None. Проброс match в live закрыт; live_match_lost не заводится.
5.2. [v3, c3: гейт, не совет] Первый шаг — замер по answers.jsonl: кейсы
     via=live_column ∧ wiki_clarify_collapsed=equal. При >0 — СТОП и доклад
     владельцу (поломка победы 6: чинить витрину/date-колонку, не только
     fallback). При =0 — выкат.
Критерий: замок aggregate_live_flags + кейс «период без date-колонки →
     отказ»; L20: match≥M0'', wrong≤W0''; рост refusal_with_data допустим;
     отдельной строкой отчёта — число answer→clarify на via=live_column.

## Этап 6 — К8: вектор-окно (после 2 зелёных тактов)
6.1. Порядок MERGE → APPLY xfer-UPDATE → DELETE unmatched (образец
     wiki_card_build.sql:90-103); pending-страховка: карта не пересоздаётся
     (CREATE OR REPLACE), пока есть неприменённые цели (условие в шапке).
     Атомарность chunked-MERGE на 26.07.3 недоступна — safety = порядок +
     идемпотентный догон.
6.2. Замки: mid-abort «kill после DELETE → повтор восстанавливает emb из
     живой карты»; порядок gate→xfer→DELETE тестом (сейчас не фиксируется).
6.3. EMBED_TRANSFER_STRICT=1 default; постчек: exact count новых emb-NULL +
     строка «bypass использован: да/нет».
     [v3, c1/c5: открытый вопрос владельцу] допуск 0.5% и люк
     MERGE_VECTOR_LOSS_BYPASS остаются как есть (ручной люк с отчётом);
     ужесточение tol / снятие люка — отдельное решение владельца, вне
     этого пакета.
Критерий: замки; приёмка по md5/count emb baseline (L20 — вторичный сигнал);
     бэкапы до выката — часть приёмки.

## Этап 7 — К3/К5: универсальность (по слову владельца)
Выжигание подстрок имён (z11:789/87/307/700-713, z13:9-18, z12:737-741/
989-1004/1065, z10:72-84/434-436, z02:390, z16:354, z09:15-18/180,
z04b:305-307) → wiki/alias/носители из базы. К5-якоря: z03:373-393 (MTD/
WTD), z03:297-302 (sliding→календарная), z02:557-561 (assumed не снят),
z20:1722-1723 (sales-освобождение от assumed-clarify), z16:431-435/617-634
(мера), z11:386-390 («продали=деньги»), z12:671+ («колич»), z04:262,
z04:207/z04b:183 (дефолт-лидеры). [v3, c2] TD5 FX-фикс (FX пишется только в
amount_col-меру) — только здесь; вариант «в пакет этапа 3» снят.

## Этап 8 — К7/К10: периметр (по слову владельца; адреса вписаны в v2,
нумерация подшагов — при старте этапа)

## Порядок и стоп
0 → 1 ∥ 2A → 2B → 3A → 3B → [3C] → 4A → 4B → 4C → 5 → 6 → 7/8 (владелец).
Каждый дифф: исполнитель → красная команда на дифф → выкат → проба → L-прогон
→ коммит+док. [v3, c3] Критерии формулой: match≥M*, wrong≤W*, где M*/W* —
база предыдущего принятого этапа (после 0.2 пересчитана прибором этапа 1);
словесные «не хуже» не используются. СТОП: Δwrong>0 или Δmatch<0 →
откат+доклад. Этап 6: стоп по md5/emb-baseline.

## Разметка отвергнутых находок
- К4 post-verify (z21:1067-1096) — отвергнуто осознанно: рабочий судья
  классов 8/9c.
- К9 SQL остатков в reshoot — отвергнуто (подгонка), бэклог п.15.
- p3-радикал «выпилить флаги целиком» и параллель 4∥2 — не приняты
  (выбор изоляции регрессий).

## Что НЕ делаем
Менять TARGET.md; ослаблять пункты; новые судьи; новый выбиратель; меню
сумм; возврат prefer как починку; HTTP-дубль эмбеда после 4B; SLOT_COVER в
дефолтах; валютную ось до TD5; TD5 вне этапа 7.
