# Контроль K2: P2 → план v2 §1.C

**Режим:** только чтение (+ этот отчёт).  
**Дата:** 11.09.2026.  
**Вердикты:** `docs/audit/speedask-plan-p2.md`.  
**Объект:** `docs/audit/SPEED_ASK_PLAN_2026-09-11.md` §1.C (+ шапка цели, §4).

---

## Вердикт

**C v2 готов.** Все обязательные правки P2 в тексте §1.C отражены; блокеров нет.

---

## Сверка

| Проверка | Итог | Где в v2 |
|---|---|---|
| C1–C3 внедрять; форма C3 = ContextVar rid, ключ, запрет LRU | **да** | §1.C:66–70 |
| C4' = только при заведомом пути без ветвления `how='base'`, иначе не внедрять | **да** | §1.C:71–74 |
| C5' = мёртвый запас z20:4399–4402 вместо «дубля осей» z17↔z05 | **да** | §1.C:75–77 |
| Обещание ≤5 → замер, ориентир ≤10, без cap | **да** | шапка:11–12; §4:110 |

### C1–C3 (форма)

- **C1:** skip entity-rerank в подвызовах арбитра при `focus`/`no_arbiter` — совпадает с P2 (скоуп `focus`∧`no_arbiter`; внешний B не трогать). Slash в контексте «подвызовы арбитра» читается как конъюнкция.
- **C2:** short-circuit N≤1 сразу после z07:355 — дословно P2.
- **C3:** кэш в пределах rid через ContextVar-словарь; ключ `(model, query, tuple(docs))`; «Глобальный LRU — ЗАПРЕЩЁН» — форма P2 соблюдена (model ≡ RERANK_MODEL/API из P2).

### C4'

Переформулирован: не слепой skip перед refuse, а пропуск HTTP **только** когда путь answer/refuse заведомо без ветвления `how='base'`; иначе **НЕ внедрять**. Совпадает с запрошенной C4' и с P2 («иначе отложить»).

### C5'

Ложная пара z17:489 + z05:684/694 снята (определение + callers, не дубль). Цель — мёртвый запас `z20:4399-4402` + эффект C3. Совпадает с P2 / S1-P5.

### Обещание счётчика

Жёсткое ≤5 снято. В шапке: эффект замером, ориентир ≤10 HTTP на тяжёлом, **БЕЗ cap**; §4 явно «Не cap по счётчику».

---

## Замечание (не блокер)

§2 даёт порядок `C1→C2→C3→C4(если доказан)→C5'`; P2 предлагал `C2→C3→C1→C5'→C4'`. На готовность текста §1.C не влияет (C4 и так «если доказан»).

---

## Итог одной строкой

C v2 готов к стоп-точке / внедрению по тексту плана.
