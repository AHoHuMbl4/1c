# mol-check-c — контроль N3 → MOL_PLAN v2

Дата: 11.09.2026. Только чтение. Источник правок: `mol-plan-n3.md`.
Объект: `MOL_PLAN_2026-09-11.md` (v2).

**Вердикт:** к стоп-точке **готов** (дыры N3 по §4 закрыты; хвост — косметика).

## Сверка правок N3 → v2

| Правка N3 | Где в v2 | Внесение |
|---|---|---|
| Порядок хвостов: 4b→4a→4d→4c(после §3)→стоп→4e | §4 заголовок; §6 шаг 3–4 | **дословно** |
| 4a: не «на A/B», а reuse `_fork_early` при совместимом пуле; риск B8-01 | §4 4a: совместимый пул, z20:2702/3508, «не на A/B», B8-01 в замок | **дословно** |
| 4b: ключ `(rid,src)`; без процессного TTL; наследование в арбитр | §4 4b: ключ (rid, src) | **частично**: ключ есть; TTL/arbiter-dict **нет** |
| 4c: потребитель витрины п.3, не клон индекса; позиция после §3 vs M4 stock-skip | §4 4c: n_with_nums/src_stats; «не клон»; M4 «до K6» снят; E6 до решения | **дословно** (ядро); запасные pred-кэш/skip sales_sum **нет** |
| 4d: multi-window SQL; cells≡; после 4a | §4 4d + порядок; риск cells/meta→замок | **частично**: форма+порядок да; явная приёмка cells≡ **нет** |
| 4e last/optional + конкретика M3 (thread-local, CSV↔fetchall, venv, 2 HTTP) | §4 4e: last/опция; thread-local; CSV↔fetchall; thread-safety | **частично**: ядро да; venv, 2 HTTP, решать после `psql_n` **нет** |
| Бюджет W4: таблица −сек → остаток; A2≠K6 на одном Q | §0 классы + таблица; цели ≤20/≤40 в шапке | **частично**: анти-двойной счёт+свод есть; фазовый Σ~27–35 (wiki/arbiter) N3 **нет**; цифры 4c/4b чуть сжаты vs N3 |

## К стоп-точке

**Готов.** Четыре блокера N3 §8 «не готов без…»: (1) правка 4a — да;
(2) 4c vs индекс/витрина — да; (3) таблица −сек/W4 — да в §0; (4) 4e M3 —
достаточно для решения «last/optional», детали — в бой шага 4.

Перед показом владельцу (не блокер): одна строка в 4b про TTL/арбитр;
в 4e — «после замера psql_n».

Внедрение не начинать до «да» владельца (§6).

*Конец mol-check-c.*
