# Ручные действия смены 08.09 → универсальные решения (п.0)

Журнал ведётся по указанию владельца 08.09: «такт до конца, но записывай всё, что
делаешь, чтобы в итоге сделать из него универсальное решение». Каждая строка —
ручное действие этой смены, которого установка «из коробки» не делает сама, и то,
каким КОДОВЫМ решением оно закрывается. Закрытые строки переносятся в CHANGELOG
коммитом своего фикса.

| # | Ручное действие | Когда | Почему понадобилось | Универсальное решение (код) | Статус |
|---|---|---|---|---|---|
| 1 | `SET GLOBAL memory_limit='102GB'` (30 GiB → 94.9 GiB) | 08.09 05:27, повторно 10:15 | merge-сессии 0.55×30=16.5 GiB; спилл не в этом (см. №2), но лимит занижен против канона box_tune (80% RAM) | ЧАСТИЧНО ЗАКРЫТО КОДОМ (38652a1): ловушка «SET memory_limit только GLOBAL» — build.sh cleanup восстанавливает ML_BEFORE при любом исходе; ОСТАЁТСЯ: почему между тактами стояло 30 GiB (не из serened.conf) — выяснить канон установки box_tune steady на этой коробке | частично закрыто |
| 2 | Пробы `EXPLAIN`/SELECT по частям p_merge_unmatched руками | 05:1x-06:3x | поиск декартова в шестом анти-соединении | ЗАКРЫТО КОДОМ: Period-диспетчер + гейт '\|' (b7396fd, армия 4/4); диагностика — не эксплуатация, в коробке не нужна | закрыто |
| 3 | Стоп юнита после каждого падения merge (`systemctl stop`, сброс `reset-failed`) | 4 раза за смену | `Restart=on-failure` + `RestartSec=300` дают карусель полных пересборок по 12 мин после каждого падения | юнит: `Restart=no`/`RestartSec` больше времени разбора, или `StartLimitBurst`; иначе коробка после первого падения такта жжёт циклы сама | открыто |
| 4 | Сброс `tmp3_pdoc_progress/stage/tail/tail_done` руками (`DELETE FROM` × 4) | 05:04 | resume-механика («run старше 6 ч») после оборванного 04.09 прогона пошла «докаткой» поверх грязи и УДВОИЛА монолит (147188 = 72206+74982) | corpus_build: свежий такт после сбоя обязан сам определять грязный tmp3_corpus (например, маркер завершённости прогона в tmp3_run: неполный прогон → сброс resume-состояния); сейчас 6-часовое окно принимает обрыв за докатку | открыто (кандидат в «полную B»/FIX_PLAN) |
| 5 | `MERGE_VECTOR_LOSS_BYPASS=1` в `/etc/1c-serene-pipeline-postgres.env` (стоит сейчас) | 08:06 | гейт вектор-бюджета считает «потерей» пересчёт при смене канона чанкования (19283×2 строк, вернётся шагом 5 досчётом); 72887/1657724=4.4% > 0.5% → STOP | гейт обязан различать «умрёт навсегда» и «emb=NULL до досчёта шага 5» (классы у него уже есть — не суммировать возвратное); после правки флаг НЕ нужен ни здесь, ни на новой базе. Вопрос владельца «допуск+BYPASS» закрыть вместе с правкой | открыто (главный кандидат) |
| 6 | `SET GLOBAL force_dict_fsst_mode='AUTO_NATIVE'` (после каждого рестарта движка) | 04:39 и позднее | обход бага dict_fsst 26.08.1 (рекомендация разработчика SereneDB, фикс у них pending) | убрать, когда выйдет их фикс; до тех пор — включать в heuristik установки? нет: временный обход стороннего бага, живёт в INSTALL_LOG/RUNBOOK, удаляется их фиксом | открыто (внешнее) |
| 7 | Рестарт `serenedb` руками | 04:35 | чистое состояние после 4 дней нездорового аптайма (зависшие statement-ы 04.09) | штатный перезапуск юнита; коробке не нужен, разовое восстановление | закрыто (разовое) |
| 8 | Транзиент «failed to pin block (3.9/4.0 GiB)» на corpus_build:2530 — проглатывается, сборка завершается эталоном (1441514 три такта подряд) | 09:32 | внутренний буферный лимит движка; statement-класс не роняет такт | выяснить, какой лимит (буфер/pin), уместно ли поднять каноном; на новой базе тот же класс должен проходить так же | открыто (разбор с :710 вместе) |
| 9 | OOM 1.9/2.0 GiB на corpus_merge:710 (такт №10) | 09:42 | неизвестен пока statement/лимит (2 GiB — не memory_limit сессии 52 GiB) | разбор ниже в смене; решение обязано быть кодовым (структура запроса или штатный лимит каноном), не ручным SET | открыто (текущее) |

| 10 | ~~`EMBED_CHUNKS_PER_ROUND=100/32`, `EMBED_THREADS=1`~~ СНЯТЫ (выдумка) / **`SET GLOBAL http_timeout=600`** | 15:39-16:47 | таймауты шага 4: ai_embed-таймаут 120 с при дефолте http_timeout; раунды 6400/1600/512 строк висели 127 с | РАБОЧИЙ СПОСОБ УЖЕ ОПИСАН: embed_bulk.sh ставит `SET GLOBAL http_timeout=600` сам (HOWTO §5/§9); мои подгоны раундов/threads — лишние, сняты. Долг: шагам 3/4 такта (малые досчёты до embed_bulk) http_timeout поднимает сам такт — правка build.sh по слову владельца | открыто (правка build.sh) |
| 11 | Слежу за живостью .11 пробами (batch16) | многократно | embed_server.py v1.3 текёл (31→48.5 GiB, 25 OOM) — владелец пропатчил v1.4 (OOM-path, 17:14 его сессии) | утечка была на стороне .11 (EMBED_MAX_LENGTH=8192×BATCH=32+padding); наш контур векторизации не менялся (md5-сверка embed_missing.sh/.sql, embed_bulk.sh, embed_check.sh — репо==окно; resolver_build.sql на окне старый, мной не выкатывался) | закрыто (починено владельцем на .11) |

Правило на остаток смены: любое новое ручное действие добавляется сюда ДО
исполнения, с тем же набором колонок. П.0: прод-база скрыта нарочно — система не
имеет права работать потому, что я знаю окна или сделал руками.
