# W2-A3 — ЕДИНЫЙ РЕЕСТР СНОСА мёртвого груза ask/

Дата: 12.09.2026. Режим: агрегация `W1-z*.py.md` (22 зоны) + `W1-X1…X4`;
выборочная сверка строк файла с `ubuntu/serenedb/ask/z*.py`. Код не менялся.
Чужие `W2-*` не читались.

**Контракт сноса:** после flip `ASK_ONEPATH=1` (прод уже на новом z20) —
вырезать legacy-тракт и всё, что зовётся только им / ниоткуда.

**Единица «строк» в таблице §1:**
- `файл` — `len(splitlines())` исходника (сверка с X4 / диском: совпало 24/24);
- `N / L / M` — **строки тел** символов из итогов зонных карт (AST/span);
- `клей` = файл − (N+L+M) — шапка, пустоты, `register_zone`, комментарии между def.

---

## 0. Метод и расхождения

| Источник | Что взято |
|---|---|
| X4 | канон **файловых** строк; оценка выреза 7.7–9.6k |
| X1 / X2 | внешние символы new/legacy; 39 жёстких legacy-only; патч `_patch_z20_wiki_primary` |
| X3 | замки ↔ зоны; что удалить/править при сносе |
| W1-z* | вердикты N/L/M по символам + скрытые выбиратели |

**Сверка / нормализация форматов отчётов**

| Зона | Расхождение в W1 | Канон здесь |
|---|---|---|
| все `z*.py` | — | файл = диск = X4 |
| z04 | итог «строк символов», без колонки «символов» | N=217 L=22 M=14 (тело 253) |
| z05 / z19 | колонки «строк \| символов» в обратном порядке | N/L/M по **строкам** |
| z07 | «459 (19 символов)» в одной ячейке | N=459 L=1 M=37 (тело 497) |
| z08 | 136 строк тел vs файл 278 | N=124 L=5 M=7; клей большой (комменты) |
| z13 | итог в шапке, не в `## Итог` | N=21 L=744 M=74 (тело 839) |
| z14 / z15 / z22 | проценты / «строк (N символов)» | тела: 487/9/91; 346/1/47; 43/0/0 |
| z21 | — | N=984 L=21 M=0 (тело 1005) |
| X4 «17730−7671≈10k» | вычитает legacy из уже без-legacy набора | **исправлено:** A+B с диска 22833−7671=**15162** |

Ограничение (как у W1/X): косвенный `getattr`/строковый dispatch может
недосчитать живость; оценка сноса — нижняя граница кандидатов до замка
`test_one_path`.

---

## 1. Сводная таблица по зонам

| зона | файл | жива новому (N) | только-legacy (L) | мёртва (M) | клей | доля N от тел |
|---|---:|---:|---:|---:|---:|---:|
| z01_infra_trace_llm.py | 883 | 540 | 5 | 11 | 327 | 97% |
| z02_intent.py | 713 | 587 | 0 | 38 | 88 | 94% |
| z03_period_windows.py | 615 | 471 | 68 | 0 | 76 | 87% |
| z04_calendar_axis.py | 297 | 217 | 22 | 14 | 44 | 86% |
| z04b_currency_axis.py | 567 | 433 | 62 | 11 | 61 | 86% |
| z05_entity_form.py | 1239 | 506 | 517 | 108 | 108 | 45% |
| z06_entity_search.py | 604 | 348 | 146 | 65 | 45 | 62% |
| z07_rrf_vectors.py | 556 | 459 | 1 | 37 | 59 | 92% |
| z08_measures_totals.py | 278 | 124 | 5 | 7 | 142 | 91% |
| z09_fork_detector.py | 1018 | 20 | 902 | 8 | 88 | 2% |
| z10_rank.py | 471 | 246 | 0 | 176 | 49 | 58% |
| z11_sales.py | 389 | 190 | 116 | 36 | 47 | 56% |
| z12_stock_balance.py | 1346 | 777 | 115 | 302 | 152 | 65% |
| z13_fork_outcomes.py | 925 | 21 | 744 | 74 | 86 | 3% |
| z14_clarify_memory.py | 680 | 487 | 9 | 91 | 93 | 83% |
| z15_answer_atoms.py | 437 | 346 | 1 | 47 | 43 | 88% |
| z16_veto_pick_entity.py | 625 | 35 | 321 | 203 | 66 | 6% |
| z17_aggregate_groups.py | 698 | 509 | 131 | 0 | 58 | 80% |
| z18_compose.py | 882 | 752 | 38 | 0 | 92 | 95% |
| z19_answer_check.py | 368 | 329 | 0 | 0 | 39 | 100% |
| **z20_ask_main_http.py** (новый) | **2953** | **≈2953** | 0 | 0 | — | 100%† |
| z20_ask_main_http_legacy.py | 5103 | 0 | **5103** | 0 | — | 0% |
| z21_wiki_choice.py | 1128 | 984 | 21 | 0 | 123 | 98% |
| z22_health_tick.py | 58 | 43 | 0 | 0 | 15 | 100% |
| **Σ зоны без обоих z20** | **14777** | **8424** | **3224** | **1228** | **1901** | — |
| **Σ диск (оба z20)** | **22833** | — | — | — | — | — |
| **Σ load ONEPATH** (без legacy) | **17730** | — | — | — | — | — |

† Новый z20: X4 — почти все тела достижимы (инфра B2 2077 + тракт B3–B4 862);
отдельной N/L/M-карты W1 по z20 нет — файл целиком «жив новому» как вход.

**Классы зон (по телам):**

| класс | зоны | смысл для сноса |
|---|---|---|
| Живые (>80% N) | z01–04b, z07–08, z15, z18–19, z21–22 | точечный вырез L/M |
| Смешанные | z05, z06, z10–12, z14, z17 | вырез L+M символами |
| Почти мёртвые | **z09, z13, z16** | кандидат на **файл целиком** (+ тонкая щель N) |
| Только-legacy файл | **z20_…_legacy.py** | снос целиком после flip |

---

## 2. ПОИМЁННЫЙ список сноса

### 2.1 Целиком (файлы / инфра)

| # | объект | строк | условие |
|---|---|---:|---|
| F1 | `ask/z20_ask_main_http_legacy.py` | **5103** | после flip; убрать из `_ZONE_FILES` / выбора `_Z20_FILE` |
| F2 | `_bootstrap._patch_z20_wiki_primary` (+ вызов в `_exec_zone` только для legacy) | ~120 в `_bootstrap.py` | умирает вместе с legacy; инжект `entity_form_gate_open`, `stock_asks_named_product` |
| F3 | `ask/z09_fork_detector.py` | **1018** | после выноса `fork_labels_of` (20 стр.) в z04b/helper |
| F4 | `ask/z13_fork_outcomes.py` | **925** | после выноса `atom_terminal_gate_text` (11) + `stock_balance_is_sales_noise` (10) |
| F5 | `ask/z16_veto_pick_entity.py` | **625** | после выноса `pair_slots_only` + `question_expects_accounting_data` (+ `_NON_DATA_MARKERS`) ≈35 стр. |

Σ файлов F1+F3–F5 = **7671** (= X4 conservative), при условии переноса тонких N-щелей.

### 2.2 Мёртвые / legacy-only символы смешанных зон

Строки — тела по зонным картам. Дубликаты строк отчётов отброшены.

#### z01 — L+M = 16

| символ | стр. | вердикт |
|---|---:|---|
| `_gate_bad_preview` | 5 | L |
| `TERMS_FOR`, `TERMS_TOP`, `ORDER_BY_MEANING`, `RERANK_TOP` | 1×4 | M |
| `_reload_embed_native_env` | 7 | M |

#### z02 — M = 38 (затёнённые определения)

| символ | вердикт |
|---|---|
| `_NON_DATA_MARKERS`, `question_expects_accounting_data` (тела z02) | M (перекрыты z16; после сноса z16 оставить **одну** реализацию — z02 или вынесенную) |

#### z03 — L = 68

| символ | стр. |
|---|---:|
| `render_window_label` | 19 |
| `_WINDOW_LEADER_FORMS` | 1 |
| `prefer_window_leader` | 17 |
| `apply_period_leader` | 31 |

#### z04 — L+M = 36

| символ | вердикт |
|---|---|
| `calendar_day_basis_prefer` | L |
| `prefer_day_basis_leader`, `_sql_ident` (затёрт z04b) | M |

#### z04b — L+M = 73

| символ | стр. | вердикт |
|---|---:|---|
| `currency_amount_basis_prefer` | 26 | L |
| `currency_sum_for_basis` | 8 | L |
| `currency_patch_fork_scan` | 20 | L |
| `_class_amount_basis` | 8 | L |
| `prefer_amount_basis_leader` | 11 | M |

#### z05 — L+M = 625 (крупнейшая смешанная корзина)

**L (entity-form / event-period терминал legacy):**  
`entity_form_count_target_is_movement`(36), `entity_form_expand_pool`(21),
`entity_form_rolling_year`(11), `entity_form_gate_open`(19),
`entity_form_applicable`(39), `entity_form_collapse_guard`(14),
`entity_form_pre_entity_ok`(15), `entity_form_atom_distinct`(11),
`entity_form_atom_complement`(17), `event_count_has_explicit_period`(11),
`event_count_period_unspecified`(6), `event_count_has_live_axis`(22),
`event_count_period_clarify_applies`(11), `event_count_period_option_readings`(21),
`event_count_period_clarify`(24), `try_event_count_period_clarify`(57),
`_event_distinct_fork_rows`(28), `event_filter_pool`(3),
`entity_form_axis_on_sales`(30), `entity_form_structs`(59),
`entity_form_compute`(30), `try_entity_form_answer`(32).

**M:** `register_count_src`(37), `event_kind_catalog_expand_pool`(41),
`event_duel_applies`(17), `event_movement_feats`(2), `entity_form_pick`(11).

#### z06 — L+M = 211

| символ | стр. | вердикт |
|---|---:|---|
| `children_by_parent` | 51 | L |
| `partial_tables` | 78 | L |
| `date_only_kind_filter` | 17 | L |
| `_fetch` | 13 | M |
| `entity_pick_counts_for_model` | 30 | M |
| `entity_matching_records_suffix` | 22 | M |

#### z07 — L+M = 38

| символ | стр. | вердикт |
|---|---:|---|
| `CLARIFY_SYS` | 1 | L |
| `signal_terms` | 34 | M |
| `clarify_text` | 3 | M |

#### z08 — L+M = 12

L: `SKIP_SERVICE_RIVALS`, `FORK_DETECT`, `FORK_OUTCOMES`, `_FORK_MEAS_TTL`, `_fork_meas_cache` (по 1).  
M: `ALIAS_TOP`, `ALIAS_VETO`, `PROBE`, `ALIAS_BY_CONCEPTS`, `VETO_NEEDS_RANK`, `VETO_HEAD_WINS`, `ASK_MEMORY_APPLY` (по 1).

#### z10 — M = 176 (L=0)

| символ | стр. |
|---|---:|
| `rank_leader_answer_text` | 21 |
| `rank_product_axis_col` | 4 |
| `rank_leader_atom` | 32 |
| `count_theme_code_pick_applies` | 56 |
| `rank_period_unspecified` | 12 |
| `rank_period_clarify_applies` | 24 |
| `try_rank_period_clarify` | 27 |

#### z11 — L+M = 152

L: `_sales_register_score`(16), `sales_lift_possible`(45), `sales_rank_engaged`(28), `sales_canon_intent`(27).  
M: `rank_groups_answer_text`(29), `sales_ticket_hatch`(7).

#### z12 — L+M = 417

L: `axis_catalog_values`(78), `warehouse_axis_values`(11), `stock_skips_warehouse_clarify`(13), `_rank_wants_quantity`(13).  
M: `_catalogs_joint_with_kind`(26), `question_asks_stock_balance`(3), `warehouse_axis_is_live`(7), `stock_subject_needs_clarify`(12), `rank_measure_hint`(15), `_stock_expense_side_penalty`(3), `stock_goods_pool`(19), `filter_stock_goods_registers`(15), `stock_canon_src`(13), `prefer_entity_for_stock`(15), `_resolve_breakdown_balance_src`(20), `_breakdown_fallback_measure`(19), `stock_breakdown_leader_fallback`(54), `_balance_map_by_src`(7), `filter_balance_structural`(40), `balance_bridge_clarify`(34).

#### z14 — L+M = 100

L: `slot_measure_uncovered`(9).  
M: `_FP_SKIP`, `_FP_STR`, `_slot_fp`, `answers_diverge`, `answers_src_conflict`, `RAW_FOCUS_TRUST`, `reset_decisions_for_tests`, `guards_skip_for_choice` (сумма тел ≈91).

#### z15 — L+M = 48

| символ | стр. | вердикт |
|---|---:|---|
| `PROOF_NA` | 1 | L |
| `stop2_active` | 11 | M |
| `determined_answer_rivals` | 36 | M |

#### z17 — L = 131

`_live_ref_key_col`(14), `aggregate_live_row_count`(26), `aggregate_live_header_count`(30),
`holders_of_target`(18), `measures_of_many`(16), `term_ref_owners`(27).

#### z18 — L = 38

`merge_period2_groups`(16), `_filled_ask`(19), `_ask_back`(3).

#### z21 — L = 21

| символ | стр. |
|---|---:|
| `wiki_leader_alive` | 21 |

#### z09 / z13 / z16 — см. F3–F5

Полные таблицы L/M — в `W1-z09/13/16*.md` (десятки символов; при сносе файла
поимённый перечень не нужен, кроме **сохраняемых N**):

| зона | сохранить (N) | стр. |
|---|---|---:|
| z09 | `fork_labels_of` | 20 |
| z13 | `atom_terminal_gate_text`, `stock_balance_is_sales_noise` | 21 |
| z16 | `pair_slots_only`, `_NON_DATA_MARKERS`, `question_expects_accounting_data` | 35 |

### 2.3 Жёсткие legacy-only имена (X2 §3.2) — 39

После сноса legacy **перестают читаться входом**; в других зонах нет Name-Load
(определение может остаться мёртвым телом до поимённого выреза):

`CLARIFY_SYS`, `FORK_DETECT`, `FORK_OUTCOMES`, `SKIP_SERVICE_RIVALS`,
`_aliases_by_src`, `_ask_back`, `_filled_ask`, `_fork_fp_diag`, `_fork_log`,
`_fork_pool_excluded`, `_fork_relevant`, `_gate_bad_preview`, `apply_period_leader`,
`build_measure_empty_pivot`, `canon_claims_question`, `children_by_parent`,
`date_only_kind_filter`, `entity_form_collapse_guard`, `event_count_has_explicit_period`,
`event_filter_pool`, `filter_dead_measure_alts`, `fork_c_options_without_left_srcs`,
`fork_detector_scan`, `fork_outcome_c`, `measure_asked_explicitly`, `measure_class_alts`,
`measures_of_many`, `merge_period2_groups`, `not_for_excludes`, `partial_tables`,
`pick_measure`, `rank_defer_fork_outcome_b`, `resolve_fork_outcome`, `sales_rank_engaged`,
`slot_measure_uncovered`, `term_ref_owners`, `try_entity_form_answer`,
`try_event_count_period_clarify`, `unresolved_quantity`.

Патч-only (нет на диске legacy без патча): `entity_form_gate_open`,
`stock_asks_named_product`.

### 2.4 Сводка объёма сноса (тела / файлы)

| слой | ≈ строк | состав |
|---|---:|---|
| A. legacy z20 файл | 5103 | F1 |
| B. z09+z13+z16 файлы | 2568 | F3–F5 (− тонкие N при выносе) |
| C. L+M смешанных (без z09/13/16) | **2200** | §2.2 |
| **Conservative (A+B)** | **7671** | = X4 |
| **Moderate (A+B+C)** | **≈9871** | близко к X4 ~9.6k (X4 считал unreachable def ≈1925+345 внутри B-части иначе) |
| Тонкие N к выносу из B | 76 | иначе сломается currency-label / gate / wiki-учёт |

---

## 3. Что остаётся после сноса

### 3.1 Итого по ask/ (файловые строки)

| сценарий | вырез | остаток диска z* | остаток load |
|---|---:|---:|---:|
| Сейчас (оба z20) | 0 | 22833 | 17730 (ONEPATH) |
| **C0:** только legacy+патч | 5103 | **17730** | 17730 |
| **C1 conservative:** C0 + z09/13/16 целиком | 7671 | **15162** | 15162 |
| C1 + вынос тонких N (+76 в другие файлы) | ≈7595 net | ≈**15238** | то же |
| **M moderate:** C1 + L/M смешанных (−2200 тел; файл короче ≈на столько) | ≈9871 | **≈12962** | ≈12962 |
| Ориентир «только живые тела» (N зон + новый z20) | — | тел ≈ **11377** + клей | — |

Доля выреза от полного диска: conservative **34%**, moderate **≈43%** (X4: 34–42%).

### 3.2 По каждому файлу после moderate (оценка)

Формула: `остаток ≈ файл − (L+M)` для смешанных; `0` для F1/F3–F5 (после выноса N);
новый z20 без изменений объёма.

| файл | сейчас | после M | примечание |
|---|---:|---:|---|
| z01 | 883 | ≈867 | −16 |
| z02 | 713 | ≈675 | −38 затенённых; +возможно restore `question_expects_*` |
| z03 | 615 | ≈547 | −68 |
| z04 | 297 | ≈261 | −36 |
| z04b | 567 | ≈494 | −73; +`fork_labels_of` |
| z05 | 1239 | ≈614 | −625 |
| z06 | 604 | ≈393 | −211 |
| z07 | 556 | ≈518 | −38 |
| z08 | 278 | ≈266 | −12 |
| z09 | 1018 | **0** | вынос `fork_labels_of` |
| z10 | 471 | ≈295 | −176 |
| z11 | 389 | ≈237 | −152 |
| z12 | 1346 | ≈929 | −417; +`stock_balance_is_sales_noise` |
| z13 | 925 | **0** | вынос 2 символов |
| z14 | 680 | ≈580 | −100 |
| z15 | 437 | ≈389 | −48 |
| z16 | 625 | **0** | вынос 3 символов → compose/wiki/z02 |
| z17 | 698 | ≈567 | −131 |
| z18 | 882 | ≈844 | −38; +`pair_slots_only` |
| z19 | 368 | 368 | без выреза |
| z20_ask_main_http.py | 2953 | 2953 | вход |
| z20_…_legacy.py | 5103 | **0** | |
| z21 | 1128 | ≈1107 | −21 |
| z22 | 58 | 58 | |
| **Σ** | **22833** | **≈12962** | |

`_bootstrap` / `_imports` / `_wire`: минус ветка legacy+патч; список `_ZONE_FILES` без z09/13/16/legacy.

---

## 4. Порядок сноса (после flip)

Предусловие: прод на `ASK_ONEPATH=1` / новый z20; `test_one_path` зелёный и
остаётся замком с **первой** волны.

| шаг | что вырезаем | замки (X3) | риск |
|---:|---|---|---|
| **S0** | Убедиться: flip сделан; legacy не грузится | `test_one_path` | низкий |
| **S1** | Удалить `z20_ask_main_http_legacy.py`; убрать патч `_patch_z20_wiki_primary` и ветку `_Z20_FILE`/`_ONEPATH` (оставить только новый файл) | **удалить:** `test_enough`, `test_axis_focus`. **править path/text→новый z20 или снять legacy-ветки:** `test_action_class`, `test_ask_choice_memory`, `test_axis_count_plain`, `test_early_clarify_atom_fps_hashable`, `test_final_stock_route_filters_absent`, `test_focus_loop`, `test_measure_hatch_luk`, `test_measure_menu_not_silent`, `test_no_pre_wiki_reorders`, `test_rank_leader_path`, `test_sales_canon_prefer`, `test_warehouse_axis_autonomy`, `test_wiki_card_hybrid`, `test_wiki_leader_not_overridden`, `test_k4_*` (legacy path), `test_zone_names_resolvable` (вычеркнуть legacy). Особо структурные legacy (предмет уходит): early-clarify / stock-route-filters / measure-hatch / wiki-patch — **кандидат на удаление целиком**, не «перенос» | средний: 19 замков с сильной связью на legacy |
| **S2** | Вынести тонкие N из z09/z13/z16 → соседние живые зоны; затем удалить **три файла**; убрать из `_ZONE_FILES` | **z09 (−14 замков сильных):** fork_*/calendar/currency/entity_form/leader_hatch/measure_hatch/action_class/atom_terminal/early_clarify… — большинство становятся бессмысленны → **удалить** fork-замки. **z13 (−18):** то же + `test_wiki_leader_not_overridden` (частично). **z16 (−10):** `test_answer_atom` (оставить z15), `test_gate`/`test_step4_guards`/`test_measure_*`/`test_k4_*` — убрать z16-ветки. `test_zone_names_resolvable` — вычеркнуть три имени | **высокий**, если не вынести N: сломается currency-label / compose `pair_slots_only` / wiki `question_expects_*` |
| **S3** | Поимённый вырез L+M в z05 (entity-form терминал) + связанные L в z11 (`sales_rank_*`, `sales_canon_intent`, …) | `test_entity_form`, `test_compare_sales`, `test_fork_detector`, `test_action_class` — сузить/удалить entity-form сценарии | средний: большой diff z05 |
| **S4** | z12 L+M (warehouse clarify / dead filters) + z06 L (`children_by_parent`, `partial_tables`, …) | `test_warehouse_*`, `test_final_stock_route_filters_absent` (уже про «absent»), `test_stock_balance_path` — подчистить stubs | средний |
| **S5** | z10 M (leader/period-clarify хвост), z17 L (holders/measures_of_many), z03 L (window leader), z04/z04b L (day/amount basis prefer), z18 L (`_ask_back`…), z21 `wiki_leader_alive`, мелочь z01/z07/z08/z14/z15 | точечно: `test_rank_leader_path`, `test_aggregate_live_flags`, `test_fork_window_readings`, `test_wiki_leader_not_overridden` | низкий–средний |
| **S6** | Зачистка `_ZONE_FILES`, `test_zone_names_resolvable`, grep по именам снесённых символов в тестах/доках; прогон `test_one_path` + контрольные | — | регресс на скрытых транзитивах |

**Не сносить в этих шагах:** новый z20 (B2 инфра + B3–B4), z21 wiki-каскад, ядро z14 decision/memory, z18 compose, z19 gate, z01 infra, z07 rows/rrf — это несущая конструкция одного пути.

---

## 5. Скрытые выбиратели, доступные НОВОМУ тракту

Критерий вердикта:
- **легитимно** — ровно один кандидат **или** выбор человека (меню/билет/память выбора);
- **выбиратель** — код сам решает при **>1** альтернативе (silent pick / rerank / словарь / эвристика).

| # | символ / цепочка | зона | что решает | вердикт | зачем |
|---|---|---|---|---|---|
| 1 | `rank_axis_resolve` (+ `rank_axis_pick` / `rank_axes_rerank`) | z10 | ось GROUP BY | **выбиратель** | при ≥2 осях: model/rerank; при 1 — авто; меню только если вернул alts |
| 2 | `count_question_skips_axis` / `total_question_skips_axis` | z10 | снять axis-clarify | **выбиратель** | код отменяет вопрос человеку |
| 3 | `rank_intent_from` / `rank_question_text` | z10 | класс «это рейтинг» | **легитимно*** | классификатор intent (не выбор из пула src); *кормит выбиратель #1 |
| 4 | `measure_choice` ← `_settle_measure` | z14 | мера по слову | **выбиратель** | silent `exact/alias/…/single`; меню лишь при `how=='ask'` |
| 5 | `resolve_measure` | z14 | текст меню → поле | **легитимно** | разбор ответа человека |
| 6 | `hold_settled_entity` | z14 | src settled vs focus | **легитимно*** | память/билет выбора; *риск, если found_by без явного меню |
| 7 | `sales_money_measure` / `sales_qty_measure` ← `_measure_dimension` | z11→z18 | денежное/qty поле | **выбиратель** | лексика/алиасы → мера без меню |
| 8 | `stock_net_register_pair` (+ `_sort_stock_pool`, `_stock_qty_measure_name`, …) | z12 | пара регистров + product col | **выбиратель** | ранжирование/эвристика регистров |
| 9 | `aggregate_stock_net_distinct` | z12 | путь агрегата | **выбиратель** | код выбирает net-distinct vs обычный |
| 10 | `resolved_warehouse_axis_word` / `resolved_unaccounted_slice_axis_word` / `intent_axis_words` | z12→z21 | ось места / срез | **выбиратель** | код резолвит слово оси (не меню) |
| 11 | `registers_for_kind_axes` / `_catalogs_for_*` | z12 | какие регистры под оси | **выбиратель** | метаданные → пул |
| 12 | `balance_routing_core` / `stock_question_engaged` | z12 | остатковый маршрут | **легитимно*** | гейт класса вопроса; *если внутри silent src — см. #8 |
| 13 | `grain_dec_from_axis_ticket` | z12 | grain из ticket | **легитимно** | уже выбранный ticket |
| 14 | `stock_balance_is_sales_noise` | z13→z12 | отсев src по подстрокам имени | **выбиратель** | эвристика имени регистра |
| 15 | `kind_axis_hits` / `term_axis_hits` | z17 | ось по kind/term | **выбиратель** | stem/meaning → ось до SQL |
| 16 | `kind_axis_rerank` | z17 | одна ось реранкером | **выбиратель** | при нескольких — код |
| 17 | `repair_period_from_question` → `period_form_from_question` | z03 | период/form_id из словаря фраз | **выбиратель** | молчаливая подстановка **до** меню |
| 18 | `period_readings` | z03 | список окон MTD/WTD/… | **легитимно*** | список для меню; *silent rewrite assumed sliding→week |
| 19 | `named_platform_kinds` + `filter_pool_by_named_type` | z21 | режет wiki-пул по типу | **выбиратель** | отсев кандидатов кодом до verify |
| 20 | `wiki_aggregate_want` / `wiki_action_class` / `wiki_axis_phrase` | z21 | сужение hybrid SQL | **выбиратель** | структурный фильтр пула |
| 21 | `_wiki_axis_has_carriers` | z21 | включать ли ось в SQL | **легитимно*** | наличие носителей в БД (факт), не выбор из меню |
| 22 | `wiki_knn_separable` / gap в `wiki_pick_from_cards` | z21 | force clarify vs leader | **легитимно** | при малом зазоре → меню человеку |
| 23 | `wiki_outcome_from_verify` | z21 | leader / clarify / none по yes/no | **легитимно*** | порог счётчика; *не «лучший из плохих», но код решает порог |
| 24 | `wiki_leader_post_verify` (+ measure/axis carried) | z21 | отвергнуть лидера | **выбиратель** | пост-гейт без нового меню |
| 25 | `wiki_validate_leader_axes` | z21 | allowlist префиксов src | **легитимно*** | жёсткий контракт вида src |
| 26 | `MEANING_TOP` / RRF / `meaning_candidates` (через z08 константы) | z08→z06/z17 | кандидаты сущностей/оси | **выбиратель** | смысловой мост без вопроса |
| 27 | `answer_slot_mode` / `atom_operation` / `answer_money` | z15 | форма слотов / операция | **легитимно*** | не src/measure/period/axis; форма ответа |
| 28 | `parse_intent` (LLM) | z02 | want/measure/period/… | **выбиратель*** | модель заполняет слоты **параллельно** wiki; не меню |
| 29 | `sales_noncanon_focus` / `_is_product_catalog` | z11 | отсев/класс источника | **выбиратель** | эвристики имени |
| 30 | `_sales_rank_top_n` | z11 | K для rank | **легитимно*** | параметр top-N, не выбор src |
| 31 | `fork_labels_of` | z09 | подпись валютного варианта | **легитимно** | label из БД, не выбор |
| 32 | `SCORER` / embed / `RERANK_*` | z01 | инфра ранжирования | **легитимно*** | не бизнес-меню; *питают #1/#16 |

**Итог по выбирателям:** на новом тракте **есть** скрытые выбиратели оси (`rank_axis_resolve`, `kind_axis_*`/`term_axis_hits`), меры (`measure_choice`, `sales_money/qty_measure`), источника/регистра (`stock_net_register_pair`, `stock_balance_is_sales_noise`, wiki pool filters) и периода (`repair_period_from_question`). Это согласуется с блокерами этапа-1 (меню любого прочтения **до** SQL; coverage/честный no_data) — снос мёртвого груза **не** снимает эти выбиратели сам по себе.

---

## 6. Одна строка

После flip снос: **legacy z20 (5103) + патч + z09/z13/z16 (2568, с выносом 76 N) + дочистка L/M смешанных (~2200)** → вырез **~7.7–9.9k** строк с диска, остаток ask/z* **≈13–15k**; несущий тракт — новый z20 + wiki z21 + settle/compose/gate. Замки: сразу удалить `test_enough`/`test_axis_focus` и fork/veto-армию; остальное — перенос path на новый z20 или снятие legacy-веток по X3. Скрытые выбиратели оси/меры/регистра/периода в NEW **живут** и требуют отдельного решения контракта, не только сноса.
