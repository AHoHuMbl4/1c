# I1 — Красная проверка плана I0 (5 атакующих, консолидация)

Дата: 13.09.2026. Пять независимых атак плана `I0-план-транспорт-и-вики.md`:
контракт/п.0, факты, пропуски, осуществимость, позиция владельца. Все — read-only,
сверки живьём (окно, база, доки сборки). Полные тексты — в логах сессии; здесь
принятые находки, которыми план I0 переработан в v2.

## Что план I0 выдержал

- Щель degraded (z21:1139-1140): подтверждена кодом обоими проверяющими.
- Дыра пула «прайс» (wiki_card_hybrid.sql:247-256 object-ветка): подтверждена живым
  SELECT — носитель informationregister_ценыноменклатуры режется до судьи; живой замер
  послабления = ровно +1 карточка в пуле.
- Вердикты fit/why не логируются: подтверждено (счётчики только в diag, why умирает в z21:735-736).
- Флип verify: подтверждён по токенам (rid fb2183…/0755191…: вход verify in=671 одинаков,
  выходы 229 vs 237 → answer vs no_data).
- llm-task как транспорт: отклонён 5/5 (optional/disabled Lobster-крючок; «not currently
  reliable» в доке; +0.42 с на вызове, на наших коротких вызовах +18-30%; причины ошибок
  наружу не выходят; usage нет; операторский токен на LAN — новый периметр).

## Что опровергнуто / исправлено

1. **П1 (посылка «судья не видит про-что/aliases») — опровергнут живым замером**:
   wiki_build.sql:58-66 кладёт aliases/bestUsedFor в frontmatter И в разделы тела;
   255/255 страниц несут их в первых 1500 символах паспорта (замер на окне).
   П1 как «+2 колонки» = дубль текста. Заменён на: живой дамп паспорта в пакете
   наблюдаемости + решение по факту, что реально видит судья.
2. **П3 (топики)**: смена типа not_enough_for на VARCHAR[] ломает коллизионный круг —
   2 SQL (wiki_alias_collision_left.sql:14, _round.sql:17) держатся на ILIKE по тексту,
   38 пар коллизий закрыты этим сравнением; ≥8 SQL + контракт агента пишут колонку.
   Исправление: топики — отдельной колонкой рядом, генератором из метаданных базы
   (не «словарь из нашего замера» — это была подгонка, п.0), коллизионный слой не трогать.
3. **П5**: «degraded → no_data» нарушал п.21 (отказ при живом пуле). Исправление:
   непроверенный пик не уходит в ответ; меню/исход по пулу сохраняются.
4. **П2**: шаг() живёт в z20 (не z21:889); i2_runner diag НЕ пишет (только
   verdict/kind/text/nums/latency). Исправление: diag-verdicts внутри
   wiki_verify_candidates + правка i2_runner (писать diag в jsonl) + санитизация why
   (в journald одна строка). П2 вынесен ПЕРВЫМ отдельным пакетом — без базы «до»
   эффект следующих правок неотличим от шума.
5. **Числа плана не воспроизводились** (p50/p90/p99 22.7/67.2/98.6 — ни один срез
   окна не даёт; «505 строк» полигона — фактически 292 и в бэкапе; «плагин тёплый» —
   disabled; maxConcurrent=4 — про agent-runs, не про /tools/invoke; кредиты 5.90, не 6.1).
   Правило на будущее: латентность в планах — только со свежим замером и методикой
   (окно/период/фильтр), цифры из чужих срезов не переносить.
6. **Пилот llm-task снят**: :18801 — боевой канал (Open WebUI клиента ходит туда же),
   рестарт — изменение прода; 20 вопросов на флип-узле ничего не докажут.

## Новый главный диагноз (недолеченный)

Флип verify при живых смысловых данных в паспорте. Лечение по образцу проекта
(INTENT_SAMPLES/INTENT_LEAD, z02 — согласие нескольких вызовов уже работает):
на критической точке sole-yes — повторный вызов verify; совпало → лидер,
разошлось → меню/none по пулу. Это не новый выбирающий механизм и не гейт по словам:
тот же судья, второй спрос. На локальной модели владельца — без расхода кредитов.

## Внешние блокеры (снять до любых замеров)

- Прод 1c-serene-ask@postgres остановлен вручную 12.09 15:37 (юнит disabled).
- Эмбеддер 10.3.1.11:8000 недоступен → такт failed, kNN-пул вопросов мёртв —
  любой живой прогон пула/verify невозможен.
- 27B: gpu-27b.timpul.pro:8000 не отвечает с окна; адрес/порт подтверждает владелец.
- Кредиты OpenRouter: 5.90 из 35 — до перехода на локальную модель прогоны впритык.

## Метрика приёмки слепа (очередь следующего эпизода)

i2_runner.classify_verdict (:238) относит clarify/no_data/unavailable к honest_no ДО
сверки с эталоном: 16 honest_no с числовым эталоном (8 денег, 4 регистра, 4
склада/справочника) и 6 меню «любой клик ≠ эталону» невидимы метрикой. Оформить
очередью отдельным документом (J-эпизод), не смешивать с текущим.
