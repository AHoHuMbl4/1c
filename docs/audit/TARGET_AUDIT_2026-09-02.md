# Аудит соответствия TARGET.md — армия 22 агентов (02.09)

Метод: 22 независимых cursor-агента (10 по зонам a1-a10, 2 охотника h1/h2 поперёк
кода, 6 досып c1-c6, 4 перепроверки ядер v1-v4), промты без подсказки известных
дологов. Отчёты: `.claude/state/result-ta-*.md` (22 файла). Живой замер env боя —
оркестратором, на окне. HEAD аудита: 980e339 (= выкат на окне, md5 сверены).

## Главные классы находок (по числу независимо сошедшихся агентов)

### К1. п.0 «из коробки»: бой работает только из-за ручных env-флагов — 6 агентов + живой замер
Код: `ASK_ENTITY_FORM`, `ASK_SQL_RRF`, `ASK_RESOLVER_IVF`, `ASK_SOLR_SYNONYMS`,
`ASK_MEMORY_APPLY`, `ASK_SALES_RANK_CANON`, `ASK_ATOM_TERMINAL`,
`ASK_HEALTH_NATIVE_FRESHNESS`, `ASK_SLOT_COVER` — все default `"0"` (z03:58-67,
z01:870, z08:48-54, z20:5278). Живой замер `/etc/1c-serene-ask-postgres.env` на
окне: все перечисленные = 1 (кроме ASK_CALENDAR_AXIS/ASK_CURRENCY_AXIS —
выключены и в бою), плюс `ASK_SCORER=dfi`, `ASK_MONEY_UNIT=лей`. Установка env
не пишет (c5: привязка копии руками; a9: deploy — ручной SSH-ритуал). След:
`ASK_WIKI_CHOICE=1` мёртвым хвостом в env (q1 удалил флаг из кода, файл не чищен).
**Вердикт: чистая коробка на новой базе стартует с урезанным путём. Нарушение п.0.**

### К2. п.0: wiki-слой не собирается тактом — бот глух на новой базе — 4 источника
`wiki_card_build.sql` («Не в такте: отдельный прогон», строка 9) не вызывается
нигде: build.sh → wiki_publish.sh → только wiki_build.sql (VIEW wiki_pages).
Таблица `search_wiki_entity_card` + emb — единственная опора гибридного пула z21;
z21:688-704 — единственный путь выбора, обходы вырезаны → пустая таблица = no_data
на любой учётный вопрос при живом корпусе (a10 grep, v4 симуляция, v1 трасса,
проверка оркестратора wiki_publish.sh). На okna карточки строились руками 29.08.
Также: `period_relative_forms.json` в бою почти пуст (только prev_week; полный —
в work/acceptance) → «прошлый месяц» целиком от LLM (v1). **Нарушение п.0/п.21.**

### К3. п.9: слова домена и имена конфигурации в коде — 5 агентов
z11:789-791 (полные имена `catalog_номенклатура` и др.), z11:87/307/700-713
(«реализац/продаж/установкацен/номенклатур»), z13:9-18 («книгапродаж/актсверки/
реализац» — известный базлайн О6, найден независимо h1 и c3), z12:737-741
(«сторн/коррект»), z12:989-1004 (пара приход/расход по имени «реализац»),
z10:72-84/434-436, z02:390, z16:354, z09:15-18 («всего» как fallback меры),
z04b:305-307 («Валюта_Key/Курс» литералами). На базе без этих корней пути
меняют поведение молча.

### К4. «Один путь»: обходы wiki-замка в z20 — v2 (карта) + подтверждение оркестратора
z20:3291-3300: `prefer_entity_for_sales/catalog_count/stock` переставляют
`arb_pool` и `sales_canon_force_pool` коллапсирует пул ПОСЛЕ wiki-замка — без
гейта `wiki_arbiter_locked` (writer_pair гейтится, эти — нет; сверено чтением
кода). z20:2856-2860 `code_ambiguous` дописывает держателей после каскада;
z20:3613 круг арбитра не гасится wiki-замком; z21:671 `sales_canon_locked`
подставляет лидера в обход каскада; z21:951-954 verify degraded → пик без
паспортов; z21:970-995 clarify при <2 опций → None → no_data вместо вопроса;
z21:1067-1096 post-verify роняет уже верифицированного лидера. Мёртвый
до-вики мусор, ещё стреляющий при сбое пары признаков wiki: `SIGNAL_DISAGREE`,
`writer_pair`, `_alias_clarify`/`REQUIRE_SUPPORT`, мёртвый `warehouse_clarify`.

### К5. п.12: молчаливые догадки — 7 агентов
Окно: MTD/WTD лидер молча (z03:373-393), sliding→календарная перепись
(z03:297-302), assumed-период не снимается (z02:557-561), sales освобождены от
assumed-clarify (z20:1722-1723), Q2-без-периода → all-time (z03:274-278).
Мера: реранк молча (z16:617-634), `names[0]` (z16:431-435, z12:959),
«продали=деньги» без вопроса (z11:386-390), «колич» ключ (z12:671+),
первый-hit группы (z18:148-164). Сущность: `stock_canon_src` голова пула
(z12:1065), первый каталог (z11:288-336 cold-lift), `readings[0]` (z04:262),
дефолт-лидеры calendar_days/doc_amount (z04:207, z04b:183).

### К6. п.13/п.10: тихая потеря/искажение — 4 агента
z17:180-187: fallback live-колонки молча роняет предикаты даты → сумма по более
широкому множеству (риск неверного числа, a3). z04b:419-422: одна FX-сумма
пишется во все меры fork_scan (c1). sync-ошибки не роняют такт (pipeline.sh:58,
a7). plain-fallback затирает nums/flags NULL без метки деградации
(corpus_build:2154+, a7). Solr-компиляция молча теряет многословные алиасы и
<2-стемовые ряды без счётчика (a10). coverage LIMIT 20 в отчёте (c4).

### К7. п.20/п.19: свои процессы над данными — h2
ask ходит в БД процессом psql на каждый SQL (z01:271-305; сотни на ответ —
норма «единицы» нарушена по построению); serene_report.py шлёт СХЕМУ в модель
(122-128, 340-358) — юнит жив; classify_entities.py (шаг такта build.sh:371)
шлёт перечень колонок в модель и пишет классы обратно; z05:1226 разность сумм
в питоне; poc_load_entity diff в памяти; question-эмбед через свой HTTP при
ASK_EMBED_NATIVE=0 (спор h1 vs h2 → в разбор TD3). embed_bulk: transfer strict
по умолчанию off + `|| true` + стирание part (a8).

### К8. Вектор-безопасность — v3 (глубокий), a8
Крит-окно: DELETE unmatched → пакетный xfer-UPDATE (corpus_merge:887-915):
обрыв между ними = массовый emb=NULL при зелёном гейте; повтор merge карту
пересоздаёт из опустевшего корпуса. Допуск 0,5% = легальная тихая потеря (~8k
векторов). Порядок xfer-до-DELETE тестами не зафиксирован. Обход
`MERGE_VECTOR_LOSS_BYPASS`. Бэкап — ручной ритуал, не коробка; restore при
сбое tar оставляет движок остановленным (serenedb_backup.sh, c5).

### К9. Приёмка п.15/п.10 — c6 + подтверждение оркестратора
i2_runner:238-239: honest_no при числовом эталоне — ДО сверки → отказ при
данных не попадает в «уверенно неверных» (п.21-дефект невидим прибору).
Reshoot-правило no_stock ставит эталон no_data без проверки данных — дыра
продукта закреплена как норма приёмки. Срезы/правила/канон TSV — под okna
руками, автопрогонщика в установке нет. Эталоны местами по search_corpus
(самосверка).

### К10. Коробка/юниты — a9, c5, c4
Ручные шаги сверх «логин-пароль 1С»: раскладка /etc, setup-receiver (root),
рестарт юнитов руками, DDL журнала/памяти/scope отдельными bash, бэкап.
firstbuild объявляет успех без проверки такта. Drop-in’ы глотают сбои (`-`).
packet_kit дефолт URL приёмника `1c-gate.timpul.ru` (п.9). serenedb_backup:
tar-сбой = движок остановлен.

## Что чисто (положительная сходимость)
- Счёт чисел: z17 aggregate / totals_of — SQL по всему множеству; в модель
  плейсхолдеры, числа подставляет код; гейт z19 заземляет (a3, a5, h2, v1).
- ASK_WIKI_CHOICE из кода удалён (h1, q1 держится).
- В исполняемом коде нет имён okna/klient/реализациятмц (только комментарии) —
  a7, h1, c4, a2, a3.
- Merge/emb: досчёт строго emb IS NULL, идемпотентен, не затирает готовое;
  гейт бюджета fail-closed (v3).
- Пакетный контур: карантин, ошибки честные, dedup в SQL (a9, h2).
- coverage/resolver/init — идемпотентный SQL внутри движка (c4).

## Конфликты → волна споров TD (3 агента на тему)
TD1 z20:3291+ обходы замка (как чинить минимально); TD2 i2_runner маскировка;
TD3 question-эмбед HTTP vs ai_embed (п.20); TD4 z17:180 предикаты; TD5 z04b:419
FX-патч мер. Результаты: `.claude/state/result-td*-*.md`.

## Вердикты споров TD (сходимость, 02.09 вечер)

Все 5 тем сошлись (TD1/3/4/5 — 3/3; TD2 — 3/3, r1 дописывал отчёт последним).

- **TD1 «один путь» — ОБХОД РЕАЛЕН (3/3).** prefer_entity_for_sales/
  catalog_count/stock (z20:3291-3296) и sales_canon_force_pool (3298-3300)
  работают ПОСЛЕ wiki-замка без гейта; cold-lift ставит канон головой arb_pool,
  круг арбитра (3613) берёт cand_src[0] по порядку пула → лидер ответа ≠
  wiki-лидер. Комментарий z20:3231-3237 («поздние канонные перебои ВЫРЕЗАНЫ»)
  противоречит живому коду. code_ambiguous (2856) раздувает замкнутый лидер.
  Фикс-направление: гейт prefer/canon/арбитра на wiki_arbiter_locked.
- **TD2 приёмка — МАСКИРОВКА РЕАЛЬНА (3/3).** 2 класса: (1) ранний honest_no
  по kind ДО сверки скрывает «отказ при числовом эталоне» из любого счётчика
  (замок test_client_gold:222 закрепляет); (2) no_stock = константный эталон
  no_data с притворным etalon_origin=live (18 вопросов, живым SQL не
  перепроверяется никогда). Фикс-направление: вердикт refusal_with_data
  отдельной строкой отчёта + no_stock с проверяемым SQL/честным origin.
  Влияние на L19: wrong (3) не меняется; из 28 honest_no до ~10 строк могут
  оказаться замаскированными п.21-дефектами (точное число — по answers.jsonl).
- **TD3 эмбед вопроса — НАРУШЕНИЕ п.20 (3/3).** «Эмбеддинг текстов — ai_embed»
  буквально; п.5 — про размещение, не про канал. Риск рассогласования
  пространств реален (дверь /embed is_query=true против документа). Из коробки
  правильнее ASK_EMBED_NATIVE=1; HTTP-ветка — удалить или вторичной.
- **TD4 z17-fallback — ДЕФЕКТ РЕАЛЕН И ЖИВОЙ (3/3).** При fallback
  aggregate_live_column без date-колонки витрины предикат периода молча
  отбрасывается; ШИРЕ заявления a3: аргумент match в live вообще не передаётся
  — текстовый отбор теряется всегда. При найденной date-колонке дефекта нет
  (try_cast). Пометки в возврате нет. Фикс-направление: отказ/clarify при
  потерянном предикате, не сумма по широкому множеству.
- **TD5 FX-патч мер — ПОДМЕНА РЕАЛЬНА, ПУТЬ ВЫКЛЮЧЕН (3/3).** Один FX-итог
  пишется во все меры fork_scan без фильтра класса; в бою ASK_CURRENCY_AXIS=0
  (мёртв), включение оси без починки = молчаливые чужие числа. Фикс-направление:
  FX только денежной мере (amount_col).

Итог сходимости: 5/5 тем закрыты единогласно; расхождений между арбитрами нет.
Все-fix кандидаты собраны; исполнение — только через красную команду и
исполнителей, в порядке: TD2 (прибор врёт о метрике) → К2 (wiki_card_build в
такт — коробка глуха) → TD1+К4 (гейты «один путь») → К1 (env-проводка
установкой) → TD4 → К3/К5 по плану владельца.

## Живой статус на момент аудита
Такт №1 идёт (монолит p_doc реализациятмц, ~3ч, вектора на baseline
1 657 724, бэкапы ×2 на окне). Аудит шёл параллельно, базу не трогал.
