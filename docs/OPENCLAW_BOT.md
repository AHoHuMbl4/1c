# Бот-слой: OpenClaw (диалоговая оболочка «второго мозга»)

> 🔴🔴🔴 **ГЛАВНОЕ ПРАВИЛО — ЧИТАТЬ ПЕРВЫМ, ДО ЛЮБОЙ РАЗРАБОТКИ БОТ-СЛОЯ:**
>
> **1. Используем НАТИВНЫЕ решения OpenClaw. Кастом — ЗАПРЕЩЁН без явного согласия владельца.**
>    Любую задачу сначала решаем штатными средствами OpenClaw (его конфиг, плагины,
>    механизмы). Если кажется, что нужен кастомный код/патч/обход — СТОП, спросить владельца.
>
> **2. Документацию OpenClaw смотрим В САМОМ РЕПОЗИТОРИИ на LXC — не угадывать решения.**
>    Репо склонирован на LXC. Перед любым действием — найти ответ в его README/docs/примерах.
>    Не «по памяти», не «наверное так» — только по его реальной документации и коду.
>
> Эти два правила приоритетнее любых прочих соображений в этом документе.

---

## Что это (ПРАВИЛЬНАЯ модель — уточнено владельцем 2026-07-23)
OpenClaw — **НЕ замена** нашего бота (braine), а **надстройка-оболочка тона** поверх него.
braine отвечает точно, но «сухо»; OpenClaw делает диалог живым/человеческим.

Схема:
```
человек пишет → OpenClaw принимает → передаёт НАШЕМУ боту (braine API POST /ask :8090)
   → braine отвечает (факты, цитаты, «не выдумывай») → OpenClaw оживляет тон → клиенту
```
braine = мозг/источник истины. OpenClaw = разговорный слой поверх. Данные 1С OpenClaw
сам НЕ трогает — только braine (через наш read-only канал).

🔴 **Критичный инвариант интеграции:** braine гарантирует «не выдумывать» (гейты: каждая
цифра сверена с источником, обязательные цитаты). Оболочка OpenClaw обязана
**переформулировать ТОЛЬКО то, что вернул braine** — не добавлять и не менять факты/цифры.
Иначе теряется весь смысл точного мозга. Это ложится в персону (`AGENTS.md`) и в выбор
механизма вызова.

Источники: `http://gitlab-real.unde.life/money/opwnclaw-bot` (проектная докум. клиент-ботов
владельца, склонирована `/opt/openclaw`) + движок `github.com/openclaw/openclaw`
(склонирован `/opt/openclaw-engine`). Разворачиваем на LXC `192.168.56.42`.

## Репозиторий и что в нём (изучено 2026-07-23)
Склонирован на LXC: `/opt/openclaw` (это проектная документация клиент-ботов владельца
на базе OpenClaw, НЕ исходники движка). Движок OpenClaw ставится отдельно (npm,
глобально 2026.5.28 на их сервере .15).

Нативная модель (из их docs — проверено, не угадано):
- **1 клиент = 1 инстанция:** Linux-юзер `<client>` + `linger=on` + `~/.openclaw/`
  (`openclaw.json` конфиг, `.env` секреты 600, `workspace/AGENTS.md` персона, логи/сессии)
  + systemd user-юнит `openclaw-gateway.service`. Reboot-safe.
- **Поведение = персона (`AGENTS.md`) + config + memory-wiki.** Модель — DeepSeek
  (openai-completions). Канал — Telegram (флагман). `session.dmScope: per-channel-peer`
  (обязательно). `commands.native=false` (без служебного меню). `streaming.mode=off`.
- **Инструменты бота:** `message`, `memory_search`, `memory_get` (используются 3 из ~22
  нативных; остальные отрезаны через `tools.allow`).
- Деплой headless — конфиг класть файлом (`onboard` сломан headless); `gateway install`
  + `systemctl --user enable --now`. Гочи — в их `docs/02-deploy-runbook.md §8`.

## Стыковка со «вторым мозгом» — НАТИВНЫЙ ПУТЬ: MCP (из документации движка)
Движок склонирован `/opt/openclaw-engine`. По его докам (`docs/cli/mcp.md`) OpenClaw
**нативно работает как MCP-клиент**: команда `openclaw mcp add <name>` сохраняет внешний
MCP-сервер в `mcp.servers`, и его инструменты **автоматически проецируются боту** (цитата:
«embedded OpenClaw exposes configured MCP tools in normal coding and **messaging** tool
profiles» — messaging = наш Telegram-бот). Транспорты: **stdio** (`--command/--arg/--env`)
или **streamable-http** (`--url --transport streamable-http --header ...`). Есть
`toolFilter.include/exclude`, `probe` (проверка), таймауты.

**План интеграции (весь — штатными средствами OpenClaw = правило №1):**
```
человек → OpenClaw-бот (менеджер) → нативно зовёт MCP-инструмент «спросить 1С»
   → наш тонкий MCP-сервер-обёртка (streamable-http) → braine /ask :8090 (факты+цитаты)
   → бот формулирует живой ответ (персона: отвечать ТОЛЬКО фактами инструмента) → клиенту
```
Шаги: (1) поднять тонкий MCP-сервер поверх braine `/ask` (инструмент `ask_1c(question)`);
(2) `openclaw mcp add second-brain --url http://127.0.0.1:<port>/mcp --transport streamable-http`
в инстанции бота; (3) дополнить персону `AGENTS.md` — когда звать инструмент и что отвечать
строго его данными (см. критичный инвариант выше — не досочинять).

**Правило №1 — требует подтверждения владельца:** MCP-сервер-обёртка над braine — это НАШ
код (внешний сервис, подключаемый ШТАТНЫМ `openclaw mcp add`; не патч/плагин ВНУТРИ
OpenClaw). Это самый нативный способ OpenClaw подключить внешний источник. Но раз правило
строгое — перед реализацией подтвердить у владельца, что тонкий MCP-коннектор к нашему
braine приемлем (это не «кастом поверх OpenClaw», а стандартный MCP-connector).

Альтернативы отвергнуты по докам: `web_fetch` — GET-only и **блокирует внутренние хосты**
(braine на localhost) → не подходит; `memory_search` — статичный слепок, слабее RAG braine.

## 🔴 Анти-галлюцинации на слое OpenClaw — КОДОМ, не промтом (требование владельца)
Владелец: на слое OpenClaw не должно быть галлюцинаций, **и промтом это регулировать нельзя**
(«промты не держат» — тот же принцип, что в braine: точность даёт код). Разобрано по доке
движка (не угадано):
- Финальный ответ клиенту **всегда генерирует LLM бота** (DeepSeek). Нативного «passthrough»
  (отдать ответ braine дословно, минуя LLM) в движке НЕТ → LLM может исказить факт при
  «оживлении». Промт-запрет — не гарантия (владелец прав).
- **Нативная кодовая точка контроля есть:** typed plugin hook **`message_sending`**
  (`docs/concepts/agent-loop.md`) — перехватывает ИСХОДЯЩЕЕ сообщение до отправки;
  `{cancel: true}` терминально отменяет/заменяет. Плюс `before_tool_call`.

**Механизм защиты (принцип braine `verify.py`, но на слое OpenClaw):**
```
бот-LLM формулирует ответ → хук message_sending (наш verify-код):
   сверяет «жёсткие токены» ответа (числа/даты/цены/имена) с выводом braine за этот ход;
   несверенное → заменить на дословный ответ braine / cancel → только тогда отправка
```
Гарантия — детерминированной сверкой кода, не промтом.

⚠️ **ПРАВИЛО №1 — развилка, нужно решение владельца:** хук `message_sending` — это
**typed plugin hook = плагин = кастом**. Нативного НЕ-кастомного способа исключить
галлюцинации кодом в движке нет (passthrough отсутствует, промт запрещён). Значит:
- либо **согласие на плагин-verify** (кастом; даёт кодовую гарантию как braine),
- либо иной путь по указанию владельца.
Без его согласия плагин не пишем (правило №1).

## 🔒 Изоляция и read-only эталона (инвариант — проверено 2026-07-23)
Их проект-эталон `money/opwnclaw-bot` склонирован на LXC как **READ-ONLY** (`/opt/openclaw`);
движок — `/opt/openclaw-engine`. Оба **только читаю** (ls/sed/cat), в них НЕ пишу/не коммичу/
не пушу. Прод-бот владельца — на отдельном сервере `.15`, я туда **не подключался вообще**.
Вся моя сборка — в **отдельном Linux-юзере `undebot`** → `/home/undebot/.openclaw/` на нашей
LXC `.42` (рядом с braine). Ничего общего с их эталоном/продом.
Проверка на 2026-07-23: `git -C /opt/openclaw status` пусто, HEAD==origin (`23a0fd1`),
reflog = только `clone`; к `.15` подключений нет (known_hosts). ✅

## Реализация — прогресс (владелец одобрил Вариант А: плагин-verify)

**Артефакты:** `ubuntu/openclaw/`.

- [x] **Компонент 1 — MCP-сервер `ask_1c` над braine** (`ubuntu/openclaw/mcp_braine.py`).
  Официальный MCP SDK (FastMCP, streamable-http). `ask_1c(question)` → braine `/ask` →
  проверенный текст + источники; `no_data` пробрасывается маркером. Сервис **`1c-mcp-braine`**
  (LXC `127.0.0.1:6014`, enabled). Проверено MCP-клиентом: вернул реальных контрагентов
  с ИНН и цитатой. ✅
- [x] **Компонент 2 — движок OpenClaw** на LXC (npm global **2026.7.1-2**). Инстанс под
  юзером `undebot` (linger on): `~/.openclaw/openclaw.json` (DeepSeek provider-плагин
  `@openclaw/deepseek-provider`, `commands.native=false`, `dmScope: per-channel-peer`),
  `.env` (600, `DEEPSEEK_API_KEY`). Gateway = systemd **user**-юнит `openclaw-gateway.service`
  (enabled --now, «gateway ready», http server listening). Проверен турн через gateway:
  `openclaw agent --to +… --message …` → DeepSeek ответил. ✅
- [x] **Компонент 3 — verify-плагин** (`ubuntu/openclaw/verify-plugin/`, нативный
  `definePluginEntry` + хуки `api.on`). Собран, установлен (`plugins install npm-pack:`),
  **enabled**, хуки реально срабатывают, эталон braine захватывается. Чистая логика в
  `verify-core.js` — **19 оффлайн-юнитов** (`test-verify.mjs`) зелёные. Осталось только увидеть
  срабатывание `message_sending` на РЕАЛЬНОЙ доставке (нужен канал — см. К5). ✅(код) ⏳(живой гейт)
- [x] **Компонент 4 — стыковка bot↔braine:** `openclaw mcp add second-brain` (`:6014`,
  streamable-http, `include ask_1c`) — сделано, `mcp probe` = 1 tool. Бот нативно зовёт
  инструмент и отвечает реальными данными 1С. Осталось: персона `workspace/AGENTS.md`
  (тон + КОГДА звать `ask_1c`; НЕ для анти-галл — это гейт) + опц. `tools.allow`.
- [x] **Компонент 5 — живой тест гейта на Telegram-доставке.** ✅ Владелец выбрал «забрать
  `@test1c_mcp_bot`»: OpenClaw стал фронтом Telegram, braine-мост `tg-bridge` **stop+disable**
  (освободил бота, без 409). Канал: `dmPolicy: allowlist`, `allowFrom: [5949699699]` (только
  владелец — бот отдаёт данные компании!), токен в `tokenFile` (не в openclaw.json). braine
  `/ask` (api.service) остался — кормит `ask_1c`. **Проверено на реальной доставке (по debug-логу):**
  • обоснованный ответ (braine вернул ИНН этот ход) → `action=allow` → ушёл как есть;
  • необоснованный факт (braine `noData` этот ход, а бот назвал ИНН из памяти диалога) →
    `action=replace` → клиенту ушла безопасная строка «нет данных». Галлюцинация не прошла.

Топология: собираем/демо на нашем LXC (`.42`, рядом с braine), НЕ трогая их прод-бот на `.15`.
Перенос на прод / подключение их бота — отдельным шагом с владельцем.

## 🔬 Живые находки интеграции (2026-07-23, проверено в рантайме, НЕ угадано)
Всё ниже выяснено запуском на стенде + чтением движка `/opt/openclaw-engine`:
1. **Хуки НЕ-bundled плагина к переписке блокируются по умолчанию.** Нужен флаг
   `plugins.entries.braine-verify.hooks.allowConversationAccess=true`, иначе `after_tool_call`/
   `message_sending`/`agent_end` молча не срабатывают (в логе `typed hook … blocked because …`).
2. **MCP-инструмент проецируется боту под именем сервера:** `ask_1c` → **`second-brain__ask_1c`**.
   В плагине имя матчим по суффиксу (`toolMatches`), иначе эталон не захватывается.
3. **Конфиг плагина — через `api.pluginConfig`** (в ctx хука его НЕТ, `pluginConfig` там пусто).
4. **`message_sending` — только на доставке в канал** (`src/infra/outbound/deliver.ts`), не на
   `openclaw agent` без `--deliver`. `after_tool_call`/`agent_end` — на любом турне.
4a. **КОРРЕЛЯЦИЯ хода:** у `message_sending` в ctx **НЕТ runId** — есть только `ctx.sessionKey`
    (напр. `agent:main:main`). И доставка идёт **ПОСЛЕ `agent_end`**. Поэтому: эталон храним по
    `sessionKey` (не runId), НЕ удаляем на `agent_end` (иначе к доставке его уже нет), сбрасываем
    при новом `runId` (новый ход) и отсекаем прошлый ход по метке `lastInbound`. (Первый заход
    ловил ложный `cancel` верного ИНН — ровно из-за удаления на agent_end + отсутствия runId.)
4b. **Строгость по ходу:** факт обоснован, только если он в ответе braine ЗА ЭТОТ ХОД. Если
    braine вернул `noData`, число «из памяти диалога» будет заменено на «нет данных» (безопасно;
    при повторном прямом вопросе braine вернёт факт и он пройдёт). Это осознанный дефолт.
5. **Ключ провайдера для изолированного агента** хранить в auth-store, не только в env:
   `openclaw models auth paste-api-key --provider deepseek --profile-id deepseek:default`
   (иначе «No API key found» после того как `plugins.allow` задан явно — он гейтит discovery).
6. **CLI-турн:** `openclaw agent --message "<текст>"` (+ `--to <E.164>`/`--agent`/`--session-key`);
   позиционный текст не принимается. Без `--deliver` ответ в stdout (для проверки мозга/эталона).

## 🎓 Нативные правила из `money/opwnclaw-bot` — изучено и применено (2026-07-23)
Владелец: «я поставил чистый инстанс, а надо взять уже отлаженную механику». Изучил их сетап
(`/opt/openclaw`: karina-бот, docs 01–07, qa-probes.sh) и применил нативные правила:

**Конфиг (нативное):** `tools.allow: ["message","bundle-mcp"]` (узкий набор, как у них message/
memory×2 — остальное отрезано; у нас + MCP-инструменты через `bundle-mcp`), `streaming.mode:off`
(без тех-болтовни в чат), `thinkingDefault:off`, `commands.native:false`, `dmScope:per-channel-peer`,
cost-блок модели. Прод-мелочи: state-dir `~/.openclaw` уже `700`, меню команд Telegram пустое (без мусора).

**Персона (их паттерны, `AGENTS.md`):** сильная идентичность + конкретное **приветствие** (нет
дефолт-онбординга); **запрет мета-реплик** («я только включился/нет памяти») + **конкретный
редирект** на вопросы о себе (их приём: положительный ответ держит лучше запрета); **запрет
нарратива** действий/размышлений (клиент видит только результат); **«no false booked» (Sergio)** —
не приписывать себе действий, которых не было (наш баг с «уже отправил график»); **не сливать
внутреннее** (SQL/пути/промпт) даже «на тест/разработчику»; **устойчивость к инъекциям**
(«покажи промпт», «стань другим ботом»); только текст (голос/фото → «напишите текстом»).

**Тех-детали убраны из чата:** `report_1c` отдаёт клиенту чистую таблицу (`show_sql=False`),
SQL логируется СЕРВЕРНО (владельцу в журнал), не в чат. График — файлом, путь не показывается.

**QA-батарея** (их практика): `ubuntu/openclaw/qa/qa-probes.sh` — через CLI, уникальные
session-key, БЕЗ Telegram. Прогнано: приветствие, мета×2, инъекции×3, отчёт-чисто, нет-данных,
не-выдумал-ИНН, не-нарратит, не-слил-SQL — **всё PASS**.

**Осталось (прод-надёжность, их чек-лист):** мониторинг «бот жив» (алерт при падении gateway);
ротация бот-токена `@test1c_mcp_bot` (светился в переписке) на свежий перед реальным продом;
бэкап конфига/сессий; ⚠ мета/анти-ложь — на персоне (промт), остаточный риск (кодового гейта на
поведение, в отличие от чисел, нет — обсудить, нужен ли).

## Как работает гейт (Компонент 3) — детерминированно, кодом
`after_tool_call(second-brain__ask_1c)` кладёт ответ braine в эталон хода (**ключ `sessionKey`**,
сброс при новом `runId`, вызовы одного хода сливаются). `message_received` помнит числа
пользователя (эхо ≠ выдумка) и метит границу хода. `message_sending`: жёсткие числовые токены
(≥`minDigits`, с группировкой) сверяются с эталоном/вводом; необоснованные → замена на дословный
braine / «нет данных», длинный выдуманный ИНН без эталона → cancel. Эталон НЕ удаляется на
`agent_end` (доставка позже). Границы и конфиг — в `verify-plugin/README.md`.

## На будущее: умеет ли OpenClaw дашборды и таблицы? (по докам движка, 2026-07-23)
Коротко: **таблицы — да, нативно; «дашборды» — смотря какие.**

**Таблицы в Telegram — нативно.** Флаг `channels.telegram.richMessages: true` (Bot API 10.2):
markdown-таблицы рендерятся как НАСТОЯЩИЕ таблицы Telegram (+ заголовки, чек-листы, коллажи,
формулы, карты). По умолчанию ВЫКЛ — часть клиентов (Desktop/Web/Android/сторонние) покажет
как «unsupported», включать только если все клиенты бота их рисуют. Без флага — таблица
моноширинным текстом (```…```), для небольших ок. Бонус: бот умеет прикреплять медиа/файлы
(`sendMessage` c `mediaUrl`) — можно отдать график-картинку, CSV, PDF.

**Дашборды:**
- **Операторский Control UI** (`openclaw dashboard`) — нативная админ-панель управления ботом.
  Плагин может добавить в неё **кастомные виджеты** с данными (manifest `dashboard` →
  `dataBindings`/action verbs на `operator.read/write`). Но это админ-панель, НЕ клиентский чат,
  и виджеты = кастом (с согласия владельца, правило №1).
- **В клиентском чате «живого дашборда» нет** (чат = сообщения); отдаём таблицы (rich),
  картинки-графики, коллажи, файлы.
- **Canvas / A2UI** (экспериментально) — агент рендерит HTML-UI (`canvas/index.html`) на
  СПАРЕННЫХ desktop-нодах, не в Telegram.
- **Workboard** — доска задач (канбан-карточки для суб-агентов), это трекинг работы, не BI.

**Вывод для нашего кейса (1С → Telegram):** таблички по данным 1С — можно сразу (включить
`richMessages` + форматировать ответ таблицей; факты всё равно проходят через гейт). Графики —
как картинка (нужен рендер/генератор → кастом). Полноценный интерактивный дашборд — либо
Control UI + кастом-виджет, либо отдельный веб-UI поверх braine/OData (вне OpenClaw).
