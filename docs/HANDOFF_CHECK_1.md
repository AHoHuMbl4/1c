# Проверка документов перед стартом на сервере — отчёт №1

Дата проверки: **2026-07-27**, вечер. Проверяющий работал один, только на чтение:
ни один файл, ни одна таблица, ни одна служба не менялись. `ivf` не трогался.

Метод: каждое фактическое утверждение документов сверено выводом команды на живом
сервере `192.168.56.42` или запросом `SELECT` к движку на `127.0.0.1:7890`.
Ниже — дословные выводы.

---

# 1. Полнота копии на сервере — ✅ расхождений нет

## Число файлов

```
локально  /srv/data/cursor/cursor/1/1c : 178 файлов (без .git),  1747 (с .git)
сервер    /srv/1c                      : 178 файлов (без .git),  1747 (с .git)
```

## Пофайловая сверка

Список путей совпал полностью; сверка `md5sum` по всем 178 файлам:

```
ALL 178 FILES IDENTICAL BY MD5
```

## Каталоги, названные в задании

| Каталог | Локально | На сервере |
|---|---|---|
| `credentials/` | 6 файлов | 6 файлов, те же имена и размеры |
| `.claude/agents/` | 7 файлов | 7 файлов, размеры совпадают |
| `.claude/hooks/` | 4 файла, `+x` | 4 файла, `+x` |
| `.git` | есть | есть |

`credentials/`: `alibaba.env`, `deepseek.env`, `gitlab-1c-test.env`,
`gitlab-openclaw.env`, `mcp-toolkit.env`, `telegram-1c-bot.env` — все доехали.

`.claude/hooks/`: `check-diff.sh`, `check-golden.sh`, `check-write.sh`,
`count-edits.sh` — все доехали, права на выполнение сохранены.

## Git на сервере работает

```
# cd /srv/1c && git log --oneline -5
a999589 передаточный документ для продолжения на сервере
6b01d28 штатная сборка корпуса: три агента, сводка, три ревизора, версия 2 с исправлениями
7cb5745 TARGET.md п.20: запрещено писать свои скрипты для работы с базой
64e5582 индекс не пересобирается каждый такт; вопросы приёмки вынесены в файл данных
01adfee поиск: родные функции движка вместо чужих из ядра DuckDB

# git rev-parse --abbrev-ref HEAD
main
# git remote -v
origin	https://<токен>@github.com/AHoHuMbl4/1c.git (fetch/push)
```

HEAD совпадает с машиной разработки: `a9995896d8516b4fdce89336533387aadeec7b5e`.

## ⚠ Одно расхождение, мелкое, но проявится завтра

```
# cd /srv/1c && git status --short
?? .claude/settings.local.json
```

На машине разработки этот файл невидим для git, потому что исключён **глобальным**
конфигом разработчика, а не `.gitignore` репозитория:

```
$ git check-ignore -v .claude/settings.local.json
/home/ahohum/.config/git/ignore:1:**/.claude/settings.local.json
```

На сервере такого глобального файла нет, поэтому локальные разрешения на команды
(в них попадают куски командных строк с путями и SSH-ключами) будут висеть
неотслеженными и рискуют уехать в первый же `git add -A`. Лечится строкой
`.claude/settings.local.json` в `.gitignore` репозитория.

---

# 2. Достоверность `docs/SERVER_HANDOFF.md`

## §2, окружение — ✅ всё подтвердилось дословно

| Утверждение документа | Факт на сервере |
|---|---|
| `git` 2.43.0 | `git version 2.43.0` ✅ |
| `node` v24.18.0 | `v24.18.0` ✅ (плюс `npm` 11.16.0) |
| `claude` **не установлен** | `which claude` → пусто ✅ |
| Python 3.12.3 в `/opt/openclaw-mcp/venv` | `Python 3.12.3` ✅ |
| Код сервисов в `/opt/1c-mcp-reports/` | каталог есть, 29 файлов ✅ |
| 62 ГБ памяти, 6 ядер | `free -g` → 62; `nproc` → 6 ✅ |
| свободно 220 ГБ | `/dev/loop3 360G 141G 220G 39% /` ✅ |
| GitHub доступен, 200 | `curl -o /dev/null -w %{http_code}` → `200` ✅ |

## §2, службы — ✅ все восемь строк верны, но список **неполон**

```
# systemctl is-active …
serenedb             active=active     sub=running
1c-serene-ask        active=active     sub=running
1c-odata-gateway     active=active     sub=running
1c-mcp-braine        active=active     sub=running
1c-mcp-reports       active=active     sub=running
1c-config-ui         active=active     sub=running
1c-serene-sync       active=failed     sub=failed
1c-serene-index      active=failed     sub=failed
```

Все восемь состояний совпали со строками таблицы §2 дословно.

🔴 **Но `systemctl --failed` показывает ТРИ упавших юнита, а документ называет два:**

```
UNIT                    LOAD   ACTIVE SUB    DESCRIPTION
● 1c-serene-index.service loaded failed failed …
● 1c-serene-sync.service  loaded failed failed …
● nightly-eval.service    loaded failed failed smart-bot nightly golden e2e (регресс качества, алерт при FAIL)
```

`nightly-eval` — ночной прогон золотого набора с алертом при провале, то есть сторож
качества ответов. Он лежит, и `SERVER_HANDOFF.md` об этом не говорит **ни слова**:
его нет ни в таблице служб §2, ни в списке «найдено, но не починено» §6.
Завтрашний Claude по этому документу решит, что регресс-контроль работает.

Порты подтверждены живьём, все на loopback:

```
127.0.0.1:7890  serened          127.0.0.1:8091  serene-ask
127.0.0.1:6011  odata-gateway    127.0.0.1:6012  config-ui
127.0.0.1:6014  mcp-braine       127.0.0.1:6015  mcp-reports
```

Версия движка: `PostgreSQL 18.3 (SereneDB 26.07.3)` — совпадает с тем, что пишут
`activeContext.md` и агент `serenedb-native`.

## §2, таймеры — ⚠ перечислены три из четырёх

Документ: «`1c-serene-sync` (03:40), `1c-serene-index` (03:55), `1c-bot-monitor` (каждые 3 мин)».

Живой вывод `systemctl list-timers`:

```
Mon 2026-07-27 14:54:06 UTC  … 1c-bot-monitor.timer   → 1c-bot-monitor.service     (каждые 3 мин) ✅
Tue 2026-07-28 03:40:00 UTC  … 1c-serene-sync.timer   → 1c-serene-sync.service     ✅
Tue 2026-07-28 03:40:00 UTC  … nightly-eval.timer     → nightly-eval.service       ← НЕ НАЗВАН
Tue 2026-07-28 03:55:00 UTC  … 1c-serene-index.timer  → 1c-serene-index.service    ✅
```

Четвёртый таймер, `nightly-eval.timer`, в документе отсутствует. Он стреляет в 03:40
одновременно с синком и вызывает службу, которая в `failed`.

Отдельно: таймера `1c-etl` (03:00), который упомянут в архивном блоке
`memory_bank/activeContext.md:226`, на сервере **нет вообще** — запись протухла.

## §2, env-файлы — ✅ состав верен, ❌ утверждение о правах неверно

Документ перечисляет 11 файлов «в `/etc/`, права `600`». Состав совпал ровно, все 11:

```
-rw-r--r-- 1 root root  49 Jul 26 07:49 /etc/1c-bot-monitor.env     ← 644, НЕ 600
-rw------- 1 root root 146 Jul 26 08:28 /etc/1c-config-ui.env
-rw------- 1 root root 245 Jul 26 08:28 /etc/1c-etl.env
-rw------- 1 root root  98 Jul 22 18:06 /etc/1c-gateway.env
-rw------- 1 root root  89 Jul 26 09:38 /etc/1c-mcp-braine.env
-rw------- 1 root root 700 Jul 26 09:38 /etc/1c-mcp-reports.env
-rw------- 1 root root 146 Jul 26 06:32 /etc/1c-odata-gateway.env
-rw------- 1 root root 563 Jul 26 17:42 /etc/1c-pg-windows.env
-rw------- 1 root root 152 Jul 27 11:25 /etc/1c-s3.env
-rw------- 1 root root  60 Jul 27 03:25 /etc/1c-serene-ask.env
-rw------- 1 root root 370 Jul 26 08:28 /etc/1c-serene-sync.env
```

`/etc/1c-bot-monitor.env` имеет права **644**, читается любым пользователем. В нём
лежит адресат телеграм-алерта. То же утверждение про «права 600» продублировано в
`CLAUDE.md:123` в разделе инвариантов — то есть инвариант нарушен, и оба документа
об этом не знают.

## §6, падение сборки индекса — ✅ подтверждено полностью, вместе с причиной

Юнит на сервере, дословно:

```
# systemctl cat 1c-serene-index.service
[Service]
Type=oneshot
WorkingDirectory=/opt/1c-mcp-reports
EnvironmentFile=/etc/1c-serene-sync.env
EnvironmentFile=-/etc/1c-mcp-reports.env
ExecStart=/opt/openclaw-mcp/venv/bin/python /opt/1c-mcp-reports/serene_search_build.py
```

Содержимое обоих файлов:

```
/etc/1c-serene-sync.env :SERENEDB_DSN=host=127.0.0.1 port=7890 user=postgres
/etc/1c-mcp-reports.env :SERENEDB_DSN=host=127.0.0.1 port=7890 user=serene_ro dbname=postgres
```

Журнал последнего запуска:

```
Jul 27 03:55:00 … Starting 1c-serene-index.service …
Jul 27 03:55:00 … RuntimeError: DDL: CREATE TABLE IF NOT EXISTS search_corpus (src_table TEXT,
                  row_key TEXT, doc TEXT, refs TEXT, doc_hash TEXT, amount DOUBLE,
                  doc_date TIMESTAMP, emb FLOAT[1536]);
Jul 27 03:55:00 … psql:<stdin>:1: ERROR:  permission denied for schema public
Jul 27 03:55:00 … 1c-serene-index.service: Failed with result 'exit-code'.
```

Диагноз документа — «побеждает последний `EnvironmentFile`, в сборку попадает
read-only роль `serene_ro`» — **подтверждаю**: порядок в юните именно такой,
второй файл действительно переопределяет `SERENEDB_DSN` на `serene_ro`, а
`serene_ro` не имеет права создавать объекты в `public`. Вывод документа «по
расписанию индекс не пересобирался ни разу» согласуется с журналом.

## §6, падение синка — ✅ подтверждено дословно, включая время и число

```
Jul 27 06:53:37 … resolver_index: SQL error: psql: error: connection to server at
                  "127.0.0.1", port 7890 failed: Connection refused
Jul 27 06:53:37 … ⚠ ИТОГ: 9 сущностей не загрузились
Jul 27 06:53:37 … 1c-serene-sync.service: Main process exited, code=exited, status=3/NOTIMPLEMENTED
Jul 27 06:53:37 … Failed to start 1c-serene-sync.service …
Jul 27 06:53:37 … 1c-serene-sync.service: Consumed 5min 41.484s CPU time.
```

Время (27.07, 06:53), причина (`Connection refused`, движок лежал) и «9 сущностей
не загрузились» — всё сходится буква в букву.

## §8, инварианты — ✅ все три верны

```
SELECT count(*) FROM search_corpus;                   →  97965
SELECT count(DISTINCT src_table) FROM search_corpus;  →  226
SELECT index_name FROM duckdb_indexes();              →  search_idx
```

Дополнительно: `SELECT count(*) FROM search_corpus WHERE emb IS NULL` → `0`,
то есть «все с эмбеддингом» тоже верно.

## §8, `golden.sh` — ✅ существует, ✅ работает, ✅ эталон совпал

`-rwxr-xr-x 1 root root 2386 Jul 27 12:28 /opt/1c-mcp-reports/golden.sh`

Прогон целиком (`bash /opt/1c-mcp-reports/golden.sh`):

```
сколько всего контрагентов     4439 мс  answer  Всего контрагентов: 13.
какой ИНН у Ромашки            4797 мс  answer  ИНН: 7701012342
на какую сумму продано за год  4716 мс  answer  За год продано на сумму 12326000 руб.
сколько продали в декабре      4370 мс  answer  В декабре продано на сумму 2 456 400 руб. (6 документов). …
какая самая крупная продажа    4670 мс  answer  Самая крупная продажа — 1 629 700 руб. (документ ТС00-000022 …)
кто наш поставщик              4333 мс  answer  В предоставленных данных нет информации о том, кто является поставщиком …
список договоров               6211 мс  answer  Вот список договоров (11 шт.) …
сколько документов в базе      3953 мс  answer  В базе 11 типов документов.
— вопросов: 8
```

Восемь вопросов ✅, все вернули `kind=answer` ✅, ни одного пустого ответа ✅.
Эталонные числа §8 — 13 / 7701012342 / 12 326 000 / 2 456 400 на 6 документов /
1 629 700 — совпали все пять.

Среднее время 4,68 с — согласуется с записью «8/8, среднее 4,8 с» в `activeContext.md`.

## §6, «остальное открытое» — точность отсылок

- **`serene_ask.py:119`** — попадание точное. Строка 119 действительно
  `return [l.split("\x1f") for l in p.stdout.split("\n") if l != ""]`.
- **«3 991 строка из 97 965 содержит перевод строки»** — сегодня замер даёт
  **4 009**:
  `SELECT count(*) FROM search_corpus WHERE doc LIKE '%'||chr(10)||'%'` → `4009`.
  Расхождение 18 строк, вывод не меняет (4,09 % вместо 4,07 %), но цифра в
  `AGENT_FINDINGS_STATUS.md:130` устарела.

---

# 3. Достоверность `memory_bank/activeContext.md`, раздел «С ЧЕГО НАЧАТЬ»

## Что подтвердилось

| Утверждение | Проверка | Итог |
|---|---|---|
| коммит `01adfee` существует | `01adfee0b9b9…`, 27.07 14:54, 4 файла | ✅ |
| в нём `array_cosine_similarity` заменён на `<=>` | в `serene_ask.py` три места: `ORDER BY array_cosine_similarity(emb, …) DESC` → `ORDER BY emb <=> …` | ✅ |
| коммит `64e5582` существует | `64e558261ad2…`, 27.07 15:25, 4 файла | ✅ |
| в нём отпечаток DDL уехал в `search_meta` | в диффе: `META = "search_meta"`, `SELECT v FROM search_meta WHERE k='index_ddl'`, `INSERT INTO search_meta VALUES ('index_ddl', …)` | ✅ |
| в нём же вопросы приёмки вынесены в файл | добавлены `golden-questions.txt` (+19) и `golden.sh` (+48) | ✅ |
| в `search_meta` есть строка `index_ddl` | `index_ddl\|CREATE INDEX search_idx ON search_corpus USING inverted(doc search_dict, refs search_dict, src_table) INCLUDE (src_table, row_key, amount, doc_date);` | ✅ |
| `search_corpus_bak` **не содержит** `refs` | `search_corpus_bak → src_table,row_key,doc,doc_hash,amount,doc_date,emb`, против `search_corpus → src_table,row_key,doc,refs,doc_hash,amount,doc_date,emb`; плюс в ней 97 085 строк против 97 965 | ✅ |

## 🔴 Что НЕВЕРНО: «первое действие — Шаг 0» уже сделано

`SERVER_HANDOFF.md §4`, `activeContext.md` и `CLAUDE.md` втроём говорят завтрашней
сессии одно и то же: **«Первое действие — Шаг 0: резервная копия корпуса со всеми
колонками»** и **«затем Шаг 1: сверить отпечатки построчно»**.

На сервере обе работы уже лежат сделанными:

```
search_corpus_backup_20260727 | src_table,row_key,doc,refs,doc_hash,amount,doc_date,emb
                              | 97965 строк, emb IS NULL → 0
tmp_prod_corpus               | src_table,row_key,doc,refs,doc_hash
                              | 97965 строк
```

Имя `search_corpus_backup_20260727` — ровно тот шаблон, который предписывает
`CHECK2_INCR_SUMMARY_V2.md:149`: `CREATE TABLE search_corpus_backup_<дата> AS SELECT * FROM search_corpus`.
Состав колонок полный, включая `refs` и `emb`. То есть **Шаг 0 выполнен**.

Сверка отпечатков (это и есть Шаг 1) даёт полное совпадение:

```
SELECT count(*) FROM search_corpus c JOIN search_corpus_backup_20260727 b
  USING(src_table,row_key) WHERE c.doc_hash = b.doc_hash;   → 97965

SELECT count(*) FROM search_corpus c JOIN tmp_prod_corpus t
  USING(src_table,row_key) WHERE c.doc_hash = t.doc_hash;   → 97965
SELECT count(*) FROM search_corpus c JOIN tmp_prod_corpus t
  USING(src_table,row_key) WHERE c.doc_hash <> t.doc_hash;  → 0
```

`tmp_prod_corpus` собран с колонками `doc`, `refs`, `doc_hash` — состав в точности
как у боевого корпуса за вычетом `emb`/`amount`/`doc_date` — и его отпечатки
совпадают с боевыми **на всех 97 965 строках, расхождений ноль**. Это ровно тот
ответ, который Шаг 1 должен был дать: **разовый пересчёт эмбеддингов не нужен**.

Ни `tmp_prod_corpus`, ни `tmp_refmap` (39 376 строк, колонки `g`, `nm` — карта
GUID→наименование) в документах проекта **не упомянуты ни разу**:

```
$ grep -rn "tmp_prod_corpus\|tmp_refmap" .   → ничего, кроме .claude/settings.local.json
```

**Чем это опасно завтра.** Claude Code прочитает «первое действие — Шаг 0», сделает
вторую копию корпуса (ещё +600 МБ вектора на диск) и заново прогонит сверку, на
которую уже есть ответ. Либо, что хуже, увидит `tmp_prod_corpus` в списке таблиц,
не найдёт его ни в одном документе и **удалит как мусор**, потеряв результат Шага 1.

Оговорка честности: происхождение `tmp_prod_corpus` я подтвердить не могу — в
документах его нет, а служба его не создаёт. Утверждаю только факт: таблица с таким
составом и такими отпечатками на сервере есть.

## 🔴 «Мусорных таблиц нет» — уже не так

`activeContext.md:166` после разбора инцидента с WAL: «Проверено после: корпус 97 965
…, мусорных таблиц нет».

Сейчас в `public` 237 таблиц, из них 226 — источники витрины, а 11 служебные:

```
base_profile                    4585 строк
resolver_index                 60552
search_corpus                  97965
search_corpus_bak              97085   ← старая копия без refs
search_corpus_backup_20260727  97965   ← копия Шага 0
search_meta                        1
search_tables                    226
search_tables_1536               226   ← не упомянута нигде, кроме CHECK1_REVIEW_1.md:179
search_tables_bak                227   ← то же
tmp_prod_corpus                97965   ← не упомянута нигде
tmp_refmap                     39376   ← не упомянута нигде
```

Пять из одиннадцати — остатки опытов, четыре из них без единого упоминания в
документах. Запись «мусорных таблиц нет» на сегодня ложна.

## Прочее по разделу

- «Золотой набор 8/8, среднее 4,8 с» — подтверждено прогоном (8/8, 4,68 с). ✅
- «резолвер 60 552» — `SELECT count(*) FROM resolver_index` → `60552`. ✅
- «226 сущностей» — ✅ (см. §6 ниже про 235).
- «`search_idx` на месте» — ✅.

---

# 4. `CLAUDE.md` и `TARGET.md`

## Файлы по ссылкам — все существуют

Все markdown-ссылки `CLAUDE.md` ведут на реальные файлы: `CHANGELOG.md`,
`memory_bank/activeContext.md`, `docs/HOW_NOT_TO.md`, `TARGET.md`,
`docs/PRODUCTION_PLAN.md`, `docs/CHECK2_INCR_SUMMARY_V2.md`. Проверено `test -e`.

Файлы, названные в `CLAUDE.md` без ссылки (таблица «куда что писать»), тоже все на
месте: `HOW_IT_WORKS.md`, `PRODUCTION_PLAN.md`, `SCALE_BLOCKERS.md`,
`CHECKLIST_SEARCH_FIX.md`, `techContext.md`, `VECTOR_DECISION.md`,
`PLAN_VECTOR_CHECKS.md`, `SERENEDB_IVF_ISSUE_RU.md`, `RUNBOOK_DEPLOY.md`,
`ARCHITECTURE.md`, `README.md`, `SERENEDB.md`, `AGENT_FINDINGS_STATUS.md`,
`INSTALL_LOG.md`.

## 🔴 `CLAUDE.md` сам себе противоречит в числе пунктов TARGET

```
CLAUDE.md:62   | [`TARGET.md`](TARGET.md) | утверждённый контракт результата, 19 пунктов …
CLAUDE.md:136  | 1. [`TARGET.md`](TARGET.md) — чем продукт обязан быть. 20 пунктов, менять нельзя.
```

В `TARGET.md` **20** пунктов (последний — «ЗАПРЕЩЕНО ПИСАТЬ СВОИ СКРИПТЫ…»,
добавлен коммитом `7cb5745`). Строка 62 осталась от версии до этого коммита.
Строка 62 — та самая, что запрещает менять файл, то есть врёт в самом чувствительном
месте: читающий может решить, что п. 20 кто-то дописал самовольно.

## Номера пунктов, на которые ссылаются другие документы — ✅ все верны

Проверены все 23 отсылки вида «п. N `TARGET.md`» во всех `.md`, `.py`, `.sh`:

| Номер | Как называют в отсылке | Что в `TARGET.md` | Итог |
|---|---|---|---|
| п. 9 | без хардкода (`CHECK2_INCR_SUMMARY_V2:19`, `golden.sh:6`) | «Без хардкода и подстройки под конкретную базу» | ✅ |
| п. 11 | «пункт закрыт только замером» (`CHANGELOG:4`) | «Оставлять любой пункт нерешённым запрещено» | ✅ |
| п. 12 | уточнение/отказ (`RUNBOOK_DEPLOY:331`) | «Отказ и уточнение — не ошибка. Догадка — ошибка» | ✅ |
| п. 13 | полнота данных (6 отсылок) | «Полнота данных» | ✅ |
| п. 15 | набор из базы клиента (`golden.sh:7`) | «Проверка независимыми агентами… набор строится из самой базы клиента» | ✅ |
| п. 17 | свежесть 20 мин (3 отсылки) | «Свежесть… не позднее 20 минут» | ✅ |
| п. 18 | честный отказ (3 отсылки) | «При отказах — честный отказ» | ✅ |
| п. 19 | модель не считает (4 отсылки) | «Моделью не считаем и не ищем» | ✅ |
| п. 20 | запрет своих скриптов (6 отсылок) | «ЗАПРЕЩЕНО ПИСАТЬ СВОИ СКРИПТЫ…» | ✅ |

Ни одной сбитой нумерации не нашёл. Это единственный участок документации, где
сходится всё.

## ⚠ `CLAUDE.md:123` — инвариант, который на сервере нарушен

«**Секреты** — в `/etc/*.env` с правами 600» — см. §2: `/etc/1c-bot-monitor.env`
имеет 644.

---

# 5. Ссылки во всех документах — ✅ битых нет

Прогнал автоматическую проверку по всем `.md` в корне и в `docs/` (а заодно по
`memory_bank/`, `ubuntu/`, `windows/`): извлёк все ссылки вида `[текст](путь)`,
отбросил `http(s)://`, отрезал якоря, проверил существование относительно каталога
самого документа.

```
--- link scan done ---
```

Ни одной битой относительной ссылки — 0 из ~120 проверенных. Внешние ссылки
(`https://…` в `ANONYMIZATION.md`, `PLAN.md`) не проверялись: они ведут наружу и на
завтрашний старт не влияют.

## ⚠ Дубль номера раздела в `docs/HOW_NOT_TO.md`

```
127: ## 1.6 Решил за владельца
138: ## 1.6 Взял одну сторону противоречия и закрыл вопрос
```

Два разных раздела под номером 1.6, дальше нумерация продолжается с 1.7. Ссылок на
`§1.6` в других документах нет, поэтому вреда сейчас нет — но отсылка на него
станет двусмысленной.

---

# 6. Противоречия между документами

## Число строк корпуса — ✅ противоречий нет

`97 965` встречается в 20+ местах и везде одинаково. Живой замер: `97965`.
Иные числа рядом с ним (`96 931`, `97 085`, `98 508`) относятся к другим объектам и
каждый раз подписаны: `96 931` — множество из чужого замера [1] в `CHECK2_INCR_*`,
`97 085` — старая `search_corpus_bak`, `98 508` — оценка планировщика.

## Число сущностей — ⚠ четыре значения, разведены не везде

В документах ходят **226 / 233 / 235 / 237**. Живая база позволяет разложить их:

```
SELECT count(*) FROM information_schema.tables WHERE table_schema='public'; → 237
из них служебных (base_profile, resolver_index, search_*, tmp_*)            →  11
остаётся источников витрины                                                 → 226
SELECT count(DISTINCT src_table) FROM search_corpus;                        → 226
SELECT count(*) FROM search_tables;                                         → 226
```

То есть **226 — это правда для корпуса и витрины**, а 233/235 — числа из замеров,
где в счёт попали служебные таблицы разного состава (в `CHECK2_REVIEW_3.md:17`
прямо сказано: «обе эти таблицы входят и в 235»).

Опасность в том, что 235 стоит там, где читается как синоним 226:

```
memory_bank/activeContext.md:95 : «на данных, сверенных с 1С строка в строку по всем
                                   235 сущностям: 1С == витрина == корпус == индекс = 97 965»
memory_bank/activeContext.md:165: «корпус 97 965 …, 226 сущностей»
docs/SERENEDB.md:99             : «Корпус — 97 965 строк по 235 сущностям»
docs/CHECKLIST_SEARCH_FIX.md:54 : «строк / сущностей | 97 965 / 226»
docs/SERVER_HANDOFF.md:237      : «count(DISTINCT src_table) → 226»
```

Один и тот же корпус в одном файле имеет 235 сущностей, в другом 226, и оба
подписаны «сверено с 1С». Верно **226**. Ошибка признана в `HOW_NOT_TO.md:407`
(«сущностей (226 / 233 / 235)… четырьмя разными значениями как одна»), но сами
записи в `activeContext.md:95` и `SERENEDB.md:99` не исправлены.

## Число таблиц/колонок в `report_1c` — совпадает

`SERVER_HANDOFF.md:195` «465 КБ схемы» и `TARGET_ARCHITECTURE.md:68`
«464 292 символа… 233 таблицы, 3 462 колонки» — одна и та же величина, округление.
Мелочь: `AUDIT_CODE_2.md:295` даёт «233 таблицы / 3465 колонок / 2297», а
`TARGET_ARCHITECTURE.md:68` — «3 462 колонки… 2 294 текстовых». Расхождение на 3 в
обоих числах, замеры разных дней, вывода не меняет.

## Времена замеров — ✅ сходятся

Поиск `<=>` 435–451 мс (`activeContext`, коммит `01adfee`), перебор 262–338 /
280–322 / 313–314 мс в разных документах — все подписаны условиями замера,
противоречием не являются. Такт сборки «5 минут» и «86 %» повторяются одинаково в
`SERVER_HANDOFF §4`, `activeContext`, `SCALE_BLOCKERS`.

## 🔴 Отдельно: боевой код на сервере отстаёт от репозитория

Не спрашивалось прямо, но это ровно тот риск, о котором предупреждает
`SERVER_HANDOFF.md §3` («две разные копии… легко забыть скопировать и отлаживать
старый код»). Сверил `md5sum` 15 файлов `ubuntu/serenedb/*` против
`/opt/1c-mcp-reports/*`. **Тринадцать совпали, два разошлись:**

```
файл                        репозиторий                       /opt
ab_scorer.py                291a4f51f92d790fc1e05e33534dc5b5  6ec0127a456ad564745a81dde2f1c8fe
build_resolver_index.py     aebed547db3b9a61e9f2fc998edd0134  58de03d314af5ce59e4bcfa31f90a065
```

Направление — репозиторий новее:

```
/opt/1c-mcp-reports/build_resolver_index.py  3137 байт  2026-07-24 02:51
ubuntu/serenedb/build_resolver_index.py      3833 байт  2026-07-27 11:52
/opt/1c-mcp-reports/ab_scorer.py             6203 байт  2026-07-26 07:13
ubuntu/serenedb/ab_scorer.py                 6799 байт  2026-07-26 16:14
```

Что именно отстало в `build_resolver_index.py` — предупреждение о `ivf`:

```
на сервере: «в этой версии SereneDB CREATE INDEX USING HNSW недоступен — перебор
             косинуса; для сотен-тысяч значений добавить индекс»
в репозитории: «HNSW в SereneDB 26.07.3 НЕТ… Единственный векторный опкласс — ivf.
             НЕ создавать его поверх заполненной таблицы: сборка не прерывается
             (pg_cancel_backend возвращает успех…), снимается только рестартом движка»
```

То есть на боевом сервере лежит версия файла, которая **советует добавить векторный
индекс** — ровно то действие, которое трижды роняло движок и стоило трёх часов
простоя. Ключевая ловушка проекта в задеплоенном коде отсутствует.
`SERVER_HANDOFF.md` предупреждает о риске рассинхрона вообще, но не говорит, что
рассинхрон **уже есть и вот в этих двух файлах**.

Важное и хорошее: `serene_ask.py`, `serene_search_build.py`, `serene_sync.py`,
`serene_report.py`, `mcp_reports.py`, `golden.sh`, `golden-questions.txt` —
совпадают побайтово. Всё, что участвует в ответах бота и в сборке, задеплоено верно.

---

# ЧТО ИСПРАВИТЬ

По убыванию важности.

### 1. 🔴 Снять указание «первое действие — Шаг 0», оно уже выполнено

Правится в трёх местах сразу: `SERVER_HANDOFF.md §4`, `memory_bank/activeContext.md`
(раздел «С ЧЕГО НАЧАТЬ»), `CLAUDE.md` (раздел «Текущая работа»).

Записать факт: на сервере есть `search_corpus_backup_20260727` — полная копия
корпуса со всеми восемью колонками, 97 965 строк, `emb` заполнен везде. Шаг 0
закрыт. Есть `tmp_prod_corpus` — корпус, собранный SQL-формулой, 97 965 строк, и его
`doc_hash` совпадает с боевым **на всех 97 965 строках, расхождений ноль**. Значит и
Шаг 1 закрыт, а его вопрос — «нужен ли разовый пересчёт эмбеддингов» — имеет ответ
**нет**.

Пока это не записано, завтрашняя сессия либо повторит обе работы, либо примет
`tmp_prod_corpus` за мусор и удалит результат.

Если происхождение `tmp_prod_corpus` вызывает сомнение — пересверка стоит один
`SELECT count(*) … WHERE doc_hash <> doc_hash`, но начинать надо с неё, а не с
пересоздания копии.

### 2. 🔴 Внести в `SERVER_HANDOFF.md` третий упавший юнит — `nightly-eval`

`systemctl --failed` даёт три юнита, документ называет два. `nightly-eval` — ночной
прогон золотого набора с алертом владельцу при провале, то есть сторож качества
ответов, и он лежит. Добавить строку в таблицу служб §2, добавить
`nightly-eval.timer` (03:40) в перечень таймеров, добавить пункт в §6 «найдено,
но не починено».

### 3. 🔴 Донести на сервер `build_resolver_index.py` и `ab_scorer.py`

В `/opt/1c-mcp-reports/build_resolver_index.py` лежит версия от 24.07, где в шапке
написано «для сотен-тысяч значений добавить индекс». Ровно это действие роняет
движок в петлю OOM-рестартов. В репозитории файл уже исправлен 27.07.
Скопировать оба файла и записать в `SERVER_HANDOFF.md §3`, что рассинхрон был и в
чём состоял.

### 4. 🔴 Убрать «19 пунктов» из `CLAUDE.md:62`

Там же, где сказано «менять нельзя без слова владельца», написано неверное число.
В `TARGET.md` двадцать пунктов; строка 136 того же файла говорит «20». Исправить 62.

### 5. ⚠ Развести 226 и 235 в записях о корпусе

Верно **226** (проверено тремя способами на живой базе). Исправить
`memory_bank/activeContext.md:95` и `docs/SERENEDB.md:99`, где стоит 235 в значении
«сущностей корпуса». Урок уже записан в `HOW_NOT_TO.md:407` — сами записи не
поправлены.

### 6. ⚠ Описать пять служебных таблиц или снять запись «мусорных таблиц нет»

`search_tables_1536`, `search_tables_bak`, `tmp_prod_corpus`, `tmp_refmap`,
`search_corpus_bak` живут в базе; четыре из них не упомянуты нигде. Либо перечислить
их в `SERVER_HANDOFF.md` с пометкой «что это и можно ли удалять», либо снять
утверждение `activeContext.md:166` о чистоте базы. Удалять без разбора нельзя —
в `tmp_prod_corpus` лежит результат Шага 1.

### 7. ⚠ Привести в соответствие утверждение о правах 600 на секреты

`/etc/1c-bot-monitor.env` имеет 644. Либо `chmod 600` (изменение состояния сервера —
только по слову владельца), либо поправить `SERVER_HANDOFF.md §2` и `CLAUDE.md:123`,
чтобы инвариант не был ложным.

### 8. ⚠ Добавить `.claude/settings.local.json` в `.gitignore` репозитория

На машине разработки файл скрыт глобальным конфигом git, на сервере — нет. В нём
оседают куски командных строк с путями к SSH-ключам.

### 9. Мелочи

- `AGENT_FINDINGS_STATUS.md:130`: «3 991 из 97 965» — сегодня замер даёт **4 009**.
- `HOW_NOT_TO.md`: два раздела под номером `## 1.6` (строки 127 и 138).
- `memory_bank/activeContext.md:226` (архивный блок): таймера `1c-etl` (03:00) на
  сервере нет — либо пометить блок как архив явно, либо снять строку.
- `AUDIT_CODE_2.md:295` и `TARGET_ARCHITECTURE.md:68` расходятся на 3 колонки
  (3465/2297 против 3462/2294) — замеры разных дней, вывода не меняют.

---

# ВЕРДИКТ

**Стартовать завтра по этим документам можно — но пункты 1–4 обязаны быть
исправлены до старта, иначе сессия начнётся с потерянного дня.**

Что документы делают хорошо. Проверил 40+ фактических утверждений
`SERVER_HANDOFF.md` — версии, объёмы, восемь состояний служб, три инварианта базы,
пять эталонных чисел приёмки, диагноз падения индекса вместе с механикой
`EnvironmentFile`, время и содержание падения синка, — и **все они подтвердились**,
многие дословно. Диагноз «в сборку попадает `serene_ro`, потому что systemd берёт
последний `EnvironmentFile`» проверен по юниту, по обоим env-файлам и по журналу:
верен целиком. Копия репозитория на сервере полная, до последнего байта. Битых
ссылок нет ни одной. Нумерация пунктов `TARGET.md` во всех 23 отсылках верна.
Это редко для документации такого объёма.

Где документы врут, и почему это дорого. Все четыре критичных дефекта — одного
рода: **документ описывает состояние, которое было верно вчера вечером, а сервер
ушёл вперёд**.

Самое дорогое — пункт 1. Три документа в один голос говорят «первое действие —
сделать резервную копию», а копия уже сделана и лежит рядом с результатом
следующего шага. Указание, повторенное трижды, читается как проверенное, и
завтрашняя сессия пойдёт его выполнять. В лучшем случае это потерянное время и
ещё одна копия вектора на диске. В худшем — `tmp_prod_corpus` не найдётся ни в
одном документе, будет опознан как остаток опытов и удалён; вместе с ним пропадёт
единственное доказательство того, что новая SQL-формула даёт побайтово те же
отпечатки, и пересчёт 98 тысяч эмбеддингов не нужен.

Пункт 3 опаснее по последствиям, хоть и проще чинится: на боевом сервере лежит файл,
который советует построить векторный индекс поверх заполненной таблицы. Это ровно
то действие, которое трижды роняло движок и один раз положило инстанс на три часа.
Предупреждение в репозитории есть, на сервере — нет.

Пункт 2 — умолчание, а не ложь, но действует так же: раздел «что упало» перечисляет
два юнита из трёх, и молчаливо пропущен именно тот, который следит за качеством
ответов.

Общая оценка. `SERVER_HANDOFF.md` как карта местности точен. Как список
«что делать дальше» — отстал на один рабочий заход. Полчаса правок по пунктам 1–4
превращают его в документ, по которому можно начинать не глядя.

_Проверка выполнена только на чтение: ни один файл, ни одна таблица, ни одна служба
не изменены. `golden.sh` запускался — он делает POST-вопросы к отвечающему сервису и
данных не меняет._
