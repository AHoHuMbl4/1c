# K6 — ранг сущности: почему «клиенты» → справочник, а не продажи

База: **okna** (gpu-erw:2202, SereneDB `127.0.0.1:7890`, ask `:8091`).  
Дата замера: **27.08.2026**. `serene_ask.py` **не правили** (К5).

Контроль: «сколько клиентов реально покупают?» → эталон SQL **141**
(`COUNT DISTINCT` покупателя за 12 мес на `accumulationregister_реализациятмц`).
Бой отвечает `clarify` по мерам справочника `catalog_контрагенты`
(дней отсрочки / кредитный лимит / 353). Разбор обрыва: [`C4_K6_BREAKDOWN.md`](C4_K6_BREAKDOWN.md).
Флаг `ASK_ENTITY_FORM` оркестратором на `:8092` **закрыт** как гипотеза
(Q1 тот же мусор; Q2 хуже — догадка, п. 12).

Доки SereneDB (обязательный заход):  
[Ranking / Top-K](https://docs.serenedb.com/sql/indexes/inverted/ranking#top-k-queries-and-wand-pruning) —  
`@@` отбирает, scorer в `ORDER BY` ранжирует;  
[Relevance Tuning — Blend in a business signal](https://docs.serenedb.com/cookbook/search/boosting#blend-in-a-business-signal) —  
BM25 (или иной scorer) — обычное число, в `ORDER BY` можно умножать на
бизнес-сигнал (popularity и т.п.).

---

## 0. Сводка

| вопрос | факт |
|---|---|
| Чем выбирается источник? | Пул (буквально + смысл/синонимы) → эвристики prefer_* → порядок (эмбед+реранкер) → **модель** `pick_entity` → арбитр. Форма F (`distinct_axis`) за флагом, до модели режется гейтом. |
| Есть ли в ранге «сущность отвечает данными»? | **Нет** для `want=count`. Есть только отсев `nums` при `want=sum` и поздний `no_live_cells` в fork по **датному** окну. |
| Почему побеждает справочник? | Лексика: kind=`клиенты`, aliases «клиенты» → `catalog_контрагенты`; у карточек нет продаж, но модель этого не видит как «не отвечает». |
| Мера | `answer_fit` — live DISTINCT оси kind-catalog на регистре vs счёт карточек (считает база, без списка слов). |

---

## 1. Путь выбора сущности (`serene_ask.py`)

Читан код (сборка ~16200 строк, md5 `bf14c57a…` на момент разбора). Функции и роли:

| шаг | функция / место | что делает |
|---|---|---|
| 1 | `parse_intent` → `answer` ~12158 | kind / want / period; к данным не ходит |
| 2 | `probe` → `match_expr` → `tables_of` ~2936, 12297 | буквальный пул `by` (индекс `@@`) |
| 3 | `meaning_candidates` / `_fused_candidates` ~3088–3241 | синонимы + карточка + вектор (RRF) → `extra` |
| 4 | сборка `cands` ~12545 | `list(by)+extra` |
| 5 | `prefer_entity_for_rank` ~5169; `prefer_entity_for_sales` ~5428; `prefer_entity_for_catalog_count` ~5905 | структурный подъём **только** при своих гейтах (sales_sum / прайс / rank). На «покупают» **не срабатывают** (`sales_sum_intent` ложен) |
| 6 | отсев `not_enough_for` ~12610 | знание установки; каталог контрагентов «покупателей» не отсекает |
| 7 | `with_value` ~12696 | **только `want=="sum"`** — у count карточки с пустыми/чужими nums остаются |
| 8 | сито эмбеддинга + `rerank` ~12778–12873 | порядок **по смыслу названия**, не по ответу |
| 9 | детектор развилки / `fork_scan` ~4102 | атомы count/sum по пулу; `no_live_cells` — нет строк в **preds** (дата). У Q1 `period_leader=none` → справочник жив |
| 10 | `try_entity_form_answer` pre_entity ~13047, `entity_form_pre_entity_ok` ~2319 | F только при `ASK_ENTITY_FORM=1` и **`early_classes≤1`**. На Q1 classes=**16** → pre_entity **молча** |
| 11 | `pick_entity` ~8321 | модель по **названиям** + matching records + ключи nums; «есть ли ответ нужной формы» не подаётся |
| 12 | второй F ~13725 | без `when=pre_entity`; на боевом флаге 0 не зовётся. При флаге 1 мог бы сработать **после** выбора, но путь Q1 уходит в clarify меры раньше/вместо верного атома |

Формы F уже написаны: `entity_form_structs` / `distinct_axis` / `complement` (~2430–2528), ось через `search_refcols` + `_sales_register_score` (~2398).

---

## 2. Признак «отвечает ли сущность» — факт

### 2.1. Код

- Ранг пула: буквальные совпадения, tfidf/bm25/RRF по aliases/карточке, cosine по emb, реранкер по **label**. Это лексика/смысл имени.
- Единственный близкий отсев по данным — `with_value` при **sum** (есть ключи `nums`). Для **count** его нет.
- `fork_scan` / `no_live_cells` — про пустой счёт в текущем WHERE (часто дата). Без периода карточки справочника «живы».
- В `pick_entity` в промпт идут matching records и имена величин; **не** идёт «DISTINCT оси kind за окно = N» vs «карточек = M».

Комментарий в коде (~12709): признак «есть спрошенное поле» **уже пробовали и убрали** по замеру 30.07 — но то был stem полей, не live axis fit.

### 2.2. Живой diag okna (`:8091`, user=`k6-diag`)

Вопрос: «сколько клиентов реально покупают?»

| шаг | факт |
|---|---|
| разбор | `тип=клиенты`, `считать=count` |
| буквально | 351 сущностей (понятий 0 → широкий пул) |
| смысл/синонимы | +0 |
| кандидаты | 75 |
| fork | 16 классов; в `pool_srcs` есть и `catalog_контрагенты`, и **`accumulationregister_реализациятмц`** |
| сущность | **`catalog_контрагенты`**, `выбрал=модель` |
| `alias_top` | `catalog_контрагенты`, … |
| `period_leader` | `none` |
| ответ | clarify: дней отсрочки / кредитный лимит / 353 |

### 2.3. SQL на том же инстансе

| сигнал | значение |
|---|---|
| карточки контрагентов | **353** |
| DISTINCT Контрагент на `реализациятмц` за 12 мес | **141** |
| `doc_date` у каталога | 357 NULL / 0 дат |
| `doc_date` у регистра | 0 NULL / 77901 дат |
| `nums` каталога | `ТОК_ДнейОтсрочки`, `ТОК_КредитныйЛимит` (= варианты clarify) |
| `nums` регистра | Количество, Всего, … |
| `search_refcols` → каталог | в т.ч. `accumulationregister_реализациятмц\|Контрагент` |
| aliases каталога | «…, клиенты, …»; best_used_for — лимит/отсрочка |
| aliases регистра | продажи/реализация; не «клиенты» |

**Вывод:** ранг чисто лексический/смысловой по именам. Признака «эта сущность даёт атом, отвечающий на вопрос» в выборе нет — справочник побеждает, потому что похож на «клиенты», а не потому что в нём есть покупки.

---

## 3. Мера `answer_fit` (без списка слов)

Модуль: [`ubuntu/serenedb/entity_answer_fit.py`](../ubuntu/serenedb/entity_answer_fit.py).

Идея = business signal из доков SereneDB (Relevance Tuning): к лексическому порядку добавить сигнал **из данных**.

При `want∈{count,''}` и `kind` → catalog `C` (те же основы label/aliases/best_used_for, что `entity_form_catalogs_for_kind`):

| src | fit | условие (всё в SQL) |
|---|---:|---|
| `C` | 1 | есть карточки (`NOT IsFolder`) |
| `H` = `accumulationregister_*`, ось `search_refcols.target_src=C`, не книга НДС | 2 | `0 < COUNT(DISTINCT refs_map[axis]) < N_cards` в окне (явный period или rolling year) |
| `H` | 1 | distinct == cards |
| иначе | 0 | |

Перестановка: больший fit раньше (стабильно).  
Проверка автономии: на чужой базе нужны только `search_refcols`, корпус с `refs_map`/`flags`/`doc_date` — **имён okna в коде нет**.

На Q1: `fit(реализациятмц)=2`, `fit(контрагенты)=1` → регистр выше справочника.

Напряжение с «сколько у нас всего клиентов?» (эталон 353): оба атома живы и **различны** (353≠141). Мера поднимает регистр и там тоже. По п. 12 честный исход при двух атомах — **clarify** «все карточки / участники движений», а не угадывание. Золото «всего» сейчас ждёт справочник — это конфликт двух эталонов при одной структуре; чинить словом «всего» в коде нельзя. В патче: либо clarify двух атомов, либо ужесточить гейт F/`has_movement` (не считать «движение в пуле» из полного literal dump при пустых terms — на Q1 351 сущность).

---

## 4. Прибор — числа до / после

Скрипт: [`work/entity-choice/k6_entity_rank_bench.py`](../work/entity-choice/k6_entity_rank_bench.py).  
Набор: `ubuntu/serenedb/ab-gold-okna.tsv` (23). Эталон — `src_table` из SQL.  
«До» = порядок `alias_idx` + expand держателей kind.  
«После» = `reorder_by_answer_fit` при form эталона count/distinct.  
Модель не зовётся. Прогон: okna 27.08, today=2026-08-27.

| | эталон-лидер | в тройке | не найден |
|---|---:|---:|---:|
| **до** | **2** / 23 | **2** / 23 | **15** / 23 |
| **после** | **1** / 23 | **2** / 23 | **15** / 23 |

Почему «после» лидеров меньше: alias-поверхность на «продали*» часто **пуста** (miss=15) — боевой путь там `prefer_entity_for_sales`, не словарь; мера K6 на sum не переставляет. Сдвиг по сути — на count/distinct:

| вопрос | etalon | place до→после | fit |
|---|---|---|---|
| сколько клиентов реально покупают? | `accumulationregister_реализациятмц` | **5 → 1** | 2 |
| сколько у нас всего клиентов? | `catalog_контрагенты` | **1 → 2** | регистр 2, каталог 1 |
| сколько позиций совсем не продаётся…? | регистр (DISTINCT в SQL) | ∅ → ∅ | kind угадан криво («базы») — отдельный дефект Q2/F |

Числа не подгонялись: лидер 2→1 — честный итог набора; выигрыш K6 виден в строке «покупают».

---

## 5. Проект патча (текст; `serene_ask.py` не в этом коммите)

1. **Импорт** `entity_answer_fit.answer_fit_table` / `reorder_by_answer_fit`.
2. **После** `prefer_entity_for_catalog_count` (~12478), до отсева not_for:
   ```python
   _fit = answer_fit_table(psql, lit, cands, intent, today=today)
   cands = reorder_by_answer_fit(cands, _fit)
   diag["answer_fit"] = {k: _fit[k] for k in cands[:12] if _fit.get(k)}
   ```
3. **F pre_entity:** в `entity_form_pre_entity_ok` не резать по `early_classes>1`, если `form_n==1` и у лидера `answer_fit==2` (структура одна — развилка классов не отменяет единственную форму оси). Либо звать F **сразу после** reorder, до `pick_entity`.
4. **Два атома (fit C=1 и H=2):** вместо молчаливого выбора — clarify options с двумя посчитанными атомами (карточки vs DISTINCT), п. 12; золото «покупают» закрывается, если F/reorder ставит H и считает distinct без модели.
5. **Не** включать `ASK_ENTITY_FORM` вслепую на всём gold: Q2 на флаге уже дал догадку; complement/канон sales — отдельный хвост (C4 §5.2).
6. Замок: офлайн-тест на `entity_answer_fit.reorder_by_answer_fit` + прибор `k6_entity_rank_bench.py` в CI/приёмке okna.
7. Выкат: только после замера прибора на staging `:8092` (числа рядом, как здесь).

---

## 6. Файлы этой задачи

| файл | роль |
|---|---|
| `ubuntu/serenedb/entity_answer_fit.py` | мера |
| `work/entity-choice/k6_entity_rank_bench.py` | прибор 23 gold |
| `docs/K6_ENTITY_RANK.md` | этот отчёт |

`serene_ask.py` — не менялся.

---

## 7. Мера v2 (прибор, `serene_ask.py` не трогали)

Дата: **27.08.2026**, okna `:7890`. Код меры/прибора:
[`work/k6-rank-v2/`](../work/k6-rank-v2/). Доки SereneDB:  
[Blend in a business signal](https://docs.serenedb.com/cookbook/search/boosting#blend-in-a-business-signal) —
scorer × бизнес-колонка в `ORDER BY`; popularity без магического порога.

### 7.1. Почему v1 даёт 2→1 / «покупают» 5→1

Разбор: [`work/k6-rank-v2/dumps/v1_noise_analysis.md`](../work/k6-rank-v2/dumps/v1_noise_analysis.md).

| шум | факт |
|---|---|
| Alias-дыра | «продали вчера?» → **0** hits в `alias_idx`; эталон вне пула → мера не на чем работать (miss≈15 в приборе K6 §4) |
| Fit=2 > карточек | держатель с `0<distinct<cards` всегда выше каталога → «всего клиентов» 1→2 |
| Нет вида объекта | префикс OData / `search_entity_class` в ранге v1 нет (см. PATH_AND_FAILURE) |
| Нет doc_date в ранге | дата только внутри окна DISTINCT, не как «есть живые ячейки» |
| Kind кривой | «прайсе» → типы цен / строки документов, не номенклатура |

### 7.2. Что делает v2

Лексикографический ключ (без весов/порогов под okna):

1. не `service` (`search_entity_class`);
2. **live-ярус** — для sum/name: есть `doc_date`+`nums`; для distinct/complement: `axis_fit` по `search_refcols`; для card-count: kind-catalog с карточками / документ с датами;
3. **вид объекта** (OData-префикс `catalog`/`accumulationregister`/`document`…);
4. popularity = `n_dated` (или `n_cards`) как колонка business signal;
5. позиция alias — последний ключ.

Пул: alias + stem-пересечение label/aliases с вопросом + все live canon
`accumulationregister_*` (есть дата, nums, ось на catalog; не книга НДС).

### 7.3. Прибор — числа

Набор: gold-23 + 5 «позиций» ([`questions_positions.tsv`](../work/k6-rank-v2/questions_positions.tsv);
в `ACCEPTANCE_AMBIGUOUS.md` §8 формулировок «сколько позиций…» нет — взяты
gold / OKNA_LIVE §B5 / ACCEPTANCE_UT).

| набор | | lead | top3 | top8 | out8 | miss |
|---|---|---:|---:|---:|---:|---:|
| gold-23 | v0 alias | 2 | 3 | 4 | 19 | 0 |
| gold-23 | v1 fit | 0 | 1 | 4 | 19 | 0 |
| gold-23 | **v2** | **18** | **18** | **21** | **2** | **0** |
| позиций-5 | v0 | 0 | 0 | 1 | 4 | 0 |
| позиций-5 | v1 | 0 | 0 | 0 | 5 | 0 |
| позиций-5 | v2 | 0 | 0 | 4 | 1 | 0 |

Гейт задачи: lead ≥12/23 → **18 PASS**; регресс v1-top3 → **0 PASS**.

Контроль K6 (place v0→v1→v2):

| вопрос | place | комментарий |
|---|---|---|
| сколько клиентов реально покупают? | 51→51→**9** | вне топ-8: kind=физические, ось не контрагенты |
| сколько у нас всего клиентов? | 1→4→**4** | v2 не чинит card-count при кривом kind |
| сколько позиций у нас в прайсе? | 33→33→**7** | в топ-8, не лидер |

Полный лог: [`work/k6-rank-v2/bench.out`](../work/k6-rank-v2/bench.out).

### 7.4. Вывод

v2 **доказала** себя на sum/name gold (популярность live-регистра + вид
объекта): 18/23 лидеров, top-3 не хуже v1. На count/distinct «клиенты /
позиции» kind-guess по числу держателей тянет `физическиелица` —
структурный хвост, не закрыт этой мерой. Позиции-5: лидеров 0, но top-8
0→4.

### 7.5. Проект патча (текст; в `serene_ask.py` **не** вносить этим коммитом)

1. Вынести `work/k6-rank-v2/measure_v2.py` рядом с `entity_answer_fit.py`
   (или заменить v1) — `features_table` + `reorder_v2`.
2. После `prefer_entity_for_*` / до `pick_entity`: расширить пул stem+live
   registers (как `expand_stem_and_live`), затем `cands = reorder_v2(...)`.
3. В diag: `answer_fit_v2` = ключ/признаки топ-12.
4. Два атома (catalog cards vs holder distinct) при равной уместности —
   clarify (п. 12), не молчаливый fit=2.
5. Отдельно: kind-guess не ранжировать голым `count(holders)` —
   иначе физлица бьют контрагентов; нужен overlap aliases@@вопрос среди
   stem-кандидатов (не делалось в замерённом прогоне).
6. Замок: `work/k6-rank-v2/bench.py` в приёмке okna; порог lead≥12 на gold
   и 0 регрессов v1-top3.
7. Выкат только после staging `:8092`, рядом с числами этого §7.3.

### 7.6. Применён (27.08, патч 2 К3)

Модуль [`ubuntu/serenedb/entity_rank_v2.py`](../ubuntu/serenedb/entity_rank_v2.py)
встроен в `serene_ask.py` после `prefer_entity_for_*`: расширение пула
`expand_holders` + `expand_stem_and_live`, `reorder_v2`, diag `answer_fit_v2`,
clarify двух атомов (`k6_dual_atom_clarify_return`). Kind — `kind_from_alias_overlap`
(не `count(holders)`).

| замок | число |
|---|---|
| bench gold-23 v2 lead | **19/23** (≥12 PASS) |
| v1-top3 регресс | **0** |
| K5 guess/meta/nodata/axis/sales_rank | **13/12/11/11/51** |
| AB_PROBE okna | **8/8**, `0err/8` |
| живой :8096 «какой клиент больше всех купил…» | DYNAMIC SELLING GROUP SRL **1 537 150.47** |
| живой «топ-3 по деньгам…» | тот же лидер в топе |

### 7.7. Остаток: count-вопрос с чужим гигантом (замер 27.08, патч 3 К3)

«Сколько анкет заполнено?» на живой okna: ранг v2 работает (шаг «K6 v2,
кандидатов=99» в трассе), но модель выбирает `accountingregister_плансчетовосновной2014`
(**совпадений=189043**) вместо informationregister_фио\* («анкеты сотрудников»
есть в корпусе). Гигантский счётчик строк побеждает в выборе модели; признаков
v2 недостаточно, чтобы поднять фио-регистры над планом счетов на этом классе.

- Это класс промахов bench 19/23, не регресс: bench и раньше не покрывал
  «обиходное слово → маленький регистр при живом гиганте».
- Подгонкой признаков под этот вопрос НЕ занимались (указание владельца:
  не латать под отдельные вопросы). Чинить классом: признак «доля строк
  сущности, несущих слова вопроса» против «сырой count» в подсказке модели.
- K4-страж тут прав: kind «анкеты» имеет опору в корпусе, no_data нельзя
  (данные об анкетах есть — см. K4_CLARIFY_VS_NODATA §7, строка «анкеты»).

### 7.8. K6a: q_meta против гиганта (27.08)

Класс «обиходное слово → маленький регистр при живом гиганте»: признак
**q_meta_overlap** — пересечение stem слов вопроса с `label`/`aliases` сущности
(без `best_used_for`: там примеры вопросов вроде «Сколько…», давали ложный
q_meta у `informationregister_календарь`). **q_row_ratio** — доля строк с overlap
stem вопроса в `doc` (дорогой проход только при `n_rows≤1000`; иначе при q_meta
ratio=1000). В `rank_key_v2` для card-count: `live=3` при q_meta+info/catalog;
popularity при q_meta = `(-q_row_ratio, n_rows)`; без q_meta — `(n_rows,)` (меньший
регистр лучше гиганта без темы). Diag `answer_fit_v2`: ключи `q_meta`, `q_row_ratio`.

| замок | до → после |
|---|---|
| bench gold-23 v2 lead | **19/23** (без регресса) |
| top3 / top8 | 20/23 · **22/23** (+1 top8) |
| v1-top3 регресс | **0** |
| k6a_class (3 вопроса) | lead **2/3** (анкеты 3→**1**, курсы **1**, ответственные top3) |
| test_k6_rank_v2 | **4/0** |
| K5 sales_rank | **51/0** (прочие K5 без регрессии от K6a) |

Bench-набор класса: [`work/k6-rank-v2/gold_k6a_class.tsv`](../work/k6-rank-v2/gold_k6a_class.tsv).
«замерывременi» на инстансе okna (17890) **нет** — в наборе заменён на
«ответственные лица». Прогон: `SERENEDB_DSN=… python3 work/k6-rank-v2/bench.py`.

### 7.9. Регрессия клиент-рангов после патча 2 (изоляция 27.08 вечер, оркестратор)

Контур-24 на полном флаговом наборе (RRF+IVF+FRESH+ATOM+ENTITY_FORM+RANK_CANON[+SOLR]):
**18/24** (без флагов 15/24). Изоляция двух FAIL-рангов («какой клиент больше
всех купил в этом месяце?», «кто из клиентов молодец…, три лучших») на
мини-наборе из 5 строк канонного gold:

| конфигурация :8096/:8097 | верных | «больше всех купил» | «молодец» |
|---|---|---|---|
| патч 3 (6fb19819) + 7 флагов (с solr) | 3/5 | FAIL figures | FAIL clarify |
| патч 3 + 6 флагов (без solr) | 3/5 | FAIL figures | FAIL clarify |
| **патч 2 (1de66a65) + 6 флагов** | 3/5 | FAIL figures | FAIL clarify |
| **патч 2 + RRF-only** | 1/5 | FAIL figures | FAIL answer(без имени) |

Вердикт: **флаги не виновники** (+3 строки контура: топ-3 по деньгам, топ-товар
за всё время, декабрь-2025); **патч 3 не виновник** (те же FAIL на патче 2).

Регрессия внесена **финальной доработкой патча 2** (между 17:05 и коммитом
19:59): по `ask_journal` тот же вопрос (q_len=44) днём 13:11–17:05 на кодах
35daa280 (=`/tmp/serene_ask_k7.py`, К5+К7) / 4ec52861 / a80cc8d5 давал
**answer**; вечером на коммитном 1de66a65 и патче 3 — только **figures**.
Трасса вечера (rid ab…214711): K6 v2 → 78 кандидатов → детектор исхода **B**
(«числа разные + подписи»), но ответ собирается из оси «mtd assumed 3817442.31»,
имени лидера в text нет — лидер-люк не собран. Дневной замер §7.6 «живой
лидер DYNAMIC SELLING GROUP SRL 1537150.47» снят на **докоммитной** версии
патча 2 и коммитному коду не соответствует.

Починка — §7.11 (K6b).

### 7.10. Попутный класс: «позавчера» на пустом периоде (контур-24, 27.08)

Не ранг, но найден тем же контуром: «сколько продали вчера?» и «сколько
продали позавчера?» при ОДИНАКОВО пустых днях (26–27.08, лаг свежести
3,7 сут) ведут себя по-разному: «вчера» — OK (честный пустой период),
«позавчера» — паспорт-мусор («00,08,1,107468,2026,25,252026…»), want=0.
Контур шёл после полуночи Europe/Chisinau (rid ab…T214439Z), эталон
легитимен. Дефект — нестандартное относительное окно «-2 дня» уходит не в
пустой период, а в развал паспорта. Класс для К-потока после K6b; свежесть
не виновата.

### 7.11. K6b: исход B не подменяет rank-лидера (27.08)

**Строка-виновник (цепочка 61edb43):** вставка `K6R.apply_to_candidates` (~13019)
поднимает верный регистр в `picked[0]` при pool=1; `_window_fork` (readings>1)
всё равно даёт исход **B** с классами mtd/full_month; `fork_outcome_b` (~6801)
собирал `render_atom_pair` по атому **периода** (assumed/mtd), не rank GROUP BY —
имени лидера в text не было.

**Фикс (класс):** `rank_defer_fork_outcome_b` + `fork_classes_window_only` —
при rank_intent и одних src в классах B откладываем исход B
(`outcome_b_deferred_rank`), лидер идёт через `rank_deterministic_answer`.
В `entity_rank_v2`: dual_atom clarify не зовётся при `rank_intent`.

| замок | число |
|---|---|
| test_rank_leader_path (K6b defer) | **30/0** (+4) |
| test_k6_rank_v2 offline | **3/0** |
| test_compose | **ok** (прогон) |
| bench v2_lead / v1-top3 | не снят (37890 auth fail / 7890 timeout в сессии) |
| AB_PROBE okna 18093 | не снят (ask timeout 90s/600s) |
