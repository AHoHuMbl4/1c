# Регрессия первой базы: что проверено после каждой правки

**Заведён 29.07 по вопросу владельца:** «все правки не поломают например работу с первой
базой или любой другой? там ведь работало всё».

Вопрос правильный, и ответ на него не может быть «вроде не должно». Здесь фиксируется,
**какой правкой** и **каким замером** доказано, что первая база цела. Без замера строка в
таблицу не попадает.

---

## Замер 02.08, ~20:50 — после закрытия долгов и подъёма конвейера

🔴 **Снят ПОСЛЕ последней правки захода.** В заход менялись: `deploy_instance.sh` (работа с
любым экземпляром), `1c-mcp-ask@.service` (шаблон моста), юниты конвейера и индекса
(`/etc/1c-embed.env`), `wiki_publish.sh` (профиль), `serene_ask.py` (`resolve_focus`),
удалён `memory-lancedb`, поднят второй бот.

### Уровень базы — совпало ВСЁ, и это после живого такта

| Что | Эталон | Сейчас |
|---|---|---|
| строк корпуса | 103 808 | **103 808** |
| дублей ключа | 0 | **0** |
| `cov_rows_1c` / `cov_rows_search` | 103 634 / 103 808 | **те же** |
| `cov_rows_lost` / `cov_ent_lost` | 2 / 1 | **те же** |
| `refmap_ambiguous_guid` / `build_noemb` | 10 / 0 | **те же** |

Ценность этого замера выше прежних: он снят **после полного такта конвейера** (1040 с,
58 874 изменённых строки из 1С за четверо суток), а не на замороженной базе. То есть
доказано не только «мы не сломали», но и «такт проходит целиком и оставляет базу такой же».

### Приёмочный набор

Снимается прогоном ниже; числа и разбор — в `CHANGELOG` за 02.08.

## Замер 02.08, ~15:50 — после выкатки кластера Э4 (гейт, мост, плагин)

🔴 **Снят ПОСЛЕ последней правки захода и ПОСЛЕ выкатки на стенд**, а не после какой-нибудь
из них. В заход менялись: `verify-core.js`, `index.js`, `openclaw.plugin.json`,
`mcp_ask.py`, `serene_ask.py` (гейт: `asked_figure_missing`, `prompt_leak`, числа вопроса,
ветка полноты, приписка о старении, класс исключения эмбеддера), `deploy_instance.sh`.

### Уровень базы — совпало ВСЁ

| Что | Эталон (30.07) | Сейчас |
|---|---|---|
| строк корпуса | 103 808 | **103 808** |
| дублей ключа | 0 | **0** |
| `cov_rows_1c` | 103 634 | **103 634** |
| `cov_rows_search` | 103 808 | **103 808** |
| `cov_rows_lost` | 2 | **2** |
| `cov_ent_lost` | 1 | **1** |
| `refmap_ambiguous_guid` | 10 | **10** |
| `build_noemb` | 0 | **0** |

### 🔴 Четыре прогона подряд, и участие реранкера ДОКАЗАНО журналом

Владелец указал, что реранкер в тот день включался и выключался, и прогоны могли попасть
на моменты выключения. Возражение справедливое, и проверить его было **нечем**: успешный
реранк не писал в журнал ничего — «отработал» и «не звался вовсе» выглядели одинаково.
Поэтому сперва заведена запись успеха (`rerank отработал: N кандидатов, порядок
изменён/без изменений`), и только потом сняты прогоны.

| Прогон | Верных | Реранк за прогон | Примечание |
|---|---|---|---|
| 1 | 5/8 | сбоев 0 (успех тогда не логировался) | |
| 2 | **6/8** | сбоев 0 | |
| 3 | 5/8 | **14 успешных, 0 сбоев, порядок изменён каждый раз** | |
| 4 | 5/8 | входит в те же 19 успешных за два прогона, 0 сбоев | 1 сбой прибора: таймаут 300 с |

`[замер]` Проба эндпоинта прямо перед прогоном 3 — `HTTP 200`, 1,2 с. За прогоны 3-4 —
**19 строк «rerank отработал», ноль строк «НЕ ОТРАБОТАЛ»**, и порядок кандидатов менялся
каждый раз. То есть версия «тесты попали на выключенный реранкер» проверена и **не
подтвердилась**: с доказанно работающим реранкером результат тот же — 5/8.

**Устойчиво неверны 2 из 8 во ВСЕХ четырёх прогонах, и это один вопрос на двух языках:**
«Сколько всего мы продали?» и «What is the total amount of all sales?» — каждый раз выбран
регистр (`задачибухгалтера`, `значенияемрц`) вместо документа реализации. Это вопрос, где
не назван ни контрагент, ни период, — то есть отбирать не по чему, и решает целиком
смысловой путь. **Плавает 1:** «Что покупало ООО Ромашка?» — неверно / верно / неверно /
таймаут.

⚠ **Задержка коррелирует с ошибкой:** верные ответы 10-15 с, неверные 54-60 с, а на
четвёртом прогоне «Ромашка» упёрлась в 300 с (`ASK_TIMEOUT`). Долго идут ровно те вопросы,
где буквальный отбор пуст и работает смысловой путь с арбитражем. Отдельная находка, в
кластер Э3.

⚠ Отметка `.golden-last-run` за прогон 4 **не поставлена** — в нём был сбой обращения. Это
штатное поведение (`№4`): прогон со сбоем замером не считается.

### Приёмочный набор — 5/8 и 6/8: ДВА прогона одной настройки, разброс ±1

`[замер]` `AB_BASE=postgres AB_SCORERS=lm_dirichlet` (боевой скорер первой базы), два
прогона подряд **без единой правки между ними**: **5/8** (средняя 32,6 с) и **6/8**
(33,7 с). Прогонялся только боевой скорер: шесть скореров — это A/B-опыт, а не регрессия,
и он оставил бы базу на `dfi`, который `techContext` (ловушка 17) называет заведомо
дефектным.

🔴 **Это и есть замер разброса, которого проекту не хватало.** ±1 верный ответ из восьми
без каких-либо изменений — то же, что записано для приёмки из десяти. Значит **ни 5/8, ни
6/8 сами по себе результатом не являются**, и сдвиг на один ответ доказательством не будет.

**Устойчиво верны в обоих прогонах 5:** продажи по контрагенту, поступления по
контрагенту, продажи за декабрь, продажи свыше порога, банки в Казани.
**Устойчиво неверны 2, и это один и тот же вопрос на двух языках:** «Сколько всего мы
продали?» и «What is the total amount of all sales?» — оба раза выбран регистр
(`задачибухгалтера`, `значенияемрц`), причём в двух прогонах разный. **Плавает 1:** «Что
покупало ООО Ромашка?» — то регистр `состоянияконтрагентов`, то верный
`document_реализациятоваровуслуг`.

Все расхождения — **выбор сущности**, главная открытая проблема продукта (кластер Э3).
Счёт не ошибся ни разу ни в одном прогоне.

🔴 **Правки этого захода к провалам отношения не имеют, и это проверено, а не
предположено:** `journalctl` за оба прогона — **ноль** срабатываний новой проверки
(`не названа цифрами`) и ни одной строки `GATE`. Проверка не отвергла ни одного ответа.

🔴 **Сравнивать с историческими 8/8 нельзя** — между ними сменились два фактора, ни один
из них не Э4: скорер первой базы `dfi` → `lm_dirichlet` (кластер Э1) и **перевекторизация
первой базы** в другое векторное пространство (`1c-revector-base1`). Этот замер доказывает
**отсутствие вреда на уровне базы**, **неучастие нового гейта** и **величину разброса** —
и ничего сверх того.

*Было ошибочно (первая редакция этого раздела): «третий фактор — реранкер мёртв, 401 на
каждом вопросе». **Чем опровергнуто:** те 401 сняты в 15:30, а настройки реранкера
владелец сменил в 15:45:22 (`stat /etc/1c-embed.env`); сервис первой базы стартовал
15:45:23 и 15:47:09, то есть **оба прогона шли с рабочим реранкером**, и `journalctl` за
их время даёт ноль строк `rerank НЕ ОТРАБОТАЛ`. Прямая проба эндпоинта тем же телом
запроса, что шлёт код (`query`/`documents`/`top_n`), вернула `results[].index` и поставила
«Реализация товаров и услуг» первой на вопрос про продажи. Ошибка была в том, что вывод из
одного замера (15:30) я перенёс на другой (15:47), не сверив время.*

⚠ Отметка `.golden-last-run` этим прогоном не поставлена: нашлось, что путь к ней считался
как «три каталога вверх от скрипта», а запускать велено из `/opt/1c-mcp-reports` — значит
из документированного способа отметка уходила в `/.claude/` и не ставилась НИКОГДА. Тот же
класс, что «хуки не вызывались вовсе» (`HOW_NOT_TO §1.42`). Починено тем же заходом:
корень ищется git-ом от места скрипта, задаётся `AB_MARK_DIR`, а при неудаче прогон
**говорит** об этом и не выдаёт себя за поставивший отметку.

## Замер 30.07, 06:14 — после починки «строка ≠ объект» и четырёх условий выкладки

🔴 **Снят ПОСЛЕ последней правки кода**, как требует `CLAUDE.md`, а не после какой-нибудь
из них. В этот заход менялись: `poc_load_entity.py` (шаг 1), `corpus_build.sql` (шаг 2,
`decl_entity`, `CTE fold`, счётчик `fold_kept_no_child`), `corpus_merge.sql` (защита по
объектам), `coverage_build.sql` (объекты витрины, `coalesce` на счётчиках), `build.sh`
(отпечаток с загрузчиком), `serene_sync.py` (`mart_changed_ts` на полной загрузке).

| Что | До | После |
|---|---|---|
| строк корпуса | 103 808 | **103 808** |
| дублей ключа | 0 | **0** |
| объектов с разным текстом внутри объекта | 0 | **0** |
| сущностей с пометкой «собралось не полностью» | — | **0** (условие ВКЛЮЧЕНО, см. ниже) |
| `cov_rows_1c` | 103 634 | 103 634 |
| `cov_rows_search` | 103 808 | 103 808 |
| `cov_rows_lost` | 2 | **2** |
| `cov_ent_lost` | 1 | **1** |
| `fold_kept_no_child` | — | **0** |
| `refmap_ambiguous_guid` | 10 | 10 |
| ошибок сборки / слияния / переписи | — | **0 / 0 / 0** |
| время сборка+слияние+перепись | — | 2 мин 13 с |

Единственная потеря названа с причиной, а не умолчана:
`informationregister_иерархиягрупппользователей` — 2 строки, «не загрузилось из 1С».
Это та же известная потеря, что и раньше, не новая.

### 🔴 Чего этот замер НЕ доказывает

**Изменённый путь шага 2 на первой базе почти не исполняется.** [замер] строк с отпечатком
в ключе у сущностей с `['Ref_Key']` — **0**; сущностей с колонками вложенного типа — 3, и
все три регистры, то есть вне области шага 2. `fold_kept_no_child = 0` означает не «всё
хорошо», а «этого класса тут нет».

Поэтому «первая база не изменилась» — доказательство **отсутствия вреда**, и не
доказательство правильности. Правильность доказана на второй базе сверкой с живой 1С
(партнёры 164 = 164, подмен реквизитов 0 из 551).

## 0. Чем именно проверяется «не сломано»

Три уровня, от дешёвого к дорогому. Все три обязательны — первый проходит и на сломанной
базе, если поломка тихая.

| # | Проверка | Что ловит | Как запустить |
|---|---|---|---|
| 1 | такт конвейера доходит до конца | падение сборки, слияния, векторов | `systemctl start 1c-serene-pipeline.service` |
| 2 | перепись полноты и `search_quality` | молчаливую потерю строк (п. 13) | внутри такта, шаг 7 |
| 3 | **приёмочный набор** | ухудшение ОТВЕТОВ при исправной сборке | `python3 ab_scorer.py` в `/opt/1c-mcp-reports` |

🔴 Третий уровень — единственный, который ловит ухудшение качества ответа. Первые два
могут быть зелёными, а бот при этом отвечать хуже: сборка не знает, что такое «верный
ответ».

---

## 1. Правки 29.07 и чем каждая проверена на первой базе

| Правка | Риск для первой базы | Замер |
|---|---|---|
| разметка сущностей моделью (`classify_entities.py`) | новый шаг в такте; при отказе модели такт мог бы падать | шаг не `fail`, а предупреждение. [замер] на первой базе 231/231 размечено, ноль неразобранных |
| очерёдность векторов двумя проходами | второй проход мог бы не покрыть остаток → строки без вектора навсегда | [замер] такт зелёный, `осталось: 0` на всех трёх целях |
| замок по `current_database()` | неверное имя → такт не стартует вовсе | [замер] обе формы DSN (с `dbname=` и без) дают `postgres`, замок один |
| рабочие таблицы по базам (`emb_<база>_<таблица>`) | потеря уже посчитанных векторов при смене имени | [замер] докатка из прежних имён: `ut_test` 632 683 → 630 562, то есть 2 121 оплаченный вектор перенесён, не потерян |
| фильтр `database_name` в досчёте | — (только сужает) | [замер] без фильтра видно 5 чужих таблиц, с фильтром 0 |
| `deploy.sh` + вызов из конвейера | раскладка поверх идущего такта могла бы порвать его | подмена атомарная (`mv`), [замер] такт с раскладкой прошёл |
| порядок «метки → резолвер → корпус» | на первой базе порядок не влияет (всё считается за секунды) | [замер] такт зелёный, 0 без вектора |
| потолок разметки `CLASSIFY_MAX_PER_RUN` | частичная разметка могла бы отправить новое во второй проход | условие первого прохода изменено на «НЕ служебное»; [замер] см. таблицу ниже |
| погашен `nightly-eval` | к первой базе отношения не имеет (чужой слой) | [замер] `timer: inactive/disabled` |

---

## 2. Хронология замеров

| Время (UTC) | Что | Результат |
|---|---|---|
| 04:21 | такт первой базы после правок 1-6 | ✅ зелёный, **134 с**, 0 строк без вектора на всех целях |
| ~04:35 | приёмочный набор после правок 1-6 | ✅ **8/8** на `tfidf` (боевой), `bm25`, `bm25_b0`, `lm_dirichlet` |
| — | | `dfi` и `lm_jm` дают 5/8 — **известный дефект этих функций ранжирования**, в продукте не используются (`techContext` ловушка 17) |
| после правок 7-9 | такт + приёмка повторно | заполняется прогоном, а не ожиданием |
| 30.07 19:1x | добавление карты булевых реквизитов `flags` | ✅ корпус **103 808** без изменений; счёт с новым отбором даёт те же 103 808 |
| 31.07 ночь | подтверждение выбора, `alias_idx`, название как алиас | ✅ корпус **103 808**; `flags` читается; `alias_idx` читается (0 строк — вики там не собрана); проверка подтверждения выполняется без ошибки |

🔴 **Что этот замер поймал — а без него ушло бы в продукт.** Сервис ответов по умолчанию
читает **первую** базу (`SERENEDB_DSN_RO` не задан → `dbname=postgres`). Новый отбор
обращается к колонке `flags`, а её там не было: `ERROR: Referenced column "flags" not found
in FROM clause`. То есть на первой базе **каждый счётный вопрос отвечал бы отказом при
наличии данных** — прямое нарушение п. 21.

Тот же класс проверки нужен КАЖДОМУ новому объекту, к которому обращается сервис: после
`alias_idx` и `search_entity_alias` первая база была проверена отдельно, и именно там
выяснилось, что права на алиасы не выданы вовсе (`HOW_NOT_TO §1.24`).

Причина шире одного случая: **`CREATE TABLE IF NOT EXISTS` у существующей таблицы не делает
ничего**, новая колонка в уже созданную базу из объявления не попадает. Поэтому добор стоит
`ALTER TABLE ... ADD COLUMN IF NOT EXISTS` в `corpus_init.sql` — в создании схемы, которое
идёт первым шагом каждого такта. Деградация плавная: пока база не пересобрана, карта пуста,
`coalesce(..., false)` читает это как «не папка», поведение совпадает с прежним.

🔴 **Урок из этой же таблицы:** приёмка 8/8 была снята ДО двух последних правок, и я
собирался считать вопрос закрытым. Так нельзя: доказательством является замер **после
последнего изменения**, а не после какого-нибудь. Владелец задал вопрос ровно вовремя.

---

## 3. «А любая другая база?»

Первая база — не доказательство универсальности, а только доказательство отсутствия
регрессии. Универсальность проверяется на **второй** базе (УТ 11.4.9), протокол —
[`TEST_SECOND_BASE.md`](TEST_SECOND_BASE.md). Именно она вскрыла все находки 28-29.07,
невидимые на одной базе.

**Правило, из этого следующее:** правка, сделанная ради второй базы, не считается
законченной, пока на первой не сняты замеры 1-3 из §0. И наоборот. Одна база всегда
врёт — тем, что молчит про случай, которого в ней нет.
