# Состояние на 2026-07-26 (конец смены)

Всё, что ниже, — ЗАМЕР на живой системе, а не намерение. Метод: сверка с 1С через
`$count`, SQL по витрине и корпусу, живые вопросы к `/ask`, прогон `ab_scorer.py`.

## Итог одной таблицей

| Показатель | Утро | Вечер |
|---|---|---|
| Золотой набор | 6/8 | **7/8** |
| Среднее время ответа | 5,14 с | 4,75 с |
| Строк 1С == витрина == корпус == индекс | не сверялось | **97 965 во всех четырёх** |
| Расхождений с `$count` по 235 сущностям | не сверялось | **0** |
| Потолок числа сущностей | 1713 (HTTP 500) | **проверено 10 000** |

Утреннее 6/8 стояло на корпусе, где организация во всех документах называлась
настройкой НДФЛ, а 35% денег по строкам накладных было потеряно дедупом. Вечернее
7/8 — на корпусе, сверенном с 1С.

## Что было сломано и как это выяснилось

### Данные

| Дефект | Замер | Как чинится |
|---|---|---|
| Дедуп по `ref_key` резал табличные части | 9 сущностей, **−1034 строки**; реализация 76→30, сумма 12 052 500→7 856 600, количество 499→213 | дедуп по ОБЪЯВЛЕННОМУ ключу из `$metadata` |
| «Первая Guid-колонка = свой ключ» | 48 регистров писали чужие имена в общую карту: организация во всех документах звалась `СтандартныеВычетыНарастающимИтогом` (400 строк против 1 верной) | собственная идентичность только при ключе ровно `["Ref_Key"]` |
| Ссылка на владельца выбрасывалась у табличных частей | строк накладных со ссылкой на документ: **0 из 42** | тот же контракт; стало 107 из 107 |
| Мусор протокола в тексте | `X_Type: StandardODATA.…` в **28 918 строках (30%)** | пара `X`/`X_Type`/`X_Base64Data` — контракт OData |
| `base_profile` в поисковом корпусе | 4585 строк схлопывались в 154 из-за пустого ключа | служебная таблица исключена; ключ без идентичности = отпечаток содержимого |
| Логические колонки не индексировались | **705 колонок**, 21 214 строк «помечено на удаление» (54%) неотличимы от живых | `Edm.Boolean` идёт в текст |
| Модель видела только подсветку | `ts_highlight` = 144 символа при строке 398; ИНН на 367-й позиции не доходил у **12 контрагентов из 13** | модели уходит подсветка И строка |

### Запреты

| Дефект | Замер |
|---|---|
| Шлюз тулкита **fail-open** | `if not GATEWAY_TOKEN: return True`, токена в env нет — запрос без заголовков уходил к 1С |
| Гейт заземлял выдуманные суммы | «3 500 000» получал лишнее прочтение 3500 и проходил — ответ, завышенный в тысячу раз |
| Гейт отвергал правильные | «1 629 700,00» рассыпалось на 1/629/700/0; NBSP не был разделителем |
| `mcp_braine` падал с `NameError` | `sys` не импортирован — fail-closed давал краш-луп без диагностики |

### Масштаб

| Дефект | Замер | Стало |
|---|---|---|
| Список кандидатов в argv | 1712 сущностей — ответ, 1713 — `OSError`, и он не `RuntimeError`: **HTTP 500 на каждый вопрос** | SQL через stdin; 10 000 проверено |
| Перечень сущностей в промпт без границы | на 4585 типах ~267 КБ в каждый вопрос | бюджет промпта в символах; порядок задан данными, режется хвост |
| Поиск владельца квадратичный | до **2 077 360 запросов**, ~21 час на полной базе | владелец запоминается при построении карты ссылок: 0 лишних запросов |

## Что сделано по-штатному (проверено агентом `serenedb-native`)

- индекс многополевой: `inverted(doc dict, refs dict, src_table)` — шаблон движка;
- `src_table` без словаря = keyword, фильтр **внутри** индекса (подтверждено планом:
  `Index Filter: And(Term doc, Term src_table)`, post-filter отсутствует);
- `ts_compound(must, must_not, should, k)` вместо цикла подбора;
- агрегаты в базе по всему множеству; группировка источников в базе;
- табличная часть — контракт `$metadata` (ключ `[Ref_Key, LineNumber]`), владелец
  найден **по данным**. Проверено на 4585 типах: под правило не попадает ни одна
  виртуальная таблица регистра.

## Открытое

**Правильность**
1. «Что покупало ООО Ромашка?» — единственный оставшийся провал, и он **не дефект
   поиска**. Разобрано до конца:
   - significant terms внедрены и работают: реализация → «оптовый склад, склад,
     62.02», поступление → «счёт организации, счёт банка, средства полученные от
     покупателей». Термы, общие для всех кандидатов, отсеиваются по данным;
   - обе сущности имеют РОВНО по 3 совпадения по имени контрагента;
   - структурный признак «есть табличная часть» не различает: у поступления на счёт
     она тоже есть («расшифровка платежа») — гипотеза проверена и отвергнута.

   Что осталось бы для «правильного» ответа — допущение **«записи ведутся от лица
   нашей компании, поэтому покупка контрагента = наша продажа»**. Оно было в подсказке
   и удалено сознательно: снайпер показал, что на агрегаторе, маркетплейсе и госреестре
   это допущение переворачивает ответ. То есть эталон золотого набора опирается на
   предметное правило, от которого мы отказались ради коробочности.

   **Решение владельца:** либо принять 7/8 и признать вопрос неоднозначным (обе
   трактовки следуют из данных: Ромашка и покупала товары, и платила), либо вернуть
   допущение как НАСТРОЙКУ конфигурации («учёт от лица одной организации: да/нет»),
   определяемую при установке из данных, а не зашитую в код.
2. **135 числовых колонок** из 170 с данными не в корпусе. Отсюда «продано 36 штук»
   вместо 499: 36 — число документов, гейт пропускает, потому что число настоящее.
3. **86 датовых колонок** мимо корпуса; `doc_date` заполнена у 14% строк.
4. **9 сущностей не загрузились** (5993 строки), а `base_profile` пишется ДО загрузки
   и утверждает `problem=''`. Каскадом: 1940 ссылок из 2279 нерезолвнутых ведут именно
   в них.
5. Клиенту не показываются пробелы: 934 сущности закрыты правами, `base_profile` не
   читает ни один сервис.

**Масштаб** (агент `context-and-scale`)
- смысловой поиск — полный скан, 0,45 с на 98 тыс. строк, ~50 с на базе ×100;
- `have`/`seen`/`refmap` держат базу в памяти Python: ~37 ГБ на базе ×100;
- `money_column` спрашивает модель про каждую сущность в каждой сборке, кэша нет.

**Вектор**
`hnsw` в сборке 26.07.3 отсутствует и **отсутствует в исходниках** (проверен коммит
от 26.07). Демка `demo4` — 100 тыс. строк × 1536 измерений, ровно наш случай —
написана под `hnsw`. `ivf` на 1536: 10 000 строк, 61 МБ векторов → **30 ГБ памяти**,
не достроился за 40 минут, и **сборка не отменяется** (`pg_cancel_backend` → `t`,
работа продолжается; снимается только рестартом движка). Диагноз «`row_group_size`»
опровергнут замером. Вопрос к разработчикам SereneDB, идти с воспроизведением.

## Что сторожит без вызова

Хуки в `.claude/settings.json`: агент-снайпер перед `git commit` и промт-проверка
после каждой правки `.py`. Ловят велосипед поверх штатного механизма, подгонку под
нашу базу, рост величины с размером базы. **Сработал в этой смене**: заблокировал
коммит, где я снял стену argv, но не ограничил рост промпта.
