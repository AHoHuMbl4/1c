# Разметка журнала clarify/ответов okna — шаг 0 (18.08.2026)

Анализ, не код. Сервис не менялся. `ask_journal` не читали.

Назначение: частоты по кучам A–G, чтобы оркестратор назначал окна **по таблице**, не по анекдотам.

Снято: `journalctl -u 1c-serene-ask@postgres --since "3 days ago"` + тот же интервал у `1c-mcp-ask@postgres`. Юнит ask: **8428** строк, **2072** TRACE, **91** rid. Мост: **12** rid (все 18.08). На момент съёма `/opt/1c-mcp-reports/serene_ask.py` md5 **`fff3e6c67e1aa3d746b3719ee200f5e5`**, файл 18.08 17:04.

---

## Как считали

| Правило | Как в следе |
|---|---|
| Единица | один **rid** = один пользовательский заход (плагин держит rid на ход) |
| Реальный вопрос | rid есть **и** в ask, **и** в мосте `1c-mcp-ask` — путь бота (Telegram/WebUI) |
| Пробы оркестратора | прямой `POST /ask` без моста (**79** rid 18.08) — **исключены** из частот. `user=` в journald ask/mcp **нет** (TRACE не несёт user/текст); фильтр `audit-*`/`probe-*`/`e2e-*` по журналу невозможен, поэтому резали по мосту, не по префиксу |
| Фраза | восстановили по TRACE: `тип`/`величина`/`считать` + сущность вилки + `331` строк регистра = окно «вчера» (SQL). Текста вопроса в TRACE нет по построению |
| Чем кончилось | последний `kind` моста + был ли повтор того же цикла / клик `выбрал=человек` / тишина |
| 16–17.08 | TRACE/rid ещё не выкатывали; только `ask СЛЕД` + POST. В основную таблицу **не входят** (нет rid) |

Кучи (как в задании):

| | Смысл |
|---|---|
| **A** | верный ответ сразу (одно число, гейт чист, без вилки) |
| **B** | верная вилка → выбор → ответ |
| **C** | отказ / «нет данных» при данных в базе |
| **D** | вопроса класса в корпусе нет (остатки и т.п.) |
| **E** | rank/разрез: скаляр вместо топ-N или неверная ось/мера |
| **F** | чужая сущность / чепуховый clarify |
| **G** | 5xx / таймаут / техсообщение человеку |

---

## Частоты — бой через бота (12 rid, 18.08)

**A = 0.** Через мост за день ни одного «сразу одно число, гейт чист».

| Куча | rid | доля | Чем кончилось (из 12) |
|---|---:|---:|---|
| **F** | **8** | **67%** | 7 бросил на clarify; 1 нажал до figures после пачки `choice_error` |
| **G** | **2** | **17%** | 1 повторил тот же цикл 4×; 1 получил `answer` после гейта `{count}` и кликнул дальше |
| **E** | **1** | **8%** | нажал несколько раз, последним видел `figures` (скаляр, не топ-N) |
| **B** | **0** | 0 | (частичный B — хвост rid F, в частоте не двоим) |
| **A** | **0** | 0 | — |
| **C** | **0** | 0 | `kind=no_data` у моста не было |
| **D** | **0** | 0 | вопроса про остатки не было |
| вне A–G | **1** | 8% | `тип=None` / `list` / `figures` без сущности и счёта — фразу не восстановить |

По **последнему kind моста** (что увидел человек): `clarify` **8**, `figures` **3**, `answer` **1**.

По **уникальным фразам** (не по повторам одной и той же):

| Фраза (восстановленная) | rid | куча |
|---|---:|---|
| «сколько продали [вчера] всего?» | 9 | F (7) + G (1) + F/E внутри (1) |
| «реализация ТМЦ / итог» | 1 | G |
| «товар / количество» (list) | 1 | E |
| не разобрано | 1 | вне |

Повтор одной фразы **9 раз за утро–день** — это частота дефекта F, не девять разных вопросов.

---

## Примеры rid (по 3, где есть)

### F — чужая сущность / чепуховый clarify (8)

Ось `ДокументОснование`: выбор «документ реализация» **становится** `document_приходныйкассовыйордер` (за 17.08 в корпусе **4** ПКО против **331** строк регистра). Дальше вилка меры на кассе. Человек бросает.

| rid | время +03 | конец |
|---|---|---|
| `83ca8b222ffcb01f` | 07:46 | бросил на clarify меры ПКО |
| `41465af4f0aab639` | 08:09 | то же |
| `ac461bea23b569a7` | 10:48 | то же |
| ещё: `0fd49edbe985d638`, `af65f07a78c45846`, `7f430d05026b79fb`, `561ab69043f6cda0`, `46944ec05ee95f54` | | |

`46944ec05ee95f54` (08:27): **10** `choice_error` + клики в `document_начислениезп` и `catalog_строки5с` → затем `figures` исход B, 2 класса. Нажал до ответа; куча всё равно F (чепуха в вилке).

`7f430d05026b79fb` / `561ab69043f6cda0`: внутри запроса база считала **Себестоимость 508 180,41** (331 строка), мост всё равно отдал **clarify** (после счёта снова ось→ПКО). Снаружи F, внутри E.

### G — техсообщение (2)

| rid | время +03 | что в следе | конец |
|---|---|---|---|
| `efafedb47f2ce12c` | 11:45–11:56 | 24 вызова `разбор`; гейт `{count}` на 508 180,41 **четырежды** | тот же цикл повторил, бросил |
| `1fe32bfc88bae412` | 11:47–11:55 | книга продаж 330 / **766 510,44**, гейт `{count}`; мост `answer` | получил ответ, кликнул в ПКО |

На прямом `/ask` (не в частоте): `2c934d58b147960c` TypeError после all-time 8246 / 79 752 611,64; `c9272d5af13fe468` UnboundLocalError `n_folders`. Оба — куча G, оба закрыты выкатом 17:04 (ниже).

Третий пример G до rid: 16.08 **30×** `Connection refused` :7890 (503).

### E — скаляр / неверная мера (1 в бою + SQL)

| rid | время +03 | спросил | посчитал | SQL |
|---|---|---|---|---|
| `dafdf95548d8b0a4` | 16:53–17:01 | товар / количество / **list** | сначала хранение 19 / 21,0 (не продажи); затем регистр **77 381 / 2 122 670,96**, зерно group **1556** | регистр all-time Количество = **2 122 670,96**; хранение = **19 / 21,00**. Скаляр, не топ-N |

Внутри F-заходов «вчера» мера **Себестоимость 508 180,41** при живом **Всего 766 578,68** (SQL, 17.08, 331 строка). Это тот же класс E, но человек его часто не видел — мост уже ушёл в clarify.

Пустышка **Сумма = 0,00** на тех же 331 строках (SQL). В бою через мост не дошла до гейта; на прямом `/ask` — `29bb46f02037628f` и ещё 4 rid до выката d273e9d.

### A, B, C, D

| Куча | В бою через мост | Комментарий |
|---|---|---|
| A | 0 | эталон «сразу одно число» за день не случился |
| B | 0 как первичная | хвост `46944ec05ee95f54` — figures после F |
| C | 0 | `no_data` у моста нет. Исторический «банки Казань» в этом следе моста не повторялся |
| D | 0 | вопроса про остатки не было. В `search_tables` по «остат» только `document_взаимозачеты_остатки` — товарных остатков в корпусе нет; если спросят — будет D |

`6f9bc290ce74535f` (08:08): `тип=None`, `считать=list`, 4 вызова модели, мост `figures` за 16 с, сущности и счёта нет. В частоты A не клали.

---

## SQL (C/E), корпус okna 18.08

DSN из `/etc/1c-serene-ask-postgres.env`, пароль из `/etc/1c-mcp-reports.env`. Доки: `map_extract_value`, агрегаты SereneDB.

| Множество | count | сумма |
|---|---:|---:|
| регистр реализациятмц, 17.08 | **331** | Всего **766 578,68** · Себестоимость **508 180,41** · Сумма **0,00** · Количество 26 879,88 |
| тот же регистр, всё время | **77 381** | Количество **2 122 670,96** |
| книга продаж, 17.08 | 331 | Всего 766 578,68 |
| ПКО, 17.08 | **4** | — |
| ПКО, всё время | 3378 | — |
| документ реализациятмц, 16.08 | **0** | пустой день («позавчера» 18.08) |
| контрагенты | 349 | — |
| передача на хранение, номенклатура | 19 | Количество 21,00 |

C: отказа при живых данных в **боевом** следе моста нет. E: 508 180,41 и 2 122 670,96 в следе **совпадают с SQL до копейки** — это не «нет данных», это не та мера / не тот grain.

---

## Что закрыли правки 18.08 — есть ли случаи после выката

Выкат на юните: рестарты 15:16, **16:42** (5c22284), **17:04** (fff3e6c6, md5 файла).

| Коммит | Что должно исчезнуть | После выката в журнале |
|---|---|---|
| **f75b4e8** граница периода | «вчера» = 0 при 331 строке | с утра 18.08 регистр стабильно **331 / 766 578,68** (прямые rid `68c2744cc9514f8f`, `030ea959511891ed`, …). Повторов «вчера=0» **нет** |
| **d273e9d** пустышка «Сумма» + курсы на пустой день | гейт на Сумма=0; вилка «Курсы валют» | последний Сумма=0 — `29bb46f02037628f` **13:30** (до выката). После 15:16 таких rid **нет**. Курсы на «позавчера» ещё были в прямых rid 13:17–14:06 (`7c9ad84c86d259f8`, `b9f63557275dc8f4`, `eb02f45306208fae`) — это **до** 16:42 |
| **5c22284** пустое окно не снимать | all-time 79 млн и 503 вместо «0 за день» | 16:43 `c9272d5af13fe468`: счёт уже **строк=0**, но 503 `n_folders`. All-time больше не считал. G **не закрыт** этим коммитом |
| **fff3e6c6** имена до `period_empty` | тот 503 | 17:06 прямой rid `c92dfc20e8a9a13a`: документ, **строк=0**, ERROR в журнале **нет**. TypeError/UnboundLocal после 17:04 **0**. Через мост после 17:01 вопросов не было — живой бот это не подтверждал |

**Не закрыто этими четырьмя коммитами** (в бою после 17:04 прямые пробы есть, моста нет):

- **F** ось→ПКО — паттерн жив весь день, в т.ч. `1bb39a7e951b6d9f` 17:06 (прямой `/ask`)
- **E** Себестоимость вместо Всего; list→скаляр (`dafdf95548d8b0a4` 16:53–17:01, до fff3e6c6 на минуты)

---

## Приоритет окон (куча × цена)

Цена: сколько rid в бою × не даёт ли числа продаж × чинится ли уже выкатанным.

| # | Окно | Куча | Цена | Почему не анекдот |
|---|---|---|---|---|
| 1 | Ось `ДокументОснование`: документ реализации → **ПКО** | **F** | **8/12** боёвых rid, человек **не видит** 766 578,68 | один и тот же TRACE-шаблон 7 раз + чепуха ЗП/строки5с |
| 2 | Мера: «сумма продаж» → **Себестоимость** (508к) или пустышка Сумма | **E** | внутри тех же заходов; SQL 508 180,41 ≠ 766 578,68 | пустышка Сумма закрыта d273e9d; себестоимость в вилке меры **осталась** |
| 3 | Rank/list «какой товар» → скаляр Количество по регистру | **E** | 1 боевой rid, 4 `figures` | 2 122 670,96 = SQL all-time; зерно 1556 групп не отдано как топ-N |
| 4 | Гейт `{count}` / 503 на пустом дне | **G** | 2 боя + 2 прямых 503 18.08 | `{count}` — stop1 (5ca1b66); 503 пустого дня — 5c22284+fff3e6c6, **после 17:04 в журнале нет** |
| 5 | Вилка без A (count контрагентов → B, не число) | **B/не A** | в мосте 0; на прямом `/ask` `f9859de24c71bdd2` исход B, 6 классов | не бой через бота 18.08; окно B8-02 |
| 6 | C отказ при данных | **C** | **0** в мосте | не назначать окно «по банкам Казань», пока не всплывёт в бою |
| 7 | D нет класса в корпусе | **D** | **0** вопросов; товарных остатков в таблицах нет | окно только если продукт обещает остатки |

Окна 1–3 — живые. Окно 4 — проверить на мосте после 17:04 (в этом следе моста уже не было). 6–7 не кормить работой.

---

## 16–17.08 без rid (контекст, не частоты)

265 POST: 219×200, 40×503, 5×400, 1×401. СЛЕД: продажи/контрагенты/книга, исходы B (в т.ч. 106 и 129 классов — старый детектор) и C `uncounted_cell`. 16.08 днём движок :7890 падал пачками. Это оркестраторские прогоны B7/вилка + простой базы, не таблица боя.

---

## Исключённые 79 rid прямого `/ask` 18.08

Не смешивать с боем. Паттерны (для сверки, что фильтр не сжевал живое): «вчера»→ПКО 4 шага; «вчера»→регистр+Всего 766 578,68 гейт чист (билеты оркестратора); «позавчера»→курсы / документ 0 строк; контрагенты исход B; TypeError/UnboundLocal на пустом дне. Это воспроизведения окон, не клиент.
