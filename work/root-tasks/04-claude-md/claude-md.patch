--- CLAUDE.md
+++ CLAUDE.md
@@ -53,7 +53,7 @@
 | Сервер | Что это | Правило |
 |---|---|---|
-| **`serenedb-docs`** | официальные доки SereneDB (api.serenedb.com, ~3100 разделов, гибридный поиск) | 🔴 **Обязателен перед любой работой с базой.** Прежде чем писать SQL, создавать индекс или утверждать «штатного средства нет» — `search_docs`, затем прочитать раздел по url находки. Отрицательный вывод без похода в доки не принимается. 🔴 **С 03.08 держится остановкой:** `check-sql-docs.sh` останавливает коммит, в котором в добавленных строках появился SQL, и требует назвать раздел доков. **Закрывается это самим коммитом** (04.08): строка `Доки: <раздел>` в сообщении — гейт её видит и пропускает, а раздел уезжает в историю вместе с кодом. Люк владельца для этого больше не нужен. Сам факт обращения к докам хук проверить не может (вызова MCP в диффе нет) — он не даёт проскочить момент. Повод: `[замер 03.08]` за день доки не спрашивались ни разу. ⚠️ `read_section` был сломан на их стороне 02.08 («No section found» на любые url) — обходили `WebFetch`-ом по тому же url. **[замер 22.08] починен: раздел возвращается целиком** — пользуемся `read_section`, WebFetch остаётся запасным. |
+| **`serenedb-docs`** | официальные доки SereneDB (api.serenedb.com, ~3100 разделов, гибридный поиск) | 🔴 **Обязателен перед любой работой с базой.** Прежде чем писать SQL, создавать индекс или утверждать «штатного средства нет» — `search_docs`, затем прочитать раздел по url находки. Отрицательный вывод без похода в доки не принимается. 🔴 **С 03.08 держится остановкой:** `check-sql-docs.sh` останавливает коммит, в котором в добавленных строках появился SQL, и требует назвать раздел доков. **Закрывается это самим коммитом** (04.08): строка `Доки: <раздел>` в сообщении — гейт её видит и пропускает, а раздел уезжает в историю вместе с кодом. Люк владельца для этого больше не нужен. Сам факт обращения к докам хук проверить не может (вызова MCP в диффе нет) — он не даёт проскочить момент. Повод: `[замер 03.08]` за день доки не спрашивались ни разу. **[замер 22.08] `read_section` починен — раздел возвращается целиком**; пользуемся `read_section`, WebFetch остаётся запасным. |
 | **`memory`** | граф знаний между сессиями; хранится в `memory_bank/mcp-memory.json` (в git) | вспомогательный, **впрок не заполняется**. Источник истины — файлы `memory_bank/*.md`; граф — только когда нужны именно связи между сущностями, чтобы не плодить второе место, где факты расходятся. Изменил граф — файл коммитится. Путь в `.mcp.json` абсолютный (`/srv/1c`). |
