# Tech Context

## Платформа
- **Продакшн (у клиента):** 1С:ERP Управление предприятием, версия «производство». К ней НЕ подключаемся, пока не отработаем всё на стенде.
- **Тестовый стенд (наш):** Windows `10.8.0.58` — здесь тестируем весь пайплайн (версия платформы — снять при инвентаризации; для 1C MCP Toolkit нужна 8.2.13+ / 8.3.25+).
- Тестируем MCP-серверы для 1С — детали и сравнение: `docs/research/1c-mcp-server-research.md`.

## Инструменты
- Git — версионирование (этот репозиторий).
- Формат хранения кода — уточнить: выгрузка конфигурации в файлы (Конфигуратор → «Выгрузить конфигурацию в файлы») / EDT-проект / внешние обработки `.epf` + исходники.

## Окружение разработки
- Рабочая папка: `/srv/data/cursor/cursor/1/1c` (Linux-машина владельца).

## Целевые машины (правила владельца, 2026-07-22)
1. **Сам проект** разворачиваем на LXC **`lxc-claude-1c`**, IP `192.168.56.42` (Ubuntu).
   Инвентаризация 2026-07-22 (доступ `root@192.168.56.42` по ключу — работает): Ubuntu 24.04.4 LTS, 6 CPU, 62 GB RAM, диск 360 GB (238 свободно), Python 3.12.3; PostgreSQL и Docker не установлены.
2. **1С** будет на **Windows**, доступ: `ssh unde@10.8.0.58` — работает (ключ добавлен 2026-07-22 в administrators_authorized_keys).
   Инвентаризация 2026-07-22: Windows 11 (build 26100), 2 CPU, 8 GB RAM, диск C: 36 GB свободно из 60. **1С НЕ установлена** (реестр/сервисы/процессы/ibases.v8i пусты), IIS нет. PowerShell 5.1. ⚠ Для длинных команд по SSH использовать `powershell -EncodedCommand` (base64 UTF-16LE) — прямое экранирование через bash→cmd ломается.
   → Подготовка стенда 2026-07-22 (сделано Claude): каталоги `C:\1c\{repo,bases,backups,distr,logs}`; git настроен (UNDE/admin@unde.life); репозиторий клонирован в `C:\1c\repo` (обновление: `git -C C:\1c\repo pull`); бэкап-скрипт — `C:\1c\repo\windows\scripts\backup-1c.ps1`.
   → **Платформа на стенде: 8.3.27.1786 x86, установлена 2026-07-22 по инструкции владельца** из `Downloads\Бухгалтерия_8 ред_3.0_Проф\windows_8_3_27_1786-x86\` (msiexec /qn exit 0, лог `C:\1c\logs\install_1c_8327.log`). Компоненты: конфигуратор+толстый (1cv8), тонкий (1cv8c), сервер кластера (ragent/rmngr/rphost), консоль rac, веб-расширение wsisapi.dll. Windows-сервис сервера НЕ зарегистрирован (тихий MSI не делает) — при переходе в клиент-серверный режим: `ragent.exe -instsrvc`. Версия в диапазоне совместимости MCP Toolkit (8.3.25+). Рядом в Downloads — дистрибутив конфигурации «Бухгалтерия 3.0 Проф» (не ERP!).
   → История: тестовая 8.5.4.1306 ставилась и **полностью удалена** по решению владельца (лицензию требовала, комьюнити-активация не проверялась).
   → Лицензия: бесплатная **комьюнити-лицензия** с developer.1c.ru (учётка у владельца есть; активация — GUI-диалог «Получить лицензию» при первом запуске) — ещё НЕ активирована.
   → **Блокеры фазы 2: активация лицензии (владелец, GUI) + тестовая база (`.dt`)**. NB: скачана Бухгалтерия 3.0, а продакшн у клиента — ERP «производство»; для механики MCP-теста Бухгалтерии хватит, для реализма запросов нужна ERP — уточнить у владельца.

## LLM / бот-слой
- Ключ DeepSeek API — в `credentials/deepseek.env` (папка в `.gitignore`, в репозиторий не попадает).
- Ключ Alibaba (Model Studio) — в `credentials/alibaba.env` (тоже вне git; добавлен 2026-07-22).
- **Бот-слой: у владельца есть боевое решение, инфу даст позже (2026-07-22).** OpenClaw не проектируем; наша граница — MCP/API «второго мозга», к которому боевой бот подключится.
- ⚠ Решение владельца требуется: DeepSeek — облачный API, данные 1С будут уходить наружу; альтернатива — self-host LLM (см. план).

## Доступ к серверам
- SSH-ключ Claude (dedicated, только для этого проекта): приватный `~/.ssh/id_ed25519_1c` на машине владельца, публичный добавляется на целевые машины.
- Подключение: `ssh -i ~/.ssh/id_ed25519_1c <user>@<host>`.

## Ограничения
- BSL-код (модули `.bsl`) пишем в кодировке и стиле исходной выгрузки.
- Не выдумывать структуру метаданных — только из реальной выгрузки/задачи владельца.
