# Tech Context

_Обновлено 2026-07-27. Полный стек с версиями — `docs/TARGET_ARCHITECTURE.md` §0._

## Что где живёт

| Узел | Что | Версия / порт |
|---|---|---|
| Windows | 1С + Бухгалтерия 3.0, публикация OData через IIS | платформа 8.3.27.1786 (x86) |
| Windows | PostgreSQL — целевое хранилище сырых данных (решение 26.07) | 17.6 x64, служба `postgresql17`, порт 5432 |
| Ubuntu .42 | шлюз OData: Bearer, только GET, режет `..` | `:6011` |
| Ubuntu .42 | SereneDB: корпус, профиль сущностей, индекс | **26.07.3**, `:7890`, PG-протокол |
| Ubuntu .42 | сервис ответов | `:8091`, юнит `1c-serene-ask` |
| Ubuntu .42 | MCP для бота | `:6014` / `:6015` |
| **свой контур** | эмбеддинги `Qwen3-Embedding-8B` BF16, dim **1024**, и реранкер `Qwen3-Reranker-8B` | 🔴 **02.08 съехали с DashScope**: аккаунт ответил `Arrearage` на все 11 ключей, бот отдавал 503 на каждый вопрос. Прежнее: `text-embedding-v4`, 1536 |
| облако | намерение, выбор сущности, формулировка | DeepSeek |

Секреты — `/etc/1c-*.env`, chmod 600, в git не попадают. Реквизиты PG на Windows —
`/etc/1c-pg-windows.env`.

## SereneDB 26.07.3 — что важно знать

**Настройки по умолчанию** (менять осознанно):
```
sdb_ivf_posting_size = 1024   sdb_nprobe = 8        threads = 6
sdb_ivf_sample_factor = 0     sdb_rerank_factor = 4 memory_limit = 50 GiB
sdb_scored_terms_limit = 1024 (молча усекает ранжирование широких запросов)
```

**Ловушки, проверенные замером:**

| Что | Суть |
|---|---|
| `quant` по умолчанию `none` | с `metric='cosine'` квантизация **запрещена вообще**; работает только с `l2`/`ip` |
| При `quant='none'` нет переранжирования | точный пересчёт кандидатов не выполняется никогда |
| `ts_regexp` термозависим | на кириллице `догово..` даёт 1678 вместо 565 — молча **неверный** ответ, а не ноль |
| `ts_offsets` в байтах | для кириллицы вдвое больше символьных |
| `WHERE` в `ts_dict_*` | фильтрует **документы**, а не термы |
| Один скорер на запрос | `Only one scorer function is allowed`; обход — `UNION` |
| `USING secondary` | молча создаёт индекс **без колонок** при неизвестном опклассе |
| Сборка `ivf` не отменяется | `pg_cancel_backend` возвращает успех, работа идёт; только рестарт |
| `CREATE INDEX` с `ivf` поверх заполненной таблицы | виснет от 6 000 строк. Рабочий порядок обратный: индекс на **пустой** таблице, потом вставка (5-10 тыс. строк/с) |
| Незавершённая работа с `ivf` попадает в **WAL** | и проигрывается при каждом старте. 27.07: движок ушёл в **петлю OOM-рестартов** — RSS рос ~19 МБ/с до OOM, рестарт, снова. Сам не выходит. Лечение: остановить, снять копию, отвести `store.db.wal` в сторону, стартовать (10 с). Боевые данные при этом целы, если они уже в `store.db` — проверять `strings` по WAL на имена боевых таблиц ПЕРЕД тем, как отводить |
| `VACUUM (REFRESH_TABLE)` на таблице с `ivf` | **самая дорогая из известных**: 98 тыс. строк одним куском — повис и съел **34 ГБ**, движок ушёл в WAL-recovery. Вся отложенная кластеризация делается здесь. Публиковать порциями |
| `lower()` и кириллица — **наоборот, работает** | в коде (`serene_report.py:264,394`) записано «DuckDB `lower()` НЕ лоуэркейсит кириллицу» — это НЕВЕРНО: `lower('АБВгд')` → `абвгд`, сборка с ICU. На ложном знании построены `text_columns()` и `_schema_tables()` |
| `hnsw` | **удалён из продукта**, есть только в устаревших демках |
| `read_text` ходит по **HTTP** | движок сам забирает внешние источники: `$metadata` 1С (16,6 МБ) за 12 с. Заголовок авторизации — через `CREATE SECRET (TYPE http, EXTRA_HTTP_HEADERS MAP{...})` |
| `read_xml` **нет** и не будет | `INSTALL` запрещён архитектурно («extensions are compiled into the server binary»). XML разбирается `regexp_extract_all` — 4 585 сущностей за 125 мс |
| 🔴 «Временный» секрет **переживает сессию** | `CREATE TEMPORARY SECRET` виден из ЛЮБОЙ другой сессии, пока не сделан `DROP SECRET`. Я на этом ошибся: решил, что шлюз пускает без токена, а запрос шёл на чужом секрете |
| `CREATE PERSISTENT SECRET` падает | `Failed to create directory "/home/serenedb": Permission denied` — секрет создаётся заново каждой сборкой |
| `ai_embed` **сам группирует строки в пачки** | и получает отказ провайдера: у DashScope предел **10** строк на запрос и **33 000** символов на вход. Ручки размера пачки в настройках движка **нет** — резать приходится самим запросом |
| `ai_embed` отдаёт размерность **модели** | параметра размерности нет: наш эндпоинт даёт **1024**. Колонка объявляется под модель, а не наоборот |
| `ALTER COLUMN TYPE` для вектора не работает | `FLOAT[1536]` → `FLOAT[1024]` не приводится. Рабочий путь: `DROP COLUMN` + `ADD COLUMN`; **инвертированный индекс это переживает** (проверено на копии) |
| Коррелированный подзапрос к VIEW с оконной функцией **роняет планировщик** | `LogicalProjection::GetExpression - table index mismatch`. Обход: материализовать представление (`CREATE TABLE AS`) или заменить на `LEFT JOIN` |
| `[[:alpha:]]` **не понимает кириллицу**, `\p{L}` понимает | доля букв, посчитанная POSIX-классом, на русской базе даёт ноль |
| `UNPIVOT` роняет `NULL` | ячейка со `NULL` исчезает вовсе. Если позиция значима (составной ключ), `coalesce` ставится **до** `UNPIVOT` |
| `query_table($1)` в `PREPARE` перепривязывает `COLUMNS(*)` | одна подготовленная формула работает на таблицах разной ширины: 14 / 38 / 57 колонок. Так обходится цикл по таблицам в питоне. Но `query_table(колонка)` — нельзя, только литерал |
| 🔴 Параллельный `UPDATE ... ai_embed(...)` в ОДНУ таблицу роняет движок | [замер 28.07] четырнадцать таких запросов одновременно — `SIGSEGV`, systemd поднял через 3 с, данные уцелели, но фоновый досчёт встал **молча**. Обход: каждый поток пишет в СВОЮ таблицу, перенос одним запросом в конце. Кандидат в баг-репорт вендору |
| Секреты исчезают при рестарте движка | они живут в памяти; после падения и подъёма `ai_embed` перестаёт работать с `secret not found`, пока секрет не создан заново |
| Запись однопоточная | DDL во время массовой записи встаёт в очередь и блокирует чтения. Проверки на копиях ставить **до** или **после** записи |

**Инструмент, о котором стоит помнить:** `CREATE TEXT SEARCH DICTIONARY zz(help)` —
движок печатает справочник всех шаблонов и опций **именно нашей сборки**. Не нужно
гадать по репозиторию.

**Скореры:** доступны `bm25`, `tfidf`, `lm_jm`, `lm_dirichlet`, `dfi` + служебные
`raw_*`. Прогнаны все на приёмочном наборе 27.07, выбрана `lm_dirichlet`.
⛔ `indri_dirichlet` **не годится**: в боевой форме запроса молча отдаёт **ноль строк**,
а в агрегате сообщает «requires an inverted index scan in the same sub-query».

**Реранкер вместо эмбеддинга метки.** Сопоставление слова человека с названием сущности
эмбеддингом **не решается**: у «продажи» ближайшая метка — «Склады» (0,521), нужный
документ не входит даже в тройку; у английского «sales» — то же. Модель-реранкер
(`qwen3-rerank`, тот же ключ) ставит «Реализация Товаров Услуг» первым (0,585).
Замер 27.07, 226 названий нашей базы.

## Наши границы

| Величина | Значение | От чего зависит |
|---|---|---|
| строк в корпусе | 97 965 | размера базы |
| сущностей объявлено в OData | 4 585 | конфигурация 1С |
| отобрано под витрину | 235 | фильтр значимых сущностей |
| реально загружено | 226 | 9 не загрузились, `problem=''` в профиле — **открытый дефект**, см. п. 13 `TARGET.md` |
| размерность вектора | **1024** | эмбеддер (было 1536 у DashScope) |
| контекст модели на вопрос | 7,4–22 тыс. символов | ограничен бюджетом |
| запросов к БД на вопрос | 5–6 | **не растёт** с базой |
| смысловой поиск | 314-511 мс полным перебором | числом строк; разброс — от формы запроса (`<=>` против `array_cosine_similarity`), это не один замер |

---

_Ниже — архив предыдущей версии._


Полная архитектура — `docs/ARCHITECTURE.md`; раскатка — `docs/RUNBOOK_DEPLOY.md`.
Здесь — окружение, машины, доступы и готчи автоматизации (важно для будущих сессий).

## Машины
- **LXC `lxc-claude-1c` `192.168.56.42`** (Ubuntu 24.04, 6 CPU, 62 GB RAM, ~238 GB своб.,
  Python 3.12) — весь наш код: OData-шлюз, ETL, config-ui, braine. Доступ `root@192.168.56.42` по ключу.
- **Windows-стенд `10.8.0.58`** (Win11, 2 CPU, 8 GB, PowerShell 5.1) — 1С + IIS.
  Доступ `ssh unde@10.8.0.58`. IIS-адрес для проброса — `192.168.122.141:80`.
  ⚠ Диск ~60 GB, тесно (загрузки владельца). Раскладка: `C:\1c\{repo,bases,backups,distr,logs}`,
  `C:\inetpub\1c` (публикация IIS).
- **Роутер `192.168.56.1`** (владельца) — проброс порта на Windows-IIS:80. LXK ходит на
  1С через `192.168.56.1:<порт>`.
- Продакшн у клиента — 1С:ERP «производство»; к ней НЕ подключаемся, пока не отработано на стенде.

## Стенд (готово)
- Платформа 1С **8.3.27.1786 x86** (компоненты: клиенты, конфигуратор, сервер, консоль,
  веб-расширение wsisapi.dll). Комьюнити-лицензия активирована.
- База **Бухгалтерия 3.0.190.11** → `C:\1c\bases\buh_test` (файловая). Первый бэкап есть.
- Опубликована на IIS, OData включён, состав — все справочники+документы; пользователи
  `admin` + `ai_reader` (Только просмотр).

## Доступ по SSH
Ключ Claude (dedicated): `~/.ssh/id_ed25519_1c`. `ssh -i ~/.ssh/id_ed25519_1c <user>@<host>`.
На Windows ключ — в `C:\ProgramData\ssh\administrators_authorized_keys`.

## ⚠ Готчи автоматизации Windows по SSH (сэкономят часы)
- Длинные PowerShell по SSH — только `powershell -EncodedCommand` (base64 UTF-16LE) ИЛИ
  `scp` файла + `-File`. Экранирование bash→cmd ломает `$`/кавычки/кириллицу; лимит cmd ~8191.
- `.ps1` с кириллицей — UTF-8 **с BOM** (PS 5.1 без BOM = ANSI → синтакс-ошибки).
- COM-коннектор: `regsvr32 comcntr.dll` из каталога `bin` платформы; класс `V83.COMConnector`;
  вызывать из **32-битного** PowerShell (`C:\Windows\SysWOW64\WindowsPowerShell\v1.0\powershell.exe`).
- COM-квирк: свойства (`Metadata`) отдаются только рефлексией; методы коллекций метаданных
  (`Найти`/`Count`) НЕ резолвятся, но **`foreach` по коллекции работает** (так задавали состав OData).

## Ключи и секреты (все в `credentials/`, вне git)
- `deepseek.env` (генерация), **`/etc/1c-embed.env` (эмбеддер и реранкер — одно место на продукт)**, `alibaba.env` (DashScope — снят 02.08),
  `gitlab-1c-test.env` (KB-репо money/1c-test id 95), `telegram-1c-bot.env` (@test1c_mcp_bot + owner id 5949699699),
  `mcp-toolkit.env` (dev). На LXC — `/etc/1c-odata-gateway.env`, `/etc/1c-etl.env`, `/etc/1c-etl-selected.txt` (chmod 600).
- Пароли 1С (`admin`/`ai_reader`) — у владельца; ai_reader — в `/etc/1c-odata-gateway.env`.
- ⚠ Приватность: DeepSeek/DashScope облачные — на проде с реальными данными решить (self-host vs компромисс).

## Инвариант и правила
- 1С только читаем (два слоя: `ai_reader` + GET-only шлюз). Перед изменением базы — бэкап.
- На серверах менять состояние только по явной инструкции владельца.

## Ловушка 15: `VACUUM (REFRESH_INDEX)` ВНУТРИ транзакции — тихий no-op
[замер 28.07] `BEGIN; INSERT; VACUUM (REFRESH_INDEX); COMMIT;` — строка поиску НЕ ВИДНА
(найдено 0), ошибки нет. Те же команды разными вызовами — видна. Файл, пишущий в корпус,
нельзя оборачивать в один `BEGIN…COMMIT`: запись закрывается своим `COMMIT`, публикация
индекса идёт отдельной командой.

## Ловушка 16: секрет `TYPE http` без `SCOPE` перебивает заголовки других запросов
[замер 28.07] секрет с `EXTRA_HTTP_HEADERS Authorization` без `SCOPE` имеет область `{}`
(ВСЕ адреса) и его заголовок перекрывает тот, что ставит `ai_embed`: эмбеддер уходит в
облако с токеном шлюза 1С и получает `HTTP 401`. Лечится `SCOPE '<адрес>'`.

## Ловушка 17: функции ранжирования `dfi` и `lm_jm` в `ORDER BY` молча теряют строки
[замер 28.07] один отбор (`doc @@ ts_phrase(...)`, 3 совпадения): `bm25`/`tfidf`/
`lm_dirichlet` → 3 строки, а `dfi`/`lm_jm` → **0**, без ошибки. Сервис был на `dfi` и
отдавал отказ при найденных данных. Рабочие: `bm25`, `tfidf`, `lm_dirichlet`.

## Ловушка 18: `psql` возвращает НОЛЬ после ошибки SQL без `ON_ERROR_STOP`
Без `-v ON_ERROR_STOP=1` процесс завершается УСПЕШНО с пустым выводом. Проверка кода
возврата в вызывающем коде бесполезна, ошибка запроса неотличима от «не нашлось».

## Ловушка 19: `map_from_entries` над ПУСТЫМ списком даёт NULL, а не пустую карту
[замер 28.07] `len(map_keys(NULL))` — тоже NULL, поэтому `FILTER (WHERE len(...) = 0)` не
срабатывает НИКОГДА. Счётчик «строк без величин» без `nums IS NULL` покажет 0 при любом
числе таких строк.

## Ловушка 20: регистры 1С приходят по OData ОБЁРТКОЙ, и типы объявлены не там

[замер 28.07] `AccountingRegister_Хозрасчетный`: коллекция возвращает **одну** запись
(регистратор), внутри неё список из 280 движений; `$count` при этом даёт 104 — три
разных числа о разном, сравнению не подлежат.

Следствия, каждое стреляет в своём месте:
- **объявленный ключ принадлежит ОБЁРТКЕ**, а не движению: дедуп по нему срезает
  большинство строк, а сборщик корпуса получает дубли ключа;
- **поля движений объявлены ОТДЕЛЬНОЙ сущностью** `<имя>_recordtype`. Классификатор,
  ищущий свойство только у самой сущности, ставит им `skip` и выбрасывает из текста —
  данные лежат в витрине целыми и до поиска не доходят;
- колонка, объявленная И у обёртки, И у вложенного типа, при соединении даёт ДВЕ строки
  на одну колонку → `Map keys must be unique` в `map_from_entries`.

Правило: искать объявление и во вложенном типе (имя начинается с имени сущности), но
оставлять ОДНО — приоритет собственному объявлению, вложенное уточняет, а не заменяет.

## Ловушка 21: `map_from_entries` падает на неуникальных ключах

`Map keys must be unique` — не предупреждение, а остановка запроса. Возникает всякий раз,
когда источник ключей содержит дубли: например, соединение классификации размножило
колонку. Чинится на источнике (`QUALIFY row_number() … = 1`), а не подавлением ошибки.

## Ловушка 22: ключ строки из `sha1(doc)` хрупок к ПРЕДСТАВЛЕНИЮ, не только к данным

У сущностей без уникального объявленного ключа (независимые инфорегистры) `row_key` в
корпусе = `sha1(doc)`. Значит ключ зависит от того, КАК записано значение, а не только от
самого значения. [замер 28.07] дважды это ложно пересобрало ключ у ~22 тыс. строк при
НЕИЗМЕННЫХ данных:
- `false` (JSON 1С) → `False` (питоновское `str(bool)`);
- дата `2022-02-07T00:00:00` (ISO из 1С) → `2022-02-07 00:00:00` (движок).
Слияние честно останавливалось защитой «>10% удаления». Урок: загрузчик обязан писать
значения в ОДНОМ каноническом виде (тут — как отдаёт 1С/движок), иначе смена одного
символа перекидывает ключ у всех строк, и это выглядит как массовое изменение данных.
Починка — нормализация в `_cell` (`poc_load_entity.py`).

## Ловушка 23: ответ модели рвётся по пределу длины, а выглядит как «модель не умеет»

[замер 29.07] разметчик сущностей отдал 581 из 1 501 и всего **один** business — вывод
напрашивался «модель не справляется со смыслом». Прямая проверка на трёх сущностях
показала обратное: `catalog_партнеры` → business, `informationregister_замерывремени` →
service, `document_реализациятоваровуслуг` → business. Дефект был в размере обращения:
40 сущностей в пачке × лимит `max_tokens` 2 000 — JSON обрывался на середине и не
разбирался целиком, то есть терялась вся пачка, а не хвост.

**Как отличить одно от другого:** прогнать спорную единицу ОТДЕЛЬНО. Верный ответ на
одиночном обращении означает, что дело в объёме, а не в способности.

**Как чинить:** не «поднять лимит и надеяться», а **делить пачку пополам при неразборе**,
рекурсивно, до одной единицы. Тогда предел находится сам и на любой модели. Плюс лимит
ответа считать от размера пачки, а не задавать наугад.

## Ловушка 24: приоритет очереди сортировкой ломает зонные карты

Соблазн «сначала важное» решается через `ORDER BY приоритет` — и это дорого. В
`embed_missing.sh` колонка `chunk` монотонна в физическом порядке ключа, поэтому выборка
пачки стоит [замер] 0,45–1,36 мс на 97 965 строках вместо полного скана. Пересортировка
по приоритету монотонность рушит.

**Как надо:** приоритет задавать **условием отбора строк** (`ROWS_WHERE`) и делать
несколько проходов — внутри каждого порядок остаётся ключевым. Обязательное условие:
последний проход идёт БЕЗ сужения, иначе это уже не очерёдность, а потеря данных (п. 13).

## Ловушка 25: `duckdb_tables()` — таблицы ВСЕХ присоединённых баз (третий укус)

Уже записано как `Н-4` для `corpus_build.sql`, но ловушка кусает везде, где по имени
таблицы что-то ищут. [замер 29.07] из базы `postgres` было видно **5** рабочих таблиц
досчёта, принадлежащих базе `ut_test`, — имена рабочих таблиц одинаковы, потому что
строятся по имени целевой.

Чем грозит там, где нашлось (`embed_missing.sh`): перенос подставил бы в `UPDATE` таблицу
чужой базы (вектор чужих данных на нашей строке), уборка снесла бы чужую оплаченную
работу. Оба вреда молчаливые.

**Правило: любой `duckdb_tables()` / `duckdb_indexes()` / `duckdb_secrets()` в коде
обязан нести `AND database_name = current_database()`**, если только цель прямо не в том,
чтобы смотреть на соседние базы. Имя текущей базы — у движка, не из разбора DSN
(`HOW_NOT_TO §3.14`).

🔴 **Но фильтр — второй рубеж, а не первый.** Указание владельца 29.07: «разве так сложно
отдельные таблицы сделать?». Фильтр надо ПОМНИТЬ в каждом новом запросе, и за два дня я
забыл его трижды. Первый рубеж — **имена, при которых столкновение невозможно**: рабочие
таблицы досчёта теперь зовутся `emb_<база>_<таблица>`.

При смене схемы имён обязательна **докатка из прежних имён**: [замер 29.07] под старой
меткой в `ut_test` лежал 2 121 уже оплаченный вектор. И уборка должна перечислять ровно
те таблицы, которые скрипт создаёт, а не «всё по шаблону `метка_%`»: со звёздочкой метка
может задеть чужую цель, если база названа префиксом имени таблицы.

## Ловушка 26: секреты движка — ОБЩИЕ НА ИНСТАНС, базы их не разделяют

[замер 29.07] `CREATE SECRET qwen` в базе `ut_test` и `DROP SECRET qwen` оттуда же влияют
на базу `postgres`: пространство имён секретов одно на весь инстанс. Сборка второй базы,
упав, снесла своим трапом секреты — и идущий такт первой базы получил
`ai_embed: secret 'qwen' not found`.

**Правило:** имена секретов обязаны нести имя базы (`odg_<база>`, `qwen_<база>`), как и
имена рабочих таблиц. И шире — у движка три РАЗНЫХ пространства имён:

| Что | Область | Чем грозит |
|---|---|---|
| таблицы | внутри базы, но `duckdb_tables()` видит все | взять/снести чужую (ловушка 25) |
| **секреты** | **весь инстанс** | погасить чужой эмбеддер (эта ловушка) |
| файловые замки | наши, имя выбираем мы | развести/свести такты (`HOW_NOT_TO §3.14`) |

Прежде чем дать имя общему объекту — спросить, в каком пространстве имён он живёт.

## Ловушка 27: `Edm.Stream`/`Edm.Binary` в OData 1С валят ВЕСЬ ответ, а не поле

[замер 29.07, УТ 11.4.9] `InformationRegister_ИсточникиДанныхВариантовАнализаЦелевых-
Показателей` отвечал `HTTP 500` при любом размере страницы, вплоть до `$top=1`, тогда как
`$count` честно отдавал 37. Причина — поля хранилища значения: `ИсточникДанных`
(`Edm.Stream`) и `ИсточникДанных_Base64Data` (`Edm.Binary`). Исключил их через `$select`
— пришли все 37 строк с кодом 200.

**Но наличие такого поля НЕ означает отказ.** [замер] `Catalog_ШаблоныПоясненийДляФНС`
имеет ровно те же два типа, 35 строк, и отдаётся с кодом 200. Отказ зависит от
СОДЕРЖИМОГО хранилища, а не от объявления. Типов с такими полями: **961 из 4 585** на
первой базе, **470 из 2 795** на УТ — это размер подверженности, не число поломок.

**Отсюда правило:** сужать `$select` можно только как ЗАПАСНОЙ вариант, после отказа.
Сузить всем подряд — значит выбросить колонки там, где всё работает, сменить схему
таблиц витрины и переписать ключи строк корпуса (ловушка 22), то есть своей же починкой
устроить массовую ложную «смену данных».

Порядок в `poc_load_entity._get_json`: как обычно → повтор (часть отказов разовые) →
без неотдаваемых полей → и только потом честный отказ с числом в переписи.

## Ловушка 28: `ai_embed` занимает ИСПОЛНИТЕЛЯ движка, и пул по умолчанию = число ядер

[замер 29.07] векторизация корпуса шла 9,2 строки/с при 12 потоках `psql`. Подняли до 64 —
скорость **та же**, а движок перестал отвечать на посторонние запросы. Прямой замер
исходящих соединений показал причину: `serened` держал к провайдеру **5-6 соединений**,
сколько бы SQL-запросов ему ни дали.

**Почему.** `ai_embed` — сетевой вызов внутри запроса, и запрос занимает исполнителя на
всё время ожидания ответа. Флаг `--cpu_threads` по умолчанию `0`, то есть пул равен числу
ядер (здесь 6). Отсюда ровно 5-6 одновременных запросов наружу — потолок **внутри
движка**, а не у провайдера и не в сети.

**Что из этого следует, и это важнее самой цифры:**
- число наших `psql`-потоков **не является** ручкой скорости, пока пул меньше;
- «купить второй ключ API» — лечение болезни, которой нет: до квоты провайдера при шести
  запросах бесконечно далеко;
- работа сетевая, а не счётная ([замер] под полной нагрузкой процессор всех процессов в
  нуле, память 9 из 62 ГБ), поэтому **переподписка пула безопасна**: исполнители не
  считают, а ждут.

**Настройка** (`/etc/serenedb/serened.conf`, 29.07): `--cpu_threads=96`, `--io_threads=8`.
Второй флаг тоже обязателен: по умолчанию он `max(1, ядер/4)` = 1, и один io-поток
сериализовал бы 64 соединения.

**Метод, который здесь сработал:** когда параллельность не даёт прироста, считать не
свои потоки, а **исходящие соединения процесса, который реально ходит наружу**
(`ss -tnp | grep serened`). Это сразу отделяет «нас режут снаружи» от «мы сами себя
ограничиваем внутри».

## Ловушка 29: `Ref_Key` табличной части — это ключ РОДИТЕЛЯ, и карта ссылок становится неоднозначной

[замер 29.07, УТ] карта ссылок: 50 535 строк на 42 107 различных идентификаторов, из них
**223 идентификатора с несколькими именами**. Причина: карта собирается по каждой
таблице, а у табличных частей документа (`Document_X_Товары`) поле `Ref_Key` содержит
ключ **родительского документа**, и каждая строка части добавляет в карту своё имя.

**Чем это кончилось.** У сущностей без объявленного ключа `row_key = sha1(doc)`, а в `doc`
подставляется имя из карты. Произвольный выбор имени → другой текст → другой ключ.
Пересборка тех же данных изменила ключ у **64 107 строк из 632 683**, и слияние
остановилось защитой «удаление снесло бы 10% корпуса». Без этой защиты корпус на каждом
такте терял бы вектора у десятой части строк и пересчитывал их за деньги.

**Правило:** любая карта «идентификатор → имя» обязана быть **однозначной по построению**
(одна строка на идентификатор) с **детерминированным** выбором. Годный порядок: сначала
владелец, где идентификатор встречается один раз (настоящий объект, а не строка
табличной части), затем устойчивые поля — имя таблицы, само имя.

**Как ловить такое вообще:** сравнение «сколько строк было и сколько стало» этот дефект
НЕ видит — число совпадало точно. Видит только сверка ключей.


## Ловушка 30: `ORDER BY` без разделителя равенства делает сборку невоспроизводимой

Продолжение ловушки 29. После того как карта ссылок стала однозначной, расхождение ключей
упало с 64 107 до 1 299 — но не до нуля. Второй источник оказался в склейке имени:
`list(val ORDER BY nc.std, nc.score DESC)`. При **равных** `(std, score)` порядок
произволен, склеенное имя выходит разным, и вместе с ним меняется `sha1(doc)`.

[замер 29.07] имён из нескольких колонок 27 177 из 42 107; групп с равными приоритетами
внутри таблицы — 30. Добавлен `nc.col` последним ключом сортировки → **0 расхождений**.

**Правило:** в любом `ORDER BY`, результат которого попадает в данные (а не только в
показ), последним ключом обязано стоять поле, гарантирующее полный порядок. Иначе сборка
невоспроизводима, а это в нашем устройстве означает смену `row_key` и пересчёт векторов
за деньги на каждом такте.

🔴 **Правило шире, чем было записано, и это стоило нам живого дефекта** (`F243`, 03.08).
Формулировка «результат которого попадает в данные (а не только в показ)» звучит как
разрешение не ставить разделитель там, где результат идёт «на показ». Но между «в данные» и
«на показ» лежит третье, и в нашем устройстве оно главное: **результат, который попадает в
РЕШЕНИЕ.** Ранжирование алиасов и `signal_terms` в `serene_ask.py` никуда не записывают —
они выбирают сущность, о которой будет ответ, и отбирают варианты уточнения. Ловушка
пережила собственный разбор именно потому, что правило описывало запись, а не решение.

`[замер 03.08]`, боевая база `ut_test`, боевой скорер `tfidf`: в первой восьмёрке
ранжирования алиасов **3–5 ничьих** на каждом из семи вопросов приёмки, и у **шести из
семи** ничья приходится на сам срез `LIMIT 8`. У `signal_terms` — по 1–2 ничьих в первой
шестёрке у двух сущностей из семи. Разделители (`src_table` и `fg.t`) сделали ключ полным:
**0 совпавших ключей** в первой девятке и первой шестёрке соответственно.

⚠ **Чего замер НЕ показал:** порядок, снятый десятью повторами одного запроса подряд, вышел
**одинаковым все десять раз**. Стабильность одного сеанса не есть детерминизм: порядок
равных не определён, и меняется он от плана, параллелизма и данных, а не от повторов. Это
довод против того, чтобы «проверять» такие дефекты повтором запроса — их видно только по
ключу сортировки.

**Штатное предписание движка на этот случай есть**, и оно совпадает с нашим правилом: доки,
*Indexes › Inverted › Ranking › Tie-breaking* — «Scores can tie. Add further `ORDER BY`
columns after the scorer for a deterministic order — typically the primary key».

**Как проверять:** собрать дважды подряд и сверить КЛЮЧИ. Сравнение числа строк такой
дефект не ловит — оно совпадает точно.

## Ловушка 31: `\gexec` с `ON_ERROR_STOP=0` молча пропускает отказавшие операторы

Так устроен досчёт векторов: поток порождает по оператору на пачку и выполняет их через
`\gexec`. При `ON_ERROR_STOP=0` упавший оператор пропускается, остальные идут дальше —
это нужно, чтобы одна плохая пачка не валила весь поток. Но у этого есть цена.

[замер 29.07] провайдер ответил `HTTP 429`, и проход закончился на 36 987 строках из
256 044 — «успешно», с кодом возврата 0. Строки не потерялись (остались с `emb IS NULL`),
но работа встала до следующего такта.

**Правило:** там, где ошибка означает «перегрузка, приходи позже», а не «эти данные
плохие», нужен ПОВТОР, а не пропуск. Досчёт сделан раундовым: пауза и повтор остатка,
выход по «раунд не сдвинул ни строки». Отличать одно от другого можно по движению: если
после паузы работа пошла — была перегрузка; если нет — дело в данных.

**И общее:** код возврата 0 у шага, который сделал 14% работы, — это тихий отказ
(`HOW_NOT_TO §2.10`). Судить надо по СДВИГУ, а не по коду возврата.

## Ловушка 32: `greatest` двух ограничений НЕ соблюдает оба, соблюдает сумма

Нарезка пачек для эмбеддера должна держать два предела разом: не больше `EMBED_BATCH_ROWS` строк и не
больше бюджета символов. Формула была `greatest(rn/10, cum/BUDGET)` — она даёт
монотонность (нужную для зонных карт), но НЕ даёт ограничения.

Почему: если после длинного куска идут короткие строки, `cum/BUDGET` стоит на месте и
превышает `rn/10`, поэтому `greatest` возвращает именно его — и группа растёт дальше
десяти строк. [замер 29.07] на полном наборе максимум оказался **45 строк**, пачек больше
десяти — **2 107**. Провайдер отвергал такие целиком: `HTTP 400 batch size is invalid`.

**Правильно — сумма:** `(rn/10) + (cum/BUDGET)`. Сумма двух неубывающих постоянна только
тогда, когда постоянны оба слагаемых, значит внутри группы соблюдены оба предела.

**Общее правило:** `greatest` объединяет ограничения через ИЛИ (сработает хотя бы одно),
сумма — через И (сработают оба). Когда нужны оба предела сразу, `greatest` не годится.

**И про проверку:** порядково-зависимое свойство нельзя проверять выборкой без сортировки.
Первый замер на `LIMIT 50000` без `ORDER BY` дал «максимум 10, всё хорошо» и едва не
закрыл вопрос; замер на полном наборе в реальном порядке дал 45.


## Ловушка 33: сравнение колонки из `LEFT JOIN` с колонкой левой таблицы В ФИЛЬТРЕ молча не применяется

Самая опасная из найденных: **ошибки нет, счёт неверный**.

[замер 30.07, SereneDB 26.07.3] `l` — три строки `(1,5) (2,5) (3,5)`, `r` — одна `(1,5)`,
`l LEFT JOIN r ON r.id = l.id`. Ожидание для «где значения не совпали» — строки 2 и 3.

| Фильтр | Вернул | Должен |
|---|---|---|
| `WHERE coalesce(r.n,0) <> l.x` | **1,2,3** | 2,3 |
| `WHERE coalesce(r.n,0) = l.x` | **1,2,3** | 1 |
| `WHERE r.n IS DISTINCT FROM l.x` | **1,2,3** | 2,3 |
| `WHERE n <> x` над подзапросом с тем же `coalesce` | **1,2,3** | 2,3 |
| `WHERE n <> x` над подзапросом с `CASE WHEN r.n IS NULL THEN 0` | **1,2,3** | 2,3 |

Фильтр **отбрасывается целиком** — возвращается результат соединения без него. Сами данные
соединение даёт верные: `1:5/5 2:0/5 3:0/5`.

**Что работает** (проверено тем же замером):

| Приём | Вернул |
|---|---|
| сравнение с **константой**: `coalesce(r.n,0) <> 5` | 2,3 ✔ |
| `coalesce(r.n,0) > 0` | 1 ✔ |
| `WHERE r.n IS NULL` | 2,3 ✔ |
| `WHERE r.n <> l.x` (без подстановки NULL) | пусто ✔ |
| `NOT EXISTS (… AND r.n = l.x)` | 2,3 ✔ |
| коррелированный подзапрос вместо соединения | 2,3 ✔ |
| фильтр по **материализованной** таблице (`CREATE TABLE AS` соединение, потом `WHERE`) | 2,3 ✔ |
| `CASE` в СПИСКЕ ВЫВОДА (не в фильтре) | верно ✔ |

Ломается именно **фильтр, где подстановка NULL сравнивается с колонкой левой таблицы**.
Обёртка в подзапрос не спасает: фильтр проталкивается назад в соединение.

**Правило.** Не фильтровать по «подставили NULL и сравнили с левой колонкой». Вместо этого
`NOT EXISTS`, коррелированный подзапрос, либо материализовать соединение и фильтровать
отдельным запросом.

**У нас этот образец не встречается — проверено, а не рассуждением.** Единственное похожее
место, `corpus_build.sql` строка 439, — булев `coalesce` без сравнения с левой колонкой;
[замер 30.07] в запросе и на материализованной копии он даёт одно и то же (`2,3`).
Перепись полноты строит причины `CASE`-ом в списке вывода, а не фильтром.

## Ловушка 34: `IN (ключ, выражение)` вместо двух равенств уводит движок в перебор

[замер 30.07] защита слияния «сколько объектов уйдёт» на 623 565 строках:

| Форма | Время |
|---|---|
| `AND t.row_key IN (c.row_key, regexp_replace(c.row_key, …))` | **не уложилась в 2 минуты** |
| два раздельных `NOT EXISTS`, в каждом равенство | **0,21 с** |

Список внутри `IN` лишает соединение равенства, по которому строится хеш. Два условия
подряд к тому же дешевле по существу: второе считается только для строк, не нашедшихся по
первому, — регулярное выражение исполняется для единиц, а не для всего корпуса.

## Возможность 35: словарь со стеммингом и синонимами — `copy_from` + `ts_lexize`

Не ловушка, а **пропущенная возможность**: она была описана в `docs/SERENE_CAPS_3.md §2.4`, а
я вместо неё написал `contains()` по подстроке и потерял на падежах верный ответ.

```sql
-- вариант основного словаря СО СТЕММИНГОМ; локаль наследуется, индекс не трогается
CREATE TEXT SEARCH DICTIONARY IF NOT EXISTS search_dict_stem (
  template = 'copy_from', from = 'search_dict', stemming = true);

SELECT ts_lexize('search_dict_stem', 'складов');   -- {склад}
SELECT ts_lexize('search_dict_stem', 'Склады');    -- {склад}
SELECT list_has_any(ts_lexize('search_dict_stem', label),
                    ts_lexize('search_dict_stem', 'складов'));  -- сравнение по основам
```

Синонимы — тем же механизмом, `solr_synonyms` в `pipeline` (нужна ЯВНАЯ локаль, иначе
`text: invalid locale`):

```sql
CREATE TEXT SEARCH DICTIONARY syn (
  template='pipeline',
  step1_template='text', step1_locale='ru_RU.UTF-8', step1_case='lower', step1_stemming=false,
  step2_template='solr_synonyms', step2_synonyms='спб, санкт-петербург, питер');
-- ts_lexize('syn','спб') -> {питер,санкт-петербург,спб}
```

**Область действия — СВОЯ БАЗА.** [замер 30.07] словарь, созданный в `ut_test`, из `postgres`
не виден: `text search dictionary "probe_stem" does not exist`. То есть здесь столкновения
между базами нет, в отличие от секретов (ловушка 26).

**Чего словарь НЕ решает.** [замер 30.07] на выборе сущности он замер не улучшил: по основам
совпадает много названий, и «Корректировка Приобретения» с «Приобретение Товаров Услуг»
неразличимы по словам. Он снимает СЛУЧАЙНЫЕ потери (падеж), но не выбирает смысл.

## Ловушка 35: булев реквизит есть только в тексте — типизированного доступа к нему нет

У витрины две карты: `nums MAP(VARCHAR, DOUBLE)` для чисел и — с 30.07 — `flags
MAP(VARCHAR, BOOLEAN)` для булевых. До этого булевы реквизиты (`kind='flag'`) попадали
**только в текст `doc`**, и отобрать по ним было нечем, кроме поиска подстроки.

**Штатного способа достать именованный реквизит из УЖЕ СОБРАННОГО текста у движка нет.**
Проверено агентом `serenedb-native` на живом движке 26.07.3: структурный доступ
(`doc['name']`, `map_extract_value`) требует, чтобы значение лежало в типизированной
колонке **на момент построения индекса**. `ts_phrase` не спасает — строка
`'Description: Гайка IsFolder true в тексте | IsFolder: false'` попадает в выдачу
`doc @@ ts_phrase('IsFolder true')` наравне с настоящей папкой, то есть даёт ровно тот же
класс ложных срабатываний, что и `contains`.

**Замер стоимости** (`ut_test`, 623 565 строк, 3 прогона):

| запрос | время |
|---|---|
| `count(*)` — базовая линия | 2,8 / 5,9 мс |
| `count(*) FILTER (contains(doc,'IsFolder: true'))` | 244 / 262 / 355 мс |
| `count(*) FILTER (map_extract_value(nums,'Количество') IS NOT NULL)` | 22 / 30 / 46 мс |

Типизированная карта дешевле разбора текста **примерно в 8 раз**.

**MAP индексировать нельзя, но в `INCLUDE` он принимается:**

```
CREATE INDEX bad ON corp USING inverted(row_key, doc ru, flags);
ERROR:  Column 'flags' has unsupported type MAP(VARCHAR, BOOLEAN) and can not be indexed
```
```sql
CREATE INDEX corp_idx ON corp USING inverted(row_key, doc ru, src_table)
  INCLUDE (nums, flags, doc_date);   -- так работает
```
В `INCLUDE` предикат по карте становится `Column Filter` **внутри** `IRESEARCH_SCAN`, а не
`FILTER` сверху. У нас `INCLUDE (src_table, row_key, doc_date)`, то есть ни `nums`, ни
`flags` туда не входят — обращение из `search_idx` работает, но с походом в таблицу.
Добавление их в `INCLUDE` требует пересоздания индекса; для правильности не обязательно.

**Пустая агрегация даёт NULL, а не пустой MAP.** Проверено:
`map_from_entries(list(...) FILTER (false)) IS NULL` → `t`. Поэтому `coalesce` до
`MAP{}::MAP(VARCHAR, BOOLEAN)` обязателен, иначе сущность без единого булева реквизита
неотличима от несобранной.

**Отбор — трёхзначный, и это существенно:**
```sql
WHERE NOT coalesce(map_extract_value(flags, 'IsFolder'), false)  -- не папка (в т.ч. если реквизита нет)
WHERE     coalesce(map_extract_value(flags, 'IsFolder'), false)  -- только папки
```

**Соседняя ловушка, если пойти через `VARCHAR[]`:** `NOT (flagset @@ ts_like(...))`
работает по правилам SQL-NULL и **выбрасывает строки с `flagset IS NULL`** — то есть молча
теряет сущности без булевых реквизитов (п. 13). Обойти через `coalesce` вокруг `@@`
нельзя: `ERROR: TSQUERY expression evaluated outside an @@ match against an
inverted-indexed column`.

**Отвергнуто по универсальности:** `STRUCT` в `INCLUDE` движок принимает, но имена полей
обязаны стоять в DDL — это ровно тот список имён конкретной базы, которого нельзя
допускать. `VARIANT` в индекс целиком тоже не принимается, только `INCLUDE`, — то есть
против пары `nums` + `flags` не даёт ничего, кроме «одной колонки вместо двух».

## Известная граница: фильтр подсказок привязан к русским словам

`serene_sync._selectable` (и его копия в `serene_select.py`) отбрасывает кандидатов по
подстрокам «присоединенныефайлы» и «удалить». В комментарии сказано, что это платформенные
соглашения 1С, — отчасти верно, но соглашения **русскоязычных** конфигураций: на
англоязычной префикс будет `Delete`, и фильтр молча перестанет работать.

**Радиус вреда мал:** функция применяется только в `_suggest` — подсказке ближайших
реальных имён, когда настроенное имя сущности мёртвое. На ЗАГРУЗКУ данных не влияет.
Поэтому это записано как граница, а не как дефект к немедленной починке.

Чинится тем же способом, что и разметка сущностей: спросить у источника истины
(`$metadata` даёт признаки, модель даёт смысл), а не сверяться со списком слов.


## Ловушка 36. Сервис эмбеддинга врёт про свою модель — двумя разными способами

`[замер 02.08]` Векторы разных моделей несравнимы и сравниваются **без ошибки**, поэтому
знать, какая модель реально отвечает, обязательно. Поле `model` в ответе для этого **не
годится**, и это выяснилось дважды подряд с противоположным знаком:

| сервис | что делает при незнакомом имени модели |
|---|---|
| llama.cpp | **подставляет свою**: просим `…-Q8_0`, получаем вектор от `…-Q6_K`, HTTP 200 |
| наш FastAPI-обвес | **возвращает эхом** запрошенное — подтвердил даже несуществующую `Qwen3-Embedding-4B-f16` |

Источник правды — **`/health`**: он называет реально загруженную модель и открыт без
ключа. На нём и построены `embed_check.sh` (останавливает такт) и
`serene_ask.embed_model_live()` (выключает смысловой путь). 🔴 У балансировщика `/health`
отдаёт **список воркеров** (`{"workers":[…]}`), а не плоское имя — спрашивать надо
рабочую копию, иначе проверка не разберёт ответ и решит, что сервис молчит.

## Ловушка 37. Cloudflare режет клиента по подписи, а не по адресу

`[замер 02.08]` Сервис за Cloudflare отвечает `HTTP 403, error code 1010` на запросы
python-urllib и пропускает точно такие же от curl. Ни адрес, ни ключ, ни тело роли не
играют — только `User-Agent`. Наши сервисы ходят питоном, поэтому подпись задаётся
настройкой `EMBED_UA`. Движок при этом проходит своим клиентом без правок.

## Возможность 38. Размер пачки эмбеддинга подбирается замером, а не берётся из головы

`[замер 02.08]` На настоящих строках корпуса (в среднем 379 символов, один поток):
пачка 10 — 7,0 строк/с, **16 — 9,5**, 32 — 8,4, 64 — 6,2. Мелкая тратится на накладные
расходы запроса, крупная упирается в токены одного прохода. Ручка — `EMBED_BATCH_ROWS`.
🔴 Замер снимается на СВОИХ строках: на коротких синтетических та же проверка давала
137 строк/с — в тридцать раз больше правды (`HOW_NOT_TO §1.37`).
Число потоков подбирается так же — замером, а не по числу карт: рост прекращается
задолго до того, как кончается железо, потому что упирается уже в нашу сторону (много
параллельных `psql` и однопоточная запись движка). Ставить «по потоку на ядро/карту»
бессмысленно: лишние только стоят в очереди.

🔴 И ещё одно, что стоит времени: **каждый перезапуск досчёта платит за перенос**
посчитанного из рабочих таблиц в целевую. Это один запрос, писатель у движка
однопоточный, и на большой базе он идёт минутами. Значит менять настройки на ходу дорого —
дешевле дождаться конца этапа.

## Ловушка 39. Переписать ответ бота можно ТОЛЬКО на пути доставки в канал

OpenClaw 2026.7.1, проверено `[код]` по типам сборки и `[замер]` по журналу шлюза.

Хуки плагина, умеющие **заменить текст** исходящего ответа, все живут на пути доставки:
`message_sending`, `reply_payload_sending`, `before_dispatch`, `reply_dispatch`. Хук,
который видит итоговый ответ **вне** доставки, ровно один — `before_agent_finalize`
(поле `lastAssistantMessage`), и его результат умеет только `revise` / `finalize`:
потребовать ещё один проход модели, но не подменить текст.

Следствие, которое кусает не в проде, а в **замерах**: `openclaw agent` без `--deliver`
в канал не пишет, значит хуки доставки не зовутся, значит любая проверка исходящего,
поставленная на них, в таком прогоне **выключена и молчит об этом**. `[замер 23.07-03.08]`
по нашему гейту: `after_tool_call` 239 срабатываний, `before_agent_finalize` 443,
`message_sending` — **1**, `message_received` — **1**.

Обойти запуском `--deliver` без настоящего канала **нельзя**: `delivery.runtime` отбивает
такой запуск до отправки с причиной `channel_resolved_to_internal` («delivery channel is
required: pass --channel/--reply-channel or use a main session with a previous channel»),
и хуки доставки при этом не зовутся вовсе.

**Как надо.** Проверку, которая обязана работать всегда, ставить на `before_agent_finalize`
(она сработает везде, но принуждать сможет только повтором прохода), а замену текста
держать на доставке — и **мерить продукт настоящей доставкой**, иначе замер описывает
другую систему. И отдельно: хук обязан писать в журнал не только отказ, но и успех —
иначе «проверил и пропустил» неотличимо от «не звался», а это различие живёт годами.

---

## Возможность 40. `map_entries` вместо двух параллельных `unnest` — пара «ключ-значение» связана по построению

Разложить карту `MAP` на строки хочется привычным способом:

```sql
SELECT unnest(map_keys(nums)) k, unnest(map_values(nums)) v FROM search_corpus
```

Он **работает**, но держится на негласном допущении: что `map_keys` и `map_values` отдают
списки в одном и том же порядке. Допущение нигде не объявлено, и проверить его на каждой
строке нельзя — а корпус пересобирается, версия движка меняется.

Штатный способ есть, доки SereneDB (`Sql › Functions › Map`):

```sql
SELECT struct_extract(e, 'key') k, struct_extract(e, 'value') v
FROM (SELECT unnest(map_entries(nums)) e FROM search_corpus WHERE nums IS NOT NULL)
```

`map_entries` отдаёт `struct(key, value)` — пара связана самим движком, порядок ни при чём.
Проверено на нашей сборке **26.07.3** `[замер 03.08]`: функция есть, и числа приборов
совпали до единицы обоими способами (1 928 пар «сущность-величина», из них 454 нулевых,
539 сущностей). То есть это не исправление ошибки, а **снятие допущения**.

⚠ **Грабли доступа к полю.** Обращение вида `e.key` в SQL останавливается рубежом среды как
попытка чтения секрета (совпадение с шаблоном `*.key`). Обходится штатной функцией
`struct_extract(e, 'key')` — она же читается лучше. Имя поля именно `key`/`value`:
`e.k` движок не понимает и честно подсказывает «Candidate Entries: "key"».

---

## Ловушка 40. Сумма `DOUBLE` не воспроизводится — и это не «последние разряды»

Доки SereneDB говорят об этом прямо, в двух местах:

- `Compatibility › Non-Deterministic Behavior → Floating-Point Aggregate Operations with
  Multi-Threading`: «when the aggregation is split across multiple threads, the order in
  which floating-point values are summed is not deterministic, so the low-order digits
  vary»;
- `Sql › Functions › Aggregate Functions → sum(arg)`: «the floating-point versions of this
  function are affected by ordering».

Соблазн прочитать это как мелочь («расходятся младшие разряды») — и именно так я и прочёл
бы, если бы не замерил. `[замер 04.08]` пять вызовов `aggregate` подряд по неизменным
данным: у трёх пар «сущность-величина» из шести сумма отличалась, а у одной различались
**видимые цифры ответа** — четыре разных числа из пяти прогонов:

```
6227761388047771648 / …774720 / …778816 / …796224
```

Причина понятна: у величин крупнее 2⁵³ шаг `DOUBLE` грубее единицы, поэтому «младший
разряд» — это тысячи. Для продукта, которому п. 3 контракта предписывает
«детерминированно на данных», это дефект, а не особенность.

Второе наблюдение того же замера: результат зависит и от **формы запроса**. Тот же итог,
посчитанный через `GROUP BY` и через одиночный агрегат, дал `…6199999973` и `…6200000104` —
то есть сравнивать числа двух разных запросов как «одно и то же» нельзя.

**Чем чинится (проверено на сборке 26.07.3):** штатной фиксированной запятой —
`sum(TRY_CAST(x AS DECIMAL(38,10)))`. Доки, `Sql › Data types › Numeric → Fixed-Point
Decimals`: DECIMAL — «exact fixed-point decimal value», сложение точное и от порядка не
зависит. Числа выбора `[замер 04.08, 60 пар]`:

| Способ | Итог | Цена |
|---|---|---|
| `sum(x)` по `DOUBLE` | невоспроизводим | 12 мс |
| `sum(x ORDER BY x)` (тоже штатный) | воспроизводим | **109 мс** |
| `sum(CAST(x AS DECIMAL(38,10)))` | точен и воспроизводим | **39 мс** |

`DECIMAL(38,10)` и `DECIMAL(38,15)` совпали на всех 60 парах — десяти знаков довольно.
Ширина 38 даёт 28 цифр до запятой; наибольшее значение боевой базы 6,4·10¹³, значений
1,9 млн, то есть до предела двенадцать порядков. `TRY_CAST`, а не `CAST`: значение, не
влезшее в разрядность, обязано стать `NULL`, а не уронить ответ, — но тогда его надо
посчитать отдельно и показать, иначе это молчаливая потеря (п. 13).

⚠ И обратная сторона: `coalesce(sum(...), 0)` на пустом множестве превращает «считать было
нечего» в «посчитано и вышло ноль». Ноль неотличим от ответа и проходит любую сверку с
данными. Разбор — `HOW_NOT_TO §1.53`.

---

## Возможность 41. Карточка сущности ищется словами по НЕСКОЛЬКИМ полям сразу — одним запросом

`[замер 04.08]`, сборка 26.07.3, доки `Sql › Indexes › Inverted Index` («Operator classes
and fields», «Querying an inverted index», «Ranking → Boosting»).

Инвертированный индекс спрашивается **как отношение**, по имени, и запрос вправе трогать
любое подмножество проиндексированных полей в любом сочетании: «a query may constrain any
subset of the indexed fields, in any combination». Условия по разным полям объединяются
через `OR`, скорер на индекс — ОДИН (`bm25`/`tfidf`/… по `<индекс>.tableoid`); двух
скореров в одном скане движок не допускает.

```sql
SELECT src_table, tfidf(entity_card_idx.tableoid) AS s
FROM entity_card_idx
WHERE label @@ ts_phrase('поставщик') OR aliases @@ ts_phrase('поставщик')
   OR about @@ ts_phrase('поставщик') OR quantities @@ ts_phrase('поставщик')
   OR attrs @@ ts_phrase('поставщик')
ORDER BY s DESC, src_table LIMIT 32;      -- разделитель равенства обязателен (ловушка 30)
```

Цена — **63 мс на 1 502 карточки**: индекс мал, потому что в нём одна строка на сущность,
а не на запись. Так шаг 3 получил четвёртую поверхность отбора.

## Ловушка 41. Поле в существующий инвертированный индекс не добавляется — только пересозданием

Там же, «Maintenance & Introspection → Schema changes on an indexed table»: `ADD COLUMN`
у таблицы разрешён и для индекса безвреден («index-neutral»), но чтобы индекс начал это
поле ИСКАТЬ, его надо снести и создать заново (`DROP INDEX` → `CREATE INDEX`). `ALTER
INDEX … ADD` не существует.

Практический вывод для такта: пересоздание индекса по карточке (1 502 строки) — часть
обычного прогона, весь шаг `entity_card_build.sql` занимает `[замер 04.08]` **1,2 с**.
Пока индекса нет, запрос к нему падает ошибкой — поэтому поверхность обязана эту ошибку
ловить и работать без неё, а не ронять ответ.

И рядом: записи становятся видимы индексу только после публикации — фоновая раз в секунду
(`refresh_interval`), явная — `VACUUM (REFRESH_TABLE) <таблица>`.

## Ловушка 42. `duckdb_columns()` отдаёт колонки ВСЕХ присоединённых баз

Продолжение ловушки 25 (та же беда у `duckdb_tables()`). Каталог колонок не ограничен
текущей базой: `[замер 04.08]` в нашем инстансе таблиц с одинаковым именем в разных базах
**91**, а `SELECT DISTINCT database_name FROM duckdb_columns()` даёт `system`, `postgres`
и `ut_test`. Соединение «сущность ↔ её колонки» по одному `table_name` тихо приносит
реквизиты ЧУЖОЙ базы — и это не ошибка, а неверные данные в карточке.

Лечится условием `c.database_name = current_database()` (`entity_card_build.sql`).

## Возможность 43. Слияние мест (RRF) считается ОДНИМ запросом, вместе с kNN

`[замер 04.08]`, сборка 26.07.3, доки `Cookbook › Search › Reciprocal Rank Fusion`.

Ветви разной природы — лексический скорер по одному индексу, лексический по другому,
косинус по вектору — сравнивать по оценкам нельзя, а по МЕСТАМ можно. Движок делает это
сам, без выгрузки наружу (п. 20):

```sql
WITH fused AS (
  SELECT src_table, RANK() OVER (ORDER BY s DESC, src_table) AS rank FROM (
    SELECT src_table, tfidf(alias_idx.tableoid) AS s FROM alias_idx
    WHERE aliases @@ ts_phrase('поставщик') ORDER BY s DESC, src_table LIMIT 50) t
  UNION ALL
  SELECT src_table, RANK() OVER (ORDER BY s DESC, src_table) FROM (
    SELECT src_table, tfidf(entity_card_idx.tableoid) AS s FROM entity_card_idx
    WHERE label @@ … OR attrs @@ … ORDER BY s DESC, src_table LIMIT 50) t
  UNION ALL
  SELECT src_table, RANK() OVER (ORDER BY d, src_table) FROM (
    SELECT src_table, emb <=> '[…]'::FLOAT[1024] AS d FROM search_entity_card
    WHERE emb IS NOT NULL ORDER BY d, src_table LIMIT 50) t
)
SELECT src_table, SUM(1.0/(60 + rank)) AS rrf
FROM fused GROUP BY src_table ORDER BY rrf DESC, src_table LIMIT 200;
```

Три вещи, без которых это не работает:

* **скорер только во вложенном запросе** — снаружи `GROUP BY`, и движок не даёт считать
  `bm25`/`tfidf` в одном запросе с ним («requires an inverted index scan in the same
  sub-query»). Тот же обход, что записан у сборки корпуса;
* **разделитель равенства в каждой ветви** (ловушка 30): `RANK()` при ничьей даёт
  одинаковые места, а `LIMIT` внутри ветви режет по порядку исполнения;
* **`k = 60`** — умолчание исходной статьи и Elasticsearch, названное в доках; это не
  подобранное нами число.

Отсутствующая ветвь просто не даёт слагаемых («A document missing from a branch
contributes nothing») — значит ветви можно включать по наличию: нет словаря синонимов,
лёг эмбеддер, нет карточки — запрос остаётся верным.

---

## Ловушка 44. Цена чтения из 1С — это ЧИСЛО ОБРАЩЕНИЙ, а не объём данных

`[замер 05.08, боевая база УТ, живая 1С через наш шлюз OData]` один и тот же регистр
`InformationRegister_ЗамерыВремени`, менялся только `$top`:

| `$top` | строк | секунд | строк/с |
|---|---|---|---|
| 1 000 | 1 000 | 6,05 | **165** |
| 5 000 | 5 000 | 12,16 | 411 |
| 20 000 | 20 000 | 15,20 | **1 316** |

Восьмикратная разница на одних и тех же данных. Значит время уходит не на строки, а на
сам заход: каждое обращение стоит несколько секунд, и с ростом `$skip` дорожает ещё —
1С отсчитывает пропускаемые строки заново.

**Чем это стоило.** Загрузчик держал `PAGE = 1000` зашитым числом. Регистр из 97 834
строк читался **888 с** — почти четверть такта свежести на одну сущность, и такт боевой
базы шёл 2 ч 18 мин при норме 20 минут (п. 17 `TARGET.md`). Симптом выглядел как «базы
слишком много», а был «обращений слишком много».

🔴 **Крупная страница — это МЕНЬШЕ нагрузки на 1С, а не больше.** Второе предложение
п. 17 требует не создавать заметной нагрузки, и здесь оно не конфликтует с первым:
меньше запросов и меньше повторных проходов по `$skip`. Ускоряться параллельными
запросами — как раз то, что этот пункт запрещает.

**Как правильно.** Размер страницы не подбирается под базу: начинать крупно и уменьшать
вчетверо, если 1С не справилась (`ETL_PAGE` / `ETL_PAGE_MIN` в `poc_load_entity.py`).
Уменьшив — начинать сущность **сначала**: при меньшей странице появляется постраничность,
а с ней обязателен `$orderby`, и смешивать упорядоченные страницы с уже прочитанными
неупорядоченными значило бы молча терять и дублировать строки (п. 13). Просить больше,
чем в сущности есть (`$count`), незачем — это снимает и лишний заход, и `$orderby`
у мелких сущностей.

## Ловушка 45. Признака изменения у независимого регистра сведений 1С не даёт вовсе

`[замер 05.08]` Проверено тремя способами, потому что отрицательный вывод требует той же
строгости, что и положительный:

1. **`$metadata` целиком** (10,8 МБ, 2 795 типов): единственный признак изменения от
   платформы — `DataVersion`, и он есть у **550 типов** (справочники, документы, планы
   видов характеристик, бизнес-процессы). Ни одного `FunctionImport` про изменения:
   есть только `Balance`, `BalanceAndTurnovers`, `Turnovers`, `SliceFirst`, `SliceLast`,
   `Post`, `Unpost`, `Start`, `ExecuteTask`.
2. **Живая 1С**: `InformationRegister_ИерархияПартнеров` отдаёт `Партнер_Key`,
   `Родитель_Key`, `Уровень` — и всё; `InformationRegister_ЗамерыВремени` отдаёт свои
   измерения и ресурсы. Ни версии, ни системной отметки времени.
3. **Документация платформы** (`kb.1ci.com`, Developer Guide 8.3.23, §17.4.13): штатный
   механизм изменений у платформы ЕСТЬ — `SelectChanges` через планы обмена. Но это
   **`POST`**, он помечает изменения как переданные (`MessageNo`) и требует настроенного
   плана обмена в конфигурации клиента. В нашей базе планы обмена в состав OData не
   выведены вовсе — `ExchangePlan_*` в `$metadata` **ноль типов**.

**Вывод.** `SelectChanges` нам недоступен по трём причинам сразу: это запись в 1С (а у
нас инвариант «1С только читаем», держится ролью без прав записи и шлюзом, режущим
не-GET), это правка конфигурации клиента (против «коробки», п. 14), и в этой базе он не
опубликован. Раскладка источников по признаку изменения: **1129 из 1374** имеют его —
свой `DataVersion`, либо владелец (табличная часть), либо регистратор (подчинённый набор);
**843** не имеют — это независимые регистры сведений и константы.

## Ловушка 46. Буфер CSV-ридера = 16 × maximum_line_size — предел строки покупает гигабайты

`[замер 12.08, okna-1]` `read_csv` с `maximum_line_size=200 МиБ` просит у буферного
менеджера разовый блок **3,125 ГиБ** (доки: Data import and export › Csv › Overview —
`buffer_size` по умолчанию `16 × max_line_size`). На деве (memory_limit ~46 ГБ) это
молчит, на юните с 6 ГиБ — «could not allocate block of size 3.1 GiB» на каждом чтении,
apply падал 83 раза подряд. Проба на тех же файлах: max_line = размер файла → 187 415
строк без ошибки. Правило: **предел строки не бывает длиннее самого файла** —
`packet_apply._csv_source` прижимает его к крупнейшему чанку; свой большой предел
задавайте только вместе с бюджетом ×16.

⚠ Дополнение 15.08: «не длиннее файла» верно, но недостаточно — читатель
ограничивает ЛОГИЧЕСКУЮ запись, а не физическую строку, см. ловушку 49.

## Ловушка 49. `maximum_line_size` ограничивает ЗАПИСЬ, а не строку — переносы в кавычках

`[замер 15.08, okna-1]` `read_csv` меряет пределом ЛОГИЧЕСКУЮ запись CSV: поле в
кавычках вправе содержать переводы строк, и вся многострочная запись обязана
влезть в `maximum_line_size` (buffer обязан вмещать четыре записи — доки: Data
import and export › Csv › Overview). Живой случай: chunk-00148 пакета 000002 —
3 записи по 1,4–2,6 МБ (base64-картинки, разбитые на строки по ~280 Б); предел
«самая длинная физическая строка × 8, пол 1 МиБ» дал 1 МиБ — `CSV Error on
Line: 1`, пакет не применялся. Правило: предел считается по самой длинной
ЗАПИСИ (`packet_apply._longest_record` — автомат кавычек поверх `bytes.split`),
а не по строке с запасом «на глаз». Разбор — `HOW_NOT_TO §3.74`.

## Ловушка 47. `SET threads` — ГЛОБАЛЕН: сессия перекраивает пул всего движка

`[замер 12.08]` После сессии apply с `SET threads=4` **новая** psql-сессия видит
`current_setting('threads') = 4` при `--cpu_threads=160` в conf — настройка переживает
разрыв соединения и действует на все сессии до рестарта движка или обратного `SET`.
Доки скоуп не оговаривают («SET threads = 4» и всё). Чем стреляло: на okna-1 precheck
сборки встал об «пул исполнителей движка = 4», на деве пул просел до 6 от прогонов
`test_packet_apply`. Правило: `SET threads` в прикладном коде — только как явная ручка
с пониманием, что она глобальна (`PACKET_APPLY_THREADS`, умолчание 0 — не трогать).

## Ловушка 48. Спилл на диск идёт в «.tmp» ОТ CWD процесса — а у юнита cwd=/

`[замер 12.08, okna-1]` Larger-than-memory запрос (сортировка QUALIFY на полном merge)
спиллит в каталог `.tmp`, который движок создаёт **относительно текущего каталога
процесса**, а не рядом с файлом базы, как пишут доки про умолчание. У systemd-юнита без
`WorkingDirectory` cwd=/, при `ProtectSystem=strict` корень read-only → «Failed to
create directory ".tmp": Read-only file system», запрос падает. На деве не стреляло
никогда — 62 ГБ RAM, спилла не было. Лечение: `WorkingDirectory=/var/lib/serenedb` в
юните (drop-in на живых юнитах, внесено в `ubuntu/serenedb/serenedb.service`) — `.tmp`
ложится рядом с данными, путь уже в `ReadWritePaths`.
