# ПЛАН+ПАМЯТКА: «Один путь — без ловушек до и после» — АКТИВНАЯ ЛИНИЯ (слово владельца «поехали» 18.09)

Старт дан и ИСПОЛНЯЕТСЯ. HEAD на момент среза: e75c747. Канон: .claude/state/design-c.md
(v19, круг 17). Приёмка-цель: 6/67 → 0, wrong 0, L67 (I0=30/36/1): match>=30, wrong<=1.

## ЧТО ДЕЛАЕТСЯ СЕЙЧАС (единственный следующий шаг)

C-канон: красная ×3 кругами до ACCEPT ×3. Идёт круг-17 (процессы redc49..51.log).
Промт красной: .claude/state/prompt-redc.md. Схема круга: 3 фоновых cursor-agent →
извлечь вердикты (python из stream-json, см. ниже) → влить [БЛОК] правками в
design-c.md (heredoc/`python3` replace — Write-инструмент ложно блокируется
check-prompt-rules, правило 7) → поднять номер круга в заголовке → запустить ×3 снова.
ACCEPT ×3 (все три без [БЛОК]) = канон готов → CHANGELOG-запись + коммит →
далее C1+C2 (один исполнитель: z21 + wiki SQL + z20-хвосты) → C4 (z06) → C3 (z20) →
C5 (замки test_onepath_rescue, краснеют на HEAD) → выкат на окно → живые пробы → доки.

Извлечение вердиктов:
python3 -: парсить cursor-run-redc<N>.log, записи "type":"assistant" с 'ВЕРДИКТ' в
content[].text, брать последний; печатать [:1800].

## СОСТОЯНИЕ КАНОНА (круг 17; 48 REJECT влито — круги 1–16)

Сводки кругов УДАЛЕНЫ из шапки (источник дрейфа — красная кругов 11/15 ловила
рассинхроны сводок с телом); единственный канон — ТЕЛО design-c.md. Ключевое:

- ТОЧКА 1 (wiki_primary_entity_cascade перед no_data; вход-гейт негативным правилом —
  любой отказ КРОМЕ off-topic) + ТОЧКА 2 (перед no_data unmatched-terms z20:3927;
  «concepts + одноразовый escalate в rescue при неподтверждённом лидере»; kNN-only →
  переverify полного пула). Понятийность = (i) слоты ∪ (ii) LLM-concepts ∪ (iii)
  span-DL-хвост выбранного src (детерминированно, ВСЕГДА до probe при известном src).
- rescue_pool = hybrid-SQL с параметрами 96/24, rescue-режим БЕЗ axis_ok/filtered;
  норм-слагаемое = скалярный damerau_levenshtein на хвосте src_table (после первого
  «_»); ПРЕДПОСЫЛКА C1: живая проба SELECT damerau_levenshtein ДО правки SQL.
- Батч-verify: enrich на каждый слайс, merge ВСЕХ батчей, исход-функция с ФОРМУЛОЙ
  sole (1 yes ∧ прочие no ∧ |verdicts|==|pool|, не среза); truncated → sole запрещён.
- Резолвер: один вызов всегда; трихотомия только до sole; ★ один (llm_option_highlight);
  сужений нет; «ровно один» при полном вердикте → меню; |pool|==1 не-sole → R-D.
- rescue_concepts (+pending) в whitelist билета z14:324–328 и accumulate_resolution
  z14:262–281 (переживают цепочку кликов); consume-повтор concepts 1 раз; повтор-fail
  = честная деградация (LLM лежит — не дефект волны).
- C4: spans ≥2 токенов через границы групп; SUPERSEDE только {unmatched,fuzzy,part}
  (exact/slop не трогать); ГАРД exclude-only для span≡имени src; norm-тег после
  count>0; probe-предикат — термовый ts_levenshtein на doc (штатный z06:74–75).
- C3: unknown_words/unknown_terms_unmatched в _journal_intent, запись при любом выходе
  прохода; на ТОЧКЕ 1 — и rescue_concepts-токены с _base_knows=False.
- src_layer: проводка в 3 местах (SELECT+карточка+при picked); замок «нет поля»
  запрещён до появления колонки.
- Замки R-A..R-K в §5; приёмка 6 вопросов ДО ЧИСЛА всей цепочкой; GO/NO-GO с единицами.

## ЖЁСТКИЕ ПРАВИЛА (без изменений)

Только сервер окно (ssh -p 2202 root@gpu-erw.timpul.pro); исполнители ТОЛЬКО
cursor-agent промт-файлами (timeout 2400, tee лог, фон); красная ×3 до ACCEPT ×3;
add/commit РАЗНЫМИ вызовами, пат-спек, «Числа:/Доки:»; граф тем же коммитом при
компонентах; красной запрещён stash; живая проба обязательна; моки формата транспорта;
ложный блок check-prompt-rules — правка файла через heredoc/python (правило 7, с
отметкой в коммите); п.0 TARGET (имена только в фикстурах, словарь руками не правится).

## МЕХАНИКА ОКНА (без изменений)

psql 7890; ask :8091 (юнит 1c-serene-ask@postgres, код /opt/1c-mcp-reports/ask/);
мост :6016 (Bearer из /etc/1c-mcp-ask-postgres.env, grep -E "^[A-Z_]+=");
веб :18801 (сейчас 402 — кредиты владельца); пробы — свои MCP-сессии, разбавитель
«сколько всего валют», выбор ask_1c(question=исходный, decision_id=…); предзамер:
search_corpus, TRY_CAST, латинская Period.

## СДЕЛАНО В ЭТОЙ СЕССИИ

- 4 линзы-реестра (c-lens1..4-*.md, коммит 4c96d06) + дизайн v19 с нуля.
- Красная кругов 1–16: 48 REJECT, все классы влиты; тренд блокеров 5/круг →
  формулы проводки; круги 11–12 выявили дрейф сводок — шапка вычищена.
- Коммиты: 4c96d06 (реестры+промты+CHANGELOG 18.09(6)), e75c747 (дизайн круг-17).

## ОТКРЫТЫЕ ДОЛГИ (владельцу, не блокируют)

Кредиты 402 (веб-профиль; субагенты Kimi); ремонт check-prompt-rules (ложные блоки
на design-документах); search_dict_syn; эталон Q5 80 173 vs client-gold tsv 78 176 —
сверить источник до выката.
