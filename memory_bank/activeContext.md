# Активный контекст

## С ЧЕГО НАЧАТЬ (срез 01.09 ~07:25 UTC, УТРО после ночной смены; читать целиком)

### ИТОГ НОЧИ 31.08→01.09 (всё проверено живьём, HEAD e04043e = origin/main)
1. **Векторы ПОЛНОСТЬЮ посчитаны и не потеряны:** корпус search_corpus
   1 657 724 живых / NULL 0 вне service; резолвер resolver_index 2 100 463
   живых / NULL 0. Бэкапы: store.db.bak-20260831-235844 (md5 30b9663c…),
   store.db.bak-20260901-021952 (md5 d7aeb15e…), снимки VECTOR_SNAPSHOT-*.txt.
2. **27B вернулся** на .12:8000 (/v1/models 200), эмбеддер на .11 жив.
   Временный эмбеддер .12:8002 снесён.
3. **Такт ЗЕЛЁНЫЙ** (финиш 06:44 UTC): vector_loss_gate=0, счётчики целы,
   content_hash-миграция завершена (NULL 0). Проба TOLERANCE=0 снята из env
   (env восстановлен из .bak-probe; копия пробы .probe-removed-20260901).
4. **Три фикса ночи (коммиты 648a66b, 6f63188, e04043e, все с замками):**
   bmap-дедуп дублей map_concat; миграция content_hash пачками (монолитный
   UPDATE 1,66 млн час висел без записи); ось места без best_used_for
   («сколько» ложно резолвилось осью по примерам вопросов → stock_override
   душил верифицированного вики-лидера).
5. **ВИКИ-ЦИКЛ ПРОВЕРЕН ЖИВЬЁМ** (вопросы владельца через :8091) — все
   исходы: лидер (сколько организаций → wiki_hybrid_pick=catalog_организации,
   verify yes=1); переспрос с чипами (движения в регистре); канон продаж
   напрямую (сколько продали за месяц → 4 224 744,87, источник реализациятмц);
   честный отказ (сколько товара на складе → verify no=2 → no_data).
6. **И2 67×2 идёт** (старт 07:17 UTC, PID 3011061, лог /tmp/i2_run_20260901.log,
   пересъём live 67/ошибок 0; дозор bash-bvi6ds4j будит при финале).

### ПУНКТ 0 TARGET.md — СВЯТО (напоминание владельца 31.08)
Система работает на СКРЫТОЙ базе ИЗ КОРОБКИ: никаких имён таблиц/баз в коде
и ответах, никаких списков слов домена, никакого разбора строк подключения.
Каждое решение проверяется: «завтра на новой чужой базе заработает без
правки кода и без ручного разбора?». Девиз владельца: автономность,
универсальность, непривязанность к базе.

### ПРАВИЛА ВЛАДЕЛЬЦА (действуют; владелец — парень)
1. 🔴 Векторы не терять НИКОГДА (гейт в такте выкачен и работает: потеря 0).
2. 🔴 Бэкап векторов перед рискованной операцией, с датой/временем + md5.
3. 🔴 Работаем ТОЛЬКО на окне okna и только с вопросами владельца.
4. Такт: Restart=no + таймер погашен; запуск руками после контроля.
5. 🔴 Схема векторизации — ТОЛЬКО docs/EMBED_BULK_HOWTO.md §9 дословно.
6. pkill и запуск — РАЗНЫЕ вызовы; git add и git commit — РАЗНЫМИ вызовами
   (гейт check-docs смотрит индекс ДО выполнения склеенной команды).
7. «postgres» в DSN/именах юнитов — имя базы SereneDB (движок один, витрина
   на 7890 по совместимому протоколу), СУБД PostgreSQL в контуре НЕТ
   (уточнение владельца 01.09 — не поднимать вопрос зря).

### СХЕМА, КОТОРУЮ ВНЕДРИЛИ: ВИКИ ДВА РАЗА В ПАЙПЛАЙНЕ (живая, проверена)
Вход: LLM+вики понимает вопрос, переспрашивает с чипами. Выход: кандидат
проверяется да/нет большим wiki-контекстом (wiki_passport). Замечание
владельца на будущее: переспрос с подсказками-чипами — уже есть, держать.

### ХВОСТЫ КАЧЕСТВА (не блокеры, найдены утром живыми вопросами)
- «сколько организаций»: сущность верна, но ответ «Подразделения: 2 ·
  рассмотрено 60/92» — ось внутри каталога выбралась «подразделения».
- «сколько движений в регистре реализациятмц»: в чипах мусорный сосед
  «итоги кассы» (второй чип верный).
- «сколько товара на складе»: no_data — пул вики не содержит регистров
  остатков (ось места не резолвится по «склад» на этой базе — каталогов
  с таким именем нет). = известный вопрос владельцу «склад 1318 vs no_data».
- Давний красный кейс К4 «stock: маркер» (требует живой базы, падает и на
  чистом HEAD) и 3 недогнанных замка (chunk_identity/plain_key/measures —
  тяжёлые, вернутся после soвободившейся машины).

### ПОРЯДОК ДЕЙСТВИЙ — УТРО 01.09
7. И2 67×2: ждать дозор → отчёт «уверенно неверных X из 67» по каждому пути.
8. К8-приёмка → автоприёмка по плану.
9. Утренний отчёт владельцу: одним письмом — векторы (числа+md5), такт,
   вики-исходы (4 примера), И2, хвосты.

### КЛЮЧЕВЫЕ ФАКТЫ ОКНА (ssh -i ~/.ssh/id_ed25519_deploy -p 2202 root@gpu-erw.timpul.pro = okna)
- Движок: psql "host=127.0.0.1 port=7890 user=postgres dbname=postgres"
  (многооператорный SQL — stdin-файлом); роли: serene_ro (сервис), postgres
  (внутренняя, журналы). GPU: .11 эмбеддер :8000 (НЕ трогать), .12 27B :8000.
- ask: :8091 прод (код e04043e выкачен, wiki-цепь жива), ASK_TOKEN из
  /etc/1c-serene-ask.env; веб-гейт :18801, WEB_TOKEN =
  /home/undebot/.openclaw-web/openclaw.json → gateway.auth.token.
- И2: work/gold/i2_runner.py на окне /opt/1c-mcp-reports, лог
  /tmp/i2_run_20260901.log, отчёт /tmp/i2-out.
- Такт: зелёный, запуск руками; гейт потери на месте (умолчание 0,5%).
- Ритуалы: пат-спек; сообщение одной строкой с «Числа:» и «Доки:»;
  после MCP-графа sleep 4; проверять staged mcp-memory.json.
