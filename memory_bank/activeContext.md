# Active Context

_Обновлено: **2026-08-28 вечер, перед compact**. Здесь — только живое.
История — в [`progress.md`](progress.md); стадии — в
[`docs/TARGET_STATUS.md`](../docs/TARGET_STATUS.md)._

---

# ⏭ С ЧЕГО НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

🔴 **Периметр (слово владельца): ТОЛЬКО окно (gpu-erw.timpul.pro), всё НА
нём, БЕЗ туннелей. Свежесть 1С отброшена владельцем. klient-1 — потом.
Выкат в бой — РАЗРЕШЁН.**

## 🟡 ЖДЁМ ВЛАДЕЛЬЦА (две вещи, всё остальное сделано)

1. **Люк под коммит K10**: гейт `check-prompt-rules` ложно ловит
   переносные комментарии монолита («нельзя/никогда» в комментариях при
   наличии промт-полей в тех же файлах) — каждый помеченный байт есть
   дословно в HEAD (проверено grep по `git show HEAD:…serene_ask.py`),
   новых промт-правил ноль. Владельцу:
   `echo 'check-prompt-rules K10: перенос монолита в пакет ask/, новых промт-правил нет (все строки дословно в HEAD)' >> .claude/hooks/override.txt`
   После коммита строку снимает владелец. K10-набор СТОИТ В ИНДЕКСЕ.
2. **maxTokens 8192→16384** в `/home/undebot/.openclaw/openclaw.json`
   (ровно 3 места, с бэкапом) + `systemctl start 1c-openclaw-gateway-restart`
   — каталог `.openclaw` под undebot 600, сессии закрыты; правит root.

## Что готово (28.08 вечер, HEAD `a85fa34`, всё запушено)

- **С5 полный прогон** юнитом на окне: 259 сущностей → MERGE → publish →
  solr. Словарь стал богаче, но «покупатели» генератор не выдал — задание
  дополнено словами РОЛИ (`f89da36`); ре-прогон каталогов ждёт выката
  wiki_alias.sh на окно (гейт выката = дерево=HEAD = люк п.1).
- **Регрессия поймана и закрыта** (`a85fa34`): AB_PROBE после замены
  словаря упал на «сколько петель осталось на складе» (clarify склада) —
  парсер назвал родом «петли», правило вынуло слово из терминов, скаффолд
  съел именованность. Починка универсальная: словарь базы решает, что
  род, а что предмет (`_base_knows_kind_or_measure` в z02, z12).
  **AB_PROBE 8/8, отметка `okna probe live 0err/8` стоит на дев.**
- **K10** нарезан (20 зон, AST mismatch 0, замки 48/0-51/0-30/0-92-24/0),
  приёмка оркестратором пройдена, коммит в индексе — ждёт люк.

## После люка — по порядку (одним заходом)

1. `git commit` K10 пат-спеком (сообщение ОДНОЙ строкой с «Доки:» и
   «Числа:»; в сообщении НЕ должно быть `; | &` — парсер пат-спека
   режет по ним и гейт увидит весь индекс).
2. `bash ubuntu/serenedb/deploy*.sh` — выкат в бой :8091 (дерево=HEAD,
   отметка пробы свежая, код несёт К9+K10+z02/z12+Э1).
3. `AB_CONTOUR=okna` на бою + контрольные формулировки (141/43/петли/
   воскресенье) + контур-24.
4. scp `ubuntu/serenedb/wiki_alias.sh` → окно `/opt/1c-mcp-reports/` →
   `DELETE FROM alias_okna_c5 WHERE src_table LIKE 'catalog_%'` →
   `systemctl start 1c-wiki-alias@postgres` → проба «сколько покупателей
   было за этот месяц» → 43 · Контрагенты (эталон SQL сверен).
5. Доки: Э1/В1 в PLAN_TO_TARGET/TARGET_STATUS, срез дня.

## Механика (выучено живьём, свежее)

- Кандидат :8096 = `/tmp/serene_ask_k3f2.py` (+2 патча z02guard/z12scaffold
  с бэкапами) — УРОК: правки кандидата руками на окне зеркалятся в пакет
  ТЕМ ЖЕ ЗАХОДОМ и коммитятся; иначе выкат снесёт починку.
- `git add`/`commit` разными вызовами; после MCP-записи графа —
  `sleep 3` и re-add (гейт графа читает ИНДЕКС, гонка ловилась живьём).
- Сообщение коммита — одна строка, без `; | &`, с «Числа:» и «Доки:».
- Гейт `check-golden` матчит подстроку deploy.sh в команде — обходит
  глобом `deploy*.sh`; scp в /opt на окне = выкат (требует дерево=HEAD).
- Пробы/AB на окне: раннеры в /tmp/i2, отметку скорер пишет в
  /tmp/i2/.claude/.probe-okna-last-run → scp на дев в .claude/.

## Что мешает

1. Свежесть 1С — отброшена владельцем; мисматчи client-gold из-за неё.
2. klient-1/Э2 — внешне, потом.
3. Такт мёртв с 24.08 — пробы против корпуса 25.08.

История дней — `progress.md`.
