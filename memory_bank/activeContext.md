# Активный контекст

# С ЧЕГО НАЧАТЬ (срез 13.09 глубокая ночь: R4/dual-Solr/scs-фикс/COV/promote ЗАКРЫТЫ — бой 259/774, comma 0, Solr 16→70, A3 боя 56/12/50→57/18/43; очередь J следующая; эмбеддер выключен ОСОЗНАННО; работа ТОЛЬКО на OKNA)

### 🔴 КОНТРАКТ «ОДИН ПУТЬ» (приказ владельца 12.09, действует всегда)

    вопрос → интерпретация из вики (z21) → запрос в базу →
    варианты с описаниями из вики (если прочтений больше одного) →
    выбор человека → ответ

- «Прочтение» — ЛЮБОГО рода: сущность/мера/окно(период)/ось. >1 → меню
  единым построителем (readings_menu; подписи человеческие из вики, без
  имён метаданных 1С). Ровно одно → сразу ответ. Count — мера НЕ
  спрашивается. Одно число в ответе.
- Люков нет, «вторых чисел» нет, обходов вики нет, молчаливых выборов
  при >1 нет. Новых «выбирателей»-гейтов под вопросы НЕ писать:
  тема-гейт 13.09 отвергнут живым замером (ниже).
- Носитель контракта — замки: test_one_path.py (77/0, проверки а-е),
  test_wiki_homonym_menu (одноимённые kind → меню), test_measure_menu_
  not_silent, test_no_pre_wiki_reorders и др. Все зелёные.

### ГДЕ МЫ (срез 13.09 вечер)

- **Эпизод I0 «семантический verify» ИСПОЛНЕН, замерен, закоммичен**
  (CHANGELOG 13.09(3)-(8); HEAD до addc717): П2 наблюдаемость (вердикты
  судьи fit+why → diag/jsonl/TRACE), П2b согласие sole-yes (повтор судьи;
  agree строго при полном втором ответе; disagree → меню/none), П5 щель
  деградации (непроверенный пик не уходит в ответ), П4 носитель alias'а
  до судьи (src_kind в пуле; object-фильтр освобождает только struct_alias).
  Полный L67 полигон :8092: **30 match / 36 honest_no / 1 wrong** (прод
  12.09 был 32/33/2): wrong 2→1 (склад закрыт честным no_data, «прайс»
  единственный), согласие 35 agree/6 disagree/26 без второго, p50 17.3с
  max 46.2с. Замки: verify 89/0, one_path 77/0, card_hybrid 79/0,
  homonym 10/0, i2_diag 13/0, trace_rid 22/0.
  **🔴 ВЫКАТ ЭТОГО НА ПРОД — СТОП-ТОЧКА ВЛАДЕЛЬЦА, НЕ СНЯТА** (владелец
  ушёл в словарь; вопрос «катить 30/36/1 или добивать match» открыт).
- **Подгонка откатана:** alias_top 3→6 ради «прайса» владелец остановил
  (п.0), с полигона снято; вместо этого — общий аудит словаря.
- **Аудит словаря/вики (армия 12+12 cursor):** docs/audit/dict-audit/
  {00-СВОДКА,A1..A6,B1..B6}.md. Ключевое: 65% частых вопросов с непокрытым
  словом; 89% слов-каша (≥3 сущностей); 333 коллизии (мета-слова список/
  справочник/типы ×28-27); 36% синонимов мёртвые; глаголы-события
  («наторговали/сделали») — 0; NEF = 96% соседские редиректы; «установка
  цен» у номенклатуры повлекла прайс-wrong; MERGE пишет только в пустое
  (новый промт не обновит живое без force); temperature не передаётся;
  локаль стемминга ru по умолчанию (п.0-риск).
- **Пакет промтов v2 УТВЕРЖДЁН владельцем («поехали»):** P1-P7 там же.
  П1/P2 дословные init/collision (запреты мета-слов с антипримерами,
  лимиты 3-8/1-3, тематическое «не я» без чеклистов, «сомневаешься — не
  пиши», EN-каркас + язык=title; красная P6: русские чеклисты в канон НЕ
  идут — п.0). P3 стоп-фильтр 22 леммы (классы платформы; тмц/склад —
  предметные, не трогать). P4 формат (strict-схема, response_format у vLLM,
  разделитель ' | ', 900→1600, temp=0; quantities maxItems=30 + потолок
  batch-select — правки снайпера). P7 сшивка = план исполнения.
- **GPU-эпизоды 188 (13.09):** утром 27B снята → генератор на OpenRouter
  (`a0993ab`, agent-рантайм — `c08bdee`); вечером вернули. Замер 2×2
  (CHANGELOG 13.09(24)): json_object на локальной 27B = 10/10, OpenRouter
  4/10 → контур = локальная 27B + json_object; OpenRouter — репо-дефолт
  (п.0). Ретрай `9a4f964`. Кредиты: OpenRouter ~30.5/35; Kimi-агенты 402.

### ПЕРВОЕ ДЕЙСТВИЕ / ОЧЕРЕДЬ

1. **Эпоха словаря ЗАКРЫТА** (13.09, коммиты до 3442037): G1-G8; боевая
   перегенерация 253/260+683/709 + лечение 20/20; пайплайн G7. **P4 ЗАКРЫТ
   (13.09 вечер):** collision 73/73, 0 битых, 0 ретраев (json_object, 27B);
   величин 795 (+112); A3 42/15/61 → 43/13/62 (шум). **Пустышки развилок
   ЗАКРЫТЫ:** 698→0, 3152/3152 классов (CHANGELOG 13.09(26)). Эмбеддер+
   реранкер 188 ВЫКЛЮЧЕНЫ ОСОЗНАННО (владелец 13.09): включит сам; бот без
   kNN — ок.
2. **Локальный прод :8091 — НЕ НАШ КОНТУР (владелец 13.09).** Молчит с 27.08 — не трогаем.
3. **Эпизоды 13.09 ночь ЗАКРЫТЫ (fedd606, 3c36b7b):** R4 (rc0+0 = попытка,
   mark_skip на MERGE) + dual-Solr + хотфикс scs (',| [|] ', §3.119/61). **COV+
   promote ЗАКРЫТЫ:** regen+collision → A3 c5 43/13/62→43/19/56; promote
   union-MERGE в бой (гейты a-d, snap *_pre_promote_20260913) + residual sep:
   бой 259/774, comma 0, Solr 16→70, A3 боя 56/12/50→57/18/43 (ОК +6).
4. Очередь J (18 honest_no с живым эталоном) — следующий эпизод.
5. Стоп-точка выката I0 на прод (L67 30/36/1) — владелец.

### 🔴 РЕЖИМ — АРМИЯМИ (docs/ORCHESTRATION_CURSOR.md, §9 ловушки читать целиком)

- 🔴 **ВСЕ агенты — ТОЛЬКО cursor-agent** (указание владельца 13.09,
  HOW_NOT_TO §1.21): анализ, красные, исполнение — всё свежие процессы
  `cursor-agent -p "$(cat .claude/state/prompt-<id>.md)" --force --model
  auto`; 1 задача = 1 процесс; субагенты Kimi ЗАПРЕЩЕНЫ (402 + режим).
  Промты только файлами; итог прогона = последний "type":"result".

- Волны: анализ ×3-4 независимых линзы (каждая САМ строит реестр, чужие
  отчёты не читает) → исполнение (1 задача=1 процесс; файлы-конкуренты
  не параллелят) → красная ×2 на РЕАЛЬНЫЙ дифф. Приёмка — ТОЛЬКО замеры
  оркестратора; веры сводкам нет. Промты из файлов .claude/state/;
  итог = последний "type":"result"; исполнители НЕ коммитят.
- git: add/commit РАЗНЫМИ вызовами; пат-спек ПЕРЕЧИСЛЕНИЕМ файлов (не
  каталогом, §9 №9-10); «Числа:»/«Доки:»; граф тем же коммитом;
  tsv-приёмка — только со «Факт 1С:». Диагностика python — heredoc,
  НЕ -c (§9 №8); pkill -f по подстрокам промта запрещён (§9 №7).

### МЕХАНИКА

- Окно: ssh -i ~/.ssh/id_ed25519_deploy -p 2202 root@gpu-erw.timpul.pro;
  движок :7890 (psql host=127.0.0.1 user=postgres dbname=postgres).
- Прод-ask :8091 (юнит 1c-serene-ask@postgres.service); полигон :8092
  (probe8092, /tmp/probe_root; scp ask/*.py → rm __pycache__ → restart).
  Выкат: scp в /opt/1c-mcp-reports/ask/ + rm pycache + рестарт + health;
  bak прод flip-20260912.
- Токен: T=$(grep -h "^ASK_TOKEN" /etc/1c-serene-ask*.env
  /etc/1c-mcp-reports.env | head -1 | cut -d= -f2 | tr -d '"').
- Полный L67 (окно): cd /opt/1c-mcp-reports/work/gold && nohup bash -c
  "…env /tmp/ask_env.bin…; I2_ASK_URL=:8091|:8092 I2_RESHOOT_DSN=…
  I2_TAG=<tag> python3 -u i2_runner.py run --tsv /tmp/v2-full.tsv
  --path engine --out /tmp/i2-<tag> --reshoot-rules
  reshoot-rules-okna.json --workers 4"; итог — verdict'ы
  i2-answers.jsonl (логика i2_runner.py:215+).
- LLM прода = OpenRouter qwen/qwen3.8-27b (env DEEPSEEK_*; 402 = стоп-
  владельцу). Генератор словаря — локальная 27B 178.63.211.188:8000
  (vllm/Qwen3.8-27B) + json_object (extra_body.response_format в params
  песочницы), ALIAS_INFER_RUNTIME=agent + OPENCLAW_HOME=песочница в env
  юнита и тика (ловушка 60 techContext). Такт жив, вектора целы;
  полигон :8092 — transient probe8092 (systemd-run; порт задавать env-файлом
  /tmp/probe_root/port.env ПОСЛЕДНИМ EnvironmentFile — Environment= средой
  перекрывается файлами!).

### ПРАВИЛА ДНЯ (полностью — HOW_NOT_TO §3.113-3.117; тут — только указатели)
- Нет ETA по досчёту; бэкап emb первым; ручной embed_bulk — §9 HOWTO;
  тикер слеп — факты INSERT/w-файлы; выкат scp+md5 (не git).
- git add/commit разными вызовами; CHANGELOG не затирает нижний заголовок;
  pgrep из ssh — маска «[.]». Потеря 09.09 = v8c-канон (§3.113).

### АРХИВ ЭПИЗОДА (сжато 13.09; полные истории — CHANGELOG 09-12.09, progress.md, HOW_NOT_TO §3.113-3.117)

- **Такт/корпус (09.09):** каскад 5 слоёв закрыт; 218-lowercase решён
  (_meta_canon); корпус 1 674 763; emb-бэкапы ×3 живы (§3.113, 6a0 свято).
- **Полная B (row-level, решение №14):** 1∥3-A выкачаны, Speed-II+сторож A
  внедрены; 1∥3-B после стоп-точки; далее 4→5→6 (6 = flip, ПРЕДУСЛОВИЕ
  6a0-бэкап). План docs/audit/FULLB_PLAN_2026-09-03.md.
- **Эмбеддер ЗАКРЫТО (10.09, армия ×4):** Qwen3-4B bf16 — канон; смена
  только после gold-прогона рядом (docs/research/EMBED_MODEL_DECISION.md).
- **Web-контур Open WebUI (11.09):** жив; долг — rerank ~30с/вопрос и
  INTENT_SAMPLES=5 (78с) — структурный пакет после базового русла.
- **Долги:** coverage-постчек; подстроки имён регистров z13 (п.0, выжигать);
  инертные stock-helpers; fsst AUTO_NATIVE снять после фикса разработчиков.
- **Красные замки (не чинить):** test_pipeline_doc 5/8, test_build_solr_
  synonyms; окруженческие ask_embed_native/focus_loop; test_client_gold 55/2
  (web-классификатор). Прогоны только на окне; эталоны L67 ЖИВЫЕ дрейфут —
  сравнивать по answers.jsonl.

### МЕХАНИКА (выучено; НЕ забывать)
- 🔴 Заголовок раздела «С ЧЕГО НАЧАТЬ» — ТОЛЬКО H1 («# С ЧЕГО НАЧАТЬ…»):
  awk session-start.sh:23 ищет `^# ` и при H2 автовброс состояния молча
  умирает (29.08–04.09, HOW_NOT_TO §3.111).
- Прогон И2 на окне (каталог выката): I2_RESHOOT_DSN user=postgres (serene_ro
  даёт reshoot_error); **workers 16** (решение владельца 02.09: ~28 мин против
  ~2 ч на 6); TSV — work/gold ВЫКАТА (канон ubuntu/serenedb/client-gold-okna.tsv);
  jsonl пишется в конце; дозор на PID; /tmp/ask_env.bin может отсутствовать —
  env собрать из /etc/1c-*.env (кавычки срезать).
- 🔴 **Файлы work/gold на окне деплой НЕ покрывает** — после правки
  i2_runner.py / reshoot-rules-okna.json синхронизировать ВРУЧНУЮ scp в
  выкат (сделано для reshoot-rules 02.09, md5 6dd0aae3).
- 🔴 **Дозор по pgrep — только с bracket-трюком** `pgrep -f "cursor-agen[t] -p"`:
  голый паттерн совпадает с командой самого дозора → вечный цикл (случай
  02.09, дозор картографов rm6 поймал сам себя).
- Извлечь итог cursor-прогона из stream-json лога: последний эвент
  type=result → поле result (скрипт-извлекалка писала result-*.txt рядом).
- Пробы: прод — `python3 /tmp/diag_ask2.py "вопрос"` на окне; код-кандидат —
  /tmp/probe_root (cp -r /opt/1c-mcp-reports) + diag_ask2p.py. /tmp не
  переживает ребут: env пересобрать из /etc/1c-*.env (кавычки срезать).
- Замки — НЕ pytest (тесты — скрипты): `cd /srv/1c/ubuntu/serenedb && for t in
  ...; do python3 $t.py; done`.
- cursor-agent: промт только из файла; хук check-prompt-rules блокирует
  StrReplace на z20 — cursor правит через shell, это норм; правки проверять
  git diff + замки самому; слова исполнителя — замером оркестратора.
- Гейты: add и commit разными вызовами; сообщение с «Числа:» и «Доки:»;
  граф MCP → sleep 3 → grep → add mcp-memory.json в тот же коммит; коммит
  пат-спеком. index.lock без процесса — старый, снести (случай 01.09 21:04).
- GPU: embed 10.3.1.11:8000 + rerank :8005; 27B 178.63.211.188:8000 жива
  (возвращена владельцем 13.09 — генератор на ней + json_object). Доступ
  `ssh -i ~/.ssh/id_ed25519_deploy root@178.63.211.188` / `root@49.13.97.101`.

### РИТУАЛЫ (выучено гейтами)
- 🔴 **git add и git commit — РАЗНЫМИ ВЫЗОВАМИ Bash, без `&&` между ними**
  (снайпер и гейты смотрят индекс ДО выполнения команды). Сначала add,
  проверить `git diff --cached --stat`, потом commit пат-спеком. Сообщение
  одной строкой с «Числа:» и «Доки:».
- MCP-граф: add_observations → sleep 3 → grep проверка на диске → git add
  mcp-memory.json → коммит с ним же.
- Промт-правила ловит check-prompt-rules: в строковых литералах питона
  (включая докстринги!) — никаких «обязан/должен»; писать описательно.
- _bootstrap.py патчит z20 при load_all по ТОЧНЫМ якорям — правка z20 в зоне
  якорей требует синхронной правки _bootstrap.py, иначе сервис не встанет.
- deploy-ask.sh только из HEAD=дерево; после выката restart
  1c-serene-ask@postgres + сверка md5 с /srv/1c. Не рестартовать сервис,
  пока идёт прогон.
- Фоновые ssh: дозоры disable_timeout=true; раннер nohup на окне; локальный
  ssh-клиент умирает по 600 с — это НЕ падение. Время серверов = UTC, не
  сдвигать (утренние «паники» 30.08 — неверные сдвиги).
- cursor-агенты: kill и запуск НИКОГДА в одной ssh-строке (pkill по литералу
  команды = самопопадание, 5 раз за смену 30.08); модель оркестратора k3,
  субагенты принудительно на дешёвой ([secondary_model] force = true).
- Гейт check-docs ловит пути вне репо в документах («файл не в дереве») —
  пути серверов писать словами, не как repo-path (дважды 01.09).
- Векторизация — только docs/EMBED_BULK_HOWTO.md §9; вектора бэкаплены (md5
  30b9663c… 31.08, d7aeb15e… 01.09).
- **Ничего лишнего:** не править файлы вне задачи, не ходить на другие
  серверы, не писать новые скрипты поверх базы, не запускать новых прогонов,
  пока идёт текущий.
- Владелец — парень; сроки без замера не обещать.

### КТО Я И ГДЕ РАБОТАЮ
Оркестратор /srv/1c (Kimi). 🔴 **Работаем ТОЛЬКО на okna** — это не деталь, а
граница сессии: ssh окно `-i ~/.ssh/id_ed25519_deploy -p 2202 root@gpu-erw.timpul.pro`
(движок SereneDB: `psql "host=127.0.0.1 port=7890 user=postgres dbname=postgres"`,
многооператорный SQL — stdin-файлом). **klient-1, klient-2, песочницы и любые
другие серверы НЕ ТРОГАТЬ — ни читать, ни писать, ни «проверить».** «postgres»
в DSN = имя базы SereneDB, СУБД PostgreSQL в контуре нет. 🔴 ЖИВАЯ СБОРКА ОКНА =
26.08.1 (SELECT version() 03.09: «PostgreSQL 18.3 (SereneDB 26.08.1)»; бинарь
/usr/local/bin/serened; строка «26.07.3» в докам устарела). GPU: 10.3.1.11
эмбеддер :8000 (НЕ трогать); 27B 178.63.211.188:8000 жива — генератор на ней
+ json_object (замер 2×2, CHANGELOG 13.09(24)).

### 🔴 TARGET.md — ПРАВИЛА, КОТОРЫЕ НЕЛЬЗЯ НАРУШИТЬ НИ В ОДНОЙ ПРАВКЕ
- **п.0/п.9 — СУТЬ, не слова** (владелец 02.09: «смысл пункта 0 не про слова,
  а про суть. Вникни»): прод-база скрыта НАРОЧНО — любая вещь, работающая
  только потому, что я знаю окна или сделал руками (env-флаг, ручной запуск,
  канон «потому что я так видел»), = нарушение. В коде — ни имени сущности,
  ни списка слов домена; в контуре — ни одного шага, который установка не
  делает сама (проверено 02.09: таймер такта в проде включает firstbuild
  ubuntu/packet/firstbuild_unit.sh:58; флаг ASK_WIKI_CHOICE удалён — коробка
  = бой). Проверка себя: «сработает ли это на базе, которую я никогда не
  видел, БЕЗ МОИХ РУК?» — нет → не делать.
- **п.12:** отказ и уточнение — не ошибка; ДОГАДКА — ошибка. Молчаливо
  подставленное окно/мера/сущность = догадка, запрещена.
- **п.21:** ответ → уточнение → отказ; отказ при наличии данных — дефект.
  Проверка, отвергшая верный ответ, — дефект проверки.
- **п.19/п.20:** считает и ищет база (SereneDB), модель — только смысл; своих
  скриптов поверх базы — нет; перед SQL — доки serenedb-docs.
- **п.10:** неверный ответ хуже отказа. Каждое ослабление проверки обязано
  быть доказано полным прогоном (wrong=0), не пробой.

### 🔴 ДЕЙСТВУЮЩИЕ РЕШЕНИЯ ВЛАДЕЛЬЦА (не открывать заново)
1. **«Один судья» (01.09, согласовано):** сторожей-судей выбора сущности нет.
   Все сигналы — ВХОДЫ ПУЛА карточек (kNN, словарь синонимов, носители меры,
   названная ось). Судья один — паспортная верификация каскада z21.
2. **«1 механизм, чинить НАЧАЛО» (01.09):** если на выходе плохо — проблема в
   начале цепочки (понимание → пул → выбор → верификация), а не в сторожах
   хвоста. Не разбирать вопросы по одному — правила общие, проверка = полный
   прогон 67, не пробы.
3. **Скилл-декомпозиция согласован (после стабильного базового прохода):**
   отдельный слой поверх ask: вопрос → LLM-разбивка на подвопросы (простой =
   одна часть) → точечные вызовы ask → сборка чисел КОДОМ (п.19) → журнал
   «вопрос → подвопросы → ответы» (наблюдаемость этапов). Не строить, пока
   точечный ответ нестабилен.
4. **П.0/п.9/п.12/п.21 TARGET — свято.** Ни одной подстройки под базу/вопросы;
   догадка (молчаливый assumed-окно) запрещена; отказ при данных — дефект.
5. **Словари синонимов — локальная 27B (178.63.211.188:8000) + json_object**
   (13.09 вечер, владелец вернул 27B; замер 2×2: на ней со схемой 10/10,
   OpenRouter со схемой 4/10 — CHANGELOG 13.09(24); отменяет дневное
   «OpenRouter» и «наш 27B» от 31.08). OpenRouter — репо-дефолт новых баз
   (п.0). Генератор — юнит 1c-wiki-alias; такт только компилирует таблицу.
6. **Пул из 1 карточки — верифицировать тоже** (02.09): struct_single
   пропускал паспортную проверку; теперь проверяется и одиночный кандидат
   (класс 5, c1c7d7a).
7. **UT забыт, работаем ТОЛЬКО на окнах** (02.09): правило про замер первой
   базы (REGRESSION_BASE1) неактуально. Строка в AGENTS.md «Куда что писать»
   осталась — файл root-owned, убрать владельцу.
8. **Такт — поднять автоматически, но СНАЧАЛА проверка армией по атомам**
   (02.09): история — такт жёг вектора, пересчёт = сутки GPU. ✅ ВЫПОЛНЕНО
   02.09: армия 8×2 дала карту (см. СДЕЛАНО ЗА ДЕНЬ 02.09), таймер включён
   (решение №13). Запись оставлена ради истории «почему армия».
9. **Приоритет (02.09): простые вопросы — верные ответы.** Скилл-декомпозиция
   («реально покупают» и др. составные) — позже, поверх стабильной точки.
   Отказы класса «одна ветка-неоднозначность» скилл НЕ лечит — их лечит
   исход A (класс 6), разобрано с владельцем 02.09.
10. **Простота решений, «не усложняй» — именно это сработало** (02.09,
    дословно владельца): один механизм вместо сторожей, удаление вместо
    заплаток, общее правило вместо разбора вопроса. Каждый шаг — сначала
    спросить «нет ли более простого пути».
11. **Текущая схема — лучшее за всё время: правильные ответы** (02.09,
    дословно владельца): «Любой отход от этой схемы — нарушение и
    усложнение». Схема: один путь через wiki (пул → pick → verify → лидер
    или clarify), судья один — паспортная верификация, без pre/post-заплаток.
    Новые механизмы вне этого пути не заводить; правки — только внутри него.
12. **Каждый анализ/план — через армию cursor: 3 агента на вопрос, круги до
    СХОЖДЕНИЯ** (02.09, дословно: «прогоняй через агентов, пока не
    сойдётесь. Не по 2, а 3 агента на 1 анализ»). Красная команда обязательна
    перед исполнением; исполнитель получает только сошедшийся план с
    оговорками всех кругов.
13. **Такт по таймеру разрешён и включён** (02.09): карта атомов (армия 8×2)
    доказала отсутствие флагов-убийц; таймер 1c-serene-pipeline@postgres.timer
    enabled. Каприз systemd: если сервис не запускался с загрузки, таймер не
    планируется (NextElapse=infinity) — лечится одним ручным тактом.
14. **Row-level дельта («полная B») — ОБЯЗАТЕЛЬНА** (03.09): сущностная
    гранулярность неприемлема (п.0 по сути). План v4.1 сошёлся —
    docs/audit/FULLB_PLAN_2026-09-03.md; исполнение после такта №2+L20.
15. **ФОРМУЛА ОТВЕТА (11.09, дословно владельца; главный приоритет):
    «вопрос → интерпретация из вики → вопрос в базу данных → вывод
    вариантов с описанием из вики → выбор → ответ. Ничего больше».** Любые
    «подготовки семейств», перестановщики пула, канонические ветки, выбор
    меры наугад — НЕ РАБОТАЮТ (2 месяца заплаток). Прочтений >1 → меню с
    подписями из вики + выбор человека; однозначно → сразу ответ. Правки
    под конкретные вопросы запрещены. Доведение тракта — УДАЛЕНИЕМ слоёв
    (волна армии: карта отклонений от формулы → план → стоп-точка).
