# Active Context

_Обновлено: **2026-08-28 ночь** (В2 закрыт: полный набор флагов в бою, контур-24
**20/24**). Здесь — только живое. История — в [`progress.md`](progress.md);
стадии — в [`docs/TARGET_STATUS.md`](../docs/TARGET_STATUS.md)._

---

# ⏭ С ЧЕГО НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

🔴 **В2 закрыт `[28.08 ночь]`**: все 7 флагов в бою `:8091` (HEALTH_NATIVE_FRESHNESS,
RESOLVER_IVF, SOLR_SYNONYMS+DICT, ATOM_TERMINAL, ENTITY_FORM, SALES_RANK_CANON;
RRF был). Каждый шаг AB_PROBE **8/8 0err**; срезы
`work/acceptance/runs/f6-rollout/<флаг>/{before,after}`. Контур-24 today-gold
лестницей: **15** (RRF) → **16** (+F/I/S) → **20/24** (полный, средняя 24,58 с),
код `24557f1` (K6b) = HEAD, md5 ask `e57873e1`. Ранговая тройка НЕ опциональна
(без неё ранги/атомы падают). CHANGELOG 28.08 «В2»; чеклист Ф6.5 — отметки ✅.

🔴 **K6b в бою `[27.08 поздно]`**: регрессия рангов патча 2 (исход B отдавал ось
mtd assumed вместо лидера) изолирована и починена — «какой клиент больше всех
купил» OK answer; `docs/K6_ENTITY_RANK.md` §7.9–7.11. Замки: rank_leader_path
**30/0**, fork **29/0**, k4 **27/0**, entity_form **47/0**, compose **92/0**.

🔴 **С1/С4 закрыты `[27.08]`**: боковая `alias_okna_c1` **258/257**, новых **0**,
расхождений **0** (генератору нечего добавить); `ts_lexize('search_dict_syn',
'клиент')` = **{клиент,контрагент,поставщик}** — значит K6-двойка НЕ в словаре,
а в выборе сущности. `period_relative_forms` в `search_meta` живьём (1 форма);
Д4: файлы шага 1-period доставлены (`dc72fd8`).

## Что мешает приёмке (житьё, числа)

1. **Свежесть / Д5** — без изменений: ждёт `--smoke`/агент ≥1.1.3 на Windows;
   такт fail до корпуса. Свежесть отложена владельцем.
2. **K6-двойка** («позиций 1891», «клиентов 141») — обрыв на выборе сущности
   при живом словаре; класс для К-потока.
3. **«Молодец»-ранг** — сленговая формулировка ранга без глагола покупки →
   clarify; хвост K6b (§7.11), воспроизводим.
4. **«как у нас дела?»** — §10-класс, исход скачет (no_data→figures); отдельно.
5. **«позавчера»** — пустое окно -2 дня разваливается в паспорт-мусор (§7.10);
   «вчера» на том же нуле отвечает корректно.

## Следующий шаг (порядок)

1. **K6-двойка классом** (выбор сущности при живом словаре) — К-поток, один
   агент на `serene_ask.py`/`entity_rank_v2.py`; диагностика детектора развилки
   в трассах /tmp/k3f2.log (rid ab…214822: 18 классов → круг арбитра).
2. Затем «молодец»/§10-класс/§7.10 — малые классы того же потока.
3. **И2-валид** — прогон на `client-gold-okna.tsv` (54 вопроса, эталоны сверены
   с 1С) на боевом контуре с полным набором флагов; затем **И3** (три прогона,
   разброс) на том же наборе.
4. **ШФ** — `docs/PIPELINE.md` заново по факту (флаги в бою, ранг v2, 1-period).
5. В3 klient-1 — после И2/И3.

🔴 **Чужие файлы:** сейчас курсоры неактивны; `serene_ask.py` свободен.

## Сделано 27.08 поздно / 28.08 ночь (доказано числом)

| Коммит / факт | Числа |
|---|---|
| K3 патчи 1–3 (`92ef1c3`/`61edb43`/`02b1016`) | страж A+B, канон>страж, гарда; контур боя 15/24 |
| K6a `abc359b` q_meta_overlap | bench v2 lead **18/23**, v1-регресс **0**; класс-набор 3 |
| С5 `2ab3a95` прибор кандидатов | замок **22/0**; candidates.tsv read-only |
| Д4 `dc72fd8` файлы 1-period | доставка md5 совпала; search_meta **1** форма |
| K6b `24557f1` лидер исхода B | контур-24 **19/24** (кандидат) → **20/24** (бой); замки **0 FAIL** |
| В2 флаги в бой (7 шт по одному) | probe **8/8** каждый; лестница контура **15→16→20** |



| Коммит / факт | Числа |
|---|---|
| Э4 `bf8ac41` partial в ответе | замок **13/5→20/0**; AB_PROBE **8/8** |
| С2 `3350b12` словарь | **254→257**, avg **55→84**; K6 live **0/2** |
| С3 `343d856` морфология | alias_rank лидер **0→3** |
| С5 `fbbc84c` stem в пайплайне | alias_idx **257**; **3/0/20**; замок **19/0** |
| Д3 `5fb8bf3` пустой alias_idx | **257/257**; замок **13/0** |
| С4 `b80d935` обрыв K6 | эталоны **141**/**1891**; clarify не на предикате |
| К2 `4ac3c38` compare | **0/8**; золото **1049991.33** |
| Д4 `247c2f3`/`0e050b3` такт | **3083×** fail → init OK → metadata **0** |
| K5 в дереве | замки 13/12/11/11/51; стейджинг **8/8** |
| K6 `bb6779f` answer_fit | gold-23 **2→1**; «покупают» **5→1** |

Отчёты: `docs/C2_ALIAS_SWAP.md`, `C3_MORPHOLOGY.md`, `C4_K6_BREAKDOWN.md`,
`C5_MORPH_IN_PIPELINE.md`, `D3_EMPTY_INDEX.md`, `D4_PIPELINE_BROKEN.md`,
`K2_COMPARE_TAIL.md`, `K5_APPLIED.md`, `K6_ENTITY_RANK.md`.

🔴 **GPU в норме `[26.08, слово владельца]`**: embed + 27B + reranker живы.

🔴 **приёмка — только okna** (ut_test выведен, 26.08).

🔴 **И0 / И5**: etalon_1c в дереве; гейт gold-split **7/7** в git — установка
`install-gates.sh` за владельцем.

🔴 **Ф6 `[28.08]`**: бой `:8091` — полный набор флагов (7, В2 закрыт);
`ASK_CALENDAR_AXIS` на okna no-op. Чеклист `docs/F6_ROLLOUT_CHECKLIST.md`
(отметки ✅). K6 словарём **не** закрыт (замер 27.08).

🔴 **режим оркестрации**: `docs/ORCHESTRATION_CURSOR.md` — свежий
`cursor-agent -p --force --model auto`, без резюме сессий.

🔴 **канон GPU vSwitch `[23.08]`**: `10.3.1.11` / `10.3.1.12`
([`docs/NETWORK.md`](../docs/NETWORK.md) §2.1).

🔴 **Полноту данных ведёт оркестратор** —
[`docs/PLAN_ORCHESTRATOR.md`](../docs/PLAN_ORCHESTRATOR.md).

История 25–26.08 и развёрнутые буллеты дня — в `progress.md` (перенос Т1).
