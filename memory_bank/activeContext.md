# Active Context

_Обновлено: **2026-07-28, вечер.** Актуально — верхний блок. Всё, что ниже раздела
«Что делаем и зачем», написано 25-27 июля и устарело (оставлено намеренно: видно, как
менялись выводы)._

---

# ⏭ С ЧЕГО НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

**Сначала три файла:**
1. [`MAP.md`](../MAP.md) — где какие файлы и что чем занимается;
2. [`docs/TARGET_STATUS.md`](../docs/TARGET_STATUS.md) — **что на какой стадии по каждому
   пункту контракта.** Спутник `TARGET.md`, отвечает на «что готово, что осталось»;
3. этот раздел — что делаем прямо сейчас.

## 🔴 31.07 ночь: приёмка 1-30 снята, дальше упёрлось во внешнее

**Первое действие следующей сессии:** проверить, пополнен ли ключ DeepSeek
(`docs/BOXED_BLOCKERS.md`). Пополнен — повторить `batch_bot.py 31 58` с НОВЫМ `RUN_TAG`.
Не пополнен — заняться эталонами (см. ниже), для них модель не нужна.

**Измерено** (`RUN_TAG=v12`, чистые сессии, `docs/ACCEPTANCE_UT.md`):
верно 7, уточнений 3, неверно 6, **без эталона 13** на вопросах 1-30.
Вопросы 31-58 оборвались на исчерпании ключа — их цифры замером НЕ являются.

**Две задачи, которые видно из этого прогона:**

1. 🔴 **Проставить эталоны 13 вопросам.** Треть набора нельзя проверить автоматически.
   Пока их нет, «сто процентов верных» недостижимо не из-за системы, а потому что мерить
   нечем. Требует 1С под рукой.
2. **Выбор сущности** — единственная причина всех оставшихся провалов. Счёт верен везде.
   Лучшая пачка (11-20, 4 верных, 0 уточнений) — та, где сущность названа своим именем;
   провалы там же, где надо догадаться, где смотреть.

**Что закрыто в эту сессию:**
- папка справочника не позиция, и число отброшенных названо в ответе (227 = эталон);
- булевы реквизиты — типизированной картой `flags`, а не поиском подстроки в тексте;
- правило подтверждения выбора сущности **впервые заработало** — было мертво по трём
  причинам сразу (`HOW_NOT_TO §1.24`);
- название сущности служит её алиасом само по себе, из `search_tables.label`;
- величина ищется в семье (шапка ↔ табличная часть).

**Что проверено и ОТКЛОНЕНО замером — не повторять:**
- перегенерация всех алиасов: 2/3/5 против 3/4/3, откачено (`HOW_NOT_TO §1.26`);
- жёсткое правило «подтверждает только лидер» (`ASK_ALIAS_VETO=1`): 1 улучшение против
  3 ухудшений, оставлено выключенным (`§1.25`);
- индекс алиасов на словаре со стеммингом: ранжирование вырождается в нули.

## 🔴 30.07 поздний вечер: приёмка по пачкам, идёт пачка 1

**Где мы.** Пачка 1 (10 вопросов) на чистых сессиях `RUN_TAG=v9`: **верно 3, уточнений 4,
неверно 3** (было 2 / 4 / 4). Метод согласован с владельцем: гоняем по 10 вопросов, после
каждой пачки разбираем. Цель — 100 % верных ответов, уточнить 2–3 раза лучше, чем ответить
неверно.

**Закрыто в этот заход:**
- вопрос 10 «сколько позиций номенклатуры» — 252 → **227**, эталон. Папка справочника не
  позиция, число отброшенных групп названо в ответе;
- вопрос 6 «сколько штук закупили» ушёл из неверных в **уточнение** — предпочтительный исход;
- булевы реквизиты собираются типизированной картой `flags MAP(VARCHAR, BOOLEAN)` вместо
  поиска подстроки в тексте (`HOW_NOT_TO §1.22`, `techContext` ловушка 35).

**Первое действие следующей сессии — дождаться/проверить пересборку корпуса** (запущена
30.07 18:45 UTC, `scratchpad/rebuild3.sh`, ~13 мин) и сверить:
`папок по карте` = `папок по прежнему подстрочному поиску` = 375, номенклатура 252 / 227,
партнёров 164, сумма приобретений 73 181 158. Затем прогнать пачку 1 заново.

**Что осталось неверным в пачке 1:**
- № 2 «НДС поставщикам»: 8 654 378,11 против эталона 11 036 086,09 — берётся не та сущность
  (сущностей со словом «НДС» в базе 18);
- № 4 «НДС в продажах» — та же причина;
- № 7 «самая высокая цена за единицу»: 18 342 против эталона 50 000 — сущность подтверждена,
  выбирается не та величина.

Общее у всех трёх: **выбор сущности/величины**, а не счёт. Дальше — пачки 11–58.

## ✅ 30.07: Windows-установщик 1.1.0 (параллельный контур `work/installer-exe`)

Доведён `setup-1c-odata.exe`: префлайт (CPU/RAM/диск/веб-модуль), два варианта состава,
`--ubuntu-host` + `--external-url` для связи Windows↔Ubuntu. [замер] TCP до
`192.168.56.42:22` OK; OData с Ubuntu через `192.168.56.1:6003` → 401; hairpin с Windows
на роутер — ожидаемо. План: `work/installer-exe/PLAN.md`.

## Состояние на конец 30.07 — приёмка через бота начата, идёт переописание вики

**Первая пачка (вопросы 1-10) через бота: верно 2, уточнений 4, неверно 4.** Разбор — три разные
причины, все записаны в `docs/ACCEPTANCE_UT.md`; главная — страницы вики не различали соседей.

**Что сделано после разбора:**

| Правка | Замер |
|---|---|
| объявленный ключ не попадает в величины | `SurrogateKey` 656 → **0** сущностей, `LineNumber` 359 → **0** |
| признак «величина» отделён от «число в тексте» | первая попытка дала «дублей ключа 1368», защита остановила перенос, корпус цел |
| страницы вики описывают сущность в отличие от соседей | пачка по близости смысла, соседи названы поимённо |
| одна осечка модели не обрывает прогон | пачка пропускается и помечается, число пропущенных печатается |

**Идёт прямо сейчас:** переописание 680 сущностей группами по смыслу (пачки по 12, ~1 мин на
пачку). Сделано 143, все с разграничением.

**Следующие шаги:**

1. дозаполнить **12 деловых сущностей без страницы** (среди них `Цены номенклатуры`, давшая
   неверную цену 59 992 вместо 50 000);
2. перезалить страницы (`wiki_publish.sh`) и скомпилировать;
3. **перезапустить первую пачку** и сравнить с «верно 2, уточнений 4, неверно 4»;
4. правило «нет обращения к данным → ответ не уходит» — **инструментом**, не промтом: механизм
   найден (`message_sending`, `reply_payload_sending`, `before_tool_call` умеют block/cancel),
   не внедрён. [замер] бот задавал уточнение при `инструменты: None`, то есть из общих знаний о
   1С, а не из этой базы.

## ✅ 30.07, вечер: путь «вопрос → вики → поиск → уточнение → ответ» замкнут через бота

**Бот отвечает по данным второй базы.** Решения владельца 30.07: база — вторая (`ut_test`), мост
— новый, отчёты пока не трогаем.

| Проверка через бота | Итог |
|---|---|
| «на какую сумму мы закупили товаров и услуг» | **73 181 157,68 по 249 документам, 4 из 4** |
| «сколько мы продали» | **переспросил** «оптовые или розничные» вместо молчаливого выбора |
| «сколько у нас партнёров» | 164, старение показано клиенту |

**Что для этого сделано:**

1. `1c-mcp-ask` (:6016) — новый мост `ask_1c` → `serene_ask` (:8099), пробрасывает
   `focus`/`measure`/`context`, поэтому уточнение двустороннее. `MCP_TOKEN` fail-closed;
2. мёртвый braine (`second-brain` → :8090) из конфигурации бота **убран** — он был единственным
   источником данных для бота, и бот на любой вопрос отвечал «сервер 1С не отвечает»;
3. вики: 697 страниц собирает база (`wiki_build.sql`), публикует `wiki_publish.sh`, алиасы даёт
   `openclaw agent` — всё шагами такта, не руками;
4. порядок перефразирования вписан в `AGENTS.md` бота;
5. модель бота и сервиса — `deepseek-v4-pro` (указание владельца).

**Следующее: полная приёмка 58 вопросов через бота** — проверены только четыре тяжёлых вопроса и
один на уточнение, честная доля пока не измерена.

## 🔴 Отложено по слову владельца: Windows-PG + attach не сделан

[замер 30.07] в `ut_test` **1 620 таблиц витрины лежат ВНУТРИ SereneDB**, присоединена только
наша же вторая база. Решение владельца 26.07 требует обратного: сырые данные в PostgreSQL на
Windows, SereneDB держит только корпус и индекс через `ATTACH` + `VIEW`. Владелец 30.07:
«отдельная работа, но запиши что надо» — перечень шагов в `docs/BOXED_BLOCKERS.md`.

## Идёт: вики OpenClaw как основа для выбора сущности (решение владельца 30.07)

Замысел владельца: у модели нет представления, что значат сущности, — она видит только список
названий. Штатный механизм `memory-wiki` даёт основу: страница на сущность с `aliases`,
`bestUsedFor`/`notEnoughFor`, связями и утверждениями, а поиск по ней (`wiki_search`, режим
`route-question`) возвращает несколько страниц, а не всю базу — значит п. 19 цел.

Согласовано владельцем: плагин включаем; зависимость от шлюза OpenClaw приемлема («он ведь
тоже не в облаке, а на нашем сервере»).

## Состояние на конец 30.07 — данные закрыты, остался ВЫБОР СУЩНОСТИ

**Данные второй базы верны и воспроизводимы.** Корпус 623 565, две пересборки подряд дают
одинаковые ключи (0/0/0), первая база цела (103 808 → 103 808), счёты сверены с живой 1С.

**Узкое место — отвечающий сервис, и у него ОДНА главная причина.** [замер, приёмка 58
вопросов, 23 с эталоном] из 11 провалов **10 — выбрана не та сущность**; одиннадцатый —
№10, где группы номенклатуры посчитаны как позиции (`IsFolder` — поле платформы).

| По 23 вопросам с эталоном | начало 30.07 | конец 30.07 |
|---|---|---|
| верно | 7 | **9** |
| неверное число | 10 | **9** |
| уточнение | 1 | **3** |
| отказ при наличии данных | 5 | **2** |

**Что сделано в отвечающем сервисе 30.07** (подробно — `docs/HOW_IT_WORKS.md §4`,
`docs/DEFECT_FOCUS_CHOICE.md`):

1. **модель формулирует параметры подсчёта** — тип, имя величины дословно из данных, что
   считать; имя сверяется с `measures_of`, выдуманное до калькулятора не доходит;
2. **догадка реранкера не принимается**, когда спрашивают величину, а модель её не назвала —
   вопрос уходит человеку;
3. **арбитр** выбирает между ГОТОВЫМИ ответами, но только там, где сомнение НАЙДЕНО;
4. **уточнение о величине** — четыре «суммы» у документа перечисляются человеку;
5. **штатный словарь движка** со стеммингом вместо моего `contains` (`techContext` 35).

**Что дальше:**

1. 🔴 **выбор сущности** — 10 из 11 провалов. Величина и арифметика уже верны;
2. №10: группы номенклатуры считаются позициями (`IsFolder`);
3. **31 вопрос приёмки без эталона** — ловушки на отказ и уточнение, не измерены;
4. `docs/BOXED_BLOCKERS.md`: резолвер на нелатинских базах, словарь `ru_RU.utf8` по умолчанию.

**Мои ошибки метода за день — все одного корня** (замер не отвечал на вопрос, ради которого
делался): `HOW_NOT_TO §3.25`, `§1.15`, `§1.17`, `§1.18`. Плюс `§1.16` — применил механизм для
спорных случаев ко всем, `§3.30` — повторил в починке тот дефект, который чинил, `§1.14` —
вынес наверх решение, уже принятое в контракте.

## Состояние на 30.07 — «строка ≠ объект» закрыта, четыре условия выкладки закрыты

**Главный дефект второй базы починен и проверен на живой 1С.** Строка витрины ≠ объект 1С:
у ссылочного объекта табличная часть разворачивалась в несколько строк, и каждая шла в
корпус отдельной записью. Отсюда завышенные счёта и суммы.

| Проверка | Результат |
|---|---|
| партнёров в корпусе = в живой 1С | **164 = 164** (было 216) |
| контрагентов | 155 (было 180); номенклатуры 252 (было 311) |
| `сумма приобретений` | **73 181 158** (было 332 313 829) |
| объекты на трёх стыках, 300 сущностей | 52 450 строк → 43 295 объектов → **43 295** в корпусе, расхождений 0 |
| **подмена реквизитов шапки значениями строк** | **0** на 14 878 значениях, сверено с живой 1С (было 551) |
| выброшенные величины нашлись у вложенной сущности | **448 из 448** |
| осиротевшие строки табличных частей | **0 из 53 057** |
| корпус второй базы | 632 720 → **623 565**; дублей ключа 0; расхождений текста внутри объекта 0 |
| первая база | 103 808 → 103 808, расхождений 0 |

**Четыре условия выкладки, названные разборами, закрыты** (`PLAN_ROW_IS_OBJECT §6-бис`):

| Условие | Как закрыто |
|---|---|
| защита «удаление >10%» по ОБЪЕКТАМ | смена ключа больше не считается потерей. [замер] на подделке прежнего состояния прежняя защита насчитала бы 165 из 165, новая — **ровно 1** настоящий пропавший объект |
| `poc_load_entity.py` в отпечатке пропуска | иначе правка загрузчика молча не применялась бы к корпусу |
| условие «есть где взять» проверяет ОБЪЯВИВШУЮ сущность | а не любую по шаблону имени; шаблон ломался на именах с `_` |
| перепись «собралось не полностью» ВОЗВРАЩЕНА | через число объектов витрины, а не отключена для `Ref_Key`. [замер] **ноль** сущностей с пометкой при включённом условии |

**Найдена ловушка планировщика движка (33).** Сравнение колонки из `LEFT JOIN` с колонкой
левой таблицы в ФИЛЬТРЕ **молча не применяется** — фильтр отбрасывается целиком, ошибки нет.
`coalesce`, `IS DISTINCT FROM`, обёртка в подзапрос — все дают все строки. Работают:
константа, `IS NULL`, `NOT EXISTS`, коррелированный подзапрос, материализация.
**У нас образец не встречается — проверено замером в обе стороны**, а не рассуждением.

**Мои две ошибки этого дня, обе — замер не той величины** (`HOW_NOT_TO §3.25`):
«сортировка по отпечатку вдвое дешевле» (мерил вне настоящего запроса) и «условие спасло
807 897 символов на первой базе» (те колонки `navigationLinkUrl` в текст не попадали
никогда). Оба решения остались верными, **обоснования были ложными**.

### Данные закрыты — узкое место переехало в ОТВЕТ

| Проверка | Состояние |
|---|---|
| замер первой базы после последней правки | ✅ 103 808 → 103 808, ошибок 0 |
| воспроизводимость: две пересборки подряд | ✅ 623 565 = 623 565, расхождений **0 / 0 / 0** |
| приёмка 58 вопросов | ⚠ прогнана: из 23 вопросов с эталоном **верно 7**, провалов 15, и **11 из 15 — выбрана не та сущность** |

🔴 **Главное, что осталось: ответ на один и тот же вопрос НЕ ПОВТОРЯЕТСЯ.** Причина найдена
и названа точно — `docs/DEFECT_FOCUS_CHOICE.md §1-бис`: модель извлекает намерение **одним
словом** и выдаёт то «закупки», то «закупка»; от грамматического числа зависит, встанет ли
первым регистр (отказ) или документ (верные 73 181 157,68). Шесть вызовов подряд: четыре
отказа, два верных ответа.

**Способ починки — решение владельца** (меняет поведение продукта: «уточнять чаще» против
«отвечать чаще»). Направления, ни одно не привязано к базе или языку, — в том же файле.

Дальше по очереди:

1. решение владельца по `DEFECT_FOCUS_CHOICE` + независимый взгляд агентов;
2. `kind` расходится с текстом: на вопрос без данных текст честен, а `kind` = `answer`;
3. **прозрачность**: ответ не называет, какую величину сложил (у сущности бывает девять
   величин со словом «сумма»);
4. `docs/BOXED_BLOCKERS.md`: резолвер мёртв на нелатинских базах, словарь `ru_RU.utf8` по
   умолчанию, остаток ручного перечня сущностей.

## Состояние на 29.07 — одной таблицей

**Идёт `docs/PLAN_AUTONOMY.md`: «запустил установщик, подождал — работает».** Указание
владельца: не останавливаемся, пока база не загрузится и не заработает **без единой
ручной правки**; любое вмешательство на поправку = цель не достигнута.

**Девиз владельца (29.07): автономность, универсальность, непривязанность к базе.** Это
правило разрешения споров, а не лозунг — см. `CLAUDE.md` и `PLAN_AUTONOMY §0`.

| Что | Состояние на 29.07 |
|---|---|
| Фаза 3 (отбор под векторизацию) | **сделана.** Разметка сущностей моделью, кэш в `search_entity_class`. [замер] УТ 1 501/1 501, ноль неразобранных; во второй проход уходит 42% строк |
| очерёдность векторов | два прохода через `ROWS_WHERE`; второй **без сужения** — полнота (п. 13) цела |
| рабочие таблицы досчёта | **отдельные по базам** (`emb_<база>_<таблица>`) по указанию владельца; фильтр по базе — второй рубеж |
| замок сборки | имя базы от движка (`current_database()`), а не разбором DSN |
| раскладка кода | `deploy.sh`, на стенде зовётся конвейером (`SERENE_SRC_DIR`) — ручного копирования больше нет |
| `nightly-eval` | **погашен** (евал выведенного слоя braine, слал алерт владельцу с боевого токена) |
| первая база после правок | такт зелёный, **134 с**, 0 строк без вектора |
| вторая база (УТ 11.4.9) | корпус 632 683 строки собран; векторы **ещё не досчитаны** — это следующий шаг |

## Состояние на вечер 28.07 — одной таблицей

| Что | Состояние |
|---|---|
| корпус | **103 808** строк, 231 сущность, вектор 1024, у строки карта всех величин |
| индекс | `search_idx` инвертированный; векторного нет (`ivf` заблокирован) |
| резолвер | **115 151** значение, инкрементальный; подключён к ответу (слово → значение) |
| сборка | целиком в движке, `build.sh`; своего кода нет |
| синк из 1С | **дельта по `DataVersion`** — тянем только изменения; перепись схемы раз в час |
| конвейер | **`1c-serene-pipeline`, по готовности** (синк → сборка → повтор), такт ~6 мин. «Ночного» нет |
| ответы | реранкер выбирает сущность; величину — вопрос; **калькулятор** ставит числа кодом; уточнение → выбор → ответ; при сбое — честный 503; старение видно |
| приёмка | оба набора **8/8** |
| 🟢 таймеры | конвейер **включён** (`OnUnitInactiveSec`). Старые `sync`/`index` отключены |

## Состояние стенда прямо сейчас (29.07, день)

| Что | Состояние |
|---|---|
| конвейер ПЕРВОЙ базы | 🔴 **остановлен намеренно** (`1c-serene-pipeline.timer`). Она проверена — замер в [`REGRESSION_BASE1.md`](../docs/REGRESSION_BASE1.md); её такты отнимали движок у первичной заливки второй. Вернуть: `systemctl start 1c-serene-pipeline.timer` |
| ВТОРАЯ база `ut_test` | пересоздана с нуля, идёт первая полная сборка на 64 потоках |
| шлюз второй базы `:6021` | порт держит осиротевший процесс от 28.07; юнит `1c-odata-gateway@ut` установлен, но занять порт не может. Заменить после замера |
| сервис ответов второй базы `:8099` | тоже осиротевший процесс, юнита нет — Фаза 2 |
| бэкап перед сносом | `/root/backups/serenedb/serenedb-20260729-044937.tar.gz`, 2,6 ГБ |

### ✅ Вторая база собрана полностью (29.07, вечер)

| Что | Значение |
|---|---|
| такт | зелёный, **790 с** полный / **99 с** с пропуском пересборки |
| полнота | сущностей не дошло **0**, строк не дошло **0**, закрыто правами 0 |
| корпус | 632 720 строк, с вектором 427 241 |
| без вектора | 205 477 служебных (по решению) + 2 невозможных — оба числа названы в переписи |
| вопрос-ответ | ⚠ **проверка недействительна** — эталон считался тем же `count(*)`, что и ответ. Три из пяти ответов НЕВЕРНЫ: строка витрины ≠ объект 1С |

### Векторизация второй базы — что настроено (29.07)

| Что | Значение | Почему так |
|---|---|---|
| ключей эмбеддера | 11 в `ALIBABA_API_KEYS` | [замер] потолок ОБЩИЙ ~62 строки/с; больше четырёх смысла нет |
| потоков | **44 (4 на ключ)** | [замер] на 110 — **458 отказов за раунд**, работа уходила в повтор; на 44 — **ноль отказов** и остаток закрыт за раунд |
| пул движка | `cpu_threads=96..160`, `io_threads=8..16` | `ai_embed` занимает исполнителя на всё время сетевого запроса |
| продолжить досчёт | `embed_all.sh` | **не** `build.sh`: пересборка корпуса не нужна |

### 🔴 Открыто: строка корпуса ≠ объект 1С (найдено 29.07 вечером)

Бот отвечает «216 партнёров», а их **164**. Раздуто 85 сущностей, 9 155 лишних строк
корпуса. **План — [`docs/PLAN_ROW_IS_OBJECT.md`](../docs/PLAN_ROW_IS_OBJECT.md)**, прошёл
три независимых разбора (полнота, универсальность, штатные средства) и проверку готовности
коробки.

| Шаг | Что | Состояние |
|---|---|---|
| 1 | загрузчик: реквизит шапки не затирается значением строки | ✅ **сделано**, проверено на двух случаях |
| 2 | корпус: одна строка на объект, колонки только своей сущности | не начат |
| 3 | величины: своё → `any_value`, вложенное → **не кладём вовсе** | не начат |
| 4 | перепись: назвать раздутие числом с причиной | не начат |

🔴 **Шаги 1 и 2 обязаны уехать ОДНИМ коммитом** — порознь возникает состояние, где корпус
свёрнут по отравленной витрине и **больше не пересобирается**, потому что отпечаток кода не
менялся. Остальные условия выкладки — §6-бис плана, их восемь.

**Состояние витрины сейчас:** починка шага 1 применена только к двум сущностям, которые я
перезалил руками. У остальных в витрине по-прежнему подменённые значения (всего 551 на
3 116 объектах) — нужен синк.

## Что делать дальше

**Ведущий документ — [`docs/PLAN_AUTONOMY.md`](../docs/PLAN_AUTONOMY.md), там журнал с
замерами.** Ближайшее:

1. **досчитать векторы на УТ** — первым проходом бизнес-сущности (владелец разрешил
   эмбеддинг). Это первая настоящая проверка очерёдности на объёме;
2. **Фаза 4** — приёмочный набор строится ИЗ БАЗЫ: вопросы и эталонный SQL. Сейчас набор
   написан под наши данные и на чужой базе бессмыслен (п. 15);
3. **Фаза 5** — приёмка агентами без контекста: число верных/неверных/отказов при данных;
4. **Фазы 1-2** — установщики сторон 1С и Ubuntu; **Фаза 6** — цикл до зелёного.

Порядок закрытия контракта — [`docs/TARGET_STATUS.md`](../docs/TARGET_STATUS.md). Из
внешнего по-прежнему ждут:
1. **п. 16** — модели внутрь контура: решение владельца о периметре;
2. **п. 14** — инсталлятор; теперь это не «последним по контракту», а Фазы 1-2 плана
   автономности — без него цель «поставил и работает» недостижима;
3. **UX-хвост**: кнопки уточнения в боте OpenClaw — бэкенд готов (`/ask` принимает
   `focus`, уточнение отдаёт `options`), см. `docs/PRODUCTION_PLAN.md`. Делаем в конце.

**Мелкий известный остаток:** дельта регистров (составной ключ/хеш) — сократит такт с
минут до секунд; сейчас регистры перезаливаются полностью, но грузятся быстро.

## Что нельзя делать (стоило потерянного времени)

- 🔴 **запускать `serene_search_build.py`** — при нынешнем составе сносит таблицу и индекс
  (авария 03:55 28.07). Снят с таймера; юнит `1c-serene-index` теперь на `build.sh`;
- **опыты с `ivf`** на боевом инстансе — роняют движок в петлю OOM;
- `VACUUM (REFRESH_TABLE)` на корпусе — 34 ГБ и смерть движка. Только `REFRESH_INDEX`;
- параллельная запись векторов в ОДНУ таблицу — `SIGSEGV`. Каждый поток в свою;
- **обернуть `corpus_merge` в один `BEGIN…COMMIT`** — `REFRESH_INDEX` внутри транзакции
  тихо не публикует (замер 28.07);
- поднимать пачку эмбеддера выше 10 или писать в неё `NULL` — падение/`SIGSEGV`.

## Решения владельца, принятые 27-28.07

| Решение | Суть |
|---|---|
| п. 20 контракта | запрещено писать свои скрипты для работы с базой |
| п. 21 контракта | **задача проекта — отдать ответ, и правильный.** Порядок: ответ → уточнение → отказ. Отказ при наличии данных — дефект |
| штатное раньше своего | спрашивать `serenedb-native` до того, как писать код, включая чтение `$metadata` |
| никакого хардкода под базу | «базы разные бывают»: понятия «денежная колонка» в коде нет, строка несёт все величины |
| подготовка базы — отдельный шаг | многошаговый разбор, который сообщает и о своей неуверенности |
| коммит и push | после каждого законченного шага, а не после серии |
| это стенд разработки | доступность бота ценой не является; беречь данные и время восстановления |

## Открыто и ждёт слова владельца

- выводить ли `report_1c` из конфига бота (решение принято 26.07, не исполнено);
- строгость гейта: числа словами против ложных отказов (`PRODUCTION_PLAN` п. 2).

---

> # ⬇⬇⬇ НИЖЕ — ИСТОРИЯ (25-28 июля). МОЖЕТ УСТАРЕТЬ.
> Это архив прошлых сессий: видно, как менялись выводы. **Актуальное состояние — в блоке
> «С ЧЕГО НАЧАТЬ» выше и в [`docs/TARGET_STATUS.md`](../docs/TARGET_STATUS.md).** Ниже —
> цифры и планы прошлых дней (корпус 97 965, «ночной синк», «переносим сборку в движок»
> — всё это уже неверно/сделано). Читать только за контекстом, не как текущую задачу.

## Что делаем и зачем

**Переносим сборку поискового корпуса из python-скрипта в штатные средства SereneDB.**

Зачем: такт сборки идёт **5 минут даже когда в данных ничего не изменилось**, и 86 % из
них — фаза, где скрипт вычитывает все 97 965 строк наружу, склеивает текст в Python,
считает там же отпечаток и заливает обратно ~490 отдельными процессами `psql`. Всего
**~1 190 запусков процесса за такт**, и это растёт с размером **всей базы**, а не с
числом изменений.

Из-за этого не выполняется п. 17 `TARGET.md` — свежесть данных не позднее 20 минут.
На базе клиента в разы крупнее такт в 20 минут не уложится, сколько бы ни менялось.

Владелец 27.07 внёс это в контракт **пунктом 20**: запрещено писать свои скрипты для
работы с базой. Данные лежат в SereneDB — работа с ними делается внутри SereneDB.

## Как делаем

**План готов и проверен: [`docs/CHECK2_INCR_SUMMARY_V2.md`](../docs/CHECK2_INCR_SUMMARY_V2.md).**
Пять шагов, у каждого критерий «готово, если» и откат. Исследование прошло полный цикл:
три изолированных агента в два прохода → сводка → три независимых ревизора → версия 2
с их исправлениями.

## ✅ ШАГИ 0 И 1 УЖЕ ВЫПОЛНЕНЫ — не повторять

**[замер, проверено 27.07 дважды]** На сервере в базе уже лежат:

| Таблица | Что это | Строк |
|---|---|---|
| `search_corpus_backup_20260727` | **резервная копия** корпуса, все 8 колонок включая `emb` и `refs` | 97 965, `emb` заполнен у всех |
| `tmp_prod_corpus` | корпус, собранный **новой SQL-формулой** | 97 965 |
| `tmp_refmap` | карта разрешения ссылок | — |

**Главный результат — шаг 1 пройден:**

```
SELECT count(*), count(*) FILTER (WHERE c.doc_hash = t.doc_hash),
       count(*) FILTER (WHERE c.doc_hash <> t.doc_hash)
FROM search_corpus c JOIN tmp_prod_corpus t USING (src_table, row_key);
→ 97965 | 97965 | 0
```

**SQL-формула воспроизводит боевой отпечаток дословно на всех 97 965 строках,
расхождений ноль.** Значит:
- разовый пересчёт эмбеддингов **НЕ требуется**;
- состав полей у новой формулы совпадает со старой;
- переход проверен на всей базе, а не на выборке.

🔴 **Эти таблицы — не мусор, не удалять.** `tmp_prod_corpus` — доказательство, что
переход безопасен; `search_corpus_backup_20260727` — единственная настоящая копия
(в `search_corpus_bak` нет колонки `refs`).

**Значит следующее действие — Шаг 3 плана:** замер новой сборки на копии, затем
переключение сборщика (Шаг 4). Шаги 0 и 1 пропустить.

## Что установлено твёрдо (не перепроверять)

- **Путь «корпус = представление» ЗАКРЫТ.** Индекс поверх `VIEW` заморожен на момент
  создания — подтвердили два агента и два ревизора независимо. Корпус остаётся таблицей;
  оптимизируется сборка, а не хранение.
- **Механизм сборки текста — `UNPIVOT` + `COLUMNS(*)`.** Выиграл у всех трёх ревизоров
  по скорости и единственный обращается с пустыми значениями так же, как боевой код
  (выбрасывает пару, а не пишет литерал `null`).
- **`sha1()` движка побайтово совпадает с `hashlib.sha1`** — проверено тремя ревизорами
  на кириллице.
- **`pg_class.reltuples` как детектор изменений негоден:** правка поля не меняет
  `count(*)`. Точный счёт по всем таблицам стоит 0,107 с — экономить не на чем.
- **Журнала изменений в движке нет** — ни CDC, ни версий строк. Только сравнение
  отпечатков.

## 🔴 Четыре ограничения — нарушить каждое значит испортить данные МОЛЧА

1. **Ссылка определяется по ФОРМЕ ЗНАЧЕНИЯ, а не по имени колонки.** GUID лежат в 88
   колонках с суффиксом `_Key` **и в 26 без него, в 34 таблицах — включая `Контрагент`**.
   Критерий по имени потерял бы разрешение контрагента: поиск по «Ромашка» перестал бы
   находить документы, и **без единой ошибки**.
2. **`doc` и `refs` — разные поля**, отпечаток от обоих. Две из трёх найденных формул их
   сливают, это меняет структуру корпуса.
3. **Тождество строки — объявленный ключ, а не `rowid`.** `rowid` двигается при
   удалении и вставке.
4. **В `MERGE` обязано остаться `t.doc_hash <> s.doc_hash`.** Без него молчаливо
   пересчитываются все 98 тысяч эмбеддингов.

Плюс: **никакого `VACUUM (REFRESH_TABLE)`** на корпусе — на таблице с вектором это дало
34 ГБ и смерть движка (записано в `serene_search_build.py:911-915`). Только
`REFRESH_INDEX`.

## Почему предыдущая сессия остановилась

Не из-за ошибок в работе. В переписку попали ключи S3, и автоматическая защита начала
резать **любую команду, которая что-то меняет** — при работающем чтении. До базы
добраться стало невозможно. Урок записан: секреты кладём в файл на сервере первым же
действием, в командную строку не подставляем никогда.

## Что уже сделано и работает (не переделывать)

- **Родные функции движка вместо чужих** (коммит `01adfee`): `array_cosine_similarity`
  из ядра DuckDB заменён на оператор `<=>`. Замер 583-627 → 435-451 мс, выдача совпала
  40 из 40, приёмка 8 из 8.
- **Индекс не пересобирается каждый такт** (коммит `64e5582`): фаза индекса
  12,64 → 0,04 с. Отпечаток желаемого DDL хранится в таблице `search_meta`, потому что
  каталог движка состав индекса не хранит (`duckdb_indexes().sql` отдаёт пустые скобки).

---

## Где мы сейчас (27.07)

Золотой набор **8/8**, среднее 4,8 с. Утром 26.07 было 6/8 при 5,14 с — и на корпусе,
где организация во всех документах называлась настройкой НДФЛ, а треть денег по строкам
накладных была потеряна дедупом. Сегодняшнее 8/8 — на данных, сверенных с 1С строка в
строку по всем сущностям: `1С == витрина == корпус == индекс = 97 965`, расхождений
с `$count` ноль.

## Решения владельца — не пересматривать без него

| Решение | Дата | Суть |
|---|---|---|
| Данные в PG на Windows | 26.07 | сырые таблицы 1С живут в PostgreSQL рядом с 1С; SereneDB держит только корпус и индекс через `postgres_attach` + VIEW. **В коде пока НЕ реализовано** |
| NL→SQL отменён | 26.07 | `report_1c` из контура убрать; модель не получает схему НИКОГДА. График строится ПОСЛЕ ответа: код дописывает группировку к уже известному отбору |
| Штатное вместо своего | 27.07 | обычный SQL и приёмы PostgreSQL **не считаются** использованием SereneDB. Своё — только после ответа агента `serenedb-native`, что штатного нет |

## Открытый вопрос номер один: вектор

**Причина 30 ГБ НЕ найдена.** Замер на живой сборке, настройки по умолчанию:

```
 5 000 строк x FLOAT[1536] -> CREATE INDEX за 1 с,   0 МБ
 6 000 строк               -> не завершилось,   +4 473 МБ
10 000 строк               -> не завершилось,  +12 494 МБ
```

Переход **скачкообразный** между 5 и 6 тысячами. `sdb_ivf_posting_size` его не двигает
(проверено 128/256/1024). Три гипотезы проверены по исходникам и **отвергнуты**: размер
обучающей выборки, `kMaxTrainSample`, переключение алгоритма кластеризации.

**Ответ разработчиков получен 27.07 и он решает половину задачи.** У них замер на
1,5 млн строк × 1536: грузили **сразу с индексом**, а не строили индекс после;
~400 строк/с, индекс ~15 ГБ. Проверили у себя — **вставка в готовый индекс работает**:
98 тыс. строк вошли при 5-10 тыс. строк/с. Ломается только `CREATE INDEX` поверх
заполненной таблицы.

Осталось: публикация (`VACUUM REFRESH_TABLE`) на 98 тыс. строк одним куском повисла и
съела 34 ГБ. Гипотеза — публиковать порциями. Подробности: `docs/VECTOR_DECISION.md` §9.

⚠ **Сборка `ivf` НЕ ОТМЕНЯЕТСЯ**: `pg_cancel_backend` возвращает успех, работа
продолжается; `SET memory_limit` не помогает. Каждый неудачный опыт = рестарт движка.

🔴 **Решение владельца 27.07: «твои скрипты не нужны, а из-за них вся беда».**
Восемь дефектов проекта — одна причина: данные в SereneDB, работа с ними в Python через
подпроцесс `psql`. Целевое: своим кодом остаётся поход в 1С за метаданными и решения,
требующие языковой модели; всё остальное — штатными средствами внутри базы.
Перечень дефектов и разбор — `docs/HOW_NOT_TO.md §0`. Правило продублировано в
`CLAUDE.md`, который читается автоматически каждую сессию.

🔴 **`ai_embed` был отклонён МНОЙ ошибочно** (исправлено 27.07): у него нет параметра
`dimensions` — это верно, но вывод «значит нельзя» неверен. Функция возвращает `FLOAT[]`
без фиксированной длины, размерность задаётся моделью, колонка объявляется под неё.
Блокер был круговым: «у нас 1536» — а 1536 выбрано по образцу braine, не замером.
Разбор — `VECTOR_DECISION.md §6`, урок — `HOW_NOT_TO.md §1.5`.

🔴 **27.07, вектор — главное:** в ответе разработчиков я пропустил вторую цифру,
**400 строк/с**. Наша вставка шла 5-10 тыс./с, то есть в 12-25 раз быстрее — значит она
работу не делает, а откладывает. Проверено: одна большая транзакция на 98 тыс. строк даёт
линейный рост 25 МБ/с без полки (13,5 → 23,9 ГБ), `count(*)` всё это время 0, а
`segment_docs_max = 5000` рост НЕ ограничивает. `VACUUM (REFRESH_*)` — это публикация, и
вызывая его на всю таблицу разом, я отбирал у движка работу, которую его фоновый цикл
(`refresh_interval`, 1000 мс) делает порциями каждую секунду. Разработчики `VACUUM` не
упоминали вовсе. **Итог дня по вектору: стена около 6 000 строк при 1536 измерениях реальна и
воспроизводима.** Отвергнуты замером все четыре гипотезы обхода — порядок сборки,
размер транзакции, `segment_docs_max`, отказ от ручного `VACUUM`. Порции по 2 000: три
прошли за секунду при 0,9 ГБ, четвёртая встала на 6 000 строк с 22,8 ГБ. Разработчики
стену прошли на 1,5 млн строк, значит различие в условиях индекса (метрика? `quant`?),
а не в объёме. Минимальный воспроизводящий пример у нас стабилен: две колонки,
`VARCHAR` + `FLOAT[1536]`, `metric='cosine'`, умолчания.
Подробности и таблицы замеров — `docs/VECTOR_DECISION.md §10`.

⚠ **27.07: движок был поднят из петли OOM-рестартов.** Мои опытные таблицы `_p`/`_inc`
с живым `ivf`-индексом остались в WAL (178 МБ). Движок проигрывал их при каждом старте,
рос до OOM, перезапускался и начинал заново — три часа, сам бы не вышел. Снял копию
`store.db` + WAL в `/var/lib/serenedb-rescue-20260727/`, отвёл WAL — поднялся за 10 с.
Проверено после: корпус 97 965 (все с эмбеддингом), 226 сущностей, резолвер 60 552,
`search_idx` на месте, мусорных таблиц нет, сервис отвечает верными числами.
**Вывод, который нужно помнить:** опыты с `ivf` на боевом инстансе оставляют мину в WAL.
Ставить их на отдельном каталоге данных.

⚠ **HNSW удалён из продукта**, а не «не собран у нас». Демки `demo4`/`demo5` на боевых
размерностях не запустятся ни у нас, ни на их `main`.

Сейчас смысловой поиск идёт **полным перебором**: 314 мс на 98 тыс. строк, recall 1.0.
Это рабочий вариант.

## Что известно про наш процесс

**Три независимых прохода дают разное** — проверено: шесть агентов с одинаковым
заданием, но независимо. Один каталог рекомендовал правку, которую другой агент
опроверг. Один ревизор нашёл ошибку, которую двое пропустили.

**Замер важнее чтения кода.** Дефект «модель видит только подсветку, а не строку» (ИНН
у 12 контрагентов из 13 не доходил) нашёлся только живым вопросом — по коду всё
выглядело правильно.

**Мои систематические ошибки этой сессии**, чтобы не повторять: выдавал вывод за факт
и канонизировал одну сторону противоречия; вставлял в цитату кода собственные
пояснения, обрезав настоящий комментарий, противоречивший выводу; трижды наступил на
самопоиск `pgrep -f`; написал хук так, что он обрывал работу на файлах, которых
не касается.

---

_Ниже — архив от 2026-07-24 — стек доведён до продакшен-уровня (zero hardcode) + документация сквозная_

## 🔎 2026-07-25 — SereneDB изучен по исходникам (важные исправления)
Клон `github.com/serenedb/serenedb` + опрос живого инстанса. SereneDB = «Elasticsearch-поиск +
ClickHouse-аналитика в одной Postgres-совместимой БД», поиск на собственном движке **IResearch**.
- ❗ **Дважды уточнено (проверено прогоном):** векторный индекс ЕСТЬ, но в НАШЕЙ сборке 26.07.3 опкласс —
  **`ivf`** (`USING inverted(emb ivf (metric='cosine'))`); `hnsw` **удалён из продукта** и в main его нет
  (движок отвечает `Unknown built-in opclass 'hnsw' … (known: included, ivf)`). Прежняя запись «векторного
  индекса нет вообще» тоже была неверна.
- **Полнотекст/BM25 на кириллице проверен:** `ts_phrase('КАЗАНЬ')`=37 (= нашему LIKE), BM25 ранжирует верно.
- **«Данные в Postgres, поиск от Serene» РАБОТАЕТ** — но индекс строится на **VIEW** над attach-нутой
  таблицей, а не на самой таблице (на таблице DDL уходит в PG как btree). Проверено: count/BM25/
  проекция и агрегация по `INCLUDE`-колонкам — из индекса; выборка НЕ-`INCLUDE` колонок пока не
  поддержана (`postgres_scan` — TODO в fast-path реестре). Правило: всё нужное — в индекс или `INCLUDE`.
- **Свежесть:** индекс над PG — СНИМОК, изменения источника не видит; но пересборка 50 тыс. строк —
  **0.4-0.5 с**, значит свежесть = частота пересборки. Живое чтение VIEW всегда актуально.
  Гибрид (BM25 + вектор `ivf`) над PG-view работает: 457 мс на 50 тыс., запросы 3-4 мс.
  Вектор требует фиксированного `FLOAT[N]` — приводить типом в самом VIEW.
- **`ai_embed` работает**, но отдаёт 1024 (нет параметра dimensions) против наших 1536.
- **`ai_embed(text, model, provider)`** — эмбеддинг прямо в SQL, в любой OpenAI-совместимый эндпоинт
  (наш DashScope такой же) → можно направить на **локальный** и не выпускать значения из периметра.
- **`postgres_attach`/`postgres_query`/`postgres_scan_pushdown`** вкомпилированы статически: данные могут
  жить в Postgres, а поиск/аналитику даёт Serene — без копии (то, что вспомнил владелец).
- **Zero-ETL** по Parquet/Iceberg/CSV/JSON на S3/HDFS с BM25 и векторами; заявлены column-wise real-time
  updates (в тему требования «данные всегда свежие»).
- Наш `resolver_index.emb` уже `FLOAT[1536]` — под `ivf` подходит без изменения схемы. Детали и что мерить —
  `docs/SERENEDB.md` §«Возможности движка».

## 🧭 Факт стека на 2026-07-25 (перепроверено вживую)
Активны и enabled: `1c-odata-gateway`(:6011), `1c-config-ui`(:6012), `1c-mcp-braine`(:6014 → ask_1c),
`1c-mcp-reports`(:6015 → report_1c), `serenedb`(:7890), `open-webui`(:3000), `oikb`(:8081),
`rerank-shim`(:8082), `api`(:8090 braine /ask), `kb-poll`, `postgresql`(:5432); `tg-bridge` — disabled.
Таймеры: `1c-serene-pipeline` (по готовности, каждую минуту), `1c-bot-monitor` каждые 3 мин.
`nightly-eval` **погашен 29.07** (евал выведенного слоя braine, алерт владельцу с боевого
токена); `1c-etl`, `1c-serene-sync` — остатки прежней схемы.
Бот — OpenClaw (user-юнит `undebot`, :18800, linger). Витрина: 12 catalog-таблиц + `resolver_index`.
Эмбеддинги — DashScope `compatible-mode/v1` (**облако**, это и есть текущая точка утечки, см.
`docs/ANONYMIZATION.md`). ОБЕ выгрузки (ETL→KB для braine и serene_sync→витрина) идут из ОДНОГО
read-only OData-шлюза.

---

## 🆕 2026-07-24 — продакшен-доводка (zero hardcode) + сквозные доки
Директива владельца: «никакого хардкода, исправить всё до уровня продакшена». **Все кодовые фазы (1–6)
`docs/PRODUCTION_PLAN.md` закрыты и задеплоены:**
- Валидатор SQL → **ALLOW-LIST через AST** (`json_serialize_sql`; таблицы обязаны быть в схеме, табл.функции/
  объекты-вне-схемы reject by construction; денайлисты — fallback). + grounding-критик метрики.
- **Резолвер де-хардкоден** (убраны `RU_STOP`/`_stem` — язык-хардкод; слова `\w{3,}`, jaro + Qwen-семантика).
- `is_folder` — **общее правило загрузчика** (не per-entity). Bespoke `banks`/`rebuild_banks` **ретайрнут** →
  классификатор через generic-синк как `catalog_классификаторбанков` (Казань=37 живьём).
- `serene_select.py` — дискавери сущностей из живого OData + **picker** (`--review`); идемпотентный `setup.sh`
  (роли+генер-пароли); тесты динамические (ноль magic-чисел); промт `gen_sql` язык-нейтральный.
- ⚠ Движковый FS-лимит per-role на SereneDB **недостижим** (один DuckDB-инстанс, GLOBAL-only) → защита = валидатор.
  Вопрос фаундерам. Правило: [[feedback_prompt_rules_dont_work]].

**Документация сведена сквозной** (владелец: «полный стек от 1С на Винде до всей Ubuntu, не забыть ни 1 детали»):
`RUNBOOK_DEPLOY.md` теперь §0 карта сервисов/портов + §10 SereneDB (деплой+новая база) + **§11 OpenClaw бот-слой
командами** (всё сверено с ЖИВЫМ деплоем .42, не по памяти). `OPENCLAW_BOT.md` переписан начисто (дневник→git).
Новый `ubuntu/openclaw/README.md`. Вычищено протухшее (banks в примерах, tg-bridge, research-баннер, memory_bank).

**Единственный остаток — НЕ код (Фаза 7, гейт продакшена):** реальные данные в 1С + публикация
`AccumulationRegister` в OData (сторона владельца). Агент-харнесс готов прогнать обороты, как появятся данные.

---

## 🔴 НОВЫЙ ФОКУС: отчёты/аналитика/дашборды на SereneDB (см. `docs/SERENEDB.md`)
Решение владельца (2026-07-23, знает фаундеров лично): строим витрину/аналитику на **SereneDB**
(Apache-2.0 self-host, Postgres-протокол; полнотекст+вектор+колоночный OLAP в одном движке).
Зачем: RAG хорош для «найди по смыслу», но для точных отчётов/агрегаций/графиков нужен structured-
путь по ВСЕМ строкам — это SereneDB. Умная система, без хардкода отчётов: NL→намерение (LLM)→код
строит запрос→числа из реальных данных→график. Риск молодой БД ограничен: витрина **производная от
1С**, пересобирается (источник истины — 1С read-only).
**Статус:** ✅ SereneDB на .42 (systemd, loopback:7890, enabled, PG18-wire). ✅ Этап 1 (реальный срез
банков 2779 → замер: пул 1.1с/загрузка 235мс/агрегация мс). ✅ Этап 2 ядро — умный NL→SQL без хардкода
(`ubuntu/serenedb/serene_report.py`: схема+примеры→LLM→read-only SELECT→валидатор→SereneDB→таблица+SQL).
✅ **`report_1c` ЖИВОЙ в Telegram**: MCP-сервер `1c-mcp-reports` (:6015), бот маршрутизирует
аналитика→report_1c / факт→ask_1c, гейт заземляет числа обоих. Проверено: «топ-5 городов по банкам» → верная таблица.
✅ **График картинкой в Telegram** (`sendPhoto`; PNG в workspace = разрешённая дир, CHART_DIR env).
✅ **Анти-слив КОДОМ** (`stripInternal` в `message_sending` + `reply_payload_sending` для caption).
✅ **Read-only роль** `serene_ro` (бот-отчёты; БД физически не даёт писать; get_schema через duckdb_columns).
✅ **Мониторинг «бот жив»** (systemd-таймер 3 мин → прямой Telegram-алерт владельцу при падении сервисов).
✅ **Нативные правила money/opwnclaw-bot применены** (персона+tools.allow, QA-батарея). Мета/анти-ложь —
на персоне (промт), кодового гейта нет (открытое поведение). Правило: [[feedback_prompt_rules_dont_work]].
**Сделано с тех пор (2026-07-24):** семантический резолвер (Qwen, `resolver_index`) + штатный ночной синк
(`1c-serene-sync.timer` 03:40) ГОТОВЫ. **Дальше (не код):** backup/restore (тест+фаундеры); ротация
bot-токена перед реальным продом; Фаза 7 (реальные данные/регистры). RAG braine — второй инструмент.

## ФОКУС (бот-слой готов): OpenClaw поверх braine (см. `docs/OPENCLAW_BOT.md`)
У владельца УЖЕ работает бот-менеджер на OpenClaw (репо `money/opwnclaw-bot`, склонирован `/opt/openclaw`); отвечает «сухо». Надо, чтобы он **черпал факты из нашего braine**. OpenClaw = надстройка тона, НЕ замена: человек→OpenClaw→наш braine→OpenClaw оживил→клиент.

🔴🔴 **ПРАВИЛА (первые при работе над бот-слоем):** (1) только НАТИВНЫЙ OpenClaw, кастом запрещён без явного согласия владельца; (2) документацию смотреть в склонированных на LXC репо (`/opt/openclaw` + движок `/opt/openclaw-engine` из github.com/openclaw/openclaw), НЕ угадывать.

**Нативный путь (docs движка):** OpenClaw — нативный MCP-клиент (`openclaw mcp add`, streamable-http). Anti-hallucination: владелец одобрил **Вариант А — плагин-verify** (хук `message_sending` сверяет факты ответа с выводом braine, режет несверенное — кодом, не промтом; промтом ЗАПРЕЩЕНО).

**Сборка (артефакты `ubuntu/openclaw/`):**
- ✅ Компонент 1 — MCP-сервер `ask_1c` над braine (mcp_braine.py, FastMCP streamable-http). Сервис **1c-mcp-braine** :6014 (enabled). Проверено: вернул контрагентов с ИНН+цитатой.
- ✅ Компонент 2 — движок OpenClaw (npm 2026.7.1-2). Инстанс под юзером `undebot` (`~/.openclaw/`, DeepSeek provider-плагин), gateway = systemd user-юнит (enabled --now). Турн через gateway проверен.
- ✅ Компонент 3 — verify-плагин (`ubuntu/openclaw/verify-plugin/`, нативный `definePluginEntry`+хуки). Установлен, enabled, хуки СРАБАТЫВАЮТ, эталон braine захватывается (refDigits>0). 36 оффлайн-юнитов зелёные (заземляет ask_1c+report_1c). Живой гейт `message_sending` проверен на реальной Telegram-доставке (обоснованное число → allow; выдуманное → replace/cancel).
- ✅ Компонент 4 — `mcp add second-brain` (:6014, include ask_1c) + персона AGENTS.md (тон+когда звать; НЕ анти-галл). Бот зовёт инструмент, отвечает данными 1С живым тоном.
- ✅ Компонент 5 — ЖИВОЙ гейт на Telegram. Владелец выбрал «забрать `@test1c_mcp_bot`»: OpenClaw = фронт Telegram, braine `tg-bridge` stop+disable, `dmPolicy allowlist` только владелец (5949699699), токен в tokenFile. braine `/ask` кормит ask_1c. Проверено на доставке: обоснованный ИНН → allow; braine noData + бот назвал число → **replace на «нет данных»** (галлюцинация отрезана кодом).
Топология: демо на нашем `.42`; их прод-бот `.15` не трогали.
🔬 Живые гочи (OPENCLAW_BOT.md §Живые находки): allowConversationAccess для хуков; MCP-имя `second-brain__ask_1c` (суффикс); config через api.pluginConfig; ключ провайдера в auth-store; **корреляция message_sending по sessionKey, НЕ runId, эталон не удалять на agent_end** (иначе ложный cancel).
🔒 Эталон `money/opwnclaw-bot` (`/opt/openclaw`) + движок (`/opt/openclaw-engine`) — READ-ONLY, только читаю; к проду `.15` не подключался. Проверено 2026-07-23 (git clean, HEAD==origin).

---

## Статус: ✅ ГОТОВО end-to-end (второй мозг)
Спросил бота в Telegram (`@test1c_mcp_bot`) → он отвечает по реальным данным 1С с цитатами. Вся цепочка сцеплена и переживает ребут без ручных действий.

```
1С (Windows, файловая база buh_test)
  └─ IIS (служба, авто-старт) → штатный OData (read-only ai_reader)
      └─ роутер 192.168.56.1 проброс → IIS:80
          └─ LXC: OData-шлюз :6011 (только GET, писать нельзя)
              └─ ETL (ubuntu/1c-etl) → md-таблицы → KB-репо money/1c-test (GitLab)
                  └─ oikb/kb-poll → OWUI/pgvector (эмбеддинги Qwen text-embedding-v4 @ 1536)
                      └─ api :8090 (POST /ask) → ответы с цитатами (DeepSeek, гейты)
                          └─ фронт Telegram = OpenClaw @test1c_mcp_bot (tg-bridge disabled); + SereneDB-аналитика
```
_(Это исходная схема braine; актуальная полная картина со всеми слоями — `docs/ARCHITECTURE.md`.)_

Эталон раскатки на новом проекте: **`docs/RUNBOOK_DEPLOY.md`** (переписан под OData).

## Прод-канал = OData на IIS (НЕ встроенный тулкит)
Тулкит отвергнут для прода: обслуживает всё через 1 клиентский idle-обработчик 1С (~1 req/s, встаёт на модалке — `docs/TOOLKIT_TRANSPORT_ROOTCAUSE.md`); тумблеры не блокируют execute_code. OData обслуживает IIS-служба: многопоточно, авто-старт, без модалок. Тулкит остался dev-опцией (`ubuntu/1c-gateway/gateway.py`, `windows/fork/`).

## Read-only — два слоя (проверено с LXC)
1. `ai_reader` (пароль qwaszx) — читает 200, пишет 500 (нет прав).
2. OData-шлюз `odata_gateway.py` — GET проходит, POST/PATCH/PUT/DELETE → 405 до 1С.

## Проверено end-to-end (2026-07-22)
- OData читает как служба (Организации→«Наша организация»), зависаний нет.
- ETL: 19 сущностей/44 записи через шлюз → push в KB → oikb «Synced 20 added» → индексация.
- Бот: «Каких контрагентов знаешь?» → «МИ ФНС России по управлению долгом (ИНН 7727406020), Казначейство России» с цитатами.
- Zero-touch: W3SVC Automatic; все LXC-сервисы enabled (postgresql/open-webui/oikb/rerank-shim/tg-bridge/api/kb-poll/1c-odata-gateway + таймеры 1c-etl; nightly-eval погашен 29.07). gsheets-sync замаскирован. Старый MCP 1c-gateway disabled.

## Перепроверка (2026-07-23) ✅
Сверено git↔LXC↔живое: все 9 сервисов + 2 таймера enabled/active; порты 6011/6012/3000/8081/8082/8090/5432 слушают; OData-шлюз чтение=1/запись=405; config-ui 116 чекбоксов; Windows IIS Automatic/Running; бот отвечает; ссылки в доках все рабочие. **Найден и починен рассинхрон:** `oc_etl.py` на LXC был старее git (без чтения галочек, в авто-режиме → ночью тянул бы мусор) — передеплоен (md5=git). Поставлен дефолт-выбор `/etc/1c-etl-selected.txt` (23 бизнес-сущности Бухгалтерии) — ночной ETL теперь тянет только их; владелец может переотметить через config-ui `:6012`.

## Конфиг-нейтральность (сделано 2026-07-23) — копипаст на любой бизнес
- **ETL без хардкод-списка:** что тянуть — из `ETL_INCLUDE` → выбора UI (`/etc/1c-etl-selected.txt`) → авто из OData. Резолв ссылок guid→наименование + чистка полей/дат/пустых колонок — универсальны.
- **config-ui `:6012`** (`1c-config-ui`, enabled): веб-галочки «что тянуть» — список всех непустых сущностей 1С (116 на стенде) с числом записей, человек отмечает бизнес-разделы. discovery параллельный (`oc_discover.py`). Кэш `/var/lib/1c-config-ui/entities.json`.
- Модель: код универсален, «что важно» — 5-мин конфиг под бизнес (галочки).
- **Документация приведена в порядок (2026-07-23):** `README.md` (точка входа) + `docs/ARCHITECTURE.md` (полная картина) новые; PLAN/PHASE2 помечены как история; memory_bank актуализирован под OData.

## Тюнинг прода (НЕ хвосты MVP — работает; улучшать на реальной ERP)
- Регистры (остатки/обороты) в состав OData + `TOP_PREFIXES` ETL.
- Инкремент документов по дате (сейчас полная перевыгрузка; идемпотентно).
- Состав OData сузить с «все 1128» до нужных разделов.
- Каветат ИТС/легальность Бухгалтерии Проф — на реальной базе с подпиской неактуально.
- Данных в чистой тестовой базе мало — на клиентской ERP наполнится.

## Ключевые доступы (пароли НЕ в git)
KB-репо money/1c-test (GitLab id 95); бот @test1c_mcp_bot (`credentials/telegram-1c-bot.env`); admin/ai_reader базы (пароли у владельца; ai_reader в `/etc/1c-odata-gateway.env` на LXC); ключи DeepSeek/Alibaba/GitLab в `credentials/`.
