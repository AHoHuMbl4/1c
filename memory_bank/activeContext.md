# Active Context

_Обновлено: **2026-08-18.** Здесь — только живое: текущее состояние и «С ЧЕГО НАЧАТЬ».
История по дням — в [`progress.md`](progress.md); стадии по контракту — в
[`docs/TARGET_STATUS.md`](../docs/TARGET_STATUS.md)._

---

# ⏭ С ЧЕГО НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

🔴 **okna веб-фронт ЗАКРЫТ `[18.08]`**: memory-core web-профиль → gpu-erw embed
(255/255 индекс, memory-error 0); STT webui.db → gpu-erw:8006/v1 (whisper 200).
Скрипты `work/acceptance/okna-web-*-fix.sh`; код `setup-okna-backend-web.sh`.
Дозор **LOCK_OK** 35/35, memory_errors=0 (06:37–07:12 +03).

🔴 **Сквозной rid + TRACE в git `[18.08]`**: `TRACE <rid> <слой> <шаг> <мс> <статус>`
— verify→мост→`/ask`→`ask_journal.rid`; `ASK_TRACE=1` по умолчанию;
`work/acceptance/trace_rid.sh <rid>` (парсер: время — голое целое+ms, токены/строки —
`in=`/`строк=` без ms; `--selftest` на `f9859de24c71bdd2`). Оффлайн **test_trace_rid 10/10**, прежние замки
зелёные; **test-verify 106**, **mcp_ask 32**. **Выкат не делали** — md5/живой
полный путь через journald назначает оркестратор; нужен `ask_journal_apply` (колонка
`rid`).

🔴 **Память шаг 6: DDL okna + apply за флагом `[18.08]`**: на okna таблицы
`ask_journal`/`ask_choice_memory` (postgres :7890); живой `:8091` journal +1,
remember stored, LOST=0. Код: `ASK_MEMORY_APPLY=0` shadow; `=1` — «помню: …»,
коллизия→clarify. Оффлайн **56/56**; md5 `617a0d02…`/`654847ad…`. Prod apply
**не включали** — решение оркестратора после отчёта.

🔴 **okna B7 приёмка ЗАКРЫТА 0/6 полных `[18.08]`**: корпус **1 227 574** ✓,
coverage_gap **0**; `/ask` `:8091` — **0/6** полных golden, **1/6** частично
(B7-01: 79 752 611,64 из трёх B-пар; утренний замер 18.08 давал три пары).
Отчёт [`docs/ACCEPTANCE_OKNA_2026-08-18.md`](../docs/ACCEPTANCE_OKNA_2026-08-18.md),
прогон `work/acceptance/runs/2026-08-18-okna-b7-*`. Блокер — step4/развилки на
боевом, не корпус.

🔴 **F2 p_doc_plain — код в git, ожидает выката на klient-1 `[18.08]`**: plain-путь
зеркалит §3.77 (порядковый `#N` при дубле ключа); тест **8/8**; md5
`corpus_build.sql` **`046d91f5559bdcc4f89801d755ae22d1`**; артефакт
[`work/packet/rollout-f2-plain-key.md`](../work/packet/rollout-f2-plain-key.md).
klient-1 `/opt` всё ещё **`43050f46`** — выкат одним файлом, затем A2 (слияние:
**37 522 → 0** дублей).

🔴 **okna опись полноты B3 ЗАКРЫТА `[18.08]`**:
[`docs/COMPLETENESS_OKNA.md`](../docs/COMPLETENESS_OKNA.md). Баланс **819 = 369 +
35 + 415**; реальная потеря **35** (все `no_read_right`, компенсации регистрами
**0**). `repair-key-dedup.py okna-1` — потерь нет. Дальше B4/B6 — шаг 14 на
Windows при визите владельца.

🔴 **okna словарь развилок ЗАКРЫТ `[18.08]`**: `branch_alias.sh` **190/190**
классов, **10 475** подписей, **пустышек 0**. Транспорт `BRANCH_ALIAS_INFER=local`
(причина: `--gateway` = RPC **120 с**, chunk 40 не влезает). Дым `--local` chunk 40
**~62 с**. Живой B `:8091` «на какую сумму мы продали» **8.8 с**, три пары
**1 572 493,22 / 79 752 611,64 / 79 925 955,81** — SQL `sum(Всего)` до копейки.
Код прогона git **`96c756b`**. Прогон на okna не крутится.

🔴 **klient-1 сборка — чанкованный p_doc в git, выкат ждёт `[18.08]`**: OOM на
`accumulationregister_себестоимостьтоваров` (#108/1457) закрывается порциями в
`corpus_build.sql` §5-бис: **chunk_rows≈60 625** при `memory_limit=10500MB`
(формула: `memory_limit×0,25 / (2GiB/50000)`); **11** порций вместо одного OOM.
md5 **`b838cd145238907a0901ebc2fe1066dd`**. Identity-тест ut_test **не закрыт**
(dev WAL invalidation после проб). **Выкат** на klient-1 (бэкап `/opt`, ручной
такт без stop apply, 3 зелёных такта, pipeline.timer) — оркестратор/SSH владельца.
Прежний снимок: **106/1457**, `search_corpus=0`, пик **11.29 GiB**.

🔴 **Полноту данных ведёт сессия-оркестратор `[17.08]`**: очередь шагов,
поправки к планам и критерий финиша — [`docs/PLAN_ORCHESTRATOR.md`](../docs/PLAN_ORCHESTRATOR.md).
Исполнительным окнам — промты от оркестратора; серверное состояние меняет
владелец по его командам.

🔴 **A1 опись полноты klient-1 ЗАКРЫТА `[17.08]`**:
[`docs/COMPLETENESS_KLIENT1.md`](../docs/COMPLETENESS_KLIENT1.md). Баланс
9151 = 1519 + 6298 + 1372 − 38. Реальная потеря 6257 (перекрыто 38 дочитанных
+ 3 сестры RecordType). Закрытые домены 35 типов / 3 021 572. Дальше A2 —
слово владельца (корпус+векторы). **A2 пробовали 17.08 вечер:** precheck жив,
p_doc не влезает в 9.3 GiB при threads=4 — таймер не оставлен.

🔴 **личность канала → /ask.user `[17.08]`**: плагин 1.1.6 кладёт
`telegram:<from.id>` (штатный `ctx.senderId`) или `session:<sessionKey>`
(WebUI: логин не шлёт) в `ask_1c.user`. Фразы «запомни/забудь» сняты — только
поле `memory`. Shadow, применение не включалось. Живой `:8097` журнал: три
разных `user_hash`; SQL памяти telegram:111 ≁ 222. md5 `serene_ask.py`
`a48d449d…` (не менялся). `/opt` не трогали. Выкат назначает оркестратор.

🔴 **Шаг 6 памяти ЗАКРЫТ shadow `[17.08]`**: таблица `ask_choice_memory`
(не corpus_init); клик не пишет; «запомни так» пишет; повтор — `diag.memory`
«память применилась бы», kind не меняется; «забудь» снимает; коллизия
`n_branches=2`. Живой `:8097` репо-код, md5 `a48d449d…`. В бой не включать
без решения после замера. `/opt` не трогали.

🔴 **FP8 Qwen3.8-27B ПРИНЯТ `[17.08]`**: веса+KV FP8, цель 48 ГБ VRAM,
инстанс `a1f19thc-8000` (max-model-len 16384, max-num-seqs 8, qwen3_xml).
Тот же `:8098` ut_test, что BF16. step4 **0/44 неверных**, доля **18/44 (41%)**
= BF16 поимённо (0 расхождений из 44). Ворота 50/50 JSON, det ×5 стабилен.
seq p50/p90 48.3/66.9 с; par4 48.3/77.4 с. Следы `runs/qwen-*-fp8-20260817.*`.

🔴 **ложная пара регистра ЗАМКНУТА в git `[17.08]`**: корень —
`_fork_headline_measure` точное `Сумма`≡0 при SQL Всего 79 925 955,81
(count 77 381). `/ask` `:8091` `/opt` `8539aec6…` ещё 0. Проба нового кода
vs DSN: 1 572 493,22 / 79 752 611,64 / 79 925 955,81, нулей нет.
md5 `6ffa35a8…`. **Выкат назначает оркестратор.**

🔴 **okna web-бот на vLLM — СТОП, парсер жив `[17.08 вечер]`**: повторный
перевод `:18801` на `vllm/Qwen3.8-27B` (thinking=off, ctx 16384, UA, ключ в
env юнита до рестарта). Прямой vLLM `tool_calls`→`ask_1c` HTTP 200. Живой
чат: Q0 ask_1c звался, ответа 349 нет; Q1 данные есть — «недоступны»; Q2
без инструмента «0 шт». Откат из
`/home/undebot/.openclaw-web/openclaw.json.bak-qwen-20260817-183136` +
`openclaw-gateway-web`. DeepSeek flash, дым `/v1` 200. Telegram не трогали.
Дальше — не парсер, а верность пересказа эталона `ask_1c`.

🔴 **Шаг 5 журнала ЗАКРЫТ `[17.08]`**: `ask_journal` в базах postgres/ut_test;
`ASK_JOURNAL=1`; одна точка `answer_checked`. test_ask_journal 15/15. Живая серия
на `:8096` (8097 занят чужим hung): 2 полные строки http. Разметка selftest:
postgres 592 / ut_test 3207, все 6 видов, uncounted=0. md5 `ae8b74e07a2c…`.
Не выкачено в `/opt` и не на `:8099`. Снятие сигнальных защит — отдельное решение.

🔴 **золотой гейт ut_test ЗАМКНУТ, Qwen на okna ОТКАЧЕН `[17.08]`**: эталоны
`ab-gold.tsv` — пакетная витрина ut_test (TRY_CAST, закупки =
`document_приобретениетоваровуслуг`; 0 по Ромашке/Ветер/ТехноСнаб и декабрю-2025
из данных). Прогон `:8097` live **2/8 сбоев 0**, отметка `ut_test live 2/8`.
dev `/opt` `serene_ask.py` **`3d2ac2df…`**, юниты на vLLM Qwen, `/health` + живой
`/ask` `:8091`→37 банков, `:8099`→45. okna серия Qwen: B-пары продаж
`1572493.22` + **`79752611.64`** (витрина выросла; SQL `sum(Всего)` до копейки;
старое `79435925.51` протухло) + ложный **0** у регистра; затем **503** (vLLM
500/404). Откат: код `8539aec6…`, модель DeepSeek pro; дым «сколько контрагентов»
200 / 349. `:8097` остановлен. Prod на Qwen не держим.

🔴 **Qwen3.8-27B vLLM — замер ответов ЗАКРЫТ `[17.08]`**: код в git
(`ASK_THINKING_OFF_BODY`); ворота 50/50 JSON, step4 **0/44 неверных**, ВЕРНО 2 vs 11
DeepSeek. На деве контур ответов — Qwen; на okna после серии — откат DeepSeek.
Следы: `runs/qwen-*-20260817.*`, `runs/qwen-golden-8097-20260817-retry.txt`.

🔴 **dev pipeline `@postgres` — upstream IIS мёртв `[17.08]`**: шлюз :6011
жив, но `192.168.56.1:6003` Connection refused → такт падает на `$metadata`;
таймер inactive/enabled. Владельцу: поднять Windows OData **или** disable
таймер (RUNBOOK §0). Зелёный такт первой базы не снят.

🔴 **Словари на vLLM Qwen3.8-27B — дым OK, полный branch в фоне `[17.08]`**:
flash/pro split окна К отменён; `alias_infer_gateway.py` → `infer model run --gateway`;
`ensure_vllm_gateway.sh` патчит allowlist undebot. Дым: parsed=1, 39 с, $0.
**Хвост:** `1c-branch-alias` ut_test CAP=0 (~1074 класса) → wiki measures vLLM;
отчёт `runs/gateway-usage-20260815-20260817.md`.

🔴 **okna шаг 4 — B sum, эталон суммы реализациитмц ОБНОВЛЁН `[17.08]`**: после
отката код снова `8539aec6…`. Новые SQL-эталоны корпуса (1 227 574): физлицо
`1572493.22` (без изменений) + реализациятмц **`79752611.64`** (было
`79435925.51`). Кассовые ордера без «Всего» — PROOF_NA.

🔴 **okna OOM + такт свежести ПОЧИНЕНЫ `[17.08]`**: `cpu_threads=4`, `memory_limit=
6500MB`, swap 4 ГiБ. `/health` зелёный. **Хвост:** снять swap; packet-meta URL.

🔴 **Волна-1 (детектор развилок + словарь подписей веток) В БОЮ на okna
`[16.08]`**: выкачен код из origin/main (md5 `4ed9bc7b…`, бэкап
`serene_ask.py.bak-20260816`), `/health` зелёный при полном корпусе
1 226 413. Детектор в бою: «на какую сумму мы продали» → clarify с двумя
ветками в `diag.fork` (8 223 / 3 826), сумма выбранной ветки 1 310 413,93 —
новые эталоны в CHANGELOG 16.08 (старые 14.08 протухли). `branch_alias.sh`
подписал 2 реальных класса (254 подписи). Схема `search_fork_class` выровнена
с детектором (`measure_ctx`, UNIQUE, GRANT INSERT/UPDATE serene_ro).

🔴 **Самопроверка трек C `[17.08]`**: гвард+чанкинг `branch_alias` в git (`3fd7774`);
фон: `1c-branch-alias` ut_test (87+ подписей, 0 пустышек) + tier2 пересъём
(334 в., ~15/334 на момент коммита, все 200). Дальше: wiki-alias → check →
финальный baseline. Postgres+okna tier2 ✅.

🔴 **Слепое пятно — базовое число снято `[15.08]`** (замок 17 плана ответов):
прибор `work/acceptance/selftest_surfaces.py` (ярус 1, без модели, детерминирован
двойным прогоном). **Итог после переключения дева на живой эмбеддер и построения
словаря postgres: слепое пятно 0 на ОБЕИХ базах** (0/178 и 0/697). Расклад:
postgres 24,2 % → 0 вектором (все 43 местом ≤3, 41 — первым) → 0; ut_test
19,2 % → 0: из 134 промахов 58 закрыл kNN, 76 — резолвер внутри `probe` (тоже
зависит от эмбеддера — двойная зависимость контура). Разбор —
[`docs/SELFTEST_BASELINE.md`](../docs/SELFTEST_BASELINE.md).
Главная находка частичного живого прогона (config=vector-off): «Сколько
Реализация Услуг?» → уверенные 19 517 по `catalog_поляформстатистики` при
достижимой `реализацияуслуг` — ошибка шага 4, не пятно.

🔴 **Правило владельца 16.08: на okna тестируем ПРЯМО НА БОЕВОМ КОНТУРЕ** —
ботом/фронтом сейчас никто не пользуется; действует, пока владелец сам не
отменит. Остальные серверы — по-прежнему только по явной инструкции.

🔴 **okna: ВИТРИНА ВОССТАНОВЛЕНА, klient-1 ВИТРИНА ПОЛНА `[15.08]`** — разборы
в `progress.md` 15.08 (сверка «потерь нет» обеих баз; приёмник `ed79d5f5…`).

🔴 **Первое действие по okna — дождаться нуля очереди векторов, сверить эталоны
(книга продаж, реализациятмц), вернуть `memory_limit='6GB'` и снять swap,
удалить дозор-cron.** Шаг 2 плана ответов (AnswerAtom + renderer) ✅ 16.08; шаг 3
(`decision_id`) ✅ 16.08 — билеты, сырой focus не гасит защиты, mcp presentation.
Шаг 4 (исходы A/B/C) ✅ 16.08 — детектор судья, A/B/C в бою на :8097; шаг 5 ✅ 17.08. Шаг 1 закрыт (ниже).

- ✅ **Заплатка сверки контура В БОЮ на обоих юнитах `[17.08 ~23:00 EEST, замер оркестратора]`**:
  на okna и klient-1 `/opt/1c-packet/packet_apply.py` = `a023c3609b01` = HEAD репо
  (включает `a8dfbf6`). Живой журнал: klient-1 «контур 9151, НЕ В ВИТРИНЕ 7632»
  (≡ баланс описи 6298+1372−38), okna «контур 819, НЕ В ВИТРИНЕ 450». Раньше
  значилось невыкачанной — запись протухла. Заход на Windows у действующих
  клиентов НЕВОЗМОЖЕН — всё нужное им делает приёмник; агентские правки только
  для новых установок.
- **Релей `89.23.101.22` с дева снова ДОСТУПЕН `[15.08 ~11:55 EEST]`** — ping
  0% (замер владельца), ssh deploy-ключом отвечает; фильтрация снялась сама.
  Цепочка до юнитов: `ssh -A root@89.23.101.22` → `ssh root@10.1.1.7` (агент
  поднимать в том же шелле: `eval $(ssh-agent -s) && ssh-add ~/.ssh/id_ed25519_deploy`).
- **Агенту в следующем релизе** — дополнять ключ после разворота
  `RecordSet`/табчасти (захода на Windows не требует).
  ~~`threads=4` у движка klient-1~~ — **снято рестартом serenedb 15.08 ~13:50 EEST**
  (по слову владельца): пул 160, память 9.3 ГиБ, apply чист (backlog пуст).

✅ **Пересборка корпуса okna ЗАВЕРШЕНА `[16.08 04:31]`**: векторизация 14,2 ч,
949 119 строк, `осталось 0 / невозможен 0 / потоков с ошибкой 0`; тактовая
постпроверка «без вектора 0» (23 432 служебных — по решению). Сверка с
витриной: строки корпуса = числу ОБЪЕКТОВ (fold по построению, не потеря):
реализациятмц 8 223 = 8 223, инвентаризационнаяопись 15 = 15, плансчетов
188 007 = 188 007. Временные меры откачены: `memory_limit` обратно 6 ГиБ,
swap-файл снят и удалён; тик после отката 39 с чисто. Разбор — CHANGELOG 16.08.
Известный хвост (не блокер): `p_doc_plain` не различает дубли ключей — если
сущность срывается в упрощённую сборку при 6 ГиБ, слияние встанет сторожем
«дублей ключа». Кандидат на правку корпуса отдельным шагом.

🔴 **Итоговый аудит модели A/B/C `[15.08]`: [`docs/AUDIT_ABC_MODEL_2026-08-15.md`](../docs/AUDIT_ABC_MODEL_2026-08-15.md).**
Исходная C (число лидера при неназванной альтернативе) отвергнута: это догадка.
A — только по полному одинаковому `AnswerAtom` всех веток; B — условные атомы,
связанные кодом; неподписанное/непосчитанное — `clarify`. Живой разрыв
витрина→корпус закрыт восстановлением витрины (выше).

🔴 **Рабочая рамка по аудиту `[15.08]`: [`docs/PLAN_ANSWER_CONTRACT.md`](../docs/PLAN_ANSWER_CONTRACT.md).**
Ядро: детектор из данных always-on (полный круг одним SQL — замер 4,6 мс на
1502 сущностях; развилка доказывается счётом, не сомнением модели),
состязательная самопроверка из данных (слепое пятно = число на базу), словарь
развилок штатным агентом. Проверено владельцем и подтверждено фактом: чипы WebUI
+ `resolve_focus` — транспорт выбора уже есть; словарь величин okna — 687 по 171
источнику. Порядок шагов и замки — в §8–§10 плана. Промты исполнительным
сессиям даёт сессия-аудитор.

🔴 **Шаг 1 плана ответов `[15.08]`: ✅ — A3 типизирован, детектор из данных, fail-closed.**
Пробы `test_a3_passport.py` (на старом коде 4/14 падает) и `test_fork_detector.py`;
детектор shadow: эталон «закупки» — обе ветки счётом (73 181 157,68 / 1 137 949,71),
cost_ms в diag; `_coverage_of` по самому полному слою, `/health` 503 при разрыве.
Первая база — паритет со старым кодом (`REGRESSION_BASE1`). Выкат — решение владельца.

**Словарь развилок (дев-дым 15.08) и стопы 1–2 (15.08) — ✅, перенесены в
[`progress.md`](progress.md) 16.08** (контур подписей веток, стопы rank/счёт и
соперники, остаток A3).

**okna-1 `[13.08]`, слой и клин klient-1 — в `progress.md` 15.08** (сборка 13.08
протухла: 15.08 корпус пересобран — 1 226 413 строк).
**Эмбеддер+реранкер `[17.08]`: переехали на `178.63.211.188`** (embed `:8000` —
балансировщик, воркер `:8001`; rerank `:8005`; новый API-ключ). Модель прежняя
(`Qwen3-Embedding-8B`, dim 1024) — пересчёт не понадобился. Переключены dev,
okna и klient-1 (`/etc/1c-embed.env`), whisper веб-фронта — `:8006`
(`/etc/1c-open-webui-stt.env`). 🔴 `EMBED_HEALTH_URL` — на ВОРКЕРА :8001, не на
LB (тот отдаёт `{"workers":[…]}`). 🔴 `EMBED_HOST` — ПОЛНЫЙ базовый URL: из
него + `EMBED_PATH` собирается дверь движка, голый хост ломает такт («код 000»,
живой случай на okna 17.08 — пойман `embed_check`). Замеры: тик okna зелёный
(38 с), обе двери dim 1024 на всех трёх, `/health` ask-юнитов дева ok, whisper
health ok. Протухший `RERANK_API_KEY` дева (15.08) закрыт новым ключом. Бэкапы
env `.bak-178-20260817`. История хоста `lr0r1k1g-*` (13.08–15.08) — в CHANGELOG.

_Хвост (03.08–16.08, таблицы «что работает», стоячее) — [`progress.md`](progress.md) 17.08._
