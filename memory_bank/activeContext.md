# Active Context

_Обновлено: **2026-08-28 поздний вечер**. Здесь — только живое.
История — в [`progress.md`](progress.md); стадии — в
[`docs/TARGET_STATUS.md`](../docs/TARGET_STATUS.md)._

---

# ⏭ С ЧЕГО НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

🔴 **Периметр (слово владельца 29.08): окно gpu-erw.timpul.pro, «работаем на этой
базе что есть» — обновление Windows-агента (1.0.2→1.1.3) ОТЛОЖЕНО вместе со всем,
что требует такта: К3 живая приёмка, пересборка корпуса, свежесть, corpus-половина
К2 (рабочие дни). klient-1 — потом.**

## Текущее состояние (29.08 день, бой :8091)

- Бой на коде дня (5 коммитов + complement `e0fdaa9` + дожим `25c551c`);
  env: `DEEPSEEK_MODEL=Qwen3.8-27B` (vLLM перезапускался, имя сменилось).
- Инцидент словаря закрыт (HOW_NOT_TO §3.99): строка контрагентов =
  «Контрагенты, клиенты, поставщики, список партнеров, Покупатели»; словарь
  синонимов 17 правил из боевой; гвард `wiki_alias.sh` (05866f1).
- Замеры в бою: контрольные **141/43** ✓, AB_PROBE **8/8** 25,23 с,
  ambiguous **11/18**, compare/К2 **0/8**.

## Следующий шаг

1. **Дождаться агентов** (если ещё идут): C4-фоллоуап (гвард исхода C по живому
   diag `.claude/state/c4-live-diag.json`) и К2 (z03/z04: окна «календарная
   неделя»/«с 1 по 15», ось-выключена → no_data). После каждого: замки сам,
   выкат deploy-скриптом (`bash /tmp/deploy-ask.sh` на деве; md5-сверка),
   рестарт `1c-serene-ask@postgres`, живые пробы.
2. **С4 приёмка**: «сколько позиций совсем не продаётся…» → complement или
   clarify C, НИКОГДА позитивное число; K6 live тогда 2/2 → открыта дорога К8.
3. **К2 приёмка**: AB_CALENDAR_AXIS=okna — ask-половина (неделя/с 1 по 15 →
   цифры 47 082 166 / 208 367 025; праздники → no_data; рабочие дни при
   выключенной оси → no_data/люк, не уверенный ответ).
4. **К8**: выкат скилла `work/skill-decomp/` в контур бота + compare 8/8.
5. **c5-перенос остальных слов** — ТОЛЬКО после механизма с защитой живых слов
   (diff до/после, потеря слова = громкий сбой); урок §3.99.
6. №16 «Как у нас дела?» live — slg1-фикс live не поднимается (темы пусты в
   сервисе) — разобрать после К2; №7 clarify-подпись склада должна попасть в
   options (сейчас только text).

## Механика дня (проверено живьём)

- Стейджинг: `systemd-run --collect --unit=serene-ask-staging-8092` с теми же
  EnvironmentFile + ПОСЛЕДНИМ файлом-оверрайдом порта (EnvironmentFile
  перекрывает Environment= — override отдельным файлом `/etc/1c-serene-ask-staging.env`).
- pkill -f по строке, которая есть в собственной ssh-команде, убивает свою
  сессию (дважды!) — убивать по pid из `ss -tlnp`.
- Гейты коммита: `git add` и `git commit` — РАЗНЫЕ вызовы Bash (хук видит индекс
  до команды); граф — observation → sleep 3 → add → commit.
- Скореры на окне: `AB_PROBE=okna` / `AB_AMBIGUOUS=okna` / `AB_CALENDAR_AXIS=okna`
  (+ `ASK_URL=http://127.0.0.1:809X`), вывод в nohup-лог; стрелки результата в
  конце; прогон полного ambiguous ~5 мин.
- Замки зон: `cd ubuntu/serenedb && python3 test_*.py` (pytest в среде нет);
  `test_fork_atom_aggregate.py` — живая БД, на деве виснет, не ждать его.
- `search_meta` на окне = ТОЛЬКО balance_registers + period_relative_forms:
  корпус старой сборки, карт календаря/валют нет и не будет без такта.

## Что мешает

1. Такт/$metadata (агент 1.0.2) — отложено владельцем; держит К3, свежесть,
   corpus-половину К2, контур-24.
2. Спорные эталоны ambiguous (№5 ответ-лучше-уточнения, №8/№12 склады без
   баланс-источников, №11 «анкеты» на okna — сущности есть, набор ждёт no_data,
   №4 игла «руб» при денежной единице «лей») — не подгонка, вопрос владельцу набора.



## Рецептура К3 (когда агент Windows станет 1.1.3)

Обновление: собрать exe `windows/packet-agent/build.cmd` → S3 → `upgrade-agent.cmd`
на машине okna → агент увидит need_metadata=1 (уже стоит) и пришлёт kind=meta →
apply напишет `/var/lib/serenedb/packet-meta/okna-1/$metadata` → такт
`1c-serene-pipeline@postgres` оживёт → corpus_build §1-quint наполнит карты →
проверка: accounting_currency_constant непуст, currency_amount_map ≥ 1 → стейджинг
:8092 с env БОЯ + `ASK_CURRENCY_AXIS=1` (оверрайд-файлом, не Environment=) → пробы
«сколько продали в августе 2026» (B/C с подписями при FX Δ375 437,03), «в евро» →
clarify. После такта перемерить ВСЁ (corpus обновится).

## Полезное из 28.08 (не потерять)

- Бой :8091 — юнит `1c-serene-ask@postgres`, env-файлы: 1c-mcp-reports → 1c-embed
  → 1c-serene-ask(-postgres).env; токен `ASK_TOKEN` в /etc/1c-serene-ask.env.
- Разовый wiki_alias.sh на окне: SERENEDB_DSN явной строкой (systemd-env не
  сорсится), обмен с движком через /var/lib/serenedb, юнит и выкат не пересекаются.
- Перенос c5-слов: только с диффом живых слов до/после (§3.99); MERGE в репо
  честный (только пустые строки), замену 28.08 сделала ручная операция.
- Выкат ask-кода: `bash /tmp/deploy-ask.sh` (md5-дифф → scp → atomic mv) —
  при исчезновении скрипта восстановить по этому описанию.

История дней — `progress.md`.
