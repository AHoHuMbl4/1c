# Active Context

_Обновлено: **2026-08-07.** Здесь — только живое: текущее состояние и «С ЧЕГО НАЧАТЬ».
История по дням — в [`progress.md`](progress.md); стадии по контракту — в
[`docs/TARGET_STATUS.md`](../docs/TARGET_STATUS.md)._

---

# ⏭ С ЧЕГО НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

**Шаг 4 читает «на что НЕ отвечает» `[06.08, вечер]`: отсев по `not_enough_for` + три
починки круга арбитра ВЫКАЧЕНЫ (21:36), приёмка 0/11/33 два прогона подряд.**
Форма отсева (кандидат исключается ДО модели, когда весь род вопроса накрыт одной записью
«не отвечает» без указателя на соседа) выбрана прибором `not_for_probe.py` среди восьми:
**0 эталонов при 6 из 9 подмен**. Ответов отсев не прибавил: после него модель выбирает
верное, но гасят вето словаря (лидер — третий сосед) и арбитр-детектор. Ценность дня —
закрытые дыры, вскрытые прогонами A-D: «одиночный ответ круга от соперника, а не от выбора»
(`single_is_rival`, №42: 4 по НДС-регистру при эталоне 0), «соперник writer_pair молчит —
не согласие» (`pair_unanswered`) и «отсев разоружил вето» (`veto_top_without`, №18).
Разбор — `SPEC_RETAIL_VS_SALES.md §4-бис`, CHANGELOG 06.08 вечер. Выключатель `ASK_NOT_FOR=0`.
Первая база: золотой 4/4 после каждой версии, после выката «12 контрагентов + 1 папка».
**Петля `1c-mcp-ask` разобрана и закрыта `[07.08]`.** Падал осиротевший одиночный юнит
(нет `MCP_PORT`, умолчание :6016 занято боевым `@ut_test`); дёргало умолчание
`deploy_instance.sh`. Остановлен; умолчание убрано (рестарт всех активных `1c-mcp-ask@*`);
мосты живы. Осталось владельцу (root): удалить `/etc/systemd/system/1c-mcp-ask.service`.

**Канал `1c-gate.timpul.ru` → Ubuntu `[06-07.08]`: ✅ В БОЮ целиком (туннель, приёмник, mTLS, fail2ban).**
Домен → HAProxy на FreeBSD `201.34.130.46` (`:443`, mTLS `verify required`, CA `1c-packet-ca`)
→ SSH reverse-туннель (юнит `1c-gate-tunnel`, `6022 → 127.0.0.1:6090`, инициатор Ubuntu) →
`packet_server` + `1c-packet-apply.timer` — active. 🔴 WireGuard замерен МЁРТВЫМ (возвратный
UDP Европа→РФ не доходит вовсе, TCP чист — `ubuntu/wireguard/README.md`, там же замеры
скорости ~37 Мбит/с и живучести). Замеры: без серта обрыв TLS, с сертом `CN=ut` — 200.
⚠ Часы релея отстают ~2 мин: свежий сертификат отвергается до notBefore по ЕГО часам.

**Пакетный транспорт `[07.08]`: ✅ E2E ПРОЙДЕН — первая full замкнута и доказана равной OData.**
Пакет 000002 (~1590 чанков, zstd+age, mTLS+Bearer) verified 09:33 → applied 09:40 (~7 мин);
эталоны УТ сошлись (партнёры 164, новости 501, замеры 97 834). Контрольный OData-такт переписал
80 таблиц — сверкой движком 0/0 доказано, что содержимое совпадало (одноразовый стык каналов);
повторный такт — **0 изменений**. Бот по пакетной витрине: «партнёров 164». **Дальше:** дельта-
такты + решение владельца о контуре стенда (агент запущен вручную — встанет с консолью;
прод-форма — планировщик из установщика 1.2.0 или обратно таймер OData). Контракт —
`docs/PACKET_CONTRACT.md`.
**Установщик Windows 1.2.0 `[06.08]` — блок 2 «агент пакетов» СДЕЛАН** (слово владельца
в этой сессии): `windows/odata-setup/src/Packet.cs` + интеграция в `Program.cs`
(префлайт комплекта/связи/места, установка в `C:\1c\packet` + `agent.ini` с ACL,
автозапуск планировщиком, smoke `--smoke` — код 0 только при подтверждённой доставке,
новый код выхода 30). Без комплекта рядом с exe поведение = 1.1.0. Контракт CLI агента,
на который опирается блок, — `work/installer-exe/AGENT_TZ.md` §7, статус — §8.
⚠ Не проверено компиляцией (на этом сервере нет csc/mono) — первая сборка `build.cmd`
на Windows; живой прогон — когда будет комплект и сам `packet-agent.exe`.

**Гейты подключены к Kimi Code `[06.08]`: ✅ вторая среда исполнения под `claudedev`.**
Конфиг `~/.kimi-code/config.toml` (root:root, ставит `install-gates.sh` из
`work/hooks/kimi-config.toml`) — тот же набор гейтов; снайпер — `sniper-kimi.sh`
(процесс `kimi -p`), вброс состояния — `prompt-start.sh` (UserPromptSubmit), правила
читаются через `AGENTS.md` (жёсткая ссылка на CLAUDE.md). Проба — 89 случаев, зелёная;
живые замеры — в CHANGELOG 06.08. Незакрытые отличия движка: stdout хуков модель Kimi
не видит (вбросы check-deps невидимы; enforce — check-graph-fresh на коммите), упавший
хук — fail-open (последний рубеж — git-гейт). Запуск: `su - claudedev -c 'cd /srv/1c && kimi'`.

**Доля ответов (п. 21) `[06.08]`: ✅ 6 → 11 ВЕРНЫХ ПРИ НУЛЕ НЕВЕРНЫХ, ответ был в контракте.**
_Предыстория дня (что держало ответы, три отвергнутые формы, пересборка словаря) перенесена
в [`progress.md`](progress.md) 05.08; полный разбор — `work/entity-choice/ANSWER_RATE_P21.md`._
Сделано по дословному тексту п. 21: «проверка не пропустила ответ — попробуй сформулировать
заново и проверить снова» — если словарь не подтвердил выбор, спрашивается итог ВСЕХ сигналов
(возглавляет ли выбор общий порядок кандидатов), и только там, **где сомнения не поднималось**.
`[замер 06.08]`, два прогона подряд: **0 / 10 / 34** (прибор следа) и **0 / 11 / 33**
(`step4_bench`, боевой юнит). Выключатель `ASK_VETO_HEAD_WINS=0`. Первая база цела
(`REGRESSION_BASE1` 06.08). Продолжение этой работы — блок «отсев по not_enough_for» выше:
снял молчание с шести уточнений-подмен, но ответов не прибавил (стены — вето словаря и
арбитр-детектор).

**П. 17 «свежесть» `[06.08]`: 🟡 такт 2 ч 18 мин → 12,5 мин (в 11 раз). Остался корпус.**
Тактовый замер на боевой базе (16:08 → 17:06). Снято двумя правками: разведение
слов-столкновений не сходилось (3580 → 120 с) и размер страницы OData был зашит числом
1000 (загрузки 2940 → 1631 с, `ЗамерыВремени` 888 → 101,8 с). 🔴 **Осталось ровно три
вещи, и все посчитаны:** перепись схемы **~815 с** внутри такта свежести (она про схему,
не про данные); **дельта по владельцу и регистратору** — полных загрузок 1374 против 3
дельт, признак изменения есть у **1129 источников из 1374**; **корпус 780 с**, который
пересобирается лишь потому, что полная загрузка объявляет витрину изменённой — заработает
дельта, сработает уже готовый пропуск. Отдельно: три источника 1С отдаёт патологически
медленно (6 531 строка за 626 с, 38 % загрузок), страницей не лечится. 🔴 **Первое действие следующей сессии — корпус.** Замерен такт В ПОКОЕ (748 с). Такт С
ИЗМЕНЕНИЯМИ дороже: любое изменение витрины пересобирает корпус ЦЕЛИКОМ — `[замер 06.08]`
сборка 1123 с, из них корпус **802 с**, то есть первый же такт после правки в 1С даст ≈ 26 мин.
Владелец 06.08 решил убрать служебные источники из загрузки совсем (807 источников, синк
1071 → 428 с); метка держится тремя признаками — объективным `only_binary` из `$metadata`,
смысловой разметкой `search_entity_class` и таблицей `search_entity_force`, где слово владельца
сильнее обоих. Исключённое видно в `search_entity_skipped` с причиной. **Прежняя развилка снята**
(её разбор перенесён в `progress.md` 06.08).

**Шаг 8 «достаточен ли вопрос для ответа» `[05.08]`: ✅ заведён, замерен (неверных 1 → 0),
выкачен 06.08.** Разбор перенесён в `progress.md` 06.08. ⚠ **Владельцу:** у №5 в
`ACCEPTANCE_UT.md` эталон 26 239,5 — набор ждёт число, а решение требует уточнения;
менять набор — не наше.

## 04.08: шаги конвейера разобраны по отдельным сессиям

Владелец разложил конвейер ответа по сессиям — каждая правит только свой шаг. Шаг 4
(выбор сущности) по-прежнему за владельцем, своих правок туда не вносить.

Числа и разбор шагов 1, 2, 3, 6 и подготовки базы (связь «кто пишет этот источник»)
перенесены в `progress.md` 04.08/05.08 — работа закончена и замерена. Здесь остаются
только **незакрытые хвосты**:

- **шаг 2** — в бой не выкачен, приёмкой через бота не перемерен;
- **шаг 3** — слияние выкачено в `/opt`, но **юнит с ним не перезапущен**;
- **шаг 6** — в бой не выкачен: в общем `serene_ask.py` лежат незаконченные правки
  соседних сессий, поэтому замеры сняты на отдельном экземпляре (`:8098` — `ut_test`,
  `:8097` — база 1);
- **шаг 1** вскрыл чужое и это не починено: №55 условие `= 0` доезжает, но считает не та
  сущность (4 при эталоне 0); «сколько контрагентов» стало уточнением вместо верного 155;
  ответ 34-66 с против ~17 с;
- **шаги 5 и 7** — ✅ выкачены, ноль ошибок обоих классов.

⚠ Каталог контура бота после правки конфига через RPC закрылся до
`drwx------` — возвращается `chmod g+rx /home/undebot/.openclaw`.
🔴 Бота теперь можно гонять без `sudo`: RPC шлюза `chat.send`/`chat.history`, адрес и
токен — в `~/.openclaw/openclaw.json` рабочего аккаунта (`0600`), перезапуск шлюза —
`systemctl start 1c-openclaw-gateway-restart`.

## 🔴 03.08: ЦЕЛЬ ПЕРЕФОРМУЛИРОВАНА ВЛАДЕЛЬЦЕМ — читать это первым

Дословно: «нужно найти и перемерить механизмы, которые обеспечат **100 % правильности
ответов**. **Переспрашивать у юзера можно**». И отдельно: «**промты — не работают, это
закон**… правила надо делать нативными способами openclaw». И: «нет никакого стенда, на
прод не выходим, пока не решим задачу до конца».

**Что из этого следует для работы:**

1. Задача не «всегда ответить верно» (недостижимо), а **никогда не ответить неверно**.
   У системы всегда есть законный выход — вопрос человеку.
2. Мерило — **число ОШИБОК**, цель ноль. «Сколько верных» больше не главный показатель.
   Прогонщик это уже печатает: сводка «ПО МЕРИЛУ „НИКОГДА НЕ ОТВЕЧАТЬ НЕВЕРНО“».
3. Любое правило, вписанное в промт, **не считается сделанным**. Держит это хук
   `.claude/hooks/check-prompt-rules.sh` (останавливает такие правки; 6 проб).

**Где мы по этому мерилу** `[замер 03.08]`: база сравнения — прогон `card-delivery`,
верных 4, уточнений 3, **названных неверных чисел 3** (две ошибки — выбор сущности, третья
— выбор величины). Сам разбор с таблицей, разброс прибора и что сделано 03.08 перенесены
в [`progress.md`](progress.md), раздел 03.08: с 04.08 работа идёт по шагам конвейера, и
базой сравнения служат замеры каждого шага выше.

### 🔴 Что ВЫКЛЮЧЕНО и почему

**Свой поход плагина за данными** (плагин сам зовёт сервис, когда модель не позвала
инструмент) — код есть, пробы 73 зелёные, но `askUrl` **очищен**. Две причины, обе
замерены: он спрашивает сервис **сырым вопросом**, минуя перефразирование по вики, и он
**гасит прежнюю защиту** (появился эталон → `requireDataTool` больше не требует переписать;
в вопросах 4 и 7 гейт стал `pass` там, где был `revise`). Включать — только после починки.

### Первое действие следующей сессии

Порядок собран по составу ошибок **исполняемой** сборки (таблица выше). Владелец 03.08:
«делаем всё по очереди, можешь меня не спрашивать».

1-2-бис. ✅ Ноль вместо отказа, выбор сущности внутри семьи, пошаговый след пути —
   сделано и выкачено 03.08, числа в `CHANGELOG` 03.08.
4-5. **Синонимы и 230 стандартных отчётов из выгрузки конфигурации** — материал для
   переспроса и готовое соответствие «деловой вопрос → источник данных»; выгрузка лежит
   на Ubuntu (`work/config-dump/ut_demo`), не разобрана.
6. Прогнать приёмку новым мерилом и получить число ошибок как базу сравнения.

🔴 **Выбор сущности владелец забрал в отдельное исследование** (его слово 03.08: «явно что
выбор сущности в такой схеме не сработает верно»). Всё, что нужно для него, сведено в
[`work/entity-choice/PATH_AND_FAILURE_2026-08-03.md`](../work/entity-choice/PATH_AND_FAILURE_2026-08-03.md):
путь по шагам с `файл:строка`, что именно видит модель на шаге выбора, три ошибки с
доказательством по числам, таблица уже отклонённого. **Своих правок в шаг 4 не вносить, пока
владелец не вернётся с решением.**

3. **Сверка величины по ЗНАЧЕНИЮ, а не по имени** (вопрос 1). Эталон подтверждён двумя
   независимыми сущностями до копейки, неверный кандидат — ничем. Это задача 18,
   отвергнутая 03.08 только потому, что сверяла одноимённое; имена и есть источник ошибки.

🔴 «Множество подсчёта» из плана **убрано**: дефекта в нём не показано, пункт держался на
моём неверном разборе (`HOW_NOT_TO §1.46`).

### Ждёт владельца

- Траты на прогоны приёмки (пункт 2 его списка). Windows и выгрузка конфигурации закрыты
  03.08 — разбор в `docs/INSTALL_LOG.md` и `progress.md`.

🔴 **Гейты отвечают `deny`, а не `ask`** — в сессии, где вопрос никому не
показывают, `ask` равен разрешению. Устройство, пробы и установка — в `progress.md`
(03.08, вечер) и `CHANGELOG` 03.08.

## Два контура (устройство — в `ARCHITECTURE.md`, история — в `progress.md`)

Боевой — `@test1c_mcp_bot` → `ut_test` (:6016/:18800, вики `wiki/main`); второй —
`@Test11c_bot` → `postgres` (:6017/:18830, вики `buh`); изоляция по построению.
🔴 **Долг:** такт не обновляет вики второй базы (`build.sh` зовёт `wiki_publish.sh` без
`WIKI_VAULT`/`OPENCLAW_PROFILE_ID`; данные целы, словарь замирает). И живое от 03.08:
**числовой гейт стоит на доставке**, прогоны без доставки мерят половину защиты.

## Работы после аудита доков (кластеры Э0–Э9, разбор — в `progress.md` и `DEV_PLAN.md`)

Порядок работ — [`work/doc-audit/DEV_PLAN.md`](../work/doc-audit/DEV_PLAN.md); Э4, Э1 и Э0
закрыты. Живого отсюда два:

- **прежние числа приёмки аннулированы** — сняты сломанной линейкой; первый прогон новым
  прибором задаёт базу сравнения и снимается дважды подряд без правок, чтобы разброс был
  числом;
- **самому аудиту верить нельзя целиком**: 15 % ошибочных вердиктов в выборке, одиннадцать
  дефектов (`F254`, `F255`, `F259`, `F265`, `F274`-`F277`, `F285`-`F287`) трёх скептиков не
  проходили — перед правкой по ним нужен свой замер.

## 🔴 Где мы сейчас

### Что работает

| Что | Чем подтверждено |
|---|---|
| сборка корпуса и слияние внутри базы | 623 565 строк, такт ~13 мин, своего кода в сборке нет |
| счёт и суммы | все денежные эталоны сошлись **с 1С** до копейки |
| гейт | ни разу не пропустил число, которого нет в данных |
| папка справочника не позиция | 227 = эталон 1С, число групп названо в ответе |
| простые вопросы «сколько у нас X» | партнёры, контрагенты, организации, склады, документы — устойчиво верно |
| отказ при отсутствии данных | проверяется прогонщиком по `kind` |
| выкатка бота | `deploy_instance.sh`: персона, мост, плагин-гейт, чистка рабочей папки |

### Что НЕ работает

| Что | Чем подтверждено |
|---|---|
| 🔴 **выбор сущности и величины** — главная причина неверных ответов (кластер Э3) | разбор в `progress.md`, раздел «Главная открытая проблема»; оговорка «и величины» — из `ACCEPTANCE_UT` |
| ~~🔴 прибор приёмки~~ — **починен 02.08** (Э1) | `F210`/`F211`/`F212`/`№24` закрыты, проба подменой 22 из 22 |
| воспроизводимость | прогоны БЕЗ правок расходятся на ±1 верный ответ из десяти |
| алиасы как подсказка | лидер для «сколько у нас партнёров» — «Принятая Возвратная Тара» |
| величина в табличной части | эталоны вопросов 2, 4, 6, 7 лежат в `..._товары`, в шапке их нет |

### Что проверено и ОТКЛОНЕНО замером — не повторять

- перегенерация всех алиасов: 2/3/5 против 3/4/3 (`HOW_NOT_TO §1.26`); жёсткое правило
  «подтверждает только лидер»: 1 улучшение против 3 ухудшений (`§1.25`); индекс алиасов на
  словаре со стеммингом — ранжирование вырождается в нули; сокращение промтов сервиса —
  пользы не подтвердило; замер в обход бота и вики даёт другой ответ (`§1.30`);
- шаг 3 `[04.08]`: отсечка вариантов написания по своей поверхности (хуже корпусной) и
  чистое слияние рангов как итоговый порядок (43 из 44 против 44 у карточки).

### Чему больше нельзя верить

- Всем выводам «правка промта помогла / не помогла» до 31.07: правился файл, который не
  читался (`§1.27`).
- Любому сдвигу на один верный ответ из десяти: это шум (`§1.34`).
- Отметкам 🟢 в `TARGET_STATUS` по пунктам 2, 3, 10 до 31.07: они стояли по набору из
  восьми лёгких вопросов, полная приёмка показала другое.

---

## Стоячее (живое, но не «прямо сейчас»)

### 🔴 Отложено по слову владельца: Windows-PG + attach не сделан

[замер 30.07] в `ut_test` **1 620 таблиц витрины лежат ВНУТРИ SereneDB**, присоединена
только наша же вторая база. Решение владельца 26.07 требует обратного: сырые данные в
PostgreSQL на Windows, SereneDB держит только корпус и индекс через `ATTACH` + `VIEW`.
Владелец 30.07: «отдельная работа, но запиши что надо» — перечень шагов в
`docs/BOXED_BLOCKERS.md`.

### Открыто и ждёт слова владельца — восемь пунктов, перечень в конце `DEV_PLAN.md`

Очерёдность п. 14 (инсталлятор «в конце» — до ответа работа по нему стоит), траты на
прогоны приёмки, доставка приёмки в мессенджер, доступы и перевыпуск токенов на стороне
1С, проверка 1С снаружи, удаление данных со стенда, перенос моделей внутрь контура (п. 16)
и **форма его правила 30.07 про переспрашивание**: выключатель `ASK_ALIAS_VETO` реализует
другое правило и по замеру рубит верные ответы, поэтому не включён.

Остальное решено разработкой: персона — `№45`, вывод `report_1c` из конфига бота —
кластер Э2 (`F004`, `F130`), строгость гейта — кластер Э4.

### Хвосты без решения владельца

- доснять приёмку 21-58 — прибор починен (Э1), осталось **ответа владельца** про траты и
  канал доставки: это пункты 2 и 3 его списка;
- проставить эталоны **двум** вопросам приёмки (№20, №53) — требует 1С под рукой.
  *Было ошибочно: «13 вопросам, треть набора не проверяется». Эталоны у большинства в доке
  были — их не читал прибор ([замер 02.08]: сверяется 47 вопросов из 58, а не 26);*
- кнопки уточнения в боте OpenClaw — бэкенд готов (`/ask` принимает `focus`, уточнение
  отдаёт `options`), UX делаем в конце (`docs/PRODUCTION_PLAN.md`);
- дельта регистров (составной ключ/хеш) — сократит такт с минут до секунд; сейчас
  регистры перезаливаются полностью, но грузятся быстро;
- `docs/BOXED_BLOCKERS.md`: резолвер на нелатинских базах, словарь `ru_RU.utf8`
  по умолчанию, остаток ручного перечня сущностей;
- среднесрочный план автономности — [`docs/PLAN_AUTONOMY.md`](../docs/PLAN_AUTONOMY.md):
  Фазы 1-2 (установщики), 4 (набор вопросов из базы), 5 (приёмка агентами без
  контекста), 6 (цикл до зелёного) не закрыты.

### Что нельзя делать (стоило потерянного времени)

- 🔴 **запускать `serene_search_build.py`** — при нынешнем составе сносит таблицу и индекс
  (авария 03:55 28.07). Снят с таймера; юнит `1c-serene-index` теперь на `build.sh`;
- **опыты с `ivf`** на боевом инстансе — роняют движок в петлю OOM;
- `VACUUM (REFRESH_TABLE)` на корпусе — 34 ГБ и смерть движка. Только `REFRESH_INDEX`;
- параллельная запись векторов в ОДНУ таблицу — `SIGSEGV`. Каждый поток в свою;
- **обернуть `corpus_merge` в один `BEGIN…COMMIT`** — `REFRESH_INDEX` внутри транзакции
  тихо не публикует (замер 28.07);
- поднимать пачку эмбеддера выше 10 или писать в неё `NULL` — падение/`SIGSEGV`.
