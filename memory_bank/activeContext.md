# Активный контекст

## С ЧЕГО НАЧАТЬ (срез 02.09 ~14:40 UTC; читать целиком; после compact)

### 🔴 РЕЖИМ: я — оркестратор, ВСЁ делается армиями агентов
Владелец 02.09: «Делай так же все армиями агентов. Задание агенту, и армия для
проверок даже. Ты — оркестратор. Видишь, какой эффект дал — за ночь почти всё
решилось». Порядок на любую задачу: (1) я сам нахожу точные file:line и пишу
самодостаточный промт в `.claude/state/prompt-<id>.md`; (2) исполнитель —
cursor-agent; (3) проверка — замки/замеры мною, не словами агента; (4) коммит
мною с ритуалами (ниже). 🔴 Квота Kimi-subagent исчерпана ночью (402) —
обёртки не работают, cursor запускаю НАПРЯМУЮ Bash-фоном:
`cd /srv/1c && timeout 3000 cursor-agent -p "$(cat .claude/state/prompt-X.md)"
--force --model auto --output-format stream-json | tee .claude/state/cursor-run-X.log`.
Извлечь итог из лога: последний JSON-эвент type=result (скрипт-извлекалка
использовалась 5 раз, см. историю сессии; финальный текст туда же пишется).

### КЛАССЫ 6-9c + q1/q2 — ВСЕ В main И ВЫКАЧЕНЫ (02.09, HEAD d6178ee = 980e339 с фиксом такта)
- **Класс 6 (e2b269b):** исход A контракта — wiki-clarify по сущности: код
  считает меру по каждой tied-ветке (aggregate z17:231 + pick_measure
  z16:589); равные числа → kind=answer сразу (src не закрепляется); разные →
  чипы. **Класс 6b (d424bad):** мера ветки = канон основного пути (sales →
  деньги), count только при want=count.
- **Класс 7 (33458a2):** verify не обрезается — WIKI_VERIFY_MAX_TOKENS 400→
  2048 + salvage обрезанного JSON + мусор=degraded не none. Корень класса
  «движений/записей в регистре X» (доказан raw-пробой: обрыв 8-го verdict).
- **Класс 8 (0a7a6d4):** post-verify лидера — ВСЕ известные базе меры
  вопроса + ось-квалификатор action_axis обязаны нестись лидером.
- **Класс 9c (837551d):** journal-ghost — ось вопроса реальна в метаданных (stem
  по documentjournal_%) и учёта остатков по ней нет → no_data. **Кейс 2402
  ЗАКРЫТ живьём** (wiki_none=axis_not_carried). Попутно починен живой баг:
  скобка SQL в entity_form_catalogs_for_kind (z05:320). Класс 9 убран из
  post-verify (ложно ронял «в компании»).
- **q1 (fc165a7):** флаг ASK_WIKI_CHOICE удалён — коробка = бой (п.0).
- **q2 (fc165a7):** gold-эталоны продаж на витрину (Всего/Period), путь
  независим от движка (§3.23). На окне синхронизировано scp.
- Карта 10 путей entity-clarify вне wiki — бэклог
  (.claude/state/result-rm6-mapC-*).

### АУДИТ TARGET АРМИЕЙ (02.09 вечер; сводная docs/audit/TARGET_AUDIT_2026-09-02.md)
22 отчёта result-ta-*: К1 п.0 — бой прав только с ручными env-флагами
(замер /etc живой: 8 флагов =1, установка их не пишет; CALENDAR/CURRENCY_AXIS
выкл и в бою); К2 п.0 — wiki_card_build.sql НЕ в такте → новая база глуха
(no_data при живом корпусе); period_relative_forms.json в бою почти пуст; К3 имена каталогов z11:789;
К4 обходы wiki-замка z20:3291+ (подтверждено чтением); К6 z17:180 молчаливый
сброс предикатов даты; К7 serene_report шлёт схему в модель; К8 вектор-окно
DELETE→xfer; К9 i2_runner honest_no при числовом эталоне ДО сверки
(подтверждено чтением). Чисто: числа — SQL, ASK_WIKI_CHOICE удалён.
Споры TD1-TD5 ×3 идут (result-td*-*). Починка — после сходимости TD и
красной команды; в код не тащить без споров.

### ОЧЕРЕДЬ (срез 02.09 ~14:40 UTC)
1. **Такт №1 идёт на окне** (см. ЖИВОЕ СЕЙЧАС) → приёмка → такт №2 («минуты»
   при 0 изменений) → таймеры обратно → отчёт владельцу.
2. **L20** (67 вопросов × 2, workers 16, ~28 мин; команда в МЕХАНИКА) — после
   такта №1. Критерий: wrong ≤ 2 (2402 закрыт классом 9c), match растёт с 35.
   Меряет q1+q2+9c + свежий корпус.
3. **Остатки honest_no при числовом эталоне** (после L20 пересчитать классы):
   «реально покупают» 145 — скилл-декомпозиция, позже (решение №9).
4. **Бэклог общих механизмов (не начинать без владельца):** 10 путей
   entity-clarify вне wiki (карта mapC); проброс dictionary-оси в group-by;
   cold-start; исход B контракта. **Полная B такта** (row-level фильтр p_doc,
   partial_rebuild=1) — отдельный пакет ПОСЛЕ доказанного B0 (замки на
   частичный rebuild обязательны; scoped DELETE по op=upsert туда же).
5. И2 web, К8 — после стабильного engine.

### ЖИВОЕ СЕЙЧАС (02.09 ~14:40 UTC — ТАКТ №1 ИДЁТ, приёмка по baseline)
- **HEAD = 980e339 = origin/main.** На окне выкачено всё (md5 ×8 файлов +
  corpus_build.sql после фикса ee0173f5). Прод :8091 — на классе 9c.
- 🔴 **Такт №1 идёт на окне с 14:38 UTC** (после фикса 980e339; sync-проверка
  14:38:47 «изменённых строк 0», сейчас шаг сборки). Дозор —
  фоновая задача **bash-0ga67hnh** (ssh-цикл is-active, при завершении сама
  придёт нотификация с tail журнала + count корпуса). Такт №1 ДОЛГИЙ — норма
  (backlog ~39 источников, реализациятмц монолит — у неё 2776 строк с пустым
  Ref_Key+LineNumber → dup-гейт штатно снимает чанкование именно на ней).
- **Первая попытка такта упала за 6 минут ДО merge** (вектора не задеты):
  md5-строка формулы в search_quality.v (BIGINT). Фикс 980e339: формулы в
  search_pdoc_formula; живой прогон блока на окне чистый (chunk_entities=6).
- **Приёмка такта №1 (когда дозор позовёт):** count(*) ≥ 1 665 427,
  count(emb) ≥ 1 657 724; multiset realiz = 71947efec4bebbc2eac4727b51694523
  (74 785 строк; baseline — .claude/state/takt-baseline.txt); md5 row_key
  realiz = b29eb438346ec9f7d87326b0243a9b9c; vector_loss_gate=0;
  длительности шагов → отчёт. Затем такт №2 (0 изменений → «минуты»),
  таймеры обратно (1c-serene-pipeline@postgres.timer + 1c-packet-apply.timer),
  отчёт владельцу /tmp/takt-report-* на окне.
- **Бэкапы векторов на окне (условие №1):** backup_corpus_emb_20260902_1430
  (row_key) + backup_corpus_emb2_20260902_1500 (+content_hash), по 1 657 724,
  сверены. Restore-drill пройден (5550/5550 бит-в-бит; restore: сначала
  row_key, fallback по content_hash ТОЛЬКО из HAVING count(*)=1 групп —
  на окне 110 254 коллизионные группы; coalesce над FLOAT[1024] движок НЕ
  умеет — два UPDATE, не coalesce). Бэкапы держать до двух зелёных тактов.
- **Что в пакете (d6178ee):** A чанкование по ячейкам; B0 search_changed_rows
  (витринный ключ, partial_rebuild=0 константой — список НИКТО не читает);
  C шаг 5 на embed_bulk (strict transfer, restore globals fail-closed, gate
  p_doc/merge+idle-in-tx, tick_guard снят с шага 5, fail при «осталось>0»,
  REFRESH corpus_ivf_idx + smoke kNN); D wiki_alias SQL-only fail-closed +
  census «вне контура» (415 «прочих ошибок» были мисклассификацией).
- **Красные замки пред-существующие (доказано stash-прогоном на HEAD):**
  test_pipeline_doc 5/8 и test_build_solr_synonyms regexp_split — НЕ из пакета.
- **Локальный движок на деве (serened :7890) ЗАВИС и не наш** (юнит serenedb вне
  polkit-правил, рестарт — root/владелец). Решение владельца 02.09: «забудь про
  дев, никаких ut_test, только okna, только SereneDB». Живые приёмки — на окне.
- Доклады: .claude/state/result-audit-*, result-tf{1..9}-*, result-drev*,
  result-exec-*, plan-takt-fix-v10.md (единый документ пакета).

### МЕТРИКА СЕЙЧАС (L19, честная)
35 match / 28 honest_no / 3 wrong / 1 unresolved из 67. TARGET: «не врёт» —
3 wrong все разобраны до корня (2402=ось, кассовыеордера=лаг, позавчера=
лаг); «отвечает, когда данные есть» — 35/67 и растёт.
🔴 Колонка nums в i2-report.md УСЕЧЁНА (2950→«295»): читать
/tmp/i2-lXX/i2-answers.jsonl, не колонку. Эталоны ЖИВЫЕ — дрейфуют между
прогонами (база растёт): сравнивать прогоны по answers.jsonl, не по вердиктам
на дрейфующих окнах. Эпоха прибора: с L20 эталоны продаж — витрина (q2),
сравнение с L13-L19 по sales/buyers несопоставимо.

### СДЕЛАНО ЗА ДЕНЬ 02.09 (утро-день; все коммиты в origin/main)
- **Такт — карта атомов (армия 8×2, сошлась), таймер включён.** Безопасно
  (доказано строками): embed_missing строго дельта `emb IS NULL` (UPDATE
  только по NULL), preflight/secrets, wiki/coverage (масштаб карточек ~1.5k,
  не корпус), таймер OnUnitInactiveSec (по готовности). Флагов-убийц на окне
  НЕТ (замер env): MERGE_VECTOR_LOSS_BYPASS, EMBED_ALLOW_LARGE_TICK,
  FORCE_REBUILD не заданы, SERENE_SRC_DIR пуст. Риски (штатно не «убивают
  всё»): (1) обрыв chunked MERGE corpus_merge.sql:790-878 → часть emb=NULL,
  досчёт точечный; (2) обрыв синка до changed_sources_ok → следующий такт
  перечитывает ВСЕ источники (лишняя работа, не потеря); (3) смена
  content_hash/row_key без карты xfer → массовый досчёт, стопит vector-гейт
  0.5% (при bypass=0); (4) sync без flock. Отчёты: .claude/state/result-t{1..8}-*.txt.
- **Классы 6+6b+7+8+9+9c, q1, q2** — см. блок выше (класс 9c = 837551d, кейс
  2402 закрыт живьём; день заканчивается на d6178ee + фикс 980e339).
- **Пакет ускорения такта (d6178ee + 980e339):** 9 волн аудита плана армией
  (5 аналитиков кода → красная команда → контроль ×5) → план v10
  (.claude/state/plan-takt-fix-v10.md) → 4 исполнителя параллельно → волна
  рецензий диффа (поймала критический дефект: md5-тег строки как фильтр порции,
  коллизии 8391/74979 — до выката) → 3 фикс-раунда с живыми пробами на движке →
  замки поверх суммы зелёные → выкат md5×8 → первая попытка такта упала до merge
  (search_quality.v BIGINT vs md5) → фикс → такт №1 идёт. Вектора ни разу не
  подвергались риску: бэкапы ×2, restore-drill, приёмка по baseline.
  Урок дня (в HOW_NOT_TO-класс): живая проба нового SQL обязана включать
  КАЖДУЮ новую запись в существующие таблицы, не только новые объекты.
- **Прогоны ускорены:** workers 16 → ~28 мин (указание владельца).
- Красная команда стала обязательным шагом (решение №12); отвергнутые
  варианты (чтобы не предлагать снова): поле квалификатора в промте
  INTENT_SYS; голое «токен ∩ справочник» для осей; «¬carriers→none» в
  post-verify; «≥2 живых money → clarify»; расширение dead-swap мер вслепую.


### ОСТАТКИ/ДОЛГИ (обновлено 02.09 ~14:40 UTC)
- struct_single — ЗАКРЫТ классом 5 (c1c7d7a, решение №6).
- REGRESSION_BASE1 — ОТМЕНЕНО владельцем 02.09 (решение №7): UT забыт.
- z13:12-16 подстроки имён регистров в коде (п.0, базлайн О6 — выжигание).
- Разброс разбора между прогонами (INTENT_MEMO внутри прогона только).
- Инертные stock-helpers (warehouse_clarify, balance_bridge_clarify,
  stock_subject_needs_clarify) живут только в тестах — отдельным решением.
- Квалификаторы места — ЗАКРЫТО классом 9c (837551d, 2402 → no_data живьём).
  not_enough_for класса 4 — частично.
- «реально покупают» (145) — скилл-декомпозиция, позже (решение №9).

### МЕХАНИКА (выучено; НЕ забывать)
- Прогон И2 на окне (из каталога выката /opt/1c-mcp-reports):
  `nohup bash -c "while IFS= read -r -d \"\" kv; do export \"\$kv\"; done <
  /tmp/ask_env.bin; I2_ENGINE_TIMEOUT=300 I2_TAG=lXX python3
  work/gold/i2_runner.py run --tsv <TSV> --path engine --out /tmp/i2-lXX
  --reshoot-rules work/gold/reshoot-rules-okna.json --workers 16" >
  /tmp/i2_lXX.log 2>&1 &` — 🔴 **workers 16** (решение владельца 02.09:
  «тестить ответы можно параллельно, уходит минут 20» — замер L19: ~28 мин
  против ~2 ч на workers 6). TSV — из каталога work/gold ВЫКАТА (в репо его
  нет; канон — `ubuntu/serenedb/client-gold-okna.tsv`, md5 совпадает с
  выкатом, замер 02.09). Локальный дозор на PID (ssh ps -p в цикле,
  disable_timeout=true). jsonl отчёт пишется только в конце.
- 🔴 **Файлы work/gold на окне деплой НЕ покрывает** — после правки
  i2_runner.py / reshoot-rules-okna.json синхронизировать ВРУЧНУЮ scp в
  выкат (сделано для reshoot-rules 02.09, md5 6dd0aae3).
- 🔴 **Дозор по pgrep — только с bracket-трюком** `pgrep -f "cursor-agen[t] -p"`:
  голый паттерн совпадает с командой самого дозора → вечный цикл (случай
  02.09, дозор картографов rm6 поймал сам себя).
- Извлечь итог cursor-прогона из stream-json лога: последний эвент
  type=result → поле result (скрипт-извлекалка писала result-*.txt рядом).
- Пробы: прод — `python3 /tmp/diag_ask2.py "вопрос"` на окне; код-кандидат —
  /tmp/probe_root (cp -r /opt/1c-mcp-reports) + diag_ask2p.py. /tmp не
  переживает ребут: env пересобрать из /etc/1c-*.env (кавычки срезать).
- Замки — НЕ pytest (тесты — скрипты): `cd /srv/1c/ubuntu/serenedb && for t in
  ...; do python3 $t.py; done`.
- cursor-agent: промт только из файла; хук check-prompt-rules блокирует
  StrReplace на z20 — cursor правит через shell, это норм; правки проверять
  git diff + замки самому; слова исполнителя — замером оркестратора.
- Гейты: add и commit разными вызовами; сообщение с «Числа:» и «Доки:»;
  граф MCP → sleep 3 → grep → add mcp-memory.json в тот же коммит; коммит
  пат-спеком. index.lock без процесса — старый, снести (случай 01.09 21:04).
- GPU: 188 embed :8000 + rerank :8005; .12 27B :8000. Доступ
  `ssh -i ~/.ssh/id_ed25519_deploy root@178.63.211.188` / `root@49.13.97.101`.

### РИТУАЛЫ (выучено гейтами)
- 🔴 **git add и git commit — РАЗНЫМИ ВЫЗОВАМИ Bash, без `&&` между ними**
  (снайпер и гейты смотрят индекс ДО выполнения команды). Сначала add,
  проверить `git diff --cached --stat`, потом commit пат-спеком. Сообщение
  одной строкой с «Числа:» и «Доки:».
- MCP-граф: add_observations → sleep 3 → grep проверка на диске → git add
  mcp-memory.json → коммит с ним же.
- Промт-правила ловит check-prompt-rules: в строковых литералах питона
  (включая докстринги!) — никаких «обязан/должен»; писать описательно.
- _bootstrap.py патчит z20 при load_all по ТОЧНЫМ якорям — правка z20 в зоне
  якорей требует синхронной правки _bootstrap.py, иначе сервис не встанет.
- deploy-ask.sh только из HEAD=дерево; после выката restart
  1c-serene-ask@postgres + сверка md5 с /srv/1c. Не рестартовать сервис,
  пока идёт прогон.
- Фоновые ssh: дозоры disable_timeout=true; раннер nohup на окне; локальный
  ssh-клиент умирает по 600 с — это НЕ падение. Время серверов = UTC, не
  сдвигать (утренние «паники» 30.08 — неверные сдвиги).
- cursor-агенты: kill и запуск НИКОГДА в одной ssh-строке (pkill по литералу
  команды = самопопадание, 5 раз за смену 30.08); модель оркестратора k3,
  субагенты принудительно на дешёвой ([secondary_model] force = true).
- Гейт check-docs ловит пути вне репо в документах («файл не в дереве») —
  пути серверов писать словами, не как repo-path (дважды 01.09).
- Векторизация — только docs/EMBED_BULK_HOWTO.md §9; вектора бэкаплены (md5
  30b9663c… 31.08, d7aeb15e… 01.09).
- **Ничего лишнего:** не править файлы вне задачи, не ходить на другие
  серверы, не писать новые скрипты поверх базы, не запускать новых прогонов,
  пока идёт текущий.
- Владелец — парень; сроки без замера не обещать.

### КТО Я И ГДЕ РАБОТАЮ
Оркестратор /srv/1c (Kimi). 🔴 **Работаем ТОЛЬКО на okna** — это не деталь, а
граница сессии: ssh окно `-i ~/.ssh/id_ed25519_deploy -p 2202 root@gpu-erw.timpul.pro`
(движок SereneDB: `psql "host=127.0.0.1 port=7890 user=postgres dbname=postgres"`,
многооператорный SQL — stdin-файлом). **klient-1, klient-2, песочницы и любые
другие серверы НЕ ТРОГАТЬ — ни читать, ни писать, ни «проверить».** «postgres»
в DSN = имя базы SereneDB, СУБД PostgreSQL в контуре нет. GPU: 10.3.1.11
эмбеддер :8000 (НЕ трогать), 10.3.1.12 = 27B :8000 (жив).

### 🔴 TARGET.md — ПРАВИЛА, КОТОРЫЕ НЕЛЬЗЯ НАРУШИТЬ НИ В ОДНОЙ ПРАВКЕ
- **п.0/п.9 — СУТЬ, не слова** (владелец 02.09: «смысл пункта 0 не про слова,
  а про суть. Вникни»): прод-база скрыта НАРОЧНО — любая вещь, работающая
  только потому, что я знаю окна или сделал руками (env-флаг, ручной запуск,
  канон «потому что я так видел»), = нарушение. В коде — ни имени сущности,
  ни списка слов домена; в контуре — ни одного шага, который установка не
  делает сама (проверено 02.09: таймер такта в проде включает firstbuild
  ubuntu/packet/firstbuild_unit.sh:58; флаг ASK_WIKI_CHOICE удалён — коробка
  = бой). Проверка себя: «сработает ли это на базе, которую я никогда не
  видел, БЕЗ МОИХ РУК?» — нет → не делать.
- **п.12:** отказ и уточнение — не ошибка; ДОГАДКА — ошибка. Молчаливо
  подставленное окно/мера/сущность = догадка, запрещена.
- **п.21:** ответ → уточнение → отказ; отказ при наличии данных — дефект.
  Проверка, отвергшая верный ответ, — дефект проверки.
- **п.19/п.20:** считает и ищет база (SereneDB), модель — только смысл; своих
  скриптов поверх базы — нет; перед SQL — доки serenedb-docs.
- **п.10:** неверный ответ хуже отказа. Каждое ослабление проверки обязано
  быть доказано полным прогоном (wrong=0), не пробой.

### 🔴 ДЕЙСТВУЮЩИЕ РЕШЕНИЯ ВЛАДЕЛЬЦА (не открывать заново)
1. **«Один судья» (01.09, согласовано):** сторожей-судей выбора сущности нет.
   Все сигналы — ВХОДЫ ПУЛА карточек (kNN, словарь синонимов, носители меры,
   названная ось). Судья один — паспортная верификация каскада z21.
2. **«1 механизм, чинить НАЧАЛО» (01.09):** если на выходе плохо — проблема в
   начале цепочки (понимание → пул → выбор → верификация), а не в сторожах
   хвоста. Не разбирать вопросы по одному — правила общие, проверка = полный
   прогон 67, не пробы.
3. **Скилл-декомпозиция согласован (после стабильного базового прохода):**
   отдельный слой поверх ask: вопрос → LLM-разбивка на подвопросы (простой =
   одна часть) → точечные вызовы ask → сборка чисел КОДОМ (п.19) → журнал
   «вопрос → подвопросы → ответы» (наблюдаемость этапов). Не строить, пока
   точечный ответ нестабилен.
4. **П.0/п.9/п.12/п.21 TARGET — свято.** Ни одной подстройки под базу/вопросы;
   догадка (молчаливый assumed-окно) запрещена; отказ при данных — дефект.
5. **Словари синонимов — на нашем 27B (10.3.1.12:8000), DeepSeek из словарей
   убран** (31.08; usage-лог доказал, что DeepSeek не участвовал). Генератор —
   юнит 1c-wiki-alias через штатного агента OpenClaw; такт только компилирует
   уже лежащую таблицу.
6. **Пул из 1 карточки — верифицировать тоже** (02.09): struct_single
   пропускал паспортную проверку; теперь проверяется и одиночный кандидат
   (класс 5, c1c7d7a).
7. **UT забыт, работаем ТОЛЬКО на окнах** (02.09): правило про замер первой
   базы (REGRESSION_BASE1) неактуально. Строка в AGENTS.md «Куда что писать»
   осталась — файл root-owned, убрать владельцу.
8. **Такт — поднять автоматически, но СНАЧАЛА проверка армией по атомам**
   (02.09): история — такт жёг вектора, пересчёт = сутки GPU. ✅ ВЫПОЛНЕНО
   02.09: армия 8×2 дала карту (см. СДЕЛАНО ЗА ДЕНЬ 02.09), таймер включён
   (решение №13). Запись оставлена ради истории «почему армия».
9. **Приоритет (02.09): простые вопросы — верные ответы.** Скилл-декомпозиция
   («реально покупают» и др. составные) — позже, поверх стабильной точки.
   Отказы класса «одна ветка-неоднозначность» скилл НЕ лечит — их лечит
   исход A (класс 6), разобрано с владельцем 02.09.
10. **Простота решений, «не усложняй» — именно это сработало** (02.09,
    дословно владельца): один механизм вместо сторожей, удаление вместо
    заплаток, общее правило вместо разбора вопроса. Каждый шаг — сначала
    спросить «нет ли более простого пути».
11. **Текущая схема — лучшее за всё время: правильные ответы** (02.09,
    дословно владельца): «Любой отход от этой схемы — нарушение и
    усложнение». Схема: один путь через wiki (пул → pick → verify → лидер
    или clarify), судья один — паспортная верификация, без pre/post-заплаток.
    Новые механизмы вне этого пути не заводить; правки — только внутри него.
12. **Каждый анализ/план — через армию cursor: 3 агента на вопрос, круги до
    СХОЖДЕНИЯ** (02.09, дословно: «прогоняй через агентов, пока не
    сойдётесь. Не по 2, а 3 агента на 1 анализ»). Красная команда обязательна
    перед исполнением; исполнитель получает только сошедшийся план с
    оговорками всех кругов.
13. **Такт по таймеру разрешён и включён** (02.09): карта атомов (армия 8×2)
    доказала отсутствие флагов-убийц; таймер 1c-serene-pipeline@postgres.timer
    enabled. Каприз systemd: если сервис не запускался с загрузки, таймер не
    планируется (NextElapse=infinity) — лечится одним ручным тактом, дальше
    сам. В проде таймер включает firstbuild (ubuntu/packet/firstbuild_unit.sh:58).
