# Active Context

_Обновлено: 2026-07-22 (вечер)_

## Текущий фокус
**Фаза 2 — архитектура защиты УТВЕРЖДЕНА (решение владельца): вся read-only граница на Ubuntu, на Винде ничего кастомного.** Мастер-runbook: `docs/RUNBOOK_DEPLOY.md`; Ubuntu-обвязка: `ubuntu/1c-gateway/`.

Что установлено на стенде:
- MCP Toolkit v1.8.0 работает; Bearer-авторизация ✅; execute_query читает ✅.
- 🔴 Тумблеры не блокируют вызов — execute_code исполняется отключённым (подтверждено трижды).
- Read-only пользователь `ai_reader` НЕ подходит: тулкит требует привилегию на нативную компоненту, под read-only не стартует («непредвиденная ошибка»); под админом — ок. Значит границу ставим СНАРУЖИ 1С.
- Форк (вырезать execute_code) собран headless, но x86-ребилд падает при открытии — отложен.

Архитектура (проверена на живой системе 2026-07-22, соединение ПО IP, без туннеля):
Windows stock тулкит слушает сетевой интерфейс :6003 (под админом) ← роутер 192.168.56.1 пробрасывает :6003 ← Ubuntu whitelist-прокси `gateway.py` (upstream http://192.168.56.1:6003, только execute_query+read) ← мозг.
- `192.168.56.1:6003/health` отвечает и с LXC ✅; прямой execute_query по IP → реальные данные ✅.
- Через шлюз: execute_query → данные ✅; **execute_code отбит проксей, до 1С не дошёл** ✅.
Три слоя: сеть(только внутри сети LXC через .1 + Bearer)/прокси(deny-by-default)/язык-запросов(нет DML).
Транспорт РЕШЁН (прямой IP). SSH-туннель не используем (тулкит на нём зависал). Артефакты обновлены под IP: `ubuntu/1c-gateway/` (туннель-юнит удалён).

**braine РАЗВЁРНУТ на LXC (2026-07-22, exit 0).** Все 7 сервисов active: postgresql, open-webui(0.10.2), rerank-shim, oikb(0.3.6), tg-bridge, api(8090), kb-poll. **Бот @test1c_mcp_bot живой в Telegram** (polling, owner 5949699699). Эмбеддер DashScope, реранкер qwen3-rerank-шим, KB `1c-kb` (OWUI id b03045e4). Ollama не ставили (install.sh пропатчен), пины сработали. `.env` — 44 переменные из credentials + секреты. Наш `1c-gateway` (:6010) тоже развёрнут и active.

🔴 **Устойчивость тулкита — КОРНЕВАЯ ПРИЧИНА найдена из кода (`docs/TOOLKIT_TRANSPORT_ROOTCAUSE.md`).** Встроенный сервер тулкита обслуживает ВСЕ запросы (включая /health) через один клиентский idle-обработчик 1С `ЦиклОпроса` (~1 сек, одна команда за цикл); нативка cpp-httplib блокирует рабочий поток до ответа 1С. Два отказа: (1) пачка/конкурентность → CLOSE_WAIT, исчерпание пула; (2) 🔴 **любой модальный диалог в сессии 1С полностью останавливает idle-обработчик** → сервер мёртв до закрытия окна (это и есть текущий wedge — даже одиночный запрос висит). Потолок ~1 req/s.
**Вывод:** встроенный сервер НЕ для прода. **РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-07-22): прод-канал = OData через IIS** (служба Windows, авто-старт, без idle-обработчика/модалок).

## Прод-канал OData — В РАБОТЕ (2026-07-22)
Сделано headless: IIS включён (+ISAPI, ребут выполнен владельцем, W3SVC Running/Automatic); база `buh_test` опубликована через webinst (`C:\inetpub\1c`, app /1c, пул DefaultAppPool 32-bit, права на файловую базу выданы); OData включён в `default.vrd` (enable="true"). **Эндпоинт `http://localhost/1c/odata/standard.odata/` живой — отвечает 401** (auth нужна; зависаний/модалок НЕТ — легальность не заблокировала). Встроенный тулкит и его сессия закрыты (ребут) — больше не нужны.
**Осталось:** (1) задать состав OData (`УстановитьСоставСтандартногоИнтерфейсаOData`, нужны админ-права), (2) read-only пользователь для OData, (3) тест чтения, (4) переключить наш шлюз на OData, (5) проброс на роутере .1 для порта IIS (80). Блокер: нужен креденшел базы (владелец создал admin+ai_reader, пароли у него).

**Следующий шаг:** владелец рестартит сервер тулкита → Claude валидирует gateway→тулкит с LXC → затем ETL 1С→KB-репо (наполнить money/1c-test выгрузками, чтобы боту было что отвечать).

Параллельно:
- **Ubuntu LXC** — дом braine, изучение завершено (`docs/UBUNTU_SETUP.md`). Входные почти собраны (KB-репо, telegram-бот); ждём команду «разворачивай» + telegram-id.

Продакшн — 1С:ERP «производство» у клиента; к нему не подключаемся, пока всё не отработано на стенде.

## 🔴 Правила (2026-07-22)
- На серверах (LXC и Windows) ничего не менять по собственной инициативе — только по явной инструкции владельца. Читать/инвентаризировать можно.
- Репо braine — read-only, «только как пример»; правки запрещены.
- Перед любым изменением на машине с 1С — бэкап (`windows/scripts/backup-1c.ps1`).

## Состояние машин (инвентаризация 2026-07-22, детали в techContext.md)
- LXC `lxc-claude-1c` 192.168.56.42: Ubuntu 24.04, 6 CPU/62 GB/238 GB свободно, пустой. Подходит под braine 1-в-1 (референс braine — такой же непривилегированный LXC, поэтому без Docker).
- Windows 10.8.0.58: Win11, 2 CPU/8 GB/36 GB свободно; C:\1c\{repo,bases,backups,distr,logs}; 1С НЕ установлена.

## Блокеры (все — на стороне владельца)
1. **Развёртывание braine на LXC** — входные почти собраны: ✅ KB-репо `money/1c-test` (gitlab-real.unde.life, id 95, main; креды `credentials/gitlab-1c-test.env`); ✅ Ollama не ставим («standby плохо себя показал») — эмбеддер сразу DashScope, fallback'а нет; ✅ Telegram-бот создан владельцем: **@test1c_mcp_bot** (токен в `credentials/telegram-1c-bot.env`). Осталось: telegram-id владельца для allowlist (кандидат из braine-оригинала: 5949699699 — подтвердить), пины open-webui==0.10.2/oikb==0.3.6 (беру по рекомендации) — и явная команда «разворачивай».
2. **1С на Windows-стенде** — дистрибутив платформы 8.3.25+ в `C:\1c\distr` + тестовая база ERP + лицензия. Учебная версия НЕ подходит (нет внешних компонент/HTTP-сервисов, лимит 2000 строк).
3. Решения §9 плана: DeepSeek-приватность данных 1С, приоритетные разделы ERP, окно ночной выгрузки, OData/IIS.

## Ключевая связка (из изучения braine)
Наш ETL 1С (холодный контур плана) должен писать выгрузки как md-файлы + md-таблицы в KB-репо → braine индексирует их и отвечает с цитатами, агрегаты считает SQL-скилл. Braine закрывает «горячий контур» и бот-интерфейс целиком; наша работа — ETL 1С → KB-репо + golden set под 1С-домен.
