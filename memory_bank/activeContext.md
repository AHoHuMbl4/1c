# Active Context

_Обновлено: **2026-08-28 день** (К9-фаза-1 в git; И2/И3/К4/И4 закрыты; подгонка
K6-двойки откачена владельцем). Здесь — только живое. История — в
[`progress.md`](progress.md); стадии — в [`docs/TARGET_STATUS.md`](../docs/TARGET_STATUS.md)._

---

# ⏭ С ЧЕГО НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

🔴 **Следующий шаг — К9 фаза 2, шаг 1: приоритет внутри event-яруса.**
Фаза 1 (`2e42e61`) в git: разбор отдаёт `action_class` (event/object/none) +
`action_axis` — модель, без списков слов; event-ярус первый ключ `rank_key_v2`;
event+count → форма distinct; `event_axis_lock` в точке выбора сущности.
Остаток (по живым пробам :8096): (а) внутри event-яруса лидером стал
`кассовыеордера`, а не `реализациятмц` — замерить ключи (live/kind_ok/
n_dated), ввести структурный признак «осевой регистр с nums» (не касса/книга
НДС); (б) count при живой оси уходит в clarify мер — distinct-атом должен
собираться ДО детектора развилки (по образцу rank_defer K6b). Шаги и приёмка —
`docs/PLAN_TO_TARGET.md` К9 (там же К8-декомпозатор и К10-нарезка модулей).

🔴 **Критерий правильности (владелец 28.08, записан в план):** решение верно,
если опирается на смысл из модели-разбора + метаданные базы (вид объекта,
`search_refcols`, словарь из базы) + структурные правила форм. Лексика имён и
списки слов в коде выбор НЕ решают. Прецедент отката — `HOW_NOT_TO §3.96`
(списки створов «прод/покуп» + ветка под два вопроса; правило-маркер: список
слов живого языка в правке = подгонка = откат).

## Замеры дня (доказано числом)

| Факт | Числа |
|---|---|
| И2 client-gold 48 на бою ×3 (run2–4) | **48/48 kind стабильны, 0 расхождений** (И3 ✅); ans 10 / fig 25 / no_data 8 / clr 4 / err 1; эталоны (12): match 4 / mismatch 8 |
| Разбор классов И2 → К4 | `docs/I2_ACCEPTANCE_CLIENT_GOLD_2026-08-28.md`: ~16/25 figures по контракту, ~6 дефект; такт 4–6, код 12–14, К8 3–4 |
| Ambiguous-26 на бою | **7/18** (было 5/18); провалы: no_data при живых именах, угадывание меры, «2022»→answer |
| Лаг корпуса (живьём) | build_ts **94,3 ч** / mart_changed_ts **19,9 ч** — Д5, такт мёртв с 24.08 |
| К9-ф1 замки | test_action_class **13/0**; серия 51/0+30/0+92+47+13/0+27/0+30/0; bench v2_lead **19/23** регресс **0** |
| Пробы К9 (:8096) | «реально покупают»→**event**/ось; «было за месяц»→**event**; «всего клиентов»→**object**; ранги целы (лидер; топ-3 = количество) |

## Коммиты дня (всё в origin, HEAD `f4885f9`)

`ddfe792` малые классы (диагноз) · `7aec082` К4-разбор И2 · `bb41bef` И4
реестр наборов · `dc8b5e2` откат подгонки + К9/К8 в плане + §3.96 ·
`2e42e61` К9-ф1 · `f4885f9` срез TARGET_STATUS. Срезы статуса: «28.08 день» и
«28.08 ночь» в `docs/TARGET_STATUS.md`.

## Механика (проверено живьём этой сессией)

- **Кандидат :8096** = код HEAD + К9-ф1 (выкачен scp → `/tmp/serene_ask_k3f2.py`
  + `/tmp/entity_rank_v2.py`, лончер `/tmp/k3f2_rel.py`, env
  `/tmp/k3f2.environ.raw`; рестарт: kill PID → `nohup python3 /tmp/k3f2_rel.py`).
  **Бой :8091 — БЕЗ К9** (HEAD до `2e42e61`); выкат К9 в бой — только после ф2
  и полной приёмки.
- Туннели на деве: 38091→8091 (бой), 38096→8096 (кандидат), 37890/17890→7890
  (SereneDB okna; 7890 на деве — СВОЯ база, не okna!).
- Пароль psql — только с ОКНА: `ssh … 'grep -m1 ^PGPASSWORD= /etc/1c-mcp-reports.env'`
  (снять кавычки; в дев-файле ДРУГОЙ пароль — курсоры на этом падали).
  Токен: `grep -oP '^ASK_TOKEN=\K\S+' /etc/1c-serene-ask.env` (окно).
- Приёмка К9-формулировками: `bash /tmp/k9_accept.sh` (на окне; событийные /
  НЕ-торговый / без глагола / ранги). Пробы разбора: `/tmp/probe_ac.py` (окно,
  PYTHONPATH /opt/1c-mcp-reports).
- Раннер И2: `work/acceptance/i2_client_gold_run.py` (env ASK_URL/ASK_TOKEN,
  артефакты `work/acceptance/runs/i2-validate/runN/`).
- Грабли: `git add X && git commit` в ОДНОМ вызове — PreToolUse-хук режет вызов
  целиком (add не проходит). Отдельными вызовами. MCP-граф пишет асинхронно —
  после `create_entities` перечитать/add заново. Снайпер коммита иногда падает
  по квоте — повторить коммит.

## Что мешает (живьё)

1. **Д5/такт**: без `$metadata` в пакетах (Win-агент) — внешн., отложено
   владельцем («работаем с теми данными, которые есть»).
2. **К9-ф2** — см. следующий шаг.
3. **Параллельная сессия**: 28.08 днём работала ВТОРАЯ сессия (её задачи:
   entity_form-фикс «polluted pool», скорер 20/25 после enforce-eager, Д5-мета,
   И1-ambiguous). Её замок уже в дереве; при возобновлении — сверять
   `git log`/`git status` до правок, чтобы не затереть друг друга.
4. klient-1 / Win-агент — «потом» (слово владельца).

🔴 **приёмка — только okna**; GPU в норме (26.08, слово владельца); канон GPU
vSwitch `10.3.1.11/.12` (`docs/NETWORK.md` §2.1); режим оркестрации —
`docs/ORCHESTRATION_CURSOR.md`; реестр наборов — `docs/GOLD_SETS.md` (И4).

История дней — `progress.md`.
