# Active Context

_Обновлено: **2026-08-28 вечер, перед compact**. Здесь — только живое.
История — в [`progress.md`](progress.md); стадии — в
[`docs/TARGET_STATUS.md`](../docs/TARGET_STATUS.md)._

---

# ⏭ С ЧЕГО НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

🔴 **Периметр (слово владельца): ТОЛЬКО окно (gpu-erw.timpul.pro), всё НА
нём, БЕЗ туннелей, других хостов не трогаем, «параллельных сессий» нет.
Свежесть 1С (Win-выгрузка) — временно отброшена владельцем. klient-1 —
потом (вопросы уже в git: `docs/KLIENT1_LIVE_QUESTIONS.md`). Выкат в бой —
РАЗРЕШЁН («остальное делай»).**

## В полёте (проверить первыми)

1. **С5-генератор на окне** (`systemctl is-active 1c-wiki-alias@postgres`;
   `psql … -tAc "SELECT count(*) FROM alias_okna_c5"`, DSN
   host=127.0.0.1 port=7890 user=postgres, пароль
   `grep -m1 ^PGPASSWORD= /etc/1c-mcp-reports.env`). На момент compact
   ~191/258, ETA ~16:10. После финиша — ЗАМЕНА по цепочке: сверка c5 vs
   `search_entity_alias` → MERGE подтверждённого → `VACUUM (REFRESH_TABLE)`
   alias_idx → `ts_lexize('search_dict_syn','покупатели')` → контрагенты →
   живая проба «сколько покупателей было за этот месяц» → **43 · Контрагенты**
   (эталон SQL проверен) → AB_PROBE 8/8 → поднять `maxTokens` **8192→16384**
   в `/home/undebot/.openclaw/openclaw.json` (3 места, БЭКАП) + рестарт
   гейта `systemctl start 1c-openclaw-gateway-restart`.
2. **К10-нарезка (курсор)**: жив — `pgrep -f prompt-k10`, лог
   `.claude/state/cursor-run-k10.log`, отчёт будет
   `.claude/state/report-k10.md`. В ДЕРЕВЕ незакоммиченная нарезка
   `serene_ask.py` → пакет. Приёмка: ВСЕ замки ДО=ПОСЛЕ числами,
   ast-сравнение тел функций, `python3 -c "import serene_ask"`,
   CODE_MAP_ASK/MAP синхронно, скорер на окне — оркестратор. Коммит
   пат-спеком; только ПОСЛЕ него дерево = HEAD.
3. **Выкат в бой :8091** (после п.1+2): `bash ubuntu/serenedb/deploy.sh`
   требует дерево=HEAD + свежую отметку AB_PROBE (ставится прогоном на
   кандидате :8096 НА ОКНЕ: `AB_PROBE=okna ASK_URL=http://127.0.0.1:8096/ask
   AB_MARK_DIR=/tmp/i2 python3 /tmp/i2/ubuntu/serenedb/ab_scorer.py`,
   отметку потом scp на дев в `.claude/.probe-okna-last-run`). После
   выката: `AB_CONTOUR=okna` на бою (:8091) + контрольные формулировки +
   контур-24. Выкат несёт: К9 ф1–ф9, Э1а/Э1б, парсер-хотфикс, С5-пайплайн,
   К10. Э1а .sql на живой 26.07.3 не гонялись — первый такт после выката
   смотреть journalctl.
4. Доки хвостом: Э1/В1 итоги в PLAN_TO_TARGET/TARGET_STATUS, срез дня.

## Достижение дня (главное)

- **К9 ф1–ф9 в git (HEAD `e68d057`)**: «сколько клиентов реально покупали
  за последний год» → **`141 · Контрагенты`** — точное совпадение с
  эталоном (K6-двойка закрыта механизмом: event-путь кодом, ось по kind
  из словаря базы, DISTINCT, исходы A/B/C, переспрос периода без окна —
  решение владельца). Замки 48/0, bench 20/23, AB 8/8 (на окне).
- К8 скилл-декомпозатор (`f2028ad`): 29/0, живое сошлось с эталоном К2.
- Э1а/Э1б (`78d88c0`): wiki 9→3 psql, embed ×3 →6–9, solr без Python.
- С5-пайплайн (`7309646`): cold start + периодическое доучивание словаря
  (REASK_EVERY=0 default), 4 находки снайпера закрыты кодом.
- Парсер-хотфикс (`e68d057`): обрезанный по токенам JSON не теряет пачку
  (спасено 7+137 из живого ответа).
- Открытые хвосты К9: «покупателей за месяц» ждёт С5-замены словаря
  (сейчас 76=касса); «валюты»→плансчетов (К4-класс, card-count признак
  есть); подпись оси на mtd-пути.

## Механика (выучено живьём)

- **Всё на окне через ssh** (ключ `/home/claudedev/.ssh/id_ed25519_deploy`,
  порт 2202). На окне в `/tmp/i2`: раннеры (i2_client_gold_run.py,
  client-gold-okna.tsv, ab_scorer.py + наборы, ubuntu/serenedb/копия,
  work/k6-rank-v2/bench.py + tsv). Кандидат :8096 = `/tmp/serene_ask_k3f2.py`
  + лончер `/tmp/k3f2_rel.py`; рестарт: kill PID слушателя (ss -tlnp) →
  setsid nohup. НЕ доверять health без сверки PID/времени старта (урок дня).
- `git add` и `git commit` — СТРОГО разными вызовами (хук режет цепочки);
  MCP-граф пишет асинхронно → sleep 2 + re-add. Снайпер видит СТАРЫЙ индекс
  при неудачном add. Проверка «не подгонка»: список слов живого языка в
  правке = откат; снайпер за день поймал 6 находок, все закрыты кодом.
- Bench на окне: `cd /tmp/i2 && SERENEDB_DSN="host=127.0.0.1 port=7890
  user=serene_ro dbname=postgres" PGPASSWORD=… python3 work/k6-rank-v2/
  bench.py` — 50 с, v2_lead 20/23 PASS стабильно.
- Client-gold 48 на окне: 3 match/9 mismatch/35 pending; 4 мисматча —
  свежесть (такт 24.08), 1 — мой 503 при параллельной нагрузке, 2 — класс
  переспроса периода (закрыт ф8), 1 — валюты (К4).

## Что мешает

1. Свежесть 1С — отброшена владельцем («временно»); 4+ мисматча client-gold
   из-за неё, эталоны относительных периодов несверяемы.
2. klient-1/Э2 — внешне, потом (слово владельца).
3. Такт мёртв с 24.08 — AB_PROBE и пробы идут против корпуса 25.08.

🔴 приёмка — только okna; GPU-хост 49.13.97.101 (vLLM Qwen3.8-27B,
контекст 64k, maxTokens у агента 8192→16384 по С5) — не трогать без слова.
Реестр наборов — `docs/GOLD_SETS.md`; C5-расчёт — `docs/C5_ALIAS_PIPELINE.md`.

История дней — `progress.md`.
