# Активный контекст

## С ЧЕГО НАЧАТЬ (срез 31.08 ~09:50 UTC — ПОСЛЕ ПОТЕРИ ВЕКТОРОВ; читать целиком)

### ПРАВИЛА ВЛАДЕЛЬЦА (действуют, нарушены мной — больше нет)
1. 🔴 **Векторы не пересчитывать.** Вектор живёт, пока жив контент. Любая
   потеря векторов = мой дефект, а не «волна». Гейт вектор-бюджета обязателен
   ДО любой записи в корпус.
2. 🔴 **Бэкап векторов перед любой рискованной операцией**, с точной датой/временем.
   Стоит: `store.db.bak-20260831-094105` (md5 в VECTOR_SNAPSHOT-20260831-094105.txt).
3. 🔴 **Тесты/приёмки без внедрённой Вики — бесполезны.** Сначала живая проверка
   wiki-цикла на вопросах владельца, потом любые прогоны.
4. 🔴 **Работаем ТОЛЬКО на okna и с вопросами владельца** (67 gold-канон
   `ubuntu/serenedb/client-gold-okna.tsv` + спека
   `/tmp/claude-1002/.../scratchpad/okna-live-questions.md`). Никаких синтетических.
5. Юнит такта `1c-serene-pipeline@postgres` ОСТАНОВЛЕН (`systemctl stop`) —
   НЕ запускать до живой пробы гейта. 11 авто-рестартов сожгли векторы
   повторно (урок: рестарт без идемпотентности = множитель ущерба → HOW_NOT_TO).

### ЧТО СЛУЧИЛОСЬ (деньги, сожжённые мной)
Причина-корень: вектор привязан к doc_hash(текст), форма меняет текст →
хэш → вектор умирает, хотя контент тот же. Три случая + сегодняшний runaway:
| Дата | Потеря | Причина |
|---|---|---|
| 28–29.08 | 5,4 млн | такт пересобрал корпус во время досчёта |
| 30.08 | 1,58 млн | волна смены $metadata (О4) |
| 31.08 | 396 тыс. корпус | перенос слабый (уникальные refs), спасено 92,5 тыс. |
| 31.08 | ~394 тыс. резолвер | пересборка REPLACE-ем: живых было ~677 тыс. (30.08), стало 283 088 |
| 31.08 | ещё ~1 млн корпус | 11 авто-рестартов такта, каждый проход пережигал |
Итого очередь к пересчёту: **~3,12 млн строк = ДВЕ РАЗНЫЕ таблицы, не подавать
одним числом** (замер 31.08 09:57 UTC):
- **корпус** search_corpus: 1 665 427 строк, живых 361 325, NULL **1 304 102**
  — это пережог 31.08, те самые «считали 10 раз»;
- **резолвер** resolver_index: 2 100 349 строк, живых 283 088, NULL **1 817 261**
  — из них потеря REPLACE ~394 тыс., остальные ~1,42 млн **никогда не считались**:
  такт отщипывал по ~5,6 тыс. за прогон (журнал 30.08: session_rows 5568/5616).
Корпус ≈ 4 ч одной карты; резолвер (короткие строки) — решение владелицы:
считать целиком / отложить (текстовый поиск слов и так работает).
Суммарно сожжено моими ошибками за период ≈ 10,1 млн эмбеддингов ≈ 28–31 GPU-час.

### ЗАЩИТА (в git f4ce011, НЕ ВЫКАЧЕНА на окно)
- **Вектор-гейт**: corpus_merge §1-тер — стоп ДО записи при потере emb вне
  карты > MERGE_VECTOR_LOSS_TOLERANCE (0.5%, env; обход MERGE_VECTOR_LOSS_BYPASS=1
  с записью в search_quality). Замок test_vector_budget_gate.py 44/0.
- **content_hash** (corpus_init макросы corpus_doc_bmap/corpus_content_hash,
  строит corpus_build): хэш от канона «колонка→значение» без
  DataVersion/__metadata/navigationLinkUrl; смена формы без смены значений =
  вектор живёт сам. row_key/doc_hash НЕ тронуты.
- **Резолвер/карточки MERGE идемпотентно** (resolver_build, entity_card_build,
  wiki_card_build): MATCHED не трогает emb; повторный такт = no-op. Замки
  test_resolver_merge_emb.py 25/0, test_entity_wiki_card_merge_emb.py 25/0.
- Все замки слияния зелёные: 44/0, 29/0, 25/0, 61/0, 20/0, 31/0.

### ИДЕЯ, КОТОРУЮ НЕ МОГУ ДОВЕСТИ ДО ЖИВОЙ ПРОВЕРКИ (слова владельца)
«Вопрос — LLM с помощью Вики понимает, что хотел спросить юзер, переспрашивает,
если надо [сразу с чипами кандидатов]. Отдаёт на поиск и собирает кандидатов.
Каждый кандидат проверяется да/нет — не просто по реранкеру, но и с большим
контекстом, который собирается при помощи Вики. Остальное не сработает в принципе.»
Состояние: ВСЕ ступени написаны и задеплоены (z21 выбор по карточкам;
clarify ≥2 options — Б3-у; wiki_passport.sql + wiki_verify_candidates —
паспорта wiki_pages.body+оси+меры+отличия, один вызов ≤5, исходы кодом:
лидер/переспрос-чипы/«ни один»). НЕ ПРОВЕРЕНО ЖИВЬЁМ ни разу — конвейер
свежести ломался под ней всё это время. ПРОВЕРКА = первый приоритет после
зелёного такта: вопросы владельца («сколько движений в регистре реализациятмц»,
«сколько организаций», «сколько за месяц», склад) глазами в diag:
wiki_hybrid_pick → verify → options → верный источник.

### ПОРЯДОК ДЕЙСТВИЙ (строго) — НОЧЬ 31.08→01.09, владелец: «проснусь — всё готово»
0. ✅ СДЕЛАНО (31.08 день): Restart=no + таймер погашен; защита выкачена; векторы
   не теряются (bulk только заполняет NULL); уроки дня: один процесс bulk
   (аттач-война), PROGRESS_SEC=600 (не 86400 — петля), скорость мерить
   переносами кругов, НЕ теориями про «очереди». Владелец парень; доступ
   ssh root@178.63.211.188 / 49.13.97.101 ключом ~/.ssh/id_ed25519_deploy
   (проверено живьём). Документ GPU_SERVERS.md (владельца) — чеклист §8.
1. ✅ ИДЁТ: bulk корпуса 2 карты (17:07 UTC, 6 потоков, EMBED_HOSTS обе,
   PROGRESS_SEC=600). 973 734/1 665 427 на 19:06; корпус ~23:30 UTC.
   Дозор-фоновик будит меня при завершении. НЕ трогать процесс руками.
2. Корпус готов → контроль count(emb) ≈ 1 657 663 (минус service ~7 703) →
   СВЕЖИЙ бэкап: ssh окно 'cp /var/lib/serenedb/engine_duckdb/store.db
   /var/lib/serenedb/engine_duckdb/store.db.bak-$(date -u +%Y%m%d-%H%M%S) &&
   md5sum + psql снимок > /var/lib/serenedb/VECTOR_SNAPSHOT-*.txt'.
3. Резолвер: ТОТ ЖЕ канон §9, аргументы из build.sh:
   EMBED_HOSTS обе карты, THREADS=6, PROGRESS_SEC=600, WORK_DIR,
   ./embed_bulk.sh resolver_index 'substr(value,1,20000)'
   'table_name,column_name,value' (ROWS_WHERE не нужен — service уже
   отфильтрован в сборке резолвера). ~1,8 млн коротких строк ≈ 1-2 ч.
4. Резолвер готов → бэкап снова (п.2) → вернуть 27B на .12 (49.13.97.101):
   ОТДЕЛЬНЫМИ командами: pkill -f embed_server.py, затем
   start_llm.sh (оба скрипта лежат НА GPU-ХОСТЕ .12 в /home/ubuntu/{llm,qwen}_services,
   это инфраструктура владельца вне репо — см. GPU_SERVERS.md §3.4/§8), затем
   curl -sf -H "Authorization: Bearer uAI_…"
   http://49.13.97.101:8000/v1/models. Эмбеддер на .11 НЕ ТРОГАТЬ (вопросы
   бота). pyannote не поднимать.
5. Снять MERGE_VECTOR_LOSS_TOLERANCE=0 из /etc/1c-serene-pipeline-postgres.env
   (вернуть из .bak-probe без строки пробы). Запустить такт
   systemctl start 1c-serene-pipeline@postgres (Restart=no уже стоит).
   Контроль: search_quality vector_loss_gate ≈ 0, corpus count/emb не упал,
   резолвер не потерял векторы. Падение такта — journalctl, не паника.
6. Вики-цикл на вопросах владельца через :8091 (ASK_TOKEN из
   /etc/1c-serene-ask.env): «сколько движений в регистре реализациятмц»,
   «сколько организаций», «сколько продали за месяц», склад — в diag:
   wiki_hybrid_pick → verify → options → верный источник.
7. И2 67×2: work/gold/i2_runner.py run --tsv ubuntu/serenedb/client-gold-okna.tsv
   --path engine --path web --reshoot-rules work/gold/reshoot-rules-okna.json,
   env I2_WEB_MODEL=openclaw, ASK_TOKEN /etc/1c-serene-ask.env, WEB_TOKEN
   ~/.openclaw-web/openclaw.json (gateway.auth.token).
8. УТРОМ: один отчёт владельцу: векторы (корпус/резолвер, числа), бэкапы md5,
   27B жив, такт зелёный, вики-цикл живой/нет, И2 «уверенно неверных X из 67».
9. Хвосты (если ночь тихая): О6 срез 2, §6 бенч, веб-вердикты.
   НЕ делать: новые схемы векторизации, нативные пробы (HOW_NOT_TO §3.103),
   два bulk одновременно, трогать .11, обещать сроки без деления остатка
   на замеренный потолок (77k симв/с на 2 карты).
1. После завершения bulk корпуса: контроль переноса (count(emb) в корпусе),
   СВЕЖИЙ бэкап store.db с md5 + снимок счётчиков (файл VECTOR_SNAPSHOT-*),
   затем bulk резолвера 1 817 261 NULL (аргументы: resolver_index,
   'substr(value,1,20000)', 'table_name,column_name,value').
2. Снять MERGE_VECTOR_LOSS_TOLERANCE=0 из /etc/1c-serene-pipeline-postgres.env
   (проба уже не нужна, штатный порог 0,5% в build.sh).
3. ЖИВАЯ ПРОБА ГЕЙТА: запуск такта → контроль: потеря 0 (content_hash стыкует),
   corpus count/emb не падают, vector_loss_gate в search_quality ≈ 0.
   Не проходит — такт не пускать, чинить. Такт — ТОЛЬКО после полного bulk
   (порядок работ: векторизация и такт не одновременно).
4. Живая проверка ВИКИ-ЦИКЛА на вопросах владельца (см. ИДЕЯ) — глазами, diag:
   wiki_hybrid_pick → verify → options → верный источник.
5. Полный И2 67×2 живыми эталонами (work/gold/i2_runner.py --reshoot-rules,
   I2_WEB_MODEL=openclaw, WEB_TOKEN из ~/.openclaw-web/openclaw.json gateway.auth).
6. К8 → автоприёмка. Вопрос владельцу: склад 1318 vs no_data.
7. Хвосты: О6 срез 2 (z10×13 z11×23 + 5 падений), §6 бенч, веб-вердикты, О4-миграция.

### КЛЮЧЕВЫЕ ФАКТЫ ОКНА (ssh -i ~/.ssh/id_ed25519_deploy -p 2202 root@gpu-erw.timpul.pro)
- База: psql "host=127.0.0.1 port=7890 user=postgres dbname=postgres", SQL stdin-файлом.
- Эмбеддер ОДНА карта: 10.3.1.11:8000 (qwen, ключ uAI_1IqA…); .12:8000 = 27B.
  /etc/1c-embed.env вычищен (мёртвый .12:8002 убран), секреты self-heal стоят.
- ask: :8091 прод / :8092 стейджинг, оба на слитом коде с wiki-ступенями;
  ASK_TOKEN из /etc/1c-serene-ask.env. Веб-гейт :18801 (openclaw --profile web).
- Юнит такта ОСТАНОВЛЕН. Такт-SQL на окне ЧАСТИЧНО старый → выкатить (п.1).
- Деньги/цифры очереди: см. «ЧТО СЛУЧИЛОСЬ» выше; снимок 09:41 UTC.
- Git: HEAD f4ce011 = origin/main (все замки зелёные). CHANGELOG 31.08 полный.
