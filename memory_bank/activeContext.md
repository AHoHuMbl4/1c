# Active Context

_Обновлено: 2026-07-22_

## Текущий фокус
**Фаза 0 завершена, фаза 1 — на паузе до инструкции владельца** (план: `docs/PLAN.md`). Продакшн — 1С:ERP «производство» у клиента; весь тест — на стенде владельца. Обе машины доступны по SSH и инвентаризированы (см. `techContext.md`): LXC — Ubuntu 24.04, 6 CPU/62 GB/238 GB, пустой; Windows — Win11, 2 CPU/8 GB/36 GB свободно, **1С не установлена**.

## 🔴 Правило (2026-07-22)
На серверах (LXC и Windows) НИЧЕГО не менять по собственной инициативе — только по явной инструкции владельца. Читать/инвентаризировать можно. Владелец сказал: «я дам точную инструкцию что делать на убунту» — ждём её.

## Сделано на Винде (2026-07-22, по явной инструкции владельца)
Каталоги C:\1c\*, git-идентичность, клон репо в C:\1c\repo, бэкап-скрипт готов. Git уже стоял (2.55). Winget нет, интернет есть, AV-помех нет.

## Блокеры
- Фаза 1 (LXC): ждём инструкцию владельца по развёртыванию на Ubuntu.
- Фаза 2 (Windows): **нужен от владельца дистрибутив платформы 1С 8.3.25+ (в C:\1c\distr) + тестовая база ERP + лицензия.** Учебная версия не подходит (нет внешних компонент/HTTP — ломает MCP Toolkit; лимит 2000 строк).
- Бот-слой: боевое решение владельца, инфа позже — OpenClaw снят. Открытые решения §9 плана: DeepSeek-приватность, приоритетные разделы ERP, окно выгрузки, OData/IIS.

## Последние изменения
- 2026-07-22 — репозиторий создан, memory_bank заложен, первый пуш в `origin/main`.

## Следующие шаги
- Целевые машины записаны (см. `techContext.md`): проект → LXC `lxc-claude-1c` (Ubuntu, `192.168.56` — уточнить полный IP), 1С → Windows `ssh unde@10.8.0.58`. SSH-ключ Claude сгенерирован (`~/.ssh/id_ed25519_1c`), публичная часть передана владельцу.
- По команде владельца: проверить подключения к обеим машинам → дописать факты в `techContext.md`.
- Получить от владельца первую содержательную задачу по 1С.
- Уточнить: версия платформы, конфигурация-основа, формат хранения кода (выгрузка/EDT/epf) → обновить `techContext.md` и `projectbrief.md`.

## Активные решения и правила
- Работа строго внутри папки `1c/`.
- Каждое изменение = коммит + push.
