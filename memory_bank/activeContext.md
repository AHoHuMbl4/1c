# Активный контекст

## С ЧЕГО НАЧАТЬ (срез 01.09 ~23:45 UTC, НОЧЬ; читать целиком; после compact)

### 🔴 ГЛАВНАЯ ЗАДАЧА — АРМИЯ: чистка старых заплаток ask-пути (в работе)
Владелец (01.09, ночь): «много косяков из-за того, что не подстроено под новую
схему. Старые заплатки срабатывают, и из-за этого неверно» → «делай всё до конца
без меня». Армия ОТРАБОТАЛА: фаза 0 (4 карты map0-A/B/C/D) → фаза 1 (81 кусок
× 2 независимых cursor-прохода = 162 прогона, отчёты .claude/state/p1-*) →
фаза 2 (сводки map2-A/B/C/D). Дальше я удаляю классами: каждый класс = cursor
задача → замки → коммит → выкат → полный прогон 67. 🔴 Квота Kimi subagent
исчерпана ночью (402 Insufficient Balance) — обёртки не работают, cursor
запускаю НАПРЯМУЮ Bash-фоном: `cd /srv/1c && timeout 3000 cursor-agent -p
"$(cat .claude/state/prompt-rmN.md)" --force --model auto --output-format
stream-json | tee .claude/state/cursor-run-rmN.log`.

### СДЕЛАНО ЗА НОЧЬ (все коммиты в origin/main)
- **L13** (до чистки): 29/33/4/1. 4 wrong разобраны пробами: (1) «позиций
  номенклатуры на складах» → 2402 прайс и (2) «позиций на всех складах» →
  78540 регистр продаж: паспорт верификации НЕ видит «на что НЕ отвечает»
  (not_enough_for не входит в паспорт); (3) «реально покупают» 366 vs 145 —
  нет механизма count distinct с фактом покупки (скилл-декомпозиция, не ночь);
  (4) «расчетныеначисления за 30 дней» — текст «0», но nums пустые (ноль окна
  не доезжал полем).
- **Класс 1 (f8448d6, −821):** мёртвый легаси (pick_entity, PICK_SYS,
  try_balance/event/count_theme, wiki_stock_canon_takeover, ветки
  register_count_locked/event_count_period_assumed — флаги нигде не
  присваивались, и др.). Каскад перенесён НА ДИСК z20 (~:2966), патч из
  _bootstrap снят — код на диске = код в бою. test_zone_names_resolvable теперь
  сам применяет _patch_z20_wiki_primary. Ловушки армии: rank_period_clarify_
  applies и catalog_count_src ЖИВЫЕ (не удалены).
- **Класс 2 (80c0731, −126):** сняты ВСЕ pre-wiki обходы: entity_form
  pre_entity, ранний event-period clarify, K6R clarify-return, все ранние
  stock-возвраты (subject/warehouse/bridge/named no_data ×3). Удалены
  stock_balance_named_no_data, k6_dual_atom_clarify_return. Инертные (висят
  только на тестах): warehouse_clarify, balance_bridge_clarify,
  stock_subject_needs_clarify — отдельным решением. Чипы clarify делает
  каскад (z21 mk_opts) и fork-исходы.
- **Класс 3 (cd78ada, −106):** align_picked_to_terms снят (подменял верифи-
  цированный выбор); regex-fallback pick (первое число из битого JSON) →
  честный none/model_unparseable; ноль пустого окна всегда в figures/atom
  (чинит wrong №4). ОСОЗНАННО оставлены: measure_in_kin (замеры 30.07, эталоны
  в ТЧ), stop2/signals_disagree (заглушены парой wiki_verify==picked),
  verify degraded→pass (п.21), z13 подстроки регистров (базлайн О6).
- Замки после каждого класса зелёные (99/110/65/35/45/162/130/27/51/53/14/24
  + period_empty 30/0, b9 8/0). Прежние красные (сверены на чистом HEAD, НЕ
  мои): k4_axis 1, k4_guess 3, sales_canon_prefer 1, action_class TypeError.

### ЖИВОЕ СОСТОЯНИЕ (срез 02.09 ~01:30 UTC)
- **Прод :8091 = HEAD 324641c (классы 1+2+3+4), active, md5 сверены.**
  Дым: «позиций на всех складах» → честный no_data (класс 4 сработал);
  «валют» 8 цел; «позиций номенклатуры на складах» → пока 2402 (у прайса
  not_enough_for про склады молчит — данные, не код).
- **L15 = 23/38/5/1, разобран по answers.jsonl (не по колонке отчёта!):**
  - «отработанноевремя/расчетныеначисления 30д»: класс 3 СРАБОТАЛ — nums
    [0,0], но их бил мой же судья: вето diag.found==0. Починено (c7e3a47):
    при period_window_empty вето пропускается.
  - «книгапродаж/реализациятмц 30д»: ДРЕЙФ живых эталонов (база растёт, лаг
    индекса ~16 ч — такт руками с 24.08), ответ движка стабилен и верен на
    момент индекса. НЕ код. «Баг префикса 224≈2240» НЕ подтвердился — в
    jsonl nums [2240,2240], колонка отчёта усечена.
  - «позиций номенклатуры на складах» 2402 — единственный настоящий wrong.
  - match 29→23: часть — флипы verify между прогонами («всего складов» в
    пробе сразу после L15 ответил верно 3), часть — дрейф.
- **И2-раннер синхронизирован с окном вручную (scp, md5 cfc75120) — деплой-
  скрипты work/gold НЕ покрывают.** **L16 идёт**: PID 388299, out
  /tmp/i2-l16, дозор bash-ra7x6su5 — чистый замер судьи + классов 2+3+4.

### ПОРЯДОК ДЕЙСТВИЙ (строго)
1. Дождаться L15 → если wrong не вырос и match держится — ночная чистка
   принята; утренний отчёт владельцу (карта ниже + map2-* в .claude/state).
   Если хуже — bisect через /tmp/probe_root (diag_ask2p), откат по классу.
2. НЕ ДЕЛАТЬ ночью: «реально покупают» (механизм count distinct с фактом
   покупки — к скилл-декомпозиции), И2 web, К8.
3. После доклада: скилл-декомпозиция (решение №3), хвосты О6.

### ОСТАТКИ/ДОЛГИ (для отчёта)
- struct_single: пул из 1 карточки пропускает паспортную верификацию
  (z21:~469-478, только axis-check) — решение владельца нужно.
- verify не видит квалификаторы («на складах») — класс 4 лечит частично.
- z13:12-16 подстроки имён регистров в коде (п.0, базлайн О6).
- Разброс разбора между прогонами (INTENT_MEMO внутри прогона только).
- Инертные stock-helpers живут в тестах.

### МЕХАНИКА (выучено; НЕ забывать)
- Прогон И2 на окне (из каталога выката /opt/1c-mcp-reports):
  `nohup bash -c "while IFS= read -r -d \"\" kv; do export \"\$kv\"; done <
  /tmp/ask_env.bin; I2_ENGINE_TIMEOUT=300 I2_TAG=lXX python3
  work/gold/i2_runner.py run --tsv <TSV> --path engine --out /tmp/i2-lXX
  --reshoot-rules work/gold/reshoot-rules-okna.json --workers 6" >
  /tmp/i2_lXX.log 2>&1 &` — TSV = client-gold-okna.tsv из каталога work/gold
  ВЫКАТА (в репо его нет, канон — `ubuntu/serenedb/client-gold-okna.tsv`,
  md5 совпадает с выкатом, замер 02.09; гейт check-docs не даёт писать
  путь выката как repo-path). Локальный дозор на PID (ssh ps -p в цикле,
  disable_timeout=true). jsonl отчёт пишется только в конце.
- Пробы: прод — `python3 /tmp/diag_ask2.py "вопрос"` на окне; код-кандидат —
  /tmp/probe_root (cp -r /opt/1c-mcp-reports) + diag_ask2p.py. /tmp не
  переживает ребут: env пересобрать из /etc/1c-*.env (кавычки срезать).
- Замки — НЕ pytest (тесты — скрипты): `cd /srv/1c/ubuntu/serenedb && for t in
  ...; do python3 $t.py; done`.
- cursor-agent: промт только из файла; хук check-prompt-rules блокирует
  StrReplace на z20 — cursor правит через shell, это норм; правки проверять
  git diff + замки самому; слова исполнителя — замером оркестратора.
- Гейты: add и commit разными вызовами; сообщение с «Числа:» и «Доки:»;
  граф MCP → sleep 3 → grep → add mcp-memory.json в тот же коммит; коммит
  пат-спеком. index.lock без процесса — старый, снести (случай 01.09 21:04).
- GPU: 188 embed :8000 + rerank :8005; .12 27B :8000. Доступ
  `ssh -i ~/.ssh/id_ed25519_deploy root@178.63.211.188` / `root@49.13.97.101`.

### ЖИВОЕ СОСТОЯНИЕ (проверено ~21:00 UTC)
- HEAD e30374b = origin/main. Прод :8091 на этом коде (md5 сверены, active).
  🔴 Прод работает в вики-схеме ТОЛЬКО потому, что в /etc/1c-serene-ask-
  postgres.env стоит ASK_WIKI_CHOICE=1 — без неё выбор сущности старый.
- **ИДЁТ полный И2 L13** (67 вопросов, engine, 6 воркеров): PID 177001 на
  окне, лог /tmp/i2_l13.log, отчёт /tmp/i2-l13; дозор bash-7c22uq0x. Команда
  запуска: из /opt/1c-mcp-reports, env из /tmp/ask_env.bin (`while read -d ""`),
  I2_ENGINE_TIMEOUT=300, --tsv и --reshoot-rules из work/gold/ в выкате.
- GPU-контур после ребута восстановлен: 188 embed :8000 + rerank :8005 (200),
  .12 27B :8000 жив. Доступ: `ssh -i ~/.ssh/id_ed25519_deploy
  root@178.63.211.188` и `root@49.13.97.101` (порт 22, мой deploy-ключ
  добавлен). API-ключ сервисов — в GPU_SERVERS.md (прислан владельцем 31.08)
  и ~/qwen_services/env на хостах; В GIT НЕ КЛАСТЬ. Если 188 лежит:
  nvidia-smi; «No CUDA» после ребута = dkms под новое ядро (HOW_NOT_TO
  §3.107); старт стека — штатный start_services.sh в ~/qwen_services на самом
  188 (вне репо; поднимет и rerank).
- Приборы на окне: /tmp/diag_ask2.py (прод, ПОЛНЫЙ diag), diag_ask2p.py (то же
  против /tmp/probe_root), diag_intent.py, diag_noarb.py, /tmp/ask_env.bin.
  /tmp НЕ ПЕРЕЖИВАЕТ ребут (§3.107): env пересобрать из /etc/1c-*.env
  (кавычки срезать!), probe_root = cp -r /opt/1c-mcp-reports.

### КОММИТЫ ДНЯ (все в origin/main)
dd5e110 «один судья» → 708e6a7 «начало цепочки» (search_form, регрессия L8)
→ 2a68f23 «две формы — один пул» (kNN по обеим формам, паспортов 8=выбору)
→ 68c92bf «struct_named» (названная сущность — карточка в пуле)
→ 23616a4 «гейты над вики сняты» (решение владельца, ЖЁСТКО: kind_unsupported
и non_accounting не рубят до каскада, только diag-маркеры)
→ 1020873 «лидер несёт названную меру» (wiki_measure_carried, носители из
search_measure_alias; носителей нет → мера=понятие, не роняем)
→ e30374b «названная сущность» (_wiki_named_entity: нет подмены src на max(by)
при пустом окне — «0 в окне» = ответ; нет distinct-оси из kind для названной
сущности — COUNT строк, не «2 · Виды Деятельности»; якорь _bootstrap обновлён).

### КАРТЫ ПРОГОНОВ (engine, 67 живых эталонов)
L6 7/55/1/4 → L7 17/35/12/3 → L8 10/42/12/3 (регрессия search_form) →
L9 18/37/11/1 → L11 29/27/10/1 (схема без гейтов) → L12 28/33/5/1 (мера).
L13 — в работе (после e30374b). Замки моей зоны зелёные: 162/110/67/35/47/27/
99/20. ПРЕЖНИЕ красные (на чистом HEAD те же, не мои): k4_axis_and_names 1,
k4_guess_vs_clarify 3 — НЕ разобраны.

### ОСТАТКИ-КЛАССЫ (после L13, по карте)
1. «позиций на складах» → 2402/78540 при эталоне no_data — идёт МИМО вики
   (путь figures/канон), моя проверка меры там не стоит.
2. «сколько из клиентов реально покупают» (эталон 145): семантика «покупают»
   теряется — нужен count distinct клиентов с фактом покупки.
3. «сколько за этот месяц уже вышло»: пул тянет «месяц» к карточкам календаря
   (классификатормесяцев/открытиемесяца) — search_form должна резать период.
4. Разброс разбора между прогонами (kind/measure/search_form гуляют) —
   симптом: один вопрос то match, то clarify. INTENT_MEMO держит внутри
   прогона, между прогонами — нет.

### ПОРЯДОК ДЕЙСТВИЙ ПОСЛЕ COMPACT (строго)
0. Перечитать этот файл + TARGET.md п.0/9/12/21 + docs/PLAN_WIKI_CHOICE.md +
   docs/ORCHESTRATION_CURSOR.md.
1. Дождаться L13 (PID 177001) → карта по классам владельцу.
2. АРМИЯ (главная задача, выше): фаза 0 → фаза 1 → фаза 2. Правки кода —
   только после одобрения карты владельцем, классами, с прогонами.
3. Хвосты по классам (пункты 1–4 выше) — после/параллельно по решению.
4. И2 web (--workers 1, полный url http://127.0.0.1:18801/v1/chat/completions,
   web-timeout 300) → К8 → отчёт; скилл-декомпозиция после стабильного
   прохода (решение №3).

### РИТУАЛЫ (выучено гейтами)
- 🔴 **git add и git commit — РАЗНЫМИ ВЫЗОВАМИ Bash, без `&&` между ними**
  (снайпер и гейты смотрят индекс ДО выполнения команды). Сначала add,
  проверить `git diff --cached --stat`, потом commit пат-спеком. Сообщение
  одной строкой с «Числа:» и «Доки:».
- MCP-граф: add_observations → sleep 3 → grep проверка на диске → git add
  mcp-memory.json → коммит с ним же.
- Промт-правила ловит check-prompt-rules: в строковых литералах питона
  (включая докстринги!) — никаких «обязан/должен»; писать описательно.
- _bootstrap.py патчит z20 при load_all по ТОЧНЫМ якорям — правка z20 в зоне
  якорей требует синхронной правки _bootstrap.py, иначе сервис не встанет.
- deploy-ask.sh только из HEAD=дерево; после выката restart
  1c-serene-ask@postgres + сверка md5 с /srv/1c. Не рестартовать сервис,
  пока идёт прогон.
- Фоновые ssh: дозоры disable_timeout=true; раннер nohup на окне; локальный
  ssh-клиент умирает по 600 с — это НЕ падение. Время серверов = UTC, не
  сдвигать (утренние «паники» 30.08 — неверные сдвиги).
- cursor-агенты: kill и запуск НИКОГДА в одной ssh-строке (pkill по литералу
  команды = самопопадание, 5 раз за смену 30.08); модель оркестратора k3,
  субагенты принудительно на дешёвой ([secondary_model] force = true).
- Гейт check-docs ловит пути вне репо в документах («файл не в дереве») —
  пути серверов писать словами, не как repo-path (дважды 01.09).
- Векторизация — только docs/EMBED_BULK_HOWTO.md §9; вектора бэкаплены (md5
  30b9663c… 31.08, d7aeb15e… 01.09).
- **Ничего лишнего:** не править файлы вне задачи, не ходить на другие
  серверы, не писать новые скрипты поверх базы, не запускать новых прогонов,
  пока идёт текущий.
- Владелец — парень; сроки без замера не обещать.

### ИСТОРИЯ ДНЯ (детали — CHANGELOG 01.09)
Утро: векторы добиты, такт зелёный. День: «один путь», L6. Вечер: «один
судья» + «начало цепочки», L7-L9. Ночь: гейты сняты (решение владельца),
мера носителем, названная сущность; ребут окна и 188 (dkms под новое ядро);
L11 29/27/10/1 → L12 28/33/5/1 → L13 в работе. Владелец дал главную задачу:
армия агентов на ревизию старых заплаток.

### КТО Я И ГДЕ РАБОТАЮ
Оркестратор /srv/1c (Kimi). 🔴 **Работаем ТОЛЬКО на okna** — это не деталь, а
граница сессии: ssh окно `-i ~/.ssh/id_ed25519_deploy -p 2202 root@gpu-erw.timpul.pro`
(движок SereneDB: `psql "host=127.0.0.1 port=7890 user=postgres dbname=postgres"`,
многооператорный SQL — stdin-файлом). **klient-1, klient-2, песочницы и любые
другие серверы НЕ ТРОГАТЬ — ни читать, ни писать, ни «проверить».** «postgres»
в DSN = имя базы SereneDB, СУБД PostgreSQL в контуре нет. GPU: 10.3.1.11
эмбеддер :8000 (НЕ трогать), 10.3.1.12 = 27B :8000 (жив).

### 🔴 TARGET.md — ПРАВИЛА, КОТОРЫЕ НЕЛЬЗЯ НАРУШИТЬ НИ В ОДНОЙ ПРАВКЕ
- **п.0/п.9:** прод-база скрыта и заработает из коробки; в коде — НИ ОДНОГО
  имени сущности, списка слов домена, подстройки под окна. Правило общее или
  его нет. Проверка себя: «сработает ли это на базе, которую я никогда не
  видел?» — нет → не делать.
- **п.12:** отказ и уточнение — не ошибка; ДОГАДКА — ошибка. Молчаливо
  подставленное окно/мера/сущность = догадка, запрещена.
- **п.21:** ответ → уточнение → отказ; отказ при наличии данных — дефект.
  Проверка, отвергшая верный ответ, — дефект проверки.
- **п.19/п.20:** считает и ищет база (SereneDB), модель — только смысл; своих
  скриптов поверх базы — нет; перед SQL — доки serenedb-docs.
- **п.10:** неверный ответ хуже отказа. Каждое ослабление проверки обязано
  быть доказано полным прогоном (wrong=0), не пробой.

### 🔴 ДЕЙСТВУЮЩИЕ РЕШЕНИЯ ВЛАДЕЛЬЦА (не открывать заново)
1. **«Один судья» (01.09, согласовано):** сторожей-судей выбора сущности нет.
   Все сигналы — ВХОДЫ ПУЛА карточек (kNN, словарь синонимов, носители меры,
   названная ось). Судья один — паспортная верификация каскада z21.
2. **«1 механизм, чинить НАЧАЛО» (01.09):** если на выходе плохо — проблема в
   начале цепочки (понимание → пул → выбор → верификация), а не в сторожах
   хвоста. Не разбирать вопросы по одному — правила общие, проверка = полный
   прогон 67, не пробы.
3. **Скилл-декомпозиция согласован (после стабильного базового прохода):**
   отдельный слой поверх ask: вопрос → LLM-разбивка на подвопросы (простой =
   одна часть) → точечные вызовы ask → сборка чисел КОДОМ (п.19) → журнал
   «вопрос → подвопросы → ответы» (наблюдаемость этапов). Не строить, пока
   точечный ответ нестабилен.
4. **П.0/п.9/п.12/п.21 TARGET — свято.** Ни одной подстройки под базу/вопросы;
   догадка (молчаливый assumed-окно) запрещена; отказ при данных — дефект.
5. **Словари синонимов — на нашем 27B (10.3.1.12:8000), DeepSeek из словарей
   убран** (31.08; usage-лог доказал, что DeepSeek не участвовал). Генератор —
   юнит 1c-wiki-alias через штатного агента OpenClaw; такт только компилирует
   уже лежащую таблицу.
