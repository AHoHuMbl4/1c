# Active Context

_Обновлено: **2026-08-28 поздний вечер**. Здесь — только живое.
История — в [`progress.md`](progress.md); стадии — в
[`docs/TARGET_STATUS.md`](../docs/TARGET_STATUS.md)._

---

# ⏭ С ЧЕГО НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

🔴 **Периметр (слово владельца): ТОЛЬКО окно (gpu-erw.timpul.pro), всё НА
нём, БЕЗ туннелей. Свежесть 1С отброшена владельцем. klient-1 — потом.**

## Следующий шаг (первым делом)

1. **К3 живая приёмка — пересборка корпуса на окне + стейджинг :8092**:
   код обоих шагов в git (`df2145d` карта `search_meta.currency_*` в
   corpus_build §1-quint, `24bbc79` зона `z04b` за `ASK_CURRENCY_AXIS`).
   Нужно: (а) пересборка корпуса на окне (карта валют наполнится;
   осторожно — это шаг конвейера), (б) выкладка стейджинга :8092 из
   текущего HEAD с флагом `ASK_CURRENCY_AXIS=1` (боевой :8091 не
   переключать до приёмки), (в) пробы «сколько продали в августе 2026»
   (ожидание B/C с подписями при FX Δ375 437,03) и «сколько продали в
   евро» (clarify doc_amount/accounting_amount, не чужое число), (г)
   полный скорер не ниже базлайна. Приёмка корпуса по дизайну:
   `accounting_currency_constant` непуст, `currency_amount_map` ≥ 1.
2. **Перенос остальных c5-слов в боевой словарь**: сегодня перенесён
   только контрагент (штатный MERGE по rows.json, бэкап
   `/tmp/kontr-alias-backup-20260828.txt` на окне); в `alias_okna_c5`
   259 строк против 257 боевых, новые слова-роли у многих. Решить
   механизмом (сверка/доучивание), не руками по одной.
3. **К8**: черновик скилла `work/skill-decomp/` закоммичен; выкат в
   контур бота + приёмка compare-набором 8/8 — после K6-двойки
   (предусловие плана К8).

## Сделано 28.08 поздний вечер (HEAD `882cfba`, всё запушено)

- **«Покупатели» дошли до боя**: прогон юнита с роль-словами дал
  контрагентам «Покупатели» из flows (модель, не руки); перенос в
  `search_entity_alias` штатным MERGE; живая проба на :8091
  «сколько покупателей было за этот месяц» → **43 · Контрагенты**
  (=эталон MTD DISTINCT, было 76 по кассе), 7,3 с. Свежий **AB_PROBE
  8/8** (8,01 с). Юнит дошёл до конца: c5 259, solr 33.
- **К3 оба шага в git**: шаг 1 карта валют в сборке корпуса
  (`df2145d`, оффлайн 40/0), шаг 2 ask-зона z04b за флагом
  (`24bbc79`, замки 18/0 + fork/calendar/rank зелёные, при 0 — no-op).
  Живая приёмка — см. «Следующий шаг» п.1.
- **К8-черновик** скилла-декомпозатора (SKILL.md + references +
  examples; compare-вердикт по двум цитатам разрешён, разность — нет).
- Падение юнита 20:39 разобрано (§3.98: выкат под работающим bash);
  мой же холостой flows-прогон — §3.97.
- Владелец снял строку люка: `override.txt` ПУСТ, коммиты идут без
  `--no-verify`. maxTokens 16384 у шлюза уже стоит (его руками).

## Трек до продакшена (рамка от владельца 29.08)

Порядок: К3 живая приёмка → приёмка контракта по TARGET_STATUS (см. его
срезы) → прод. Открытые пункты (все — в TARGET_STATUS):
1. **К3 живая приёмка** (рецептура ниже) → при зелёном включить флаг
   `ASK_CURRENCY_AXIS=1` в бою (решение — по итогу проб, помня исход B).
2. **Ambiguous 11/18** — доводка классов: склад-subject, «всех складах
   вместе», ранг-топ без периода, сленг (срез вечера в TARGET_STATUS).
3. **К8** выкат в контур + compare 8/8 (после K6-двойки).
4. **Перенос остальных c5-слов** в боевой словарь механизмом (259 vs 257).
5. **Свежесть 1С** — держит контур-24 и «за вчера»; отложена владельцем,
   без неё приёмку «вчера/топ за вчера» не закрыть.
6. klient-1/Э2, В1 перемер латентности — по ходу.

## Рецептура К3-приёмки (точные команды, окно)

- Бой :8091 = `/opt/openclaw-mcp/venv/bin/python /opt/1c-mcp-reports/serene_ask.py`
  c env: `ASK_ATOM_TERMINAL=1 ASK_ENTITY_FORM=1 ASK_HEALTH_NATIVE_FRESHNESS=1
  ASK_MEMORY_APPLY=1 ASK_MONEY_UNIT=лей ASK_RESOLVER_IVF=1
  ASK_SALES_RANK_CANON=1 ASK_SCORER=lm_dirichlet ASK_SOLR_SYNONYMS=1
  ASK_SOLR_SYNONYMS_DICT=search_dict_syn ASK_SQL_RRF=1
  ASK_THINKING_OFF_BODY=1` (токен в /etc/1c-serene-ask.env).
- Стейджинг :8092 сейчас — УСТАРЕВШИЙ `serene_ask_cand.py` с урезанным
  env: для К3 не годится. Поднять заново: свежий код из HEAD (serene_ask.py
  + ask/) в /opt/1c-mcp-reports (юнит inactive!), убить pid :8092
  (`ss -tlnp | grep :8092`), поднять тем же интерпретатором с env БОЯ
  плюс `ASK_CURRENCY_AXIS=1 ASK_LISTEN_PORT=8092`.
- Сначала пересборка корпуса (карта `search_meta.currency_*` наполнится,
  corpus_build §1-quint из `df2145d`); проверка: accounting_constant
  непуст, currency_amount_map ≥ 1 (SQL в work/currency-axis-design.md).
- Пробы :8092: «сколько продали в августе 2026» → B/C с подписями при FX
  Δ375 437,03; «сколько продали в евро» → clarify doc_amount /
  accounting_amount, не чужое число. Полный скорер — не ниже базлайна,
  при флаге 0 — прежние ответы.

## Механика (свежее)

- **Юнит словаря и выкат в `/opt/1c-mcp-reports` не пересекаются по
  времени** (§3.98): юнит — только при inactive, выкат — при inactive
  юнита.
- **Разовый запуск wiki_alias.sh**: env-файлы юнита — формат systemd
  (значения с пробелами БЕЗ кавычек), shell-сорсинг режет DSN по
  первому пробелу → задавать SERENEDB_DSN явной строкой; ALIAS_TABLE
  на окне = `alias_okna_c5` (боковая), боевой словарь —
  `search_entity_alias`, перенос — MERGE по rows.json через
  `/var/lib/serenedb` (обменный каталог движка, /tmp движок не видит).
- Сообщение коммита — ОДНОЙ строкой, без `; | &` (парсер пат-спека
  режет по ним → гейт видит весь индекс), с «Числа:» и «Доки:».
- Граф: после MCP-записи sleep 3 + re-add (гейт читает ИНДЕКС).
- Выкат на окно: tar по diff-md5 → scp → atomic-mv в /opt → рестарт
  юнита; гейты check-golden пропускают при дерево=HEAD + свежая отметка
  (AB_PROBE отметка свежая, 21:20).
- Правки проверять сразу на :8091 (кандидат :8096 не нужен).

## Что мешает

1. Свежесть 1С — отброшена владельцем; контур-24 и «за вчера» недоступны.
2. klient-1/Э2 — внешне, потом.
3. maxTokens 16384 у шлюза — уже стоит (руки владельца, 28.08).

История дней — `progress.md`.
