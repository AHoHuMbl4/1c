# Активный контекст

## С ЧЕГО НАЧАТЬ (срез 31.08, п.0 механизмы дня)

0. **Четыре ручные операции дня → механизмы (репо готово, окно — за оркестратором):**
   - **Секреты эмбеддера self-heal**: `embed_secrets_install.sh` перед CREATE
     делает `DROP SECRET IF EXISTS` для всех `emb_<DB>_*` и `qwen`, затем
     создаёт по текущему `EMBED_HOSTS` (осиротевшие от старого списка больше
     не ломают count-check). Замок 30/30.
   - **Карантин автоповтор**: `packet_apply` в начале прохода, если есть
     applied с seq больше quarantined — `quarantined→verified` + `attempts`
     (макс. `PACKET_QUARANTINE_RETRY_MAX=3`, дальше `attempts_exhausted` в
     state/журнале/`/health`). Замок `test_packet_apply_retry.py` 14/0.
   - **Деплой такт-SQL**: `work/deploy-sql.sh` (+ `deploy-common.sh`,
     deploy-ask на общий lib) — md5 репо↔окно, atomic mv; `--dry` /
     `--offline-dry`. Замок 17/17.
   - **О5-канон serenedb drop-in**: `wal-autocheckpoint.conf` —
     `SET GLOBAL wal_autocheckpoint = 512MB` без двойных кавычек; 
     `embed-secrets.conf` — прямой вызов установщика (`-+/bin/sh -c …`),
     не systemctl (polkit). Живой юнит на окне оркестратор обновит по канону;
     форма `512MB` на 26.07.3 — живая сверка оркестратором.
1. **И2 [решение владельца 31.08]**: восстановить канонические **67** gold с
   живыми эталонами дня → прогон 67×2 (`i2_runner.py`).
2. Дальше по приоритету: К8 → §6 бенч ai_embed → О6 → coverage-хвост.

## Срез 30.08 (история этапа)

0. **Этап зафиксирован — это НЕ авария и не простой:**
   - **§5 ЗАКРЫТ (ночь 30.08)**: z21-первичность влита в main (`9c539db`),
     контрольные 6/6 живьём. Health «503/degraded» — coverage-хвост 15 строк.
   - **Вектора закрыты**: embed_bulk канон §9, рабочая очередь **0**.
   - **Такт ЗЕЛЁНЫЙ 30.08 16:37 UTC** (236 с). Таймера нет — oneshot руками.
1. **Словарь синонимов → наш 27B** (в работе, указание владельца 30.08).
2. **После переключения**: лаг свежести → эталоны → И2 67×2.
3. **ЧП1 закрыт**: client-gold 67/67 (match 65 / mismatch 2 — полнота), 52b47f8.
4. **z21-первичность** — ветка wip/z21-primary, живой 6/6 до merge; патч
   `.claude/state/z21-uncommitted.patch` входит при мердже.
5. **GPU-серверы (по внутренним адресам, указание 30.08)**: 10.3.1.11 = эмбеддер
   Qwen3-Embedding-4B :8000 (ОБЯЗАТЕЛЬНО живой — вопросы бота EMBED_BASE_URL),
   реранкер :8005, whisper :8006; 10.3.1.12 = Qwen3.8-27B :8000 (64k), pyannote :8001.
   Эмбеддер с .12 УБРАН владельцем: секрет `emb_postgres_0` (старый 10.3.1.12:8002)
   мёртв — перенацелить на 10.3.1.11:8000 или удалить.
6. Секреты эмбеддера: `embed_secrets_install.sh` (self-heal DROP+CREATE по
   EMBED_HOSTS) + oneshot-юнит; drop-in serenedb зовёт установщик напрямую
   (`-+/bin/sh`), не через менеджер юнитов. TEMPORARY в такте — как было.
   Выкат такт-SQL: `work/deploy-sql.sh` (общий `deploy-common.sh` с deploy-ask).

## Механика стейджинга :8092 (восстановлено 30.08)

- Юнит `1c-serene-ask@staging` (шаблон `1c-serene-ask@.service`), env
  `/etc/1c-serene-ask-staging.env`: `ASK_LISTEN_PORT=8092`, `ASK_WIKI_CHOICE=1`,
  токен — `/etc/1c-serene-ask.env` (ASK_TOKEN).
- **Код общий с продом**: `/opt/1c-mcp-reports/serene_ask.py` + `ask/z*.py` —
  выкат файла меняет оба инстанса при их следующем рестарте; боевой `@postgres`
  (:8091) держит старый код в памяти до своего рестарта.
- Было: процесс на 8092 запускался вручную из SSH-сессии (умирал с ней);
  30.08 переведён на юнит. Файлы z21-каскада: `ask/z21_wiki_choice.py`,
  `ask/_bootstrap.py`, `ask/z20_ask_main_http.py`, `wiki_card_hybrid.sql`
  (бэкапы `*.bak-prez21-20260830`).

## Состояние (29.08 вечер, транспорт f7f10236)

- **Дефект транспорта f7f10236 — разобран, сервер закрыт:**
  - Потеря на **seq195** (`000195-dc66c520`): partial apply — `full_entity` регистров
    без Recorder=f7 перезаписал витрину; document delta → `delta_without_key`
    (Ref_Key/ref_key); карантин не откатывает DROP/CREATE.
  - seq154–176 applied — движения **в чанках**; винда выгружала.
  - **Починка:** `_ci_col` (git 30cf7d7), выкат okna; re-apply **000171-9ca09876** →
    витрина f7 **19/19/19/21**. **000195** в карантине — не apply (f7=0 в регистрах).
- **okna такт:** merge f7 без STOP; **красный** solr_synonyms_compile + embed
  (1 642 476 corpus, emb NULL 1 584 759) — отдельно от транспорта.
- **coverage_build**: `_ci_col`/duckdb_columns — в коде (30cf7d7).

## Следующий шаг (по приоритету)

1. **И2 [решение владельца 31.08 «конечно восстановить»]**: восстановить
   канонические **67 вопросов** gold с живыми эталонами дня (пересборка);
   22 эталона без пакетного следа — пометить в отчёте, не блокер; прогон
   **67×2** (:8091 движок, :18801 веб) раннером `/opt/1c-mcp-reports/work/gold/i2_runner.py`;
   критерий «уверенно неверных = 0». Сборка gold — DSN **postgres**
   (serene_ro не читает ask_journal): см. историю-команду в CHANGELOG 30.08.
   Причина урезания до 50: генератор журнала клюнул на рабочие вопросы
   оркестратора — фильтр самоисключения при пересборке.
2. **К8**: скилл ask-decomposer → контур бота → compare 8/8. Затем автоприёмка.
3. **§6 бенч нативного ai_embed** (без конкуренции) — при сравимости упростить такт.
4. **О6**: выжечь базлайн слов домена (43) до 0 без слома канонов продаж.
5. **Coverage-хвост**: 15 строк / 5 сущностей (договоры 713/704) — диагноз
   фильтра vs merge-пропуск, замок, /health ок. Карантин 195 — ждать seq196+.
2. **К2-остаток**: мера-развилка при недосчитываемой ветке → исход B с лидером
   (сейчас clarify вместо цифр на «календарная неделя»/«с 1 по 15»; эталоны
   47 082 166 / 208 367 025 живым SQL). Агентом, не руками.
3. **Скорость С4-вопроса**: «позиций не продаётся» >120 с — таймауты curl;
   посмотреть латентность (В1-класс).
4. Спорные эталоны ambiguous (№5/№8/№12/№11/№4-игла-руб-при-лее) — вопрос
   владельцу набора, не подгонка.
5. c5-перенос остальных слов — ТОЛЬКО механизмом с диффом живых слов (§3.99).

## Механика дня (проверено живьём)

- **Контур бота 29.08 (всё проверено живьём, разбор — INSTALL_LOG §29.08)**:
  рабочий фронт — **веб** baulogistic → `http://10.3.1.11:18801/v1` (LXD proxy
  на хосте → web-шлюз okna). **Телеграм :18800 остановлен+disable (решение
  владельца)**, возврат `systemctl --user enable --now openclaw-gateway`.
  Web-шлюз: `--bind lan` + `gateway.http.endpoints.chatCompletions.enabled=true`
  (❗ НЕ `gateway.http.enabled` — 78/CONFIG); дефолтный агент — явный
  `{"id":"main","default":true}` в `agents.list`; auth main — per-agent sqlite
  (копируется из основного профиля); токен = ключ морды. Память OpenClaw —
  наш эмбеддер по домену `http://gpu-erw.timpul.pro:8000/v1`
  (Qwen3-Embedding-**4B**, ключ `/etc/1c-embed.env`). verify-плагин ставится
  `npm pack` → `openclaw plugins install npm-pack:<tgz> --force` в каждый
  профиль (29.08 отсутствовал у обоих с 23.08 — переустановлен).
  Тяжёлые вопросы веб-чата — до ~3 мин (С4-класс, отчёт
  `docs/drafts/s4-latency-2026-08-29.md`).
- **Сеть окно/фронт (зафиксировано 29.08, полная схема —
  `ubuntu/open-webui/README.md` §«Сеть»)**: прод = контейнер **okna 10.10.10.12**
  на хосте gpu-1c (`gpu-erw.timpul.pro`, vSwitch-нога **10.3.1.11**, ssh снаружи
  `178.63.211.188:2202`); фронт baulogistic = 2.28.49.158 / 10.3.0.2. 🔴 10.3.0.4
  и 167.233.249.110 — мёртвые адреса старого бэкенда. Внутри okna: ask :8091
  loopback, не трогать.
- Стейджинг: systemd-run + EnvironmentFile-цепочка, ПОРТ — отдельным
  оверрайд-файлом ПОСЛЕДНИМ (EnvironmentFile перекрывает Environment=).
- Гейты коммита: add и commit РАЗНЫЕ вызовы Bash; граф — observation → sleep 3
  → add → commit. Замки: `python3 test_*.py` (pytest нет);
  test_fork_atom_aggregate живой-БД — на деве виснет.
- pkill -f по строке из своей ssh-команды убивает свою сессию — kill по pid из ss.
- Выкат ask: `bash work/deploy-ask.sh`; такт-SQL: `bash work/deploy-sql.sh`
  (`--dry` / `--offline-dry`). Общий SSH — `work/deploy-common.sh`.
- НЕ рестартовать :8091 во время скореров (дважды смешивал состояния прогонов).
- `search_meta` окна = только balance_registers + period_relative_forms:
  календарные/валютные фразы и карты отсутствуют — всё, что их читает,
  на этой базе no-op'ает (calendar_axis_unavailable_block и значения оси места).

## Что мешает

1. Такт/$metadata (агент 1.0.2) — держит К3, свежесть, corpus-половину К2,
   контур-24, живое действие календарного словаря. Отложено владельцем.
2. Скорость тяжёлых вопросов (С4 >120 с) на 27B.

## Рецептура К3 (когда агент Windows станет 1.1.3)

Собрать exe `windows/packet-agent/build.cmd` → залить в S3 (сейчас там 1.1.2 от
13.08!) → `upgrade-agent.cmd` на машине okna → агент увидит need_metadata=1 (уже
стоит) → kind=meta → apply напишет `/var/lib/serenedb/packet-meta/okna-1/$metadata`
→ такт `1c-serene-pipeline@postgres` оживёт → corpus_build §1-quint/§1-кватер
наполнят карты валют/календаря → проверка: accounting_currency_constant непуст,
amount_map≥1, calendar_day_basis_phrases непуст → перемерить ВСЁ (corpus сменится).

История дней — `progress.md`.
