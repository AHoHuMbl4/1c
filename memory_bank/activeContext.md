# Активный контекст

## С ЧЕГО НАЧАТЬ (срез 01.09 ~16:40 UTC, ВЕЧЕР; читать целиком; после compact)

### КТО Я И ГДЕ РАБОТАЮ
Оркестратор /srv/1c (Kimi). 🔴 **Работаем ТОЛЬКО на okna** — это не деталь, а
граница сессии: ssh окно `-i ~/.ssh/id_ed25519_deploy -p 2202 root@gpu-erw.timpul.pro`
(движок SereneDB: `psql "host=127.0.0.1 port=7890 user=postgres dbname=postgres"`,
многооператорный SQL — stdin-файлом). **klient-1, klient-2, песочницы и любые
другие серверы НЕ ТРОГАТЬ — ни читать, ни писать, ни «проверить».** «postgres»
в DSN = имя базы SereneDB, СУБД PostgreSQL в контуре нет. GPU: 10.3.1.11
эмбеддер :8000 (НЕ трогать), 10.3.1.12 = 27B :8000 (жив).

### 🔴 TARGET.md — ПРАВИЛА, КОТОРЫЕ НЕЛЬЗЯ НАРУШИТЬ НИ В ОДНОЙ ПРАВКЕ
- **п.0/п.9:** прод-база скрыта и заработает из коробки; в коде — НИ ОДНОГО
  имени сущности, списка слов домена, подстройки под окна. Правило общее или
  его нет. Проверка себя: «сработает ли это на базе, которую я никогда не
  видел?» — нет → не делать.
- **п.12:** отказ и уточнение — не ошибка; ДОГАДКА — ошибка. Молчаливо
  подставленное окно/мера/сущность = догадка, запрещена.
- **п.21:** ответ → уточнение → отказ; отказ при наличии данных — дефект.
  Проверка, отвергшая верный ответ, — дефект проверки.
- **п.19/п.20:** считает и ищет база (SereneDB), модель — только смысл; своих
  скриптов поверх базы — нет; перед SQL — доки serenedb-docs.
- **п.10:** неверный ответ хуже отказа. Каждое ослабление проверки обязано
  быть доказано полным прогоном (wrong=0), не пробой.

### 🔴 ДЕЙСТВУЮЩИЕ РЕШЕНИЯ ВЛАДЕЛЬЦА (не открывать заново)
1. **«Один судья» (01.09, согласовано):** сторожей-судей выбора сущности нет.
   Все сигналы — ВХОДЫ ПУЛА карточек (kNN, словарь синонимов, носители меры,
   названная ось). Судья один — паспортная верификация каскада z21.
2. **«1 механизм, чинить НАЧАЛО» (01.09):** если на выходе плохо — проблема в
   начале цепочки (понимание → пул → выбор → верификация), а не в сторожах
   хвоста. Не разбирать вопросы по одному — правила общие, проверка = полный
   прогон 67, не пробы.
3. **Скилл-декомпозиция согласован (после стабильного базового прохода):**
   отдельный слой поверх ask: вопрос → LLM-разбивка на подвопросы (простой =
   одна часть) → точечные вызовы ask → сборка чисел КОДОМ (п.19) → журнал
   «вопрос → подвопросы → ответы» (наблюдаемость этапов). Не строить, пока
   точечный ответ нестабилен.
4. **П.0/п.9/п.12/п.21 TARGET — свято.** Ни одной подстройки под базу/вопросы;
   догадка (молчаливый assumed-окно) запрещена; отказ при данных — дефект.

### ЖИВОЕ СОСТОЯНИЕ (проверено ~19:30 UTC)
- HEAD 68c92bf = origin/main. Прод :8091 на этом коде (md5 сверены, active).
- Замки зелёные: intent 162/0, step4 110/0, hybrid 65/0, verify 35/0,
  zones 99/0, build 20/0, stock 47/0. ПРЕЖНИЕ красные (не от этих правок, на
  HEAD те же): k4_axis_and_names 1 («осталось на складе»),
  k4_guess_vs_clarify 3 FAIL — разобрать отдельным заходом.
- **ИДЁТ полный И2 L10** (67 вопросов, 6 воркеров, engine): PID 4185326 на
  окне, лог /tmp/i2_l10.log, отчёт /tmp/i2-l10. Запуск: из
  /opt/1c-mcp-reports, env из /tmp/ask_env.bin (NUL-разделённый, `while read
  -d ""`), I2_ENGINE_TIMEOUT=300, --tsv <копия ubuntu/serenedb/
  client-gold-okna.tsv в выкате на окне> --reshoot-rules <reshoot-rules-
  okna.json рядом с i2_runner.py в выкате>.
- Диаг-приборы на окне: /tmp/diag_ask.py (TRACE+WIKI_DIAG), /tmp/diag_ask2.py
  (прод, полный diag), /tmp/diag_ask2p.py (то же против /tmp/probe_root),
  /tmp/diag_ab.py (intent + пул по интенту/по сырому вопросу), env
  /tmp/ask_env.bin. Пробная зона /tmp/probe_root держать равной прода
  (scp изменённых файлов) — иначе пробы врут.

### ЧТО СДЕЛАНО ЗА ДЕНЬ (коммиты)
- **dd5e110 «один судья»:** struct_alias (топ-3 словаря bm25 → пул), гашение
  signals_disagree/writer_pair/вето (_checked и REQUIRE_SUPPORT-блок) при
  wiki_hybrid_pick+wiki_verify==picked[0]; assumed rolling_year снят (п.12);
  смысловой мост оси только при названной action_axis. Замеры: книгапродаж
  76 075, реализациятмц 78 537, валют 8, организаций 1 — эталоны.
- **708e6a7 «начало цепочки»:** search_form (разбор z01/z02 возвращает форму
  без периода/чисел; пул строится по ней, LLM-шаги видят исходник);
  struct_measure (носители спрошенной меры из search_measure_alias → пул);
  ось пула только из названной action_axis; WIKI_PICK_N=8 (паспортов 5).
- **2a68f23 «две формы — один пул»:** замена входа на search_form оказалась
  потерей (L8: 17→10 match — склеенное имя сворачивается в голый токен, kNN
  по нему промахивается; по сырому вопросу карточка №2). Пул = kNN(вопрос) ∪
  kNN(search_form) ∪ словарь по обеим ∪ структурные; паспортов 8 = карточкам
  выбора (карточка №6 была неверифицируема).
- **68c92bf «названная сущность поддержана всегда»:** struct_named — стемы
  слов вопроса (≥4) против стемов src_table/label из search_tables → карточка
  в пуле (kNN тонет на общих словах, bm25 слеп на склеенных именах); гейт
  kind_unsupported проверяет и слова вопроса (question_names_corpus_entity,
  z16) — раньше kind=«записи» от модели убивал вопрос до каскада.

### КАРТЫ ПРОГОНОВ (engine, 67 живых эталонов)
- L6 (до правок): match 7 / honest_no 55 / wrong 1 / timeout 4.
- L7 (после «одного судьи»): match 17 / honest_no 35 / **wrong 12** / 3.
- L8 (после «начала цепочки»): 10/42/12/3 — регрессия класса «записей в X».
- L9 (после «двух форм»): 18/37/11/1 — лучше L7; класс вернулся наполовину
  (12/24); wrong: 8 складских (остатки→«3 склада»/2402/78540), отработанное
  время за 30 дн → 6868 (план счетов), реализациятмц за 30 дн → 2 (эт. 2240,
  новый). Ушли из wrong: кассовыеордера, «сегодня наотгружали», прайс 29.
- L10 — в работе (после struct_named).

### ИЗВЕСТНЫЕ КЛАССЫ-ОСТАТКИ (по карте L10 решать классами)
1. Складские: measure=«остатки» (нестабильно из-за голосования прогонов!),
   носители меры в базе — актсверка/взаимозачёты (не складские) → цель:
   честный no_data вместо списка складов.
2. «Пусто за окно → no_data» вместо ответа «0» (7 вопросов L6 с эталоном 0.00;
   отработанноевремя за июль-2026: сущность+период верны, итог no_data).
3. Разброс полей разбора (kind/measure/search_form) между прогонами —
   симптом: один вопрос отвечает в форме «движений в регистре X» и молчит в
   форме «записей в X». INTENT_MEMO=512 внутри прогона держит, между
   прогонами — нет.
4. Спорный эталон «позиций номенклатуры числится на складах» (reshoot
   no_data при живых 2402 каталога).

### ПОРЯДОК ДЕЙСТВИЙ ПОСЛЕ COMPACT (строго)
0. Перечитать этот файл + TARGET.md п.0/9/12/21 + docs/PLAN_WIKI_CHOICE.md.
1. Дождаться L10 (PID 4185326, лог /tmp/i2_l10.log) → дифф против L9/L7 по
   классам. Класс «записей в X» целиком? wrong ≤ 11?
2. По классам (не по вопросам): складские → честный no_data через меру;
   «пусто за окно → 0»; новый wrong «реализациятмц за 30 дней → 2».
3. Чистый engine (wrong=0) → И2 web (--workers 1, ПОЛНЫЙ url
   http://127.0.0.1:18801/v1/chat/completions, web-timeout 300) → К8.
4. Скилл-декомпозиция (по решению №3). Отчёт владельцу одним письмом.

### РИТУАЛЫ (выучено гейтами)
- 🔴 **git add и git commit — РАЗНЫМИ ВЫЗОВАМИ Bash, без `&&` между ними**
  (снайпер и гейты смотрят индекс ДО выполнения команды: склеенная цепочка =
  «ДИФФ НЕ СНЯТ», коммит остановлен; обжигался дважды 01.09). Сначала add
  отдельным вызовом, проверить `git diff --cached --stat`, потом commit
  пат-спеком. Коммит пат-спеком обязателен (в папке работает >1 сессии).
  Сообщение одной строкой с «Числа:» и «Доки:».
- MCP-граф: add_observations → sleep 3 → grep проверка на диске → git add
  mcp-memory.json → коммит с ним же (гейт check-graph-fresh).
- Код без CHANGELOG не пройдёт; SQL в диффе требует «Доки: <раздел>».
- deploy-ask.sh только из HEAD=дерево; после выката restart
  1c-serene-ask@postgres + сверка md5 с /srv/1c.
- Фоновые ssh: дозоры disable_timeout=true; раннер nohup на окне; локальный
  ssh-клиент умирает по 600 с — это НЕ падение.
- Векторизация — только docs/EMBED_BULK_HOWTO.md §9; вектора бэкаплены (md5
  30b9663c… 31.08, d7aeb15e… 01.09).
- **Ничего лишнего:** не править файлы вне задачи, не ходить на другие
  серверы, не писать новые скрипты поверх базы, не запускать новых прогонов,
  пока идёт текущий. Перед действием — вопрос: «это входит в порядок
  действий ниже?» — нет → не делать.
- Владелец — парень; сроки без замера не обещать.

### ИСТОРИЯ ДНЯ (детали — CHANGELOG 01.09)
Утро: векторы добиты, такт зелёный, вики-цикл впервые живьём. День: «один
путь» (вырезаны обходы), L6 (7/55/1). Вечер: «один судья» + «начало цепочки»,
L7 (17/35/12) → разбор по семействам → L8 в работе.
