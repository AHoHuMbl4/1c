# Активный контекст

## С ЧЕГО НАЧАТЬ (срез 01.09 ~11:30 UTC, ДЕНЬ; читать целиком; после compact)

### КТО Я И ГДЕ РАБОТАЮ
Оркестратор /srv/1c (Kimi). 🔴 **Работаем ТОЛЬКО на okna** = ssh окно
`-i ~/.ssh/id_ed25519_deploy -p 2202 root@gpu-erw.timpul.pro` (движок SereneDB:
`psql "host=127.0.0.1 port=7890 user=postgres dbname=postgres"`, SQL
многооператорный — stdin-файлом). **klient-1 и прочие серверы НЕ ТРОГАТЬ**
(слово владельца 01.09). «postgres» в DSN/юнитах = имя базы SereneDB,
СУБД PostgreSQL в контуре НЕТ (п.4 TARGET). GPU: 10.3.1.11 эмбеддер :8000
(НЕ трогать), 10.3.1.12 = 27B :8000 (жив). Хосты владельца: ssh root@49.13.97.101
(27B) тем же ключом.

### 🔴 ДЕЙСТВУЮЩИЕ РЕШЕНИЯ ВЛАДЕЛЬЦА (не открывать заново)
1. **«Физически один путь» (01.09):** выбор сущности для ВСЕХ вопросов —
   ТОЛЬКО через Вики-каскад: focus человека → wiki_primary_entity_cascade
   (пул карточек kNN+структура → LLM-выбор → верификация паспортами) →
   лидер / чипы / честный no_data. Все обходы ВЫРЕЗАНЫ из кода
   (замки/каноны/лестница/осевые перебои; коммиты c8a277a→3e28177).
   «Чтобы физически не могло пойти по другому — тогда на любой базе
   сработает».
2. **Метод (последнее слово, 01.09): «ХВАТИТ ГАДАТЬ. Система простая.
   Смотреть КАЖДЫЙ ШАГ, не мусорить в коде».** Не patch-симптомы условиями:
   взять вопрос, прочитать его ЦЕПОЧКУ ШАГОВ в TRACE (diag-скрипты на окне),
   найти ЕДИНСТВЕННОЕ место излома, минимальная правка.
3. **П.0/п.9 TARGET — свято:** скрытая прод-база заработает из коробки;
   «другой язык — без правки кода». Схема «языковые списки как вспомогательные»
   НЕ принимается (долг: z02:390, z09:17, z11:788, z20:1820 → заменить
   структурными признаками, отдельным заходом).
4. Схема продукта: Вики ДВАЖДЫ — вход (LLM+вики понимает вопрос; не уверена —
   переспрос СРАЗУ с чипами кандидатов) и выход (кандидат проверяется да/нет
   большим wiki-контекстом). Одно число сразу (п.21: ответ → уточнение →
   отказ; отказ при данных = дефект; догадка = ошибка).

### ЖИВОЕ СОСТОЯНИЕ (проверено ~11:20 UTC)
- HEAD 2717db2 = origin/main. Все замки зелёные: wiki 63/0, stock 47/0,
  fork 51/0, axis 41/0, verify 35/0, budget 50/0.
- Векторы: корпус 1 657 724 живых / NULL 0; резолвер 2 100 463 / 0.
  Бэкапы md5: 30b9663c… (31.08 23:58), d7aeb15e… (01.09 02:19).
- Такт ЗЕЛЁНЫЙ (vector_loss_gate=0, content_hash NULL 0). 27B жив.
- **ИДЁТ полный L6-прогон И2 engine** (PID 3742289 на окне, лог
  /tmp/i2_l6.log, отчёт /tmp/i2-l6; дозор bash-zkikdp5y разбудит).
- Диагностика цепочек: /tmp/diag_ask.py и /tmp/diag_ask2.py на окне
  (загрузчик зон + env юнита /tmp/ask_env.bin) — показывают TRACE+diag
  любого вопроса без HTTP.

### ГДЕ СЕЙЧАС СЛОМАНО (по одному пути; материалы для «посмотреть шаги»)
1. «Сколько записей в «книгапродаж»?» — вики-цепочка ИДЕАЛЬНА (пул → pick →
   verify=книгапродаж, wiki_arbiter_locked), но итог = clarify с чипами
   примеров-сумм («Какова сумма НДС…»). Излом в слое ВЕЛИЧИНЫ после выбора:
   want=count при мерах сущности уходит в переспрос вместо счёта строк.
   Эталон 76 075 = count строк.
2. «движений в регистре реализациятмц» — даёт «rolling_12m: 26» — прилип
   период при вопросе БЕЗ периода (эталон 78 537 всего). Излом в слое
   ПЕРИОДА.
3. «Сколько валют?» → 10 при эталоне 8 (вики выбирает классификатор валют
   вместо валют — калибровка pick/verify между близнецами).
4. «сколько продали за месяц» → clarify-чипы вместо лидера (раньше канон
   давал 4 224 744,87; теперь верификация обязана дать лидера).
5. Складские = честный no_data (в базе нет регистров остатков; эталон
   no_stock согласован).

### ПОРЯДОК ДЕЙСТВИЙ ПОСЛЕ COMPACT (строго)
0. Перечитать этот файл + TARGET.md п.0/9/21 + docs/PLAN_WIKI_CHOICE.md.
1. Дождаться L6 (дозор) → снять карту: сколько/какие clarify-no-data-wrong.
2. По методу владельца: 2-3 вопроса из карты — прочитать ПОЛНЫЕ цепочки
   TRACE (diag_ask), найти единственный излом слоя (величина/период),
   минимальная правка + замок + выкат + живая проба.
3. Перегнать И2 engine; web — отдельным последовательным прогоном
   (--workers 1, --web-url ПОЛНЫЙ путь /v1/chat/completions, web-timeout
   300; гейт не тянет параллель — upstream timeout).
4. К8 → автоприёмка. Утренний отчёт владельцу одним письмом.
5. Хвосты: п.9-языки; валюты/классификатор; ось-подразделения в подписи
   «организаций»; К4-кейс (давний красный, требует живой БД); 3 тяжёлых
   замка (chunk_identity/plain_key/measures).

### РИТУАЛЫ (выучено гейтами за день)
- git add и git commit — РАЗНЫМИ вызовами Bash (гейты смотрят индекс ДО
  команды); пат-спек; сообщение одной строкой с «Числа:» и «Доки:».
- MCP-граф: add_observations → подождать записи на диск (grep!) → git add
  mcp-memory.json отдельным вызовом → коммит с ним же.
- Кодовый коммит без CHANGELOG не пройдёт; SQL в диффе требует «Доки: раздел».
- deploy-ask.sh только из HEAD=дерево; scp в /opt гейтится так же.
- pkill и запуск — разные вызовы; паттерн с [] от самопопадания.
- Фоновые ssh-дозоры: только с disable_timeout=true; они следят за PID на
  окне (nohup), локальный ssh-клиент умирает по 600 с — это НЕ падение.
- Векторизация — только docs/EMBED_BULK_HOWTO.md §9 дословно; один bulk.
- Владелец — парень; не обещать сроки без деления остатка на замер.

### ИСТОРИЯ ДНЯ (кратко, детали в CHANGELOG 01.09)
Ночь: векторы добиты (корпус+резолвер, 0 NULL), такт зелёный с гейтом
(2 фикта: map_concat-дедуп, пачечная миграция content_hash), 27B вернули,
вики-цикл впервые проверен живьём (все исходы), И2-1: 11/67 wrong.
День: «один путь» (вырезано ~230 строк обходов, замки переписаны), 6 слоёв
починено по живым пробам, прибор И2 получил kind-less web-вердики и
честный kind-первой-веткой; «организации» = 1 (эталон!) на одном пути.

### КЛЮЧЕВЫЕ ФАКТЫ ОКНА (продолжение)
- ask: :8091 прод (код 2717db2 выкачен), ASK_TOKEN из /etc/1c-serene-ask.env;
  веб-гейт :18801, WEB_TOKEN = /home/undebot/.openclaw-web/openclaw.json →
  gateway.auth.token; И2: work/gold/i2_runner.py на окне (есть --workers).
- Такт: запуск руками systemctl start 1c-serene-pipeline@postgres
  (Restart=no, таймер погашен); гейт потери 0.5% в умолчании.
- Reshoot-правила приёмки: /opt/1c-mcp-reports/work/gold/reshoot-rules-okna.json
  (67 bindings, правила sales_sum/org_count/stores_count/no_stock и др.;
  эталоны сверены живьём: организаций 1, валют 8, книгапродаж 76 075).
