# ПАМЯТКА линии «СЛОВАРЬ: axes + чистая генерация» (главный якорь после компакта 20.09 утро)

## ТРИ ОПОРЫ — как всегда (полностью: continuation-onepath-20260919.md)

1. **Окно**: ssh -i ~/.ssh/id_ed25519_deploy -p 2202 root@gpu-erw.timpul.pro;
   psql -h 127.0.0.1 -p 7890 -U postgres -d postgres; ask :8091 (Bearer
   pmPzRL4Zm54-jfuLN06hRiwFrkXNvPtV). Полигон :8092. Выкат ask:
   work/deploy-ask.sh; генератора: ubuntu/serenedb/deploy_wiki_alias.sh.
2. **TARGET**: п.0 КОРобочность — ЖЁСТКО (владелец повторил 20.09); п.12 меню;
   п.19 модель не считает; п.21 отказ при живых данных = дефект.
3. **Агенты**: исполнители ТОЛЬКО свежие `cursor-agent -p "$(cat промт.md)"
   --force --model auto --output-format stream-json | tee лог`, фоном,
   disable_timeout; красная ×3 до ACCEPT×3; диагностика/аудит ×3 одинаковых
   заданий (правило владельца); git: add и commit РАЗНЫМИ вызовами; пат-спек;
   «Числа:/Доки:» в сообщении; граф тем же коммитом.

## СОСТОЯНИЕ (20.09 утро, HEAD 6db9176, всё запушено)

- **Система = до-линейное состояние**, проверено якорями (A1 старый известный
  name-отказ; B1/B2/A2/A3 старые меню, регистр первым). Боевой словарь
  259/777 из снапшота, карточки 351 снапшотом, солр 161, таймер
  1c-serene-pipeline@postgres.timer ЗАПУЩЕН (имя именно @postgres!).
- **Сделано и закоммичено**: 9e2b387 (axes во входе генератора + V2 промты),
  24e6fca (9 правок промтов в 4 круга красной до ACCEPT×3), f831413
  (§3.121-122), ee82183 (итог генерации), 6db9176 (свайп→батарея→откат,
  §3.125-126). Замки генератора 526/0 (prompts_v2 139). Промты/селекты с axes
  задеплоены на окно (33 файла, md5 ok).
- **НЕ закоммичено (в рабочем дереве)**: git-инструмент окна замены
  `ubuntu/serenedb/wiki_dict_swap.sh` + `wiki_dict_rollback.sh` +
  `test_wiki_dict_swap.py` (замок 30/0, bash -n чист). **Красная ×3
  ОТКЛОНИЛА (2 из 3 на момент компакта; 3-й лог
  .claude/state/cursor-run-swaptool-red3.log)**: блокер — Я зашил имя базы
  `alias_okna_c5` в имена черновиков (п.0!). Фикс-круг: имена черновиков из
  env ALIAS_TABLE/MEASURE_TABLE (как wiki_alias.sh cycle), замку —
  отрицательную проверку на слова базы, потом свежая красная ×3, коммит.
- **Сохранено на окне**: черновик alias_okna_c5_cycle_20260919 (255 записей,
  253 алиаса, 2 скипа) + alias_okna_c5_measure_cycle_20260919 (753 меры) +
  probe (ok=314 слов, failed=2); снапшоты *_pre_fullswap_20260920 (словарь
  259/меры 777/карточки 351). Env окна /etc/1c-wiki-alias-postgres.env
  ВОЗВРАЩЁН к OpenRouter (бэкаки .bak-dictwave-*); sandbox-конфиг генератора
  .openclaw-sandbox переведён на OpenRouter, maxTokens 24576 (фикс §3.123),
  ALIAS_ENSURE_VLLM убран (дефолт вкл; ловушка §3.122 — ensure от рута
  переписывает конфиг при СМЕНЕ ключа; долг: чинить ensure runuser'ом).

## ГЛАВНЫЙ ФАКТ ЭКСПЕРИМЕНТА (зачем всё было)

Полный cycle «как с новой базой» на 27B владельца (178.63.211.188:8000,
инстанс временный, МОЖЕТ быть погашен): осевые слова легли на 16
документов-событий, но НЕ на целевую пару продаж — модель выбросила их там
под давлением плотного промта (INIT-A = 8 вложенных правил после 9 правок).
Свежий словарь на якорях ХУЖЕ старого (A1 мусор-меню 24, мостовые слова
распределились) → свап откачен по стоп-правилу. Вывод владельцу озвучен:
**строгий п.0 сейчас не выполняется** — новая база получит такой же словарь
с теми же дырами; лечить генератор (упрощение/разнос правил, обязательный
минимум первой оси), НЕ ручными заплатками.

## ОШИБКИ СВОИ — ВСЕ ЗАПИСАНЫ (HOW_NOT_TO §3.121–3.128)

121 экстраполяция времени (владелец: «враньё»); 122 ensure root-права;
123 maxTokens length (12288→24576); 124 хвост collision стоп с сохранением;
125 откат ≠ пересборка (карточки из снапшота!); 126 stateless-меню пробы
(«1» = новый вопрос, не выбор; меню — только с сессией/веб); 127 откат
heredoc'ами вместо git-канона (владелец: «откатывай через git»); 128 нет
контрольной точки на ЦЕЛЕВЫХ записях в движении прогона (владелец: «5 часов
впустую») — обязательный SELECT к паре продаж на первом часу любого прогона.

## ДАЛЬШЕ — ТОЛЬКО ПО СЛОВУ ВЛАДЕЛЬЦА (три опции, моя рекомендация — 1)

1. **Дожать промт**: упростить/разнести правила (меньше вложенности; «первая
   ось обязательна всегда» вместо «1–3 на усмотрение») → красная ×3 →
   генерация ТОЛЬКО кластера продаж (~15 вызовов, минуты) → SELECT-контроль
   целевой пары → только потом полный прогон (с контрольной точкой §3.128).
2. **Union-promote** черновика поверх старого боя (штатный promote;
   старые мосты остаются, новые ранги добавляются) — одной командой после
   починки swap-инструмента.
3. **Стоп линии** — система работает как до, всё наработанное в git.

## ЧЕСТНЫЕ ГРАНИЦЫ (помнить)

- Батарея stateless: якорь только прямые числа; меню — первый-правильный
  или в вебе владельцем (§3.126). «B3 = прямо 3027» было моим непроверенным
  допущением — по контракту «сколько документов» без типа = меню.
- Инстанс 27B: если погашен — генерация невозможна до нового слова владельца.
- Эмбеддер/реранкер: владелец вернул; при след. свапе карточки — только из
  снапшота (§3.125), эмбед через embed_missing.sh (env /etc/1c-embed.env).
