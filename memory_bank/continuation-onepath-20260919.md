# ПАМЯТКА линии «ОДИН ПУТЬ + ПРАВИЛЬНЫЕ ОТВЕТЫ» (главный якорь; обновлена 19.09 поздний вечер)

## 🔴 ТРИ ОПОРЫ — читать первыми, перед любым действием

### 0. Работаем ТОЛЬКО на сервере окно
- Окно: `ssh -i ~/.ssh/id_ed25519_deploy -p 2202 root@gpu-erw.timpul.pro`
- psql: `-h 127.0.0.1 -p 7890 -U postgres -d postgres`
- ask-прод :8091 (Bearer pmPzRL4Zm54-jfuLN06hRiwFrkXNvPtV); мост :6016.
- Полигон :8092 = /tmp/probe_root (ручной процесс; рестарт: kill, затем
  `cd /tmp/probe_root && set -a && . /etc/1c-serene-ask-staging.env &&
  . /etc/1c-serene-ask.env && set +a && nohup /opt/openclaw-mcp/venv/bin/python
  /tmp/probe_root/serene_ask.py >>/tmp/probe_root/staging.log 2>&1 &`).
  Локальная проба полигона: туннель `ssh -L 18092:127.0.0.1:8092`, потом
  AB_PROBE=okna ASK_URL=http://127.0.0.1:18092/ask — отметка
  .claude/.probe-okna-last-run (0err/N) открывает гейт выката.
- Выкат: `bash work/deploy-ask.sh` (после push; дерево=HEAD) →
  `systemctl restart 1c-serene-ask@postgres`.

### 1. TARGET.md (контракт, менять нельзя)
- **п.0 КОРобочность**: работает на чужой базе из коробки, без правки. Никаких
  слов/имён конкретной базы в коде, порогов под базу, подгонки под известные
  вопросы. Проверка каждой правки: «может ли механизм узнать нужное из движка/
  $metadata/модели — спроси их, не разбирай слова». Главный фильтр любых решений.
- **п.10**: ошибочные ответы запрещены. **п.12**: уточнение/отказ — не ошибка,
  догадка — ошибка; >1 прочтение → меню, ровно 1 → сразу ответ.
- **п.19**: модель НЕ считает и НЕ ищет — только смысл; числа всегда из SQL.
- **п.20**: своих скриптов для базы нет. **п.21**: ответ → уточнение → честный
  отказ; отказ при живых данных = дефект; после уточнения ответ обязан доехать.
- 🔴 Запрет промт-правил: правила для модели живут КОДОМ (валидатор, fallback),
  не текстом промта. Промт описывает задачу, не долженствования.

### 2. КОНТРАКТ «ОДИН ПУТЬ» (приказ владельца 12.09; подтверждён 19.09: «никакого больше»)
вопрос → интерпретация из вики (z21 wiki-каскад: пул → pick → verify) → запрос
в базу → варианты с описаниями из вики (>1 прочтение → меню) → выбор → ответ.
- Вторых выбирающих НЕТ (снесены 12.09, −7800 строк; держит замок test_one_path
  78/0). Все меню — единый построитель readings_menu.
- Добавленное 17–19.09 — СТУПЕНИ того же тракта, не пути рядом: ТОЧКА 1
  (rescue полного пула при смерти каскада), ТОЧКА 2 (concepts перед отказом),
  пачковый pick+verify (FALLBACK1), параллельные паспорта, CNT-SUBJECT (слой
  «что считать»: записи vs разные значения оси, до SQL).
- 🔴 Указание владельца 19.09 (дважды): НИКАКИХ новых механизмов и усложнений.
  Сняты с рассмотрения как подгонка: фильтр живости кандидата (ломает честные
  нули), эвристика различимости подписей (ловля по словам). Правило: сначала
  живой журнал/diag, потом выводы; не лечить по симптомам.

### 3. ПРАВИЛА РАБОТЫ АГЕНТАМИ (оркестратор Kimi → исполнители cursor-agent)
- Исполнители — ТОЛЬКО свежие `cursor-agent -p "$(cat промт.md)" --force
  --model auto --output-format stream-json 2>&1 | tee лог`, фоном, таймаут
  ОБОЛОЧКИ ≥1800000мс (урок: без явного timeout фоновая задача умирает на 600с).
  1 задача = 1 процесс; резюмировать сессии запрещено. Регламент:
  docs/ORCHESTRATION_CURSOR.md §9.
- **Красная ×3** свежих на каждый дифф до ACCEPT ×3; промт красной — под новый
  контекст; вердикт из последней assistant-записи с «ВЕРДИКТ». Работает: две
  волны подряд красная ловила реальные дыры принятого ×2 диффа (AXISLOOP: слip
  глотал «по складам»; CNT-SUBJECT: rank-кража + later-щель) — доводка кругами
  до ACCEPT×3 обязательна.
- Разбор непонятного — тоже агентами (диагносты, независимые линзы, только
  чтение); оркестратор сам делает: живые замеры/пробы на окне, приёмку, коммиты.
- Промты самодостаточны: цель с числом-критерием, точные файлы, что НЕ трогать,
  живые факты вложены текстом (агенты на сервер не ходят). Ложный блок
  check-prompt-rules → python-heredoc, отметка в коммите.
- Git-ритуалы: add и commit РАЗНЫМИ вызовами (снайпер в одной команде с commit
  блокирует ВСЁ, включая python); после MCP-записи графа — пауза 2–4с, re-add
  mcp-memory.json, проверка `git show :...| grep -c <маркер>` ДО commit (гонка
  ловилась 4 раза за день); коммит пат-спеком `git commit -- файлы`; push;
  выкат ОТДЕЛЬНОЙ командой; «Числа:/Доки:» в сообщении; граф тем же коммитом.

## СОСТОЯНИЕ (19.09 поздний вечер)

- git HEAD **142b8a2** (всё запушено). День: 9210b8e AXISLOOP+LLMCLICK → 7b3e1f7
  LLMCLICK-2 → 90eb639 CLEANUP → 8f6d8fa L67-фин → a9290ec решение владельца →
  40a8af9 CNT-SUBJECT → 142b8a2 якоря.
- Прод (окно): z05/z10/z18/z20/z21 выкачены, юнит активен. Конфиг прежний
  (OpenRouter qwen/qwen3.8-27b, DS_REASONING_OFF=1, воркеры 8).
- **Что работает живьём**: петли меню мертвы (после клика ответ доезжает всегда,
  L67 refusal_defect=0); «сколько продали за период» — числом за 2–4 клика;
  «сколько X» через регистр — уникальные X (живое: «183 уникальных клиента»
  DISTINCT оси, было 76 202 записей; diag cnt_subject=axis_values);
  «сколько документов за декабрь 2025» = 3 027 не сломан; топ-вопросы идут
  rank-путём (rank-guard не съел).
- **Решение владельца 19.09**: скорер/L67-линейку НЕ чинить (числа прогонов —
  не мерило), приёмка — руки владельца в ВЕБЕ.
- Замки (все зелёные): cnt_subject 38/0, one_path 78/0, axis_loop 23/0,
  terminal_round 24, compose 91, axis_count 13/0, degenerate 97, decision_id 24,
  gate 56/0, step4 27/0, onepath_rescue 119, homonym 42 и др.

## ТЕКУЩАЯ РАБОТА: диагностика name-хвоста (топ-3/лучший клиент) — ДИАГНОЗ ГОТОВ, лечение не начато (ждём слова)

Три независимых диагноста сошлись (отчёты .claude/state/cursor-run-diag-{pool-a,verify-b,pool-c}.log):

- **Путь цел; ошибка ДО него — в поиске кандидатов.** Пул = UNION слоёв (kNN
  вектор + структурные совпадения стемов + bm25-алиасы) → DISTINCT ON → сортировка
  src_layer DESC (структурные ВЫШЕ векторных) → LIMIT 8.
- «сколько ПРОДАЛИ за вчера»: стем «прода*» держит основной document_реализациятмц
  в топе → меню близнецов → верно. «топ-3 ТОВАРА за вчера»: глагола продажи нет,
  «товар» тянет номенклатуру/экспорт, «вчера» (в сыром тексте) — календари/константы;
  мусор занимает все 8 мест, ОСНОВНОГО РЕГИСТРА В ПУЛЕ НЕТ → verify sole-yes на
  единственном осмысленном (экспорт) → leader → SQL 0 записей → «данных нет».
  Штатно по контракту: тракт не может знать, что «настоящий» кандидат не долез.
- «какой клиент больше всех купил» — пул широкий и правильный, меню приходит
  (контракт работает; WRONG в L67 был от скорера).
- **Имена в топ-ответах — подписи ссылок из корпуса** (не ключи): несовпадения —
  следствие неверного источника; отдельной починки имён НЕ нужно.
- Rescue (ТОЧКА 1) не участвовал: он только после none, не после ложного лидера.

**Согласованный порядок лечения (владелец: «усложнять нельзя», коробочность п.0):**
1. Агенты: снять пробелы — каких связок не хватает (в терминах СТРУКТУРЫ:
   ранговая формулировка + сущность ↔ регистр оборотов с этим измерением), не слов базы.
2. Правка промта ГЕНЕРАТОРА словаря (универсальная, п.0 цел; не бота!) — охват
   формулировок без глагола («топ/лучший/больше всех + сущность»).
3. Перегенерация/дообучение словаря на окне — как ПРОВЕРКА: те же вопросы →
   основной регистр в пуле → меню → верный ответ.
4. НЕ делать: ручные алиасы в словарь окна (подгонка), полная перегенерация всего
   словаря (риск), новые механизмы в тракте (запрещено владельцем).
- Коробочность держится: словарь = производная от метаданных базы (на новой базе
  строится заново её же генератором) + шаг 7 пайплайна (живой доучивающий контур
  из журналов любой базы). Штатный пайплайн: PLAN_DICT_AUTOMATION, шаги 2–7.

## ОТКРЫТЫЕ ХВОСТЫ (после словарной волны)

- Хвост-2 (не закрыт, отдельные две точки интерпретации, НЕ выбор сущности):
  «почему в воскресенье ноль, это сбой?» → лишний clarify (нужен ответ-объяснение
  факта «0 записей — данных нет, не сбой»); «сколько петель осталось на складе?» →
  меню осей вместо честного no_data по незнакомому товару.
- 145 vs 183 на «сколько клиентов реально покупают»: обе честные (уникальные),
  разные источники/периоды под формулировку — какое правильное, решит владелец
  руками в вебе.
- Долги: зависание :8091 (причина не найдена, сторож /tmp/run_l67_watch.sh);
  кредиты 402 (веб); ложные блоки check-prompt-rules (.md); словарная линия шаги 2–7.

## УРОКИ ДНЯ (кратко; полные в HOW_NOT_TO при закрытии линии)

1. Факты раньше предложений: предложил механизмы до разбора журнала — владелец
   верно снял (подгонка). Сначала живой diag, потом вывод.
2. Красная ×3 реально ловит: две волны подряд по 2 круга доводки. Не пропускать.
3. Гонка графа — 4 раза за день: пауза + re-add + проверка staged перед commit.
4. Снайпер блокирует команду целиком, если в ней есть `git commit` — python/add
   отдельными вызовами.
5. «Замки зелёные + точечные пробы ≠ система ок»: имя-дыра жила вне диффов
   (наследие конструкции каскада), красные её не видели — полный набор живых
  вопросов показывает.
