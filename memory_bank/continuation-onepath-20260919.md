# ПАМЯТКА линии «ОДИН ПУТЬ + ПРАВИЛЬНЫЕ ОТВЕТЫ» (активная; обновлена 19.09 вечер)

## 🔴 СОСТОЯНИЕ ВЕЧЕРА 19.09 (после волны AXISLOOP)

- git HEAD **90eb639** (всё запушено). Коммиты дня: 9210b8e (AXISLOOP+LLMCLICK,
  красная ×3 оба диффа) → 7b3e1f7 (LLMCLICK-2: max_tokens 512, дефолт gpt-4o-mini)
  → 90eb639 (CLEANUP мёртвого score-кода, красная ACCEPT ×3).
- Прод (окно): z20+z10 выкачены deploy-ask.sh, юнит @postgres перезапущен; полигон
  :8092 = /tmp/probe_root (ручной, перезапускается: kill + cd /tmp/probe_root +
  source двух env: /etc/1c-serene-ask-staging.env + /etc/1c-serene-ask.env).
- **Петли меню осей МЕРТВЫ**: refusal_defect=0 в полном L67. Живые пробы: «сколько
  продали на этой неделе» → 432 783.89 за 3 клика (меню осей исчезло);
  «сколько клиентов реально покупают» → ответ за 2 клика (меню 30→13 опций).
- Финальный L67: **8 OK / 17 неверных / 0 honest_no / 0 refusal_defect**, средняя
  35.7 с. Разбор 17 по классам — CHANGELOG 19.09(7): (а) LLM-клик портит МЕРЫ
  («кол-во» вместо «итого»; сущности выбирает верно); (б) дефекты замера (got-парсер
  склеивает даты/числа; name-эталоны с запятыми ломают missing; clarify-needle=0);
  (в) продуктовые: count по оси без DISTINCT (76 202 записей vs 145 клиентов),
  name-класс, 2 лишних clarify.
- Порядок следующего фронта: (б) замер починить первым (обесценивает вердикты) →
  (в) count-DISTINCT → name-класс → (а) решить политику клика (сущности LLM,
  меры/периоды токен-матч) — решение владельца.

## 🔴 ЧИТАТЬ ПЕРВЫМ, ПЕРЕД ЛЮБЫМ ДЕЙСТВИЕМ — уроки, стоившие дня работы

Эти правила выучены живыми деньгами этой сессии. Они ЗДЕСЬ, а не в HOW_NOT_TO,
потому что туда ты не смотришь. Перед каждым шагом сверься с этим списком:

1. **«Готово» = полный L67 прогнан и зелёный.** Не 6 вопросов, не замки, не смоук.
   Замки зелёные + точечные пробы OK ≠ система ок (доказано 19.09: замки 24/24
   зелёные, а L67 живой показал картину). Полный набор — ДО доклада владельцу.
2. **Правильность важнее формы.** Эксперимент «1 запрос = 1 вызов без пачек»
   (решение владельца) убил СРАВНЕНИЕ кандидатов: близнецы-регистры получали
   одинаковые «да»/оценки → меню вместо ответов → L67 1-2/25. Откат на пачки
   выполнен (FALLBACK1, 0912ee6). Сравнение кандидатов — только в одном вызове.
3. **Не измеряй новым инструментом и не хвались числом, пока не проверил, что
   инструмент гоняет ТО, что ты выкатил** (L67-прогон взял старую копию скорера
   из /tmp — результат «2/25» был старым скорером без кликов).
4. **Скорер должен вести себя как человек**: кликать меню по совпадению подписи с
   вопросом, до 4 хопов, вердикты по TARGET п.12/п.21. НО: LLM-клик силён для
   СУЩНОСТЕЙ (гомонимы: клиенты→Контрагенты) и слаб для МЕР (кликает «кол-во»
   вместо «итого») — живой L67 19.09.
5. **Правки файлов канона/скриптов — проверять grep'ом сразу после правки**
   (NameError в heredoc молча съел правку канона).
6. **Промежуточные состояния не дожимать** — если направление отменено, коммить
   только финал (владелец: «зачем дожимать то, что не нужно»).
7. **Зависание сервиса :8091 под длительной нагрузкой** — symptom: LISTEN есть,
   ответа нет, рестарт лечит. Сторож: /tmp/run_l67_watch.sh. Причина НЕ найдена.
8. **Гонка графа ловится ЧЕТЫРЕ раза за день**: после MCP-записи — пауза 2–4 с,
   `git add mcp-memory.json` ОТДЕЛЬНО, проверка `git show :...| grep -c <маркер>`
   ДО commit, и только потом commit.

## 🔴 ТРИ ОПОРЫ — держать в голове всю сессию

### 1. TARGET.md (контракт, 21 пункт, менять нельзя)
- **п.0**: база разработки ≠ прод; прод скрыт специально — работает из коробки на
  чужой базе БЕЗ правки. Никаких списков слов/имён базы в коде, порогов под базу,
  подгонки под известные вопросы. Проверка каждой механики: «что знает движок/
  $metadata/модель — спроси у них, не разбирай текст».
- **п.10**: ошибочные ответы запрещены (числовые и смысловые).
- **п.12**: отказ и уточнение — НЕ ошибка; догадка — ошибка. Несколько прочтений →
  меню; «продали» = 4 регистра-близнеца → меню канонично.
- **п.19**: модель НЕ считает и НЕ ищет — только смысл; числа всегда из SQL;
  контекст в модель ограничен и не растёт с базой.
- **п.20**: своих скриптов для базы нет — всё внутри SereneDB штатным.
- **п.21**: ответ → уточняющий вопрос → честный отказ; отказ при живых данных =
  дефект; после уточнения ответ ОБЯЗАН доехать.

### 2. КОНТРАКТ «ОДИН ПУТЬ» (приказ владельца 12.09, действует всегда)
вопрос → интерпретация из вики (z21) → запрос в базу → варианты с описаниями из
вики (если прочтений больше одного) → выбор → ответ. Ничего больше. Вторых
выбирающих сущности нет; выбор сущности — только wiki-каскад (пул → pick →
verify). Текущая реализация: пачковый pick+verify (FALLBACK1) + C-волны (ТОЧКА 1
rescue, ТОЧКА 2 concepts, spans z06, гард z14, журнал unknown_*) + параллельные
паспорта + (19.09) исправные билеты осей (AXISLOOP) и skip-стражи на месте.
Мёртвый score-слой снесён (90eb639). Канон решений: .claude/state/design-c.md §2.

### 3. ПРАВИЛА РАБОТЫ С АГЕНТАМИ (оркестратор Kimi → исполнители cursor-agent)
- Исполнители — ТОЛЬКО свежие процессы `cursor-agent -p "$(cat промт.md)" --force
  --model auto --output-format stream-json | tee лог` фоном с timeout≥1800с у
  ОБОЛОЧКИ (урок дня: фоновая задача без явного timeout умирает на 600с);
  1 задача = 1 процесс; резюмировать сессии запрещено. Регламент:
  docs/ORCHESTRATION_CURSOR.md §9.
- **Красная ×3** свежих на каждый дифф до ACCEPT ×3; промт красной обновлять под
  НОВЫЙ контекст; вердикты из последней assistant-записи с «ВЕРДИКТ». Работает:
  красная №3 круга-1 нашла ДВЕ реальные дыры в принятом ×2 диффе — не пропускать.
- Промты самодостаточны: цель с критерием-числом, точные файлы, что НЕ трогать.
  Ложный блок check-prompt-rules → python-heredoc (правило 7) с отметкой в коммите.
- Обязанности оркестратора: замеры, приёмка, коммиты. Вера сводке запрещена.
- Git: add/commit РАЗНЫМИ вызовами (снайпер); после MCP-записи графа — пауза и
  re-add с проверкой staged; коммит пат-спеком; выкат ОТДЕЛЬНОЙ командой после
  пуша; «Числа:/Доки:» в сообщении.

## МЕХАНИКА (коротко)

- Окно: ssh -i ~/.ssh/id_ed25519_deploy -p 2202 root@gpu-erw.timpul.pro; psql
  -h 127.0.0.1 -p 7890 -U postgres -d postgres; ask :8091 (Bearer pmPzRL4Zm54-
  jfuLN06hRiwFrkXNvPtV); полигон :8092 = /tmp/probe_root (ручной процесс,
  перезапуск: два env-файла staging+ask); туннель для локальной пробы:
  ssh -L 18092:127.0.0.1:8092.
- L67: ab_scorer в репо → scp на окно /tmp/ab_scorer.py (ПОСЛЕ каждой правки!);
  watcher nohup /tmp/run_l67_watch.sh (нужны env прода: DEEPSEEK_API_KEY и пр. —
  запускать `set -a; . /etc/1c-serene-ask-postgres.env; set +a;` перед nohup);
  вывод /tmp/l67_click.out, DONE в /tmp/l67_watch.log.
- Проба выката: AB_PROBE=okna ASK_URL=http://127.0.0.1:18092/ask (туннель к
  полигону с КАНДИДАТОМ) → отметка .claude/.probe-okna-last-run (0err/N) →
  гейт check-golden пускает deploy-ask.sh → рестарт 1c-serene-ask@postgres.
- Замки: axis_loop 23/0, one_path 78/0, onepath_rescue 119/0, terminal_round 24,
  compose 91, measure_degenerate 97, degenerate/homonym/gate/step4 и др. — все
  зелёные (19.09 вечер).

## ДОЛГИ ВЛАДЕЛЬЦУ (не блокируют)

Причина зависания :8091 (не найдена); кредиты 402 (веб); ремонт
check-prompt-rules (ложные блоки .md); search_dict_syn; словарная линия шаг 7.

## 🔴 РЕШЕНИЕ ВЛАДЕЛЬЦА (19.09, вечер)

- Скорер/линейку НЕ чинить: приёмка — руки владельца в вебе. Числа L67-прогонов
  далее не мерило; живые вопросы в вебе — мерило.
- Единственная следующая правка продукта: «сколько X» через регистр по оси X
  считает строки (76 202), а не разные X (145 клиентов) — правило тракта
  «count по оси = DISTINCT по оси», одно место в общем механизме счёта, без
  ветвлений по типам вопросов.

## CNT-SUBJECT ВЫКАЧЕН (40a8af9, вечер 19.09)

- Слой «что считать» в проде: клик-цепочка «сколько уникальных клиентов покупает?» →
  клик регистра → клик оси «Контрагенты (Контрагент)» → «У нас 183 уникальных
  клиента», diag cnt_subject=axis_values, count_distinct_axis=Контрагент (было
  76 202 записей словами «клиентов»). «Сколько документов за декабрь 2025» = 3 027
  (records, не сломан). Топ-3 товара → rank-путь жив (rank-guard не съел).
- Эталон L67 «клиентов реально покупают»=145 vs живое 183 — механика верна
  (уникальные по оси), расхождение — интерпретация источника/периода; приёмка —
  руки владельца в вебе.
- Красная: круг-1 REJECT×2 (rank-кража) → круг-2 REJECT×2 (later/diag-None) →
  круг-3 ACCEPT×3. Замок test_cnt_subject 38/0.
