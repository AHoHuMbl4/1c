# ПАМЯТКА ПРОДОЛЖЕНИЯ: волна «слои 2/3 + дайджесты» — ЗАВЕРШЕНА 18.09 (приёмка живыми пробами)

Читать ПЕРВЫМ при продолжении. Сестринские: continuation-web-20260915.md (web1/web4),
continuation-pipeline-20260915.md (словарный пайплайн).

## ИТОГ ВОЛНЫ (коммиты 6b7148a → ee40415, все запушены)

- D0 контракт (числа в опциях clarify; sole с пометкой — политика) — activeContext.
- D1 guard вырожденности (z20+z14; замок 94→97) — красная ×3 (10 кругов).
- D2 гомоним из базы + kind-подписи (z21+z20; замок 42) — красная ×3 (5 кругов).
- D4 ч.1 дайджесты+kind-prior+LLM-арбитр (замок 52) — ×3 (6 кругов); ч.2 мост
  label+hint + verify 144 — ×3 (1 круг).
- D3 ask_journal: measure_degenerate JSON + decision_id (замок 21) — ×3 (1 круг).
- Выкат: ask (z14/z20/z21) + мост mcp_ask + миграция на окно; рестарты; health ок.
- F-hotfix (ee40415): psql-csv булевы 'f'→bool()=True — все меры «живы» → ложный C1
  (боевой дефект пробы); фикс _measure_sql_bool; красная ×3; перевыкат.
- ПРИЁМКА ЖИВЫМИ ПРОБАМИ: решающая «сколько выручки за август» → entity-меню
  (дайджесты+★) → выбор регистра → МЕНЮ ЖИВЫХ МЕР С ЧИСЛАМИ (итого 4 218 825,39 /
  без НДС 3 515 686,39 / затраты 2 576 877,60 / кол-во 94 904,20 / выручка —
  «0 · не ведётся») → клик → **4 218 825,39** atom, «на момент вопроса»,
  «(регистр накопления)». НИ «0,00», НИ «продаж не было». Разбавитель/named-kind
  (честный отказ на пустом окне)/розница (NO DATA)/формы (entity-меню) — по канону.
- web4-регресс :18801 — ЗАБЛОКИРОВАН ВНЕШНЕ: 402 Insufficient Balance на модели
  веб-профиля (кредиты); миля мост→меню с числами доказана прямой MCP-пробой.
  Повторить веб-пробу после пополнения.

## ОТКРЫТО (владельцу)

0. **Прогон 67 вопросов (вход владельца 18.09, не чинить до слова)**: wrong 0; 8 меню
   (честные); 18 отказов корректны (no_data эталон). 6/67 (9%) — отказ при живых
   данных: 4 × «редкое слово» (наторговали/наотгружали/вышло — глаголы-события,
   словарная линия, аудит 12.09 это уже знал); 1 × «Сколько движений в регистре
   реализации ТМЦ?» — склеенное имя с пробелом (entity-поиск z06, родственный
   D2-нормализации класс, остальные 4 того класса закрылись); 1 × «сколько у нас
   вообще клиентов сейчас» — механизм bm25-топ-3 (диагноз 15.09, ждёт РЕШЕНИЯ
   ВЛАДЕЛЬЦА; разблокирует и дайджест-ветку для таких вопросов — кандидат сейчас
   не доходит до меню). Все 6 — до механизмов D1–D4 (поиск/словарь), не guard.
1. Кредиты: 402 на модели веб-профиля (веб-проба ждёт); субагенты Kimi — 402.
2. Ремонт хука check-prompt-rules: при StrReplace сканирует весь файл, не добавленные
   строки (ложные блоки z20/z21/z14 — волна работала через apply-скрипты
   .claude/state/d{1,2,4,3,4b}fix*-apply.py, все проверены и применены).
3. search_dict_syn: ASK_SOLR_SYNONYMS_DICT указывает на отсутствующую таблицу.
4. CHANGELOG: вшитые маркеры конфликта (коммит 6d4b955, соседняя линия).
5. llm_pick↔human_pick: метрика теперь пишется в ask_journal (D3) — базовая линия
   для будущего решения об автоответе арбитра (вне волны).

## КАК ПРОДОЛЖИТЬ (следующая сессия)

- Прочитать design-b.md §1/§1b/§2/§3 (канон), CHANGELOG 17.09(1)–18.09(5), этот
  итог. Замки: ubuntu/serenedb/test_{measure_degenerate(97),measure_digest(52),
  wiki_homonym_menu(42),one_path(78),ask_journal(21)}.py + verify-plugin 144.
- Правила те же (оркестратор/исполнители/красная ×3/git-ритуалы/гейты; add и commit
  РАЗНЫМИ вызовами — гейт снайпера/graph ловят цепочку).
- Сервер окна: ask=HEAD md5 (z20 6e98e2a9…), мост и миграция применены.

## ОТКУДА

Веб-диалог владельца «сколько выручки…» дал 4 слоя дефектов. Слои 1 и 4 (конверт моста,
финальная миля плагина) закрыты 15.09. Эта волна — слои 2/3: гомоним «Реализация ТМЦ»
(документ↔регистр) и мёртвая мера («Сумма»≡0 на 79 902/79 902 строк, деньги в «Всего»,
словарь вешает «выручку» на мёртвое). 15–16.09 обсуждение с владельцем расширило волну:
дайджесты в опциях меню (числа/счётчики/превью прямо в кнопках) — правка контракта
«один путь». СТАРТ исполнения — только по слову владельца.

## СОСТОЯНИЕ (что уже сделано, не повторять)

- Анализ ×4 линзы (cursor-agent, реестры): `.claude/state/a1-registry.md` (ask-путь:
  точка `_onepath_compose_gate` z20:1630, после period_empty ~1650 и distinct_axis,
  до `_answer_pairs` z20:1665), `a2-registry.md` (гомоним pool-local: z21:847),
  `a3-registry.md` (эталоны окна: регистр «Всего» август 4 218 825,39 / 14.09 72 948,21;
  «Сумма»≡0; розница до 2024-01-30; карточки вики обоих гомонимов есть; «до»-пробы:
  живой ноль по мёртвой мере, меню обоих прочтений уже приходит),
  `a4-registry.md` (карта всех форм: compare agg.sum=DIFF, count только окна-1;
  group верхние min/max=None; rows coalesce NULL→0; live-путь только у aggregate).
- Дизайн `design-b.md` прошёл **17 красных кругов ×3 = 51 вердикт** (все REJECT,
  блокеры убывали от архитектурных до однострочных). Текущая версия **v17** — канон.
  Ключевое внутри (всё выстрадано красной, не выдумывать заново):
  - триггеры по формам с приоритетом compare > group|rank > sum > list; sum без
    compute=="sum" (на бою compute пуст); rank при пустых groups выровнен с list
    (∨ count_amount==0); список исключений (count, stock/distinct — граница);
  - degeneracy: ЧИСЛОВАЯ мера мертва ⟺ нет ни одного ненулевого во всей области
    (константа ≠0 — ЖИВАЯ, п.21); нечисловые — DISTINCT≤1; ОДИН SELECT (FILTER по всем
    мерам, alive+max_abs скалярами), FROM всегда search_corpus(+folder coalesce!) или
    query_table, НИКОГДА search_idx; без match/периода/preds; предфильтр по agg;
    дедлайн перед SELECT; TTL-кэш только полных вердиктов, ключ (src_table, layer);
  - live-probe обязателен каждой мере без ненулевых в nums; force-live = прямой вызов
    aggregate_live_column с match/preds ОКНА (не table-wide! не штатный aggregate);
    формы без live-пути (group/rank/list): 0 живых→текст, 1 nums-alive→sole формой,
    1 live-only→текст-с-итогом, ≥2→меню (live-only пункты с hint ДО клика);
  - sole при n_live==1 ∧ не-proven — ВСЕГДА с видимой пометкой (ADJUDICATION после 12
    качаний п.12↔п.21; ВЛАДЕЛЬЦУ ПОДТВЕРДИТЬ/ОТКЛОНИТЬ на старте; красные не блокируют);
    sole без рекурсии gate, пересобирает measure/totals(live→[])/_num_pred/money/
    say_measure/атом; totals←[] при live или nums-нулях; post-sole zeroish у живой =
    честный 0;
  - proven: settle ВСЕГДА пишет diag["measure_proven_human"] ТОЛЬКО при "measure" in
    choice_levels_proven (не голый measure_pick); guard читает только флаг;
    proven∧1 живой → меню 2 пунктов (запрошенная «не ведётся» + живая) — не тупик;
  - fail-soft: БЕЗ числового атома (текст «не удалось проверить меру» + diag);
    «Доки:» строка готова (FILTER; Aggregates; sdb_columns; Casting/TRY_CAST; ALTER ADD
    COLUMN IF NOT EXISTS);
  - D2: пиры из search_tables по нормализованному label (stem — граница), после
    УСПЕШНОГО wiki_leader_post_verify, общий helper конвейера clarify; ≥3 пира — все;
    названный kind — пиры чужого рода не поднимаются; дешёвый фильтр ambiguous_labels
    с той же норм-функцией; keep_empty_period_opts — граница;
  - D3: ALTER TABLE ask_journal ADD COLUMN IF NOT EXISTS measure_degenerate JSON,
    тем же INSERT при исходе (б), live_measures полным списком.
- **Решения владельца**: долги не оставлять (полное покрытие форм); дайджесты в опциях
  меню (см. D4 ниже); «не подгоняться под окно» (имена только в фикстурах/приёмке —
  карантин, красные проверяли п.0 каждый круг).

## ПЛАН ВОЛНЫ (исполнение по СТАРТУ владельца)

Шаги, каждый = отдельный cursor-agent (не коммитит) + красная ×3 на дифф (до ACCEPT ×3):
0. **v18** дизайна = v17 + D4 → красная ×3 на дизайн.
1. **D0 контракт**: строка-правка «вторых чисел» в activeContext (текст утвердить
   у владельца в докладе): «числа допустимы в опциях clarify; ответ — одно число»;
   замки test_one_path.
2. **D1 guard** (design §1): замок test_measure_degenerate.py (матрица E/B/C/T/F/R из
   v17) — ОБЯЗАН краснеть на HEAD.
3. **D2 гомоним** (design §2): расширение test_wiki_homonym_menu.py.
4. **D4 дайджесты опций** (решение владельца 16.09): единый helper — при построении
   любого меню >1 опции каждая несёт предвычисленный дайджест прочтения: сумма /
   count / размер+превью top-3 / диапазон дат; мёртвые — «0 · не ведётся».
   Теми же SQL, что дали бы ответ, одним SELECT на слой, в бюджете дедлайна; не влезло
   в бюджет — опция без дайджеста с подписью «посчитаю по выбору» (не молча, п.13);
   клик переиспользует посчитанное (pending хранит), пометка «на момент вопроса»;
   verify-плагин пускает числа в опциях (нативная правка плагина, своя красная).
   Подменяет точечные UX-решения: меню живых мер несёт итоги, гомоним — числа обоих
   источников, тупик proven-ветки исчезает. Guard остаётся для одно-прочтённых путей.
5. **D3 feedback** (design §3) + миграция.
6. **E замеры оркестратора**: все замки зелёные (включая mcp_ask 39 + envelope 25 +
   pending 16 + verify 138, one_path, homonym, measure_*, ask_journal, compose,
   rank_*, period_empty, hatch_luk); grep нового кода на имена окна (Сумма/Всего/
   реализцатмц/okna) — пусто в логике; ПРЕДЗАМЕР: живость 5 мер регистра по слоям +
   меры документа + want/compute дефектного вопроса + p95 guard-SELECT (GO/NO-GO:
   целевая мера не видна ни в одном слое — СТОП; p95 guard > 2×p95 aggregate — СТОП).
7. **F коммит→push→выкат→пробы→доки**: коммит пат-спеком (Числа:/Доки:), scp ask/*.py +
   миграция → применение → рестарт 1c-serene-ask@postgres; мост НЕ трогаем; живые пробы
   (мост :6016, каждый свободный вопрос своей MCP-сессией, выбор только по decision_id
   своей сессии, между последовательностями разбавитель «сколько всего валют», между
   кликами никакого свободного текста):
   - «сколько выручки за август» → меню прочтений С ЧИСЛАМИ (D4); выбор регистра →
     **4 218 825,39**; ни «0,00», ни «продаж не было»; фактическая мера названа;
   - «итог по регистру реализации ТМЦ за 13 сентября» (названный kind) → «записей за
     период нет»; контраст без kind → entity-меню (D2);
   - «выручка по рознице за сентябрь 2026» → честное «нет данных»;
   - формы: «средняя выручка за август» (пойдёт sum-веткой), «август против июля»
     (compare), «топ номенклатуры по выручке» (rank/group) → ветка (б)/меню с дайджестами;
   - web4-регресс: «сколько выручки за вчера» через веб-шлюз :18801 → меню доезжает;
     живая web-цифра по ненулевой мере не деградирует;
   - метрика: доля clarify в ask_journal до/после (рост только гомонимный).
   Доки: HOW_IT_WORKS.md, CHANGELOG, эта памятка.

## ОТКРЫТОЕ ВЛАДЕЛЬЦУ (ответить на старте)

1. **Sole-политика** (adjudication): при единственном живом соседеотвечаем его числом
   с видимой пометкой «вместо X, которая не ведётся» — или даём текст? Моя рекомендация:
   оставить sole с пометкой (п.21; помеченное ≠ молчаливое).
2. Текст правки контракта «один путь» для D0 (см. шаг 1).

## ПРАВИЛА (выжимка; полные — AGENTS.md, ORCHESTRATION_CURSOR §9)

- Русский; работа ТОЛЬКО на окне `ssh -i ~/.ssh/id_ed25519_deploy -p 2202
  root@gpu-erw.timpul.pro`; база `psql "host=127.0.0.1 port=7890 user=postgres
  dbname=postgres"`.
- Исполнители — ТОЛЬКО cursor-agent промт-файлами (`.claude/state/prompt-<id>.md`):
  `timeout 1800 cursor-agent -p "$(cat …)" --force --model auto --output-format
  stream-json | tee .claude/state/cursor-run-<id>.log`, фоновым Bash. 1 задача=1
  процесс; не коммитят; приёмка только моими замерами. Красная ×3, круги до ACCEPT ×3.
- git: add/commit РАЗНЫМИ вызовами; пат-спек; «Числа:/Доки:»; CHANGELOG якорем
  ЗАГОЛОВКА с возвратом заголовка в new_string (HOW_NOT_TO §1.71); номер grep'ом
  (сосед гоняет номера).
- Гейты: check-prompt-rules (инструкции позитивно, в DEFAULTS); check-golden (держит
  выкат при чужих py≠HEAD в ubuntu/ — дозор); check-active-size 300; снайпер.
- Соседняя линия (пайплайн PLAN_DICT_AUTOMATION) — её файлы не трогать; шаг 7 (живость
  при генерации) — её территория, наш D3 кормит.
- Python — heredoc-файлами; никаких pkill/pgrep -f; ssh — одной сессией с heredoc.

## МЕХАНИКА ВЕБ-КОНТУРА (из continuation-web-20260915, действует)

| Что | Где/как |
|---|---|
| фронт | Open WebUI → 10.3.1.11:18801 |
| шлюз | OpenClaw --profile web, юнит undebot `openclaw-gateway-web`, `/home/undebot/.openclaw-web/` |
| сессии | `.openclaw-web/agents/main/sessions/*.jsonl` |
| журнал плагина | `.openclaw-web/logs/gateway.log`, grep braine-verify |
| мост | MCP :6016 (Bearer MCP_TOKEN из /etc/1c-mcp-ask-postgres.env), код /opt/openclaw-mcp/ |
| ask-сервис | :8091, юнит 1c-serene-ask@postgres; выкат scp ubuntu/serenedb/ask/*.py + рестарт |
| выкат плагина | npm pack → scp → `su - undebot -c "openclaw plugins install npm-pack:<tgz> --force --profile web"` (+без --profile) → рестарт gateway-web |
| живой вызов шлюза | POST :18801/v1/chat/completions, Bearer gateway.auth.token |
| verify-плагин | ubuntu/openclaw/verify-plugin/, замки `node test-verify.mjs` (138) |

## СЛЕДУЮЩИЙ ШАГ

**17.09 ночь: D0/D1/D2 ПРИНЯТЫ И ЗАКОММИЧЕНЫ; D4 (ask-сторона) В РАБОТЕ.**
- Коммиты: 6b7148a (D0+ресёрч+дизайн v18 канон), b0fa5f1 (D1: guard, замок 94/94,
  красная ×3 круг 10), ba48cd9 (D2: гомоним+kind, замок 42/42, красная ×3 круг 5).
- В работе: D4 ч.1 (ask) — дайджесты hint+label-чисто, kind-prior
  (accumulation>document), LLM-арбитр llm_option_highlight (★ в hint, ≤800 мс),
  short-circuit клика; исполнитель cursor-run-d4.log, промт prompt-d4.md.
  Затем красная ×3 (prompt-redd4.md — сделать по образцу redd2), затем D4 ч.2
  (мост mcp_ask: presentation label+hint, hint в measure-OPTIONS, TG-хвост «ещё N»;
  verify-подволна node test-verify) — своя красная.
- Затем D3 (ask_journal: measure_degenerate JSON + decision_id VARCHAR + миграция),
  E (замеры: все замки, grep п.0, предзамер GO/NO-GO по §4), F (выкат scp
  ask/*.py + мост + миграция → рестарт → живые пробы §4 → доки → доклад).
- Правила те же (§ПРАВИЛА ниже); git: add/commit РАЗДЕЛЬНО (индекс иногда
  отстаёт — проверять git show :файл), пат-спек, «Числа:/Доки:», гейт
  check-prompt-rules ловит долженствования в комментариях — писать описательно.
- Открытые долги владельцу: хук check-prompt-rules сканирует весь файл при
  StrReplace (ложные блоки z20/z21 — правки шли через apply-скрипты d1fix*/d2fix*/);
  search_dict_syn отсутствует по конфигу; в CHANGELOG вшиты маркеры конфликта
  (коммит 6d4b955 соседа).
