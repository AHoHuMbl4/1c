# Аудит T1-F2 (волна F): узкая сверка дельты v4 — вектора + свежий взгляд

Дата: 2026-09-02. Контекст нулевой. Серверы не трогались.
Чужие файлы не правились (кроме этого отчёта).

Материал: `docs/audit/TAKT_SPEED_PLAN_2026-09-02.md` (v4);
`.claude/state/result-t1e2.md`, `result-t1e4.md`;
`ubuntu/serenedb/{corpus_merge.sql,corpus_build.sql,build.sh}`.

Линза: **только** дельта v4 по пунктам задания (E2 §3.5, E4-Д1 §2.1,
§3.4, приёмка §3.3–3.8).

---

## 1. §3.5 — противоречие критериев (E2)

**Было (E2):** v3 держал `векторов_умрёт=0` + anti-join «ключ бэкапа ↔ корпус»
как зелёные критерии; при законном дрейфе (выбытие ключей / смена
`content_hash`) зелёный путь недостижим или провоцирует ослабление гейта.

**Стало (v4 §3.5):**
- (а) `векторов_умрёт` — **отчётная** величина merge-фазы, ожидание ≈647 /
  класс законного дрейфа, **не** критерий зелёного;
- (б) дырка = ключ бэкапа §3.3, **присутствующий** в финальном
  `search_corpus`, с `emb IS NULL` → **0**; чтение «только ключ» запрещено;
- (в) `count(emb) ≥ 1 657 724` — пол;
- (г) departed (бэкап ∩ нет в корпусе) — отдельный отчёт, ≈647, сверх — разбор;
  гварды `ent_guard`/STOP ограничивают сами.

### Согласование с merge + шагом 5

Код (`corpus_merge.sql:850-890`, `build.sh:451-499`):

| Дрейф (§3.4) | Механика | После полного такта |
|---|---|---|
| ~647 departed (row_key ушёл) | `unmatched` → DELETE из корпуса; в `vec_budget` как `unmatched_kill` / вклад в `векторов_умрёт` | ключа нет → **не дырка** по (б); уходит в (г) |
| ~554 acc, живой ключ, новый `content_hash` | MATCHED → `emb = NULL` (если не `bmap_common_eq` / не карта xfer) → шаг 5 `embed_bulk` | при полном шаге 5: `осталось=0` (`build.sh:498-499`) → дырок по (б) **0** |
| шаг 5 оборвался | `fail "embed_bulk…"` / `осталось ≠ 0` | такт **красный**; NULL на живых ключах **видны** приёмке (б) |

Продуктовый гейт `vector_loss_gate` (`:688-714`) по-прежнему стопит при доле
> `MERGE_VECTOR_LOSS_TOLERANCE` (0.5%), не при абсолютном нуле. Ожидаемый
бюджет ~647…~1201 из ~1.66M ≪ 0.5% — merge проходит **без**
`MERGE_VECTOR_LOSS_BYPASS` (запрещён §8). Давления обойти гейт нет.

Контракт §0.3 («0 дырок на живых ключах») совпадает с (б).

**Противоречие «зелёный недостижим при законном дрейфе» — ЗАКРЫТО**
(для критериев приёмки §3.5).

**Остаток (не молчаливая потеря emb):** 637 doc-departed ≈ 0.85% от «было»
сущности > порога 0.1% `ent_guard` (`:364-372`), если кейс не уйдёт в
`key_collapse` / `deleted_delta`. Тогда №1 стопнется гвардом (fail-closed),
а не пройдёт зелёным с дырками. Это риск **достижимости** зелёного при
классификации гварда, не путь «зелёный + emb потерян молча».

---

## 2. §2.1 (E4-Д1) — перепись dup-блока

**Было:** два прочтения — (1) переписать `:1524-1534` под ветки V5/mono;
(2) оставить полный DELETE-каскад + отдельно наполнить tail (ломает V5).

**Стало (v4 §2.1):** явная спецификация:
- порядок гейтов: (в) неполный order → **mono**; иначе collision set; (б)
  oversize → **mono** + `pdoc_tail_oversize`; иначе (а) **V5**;
- **mono** = нынешний полный каскад DELETE order/chunks/formula/entity → `p_doc`;
- **V5** = сущность **ОСТАЁТСЯ** в chunks/formula/entity; из order — **ТОЛЬКО**
  `keyvals ∈` коллизионному множеству; `tmp3_pdoc_tail`;
- гейт покрытия: `collision_rows` до DELETE; форма
  `count(order)+count(DISTINCT tail)` запрещена.

Два прочтения E4-Д1 **устранены**. Порядок гейтов (мелкий пункт E4) тоже
зафиксирован.

**Вердикт: ЗАКРЫТО.**

---

## 3. §3.4 — эталон

v4:
- эталон после finalize: doc **74 982 / 57ad85de…**, acc **78 733 / bf9de8a6…**;
- обоснование: **два** независимых замера (20:04 и 20:26:30 UTC), md5
  совпали; монолит завершён 19:35:13, merge умер позже, `tmp3_corpus` не
  трогали;
- без формулы «стейдж = монолит»: сказано, что вывод **после внутреннего
  QUALIFY** (`:1815-1817`) равен витрине **как факт данных** этой сущности
  (fold не сокращает), не как отмена QUALIFY; контроль count витрины
  74 982 до/после;
- дрейф «до» измерен и согласован с §3.5 (647 / 554);
- mismatch → merge не зовём; multiset без emb.

Логика самосогласована: эталон — финальный корпус сборки, не сырой stage;
QUALIFY не «выключен», а измерен на данных; барьер №0 fail-closed.

**Вердикт: логически чисто — ДА.**

---

## 4. Приёмка §3.3–3.8 целиком (путь «зелёный, вектор потерян молча»)

Прогон как исполнитель:

| Шаг | Защита | Тихий emb-NULL? |
|---|---|---|
| 3.3 бэкап | снимок `emb IS NOT NULL`, count ≥ baseline | нет (предпосылка) |
| 3.4 №0 | multiset vs эталон; mismatch → merge нет | нет |
| 3.5 (б) | живой ключ бэкапа + `emb IS NULL` → 0 | **главная дыра E4-П1 закрыта** |
| 3.5 (а)(г) | бюджет/departed — отчёт, не маска | нет |
| 3.5 (в) | пол count(emb) — smoke | один не закрывает, но (б) закрывает |
| 3.5 шаги 2–8 | step 5: `осталось=0` или fail | обрыв → красный |
| 3.5 DELETE→xfer | принятый риск + запрет kill + откат бэкапом | не «успешный зелёный» |
| 3.6–3.7 | таймеры/apply только после №2 | не маскируют |
| 3.8 | сброс progress/stage/tail/_done после сорванного №0 | fail-closed |

**Путь E4-П1** (ключ жив, emb мёртв, anti-join только по ключу, пол
закрыт чужими эмбеддингами) — **закрыт** формулировкой (б) + запретом
чтения «только ключ».

Остаточные (не блокеры дельты v4 / не «молча при зелёном»):
- multiset жёсткий только на двух сущностях (E4-П2) — порча вне scope при
  стопнутом apply маловероятна; (б) всё равно ловит emb-NULL на любом
  ключе из бэкапа, если он остался в корпусе;
- строки, которых не было в бэкапе (`emb` уже был NULL) — вне контракта
  «вектора» (E4-П3);
- окно mid-merge — признано, не выдаётся за зелёный конец такта.

**Путь «зелёный, а вектор потерян молча» по дельте v4 — НЕ вижу.**

---

## Сводка

| Пункт | Вердикт |
|---|---|
| 1. §3.5 / E2 противоречие критериев | **ЗАКРЫТО** (согласовано с merge+step 5) |
| 2. §2.1 / E4-Д1 два прочтения | **ЗАКРЫТО** |
| 3. §3.4 эталон + QUALIFY-факт | **чисто** |
| 4. Тихий emb-NULL при зелёном §3.3–3.8 | **нет** |

**Вектора защищены — ДА**

**План готов — ДА**

(Адресные «НЕТ» круга 3 по E2/E4 в зоне этой линзы закрыты дельтой v4.
Остаток ent_guard vs ожидаемые 637 departed — про достижимость зелёного
при классификации гварда, fail-closed для emb.)

*Конец T1-F2.*
