[Service]
# После каждого старта/рестарта движка — идемпотентно пересоздать персистентные секреты.
# Префикс «-» — сбой не роняет serenedb (секреты можно поднять руками тем же юнитом).
ExecStartPost=-/bin/systemctl start 1c-serene-embed-secrets.service
