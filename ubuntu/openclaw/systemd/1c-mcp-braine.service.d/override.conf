# Переключение «второго мозга» с braine (chunk-RAG) на поиск в SereneDB.
# Контракт /ask одинаковый, поэтому меняется только адрес.
# ОТКАТ: удалить этот файл, systemctl daemon-reload, restart 1c-mcp-braine.
[Service]
Environment=BRAINE_URL=http://127.0.0.1:8091
