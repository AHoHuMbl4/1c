[Service]
# Ключ эмбеддера для смыслового поиска по вики. Отдельный файл на профиль (600):
# у второго экземпляра своё всё, включая секреты. Штатный установщик юнита
# `EnvironmentFile` не прописывает, поэтому drop-in — он переживёт переустановку юнита.
EnvironmentFile=-/home/undebot/.openclaw-buh/gateway.systemd.env
